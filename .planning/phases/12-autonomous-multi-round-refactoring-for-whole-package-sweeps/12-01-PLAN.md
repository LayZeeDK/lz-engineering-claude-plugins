---
phase: 12-autonomous-multi-round-refactoring-for-whole-package-sweeps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/evals/trigger-eval.json
  - .claude/skills/lz-refactor-workspace/evals/d07-chunks/negatives.json
  - .claude/skills/lz-refactor-workspace/e2e-nx/suite.json
  - .claude/skills/lz-refactor-workspace/e2e-nx/targets.json
  - .claude/skills/lz-refactor-workspace/e2e-nx/prompts/p7cmd-runtime-lint-utils-sweep-command.md
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/suite.json
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/targets.json
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/gr3cmd-update-quality-sweep-command.md
autonomous: true
requirements: [SC1, SC2, SC4]
user_setup: []

must_haves:
  truths:
    - "**D-12:** trigger-eval.json gains exactly 3 sweep-shaped POSITIVES (should_trigger:true) and exactly 3 sweep-shaped near-miss NEGATIVES (should_trigger:false); the 3 negatives are dual-written byte-consistent into evals/d07-chunks/negatives.json (the file the spec runner actually reads); check-evals.mjs exits 0 (>= 8 positives, >= 8 negatives, >= 2 seam negatives, ASCII-only)."
    - "**D-03:** each new positive names a PACKAGE / DIRECTORY / CODEBASE scope (not one symbol) framed as scan-and-fix of existing green code, and is PAIRED with a sweep-shaped hard negative on a neighboring category (feature-adding sweep, performance sweep, red-test sweep); the specificity guarantee lives in the eval set, not the prose."
    - "**D-13:** each e2e suite gains exactly ONE multi-round sweep-COMMAND scenario (prompt file + targets.json row + suite.json prompts[] row): nx sweeps runtime-lint-utils.ts (T3 loop->pipeline + T4 groupImports reduce->Map = 2+ safe rounds); kata sweeps updateQuality in app/gilded-rose.ts (guard clauses -> per-branch updater -> name magic numbers = 3+ safe rounds); each scenario carries N safe behavior-preserving items PLUS exactly ONE seeded should-pause TRAP (nx = an exported-signature / public-API break; kata = a behavior change such as Conjured/Sulfuras the golden master does not cover)."
    - "**D-14:** each seeded trap is documented in its targets.json row judgment so posture reads deterministically from meta.json + diff.patch + transcript (NOT used_refactor): the expected pass shape is fired + rounds_completed >= 2 + tests_green_each_round + behavior_preserved (original unmodified tests against the edited source) + scope_contained (changed files subset of the target file(s); no test-file or unrelated-file edits)."
    - "**D-15:** the ONE-trap-per-scenario design makes AUTONOMY-PRECISION measurable: a pass drives all N_safe rounds without asking, pauses at most once, and lands ONLY on the seeded should-pause item with behavior preserved; over-pausing => posture B (timid), driving through the seeded risk => posture C (reckless)."
    - "**D-16:** the added positives + sweep scenarios are the instruments the before/after protocol runs against; they are COMMITTED as their own commit (Task 3) BEFORE the Plan-02 SKILL.md edit so the pre-edit baseline tree (the commit before the 12-02 SKILL.md commit) contains OLD SKILL.md + these NEW instruments, letting a baseline be captured against the current shipped skill (expect ~0 sweep trigger + stop-and-ask drive)."
    - "SC4: both gaps are made measurable in BOTH suites -- the sweep positives measure Gap-1 recall and the sweep-command scenarios measure Gap-2 multi-round drive."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/evals/trigger-eval.json"
      provides: "3 new sweep positives + 3 new sweep negatives on the existing flat {query, should_trigger} shape"
      contains: "should_trigger"
    - path: ".claude/skills/lz-refactor-workspace/evals/d07-chunks/negatives.json"
      provides: "the same 3 sweep negatives, byte-consistent with trigger-eval.json negatives"
      contains: "should_trigger"
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/prompts/p7cmd-runtime-lint-utils-sweep-command.md"
      provides: "nx sweep-command prompt (whole-file sweep of runtime-lint-utils.ts)"
      min_lines: 1
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/gr3cmd-update-quality-sweep-command.md"
      provides: "kata sweep-command prompt (updateQuality end-to-end)"
      min_lines: 1
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/evals/trigger-eval.json (negatives)"
      to: ".claude/skills/lz-refactor-workspace/evals/d07-chunks/negatives.json"
      via: "dual-write byte-consistent negatives (spec runner reads d07-chunks/negatives.json; check-evals lints trigger-eval.json)"
      pattern: "should_trigger.*false"
    - from: ".claude/skills/lz-refactor-workspace/e2e-nx/suite.json (prompts[] row)"
      to: ".claude/skills/lz-refactor-workspace/e2e-nx/prompts/p7cmd-runtime-lint-utils-sweep-command.md + targets.json row"
      via: "suite row references the prompt file and a targets id; run-e2e.mjs discovers the scenario"
      pattern: "p7cmd"
---

<objective>
Build the measurement instruments for both Phase-12 gaps WITHOUT touching the shipped skill and WITHOUT any metered claude -p spend: add 3 sweep-shaped trigger positives + 3 sweep-shaped hard negatives (dual-written) to the native trigger-eval set, and add ONE multi-round sweep-COMMAND scenario to each e2e suite, each seeded with N safe rounds + exactly ONE should-pause trap.

Purpose: These instruments must be COMMITTED (Task 3, as their own commit) BEFORE the SKILL.md edit so the pre-edit baseline tree (the commit before 12-02's SKILL.md commit) carries the NEW instruments alongside the OLD SKILL.md, letting a baseline be captured against the CURRENT shipped skill (D-16). The specificity guarantee for the broadened description lives in these negatives, not in the prose (D-03).
Output: Edited evals/trigger-eval.json + evals/d07-chunks/negatives.json (still check-evals-green); two new e2e sweep scenarios (prompt + targets row + suite row) in e2e-nx and e2e-gilded-rose. No SKILL.md change; no metered run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-CONTEXT.md
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-RESEARCH.md
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-PATTERNS.md

Analog fixtures (read the current form before extending; the workspace is gitignored, use Read/rg not git grep):
@.claude/skills/lz-refactor-workspace/evals/trigger-eval.json
@.claude/skills/lz-refactor-workspace/evals/d07-chunks/negatives.json
@.claude/skills/lz-refactor-workspace/check-evals.mjs
@.claude/skills/lz-refactor-workspace/e2e-nx/suite.json
@.claude/skills/lz-refactor-workspace/e2e-nx/targets.json
@.claude/skills/lz-refactor-workspace/e2e-nx/prompts/p2cmd-validate-entry-command.md
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/suite.json
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/targets.json
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/gr1cmd-update-quality-command.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 sweep positives + 3 sweep negatives (dual-written) to the trigger-eval set</name>
  <read_first>
    - .claude/skills/lz-refactor-workspace/evals/trigger-eval.json (existing 13 positives lines 2-14, 11 negatives lines 16-26; flat array of {query, should_trigger})
    - .claude/skills/lz-refactor-workspace/evals/d07-chunks/negatives.json (the 11 negatives the spec runner reads; pretty-printed one-field-per-line; currently byte-identical query strings to trigger-eval.json negatives)
    - .claude/skills/lz-refactor-workspace/check-evals.mjs (gates: >= 8 positives, >= 8 negatives, >= 2 seam negatives via the regex on lines 71-72, ASCII-only)
    - 12-PATTERNS.md sections "trigger-eval.json" and "d07-chunks/negatives.json" (entry shapes, dual-write invariant, seam-lint constraint) and 12-RESEARCH.md Pattern 1 + Pitfalls 1/2
  </read_first>
  <action>
    Append 3 sweep-shaped POSITIVES to the positives block of evals/trigger-eval.json, in the existing natural lowercase first-person developer voice with a green-state cue (tests pass / green bar). Address the missing intent CATEGORY per D-02: "whole-package / codebase / multi-file scan-and-fix of existing green code" -- each positive names a PACKAGE, DIRECTORY, MODULE, or CODEBASE scope (not one function/class/symbol) and asks to both FIND smells and FIX/clean/sweep them. Address the category, not the e2e prompt phrasing (Pitfall 1 / anti-overfit); do NOT paste the p7cmd/gr3cmd prompt keywords into any query.
    Append 3 sweep-shaped near-miss NEGATIVES (should_trigger:false) guarding the three neighboring boundaries per D-03/D-12: (1) a feature-adding sweep across a package/module (feature work, not refactor); (2) a performance sweep of a whole module/package (speed rewrite, not structure); (3) a red-test sweep across a package (the lz-tpp green step). The red-test sweep negative MUST match the check-evals seam regex (failing test | make .* pass | minimal transformation | go green | green it | smallest edit|step) so the >= 2-seam gate stays satisfied. All queries ASCII-only (straight quotes, arrow written as -> not the Unicode glyph).
    Dual-write the SAME 3 negatives into evals/d07-chunks/negatives.json using its pretty-printed one-field-per-line object shape, with byte-consistent query strings (the dual-write trap, Pitfall 2). Do NOT add positives to negatives.json; it holds negatives only.
  </action>
  <acceptance_criteria>
    - check-evals.mjs exits 0 and reports 16 positives / 14 negatives with >= 2 lz-tpp-seam negatives, ASCII-clean.
    - trigger-eval.json has exactly 3 new positives (all naming a package/dir/module/codebase scope, all with a green-state cue) and exactly 3 new negatives (feature-sweep, perf-sweep, red-test-sweep).
    - The negatives lists in trigger-eval.json and evals/d07-chunks/negatives.json are byte-consistent (same query strings, same order for the shared set).
    - No query quotes or paraphrases the p7cmd/gr3cmd e2e prompt wording (anti-overfit).
    - Both JSON files parse as valid JSON.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node check-evals.mjs</automated>
  </verify>
  <done>check-evals.mjs is green with the new sweep positives and dual-written sweep negatives; the two negative lists are byte-consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Add ONE multi-round sweep-COMMAND scenario per e2e suite (nx + kata)</name>
  <read_first>
    - .claude/skills/lz-refactor-workspace/e2e-nx/prompts/p2cmd-validate-entry-command.md (the imperative COMMAND framing that drives apply mode) and e2e-nx/targets.json T3 (findTransitiveExternalDependencies, loop->pipeline) + T4 (groupImports, reduce->Map) + e2e-nx/suite.json prompts[] row shape (id/file/target/code)
    - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/gr1cmd-update-quality-command.md + e2e-gilded-rose/targets.json G1 (updateQuality) + e2e-gilded-rose/suite.json prompts[] row shape
    - 12-PATTERNS.md sections "e2e-nx/prompts/<new sweep-cmd>.md" and "e2e-gilded-rose/prompts/<new sweep-cmd>.md" (prompt/targets/suite triplet; the seeded-trap requirement) and 12-CONTEXT.md D-13/D-14/D-15
  </read_first>
  <action>
    nx suite: create e2e-nx/prompts/p7cmd-runtime-lint-utils-sweep-command.md in the same imperative COMMAND voice as p2cmd but broaden the SCOPE from one symbol to the whole file -- sweep packages/eslint-plugin/src/utils/runtime-lint-utils.ts, state the tests are green, ask to go through the file and refactor the smells away then run the eslint-plugin suite. Add ONE targets.json row (id p7sweep) describing the file-scope sweep: N safe rounds = T3 (Replace Loop with Pipeline / functional filter-flatMap) + T4 (Map group-by) and exactly ONE seeded should-pause TRAP = an exported-signature / public-API change on a symbol in that file (the D-07.6 blast-radius-escapes-scope / Pitfall 8 monorepo case); record in its judgment that the correct posture PAUSES and asks on the exported-signature change and drives the 2 safe rounds. Add the suite.json prompts[] row with id p7cmd, file p7cmd-runtime-lint-utils-sweep-command.md, target p7sweep, code true; leave skillCommand / applyBase / protectedBranches untouched.
    kata suite: create e2e-gilded-rose/prompts/gr3cmd-update-quality-sweep-command.md in the gr1cmd COMMAND voice but sweep updateQuality end-to-end (guard clauses -> per-branch updater -> name the magic numbers = 3+ safe rounds), tests green, then run the tests. Add ONE targets.json row (id G1sweep) reusing G1 file/symbol but recording N safe rounds PLUS exactly ONE seeded should-pause TRAP = a behavior change the golden master does NOT cover (Conjured handling or Sulfuras degradation); record in its judgment that behavior_preserved is checked by running the ORIGINAL unmodified approval/golden-master tests against the EDITED source, and the correct posture PAUSES on the uncovered behavior change (D-07.5 ambiguous behavior). Add the suite.json prompts[] row with id gr3cmd, file gr3cmd-update-quality-sweep-command.md, target G1sweep, code true.
    All new files ASCII-only (straight quotes, arrow written as ->, no em-dashes/emojis). Do NOT edit run-e2e.mjs or any harness code (D-05/D-12, read-only harness contract).
  </action>
  <acceptance_criteria>
    - Both new prompt files exist under their suite prompts/ dir and are ASCII-only.
    - e2e-nx/targets.json and e2e-nx/suite.json parse as JSON; the new p7cmd suite row file exists on disk and its target (p7sweep) resolves to a targets.json row.
    - e2e-gilded-rose/targets.json and e2e-gilded-rose/suite.json parse as JSON; the new gr3cmd suite row file exists on disk and its target (G1sweep) resolves to a targets.json row.
    - Each new targets row documents exactly N safe rounds (nx: 2 via T3/T4; kata: 3+) and exactly ONE seeded should-pause trap (nx: exported-signature/public-API break; kata: uncovered behavior change) in its judgment, so posture A/B/C reads from transcript + diff.
    - No harness .mjs file is modified (git status shows only the JSON + prompt additions under the two suite dirs; note: the workspace is gitignored, so confirm by directory listing of changed files).
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node scripts-check-e2e-suites.mjs</automated>
    <note>Write a throwaway ESM checker (scripts-check-e2e-suites.mjs, delete after) that for each of e2e-nx and e2e-gilded-rose reads suite.json + targets.json, asserts both parse, and for every prompts[] row asserts the prompt file exists under prompts/ and its target id is present in targets.json; exits non-zero on any miss. Avoid inline node -e per the shell rules.</note>
  </verify>
  <done>Both suites discover their new sweep-command scenario (prompt + resolvable target + suite row); the seeded trap is recorded in each new targets row.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the 12-01 instruments as their own commit (before the SKILL.md edit)</name>
  <read_first>
    - The files edited/added in Tasks 1-2: evals/trigger-eval.json, evals/d07-chunks/negatives.json, e2e-nx/{suite.json,targets.json,prompts/p7cmd-runtime-lint-utils-sweep-command.md}, e2e-gilded-rose/{suite.json,targets.json,prompts/gr3cmd-update-quality-sweep-command.md} (all git-tracked; the workspace instrument files are NOT gitignored)
    - AGENTS.md public-repo hygiene (maintainer identity / allowlist-inversion) + CLAUDE.md ASCII-only committed output
  </read_first>
  <action>
    Commit ONLY the 12-01 instruments (the two eval JSONs + the two e2e triplets: suite.json + targets.json + the new prompt file per suite) as a single dedicated commit, staged by explicit path (never git add . / -A / -u). Do NOT stage or touch plugins/lz-tdd/skills/lz-refactor/SKILL.md -- the SKILL.md edit is Plan-02's own separate commit, so the commit created here is the pre-edit baseline tree D-16 checks out. BEFORE committing run the two hygiene gates on the staged content: (1) ASCII-only (straight quotes, arrow written as ->, no em-dashes/emojis) and (2) email allowlist-inversion (assert the only email-shaped token present is the approved public gmail larsbrinknielsen@gmail.com; flag anything else; never write the maintainer work email/domain even as a search needle). Commit message ASCII + PII-clean, no AI-attribution trailer. Confirm the maintainer committer/author email is the approved public gmail before committing.
  </action>
  <acceptance_criteria>
    - Exactly the 12-01 instruments are committed (two eval JSONs + two e2e triplets); SKILL.md is NOT in this commit (git show --stat lists no plugins/ path).
    - ASCII-only + email allowlist-inversion scan clean on the staged content; commit message ASCII + PII-clean, no AI-attribution.
    - HEAD after this commit is the pre-edit baseline tree: it contains the NEW instruments AND the OLD (pre-Plan-02) SKILL.md.
  </acceptance_criteria>
  <verify>
    <automated>git show --stat HEAD</automated>
    <note>Confirm the commit contains only the workspace instrument paths and NO plugins/ path. Run the email allowlist-inversion scan on the staged diff before committing (only the approved public gmail may appear).</note>
  </verify>
  <done>The 12-01 instruments are committed as their own pre-edit commit (no SKILL.md), giving D-16 a clean pre-edit baseline tree = OLD SKILL.md + NEW instruments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-authored eval JSON -> metered eval subprocess | The trigger positives/negatives and e2e prompts drive claude -p subprocesses at run time; malformed or overfit fixtures produce false measurement, not a runtime exploit |
| eval fixtures -> public repo surface | These files live under .claude/skills/lz-refactor-workspace/ (gitignored raw outputs, git-tracked record) but any committed prose is public and must be ASCII + PII-clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering (measurement integrity) | trigger-eval.json specificity guard | mitigate | Every new sweep positive is paired with a sweep-shaped hard negative on the neighboring category (feature/perf/red-test); the red-test negative matches the seam regex so the >= 2-seam lint holds; check-evals.mjs gates the split before any run (D-03/D-12) |
| T-12-03 | Information disclosure (public-repo PII/hygiene) | new eval queries + e2e prompt files | mitigate | ASCII-only (check-evals gate 4 for trigger JSON; manual ASCII read for prompts); email allowlist-inversion scan before commit -- only the approved public gmail may appear, never the maintainer work email/domain |
| T-12-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package install occurs in this phase (Package Legitimacy Audit N/A per 12-RESEARCH.md); nothing to verify |
</threat_model>

<verification>
- `node check-evals.mjs` (from the workspace dir) exits 0: 16 positives / 14 negatives, >= 2 seam, ASCII-clean.
- The two negative lists (trigger-eval.json vs d07-chunks/negatives.json) are byte-consistent.
- Both e2e suites parse and each new sweep scenario resolves (prompt file present + target id present in targets.json).
- No harness .mjs modified; no SKILL.md change; no metered claude -p executed.
- The 12-01 instruments are committed as their own commit (Task 3) with NO plugins/ path, forming D-16's pre-edit baseline tree (OLD SKILL.md + NEW instruments).
- ASCII + email allowlist-inversion scan clean on all edited/added/committed files.
</verification>

<success_criteria>
The native trigger-eval set and both e2e suites carry sweep-shaped instruments (recall positives + specificity negatives + one multi-round sweep-command scenario per suite with a seeded trap) that make Gap-1 (trigger) and Gap-2 (drive) measurable before/after, with check-evals green and no shipped-skill or harness code touched.
</success_criteria>

<output>
Create `.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-01-SUMMARY.md` when done.
</output>
