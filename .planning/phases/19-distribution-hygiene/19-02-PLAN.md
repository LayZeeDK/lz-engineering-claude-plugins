---
phase: 19-distribution-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/lz-tdd@0.0.2-phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-SECURITY.md
autonomous: true
requirements: [DST-03]

must_haves:
  truths:
    - "The T-06-01 threat-register mitigation cell in the archived 06-SECURITY.md no longer proves work-email absence by embedding the maintainer work-domain as a search needle inside a shell command; the VERIFIED evidence is rewritten to a NON-ENCODING allowlist-inversion statement -- it asserts that enumerating every email-shaped token over the created tree and subtracting the approved public gmail (larsbrinknielsen@gmail.com) leaves an empty remainder, and it never spells the forbidden value in any form"
    - "The sibling T-06-03 ASCII mitigation cell is UNCHANGED -- its non-ASCII byte-class needle encodes a generic character class (not the forbidden value), which is the correct non-encoding form and must be left as-is; the contrast between the two cells is the exact tell the sweep uses"
    - "Every other tracked planning doc that carries the SAME encode-the-forbidden-value anti-pattern (surfaced by an ephemeral, never-committed grep the executor holds only on the in-session command line) is rewritten to the same non-encoding allowlist-inversion form; the archived Phase-10 distribution-hygiene plan / security docs are known likely instances the sweep must check and fix"
    - "The forbidden work-email and its bare domain are NOT written into any committed file -- not the rewritten 06-SECURITY.md, not any swept doc, not this plan's summary; the executor holds the needle only ephemerally to grep, and verifies removal by asserting only the approved public gmail is present (allowlist-inversion), never by grepping FOR the forbidden value"
    - "The diff is docs-only under .planning/ -- no plugins/lz-tdd SKILL.md or shipped tree file is touched (so no skill-review and no /reload-plugins are needed, D-10); there is NO shared-history rewrite of main (the operator chose forward-fix the tip only, D-12), and the commit lands before the 0.0.3 push / PR to main"
  artifacts:
    - path: ".planning/milestones/lz-tdd@0.0.2-phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-SECURITY.md"
      provides: "the T-06-01 mitigation cell rewritten to a non-encoding allowlist-inversion form (the GA-7 forward-fix)"
      contains: "larsbrinknielsen@gmail.com"
  key_links:
    - from: ".planning/milestones/lz-tdd@0.0.2-phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-SECURITY.md"
      to: ".claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs"
      via: "the rewrite mirrors the checker's allowlist-inversion logic in prose -- assert only the approved gmail is present, flag the remainder; never encode the forbidden value"
      pattern: "larsbrinknielsen@gmail.com"
  decisions: |
    - **D-12:** forward-fix the tip -- a NEW commit rewrites the archived 06-SECURITY.md T-06-01 needle into a non-encoding allowlist-inversion form (assert only the approved gmail is present; never spell the forbidden value), and sweeps all tracked planning docs for the same anti-pattern and fixes each; NO shared-history rewrite of main (rejected as disproportionate -- rewrites public main + re-points the released 0.0.2 tag; the exposure is a bare domain in an archived planning meta-doc, and bare-domain-in-history detection is out of scope for the automated detector by design); this runs as its OWN scoped docs-only commit, landed before the 0.0.3 push / PR to main; it does NOT expand the DST-01/02/03 authoring scope
---

<objective>
Land the GA-7 public-repo git-history hygiene forward-fix (D-12) as its own scoped docs-only commit,
separate from the three-skill content edits (19-01) and the finalize gate (19-03). The archived
06-SECURITY.md T-06-01 mitigation cell currently proves work-email absence by embedding the maintainer
work-DOMAIN as a search needle inside a shell command -- which IS the leak (AGENTS.md: "the needle is
itself a leak"). Rewrite that evidence to a NON-ENCODING allowlist-inversion statement, sweep the tracked
planning docs for sibling instances of the same anti-pattern, and fix each -- without ever writing the
forbidden value into any committed file.

Purpose: the .planning/ tree is published to the public repo, so an encoded work-domain needle in a
committed meta-doc is a live information-disclosure anti-pattern. The operator decided forward-fix (no
history rewrite of main) because the exposure is a bare domain (no local part) in an archived planning
meta-doc -- not the shipped plugin tree -- and rewriting public main would re-point the released 0.0.2 tag.
Output: an anti-pattern-free 06-SECURITY.md (plus any swept siblings), verified clean by allowlist-inversion.

## Decisions implemented (D-NN)

- **D-12:** forward-fix the tip only. Rewrite the 06-SECURITY.md T-06-01 needle to the non-encoding allowlist-inversion form; sweep tracked planning docs and fix each sibling; the fix must not spell the forbidden value; NO shared-history rewrite of main; own scoped docs-only commit before the 0.0.3 push / PR. Refs: memories archived-06-security-workdomain-needle-unremediated, public-repo-work-email-allowlist, lz-plugins-phase1-workemail-git-history-exposure.

## Artifacts produced

- 06-SECURITY.md: the T-06-01 mitigation cell rewritten from an encoded-domain-needle shell command to an allowlist-inversion statement (assert only the approved gmail present, remainder empty).
- Any sibling tracked planning docs the sweep surfaces, each rewritten to the same non-encoding form (recorded in the SUMMARY by path).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-distribution-hygiene/19-CONTEXT.md
@.planning/phases/19-distribution-hygiene/19-RESEARCH.md
@.planning/phases/19-distribution-hygiene/19-PATTERNS.md

The GA-7 mechanics are in 19-RESEARCH.md Finding 8 (the T-06-01 encoded-needle anti-pattern vs the
legitimate T-06-03 byte-class needle; the non-encoding rewrite pattern; the ephemeral-grep sweep) and
19-PATTERNS.md "06-SECURITY.md (GA-7 / D-12)" (the structural role-match: the check-hygiene.mjs
allowlist-inversion shape + the AGENTS.md rule; the needle is described structurally and NOT reproduced).
The maintainer-identity rule and the allowlist-inversion detection method are in the project AGENTS.md /
CLAUDE.md. Do NOT read those research/pattern files expecting the needle to be spelled -- it is not, by
design; you reconstruct it only ephemerally on the command line to grep, never committing it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the 06-SECURITY.md T-06-01 needle to a non-encoding allowlist-inversion form + sweep and fix sibling anti-pattern instances (D-12)</name>
  <files>.planning/milestones/lz-tdd@0.0.2-phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-SECURITY.md (+ any tracked planning docs the ephemeral sweep surfaces)</files>
  <read_first>
    - .planning/milestones/lz-tdd@0.0.2-phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-SECURITY.md (the FILE being edited -- locate the T-06-01 threat-register row and its VERIFIED evidence, which embeds the maintainer work-domain as an escaped search needle inside a shell command; and the sibling T-06-03 ASCII row, whose non-ASCII byte-class needle is LEGITIMATE and stays unchanged)
    - .planning/phases/19-distribution-hygiene/19-RESEARCH.md (Finding 8 -- the exact anti-pattern instance described structurally, the non-encoding rewrite pattern, and the ephemeral-grep sweep discipline; the CRITICAL never-write-the-forbidden-value rule)
    - .planning/phases/19-distribution-hygiene/19-PATTERNS.md ("06-SECURITY.md (GA-7 / D-12)" -- the structural role-match against the check-hygiene.mjs allowlist-inversion shape and the AGENTS.md rule; the T-06-01-encodes vs T-06-03-legitimate contrast)
    - AGENTS.md (the maintainer-identity rule + "Detect by ALLOWLIST-INVERSION, never by encoding the forbidden value"; the escaped-form-only-in-private-uncommitted-notes rule)
  </read_first>
  <action>
    Rewrite the VERIFIED evidence of the T-06-01 mitigation cell in 06-SECURITY.md so it no longer contains
    a shell command that greps a literal work-domain needle. Replace it with a NON-ENCODING
    allowlist-inversion STATEMENT that mirrors the check-hygiene.mjs logic in prose: assert that enumerating
    every email-shaped token over the created tree and subtracting the approved public gmail
    (larsbrinknielsen@gmail.com) leaves an empty remainder -- i.e., the only email-shaped token present is
    the approved gmail. Do NOT name, spell, split, or escape the forbidden work-email or its bare domain in
    the rewritten cell or anywhere else in the file. Leave the sibling T-06-03 ASCII cell UNCHANGED -- its
    non-ASCII byte-class needle encodes a generic character class, not the forbidden value, so it is already
    the correct non-encoding form. Then run the sweep (D-12 "fix each"): reconstruct the forbidden needle
    ONLY ephemerally on the in-session command line (never write it into a file, never commit it) and grep
    the tracked planning docs for sibling instances of the SAME encode-the-forbidden-value anti-pattern --
    the archived Phase-10 distribution-hygiene plan and security docs are known likely instances; check them.
    Rewrite each sibling instance to the same non-encoding allowlist-inversion form. Keep every edited file
    ASCII-only. Do NOT touch any plugins/lz-tdd SKILL.md or shipped-tree file; do NOT rewrite shared main
    history. Record, by path, every file the sweep rewrote (for the SUMMARY).
  </action>
  <acceptance_criteria>
    - 06-SECURITY.md is ASCII-only and, after the rewrite, contains the approved public gmail larsbrinknielsen@gmail.com within an allowlist-inversion statement in the T-06-01 evidence (the non-encoding form is present)
    - The T-06-01 VERIFIED evidence no longer contains a shell command whose search needle is a literal work-domain (confirmed by the executor from its own before/after diff -- NOT by grepping FOR the forbidden value)
    - The sibling T-06-03 ASCII byte-class cell is unchanged
    - Full-tree allowlist-inversion is clean: enumerating every email-shaped token across the tracked tree and subtracting the approved public gmail leaves an empty remainder (no non-approved email-shaped token anywhere)
    - The diff is docs-only under .planning/; no plugins/lz-tdd SKILL.md or shipped file was modified; main history was not rewritten
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8"); if(/[^\x00-\x7F]/.test(s))process.exit(1); if(!s.includes("larsbrinknielsen@gmail.com"))process.exit(1);' ".planning/milestones/lz-tdd@0.0.2-phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-SECURITY.md" && test -z "$(rg --hidden --glob '!.git/**' -o -i -N --no-filename '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' . | sort -u | rg -iv 'larsbrinknielsen@gmail\.com')"</automated>
  </verify>
  <done>06-SECURITY.md's T-06-01 evidence is rewritten to a non-encoding allowlist-inversion statement naming only the approved gmail; the legitimate T-06-03 byte-class cell is untouched; sibling anti-pattern instances in tracked planning docs are swept and fixed; the full-tree allowlist-inversion is clean; the diff is docs-only and main history is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The core threat is the phase's load-bearing one: a maintainer work-email / work-domain encoded into a
committed public-repo file. This plan removes an existing instance of that anti-pattern and must not
reintroduce it while doing so. No runtime / network / session / crypto / input surface.

| Boundary | Description |
|----------|-------------|
| archived planning meta-doc -> published public repo | must carry no encoded work-domain needle; only the approved public gmail may appear as an email-shaped token |
| the forbidden needle -> any committed file | FORBIDDEN; the executor holds it only ephemerally on the command line to grep, and verifies removal by allowlist-inversion (never by grepping FOR it) |

## STRIDE Threat Register

ASVS level 1; block on `high`. Neither high-severity threat is left OPEN -- the rewrite closes T-19-EM
and the allowlist-inversion discipline closes T-19-REINTRO.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-EM | Information Disclosure (PII) | the T-06-01 encoded work-domain needle in the archived 06-SECURITY.md + any sibling planning-doc instances | high | mitigate | Rewrite each to a non-encoding allowlist-inversion statement (D-12); verify only the approved public gmail is present across the tracked tree; forward-fix the tip (no main history rewrite) |
| T-19-REINTRO | Information Disclosure (PII) | the fix itself spelling the forbidden value while removing it | high | mitigate | Allowlist-inversion phrasing only; the executor holds the needle only ephemerally on the command line and never commits it; verification asserts the approved gmail is present, never greps FOR the forbidden value |
| T-19-SC | Tampering (supply chain) | npm/pip/cargo package installs | low | accept | No package installs this phase; docs-only diff under .planning/ |
</threat_model>

<verification>
- 06-SECURITY.md is ASCII-only and carries the approved gmail in a non-encoding allowlist-inversion statement; the T-06-01 shell-command-with-a-domain-needle is gone (executor-confirmed from the diff).
- The legitimate T-06-03 non-ASCII byte-class cell is unchanged.
- Sibling anti-pattern instances in tracked planning docs are swept and rewritten to the same form.
- Full-tree allowlist-inversion is clean (only the approved gmail present; remainder empty).
- Docs-only diff under .planning/; no SKILL.md / shipped file touched; no main history rewrite; the commit lands before the 0.0.3 push / PR.
</verification>

<success_criteria>
The GA-7 work-domain encode-the-forbidden-value anti-pattern is removed from 06-SECURITY.md and every
sibling tracked planning doc via a non-encoding allowlist-inversion rewrite, the fix never spells the
forbidden value, the full-tree allowlist-inversion is clean, and the change is a scoped docs-only commit
that leaves main history intact -- landed before the public 0.0.3 push / PR.
</success_criteria>

<output>
Create `.planning/phases/19-distribution-hygiene/19-02-SUMMARY.md` when done, recording every file the
sweep rewrote (by path) and the clean allowlist-inversion result -- without spelling the forbidden value.
</output>
