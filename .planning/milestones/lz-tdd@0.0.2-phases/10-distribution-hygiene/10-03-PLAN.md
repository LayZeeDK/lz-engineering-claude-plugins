---
phase: 10-distribution-hygiene
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/
  - .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md
autonomous: true
requirements: [DST-04]

must_haves:
  truths:
    - "The DST-04 layer-2 clean-room sweep runs the read-only oracle-reviewer subagent over BOTH proven-collision surfaces across ALL leaves, surface-scoped: every '## Intent' line in gof-catalog (23) + extra-patterns-catalog (5) = 28 Intents, and every '## Mechanics' step list in fowler-catalog (62) + kerievsky-catalog (27) = 89 mechanics; the sweep reviews ONLY those surfaces, NOT motivation / examples / Consequences prose (those rest on the layer-3 attestation)"
    - "The MAIN executor context NEVER reads .oracle/; every fresh draft-vs-source comparison is forced through the oracle-reviewer subagent, which reads .oracle/ in its own isolated context and returns pass/revise/blocked verdicts in its own words without echoing source prose"
    - "The sweep is batched by chapter/family (one invocation per batch, ~15-17 total, within the D-03 15-20 envelope): GoF Intents in 3 family batches (Creational 5 / Structural 7 / Behavioral 11), extra-patterns Intents in 1 batch, Fowler mechanics in 7 chapter batches (Ch.6-12), Kerievsky mechanics in 5-6 chapter batches; each invocation passes only the shipped surface for that batch's leaves plus the book + chapter/topic via index.md (chapter filenames are NOT hardcoded -- the reviewer resolves the file)"
    - "The rubric is the DST-04 near-verbatim axis ONLY (a too_close_to_source boolean + a structural reason, no source span echoed), surface-scoped; a revise/blocked verdict on a surface is handled by rewording that surface BLIND (never reading the source) from the reviewer's short original directives and re-gating; round cap is 3 per batch and non-convergence escalates to the owner via AskUserQuestion"
    - "After any reword, node check-hygiene.mjs (hardened axis c) stays GREEN and the leaf-contract checkers (check-gof / check-kerievsky / check-catalog) stay GREEN (no leaf dropped its Intent or Mechanics surface); if a reword ever forced a change to either SKILL.md it would trigger the D-14 subagent review -- but the swept surfaces live in leaf files, not SKILL.md"
    - "The layer-3 attestation is recorded in a durable phase artifact (10-DST-04-ATTESTATION.md): it cites the per-leaf oracle-reviewer verdicts already recorded in Phase 7 (07-LEARNINGS + 07-0N-SUMMARY, incl. the push-down-method accidental-mechanics collision caught + reworded + cleared), Phase 8 (08-LEARNINGS + 08-0N-SUMMARY), and Phase 8.1 (08.1-LEARNINGS + 08.1-0N-SUMMARY, incl. the canonical ## Intent near-verbatim trap) as the standing evidence for the leaf surfaces the sweep does not re-open, and notes functional-catalog is no-oracle (outside the book-prose axis, attested by FUN-04 / Phase-8.2 skill-reviewer PASS)"
  artifacts:
    - path: ".planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md"
      provides: "The recorded layer-2 sweep results + layer-3 attestation citation (an undocumented attestation is not an attestation)"
      contains: "07-LEARNINGS"
  key_links:
    - from: "the executor (main context)"
      to: ".claude/agents/oracle-reviewer.md"
      via: "the ONLY sanctioned reader of .oracle/; the sweep is expressed as oracle-reviewer subagent invocations, never a main-context diff against the book"
      pattern: "oracle-reviewer"
  decisions: |
    - **D-01:** layers 2 and 3 of the three-layer DST-04 scan -- layer 2 is the DST-04-only clean-room oracle-reviewer pass over a risk-ranked surface subset (main context MUST NEVER read .oracle/); layer 3 cites the per-leaf oracle-reviewer verdicts recorded in Phase 7/8/8.1 for leaves the sweep does not re-open, RECORDED in the phase artifact
    - **D-02:** target the known-collision surfaces first -- canonical one-line pattern ## Intent lines (GoF + extra) and short imperative ## Mechanics step lists (Fowler + Kerievsky); the Applicability-first-line mirror into gof-catalog/README.md is already deterministically gated by check-gof, so it rests on layer-3 attestation
    - **D-03:** operator-locked breadth -- both proven collision surfaces across all leaves, surface-scoped (every ## Intent in gof + extra; every ## Mechanics step list in fowler + kerievsky), batched by chapter/family (one invocation per batch), ~15-17 invocations, not ~120; surfaces only, not motivation / examples / Consequences
    - **D-04:** the book-prose axis is scoped to the lz-refactor tree; the sweep covers the catalog leaf surfaces (Fowler / Kerievsky / GoF / extra) and does NOT touch lz-tpp (its transformations.md is cited FibTPP by design, MUST NOT be scrubbed); functional-catalog is no-oracle and outside this axis
    - **D-14:** if a sweep reword were ever forced onto either SKILL.md it needs its own subagent review before acceptance; leaf-file rewords do not (the swept surfaces are leaf files, so this is a guard, not an expected path)
---

<objective>
Run the DST-04 no-verbatim clean-room verification (D-01 layers 2 and 3) that the deterministic layer-1
gate (10-01) cannot: a semantic near-verbatim sweep of the two proven-collision surfaces across all
catalog leaves, performed entirely through the read-only oracle-reviewer subagent so the main context
never reads book prose, plus a recorded attestation citing the standing per-leaf verdicts for everything
the sweep does not re-open.

Purpose: the deterministic detector is source-free and can only block long quoted runs and route risk
surfaces; the actual "is this too close to the book?" judgment requires reading the source, which only
oracle-reviewer may do. The two collisions that were ACTUALLY observed in prior phases sit on different
surfaces -- the canonical ## Intent trap (Phase 8.1) and an accidental ## Mechanics collision
(push-down-method, Phase 7) -- so both surfaces are swept; motivation/examples/Consequences rest on the
attestation.

Output: a converged (all-pass) sweep of 28 Intents + 89 mechanics, any revises reworded blind and
re-passed, and a durable 10-DST-04-ATTESTATION.md recording both the sweep results and the layer-3
citations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-distribution-hygiene/10-CONTEXT.md
@.planning/phases/10-distribution-hygiene/10-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.claude/agents/oracle-reviewer.md

The clean-room loop, the driver-responsibilities / fan-out firewall, and the oracle-availability matrix
are in 07-ORACLE-MODEL.md. RESEARCH "Layer 2" specifies the per-invocation contract, the batch
boundaries, the surface counts (28 Intents / 89 mechanics), and the .oracle/ book-dir names
(refactoring-2e = Fowler, refactoring-to-patterns = Kerievsky, design-patterns = GoF; extra-patterns
are Kerievsky+GoF-grounded, gated against refactoring-to-patterns + design-patterns). The layer-3
attestation artifacts (07/08/08.1 LEARNINGS + per-plan SUMMARYs) are cited, not re-read for content.
</context>

<tasks>

<task type="auto">
  <name>Task 1: DST-04 layer-2 clean-room oracle-reviewer sweep over 28 Intents + 89 mechanics, batched by chapter (D-01 L2, D-02, D-03, D-04)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/ (gof-catalog, extra-patterns-catalog, fowler-catalog, kerievsky-catalog leaf files -- edited ONLY on a revise verdict)</files>
  <read_first>
    - .claude/agents/oracle-reviewer.md (the ONLY sanctioned .oracle/ reader -- its input contract, the pass/revise/blocked verdict shape, and that it chunk-reads long chapters to EOF and never echoes source prose)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md (the clean-room loop; "Driver responsibilities" fan-out firewall -- the driver does NOT persist a full ordered name+chapter map; how Phase 7/8 drove one invocation per chapter)
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md ("Layer 2" -- the 6-part per-invocation spec, the batch boundaries, the 28/89 surface counts, the .oracle/ book-dir-to-catalog mapping)
    - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/ (the 23 leaves whose ## Intent line is swept -- and extra-patterns-catalog/ 5 leaves; fowler-catalog/ 62 + kerievsky-catalog/ 27 leaves whose ## Mechanics step list is swept -- the DRAFT surfaces; do NOT read .oracle/)
  </read_first>
  <action>
    Run the DST-04 layer-2 sweep as a series of oracle-reviewer subagent invocations. DO NOT read .oracle/
    from this context under any circumstances -- pass the shipped surface to the subagent and let it read
    the source. Batch by chapter/family, one invocation per batch (~15-17 total): (A) GoF ## Intent lines
    in 3 family batches -- Creational (5), Structural (7), Behavioral (11) -- gated against
    .oracle/design-patterns; (B) extra-patterns ## Intent lines in 1 batch (5) gated against
    .oracle/refactoring-to-patterns + .oracle/design-patterns; (C) Fowler ## Mechanics step lists in 7
    chapter batches (Ch.6 through Ch.12) gated against .oracle/refactoring-2e; (D) Kerievsky ## Mechanics
    step lists in 5-6 chapter batches (Creation / Simplification / Generalization / Protection+Accumulation
    +Utilities, split as convenient within the 15-20 envelope) gated against .oracle/refactoring-to-patterns.
    For EACH invocation supply: (1) agent = oracle-reviewer; (2) the DRAFT = only that batch's shipped
    surface (the ## Intent line, or the ## Mechanics step list, per leaf -- NOT whole leaves, NOT
    motivation/examples/Consequences); (3) the source scope = the book + the chapter/topic pointed at via
    the book's index.md (do NOT hardcode chapter filenames -- the reviewer resolves the file); (4) the
    rubric = the DST-04 near-verbatim axis ONLY (return a too_close_to_source boolean + a short structural
    reason per surface, no echoed source span), surface-scoped; (5) round cap = 3 per batch; (6) the
    fan-out firewall -- do NOT persist a full ordered name+chapter map across invocations. On any
    revise/blocked verdict: reword that surface BLIND (never reading the source) using only the reviewer's
    short original directives, then re-gate the reworded surface in a fresh invocation. If a batch does
    NOT converge to all-pass within 3 rounds, STOP and escalate to the owner via AskUserQuestion with the
    non-converging surface(s) and the reviewer's directives -- do not force a resolution. If a reviewer
    returns blocked for a missing source, surface that to the owner too. Record, per batch, the final
    verdict and any surfaces reworded (for Task 2 to fold into the attestation artifact).
  </action>
  <acceptance_criteria>
    - Every batch (GoF Intents x3 + extra Intents x1 + Fowler mechanics x7 + Kerievsky mechanics x5-6) reaches an all-pass oracle-reviewer verdict; the total invocation count is within 15-20
    - No .oracle/ path was read from the main executor context (the sweep ran exclusively through oracle-reviewer subagents)
    - Every revise/blocked surface was reworded BLIND from the reviewer's directives and re-passed in a fresh invocation; any non-converging batch was escalated to the owner, not force-resolved
    - After all rewords, from .claude/skills/lz-refactor-workspace: node tools/check-hygiene.mjs, node tools/check-gof.mjs, node tools/check-kerievsky.mjs, and node tools/check-catalog.mjs all exit 0 (no leaf dropped its Intent/Mechanics surface; the hardened axis-c gate stays GREEN)
    - The 28 Intent + 89 Mechanics surface counts are preserved (no leaf lost a surface during a reword)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-hygiene.mjs && node tools/check-gof.mjs && node tools/check-kerievsky.mjs && node tools/check-catalog.mjs</automated>
  </verify>
  <done>All 28 Intent + 89 Mechanics surfaces pass the clean-room oracle-reviewer near-verbatim gate (revises reworded blind + re-passed; non-convergence escalated), the main context never read .oracle/, and the leaf-contract + hardened hygiene checkers stay GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Record the layer-3 attestation citation in a durable phase artifact (D-01 L3)</name>
  <files>.planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md ("Layer 3 -- attestation": the confirmed-present artifacts to cite and what each stands for; the functional-catalog no-oracle note)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md (the push-down-method accidental-mechanics collision -- caught, reworded blind, cleared; the standing Fowler attestation)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-LEARNINGS.md (the standing Kerievsky attestation cited by this task's action + acceptance)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-LEARNINGS.md (the canonical ## Intent near-verbatim trap caught here; the standing GoF + extra-patterns attestation)
    - .planning/phases/10-distribution-hygiene/10-CONTEXT.md (D-01 layer 3: "an undocumented attestation is not an attestation" -- the citation must be recorded in a phase artifact)
  </read_first>
  <action>
    Create .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md. Record two things. (1) The
    layer-2 sweep results from Task 1: which surface batches were swept (GoF/extra Intents; Fowler/Kerievsky
    mechanics), the all-pass verdict, and a list of any surfaces reworded blind (leaf + one-line reason,
    NO echoed source prose). (2) The layer-3 attestation: cite by filename the standing per-leaf
    oracle-reviewer verdicts for the leaf surfaces the sweep did NOT re-open (motivation, examples,
    Consequences) -- Fowler via 07-LEARNINGS.md + the 07-01..10 SUMMARYs (note the push-down-method
    collision as precedent that justifies keeping mechanics in the sweep), Kerievsky via 08-LEARNINGS.md +
    the 08-01..06 SUMMARYs, and GoF + extra via 08.1-LEARNINGS.md + the 08.1-01..07 SUMMARYs (note the
    canonical ## Intent trap). State explicitly that functional-catalog is no-oracle (no book source;
    grounded in the committed research artifact) and therefore OUTSIDE the DST-04 book-prose axis -- its
    clean status is attested by FUN-04 / the Phase-8.2 skill-reviewer PASS, not a book sweep. Keep the file
    ASCII-only and free of the work-email literal; do NOT quote any .oracle/ source text.
  </action>
  <acceptance_criteria>
    - 10-DST-04-ATTESTATION.md exists and records the Task-1 sweep result (batches swept + all-pass + any blind rewords listed by leaf + one-line reason)
    - It cites, by filename, 07-LEARNINGS.md (Fowler), 08-LEARNINGS.md (Kerievsky), and 08.1-LEARNINGS.md (GoF + extra) as the layer-3 standing evidence for the un-swept leaf surfaces
    - It states functional-catalog is no-oracle / outside the book-prose axis (attested by FUN-04 / Phase-8.2 skill-reviewer PASS)
    - The file is ASCII-only and contains no work-email literal and no echoed .oracle/ source prose
  </acceptance_criteria>
  <verify>
    <automated>rg -q '07-LEARNINGS' .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md && rg -q '08-LEARNINGS' .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md && rg -q '08.1-LEARNINGS' .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md && rg -q -i 'functional-catalog' .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md && test -z "$(rg -oiN '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' .planning/phases/10-distribution-hygiene/10-DST-04-ATTESTATION.md | rg -iv 'larsbrinknielsen@gmail\.com' | rg -iv 'lz-tdd@')"</automated>
  </verify>
  <done>10-DST-04-ATTESTATION.md records the layer-2 sweep outcome and cites the Phase 7/8/8.1 standing verdicts for the un-swept leaf surfaces (an documented attestation), notes functional-catalog as no-oracle, and is ASCII-only + work-email-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The core threat of the phase (verbatim copyrighted book prose in the public tree) is verified here. The
critical boundary is the clean-room firewall: source text (.oracle/) must never cross into the main
context or into any committed file.

| Boundary | Description |
|----------|-------------|
| .oracle/ source text -> main context / committed tree | FORBIDDEN; only oracle-reviewer reads .oracle/ and it returns verdicts in its own words |
| catalog leaf surfaces -> published public tree | ## Intent + ## Mechanics surfaces must not be near-verbatim to the source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-IP | Information Disclosure / IP | verbatim Fowler/Kerievsky/GoF prose or code in the lz-refactor catalog | mitigate | Clean-room oracle-reviewer sweep of both proven-collision surfaces across all leaves (D-01 L2, D-03); revises reworded blind + re-gated; layer-3 attestation for un-swept surfaces (D-01 L3) |
| T-10-ORACLE | Information Disclosure | .oracle/ source leaking into the main context or a committed artifact | mitigate | Main context never reads .oracle/ (07-ORACLE-MODEL); sweep runs only through the read-only oracle-reviewer subagent; the attestation artifact quotes no source prose |
| T-10-SKILLEDIT | Tampering | a forced reword of either SKILL.md bypassing review | mitigate | Swept surfaces live in leaf files; if a reword ever hit a SKILL.md it triggers the D-14 subagent review before acceptance |
</threat_model>

<verification>
- All 28 Intent + 89 Mechanics surface batches reach an all-pass oracle-reviewer verdict (revises reworded blind + re-passed; non-convergence escalated to owner).
- The main executor context read no .oracle/ path (clean-room firewall intact).
- node tools/check-hygiene.mjs + check-gof + check-kerievsky + check-catalog exit 0 after any rewords (surfaces preserved; hardened axis-c GREEN).
- 10-DST-04-ATTESTATION.md records the sweep result + cites 07/08/08.1 standing verdicts + notes functional-catalog no-oracle; ASCII-only, work-email-free.
</verification>

<success_criteria>
DST-04's semantic no-verbatim guarantee is verified through the clean-room subagent for the two proven
collision surfaces across every catalog leaf, with all revises reworded blind and re-passed, and the
standing per-leaf attestation for un-swept surfaces recorded in a durable phase artifact -- the main
context never having read book prose.
</success_criteria>

<output>
Create `.planning/phases/10-distribution-hygiene/10-03-SUMMARY.md` when done.
</output>
