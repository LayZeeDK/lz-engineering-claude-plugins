---
phase: 17-assertion-design-stance-router-ts-vitest-mechanics
plan: 04
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
  - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
autonomous: true
requirements: [ASRT-03, RTR-01, VIT-01, VIT-02]
must_haves:
  truths:
    - "message-matrix.md carries the query/command message matrix (origin x type): incoming query -> assert the returned value; incoming command -> assert the public side effect; outgoing command -> expect-to-send (the one warranted double); outgoing query + self-messages -> do not test"
    - "the matrix is the design-agnostic assert-vs-mock firewall: the ONLY cell warranting a double is the outgoing command; a tsc-strict Vitest fence shows an incoming-query return assertion and an outgoing-command vi.fn expect-to-send"
    - "vitest-typescript-mechanics.md maps Vitest 4.x to RED concepts: it.todo = test list; test.each/it.each = triangulation; vi.* doubles WITH RESTRAINT; watch = feedback loop; fails-for-the-right-reason as a MECHANIC (AssertionError = valid RED; ReferenceError/import = broken harness)"
    - "type-level (expectTypeOf/assertType) and property-based (fast-check) are BRIEF forward-pointers to ADV-01/ADV-02 only -- no leaves authored, no fast-check dependency added; the Phase-18 LAW-02 spine marker remains"
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
    - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
  key_links:
    - "message-matrix.md is the ASRT-03 assert-vs-mock rule + the RTR-01 message-oriented route leaf"
    - "vitest-mechanics vi.* restraint -> anti-patterns listen-to-the-tests + message-matrix outgoing-command"
---

<objective>
Author the OWNED Metz message-matrix leaf (the design-agnostic assert-vs-mock rule) and the
no-oracle Vitest-4.x mechanics leaf (RED-concept mapping + tsc-strict examples + the ADV
forward-pointers). These are the two example-bearing leaves that carry the assert-firewall and the
concrete demo stack.

Purpose: close ASRT-03 (the query/command matrix), the RTR-01 message-oriented route leaf, and
VIT-01/VIT-02 (Vitest mechanics mapped to RED, examples tsc --strict clean).
Output: two filled leaves, each with >= 1 tsc-strict Vitest fence.

Executor note (executor cannot spawn agents): message-matrix.md is an OWNED surface (Metz + Owen,
99 Bottles of OOP 2nd Ed JavaScript Edition). Author it BLIND own-words from the 17-RESEARCH planning
grain (the 6-cell matrix STRUCTURE is a plain fact); do NOT read `.oracle/`. Pitfall 2: the matrix
cells are terse and memorable -- paraphrase every canonical one-liner in original words. The
orchestrator runs the oracle-reviewer converge-to-clean gate on this leaf AFTER this wave (Plan
17-06). vitest-typescript-mechanics.md is no-oracle (Vitest docs), authored blind.

Decisions implemented (decision-coverage):
- **D-01:** message-matrix (99 Bottles JS ed) is OWNED, oracle-gated -- own-words draft here; the
  oracle-reviewer gate is the orchestrator's (Plan 17-06); main context never reads `.oracle/`.
- **D-02:** Vitest mechanics are no-oracle (public API docs), authored blind.
- **D-05:** ASRT-03 -- the query/command matrix as the assert-vs-mock rule: assert the RETURN of an
  incoming query; assert the public SIDE EFFECT of an incoming command; IGNORE self + outgoing
  queries; EXPECT-to-send only for outgoing commands.
- **D-06:** RTR-01 -- message-matrix.md is the message-oriented route leaf of the stance subdir.
- **D-08:** VIT-01 -- map Vitest 4.x (it.todo / test.each / vi.* restraint / watch) to RED concepts;
  fails-for-the-right-reason appears here ONLY as a Vitest MECHANIC (read the red bar).
- **D-09:** cover VIT-01's NAMED surface fully; mention type-level (expectTypeOf/assertType) and
  property-based (fast-check) ONLY as brief ADV-01 / ADV-02 forward-pointers -- no full leaves, and
  NO fast-check dependency.
- **D-10:** VIT-02 -- examples land in these references (tsc --strict clean); the SKILL.md body is
  not edited here (Phase 18 owns its worked example).
- **D-14:** no-verbatim gate -- the matrix prose + every fence pass the no-verbatim scan; the matrix
  additionally clears the orchestrator oracle-reviewer.
- **D-15:** co-edit, do not split -- the vitest leaf leaves the Phase-18 LAW-02 spine marker intact.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-RESEARCH.md
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
@plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
@plugins/lz-tdd/skills/lz-red/references/naming.md
@plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author message-matrix.md (Metz query/command matrix, OWNED, own-words)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md (content contract lines 13-27: Message type / Assert rule / Mock rule; the five occupied cells)
    - plugins/lz-tdd/skills/lz-red/references/naming.md (the OWNED-attribution bullet form -- "(Sandi Metz and Katrina Owen, owned; oracle-verified)" -- and Sources tier string) and three-laws-and-test-selection.md lines 54-72 (fence shape)
    - 17-RESEARCH.md "Message Matrix" (lines 257-282, the 6-cell table + planning notes) + Pitfall 2 near-verbatim (lines 544-552) + 17-PATTERNS.md "message-matrix.md" (lines 127-153)
  </read_first>
  <action>
    Author the leaf own-words (OWNED; author BLIND from the 17-RESEARCH planning grain -- do NOT read
    .oracle/). Present the matrix on two axes: message ORIGIN (incoming / sent-to-self / outgoing) x
    message TYPE (query = returns a value, no observable side effect; command = changes observable
    state, no meaningful return). Sections carrying the contract fields Message type / Assert rule /
    Mock rule:
    - The query vs command distinction (own-words definition).
    - Incoming query: assert the RETURNED value (no double).
    - Incoming command: assert the direct public SIDE EFFECT (no double).
    - Outgoing command: EXPECT the message was sent -- the ONE warranted double (expect-to-send).
    - Outgoing query + sent-to-self: do NOT test (implementation detail of a collaborator / private).
    Present a 6-cell (origin x type) Markdown table AND state the firewall explicitly: the only cell
    that warrants a mock/double is the outgoing command; everything else is assert-a-value or
    do-not-test (the design-agnostic firewall against over-mocking / test-per-class). Carry the OWNED
    attribution parenthetical "(Sandi Metz and Katrina Owen, owned; oracle-verified)" on the distilled
    line. Add ONE self-contained tsc-strict Vitest fence: an incoming-query return assertion AND an
    outgoing-command `vi.fn()` `toHaveBeenCalled()` expect-to-send (import { describe, it, expect, vi }
    from 'vitest'; define the SUT + collaborator in-fence; example compiles clean). Rewrite the draft
    `## Sources` heading to `## Sources` with the Metz + Owen owned row (canonical owned tier string).
    ASCII-only. Paraphrase every terse canonical one-liner in original wording (Pitfall 2).
  </action>
  <acceptance_criteria>
    - Tokens present: incoming query, incoming command, outgoing command, outgoing query, expect-to-send.
    - The 6-cell origin x type matrix is present and states the outgoing command as the ONLY warranted
      double.
    - A tsc --strict Vitest fence is present (incoming-query return assertion + outgoing-command vi.fn
      expect-to-send) and compiles under `run typecheck`.
    - The OWNED attribution parenthetical is on the distilled line; `## Sources` (no draft
      parenthetical) with the Metz + Owen owned row; check-hygiene ASCII + no-verbatim clean.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>message-matrix.md carries the 6-cell matrix + firewall + a compiling incoming-query/outgoing-command fence, own-words; typecheck + hygiene GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author vitest-typescript-mechanics.md (Vitest 4.x -> RED concepts + ADV forward-pointers)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md (content contract lines 13-31: Mechanic / When-to-use / Distilled note)
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (fence shape) + naming.md (bullet + Sources form)
    - 17-RESEARCH.md "Vitest 4.x Mechanics" (lines 320-365, the verified API + 4.0 traps to avoid) + Standard Stack signature table (lines 171-186) + Pitfall 6 version drift (lines 576-581) + 17-PATTERNS.md "vitest-typescript-mechanics.md" (lines 185-213)
  </read_first>
  <action>
    Fill the leaf own-words (no-oracle). Sections carrying Mechanic / When-to-use / Distilled note,
    each mapping a Vitest 4.x facility to a RED concept:
    - it.todo (test.todo) = the running test list (stub a not-yet-written test).
    - test.each / it.each = triangulation over concrete examples (array-table or tagged-template).
    - vi.* doubles WITH RESTRAINT: vi.fn / vi.spyOn / vi.mock -- linked to the listen-to-the-tests
      meta-rule (anti-patterns.md) and the message matrix (the only warranted double is the
      outgoing-command expect-to-send).
    - watch mode = the fast feedback loop (bare `vitest` = watch; `vitest run` = single CI run).
    - Fails-for-the-right-reason as a MECHANIC ONLY (D-08): how to read the red bar -- an
      AssertionError (expected X, received Y) is a valid RED; a ReferenceError / TypeError / import
      failure is a broken harness, not a valid RED.
    Add at least one self-contained tsc-strict Vitest fence (e.g. a test.each triangulation example
    over a pure SUT; import from 'vitest'; module-scoped; compiles clean). AVOID the 4.0 traps
    (Pitfall 6): no toMatchTypeOf, no config snippets, no arrow-fn mock invoked with `new`.
    Add a BRIEF ADV forward-pointer (one or two sentences, marked advanced/deferred): type-level RED
    (expectTypeOf / assertType in *.test-d.ts, needs --typecheck) -> ADV-01; property-based RED
    (fast-check express-the-invariant + shrink) -> ADV-02. Do NOT author these leaves; do NOT add
    fast-check as a dependency. Leave a labelled "(Phase 18)" LAW-02 spine marker (the fail-for-the-
    right-reason PROCEDURE / Three Laws framing is Phase 18) so the instrument deferral guard passes.
    Rewrite the draft `## Sources` heading to `## Sources` (Vitest 4.x cited, version-pinned 4.1.10;
    fast-check named only as a deferred pointer). ASCII-only.
  </action>
  <acceptance_criteria>
    - Tokens present: it.todo (or test.todo), test.each/it.each, restraint, watch, and a
      fails-for-the-right-reason mechanic token (right reason / AssertionError).
    - An ADV forward-pointer token (ADV-0 / expectTypeOf / fast-check) is present; no full type-level
      or property-based leaf is authored; no fast-check dependency is added anywhere.
    - At least one tsc --strict Vitest fence present and compiling under `run typecheck`; no
      toMatchTypeOf / config snippet / arrow-fn `new` mock.
    - A "(Phase 18)" LAW-02 marker line is present (deferral guard passes); `## Sources` (no draft
      parenthetical); check-hygiene clean.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>vitest-typescript-mechanics.md maps the named Vitest surface to RED concepts + a compiling fence + a brief ADV pointer + the Phase-18 marker; typecheck + hygiene GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| book prose (.oracle) -> shipped tree | the Metz matrix cells are terse/memorable; verbatim reconstruction must not cross into the leaf. |
| forward-pointer -> dependency | fast-check is NAMED as a deferred pointer only; it must NOT be installed into any workspace or `plugins/lz-tdd`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure (copyright, DST-04) | message-matrix.md (owned 99 Bottles) | high | mitigate | Own-words BLIND authoring from the RESEARCH planning grain (never reads .oracle/); paraphrase every terse cell (Pitfall 2); deterministic no-verbatim scan per commit; orchestrator oracle-reviewer converge-to-clean on this leaf in Plan 17-06. |
| T-17-02 | Information Disclosure (PII) | committed Markdown + commit identity | high | mitigate | check-hygiene allowlist-inversion; author/committer = public gmail. |
| T-17-04 | Tampering (supply chain) | fast-check forward-pointer | medium | mitigate | fast-check is a documented ADV-02 pointer only; D-09 forbids installing it; no package added; the shipped tree stays Markdown-only. |
| T-17-03 | Tampering (mojibake) | ASCII shipped tree | medium | mitigate | check-hygiene ASCII byte scan; `--` / `->` only. |
</threat_model>

<verification>
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 (both fences compile tsc --strict; no 4.0-trap APIs).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` shows message-matrix.md and vitest-typescript-mechanics.md rows PASS (incl. the vitest Phase-18 deferral guard); sibling stubs RED until their Wave-2 plans land -- expected.
- `git diff` adds no dependency to any package.json.
</verification>

<success_criteria>
- ASRT-03 matrix + RTR-01 message route leaf present own-words with the outgoing-command-only firewall.
- VIT-01 named surface mapped to RED concepts; VIT-02 examples tsc --strict clean; ADV forward-pointers only (no leaves, no fast-check dep).
- Phase-18 LAW-02 marker intact; typecheck + hygiene GREEN.
</success_criteria>

## Artifacts this phase produces (Plan 17-04)

- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md`
  (Metz query/command 6-cell matrix + expect-to-send firewall + one tsc-strict Vitest fence).
- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md`
  (Vitest 4.x -> RED-concept mapping + >= 1 tsc-strict fence + brief ADV-01/ADV-02 forward-pointers
  + a Phase-18 LAW-02 marker).
- No new files. No new dependency (fast-check NOT installed). Owned surface authored this plan:
  the Metz matrix (oracle-reviewer gated by the orchestrator in Plan 17-06).

<output>
Create `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-04-SUMMARY.md` when done.
</output>
