---
phase: 05-skill-effectiveness-evals
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .planning/phases/05-skill-effectiveness-evals/trigger-workspace/results.json
  - .planning/phases/05-skill-effectiveness-evals/trigger-workspace/run_loop_stdout.json
  - .planning/phases/05-skill-effectiveness-evals/trigger-workspace/run_loop_stderr.log
autonomous: false
requirements: [EVAL-01]
user_setup: []

must_haves:
  truths:
    - "Under WSL2, the smoke test reads rate=1/1 for a should-trigger query BEFORE the full loop runs, de-risking WSL interop streaming (A1) and the --model claude-opus-4-8 id (A2); if it reads 0 the trigger eval STOPS and surfaces to the user (D-01)"
    - "run_loop.py runs under WSL2 (native Windows select() on the subprocess pipe is broken) and reports held-out accuracy + per-query trigger rates for the description, sampling each query 3x with a 40% held-out split (EVAL-01, D-01, D-05)"
    - "Near-miss should-not-trigger queries stay quiet and the held-out accuracy is assessed against the D-06 soft bar (~90%); missing the bar feeds at most the 05-04 tuning pass and never reopens Phases 1-4 (D-06)"
  artifacts:
    - path: ".planning/phases/05-skill-effectiveness-evals/trigger-workspace/results.json"
      provides: "run_loop output: best_description, best_test_score (held-out), history[] per-query rates (EVAL-01, D-01)"
      contains: "best_description"
  key_links:
    - from: "evals/trigger-eval.json"
      to: "scripts.run_loop (WSL2)"
      via: "claude -p --model claude-opus-4-8 synthetic-command trigger probe"
      pattern: "should_trigger"
---

<objective>
Run EVAL-01 (the trigger eval) UNDER WSL2. Native Windows Python raises OSError WinError 10093 on select() over the subprocess pipe and silently scores every query as "did not trigger" (~50% meaningless), so this MUST run under WSL2 Ubuntu (verified working). A BLOCKING smoke test gates the full loop: it must read rate=1/1 for a should-trigger query first, or the loop scores are not trustworthy and the eval stops for manual intervention.

Purpose: Prove EVAL-01 - the lz-tpp `description` fires on red-green-refactor / TDD / transformation-priority / TPP contexts and stays quiet on genuine near-misses - and produce the D-07 tuning candidate (best_description by held-out score) for 05-04.
Output: trigger-workspace/results.json (best_description + held-out score + per-query rates) plus the held-out accuracy and original-vs-best description recorded in the SUMMARY.

NOTE (autonomous:false): the smoke gate can require human intervention (install a Linux-native claude in WSL, or investigate the model id) and A3 must be re-verified at run time. Do not proceed past a failing smoke test autonomously.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md
@.planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md
@.planning/phases/05-skill-effectiveness-evals/05-01-SUMMARY.md

Inputs authored in 05-01 (must exist): evals/trigger-eval.json (>=18 queries), evals/trigger-smoke.json (2 queries).

Constants (WSL2 path forms - this plan runs under WSL2):
- SKILL_CREATOR_DIR (WSL) = /mnt/c/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator  (this is the cwd for python3 -m scripts.*)
- LZTPP (WSL) = /mnt/d/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp
- PHASE_DIR (WSL) = /mnt/d/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals
- Model id: claude-opus-4-8 (the session model - matches what users experience)

Environment facts (verified, 05-RESEARCH.md lines 193-203, 410-424): WSL2 python3 = 3.12.3; `claude` in WSL resolves to /mnt/c/.../claude 2.1.198 via /mnt/c interop; select()-on-pipe SUCCEEDS under WSL. lz-tdd/lz-tpp are NOT enabled in ~/.claude/plugins/config.json or ~/.claude/settings.json (A3) - re-verify at run time so the real skill cannot steal the trigger (Pitfall 2). Invoke every script as `python3 -m scripts.<name>` with cwd = SKILL_CREATOR_DIR (scripts is a package).
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] WSL2 interop + model-id smoke test</name>
  <files>
    (no committed files; produces console output only)
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 193-242 (Pitfall 1 the Windows select() blocker + Pitfall 2 the enabled-skill false negative + the exact smoke command) and lines 395-402 (assumptions A1/A2/A3)
    - .planning/phases/05-skill-effectiveness-evals/05-VALIDATION.md lines 72-78 (the manual-only smoke verification: a should-trigger query MUST read rate=1/1)
    - .planning/phases/05-skill-effectiveness-evals/evals/trigger-smoke.json
  </read_first>
  <action>
    First re-verify A3: confirm lz-tdd/lz-tpp are absent/disabled in ~/.claude/plugins/config.json and ~/.claude/settings.json (an enabled real skill would steal the trigger and cause false negatives). Then, under WSL2, set the Bash working directory to SKILL_CREATOR_DIR (WSL form) via a standalone cd (do NOT use `cd ... &&`) and run the smoke test on trigger-smoke.json: `python3 -m scripts.run_eval --eval-set /mnt/d/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/evals/trigger-smoke.json --skill-path /mnt/d/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp --model claude-opus-4-8 --runs-per-query 1 --verbose`. Read the per-query output: the should_trigger:true query MUST read rate=1/1 and the should_trigger:false query should read rate=0/1. If the should-trigger query reads 0 (or every query reads 0/1), STOP: do NOT run run_loop; report the smoke failure and the two most likely causes - A1 (WSL /mnt/c interop mangled the stream-json pipe) and A2 (the --model claude-opus-4-8 id is stale/invalid) - and the documented fallbacks (install a Linux-native claude in WSL; verify the model id) and await user direction. Do NOT trust or run the full loop on a failed smoke.
  </action>
  <verify>
    <automated>echo "MANUAL/WSL: the should_trigger:true smoke query must read rate=1/1 under WSL2. Recorded in SUMMARY as smoke_rate. This automated line is the Nyquist marker; the smoke result itself is captured from the WSL run_eval --verbose output."; test -n "1"</automated>
  </verify>
  <acceptance_criteria>
    - A3 re-verified: lz-tdd/lz-tpp absent/disabled in config.json + settings.json (record the check in SUMMARY).
    - The smoke run on trigger-smoke.json prints rate=1/1 for the should_trigger:true query under WSL2 (and ~0/1 for the near-miss).
    - If rate=0, the plan HALTS with a surfaced report (A1/A2 causes + fallbacks); run_loop is NOT executed.
  </acceptance_criteria>
  <done>Smoke test passed (rate=1/1) under WSL2 with lz-tpp not enabled, OR the plan halted and surfaced the failure to the user.</done>
</task>

<task type="auto">
  <name>Task 2: Full Description Optimization loop under WSL2 + capture results</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/trigger-workspace/results.json
    .planning/phases/05-skill-effectiveness-evals/trigger-workspace/run_loop_stdout.json
    .planning/phases/05-skill-effectiveness-evals/trigger-workspace/run_loop_stderr.log
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 243-277 (the full run_loop command + the output shape: best_description, best_test_score, history[] per-query trigger_rate; and the D-07 application rule that iteration 1 evaluates the CURRENT description)
    - .planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md D-01, D-05, D-06 (loop harness, 3x sampling, soft held-out bar)
    - .planning/phases/05-skill-effectiveness-evals/evals/trigger-eval.json
  </read_first>
  <action>
    Only after Task 1's smoke passes. Under WSL2, cwd = SKILL_CREATOR_DIR (WSL), run the Description Optimization loop (D-01): `python3 -m scripts.run_loop --eval-set /mnt/d/.../.planning/phases/05-skill-effectiveness-evals/evals/trigger-eval.json --skill-path /mnt/d/.../plugins/lz-tdd/skills/lz-tpp --model claude-opus-4-8 --max-iterations 5 --runs-per-query 3 --holdout 0.4 --report none --results-dir /mnt/d/.../.planning/phases/05-skill-effectiveness-evals/trigger-workspace --verbose` redirecting stdout to trigger-workspace/run_loop_stdout.json and stderr to trigger-workspace/run_loop_stderr.log (use full /mnt/d paths, not the elided .../). --report none because the box is headless (avoid webbrowser.open). From results.json / stdout capture: exit_reason, original_description, best_description, best_train_score, best_test_score (HELD-OUT), iterations_run, and the per-query history[] trigger rates for both train and held-out. Sanity-check with the --verbose stream that obvious should-trigger queries read rate=3/3 and near-misses read low (Pitfall 2 guard). Record the held-out accuracy, the per-query pass table, and original-vs-best_description in the SUMMARY, and assess it against the D-06 soft bar (~90% held-out, near-misses quiet). Do NOT apply any description change here - the D-07 apply decision lives in 05-04. The whole trigger-workspace is gitignored (D-08, set in 05-01); it stays on disk for 05-04 to read.
  </action>
  <verify>
    <automated>echo "WSL2-run artifact check (run from Git Bash after the WSL loop): the loop must have written results.json with a best_description field."; node -e "const fs=require('fs'); const p='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/trigger-workspace/results.json'; if(!fs.existsSync(p)){console.error('FAIL results.json missing (did the WSL loop run + smoke pass?)');process.exit(1);} const r=JSON.parse(fs.readFileSync(p,'utf8')); const ok=('best_description'in r)&&('best_test_score'in r||'best_score'in r)&&Array.isArray(r.history); if(!ok){console.error('FAIL results.json shape',Object.keys(r));process.exit(1);} console.log('OK results.json: iterations',r.iterations_run,'held-out',r.best_test_score||r.best_score);"</automated>
  </verify>
  <acceptance_criteria>
    - trigger-workspace/results.json exists with best_description, a held-out score (best_test_score or best_score), and a history[] of per-query trigger rates.
    - The --verbose stream shows obvious should-trigger queries at rate=3/3 (not 0/3 - which would mean the Windows blocker or an enabled real skill corrupted the run).
    - SUMMARY records held-out accuracy vs the ~90% soft bar, per-query pass/quiet table, and original-vs-best_description (no SKILL.md edit made here).
  </acceptance_criteria>
  <done>The loop ran under WSL2, results.json captured with best_description + held-out score + per-query rates, and the held-out accuracy assessed against the D-06 bar. No description applied yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger-eval.json + skill description -> claude -p subprocess | run_eval synthesizes a throwaway command file from the description and shells claude -p; input is author-supplied only |
| WSL2 /mnt interop -> native claude binary | The WSL claude is the Windows binary bridged via /mnt/c; the smoke test verifies the stream survives the pipe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering / EoP | run_eval synthetic .claude/commands/*.md + claude -p | mitigate | Synthetic command files are generated by the first-party harness from the authored description and deleted after use; run only against the authored trigger sets. Re-verify (A3) lz-tpp is not an enabled competing skill before running (Pitfall 2). |
| T-05-05 | Spoofing (false negative) | trigger detection via synthetic-command uuid | mitigate | The BLOCKING smoke gate confirms rate=1/1 on a should-trigger query before trusting any loop score; a 0/3 pattern signals the Windows select() blocker or an enabled real skill and halts the run. |
| T-05-SC | Tampering (supply chain) | run_loop / run_eval | accept | No installs: scripts ship with skill-creator (Python stdlib); claude CLI already present. |
</threat_model>

<verification>
- A3 re-verified (lz-tpp not enabled) and smoke test rate=1/1 under WSL2 before the loop (Task 1).
- results.json captured with best_description + held-out score + per-query history (Task 2).
- Obvious should-trigger queries at 3/3, near-misses quiet; held-out accuracy assessed vs the ~90% soft bar.
</verification>

<success_criteria>
- EVAL-01 measured under WSL2 with a passing smoke gate: the description's per-query trigger rates and held-out accuracy are known and trustworthy (not corrupted by the native-Windows select() blocker).
- best_description (the D-07 tuning candidate) and its held-out score are captured for 05-04's apply decision.
- No SKILL.md change made in this plan (tuning is 05-04's gated call).
</success_criteria>

<output>
Create `.planning/phases/05-skill-effectiveness-evals/05-03-SUMMARY.md` when done, recording: the smoke result (rate + A3 recheck), the held-out accuracy vs the ~90% bar, the per-query trigger/quiet table, exit_reason + iterations_run, and original-vs-best_description (the D-07 candidate) with their held-out scores. If the smoke halted the run, record the failure + the A1/A2 cause and that EVAL-01 needs manual intervention.
</output>
