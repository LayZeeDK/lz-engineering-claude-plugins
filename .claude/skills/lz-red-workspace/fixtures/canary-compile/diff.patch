diff --git a/TypeScript/test/vitest/canary-compile.spec.ts b/TypeScript/test/vitest/canary-compile.spec.ts
new file mode 100644
index 0000000..e11f972
--- /dev/null
+++ b/TypeScript/test/vitest/canary-compile.spec.ts
@@ -0,0 +1,9 @@
+import { Item, GildedRose } from '@/gilded-rose';
+
+describe('Conjured items', () => {
+  it('degrades in quality twice as fast as a normal item', () => {
+    const app: number = new GildedRose([new Item('Conjured Mana Cake', 3, 6)]);
+
+    expect(app.updateQuality()[0].quality).toBe(4);
+  });
+});
