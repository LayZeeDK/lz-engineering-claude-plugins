---
phase: 08.1-gof-design-patterns-catalog
plan: 01
type: execute
wave: 1
depends_on: []
branching_strategy: none
tags: [gof-catalog, extra-patterns-catalog, harness, checkers, linkify, typescript, validation, non-shipped-workspace]
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/check-gof.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
  - .claude/skills/lz-refactor-workspace/extract-samples.mjs
  - .claude/skills/lz-refactor-workspace/package.json
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md
autonomous: true
requirements: [GOF-01, GOF-02, GOF-03, GOF-04, XTR-01]
must_haves:
  truths:
    - "D-10: a new check-gof.mjs (clone of check-catalog.mjs) asserts the locked 23 GoF names grouped by family (Creational 5 / Structural 7 / Behavioral 11) are each present EXACTLY once as a leaf H1 (identity, not cardinality), filename == kebab-slug, single H1, no unknown/typo heading, no SCAFFOLD_RES phrase"
    - "D-04: check-gof asserts the 5-section leaf contract per leaf -- ## Intent, ## Applicability, ## Consequences, ## Example (with >=1 ts fence), ## Related patterns -- with optional leading Also known as: NOT asserted; the Fowler [web-only]/[web-example] provenance logic is deleted"
    - "D-05: check-gof extracts each leaf's ## Applicability FIRST LINE and asserts it is mirrored verbatim into a README row linking to that leaf's bare slug (no #anchor), and asserts the README carries the 3 family headings Creational / Structural / Behavioral"
    - "D-07: check-gof enforces ## Consequences present + populated (>=1 non-blank content line before the next ##), and does NOT assert a liability exists (the 8 benefits-only patterns must not false-fail); liability-only-where-sourced is left to the oracle-reviewer consequences axis"
    - "D-06: check-gof asserts the Singleton leaf's ## Consequences body references Dependency Injection (case-insensitive) -- the one deterministic GOF-02 assist (A1); the rest of the modern-caveat semantics is the oracle-reviewer consequences axis"
    - "D-09: check-gof carries a REQUIRED_AWAY map of exactly 3 entries (Singleton -> ../kerievsky-catalog/inline-singleton.md#inline-singleton; Composite -> ../kerievsky-catalog/encapsulate-composite-with-builder.md#encapsulate-composite-with-builder; Iterator -> ../kerievsky-catalog/move-accumulation-to-visitor.md#move-accumulation-to-visitor) and asserts those 3 leaves each carry the exact link string (presence only; resolution is check-crossrefs)"
    - "D-10: a new check-extra-patterns.mjs (small clone) asserts NAMES = {Null Object, Factory, Creation Method, Composed Method, Collecting Parameter} identity + the same 5-section contract + Consequences present+populated + Applicability-first-line mirror; flat 5-row index, NO family grouping, NO REQUIRED_AWAY"
    - "D-11: check-kerievsky.mjs's presence-only GoF pattern: check is replaced by the link-parse + set-membership linkify branch -- GoF-23 name MUST link to ../gof-catalog/<slug>.md#<slug>, extra name MUST link to ../extra-patterns-catalog/<slug>.md#<slug>, a name in neither set MUST carry NO gof/extra link -- via EXACT-name set lookup, never substring (Factory extra vs Factory Method GoF-23)"
    - "D-11: check-crossrefs.mjs adds gof-catalog + extra-patterns-catalog to its source set (mutuality guard stays Fowler-CATALOG-scoped); extract-samples.mjs adds gof (prefix gof) + extra (prefix extra) CATALOGS entries; package.json inserts check-gof + check-extra-patterns AFTER check-kerievsky in the check chain; check-hygiene.mjs is NOT changed"
    - "D-03: gof-catalog/README.md and extra-patterns-catalog/README.md are scaffolded as thin indexes with family/section headings + intro ONLY -- NO leaf-linking rows (rows land in Wave 3, D-05) so check-crossrefs is not false-RED"
    - "D-15: this is the single Wave-1 harness plan that owns ALL checker edits + both README scaffolds; the RED baseline (check-gof 0/23, check-extra-patterns 0/5, check-kerievsky RED on ~23 un-linkified tokens) is the EXPECTED instrument-first state, asserted explicitly, not a regression"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/check-gof.mjs"
      provides: "GOF-01/02/03 checker: 23-name family identity + 5-section contract + Applicability-first-line mirror + Consequences present+populated + Singleton-DI assist + REQUIRED_AWAY 3-map + family headings"
      min_lines: 200
    - path: ".claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs"
      provides: "XTR-01 checker: 5-name identity + 5-section contract + Consequences present+populated + Applicability-first-line mirror; flat index"
      min_lines: 120
    - path: ".claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs"
      provides: "linkify branch replacing the presence-only GoF pattern: check (set-membership over GoF-23 + extra-5)"
      contains: "gof-catalog"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md"
      provides: "thin GoF index scaffold: 3 family headings + intro, NO leaf rows"
      contains: "Creational"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md"
      provides: "thin extra-patterns index scaffold: intro, NO leaf rows"
      contains: "index"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/*.md"
      via: "linkify branch requires ../gof-catalog/<slug>.md#<slug> for GoF-23 tokens"
      pattern: "gof-catalog"
    - from: ".claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/*.md"
      via: "gof-catalog + extra-patterns-catalog added to sourceFiles"
      pattern: "gof-catalog|extra-patterns-catalog"
    - from: ".claude/skills/lz-refactor-workspace/extract-samples.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/*.md"
      via: "CATALOGS entry {dir: gof-catalog, prefix: gof} extracts + compiles ts fences"
      pattern: "gof-catalog"
---

<objective>
Extend the NON-shipped checker battery to gate the 23 GoF leaves, the 5 extra-pattern leaves,
their 5-section contract, the 3 required Away links, and the Kerievsky linkify branch -- and
scaffold both catalog READMEs (headings + intro only). No pattern CONTENT is authored here; this
plan produces only the instruments the Wave-2 authoring plans and the Wave-3 gate are graded against.

Purpose: make GOF-01..GOF-04 + XTR-01 machine-checkable BEFORE any content lands (Nyquist
scaffold-first), by cloning the shipped-green Phase-7/8 harness. Every new/edited check is RED
against the empty catalogs now and turns GREEN as Wave 2 authors leaves and Wave 3 linkifies +
finalizes -- this RED baseline is the expected instrument-first state, not a failure.

Output: new tools/check-gof.mjs + tools/check-extra-patterns.mjs, a retrofitted
tools/check-kerievsky.mjs (linkify branch), extended tools/check-crossrefs.mjs + extract-samples.mjs
+ package.json, and two thin README scaffolds under references/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-VALIDATION.md
@.claude/skills/lz-refactor-workspace/tools/check-catalog.mjs
@.claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs
@.claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
@.claude/skills/lz-refactor-workspace/extract-samples.mjs
@.claude/skills/lz-refactor-workspace/package.json
@plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md

<interfaces>
This is a clone + self-edit job, NOT new tooling. check-catalog.mjs (337 lines) is the EXACT
template for check-gof.mjs and check-extra-patterns.mjs -- transfer the whole skeleton verbatim
(imports; here/repoRoot 4-levels-up resolution lines 20-27; report/isDir/slugFor/walkMd/collectLeaves;
readmeRowsBySlug 184-208; the byName build + identity loop; the unknown-heading sweep 321-327;
SCAFFOLD_RES 106; the exit-code + SUMMARY contract 331-337). 08.1-PATTERNS.md gives the exact analog
line ranges and the 7 named deltas for check-gof (NAMES-by-family; 5-section contract regexes;
Applicability-first-line mirror; Consequences present+populated; Singleton-DI assist; REQUIRED_AWAY
3-map; family headings) and the linkify-branch replacement for check-kerievsky lines 272-277.

The locked NAMES (D-01/D-02):
  CREATIONAL (5): Abstract Factory, Builder, Factory Method, Prototype, Singleton
  STRUCTURAL (7): Adapter, Bridge, Composite, Decorator, Facade, Flyweight, Proxy
  BEHAVIORAL (11): Chain of Responsibility, Command, Interpreter, Iterator, Mediator, Memento,
    Observer, State, Strategy, Template Method, Visitor
  EXTRA (5): Null Object, Factory, Creation Method, Composed Method, Collecting Parameter

The 3 REQUIRED_AWAY entries (D-09, exact link strings, presence only):
  Singleton -> ../kerievsky-catalog/inline-singleton.md#inline-singleton
  Composite -> ../kerievsky-catalog/encapsulate-composite-with-builder.md#encapsulate-composite-with-builder
  Iterator  -> ../kerievsky-catalog/move-accumulation-to-visitor.md#move-accumulation-to-visitor

The linkify branch (D-08/D-11) is the ONE correctness-critical piece: parse the first Markdown link
on each leaf's GoF pattern: line (name = link text) else the leading token before " (" (name = plain
token), then EXACT-name set lookup -- GoF-23 -> require ../gof-catalog/<slug>.md#<slug>; extra-5 ->
require ../extra-patterns-catalog/<slug>.md#<slug>; neither set -> require NO gof/extra link. NEVER
substring: Factory (extra) must map to extra-patterns-catalog/factory.md, NOT Factory Method
(gof-catalog/factory-method.md). slugFor is already in check-kerievsky.mjs. The census (17 GoF-23 + 6
extra = 23 leaves require a link; 4 free text: chain-constructors, unify-interfaces, extract-parameter
= "n/a -- utility", replace-type-code-with-class = "Class / type-safe value") is in 08.1-RESEARCH.md
S1b. This self-produces the RED baseline (23 un-linkified tokens FAIL; 4 free-text tokens PASS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New check-gof.mjs + check-extra-patterns.mjs (clone check-catalog.mjs, swap field set) and wire the check chain</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-gof.mjs, .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs, .claude/skills/lz-refactor-workspace/package.json</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs (the EXACT clone template: repoRoot 4-levels-up lines 20-27; CATALOG constant line 27; NAMES + identity loop; contract-field regexes lines 256-274; readmeRowsBySlug + Use-when mirror lines 184-208, 303-315; WEB_ONLY membership pattern 101-102, 280-294 as the model for REQUIRED_AWAY; SCAFFOLD_RES 106; exit/SUMMARY 331-337)
    - .claude/skills/lz-refactor-workspace/package.json (the check script chain to extend, line 8)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (check-gof section: the 7 named deltas + exact line refs; check-extra-patterns small-clone spec)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md (Harness Mechanics S1 + the check-extra-patterns spec + S2 package.json insert point)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-01, D-02, D-04, D-05, D-06, D-07, D-09, D-10)
  </read_first>
  <action>
    Clone check-catalog.mjs wholesale into a new sibling tools/check-gof.mjs; do NOT edit
    check-catalog.mjs. Repoint CATALOG to references/gof-catalog. Apply the 7 deltas from
    08.1-PATTERNS.md exactly: (Delta 1) replace the flat 62-name array with three family arrays
    CREATIONAL/STRUCTURAL/BEHAVIORAL grouped with comment headers (model on check-kerievsky's
    grouped NAMES), a flat union NAMES with EXPECTED = 23, and a FAMILY_OF name->family map;
    (Delta 2) replace the Fowler Use-when/Motivation/Mechanics block with the 5-section regexes for
    ## Intent, ## Applicability, ## Consequences, ## Example (word-boundary), ## Related patterns,
    plus a ts-fence presence check, and DELETE the WEB_ONLY/WEB_EXAMPLE provenance logic entirely;
    do NOT assert Also known as:; (Delta 3) source the selector from the ## Applicability FIRST
    non-blank line and require it mirrored verbatim into a README row keyed by the leaf slug
    (readmeRowsBySlug regex matches BARE .md links only); (Delta 4) add a Consequences
    present+populated check -- at least one non-blank content line between ## Consequences and the
    next ## -- and do NOT assert a liability exists; (Delta 5) for the Singleton leaf assert its
    Consequences body matches /dependency injection/i (per D-06/A1); (Delta 6) add the 3-entry
    REQUIRED_AWAY map (Singleton/Composite/Iterator -> the exact Kerievsky Away link strings in the
    interfaces block) and, for those 3 leaves only, assert the exact link string appears in the leaf
    body (presence only); (Delta 7) assert the README contains the 3 family headings Creational /
    Structural / Behavioral (the stronger section-scan family placement is optional per A3 -- ship
    the minimum if it exceeds ~20 extra lines). Keep the identity loop, unknown-heading sweep, and
    the report()/SUMMARY/process.exit(failures===0?0:1) idiom; SUMMARY label
    "GOF-01 GREEN -- N/23 patterns present with 5-section contract + family index". Then create
    tools/check-extra-patterns.mjs as a small clone: CATALOG = references/extra-patterns-catalog,
    NAMES = the 5 extra names, EXPECTED = 5, the SAME 5-section contract + Consequences
    present+populated + Applicability-first-line mirror, NO family grouping, NO REQUIRED_AWAY, NO
    Singleton-DI assist; SUMMARY label "XTR-01 GREEN -- N/5". Assert name IDENTITY not cardinality
    in both. Finally, in package.json insert "&& node tools/check-gof.mjs && node
    tools/check-extra-patterns.mjs" into the check chain immediately AFTER check-kerievsky.mjs (D-11).
    Node builtins only; ASCII-only; add NO runtime dependency. Both new checkers MUST run against the
    empty catalogs and report the RED baseline (0/23, 0/5) -- that proves the instruments work before
    content lands.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` executes, prints a `SUMMARY:` line, and against the empty gof-catalog exits 1 reporting 0/23 present (expected RED baseline).
    - `node .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs` executes, prints a `SUMMARY:` line, and against the empty extra-patterns-catalog exits 1 reporting 0/5 (expected RED baseline).
    - `rg -c "Abstract Factory|Chain of Responsibility|Template Method|Flyweight|Visitor" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` confirms the 23 family names are enumerated; `rg -q "Creational" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && rg -q "Behavioral" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` match (family grouping).
    - `rg -q "Intent" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && rg -q "Applicability" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && rg -q "Consequences" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && rg -q "Related patterns" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` all match (5-section contract encoded).
    - `rg -q "inline-singleton.md#inline-singleton" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && rg -q "encapsulate-composite-with-builder" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && rg -q "move-accumulation-to-visitor" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` match (REQUIRED_AWAY 3-map); `rg -qi "dependency injection" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` matches (Singleton-DI assist).
    - `rg -q "Null Object" .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs && rg -q "Collecting Parameter" .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs` match; `rg -q "check-gof.mjs" .claude/skills/lz-refactor-workspace/package.json && rg -q "check-extra-patterns.mjs" .claude/skills/lz-refactor-workspace/package.json` match (wired into the chain, after check-kerievsky).
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs` returns no hits (ASCII-only); check-catalog.mjs is unchanged (`git diff --stat` shows no edit to it).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-gof.mjs; test $? -eq 1 && node .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs; test $? -eq 1 && rg -q "check-gof.mjs" .claude/skills/lz-refactor-workspace/package.json && rg -q "check-extra-patterns.mjs" .claude/skills/lz-refactor-workspace/package.json && ! rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-gof.mjs .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs</automated>
  </verify>
  <done>check-gof.mjs (23-name family identity + 5-section contract + Applicability-first-line mirror + Consequences present+populated + Singleton-DI assist + REQUIRED_AWAY 3-map + family headings) and check-extra-patterns.mjs (5-name identity + same contract, flat index) exist as siblings of check-catalog.mjs, both exit 1 on their empty catalogs (RED baseline), are ASCII-only, and are wired into the package.json check chain after check-kerievsky; check-catalog.mjs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit check-kerievsky.mjs linkify branch + extend check-crossrefs.mjs source set + extract-samples.mjs CATALOGS</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs, .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs, .claude/skills/lz-refactor-workspace/extract-samples.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs (the presence-only GoF pattern: branch at lines 272-277 to replace; the existing slugFor helper line 111; the grouped NAMES arrays as the model for duplicating the GoF-23 + extra-5 sets)
    - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs (CATALOG/KERIEVSKY/SMELLS_DIR constants lines 26-30; sourceFiles line 102; the Fowler-CATALOG-scoped inverse-of mutuality guard line 180 that must STAY Fowler-only)
    - .claude/skills/lz-refactor-workspace/extract-samples.mjs (the CATALOGS array lines 21-24; the per-catalog prefix output-name at line 131; the empty-catalog GREEN guard 141-146)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (check-kerievsky / check-crossrefs / extract-samples MODIFY sections -- exact excerpts + line refs)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md (S1b linkify design + token census; S2 extract-samples; S3 check-crossrefs; Pitfall 1 Factory vs Factory Method)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-08, D-11)
  </read_first>
  <action>
    In check-kerievsky.mjs, replace the presence-only GoF pattern: branch (lines 272-277) with the
    link-parse + set-membership linkify branch (D-08/D-11). Duplicate the GoF-23 (23) and extra-5 (5)
    name arrays into this file (or a shared token map). For each leaf's GoF pattern: line: parse the
    first [text](../<catalog>/<slug>.md#<anchor>) link if present (name = link text), else the
    leading token before " (" (name = plain token); then EXACT-name set lookup -- name in GoF-23 ->
    REQUIRE a link to ../gof-catalog/<slugFor(name)>.md#<slugFor(name)> (fail if absent or wrong
    catalog/slug); name in extra-5 -> REQUIRE ../extra-patterns-catalog/<slugFor(name)>.md#<slugFor(name)>;
    name in NEITHER set -> REQUIRE that NO gof/extra link is present (free text). NEVER substring
    (Factory extra must not resolve to Factory Method GoF-23). Leave RESOLUTION of the link target to
    check-crossrefs; this branch only parses + checks the required form. Keep every OTHER
    check-kerievsky axis (27-name identity, contract fields, Direction, composed-Fowler primitive,
    README-row mirror) unchanged. In check-crossrefs.mjs, define GOF and EXTRA constants (mirroring
    the KERIEVSKY line) for references/gof-catalog + references/extra-patterns-catalog and add
    ...collectLeafFiles(GOF) + ...collectLeafFiles(EXTRA) to sourceFiles; do NOT broaden the inverse-of
    mutuality guard (it stays CATALOG/Fowler-scoped so one-directional Kerievsky<->gof links are
    correctly excluded). In extract-samples.mjs, append two CATALOGS entries: {dir: gof-catalog,
    prefix: "gof"} and {dir: extra-patterns-catalog, prefix: "extra"} (the per-catalog prefix prevents
    sample-module collisions -- a gof factory-method fence and an extra factory fence never overwrite);
    no tsconfig change, no other edit. Node builtins only; ASCII-only. Against the current tree the
    edits MUST still run: check-kerievsky flips to RED on the ~23 un-linkified tokens (EXPECTED
    baseline; the other axes stay GREEN); extract-samples stays GREEN (empty gof/extra catalogs handled
    by the existing guard); check-crossrefs stays GREEN (449 existing links still resolve; no new
    leaf-rows yet).
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs; echo $?` exits 1 and its SUMMARY reports RED on the GoF-pattern axis (~23 un-linkified tokens FAIL, the 4 free-text tokens PASS) -- the EXPECTED instrument-first baseline; `rg -q "gof-catalog" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs && rg -q "extra-patterns-catalog" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` match (linkify branch present).
    - `rg -q "gof-catalog" .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && rg -q "extra-patterns-catalog" .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` match; `node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` exits 0 over the current tree (449 links still resolve).
    - `rg -q "gof-catalog" .claude/skills/lz-refactor-workspace/extract-samples.mjs && rg -q "extra-patterns-catalog" .claude/skills/lz-refactor-workspace/extract-samples.mjs` match; `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` exits 0 over the current (Fowler+Kerievsky) tree.
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs .claude/skills/lz-refactor-workspace/extract-samples.mjs` returns no hits (ASCII-only).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs; test $? -eq 1 && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && node .claude/skills/lz-refactor-workspace/extract-samples.mjs && rg -q "gof-catalog" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs && rg -q "extra-patterns-catalog" .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && rg -q "gof-catalog" .claude/skills/lz-refactor-workspace/extract-samples.mjs && ! rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs .claude/skills/lz-refactor-workspace/extract-samples.mjs</automated>
  </verify>
  <done>check-kerievsky.mjs's presence-only GoF pattern: check is replaced by the set-membership linkify branch (exact-name lookup, never substring) and now reports the RED baseline (~23 un-linkified tokens) with all other axes GREEN; check-crossrefs.mjs sources gof-catalog + extra-patterns-catalog (mutuality still Fowler-scoped) and stays GREEN; extract-samples.mjs walks both new catalogs with per-catalog prefixes and stays GREEN; all three are ASCII-only.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the two thin catalog READMEs (family/section headings + intro ONLY -- no leaf rows)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md, plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md (the thin-index shape to mirror: H1 + "index" suffix, Scope intro noting content lives in leaves not inlined, chapter/family H2 headings, one table per group, Sources footer)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (README section: copy structure, GoF groups by the 3 families, Pitfall 4 / A2 -- NO leaf rows in Wave 1)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-03 two sibling catalogs; D-05 family grouping index-side)
  </read_first>
  <action>
    Create references/gof-catalog/README.md as a thin index scaffold: an H1 with an "index" suffix,
    a Scope intro paragraph stating it lists the 23 GoF patterns grouped by family and that entry
    content lives in per-pattern leaf files (never inlined here, SKEL-04), the 3 family H2 headings
    ## Creational / ## Structural / ## Behavioral (each with a one-line family gloss), and a
    ## Sources footer stub. Create references/extra-patterns-catalog/README.md the same way but flat
    (no family split): H1 + "index" suffix, a Scope intro stating it lists the 5 Tier-1 extra
    patterns (Kerievsky-grounded, non-GoF-23) on the same 5-section leaf contract, and a ## Sources
    footer stub. CRITICAL (Pitfall 4 / A2): carry family/section headings + intro ONLY -- NO
    leaf-linking rows. Rows that link to not-yet-authored leaves would false-RED check-crossrefs; the
    rows (each = a bare [Name](slug.md) link + the leaf's Applicability first line mirrored verbatim)
    land in Wave 3 (D-05 "Wave 3 finalize"). ASCII-only; public contact larsbrinknielsen@gmail.com
    if any contact is referenced; no verbatim GoF/Kerievsky prose.
  </action>
  <acceptance_criteria>
    - Both READMEs exist: `test -f plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md && test -f plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md`.
    - gof-catalog/README.md carries the 3 family headings: `rg -q "^## Creational" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md && rg -q "^## Structural" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md && rg -q "^## Behavioral" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md`.
    - Neither README carries a bare leaf-linking row yet: `! rg -q "\]\([a-z0-9][a-z0-9-]*\.md\)" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md` and the same for extra-patterns-catalog/README.md (no `](slug.md)` rows -- Wave-3 baseline preserved).
    - `node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` exits 0 (the README scaffolds add no dangling links); `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII + email clean over references/**).
    - `rg -n "[^\x00-\x7F]" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md` returns no hits.
  </acceptance_criteria>
  <verify>
    <automated>test -f plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md && test -f plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md && rg -q "^## Creational" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md && ! rg -q "\]\([a-z0-9][a-z0-9-]*\.md\)" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>Both catalog READMEs exist as thin scaffolds -- gof-catalog with the 3 family headings + intro, extra-patterns-catalog flat -- with NO leaf-linking rows (Wave-3 rows deferred), check-crossrefs + check-hygiene GREEN, ASCII-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored harness/checker files -> public GitHub repo | anything committed is world-readable; the harness stays in the NON-shipped .claude/skills/lz-refactor-workspace/ (never shipped with the plugin) |
| README scaffolds -> shipped skill tree | the two READMEs ship under references/; they carry only original prose + public pattern names (no GoF text) |
| npm registry -> workspace devDependency | no NEW dependency enters this phase; the sole dep typescript@6.0.3 is pre-existing + Phase-7/8-verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-02 | Information Disclosure | work email in any committed checker / README | mitigate | check-hygiene work-email allowlist hard-fail over references/**; ASCII/email acceptance checks on the .mjs; public contact larsbrinknielsen@gmail.com only |
| T-08.1-03 | Tampering (integrity) | non-ASCII / mojibake corrupting a checker or README | mitigate | ASCII-only acceptance check (`rg -n "[^\x00-\x7F]"` no hits) on every new/edited .mjs + README |
| T-08.1-05 | Tampering | a checker that silently passes (identity-vs-cardinality gap; substring linkify confusing Factory / Factory Method) | mitigate | check-gof/check-extra assert name IDENTITY (each present once); check-kerievsky uses EXACT-name set lookup, never substring; every checker ends with SUMMARY + non-zero exit so a wave merge gates on exit code; RED baseline asserted explicitly |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | NO new package installs this phase (RESEARCH Package Legitimacy Audit: N/A -- zero new packages); sole dep typescript@6.0.3 pre-existing + verified; no registry surface change |

No high-severity threats: this plan ships no runtime, no network, no user input, and no copyrighted
content (checkers hardcode only public pattern names; READMEs carry original prose only). Nothing
rises to the block_on: high bar; no package-legitimacy checkpoint is needed (no installs).
</threat_model>

<verification>
- check-gof.mjs reports 0/23 and check-extra-patterns.mjs reports 0/5 on the empty catalogs (exit 1 -- expected RED baseline); both are wired into the package.json check chain after check-kerievsky.
- check-kerievsky.mjs reports the RED baseline (~23 un-linkified tokens) via the set-membership linkify branch; its other axes stay GREEN.
- check-crossrefs.mjs sources gof-catalog + extra-patterns-catalog and stays GREEN over the current tree (mutuality Fowler-scoped); extract-samples.mjs walks both new catalogs and stays GREEN.
- Both READMEs are thin scaffolds (family/section headings + intro, NO leaf rows); check-crossrefs + check-hygiene GREEN.
- check-catalog.mjs is unchanged; the harness lives only under .claude/skills/lz-refactor-workspace/; all new/edited files are ASCII-only.
</verification>

<success_criteria>
- The full instrument set for GOF-01..04 + XTR-01 exists (check-gof, check-extra-patterns, the check-kerievsky linkify branch, the extended check-crossrefs + extract-samples, the wired package.json) and gates on exit code.
- The expected RED baseline is established and asserted: check-gof 0/23, check-extra-patterns 0/5, check-kerievsky RED on ~23 un-linkified tokens -- not a regression.
- Both catalog READMEs are thin scaffolds ready for Wave-3 row finalization; no new runtime dependency; all tooling ASCII-only and inside the non-shipped workspace.
</success_criteria>

<output>
Create `.planning/phases/08.1-gof-design-patterns-catalog/08.1-01-SUMMARY.md` when done, recording the RED baseline counts (check-gof 0/23, check-extra-patterns 0/5, check-kerievsky RED token count), the exact harness diffs, and confirmation that check-catalog.mjs + check-hygiene.mjs are unchanged.
</output>
