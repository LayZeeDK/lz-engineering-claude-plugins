---
phase: 03-lz-tpp-skill-authoring
plan: 01
type: execute
wave: 2
depends_on: [03-02, 03-03]
files_modified:
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05, SKILL-06]

must_haves:
  truths:
    - "Explicit /lz-tdd:lz-tpp invocation lands in reference mode and explains the transformations plus their priority ordering with rationale, sourced from the LOCKED transformations.md (SKILL-04; D-03 reference route, D-05 anti-dogma)"
    - "A failing test + current code prompt routes to coach mode and yields a NAMED next transformation via the concrete 7-step decision procedure, including the impasse/backtrack move (SKILL-03; D-04)"
    - "The skill auto-triggers on TDD / red-green-refactor / transformation-priority / TPP contexts via a scoped third-person description (<= 1024 chars) and does NOT over-trigger on generic write-a-function requests (SKILL-02, SKILL-05; D-10)"
    - "SKILL.md body stays well under ~500 lines / ~1.5-2k words; the full 14-item list, the worked example, and the TS/TCO depth live in references/ and are reached by pointers only (SKILL-06; D-02)"
    - "Default frontmatter only: disable-model-invocation, user-invocable, version, and allowed-tools are ABSENT, so the skill both auto-triggers as coach and is user-invocable as reference (SKILL-02; D-03)"
    - "The body frames TPP as a heuristic (deviation with a stated reason is legitimate), states the transformations-vs-refactorings distinction in one line, and carries the TS/JS overlay as a one-line source-sanctioned note, not dogma (D-05, D-09)"
    - "The body never instructs auto-editing or auto-running the user's code except when explicitly asked -- coach, don't drive (D-04; PROJECT out-of-scope boundary)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      provides: "Dual-mode router + amended RGR framing + 7-step coach decision procedure + impasse/backtrack heuristic + transformations-vs-refactorings one-liner + TS/JS overlay note + anti-dogma framing + reference pointer block"
      contains: "name: lz-tpp"
      min_lines: 60
  key_links:
    - from: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-tpp/references/transformations.md"
      via: "relative markdown link in the reference-material pointer block (reference-mode corpus)"
      pattern: "references/transformations.md"
    - from: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md"
      via: "relative markdown link in the reference-material pointer block"
      pattern: "references/fibonacci-worked-example.md"
    - from: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md"
      via: "relative markdown link in the reference-material pointer block"
      pattern: "references/typescript-and-tco.md"
---

<objective>
Replace the Phase-1 placeholder SKILL.md with the real, lean, dual-mode lz-tpp skill: a
scoped triggering description plus a body that routes by intent (coach vs reference), carries
the 7-step coach decision procedure and the impasse/backtrack heuristic, states the
transformations-vs-refactorings distinction and the TS/JS overlay in one line each, frames
TPP anti-dogmatically, and points at the three bundled reference files.

Purpose: SKILL.md is the always-on-trigger tier and the operational heart of the skill; it
must satisfy SKILL-01..06 while pushing all heavy material to references/ (progressive
disclosure). It is the consumer that wires the two new reference files (built in Wave 1) plus
the LOCKED transformations.md into the two runtime modes.
Output: A validated plugins/lz-tdd/skills/lz-tpp/SKILL.md (default frontmatter, no version
field, description <= 1024 chars, body under budget, live pointers to all three reference
files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md
@.planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md
@.planning/research/FEATURES.md

<interfaces>
Reference files this SKILL.md points at (names LOCKED for this phase; two are built in Wave 1):
- plugins/lz-tdd/skills/lz-tpp/references/transformations.md  (Phase-2 LOCKED; the reference-mode corpus)
- plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md  (built in 03-02)
- plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md  (built in 03-03)

Progressive-disclosure rule (RESEARCH, VERIFIED against current docs 2026-07-02): references
must be ONE level deep from SKILL.md -- SKILL.md links each of the three files directly, and no
reference file links to another reference file.

Frontmatter field rules (RESEARCH / STACK): name <= 64 chars, lowercase/hyphens; description
non-empty and <= 1024 chars; name and description must NOT contain XML tags or the words
"claude"/"anthropic"; OMIT version, disable-model-invocation, user-invocable, allowed-tools.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the lean dual-mode SKILL.md (frontmatter + body)</name>
  <files>plugins/lz-tdd/skills/lz-tpp/SKILL.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md (the Phase-1 placeholder being replaced)
    - .planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md (D-02 body contents, D-03 dual-mode default frontmatter, D-04 7-step procedure, D-05 anti-dogma, D-09 TS/JS overlay, D-10 description posture)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md ("Code Examples": frontmatter skeleton, description posture draft, the 7-step Coach Decision Procedure to embed, and the Pattern 1 pointer-block shape; "Component Responsibilities"; "Anti-Patterns to Avoid")
    - .planning/research/FEATURES.md (Part 1 anti-features; Part 2 "Coach Decision Procedure", "The Amended Red-Green-Refactor Process", transformations-vs-refactorings, Word Wrap impasse)
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (reference-mode corpus the body points at; do not restate the full 14-item list in the body)
  </read_first>
  <action>
    Overwrite SKILL.md. Frontmatter: keep name: lz-tpp; write a scoped, third-person description
    (well under 1024 chars) that fires on red-green-refactor / TDD / "next transformation" /
    "make the failing test pass" / Transformation Priority Premise / transformation-priority
    contexts and on explicit "explain TPP" requests, and explicitly states it is NOT for generic
    "write a function" / "add a feature" / plain refactoring. Model the wording on the RESEARCH
    "Description posture" draft but tighten it; do not pad toward the cap. Do NOT add
    disable-model-invocation, user-invocable, version, or allowed-tools keys. Ensure the
    description contains no angle-bracket tags and neither the word "claude" nor "anthropic".

    Body (lean, pointer-driven -- never inline the full list, the worked example, or the TCO
    depth): (1) a short dual-mode router that sends a failing-test-plus-code prompt to coach mode
    and an "explain TPP / why this order / what does (X) mean" prompt to reference mode; (2) a
    one-line transformations-vs-refactorings distinction (transformations change behavior to pass
    a red test; refactorings change structure only and are not priority-ranked); (3) the amended
    red-green-refactor bullets (prefer higher-priority transformations when passing a test; pose
    tests passable by higher-priority transformations; backtrack when only a low-priority
    transformation fits); (4) the 7-step Coach Decision Procedure as an explicit numbered workflow
    (confirm green phase -> enumerate candidate minimal changes -> classify and map each to the
    canonical list -> recommend and NAME the highest-priority available transformation -> apply the
    TS/JS overlay when it changes the pick -> impasse check (pose a simpler test, or a
    structure-only refactor, before forcing a low-priority transformation; Word Wrap is the
    illustration) -> show the minimal diff and let the human apply and run it, never editing code or
    running tests unless explicitly asked); (5) a one-line TS/JS overlay note (prefer (if -> while)
    and (variable -> assignment) above the recursion transformations because JS/TS lacks reliable
    TCO -- a source-sanctioned heuristic with a stated reason, not dogma); (6) an anti-dogma
    framing line (the ordering is informal, roughly ordered, language-specific; deviation with a
    stated reason is legitimate); (7) a "Reference material" pointer block with three relative
    markdown links -- references/transformations.md (full 14-item list + rationale), then
    references/fibonacci-worked-example.md (worked walk), then references/typescript-and-tco.md
    (paired TS examples + TCO guidance).

    Reference-mode behavior text must direct the reader to explain the list and ordering rationale
    FROM references/transformations.md (do not duplicate the list in the body). Keep all arrows as
    ASCII "->". Keep the body well under ~500 lines / ~1.5-2k words.
  </action>
  <verify>
    <automated>claude plugin validate ./plugins/lz-tdd</automated>
  </verify>
  <acceptance_criteria>
    - `test -f plugins/lz-tdd/skills/lz-tpp/SKILL.md` succeeds and `claude plugin validate ./plugins/lz-tdd` exits 0.
    - Frontmatter name equals the skill directory name: `rg -n '^name: lz-tpp$' plugins/lz-tdd/skills/lz-tpp/SKILL.md` matches exactly one line.
    - A non-empty `description` field is present, contains no `<...>` angle-bracket tags, and contains neither "claude" nor "anthropic" (case-insensitive).
    - The `description` field VALUE is <= 1024 characters (extract the frontmatter description scalar and measure; a small temp Node .mjs is acceptable per repo shell rules -- do not use a fragile inline node -e with backticks or $ expansion).
    - No forbidden frontmatter keys: `rg -n '^(version|disable-model-invocation|user-invocable|allowed-tools):' plugins/lz-tdd/skills/lz-tpp/SKILL.md` returns nothing.
    - Body budget: `rg -c '' plugins/lz-tdd/skills/lz-tpp/SKILL.md` reports < 500 lines; word count target ~<= 2000 words.
    - Coach procedure present as a numbered workflow: the body contains all seven step cues -- at minimum tokens for "green phase", "enumerate", "classify"/"map", "highest-priority", "overlay", "impasse", and "show" (verify each via `rg -n`).
    - Named transformation coaching: the body instructs NAMING the transformation and contains at least the tokens `(constant -> scalar)` and `(if -> while)` as examples (`rg -F` each).
    - Impasse/backtrack heuristic present and references Word Wrap as the illustration (`rg -in 'word wrap'` matches; `rg -in 'simpler test'` matches).
    - Anti-dogma framing present: `rg -in 'heuristic'` matches and there is no "ALWAYS use transformation" mandate (`rg -in 'always use transformation'` returns nothing).
    - Coach-not-drive guard: any imperative to edit code or run tests is qualified -- `rg -in 'unless (you are |explicitly )?asked'` matches, and there is no unguarded "edit the file" / "run the tests" directive in the coach body (manual confirm plus `rg -in 'run the tests'` reviewed in context).
    - Reference pointer block links all three files: `rg -F 'references/transformations.md'`, `rg -F 'references/fibonacci-worked-example.md'`, and `rg -F 'references/typescript-and-tco.md'` each match in SKILL.md.
    - Link targets exist (no dangling pointers): `test -f` succeeds for each of the three linked reference files.
    - ASCII-only: `rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-tpp/SKILL.md` returns nothing.
    - transformations.md untouched: `git diff --exit-code -- plugins/lz-tdd/skills/lz-tpp/references/transformations.md` exits 0.
  </acceptance_criteria>
  <done>SKILL.md is a lean, ASCII-only, default-frontmatter dual-mode skill with a scoped <=1024-char description, the 7-step coach procedure, the impasse heuristic, the one-line distinctions/overlay, and live pointers to all three reference files; `claude plugin validate ./plugins/lz-tdd` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Full mechanical + hygiene verification of SKILL.md and the skill tree</name>
  <files>plugins/lz-tdd/skills/lz-tpp/SKILL.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md (the file just authored)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md ("Validation Architecture": Phase Requirements -> Test Map, the mechanical check list, and the Wave 0 gaps that define these commands)
    - .planning/phases/02-tpp-source-distillation/02-LEARNINGS.md (the mechanical-check pattern, the work-email allowlist gate that never spells the work email, and the line-wrap brittleness note for phrase-presence checks)
  </read_first>
  <action>
    Run the full mechanical gate battery over SKILL.md and the skill directory, then run
    `claude plugin validate .` at the repo root. If any check fails, fix the issue in SKILL.md in
    place (formatting or wording only -- do not weaken the scope or drop required content) and
    re-run until green. If every check passes on the first pass, record the passing checks in the
    plan SUMMARY rather than forcing a no-op edit (verified-no-op pattern from Phase 2). The email
    allowlist gate MUST be written so it never spells the private work-email literal: enumerate all
    email addresses in the changed files, filter out the approved public gmail, and assert an empty
    remainder.
  </action>
  <verify>
    <automated>claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - Full SKILL.md gate battery from Task 1 acceptance re-run and green (frontmatter parse, <=1024-char description, forbidden-key absence, line/word budget, procedure/token presence, three live reference links, ASCII gate).
    - No-nested-reference guard from SKILL.md's perspective is satisfied: SKILL.md links the three reference files directly and the reference files are reached one level deep only.
    - Work-email allowlist gate green: enumerating emails in the changed files and filtering out `larsbrinknielsen@gmail.com` yields no remaining address (command must not contain the work-email literal). Suggested: `rg -oIN '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' plugins/lz-tdd/skills/lz-tpp/SKILL.md | rg -v '^larsbrinknielsen@gmail\.com$'` returns nothing.
    - `claude plugin validate .` at the repo root exits 0 with no errors.
    - transformations.md unchanged: `git diff --exit-code -- plugins/lz-tdd/skills/lz-tpp/references/transformations.md` exits 0, and transformations.md is absent from this plan's files_modified.
  </acceptance_criteria>
  <done>All mechanical, hygiene, and validator gates pass for SKILL.md and the skill tree; any failures were fixed in place; verified-no-op recorded in SUMMARY if nothing needed changing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SKILL.md -> public git repo | Committed skill content becomes a public artifact; hygiene and fidelity errors ship publicly. |
| skill coach output -> user's codebase | The coach recommends changes a human may apply; overreach (auto-edit/auto-run) would cross into driving the user's code. |
| description metadata -> session trigger surface | The description is the sole always-loaded trigger; over-broad wording mis-fires and erodes trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | SKILL.md committed content | mitigate | Work-email allowlist gate (Task 2) that filters out the public gmail and asserts no remaining address; check never spells the work-email literal. |
| T-03-01-02 | Elevation of Privilege / Tampering | Coach body instructions | mitigate | Enforce coach-don't-drive: the body presents a diff and lets the human apply/run; any edit/run directive is qualified with "unless explicitly asked" (D-04; verified by rg gate + manual review in Task 1/2). |
| T-03-01-03 | Spoofing (trust erosion via mis-trigger) | Frontmatter description | mitigate | Scoped, third-person description with explicit should-NOT-trigger clause for generic coding; <= 1024 chars, no padding (D-10). Empirical tuning deferred to Phase 5. |
| T-03-01-04 | Tampering (source-fidelity drift) | Reference-mode routing | mitigate | Reference mode explains FROM the LOCKED transformations.md; the body does not restate the list; transformations.md-unchanged git guard. |
| T-03-01-05 | Tampering (integrity/hygiene) | Non-ASCII bytes | mitigate | ASCII-only gate (`rg -n '[^\x00-\x7F]'`); arrows as `->`. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (Markdown-only deliverable; RESEARCH Package Legitimacy Audit = not applicable). No supply-chain surface to mitigate. |
</threat_model>

<verification>
- `claude plugin validate .` and `claude plugin validate ./plugins/lz-tdd` both exit 0.
- Frontmatter: name == lz-tpp; description present, <= 1024 chars, no XML tags, no reserved words; version / disable-model-invocation / user-invocable / allowed-tools absent.
- Body under ~500 lines / ~1.5-2k words; 7-step procedure, impasse heuristic, distinctions, overlay note, anti-dogma framing, and three live reference links all present.
- ASCII-only; work-email absent (allowlist gate); transformations.md unchanged.
</verification>

<success_criteria>
- ROADMAP Phase 3 SC1 (coach recommends a named next transformation, incl. backtracking/simpler test at impasse) and SC2 (explicit invocation returns a reference explanation with rationale) are satisfied by the SKILL.md body routing and content.
- ROADMAP Phase 3 SC5 (lean SKILL.md, <= 1024-char description, heavy material in references/) is satisfied and mechanically verified.
- SKILL-01..06 are all addressed by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-lz-tpp-skill-authoring/03-01-SUMMARY.md` when done.
</output>
