---
context: phase
phase: 14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k
task: 5
total_tasks: 5
status: phase_complete_pending_post_completion_audits
last_updated: 2026-07-15T14:13:26.842Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> These were discovered through actual failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: Phase 14 is DONE + verified + committed -- do NOT re-run metered evals or re-execute plans. The 18 head-to-head runs (results/recommend/**/cr-*/) and 14-RESULTS.md are committed. The remaining work is RETROACTIVE audits on existing artifacts. Re-running would waste metered spend for zero benefit. Structural mitigation: run only /gsd-secure-phase, /gsd-validate-phase, /gsd-extract-learnings (which read artifacts, never invoke `claude -p`); if any tool tries to launch run-e2e.mjs, stop.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-running a completed metered phase | Phase 14 is complete + committed; re-executing plans or re-running run-e2e would re-spend for nothing | blocking | Post-completion sequence is secure/validate/extract only -- all read artifacts, none spend on `claude -p`. Verify a SUMMARY exists for each plan before considering any re-run. |
| Spend-limit thrash | Org monthly spend-limit hit twice mid-run this session; retrying in a loop just re-fails | advisory | Run audits one at a time; on a spend/rate-limit sentinel, STOP and resume the specific audit after reset -- artifacts are durable. |
| Kata run-e2e path bug | `--suite ../e2e-gilded-rose` overshoots (run-e2e.mjs:57 resolves --suite vs CWD); correct is `--suite e2e-gilded-rose` from the workspace root | advisory | Only matters if evals are ever re-run (they should NOT be here). Documented in 14-04-SUMMARY. |
| Stale auto-chain flag | `workflow._auto_chain_active: true` is still set in .planning/config.json (uncommitted) | advisory | Run the audits as PLAIN commands (not --auto); each GSD workflow's "sync chain flag with intent" step clears it when invoked without --auto/--chain. |

<current_state>
Phase 14 (head-to-head: lz-refactor recommend vs mattpocock code-review) is COMPLETE: all 5 plans
executed + committed, gsd-verifier PASSED 12/12, phase marked complete via `phase.complete` (commit
8130de0), is_last_phase of milestone lz-tdd@0.0.2. The deliverable 14-RESULTS.md carries the empirical
verdict, unbiased-reviewer-confirmed. Paused deliberately BEFORE the mandatory post-completion audit
sequence to run it in a fresh, low-context session (this session hit the org spend-limit twice).
</current_state>

<completed_work>

Completed (all committed):
- 14-01 (d0b4d37): run-e2e.mjs `code_review` competitor arm + token/tool meta capture + `--synthetic-base`
  whole-file-diff builder + offline `selfcheck-code-review.mjs` (nx + kata cruxes). Self-check exit 0.
- 14-02 (03bf58d): 3 report-framed `cr-*` recommend queries + suite wiring; 2 subagent reviewers (1
  unbiased) PASS, no prompt edits needed.
- 14-03 (932a466): `tabulate-mechanical.mjs` (tokens/cost/tools/spawn/lift/Pass@k) + fixtures + 7-dimension
  rollup template. `--selfcheck` exit 0.
- 14-04 (55fce02): 18 metered recommend runs behind the D-12 gate (USER-APPROVED approve-3cell), all exit
  0; A1 (headline = total_cost_usd/model_usage, rolls up sub-agents) + A2 (code_review spawns 1-2
  sub-agents; lz-refactor 0) recorded; both borrowed repos left pristine.
- 14-05 (8130de0): all 7 dimensions graded -- mechanical.json + claims.json + fidelity.json (oracle,
  DST-04, no fails) + summary.json + 14-RESULTS.md; unbiased reviewer PASS; hygiene clean.
- gsd-verifier PASSED 12/12 (14-VERIFICATION.md); phase.complete run.
</completed_work>

<remaining_work>

Run in a fresh session, in this exact order (all read existing artifacts; none re-run evals):
- post-1: `/gsd-secure-phase 14` -> gsd-security-auditor verifies the PLAN threat mitigations
  (T-14-01/02 borrowed-repo teardown, T-14-05 D-12 spend gate, T-14-03/04/08 PII + DST-04 + plugin
  disclosure) exist -> writes 14-SECURITY.md. (security_enforcement is enabled.)
- post-2: `/gsd-validate-phase 14` -> gsd-nyquist-auditor audits Nyquist coverage, flips 14-VALIDATION.md
  `nyquist_compliant`/`wave_0_complete` true, fills any gaps. The 3 offline Wave-0 self-checks are the
  automated verification; the oracle/judge-graded dims are Manual-Only (already documented).
- post-3: `/gsd-extract-learnings 14` -> 14-LEARNINGS.md. LAST of the phase sequence (captures everything
  secure/validate surface). Then bridge to the global store per CLAUDE.md if features.global_learnings.
- post-4: `/gsd-audit-milestone lz-tdd@0.0.2` (all phases 6-14 complete). Commit the audit.
- post-5: `/gsd-complete-milestone lz-tdd@0.0.2`.
</remaining_work>

<decisions_made>

- Phase 14 empirical verdict (differently-shaped work, not a skill win): lz-refactor is the book-grounded
  refactoring specialist (named Fowler/Kerievsky refactorings, FP-axis reach, net-cost APPLY/DECLINE
  proportionality, ~30-45% cheaper, 0 sub-agents); code_review is a broader general reviewer with distinct
  off-axis value (caught a real regex bug in 1/3 cr-emb runs; Spec-axis kata faithfulness). ~139-vs-12
  vocabulary breadth is the idiom/pattern headline. Both missed T4 (groupImports reduce->Map).
- D-12 metered spend gate was explicitly USER-approved (approve-3cell = 18 runs), never self-approved.
- Kata command fix: `--suite e2e-gilded-rose` not `../e2e-gilded-rose` (run-e2e.mjs:57 resolves vs CWD).
</decisions_made>

<blockers>
- Org monthly spend-limit: hit twice this session, reset by the user both times. Not currently blocking,
  but the post-completion audits spawn agents that spend -- run one at a time; resume the specific audit
  after any reset (artifacts are durable). No current hard blocker.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` -- machine-readable state (completed/remaining/decisions/blockers).
2. `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESULTS.md` -- the deliverable + verdict + caveats (D-02 whole-file-diff, D-03 Spec-axis skip, A1 token boundary, A4 plugin inflation).
3. `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-VERIFICATION.md` -- the 12/12 goal-achievement report (feeds the milestone audit 3-source cross-ref).
4. `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-CONTEXT.md` -- decisions D-01..D-12 (secure/validate audit against these).
5. `CLAUDE.md` "GSD Workflow" rules -- the mandatory secure->validate->extract order + the dedicated-agent (no self-certify) rule + the auto_copy_learnings global-store bridge.

## Critical Anti-Patterns (do NOT repeat these)
- Re-running the 18 metered evals or re-executing phase-14 plans -> zero benefit, wasted spend. They are committed.
- Self-certifying the secure/validate gates inline -> spawn gsd-security-auditor and gsd-nyquist-auditor; the orchestrator only routes their verdicts.

## Infrastructure State
- Borrowed repos pristine: nx `D:/projects/github/nrwl/nx @ origin/23.0.x`, kata
  `D:/projects/github/emilybache/GildedRose-Refactoring-Kata @ main` -- both `git status` clean, no
  `review-*` residue.
- `.planning/config.json` uncommitted (carries `workflow._auto_chain_active: true`).
- No running background processes tied to this work.

<context>
The whole discuss->plan->execute->verify chain is done and durable. The only reason for the pause is
context hygiene + spend-limit volatility: the retroactive audits deserve a clean, low-context session so
they don't get cut off mid-run. Nothing is in flight. The empirical result is the headline: against a real
external code-review skill (not just base Opus), lz-refactor's book-grounded catalog measurably differs
(richer named-refactoring lift, FP reach, proportionality, lower cost), while code_review brings
complementary bug-finding + spec-verification -- use them for different jobs.
</context>

<next_action>
Start with: `/clear`, then `/gsd-secure-phase 14`. Then `/gsd-validate-phase 14`, then
`/gsd-extract-learnings 14`, then `/gsd-audit-milestone lz-tdd@0.0.2`, then
`/gsd-complete-milestone lz-tdd@0.0.2`. Run each as a plain command (not --auto). Do NOT re-run any eval.
</next_action>
