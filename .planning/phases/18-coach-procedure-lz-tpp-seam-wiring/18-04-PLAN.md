---
phase: 18-coach-procedure-lz-tpp-seam-wiring
plan: 04
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
autonomous: true
requirements: [SEAM-02]
must_haves:
  truths:
    - "D-09: BOTH reverse pointers -- lz-tpp -> lz-red (red step) and the deferred lz-tpp -> lz-refactor (refactor step) -- are added to lz-tpp/SKILL.md in ONE additive edit; lz-tpp had no cross-skill pointer section before."
    - "D-10: the edit is authored BLIND own-words; the >= 1-unbiased-from-scratch review is an ORCHESTRATOR gate at finalize (18-06), NOT an executor step; going live needs a human /reload-plugins at Phase-19 ship (not a task here)."
    - "D-12: lz-tpp/SKILL.md is EXCLUDED from the no-verbatim scan, so this prose relies on own-words authoring + the D-10 review; it IS covered by the ASCII + work-email scans."
  artifacts:
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md
  key_links:
    - "the new lz-tpp section names lz-red (red step) and lz-refactor (refactor step) so all three skills point at each other -- the red-green-refactor seam is fully wired"
    - "the checker SEAM-02 block (18-01) asserts both /lz-red/ and /lz-refactor/ appear in lz-tpp/SKILL.md"
---

<objective>
Close the carried reverse-pointer tech-debt: add a short red-green-refactor-seam pointer section to the
SHIPPED `lz-tpp/SKILL.md`, naming lz-red (the red step) and lz-refactor (the refactor step) in ONE
edit (D-09, ROADMAP SC4). lz-tpp currently has NO cross-skill pointer section, so this is purely
additive. After it lands, all three skills point at each other and the red-green-refactor seam is fully
wired.

This edits a LIVE shipped skill. It is authored BLIND own-words and is gated by an ORCHESTRATOR
subagent review (>= 1 unbiased from-scratch) at finalize (18-06) -- the executor does NOT spawn the
reviewer (gsd-executor has no Agent/Task tool). Making the edit LIVE requires a human /reload-plugins,
deferred to the Phase-19 ship (NOT a task here). lz-tpp is EXCLUDED from the no-verbatim gate, so
own-words authoring + the D-10 review are the only backstops for verbatim leakage; ASCII + work-email
scans DO cover it.

Purpose: SEAM-02 -- the reverse half of the classify-first seam. Output: one additive pointer section
in lz-tpp/SKILL.md.

## Decisions implemented (D-NN)

- **D-09:** add both pointers (lz-tpp -> lz-red and the deferred lz-tpp -> lz-refactor) in ONE edit, mirroring the sibling "vs the green step" sections lz-red and lz-refactor already carry; placement + phrasing are executor discretion.
- **D-10:** author blind own-words; the >= 1-unbiased review is an orchestrator gate at 18-06; /reload-plugins is a Phase-19 human action.
- **D-12:** own-words authoring (lz-tpp is out of the no-verbatim scan); rely on the D-10 review for verbatim safety; stay ASCII-only and work-email-free (both scans cover lz-tpp).

## Artifacts produced

- `lz-tpp/SKILL.md`: a NET-NEW red-green-refactor-seam pointer section (a heading + short prose) naming lz-red and lz-refactor from the GREEN vantage point.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-CONTEXT.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-PATTERNS.md
@plugins/lz-tdd/skills/lz-tpp/SKILL.md
@plugins/lz-tdd/skills/lz-red/SKILL.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the red-green-refactor-seam reverse-pointer section to lz-tpp/SKILL.md</name>
  <files>plugins/lz-tdd/skills/lz-tpp/SKILL.md</files>
  <read_first>plugins/lz-tdd/skills/lz-tpp/SKILL.md, plugins/lz-tdd/skills/lz-red/SKILL.md, plugins/lz-tdd/skills/lz-refactor/SKILL.md</read_first>
  <action>
Add ONE short cross-skill pointer section to lz-tpp/SKILL.md, placed after the existing
"## Transformations vs refactorings" section (lines 30-34) -- executor discretion on exact placement
and phrasing. Write it from the GREEN vantage point, mirroring the shape of the sibling sections
lz-red carries ("## RED vs the green step (lz-tpp) and the refactor step (lz-refactor)", lines 36-42)
and lz-refactor carries ("## Refactoring vs the green step (the lz-tpp seam)", lines 37-41). Author
BLIND own-words.

The section must state, as pointers (not restatements of the sibling skills' content):
- lz-tpp is the green / transformation step: making a failing (red) test pass by changing behavior.
- The red step -- choosing and writing the next failing test -- is lz-red.
- The refactor step -- restructuring passing code without changing behavior -- is lz-refactor.
- Classify the request first: if there is no failing test yet, that is lz-red; if the tests are green
  and the code needs cleanup, that is lz-refactor.

The section body must contain the tokens `lz-red` and `lz-refactor` (the SEAM-02 checker block asserts
both). Keep it short (a heading + one short paragraph), consistent with lz-tpp's lean ~81-line grain.
ASCII-only, no work-email. Do not restate lz-red's or lz-refactor's procedures -- point, do not copy.
Do not touch any other part of lz-tpp/SKILL.md.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "SEAM-02|lz-tpp" ; node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs SEAM-02 block reports PASS (both `lz-red` and `lz-refactor` appear in lz-tpp/SKILL.md).
    - check-hygiene.mjs exits 0 (lz-tpp/SKILL.md stays ASCII-only and work-email-free).
    - `claude plugin validate .` exits 0.
    - The new section is additive (a single heading + short paragraph); no other part of lz-tpp/SKILL.md is changed; it points at lz-red / lz-refactor without restating their content.
  </acceptance_criteria>
  <done>lz-tpp/SKILL.md carries a single additive reverse-pointer section naming lz-red and lz-refactor; the SEAM-02 block and hygiene / validate gates pass; the edit is queued for the D-10 orchestrator review at 18-06.</done>
</task>

</tasks>

<orchestrator_gate>
The shipped lz-tpp/SKILL.md edit MUST be reviewed by >= 1 UNBIASED from-scratch subagent BEFORE
acceptance (D-10, SC4, and the standing rule that every SKILL.md change is subagent-reviewed). This is
an ORCHESTRATOR-driven gate that runs AFTER the executor returns its blind edit -- gsd-executor has no
Agent/Task tool, so the executor does NOT spawn it. The gate is documented and run at finalize (18-06).
Making the edit LIVE is a human `/reload-plugins` at the Phase-19 ship, NOT a task in this phase.
</orchestrator_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content -> shipped LIVE skill | lz-tpp is a shipped, user-facing skill; an unreviewed or verbatim-leaking edit ships to users |
| authored content -> committed public repo | maintainer work-email/domain and non-ASCII must not enter committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Info disclosure (IP) | lz-tpp/SKILL.md reverse-pointer prose (EXCLUDED from the no-verbatim gate) | medium | mitigate | Own-words blind authoring + the D-10 >= 1-unbiased orchestrator review; there is NO deterministic no-verbatim backstop for lz-tpp, so the review carries it. |
| T-18-02 | Info disclosure | lz-tpp/SKILL.md | medium | mitigate | check-hygiene allowlist-inversion work-email scan (lz-tpp is in the wideTargets set). |
| T-18-03 | Tampering (integrity) | lz-tpp/SKILL.md | low | mitigate | check-hygiene ASCII scan (lz-tpp is in the wideTargets set). |

No high-severity runtime threats: lz-tpp/SKILL.md is Markdown that executes no code. ASVS level 1;
block on `high` (none exist).
</threat_model>

<verification>
- check-red-references.mjs SEAM-02 block PASS (both reverse pointers present).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- `claude plugin validate .` exits 0.
- Orchestrator gate (18-06): >= 1 unbiased from-scratch review of the lz-tpp diff.
</verification>

<success_criteria>
lz-tpp/SKILL.md carries a single additive reverse-pointer section naming both lz-red and lz-refactor,
the SEAM-02 checker block and the ASCII / work-email / validate gates pass, and the edit is queued for
the D-10 orchestrator review (live at the Phase-19 reload).
</success_criteria>

<output>
Create `.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-04-SUMMARY.md` when done.
</output>
