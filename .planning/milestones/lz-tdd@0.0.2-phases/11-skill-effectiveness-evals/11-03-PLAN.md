---
phase: 11-skill-effectiveness-evals
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/evals/evals.json
  - .claude/skills/lz-refactor-workspace/grade-run.mjs
autonomous: true
requirements: [EVL-02]
user_setup: []

must_haves:
  truths:
    - "**D-03:** evals/evals.json follows the skill-creator behavior posture (skill_name lz-refactor; per-eval {id (0-based contiguous), prompt, expected_output, files:[], expectations[]}); each scenario is tests-GREEN code carrying a recognizable smell (NO failing test -- a failing test is the lz-tpp green step, out of scope). The with-skill vs baseline (no-skill), unnamed fire-and-forget subagents in isolated scratch cwds, and the grade -> judge -> merge -> verify -> aggregate chain are the presented RUN mechanic (gated per D-10), not executed here."
    - "**D-04-RUBRIC:** grade-run.mjs grades name + layer DETERMINISTICALLY per the HYBRID rubric -- a recommendation SCORED-PASSES when it names a refactoring in the detected smell's candidate set (per references/smells.md leaf) via `{candidateSet:[names]}` (one-to-many tolerance; a correct-but-alternative in-set pick still passes) OR names the pinned single best-fit via `{bestFit:name}` where the scenario's details pin one answer, AND `{layer: Fowler|Kerievsky|Kerievsky-Away|functional}` resolves the named refactoring to the correct layer via the name->layer lookup; `{nodrive:true}` asserts the coach did NOT edit code or run tests (no Edit/Write/MultiEdit/NotebookEdit/Bash); `{judge:question}` is used ONLY for rationale quality (passed:null, resolved by the grader agent + merge-judge). The RUBRICS<->evals.json count-alignment selfcheck (RUBRICS[id].length === expectations[id].length, Pitfall-6) is PRESERVED."
    - "**D-05:** EVL-02 scenarios are sourced PRIMARILY from shipped catalog leaves' Use-when selectors + before/after examples (ground truth pre-proven), spanning BOTH layers -- several Fowler mechanical (Long Function -> Extract Function; Long Parameter List -> Introduce Parameter Object; Duplicated Code in siblings -> Pull Up Method; Feature Envy -> Move Function) and several Kerievsky pattern-directed (Conditional Complexity -> Replace Conditional Logic with Strategy / Replace State-Altering Conditionals with State; Combinatorial Explosion -> Replace Implicit Language with Interpreter) -- with >= 1 de-patterning case (unwarranted Singleton -> Inline Singleton OR Module Namespace) and >= 1 routing-boundary case (the SAME repeated switch across sites -> Fowler Replace Conditional with Polymorphism, NOT a Kerievsky pattern)."
    - "grade-run.mjs --selfcheck exits 0 (SELFCHECK OK); every candidateSet/bestFit name in RUBRICS resolves in the name->layer lookup; the lookup is derived from the 3 shipped catalog READMEs (Fowler 62 -> Fowler; Kerievsky 27 -> Kerievsky + the 3 Away rows also -> Kerievsky-Away; functional idioms -> functional)."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/evals/evals.json"
      provides: "EVL-02 scenario set (>= 8 scenarios; skill_name lz-refactor; expectations count-aligned 1:1 with RUBRICS)"
      contains: "\"skill_name\": \"lz-refactor\""
    - path: ".claude/skills/lz-refactor-workspace/grade-run.mjs"
      provides: "Deterministic grader: nameRe matcher + candidateSet/bestFit/layer/nodrive/judge checks + RUBRICS + name->layer lookup + alignment selfcheck"
      contains: "SELFCHECK"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/grade-run.mjs"
      to: ".claude/skills/lz-refactor-workspace/evals/evals.json"
      via: "RUBRICS[id].length === expectations[id].length alignment selfcheck (Pitfall-6)"
      pattern: "expectations"
    - from: ".claude/skills/lz-refactor-workspace/grade-run.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references"
      via: "name->layer lookup derived from fowler-catalog / kerievsky-catalog / functional-catalog READMEs; candidate sets from smells/*.md leaves"
      pattern: "layer"
---

<objective>
Author the EVL-02 behavior scenario set and rewrite the deterministic grader for lz-refactor's one-to-many smell->refactoring mapping. This is the only genuine authoring work in the phase: the grade-run.mjs matcher + RUBRICS + name->layer lookup.

Purpose: EVL-02 measures whether the coach recommends the correct next NAMED refactoring in the correct layer, with-skill vs baseline. The grader encodes the D-04-RUBRIC hybrid so grading is scripted and reusable across iterations.
Output: evals/evals.json (>= 8 leaf-sourced scenarios) and grade-run.mjs (rewritten, selfcheck GREEN). Nothing is RUN (D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skill-effectiveness-evals/11-RESEARCH.md
@.planning/phases/11-skill-effectiveness-evals/11-PATTERNS.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md

Ground-truth sources (shipped, tracked):
- plugins/lz-tdd/skills/lz-refactor/references/smells/*.md (candidate sets per smell leaf)
- plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (62 names -> Fowler)
- plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md (27 names -> Kerievsky; Direction=Away rows -> Kerievsky-Away)
- plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md (idiom names -> functional; Singleton -> Module Namespace)

Analog (gitignored -- read with Read/rg/Glob, NEVER git grep):
- .claude/skills/lz-tpp-workspace/evals/evals.json (schema shape)
- .claude/skills/lz-tpp-workspace/grade-run.mjs (skeleton to keep; matcher + RUBRICS to replace)

<interfaces>
grade-run.mjs check kinds to implement (per 11-PATTERNS.md "grade-run.mjs" section):
- { candidateSet: [names] } -> scored pass if the response names ANY in-set refactoring (names.some(n => nameRe(n).test(resp))).
- { bestFit: name } -> scored pass only if the pinned name is present (nameRe(name).test(resp)).
- { layer: "Fowler"|"Kerievsky"|"Kerievsky-Away"|"functional" } (single OR array for the de-patterning case) -> scored pass if the named refactoring resolves to an accepted layer via the name->layer lookup.
- { nodrive: true } -> reuse the analog toolDrive() verbatim (drove = any Edit/Write/MultiEdit/NotebookEdit/Bash > 0; no metrics -> fail; unknown shape -> fail).
- { judge: "question" } -> reuse verbatim (passed:null; resolved by grader agent + merge-judge). Rationale quality ONLY.
grading.json output shape is unchanged (what aggregate_benchmark.py reads): { expectations:[{text,passed,evidence}], summary:{passed,failed,total,pass_rate}, preliminary, judge_required }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author evals/evals.json (>= 8 leaf-sourced behavior scenarios)</name>
  <files>.claude/skills/lz-refactor-workspace/evals/evals.json</files>
  <read_first>
    - 11-RESEARCH.md "EVL-02 Scenario Candidates" table (ids 0-9: scenario -> smell leaf -> candidate set -> best-fit -> layer, with the design notes for id 7 de-patterning and id 8 routing-boundary)
    - plugins/lz-tdd/skills/lz-refactor/references/smells/{long-function,long-parameter-list,duplicated-code,feature-envy,conditional-complexity,combinatorial-explosion,repeated-switches}.md (the ## Candidate refactorings list per leaf = the D-04-RUBRIC candidate set)
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (coach decision procedure lines 38-70 = layer-routing + "Coach, don't drive" ground truth)
    - .claude/skills/lz-tpp-workspace/evals/evals.json (schema shape; note expectations count == RUBRICS[id] check count)
  </read_first>
  <action>Create evals/evals.json with skill_name "lz-refactor" and an evals array of >= 8 scenarios (author the RESEARCH ids 0-8; id 9 no-tests is optional, add only if keeping 9). Each scenario: id (0-based contiguous), prompt describing tests-GREEN code carrying a specific smell and asking for the next refactoring (NO failing test -- a failing test is the lz-tpp green step), expected_output naming the best-fit refactoring + layer in one human-readable line, files: [] (prompt-only), and expectations: an array of prose strings, ONE per grade-run.mjs RUBRICS[id] check IN THE SAME ORDER (candidateSet check, bestFit check where pinned, layer check, judge check(s), nodrive check). Lift each scenario's smell + candidate set + best-fit + layer from its anchoring leaf per the RESEARCH table; keep the code snippets minimal and ASCII. Ensure the set spans both layers with the D-05-required de-patterning case (id 7) and routing-boundary case (id 8). Keep prompts self-contained and concrete.</action>
  <verify>
    <automated>python -m json.tool .claude/skills/lz-refactor-workspace/evals/evals.json > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Valid JSON; skill_name is "lz-refactor"; evals is an array of >= 8 objects with 0-based contiguous ids.
    - Every scenario has prompt, expected_output, files:[], and a non-empty expectations array.
    - Scenarios span both layers; >= 1 de-patterning case (unwarranted Singleton) and >= 1 routing-boundary case (repeated switch -> Fowler) present.
    - Every named refactoring in a scenario resolves to a real shipped leaf (Fowler / Kerievsky / functional).
    - Each expectations array is count-aligned to its RUBRICS[id] (validated by the grade-run selfcheck in Task 2).
    - ASCII-only; allowlist-inversion email scan clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build name->layer lookup + rewrite grade-run.mjs; selfcheck GREEN</name>
  <files>.claude/skills/lz-refactor-workspace/grade-run.mjs</files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/grade-run.mjs (the skeleton: KEEP toolDrive, scoreCheck nodrive/judge branches, grade() preliminary/judge_required assembly, finalOutPath, parseArgs, main, and the selfcheck STRUCTURE incl. the RUBRICS<->evals.json alignment loop; REPLACE transformRe and the RUBRICS object)
    - 11-PATTERNS.md "grade-run.mjs (deterministic grader, HEAVY REWRITE)" (exact keep/replace line map, the nameRe matcher shape, the 5 check kinds, the illustrative RUBRICS[2] shape, the id-7 de-patterning layer-set)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md, kerievsky-catalog/README.md, functional-catalog/README.md (source the name->layer lookup: Fowler 62 -> Fowler; Kerievsky 27 -> Kerievsky, the 3 Direction=Away rows also -> Kerievsky-Away; functional idiom names -> functional; Singleton dissolves to Module Namespace)
  </read_first>
  <action>Rewrite grade-run.mjs keeping the analog skeleton verbatim (toolDrive, nodrive/judge scoreCheck branches, preliminary/judge_required assembly, finalOutPath, parseArgs, main, and the selfcheck structure). Replace two skill-specific pieces: (1) delete the lz-tpp transformRe (a->b token matcher) and add nameRe(canonical) -- a case-insensitive, whitespace-tolerant (\s+ between words), word-bounded (no \w or - on either side) proper-noun phrase matcher per PATTERNS; (2) replace the RUBRICS object with lz-refactor's per-eval-id checks using the five kinds candidateSet / bestFit / layer / nodrive / judge, one RUBRICS[id] array per evals.json scenario, count-aligned 1:1 with that scenario's expectations. Build the name->layer lookup (a map literal in grade-run.mjs or a sibling data module it imports) from the three catalog READMEs; the id-7 de-patterning check accepts candidateSet ["Inline Singleton","Module Namespace"] and layer ["Kerievsky-Away","functional"]. Add judge checks ONLY for rationale quality (tie-to-smell; id-8 Fowler-over-pattern justification; id-7 unwarranted-pattern framing) -- never judge what name+layer already covers. Extend the selfcheck to exercise candidateSet any-match, bestFit exact-match, layer resolve-via-lookup (Fowler + Kerievsky + Away + functional), the nodrive nested/flat/miskeyed/no-metrics cases (copy those assertions verbatim), the finalOutPath preliminary path, and the RUBRICS<->evals.json alignment loop (RUBRICS[e.id].length === e.expectations.length for every id -- KEEP the relative evals/evals.json path resolution). Widen the --eval-id range in usage/validation to the lz-refactor eval count. Add a selfcheck assertion that every candidateSet/bestFit name resolves in the name->layer lookup. grading.json output shape stays what aggregate_benchmark.py reads.</action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/grade-run.mjs --selfcheck</automated>
  </verify>
  <acceptance_criteria>
    - `node grade-run.mjs --selfcheck` prints SELFCHECK OK and exits 0.
    - The selfcheck asserts RUBRICS[id].length === evals.json expectations[id].length for every id (fails loudly on drift -- Pitfall-6).
    - The selfcheck asserts every candidateSet/bestFit name resolves in the name->layer lookup.
    - nameRe is word-bounded (does not sub-match, e.g. "Extract Function" != "Extract Functionality") and whitespace-tolerant.
    - toolDrive / nodrive branches are byte-identical to the analog (fail-safe on no metrics, fail-loud on unknown shape).
    - id-7 accepts both Inline Singleton (Kerievsky-Away) and Module Namespace (functional); id-8 pins Replace Conditional with Polymorphism at the Fowler layer.
    - ASCII-only; allowlist-inversion email scan clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored scenario prose -> local git tree | New committed scenarios (with code snippets), local only |
| grade-run.mjs -> local JSON + response text | Parses maintainer-authored evals.json + (at RUN time) model response text / metrics.json; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Information Disclosure | committed evals.json scenario prose + code snippets | mitigate | ASCII-only; allowlist-inversion email scan before commit; no secrets/PII in prompts. |
| T-11-06 | Tampering | grade-run.mjs parses local JSON + response text | mitigate | Fail-safe (no metrics -> fail) + fail-loud (unrecognized shape -> fail); RUBRICS<->evals.json alignment selfcheck (Pitfall-6); name-resolve selfcheck. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; grade-run.mjs uses only Node stdlib. Low severity, no ASVS L1 install surface. |
</threat_model>

<verification>
- python -m json.tool validates evals.json structurally.
- node grade-run.mjs --selfcheck GREEN (matcher + RUBRICS + layer lookup + alignment + name-resolve).
- No behavior benchmark, subagent run, or claude -p invocation runs in this plan (D-10).
</verification>

<success_criteria>
- evals/evals.json exists with >= 8 leaf-sourced scenarios spanning both layers incl. de-patterning + routing-boundary.
- grade-run.mjs rewritten (nameRe + candidateSet/bestFit/layer/nodrive/judge + name->layer lookup), selfcheck GREEN, alignment + name-resolve preserved.
- ASCII-only, work-email-free; no writes into plugins/ or .planning/.
</success_criteria>

<output>
Create `.planning/phases/11-skill-effectiveness-evals/11-03-SUMMARY.md` when done.
</output>
