---
phase: 21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md
autonomous: false
requirements:
  - EVL-03
must_haves:
  truths:
    - "The full deterministic battery is GREEN on the merged tree, zero spend: grade-red --selfcheck, tabulate-mechanical-red --selfcheck, merge-judge --selfcheck, selfcheck-red, and the existing check-red-references / extract-samples / check-evals all exit 0 (EVL-03.1-.6 BUILD closure)."
    - "The borrowed repos are pristine (git status --porcelain clean on the kata git root) and plugins/lz-tdd is untouched after the whole build -- the skill under test is unmodified (D-12; SC4)."
    - "The metered 3-arm apply RUN is user-gated and NOT executed in this phase: RUN-GATE.md presents the ready-to-run commands and the phase HALTS for fresh explicit approval (D-11 / D-12 / eval-run-approval-gate)."
    - "The gated first round runs the THREE own-skill arms (no_skill / with_skill genuine auto-trigger via --plugin-dir / invoke_skill force-invoke /lz-tdd:lz-red) on the Conjured anchor; the mattpocock-skills:tdd competitor is a LATER, CONTINGENT round only after own lift is measured, with its red->green scope-mismatch noted (D-04/D-05)."
    - "The corpus size (~2-3 targets x k=3; the discriminating 2nd/3rd target) is confirmed at the gate against the RESEARCH qualification checklist, and Pass@k/Pass^k (k=1,3,5,total) are reported per target + overall (D-01/D-03)."
    - "Run-time orchestration hygiene is documented for the gated run: small waves (org concurrency cap / 24-in-flight), an explicit 'normal mode / stop ponytail' directive in every in-session subagent prompt, resume spend-limit-killed agents via SendMessage, and each run driven in an isolated git-ignored worktree/dir (ground-truth nodrive) (D-13)."
    - "The results path is fixed: after the gated run, tabulate-mechanical-red + the <= 2-dim blind judge (blinded to arm) + oracle-reviewer fill EVAL-RESULTS.md, then >= 1 from-scratch UNBIASED reviewer audits the numbers with a substance-only headline before they are recorded (D-08/D-09/D-10)."
    - "If a NEW real-OSS target is nominated at the gate, the package-legitimacy gate is run on it (npm install of a real, maintained repo) before vendoring (RESEARCH gate note; T-21-SC)."
  artifacts:
    - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md
  key_links:
    - "RUN-GATE.md commands <-> run-e2e.mjs --suite e2e-red-gilded-rose --mode apply --arm all (the reused driver) + grade-red + tabulate-mechanical-red + merge-judge + oracle-reviewer"
    - "the HALT <-> eval-run-approval-gate (no metered claude -p without fresh explicit user approval)"
---

<objective>
Close the BUILD: prove the whole instrument GREEN offline, confirm the borrowed repos are pristine
and plugins/lz-tdd is untouched, then HALT at the run gate. The metered 3-arm apply RUN is a separate,
freshly-approved step -- this plan writes down the exact ready-to-run commands, the corpus/target
confirmation, the run-time hygiene, and the post-run results path, and STOPS. Nothing metered runs
during execute-phase.

Purpose: the instrument-first discipline (D-12 / eval-run-approval-gate) -- build + zero-spend
selfcheck freely, gate the spend. This is the same "build complete; empirical run gated" closure
EVL-01/EVL-02 used. The HALT is mandatory: a metered run spends real tokens and must not start
without explicit fresh approval.

Output: the full-battery-GREEN proof, the pristine/untouched attestation, and RUN-GATE.md (the
ready-to-run gated commands + confirmation checklist + hygiene + results path). No metered run.

## Locked decisions honored
- **D-01:** Confirm the discriminating 2nd/3rd target at the gate against the RESEARCH qualification checklist (Conjured is the fixed anchor); the user steers. If a NEW real-OSS repo is nominated, run the package-legitimacy gate on it before vendoring.
- **D-03:** ~2-3 targets x k=3, exact count tuned at the gate for spend; report Pass@k AND Pass^k (k=1,3,5,total) per target + overall.
- **D-05:** The mattpocock-skills:tdd competitor arm is a LATER, CONTINGENT round -- only after own lift is measured, and only because the standalone check passed (no hard sibling dep). Note the scope mismatch (mattpocock tdd is a full red->green loop with an interactive seam-confirmation step vs lz-red's RED-step-only coach). NOT in the first fan-out.
- **D-10:** >= 1 from-scratch UNBIASED reviewer (neutral brief) audits the results before they are recorded -- mandatory; it caught the Phase-20 vocabulary inflation.
- **D-11:** Reuse the lz-refactor apply harness for the run; the run is user-gated ([[eval-run-approval-gate]]).
- **D-12:** (HARD GATE) the metered apply RUN is user-gated -- HALT for fresh approval; nothing metered runs in this phase. Per-run byproducts git-ignored; NO dependency added to plugins/lz-tdd.
- **D-13:** Run-time orchestration hygiene -- small waves (org cap / 24-in-flight); an explicit "normal mode / stop ponytail" directive in every in-session Agent subagent prompt (the env-var lock does not reach subagents); resume spend-limit-killed agents via SendMessage; drive each run in an isolated git-ignored worktree/dir (ground-truth nodrive).

## Executor note (tool limits)
The judge / oracle-reviewer / unbiased-reviewer are ORCHESTRATOR-driven steps that run AFTER the
gated metered run (the gsd-executor has no Agent/Task tool). This plan only DOCUMENTS them in
RUN-GATE.md; it spawns no subagent and runs no eval.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-RESEARCH.md
@.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-CONTEXT.md

# The Phase-20 halt precedent to mirror (present commands, then STOP):
@.claude/skills/lz-red-workspace/EVAL-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase-gate -- prove the full deterministic battery GREEN + repos pristine + plugins/lz-tdd untouched</name>
  <files>(verification only -- runs the selfcheck battery + attests pristine/untouched; modifies no source files)</files>
  <read_first>
    - .claude/skills/lz-red-workspace/{grade-red.mjs, tabulate-mechanical-red.mjs, selfcheck-red.mjs, merge-judge.mjs} (the selfchecks to run)
    - .claude/skills/lz-red-workspace/package.json (VERIFY the actual scripts: `check` runs ONLY `node tools/check-red-references.mjs`; `typecheck` runs `node extract-samples.mjs`; `check-evals.mjs` is a SEPARATE script NOT wired into any npm script -- do not assume `run check` spawns all three)
  </read_first>
  <action>
    Run the full offline battery on the merged tree and record the results (no metered spend):
    1. `node .claude/skills/lz-red-workspace/grade-red.mjs --selfcheck`
    2. `node .claude/skills/lz-red-workspace/tabulate-mechanical-red.mjs --selfcheck`
    3. `node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck`
    4. `node .claude/skills/lz-red-workspace/selfcheck-red.mjs`
    5. The existing workspace deterministic battery, as THREE SEPARATE steps (the `check` npm script runs ONLY `node tools/check-red-references.mjs` -- it does NOT spawn extract-samples or check-evals; those exist as standalone scripts): `npm --prefix .claude/skills/lz-red-workspace run check` (= check-red-references, the reference-completeness gate), then `node .claude/skills/lz-red-workspace/extract-samples.mjs` (the RED-samples tsc --strict gate; also the `typecheck` npm script), then `node .claude/skills/lz-red-workspace/check-evals.mjs` (the trigger/behavior eval-JSON lint). Then `claude plugin validate .`.
    Assert ALL exit 0. Then attest: `git status --porcelain` on the kata git root is clean (borrowed repo pristine after every worktree teardown), and `git status --porcelain plugins/lz-tdd` is empty (the skill under test is unmodified). If any selfcheck is RED, STOP and fix before proceeding to Task 2 -- the gate must be GREEN before the HALT.
    Record the battery results in the SUMMARY. Nothing here spends claude tokens.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/grade-red.mjs --selfcheck && node .claude/skills/lz-red-workspace/tabulate-mechanical-red.mjs --selfcheck && node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck && node .claude/skills/lz-red-workspace/selfcheck-red.mjs && npm --prefix .claude/skills/lz-red-workspace run check && node .claude/skills/lz-red-workspace/extract-samples.mjs && node .claude/skills/lz-red-workspace/check-evals.mjs && test -z "$(git status --porcelain plugins/lz-tdd)" && echo OK-battery-green</automated>
  </verify>
  <acceptance_criteria>
    - grade-red --selfcheck, tabulate-mechanical-red --selfcheck, merge-judge --selfcheck, and selfcheck-red all exit 0.
    - The workspace deterministic battery exits 0 as SEPARATE steps: `npm --prefix .claude/skills/lz-red-workspace run check` (check-red-references only), `node .claude/skills/lz-red-workspace/extract-samples.mjs` (RED-samples tsc gate), and `node .claude/skills/lz-red-workspace/check-evals.mjs` (eval-JSON lint); `claude plugin validate .` exits 0.
    - The kata git root is pristine (`git status --porcelain` clean); no leftover review/red worktree or branch.
    - `git status --porcelain plugins/lz-tdd` is empty (skill under test untouched).
    - Zero claude spend during the gate.
  </acceptance_criteria>
  <done>The whole instrument is GREEN offline, the borrowed repos are pristine, and plugins/lz-tdd is unmodified -- the BUILD sub-criteria (EVL-03.1-.6) are closed and the phase is ready to HALT.</done>
</task>

<task type="auto">
  <name>Task 2: Write RUN-GATE.md -- the ready-to-run gated commands, target confirmation, hygiene, and results path</name>
  <files>.claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md</files>
  <read_first>
    - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/{suite.json, targets.json, EVAL-RESULTS.md} (the corpus + the discriminating_target_checklist + the scaffold to fill post-run)
    - .planning/phases/21-.../21-RESEARCH.md ("Pitfall 8" concurrency/hygiene; the D-01 qualification checklist; the differential-tsc + runner gate; D-05 contingent competitor)
    - .planning/phases/21-.../21-CONTEXT.md (D-11/D-12/D-13 run-time constraints)
  </read_first>
  <action>
    Create `e2e-red-gilded-rose/RUN-GATE.md` -- the presentation of the gated metered run. It PRESENTS
    commands and STOPS; it must not be executed during execute-phase. Include:
    - A bold HALT banner: "METERED RUN -- requires fresh explicit user approval (eval-run-approval-gate). Do NOT run any command below during execute-phase."
    - Pre-run confirmation checklist (D-01): confirm the anchor (Conjured) + confirm/nominate the discriminating 2nd/3rd target against the 7-point qualification checklist; if a NEW repo is nominated, run the package-legitimacy gate on it and `npm install` it before vendoring.
    - Residual-risk CANARY (runner-JSON shape drift, RESEARCH A1 / Pitfall 4): grade-red --selfcheck exercises the classifier against the workspace's pinned vitest@4.1.10, NOT the kata's actual vitest@^0.28.5 / jest@^29.4.3 that the metered gate shells into. A genuine vitest-0.28 JSON-shape difference would surface only at the metered run (mitigated by grade-red's fail-closed contract, so not a build blocker). Before committing to the full k=3 x 2-3-target spend, run ONE canary: a single real `node .claude/skills/lz-red-workspace/grade-red.mjs --run <one captured kata runDir> --suite .claude/skills/lz-red-workspace/e2e-red-gilded-rose` and confirm the verdict is sane (genuinely_red on the known-red Conjured run, runner + version recorded) -- OR add a `jest` devDependency to the workspace so the fixtures can also exercise the `jest --json` shape offline. State this canary as a REQUIRED gate step before the full fan-out.
    - The exact first-round commands (3 own-skill arms), e.g.: create a throwaway branch checkout of the kata (never the pristine tree), then `node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --suite .claude/skills/lz-red-workspace/e2e-red-gilded-rose --mode apply --arm all --cwd <throwaway kata checkout>/TypeScript --runs 3` (arm all = no_skill + with_skill + invoke_skill). Then per captured run: `node .claude/skills/lz-red-workspace/grade-red.mjs --run <runDir> --suite .../e2e-red-gilded-rose`, then `node .claude/skills/lz-red-workspace/tabulate-mechanical-red.mjs`.
    - The graded-dims path: the blind LLM judge (<= 2 dims: "is THIS the right next test?" + "does it assert observable behavior, not implementation?"), blinded by feeding ONLY the produced test code + the target behavior spec (no arm label, no skill self-identification -- Pitfall 7); merge via `merge-judge.mjs --merge` + `--verify`; oracle-reviewer for book/source authenticity vs .oracle/ (DST-04); then fill EVAL-RESULTS.md.
    - The competitor round (D-05): mattpocock-skills:tdd as a LATER CONTINGENT round only after own lift is measured; note the red->green scope mismatch.
    - Run-time hygiene (D-13): small waves (org cap); "normal mode / stop ponytail" in every subagent prompt; resume spend-limit kills via SendMessage; isolated git-ignored worktree per run.
    - Post-run: >= 1 from-scratch UNBIASED reviewer audits the numbers with a substance-only headline before recording (D-08/D-10).
    ASCII-only; no work-email/domain. This file is documentation; running it is out of scope for this phase.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md','utf8');for(const k of ['HALT','approval','arm all','no_skill','with_skill','invoke_skill','unbiased','ponytail','mattpocock','canary'])if(!new RegExp(k,'i').test(t))throw('missing '+k);console.log('OK-run-gate')"</automated>
  </verify>
  <acceptance_criteria>
    - RUN-GATE.md exists with a HALT banner requiring fresh explicit approval (eval-run-approval-gate) and states no command runs during execute-phase.
    - It contains the exact first-round 3-arm command (`run-e2e.mjs --suite e2e-red-gilded-rose --mode apply --arm all --cwd <throwaway checkout> --runs 3`) + the per-run grade-red + tabulate-mechanical-red steps.
    - It documents the D-01 target-confirmation checklist, the blinded <= 2-dim judge + merge/verify + oracle-reviewer path, the D-05 contingent mattpocock round + scope mismatch, the D-13 run-time hygiene, and the D-10 unbiased-reviewer + substance-only headline.
    - It documents the runner-JSON shape-drift residual risk (fixtures pin vitest@4.1.10 vs the kata's vitest@^0.28.5 / jest@^29.4.3) and REQUIRES a single canary `grade-red --run` against one real kata run before the full spend (or the jest-devDependency offline alternative).
    - ASCII-only; no work-email/domain.
  </acceptance_criteria>
  <done>RUN-GATE.md is the complete, ready-to-run gated-run presentation -- commands, target confirmation, hygiene, and the results/judge/oracle/unbiased path -- so the run can start immediately on fresh approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: HALT -- gated metered-run approval gate (eval-run-approval-gate)</name>
  <action>Blocking-human gate (never auto-approvable; workflow.auto_advance is ignored). Present the BUILT + GREEN instrument and RUN-GATE.md for review; automate NOTHING further -- do not run any metered claude -p, grade, judge, or oracle-reviewer step here. The metered 3-arm apply run and the subsequent judge / oracle-reviewer / unbiased-reviewer gates are a separate, orchestrator-driven step that starts only on explicit fresh approval per RUN-GATE.md. This is the build-then-halt boundary (D-12 / eval-run-approval-gate).</action>
  <what-built>
    The RED apply instrument is BUILT and GREEN offline (grade-red + tabulate-mechanical-red + selfcheck-red + merge-judge selfchecks all exit 0; borrowed repos pristine; plugins/lz-tdd untouched), EVL-03 is formalized, and RUN-GATE.md holds the ready-to-run 3-arm metered commands. NOTHING metered has run (D-12 / eval-run-approval-gate).
  </what-built>
  <how-to-verify>
    1. Review RUN-GATE.md at `.claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md` -- the first-round 3-arm command, the target-confirmation checklist, and the results/judge/oracle/unbiased path.
    2. Confirm the discriminating 2nd/3rd target (D-01): keep the Conjured anchor alone at k=3-5 for a first round (backup), OR nominate a second emilybache kata / a real-OSS repo passing the 7-point qualification checklist (run the package-legitimacy gate on any new repo first).
    3. Decide the run scope for spend (D-03: ~2-3 targets x k=3) and whether to include the contingent mattpocock tdd competitor round (D-05: only after own lift is measured).
    4. Only on explicit approval does the metered run start (a separate step, orchestrator-driven, per RUN-GATE.md); the judge/oracle-reviewer/unbiased-reviewer gates run after it.
  </how-to-verify>
  <resume-signal>Reply "approved -- run <target(s)> at k=<N>" to start the gated metered run, or describe changes to the instrument/targets first. Until then the phase is BUILD-complete and HALTED.</resume-signal>
  <done>The human has reviewed the built instrument + RUN-GATE.md and either approved the gated metered run (a separate orchestrator-driven step) or requested instrument/target changes; no metered spend occurred during execute-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gated run -> claude -p (bypassPermissions) | the metered run drives model-generated test code + third-party OSS suites under bypassPermissions in disposable worktrees (gated; not run this phase) |
| nominated target repo -> workspace | a NEW real-OSS target nominated at the gate is npm-installed + vendored (supply-chain surface) |
| committed RUN-GATE.md -> public repo | tracked doc (maintainer-PII info-disclosure surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Elevation of Privilege / Tampering | metered apply run (run-e2e --mode apply, bypassPermissions) | medium | mitigate | Run is GATED (no spend without fresh approval); each run is a disposable worktree reset to applyBase; process-tree kill on timeout (reused rig); driven in an isolated git-ignored checkout, never the pristine tree |
| T-21-SC | Tampering (supply chain) | npm/vendor of a gate-time-nominated target repo | high | mitigate | RUN-GATE.md requires the package-legitimacy gate on ANY newly nominated target (real, maintained repo verified) before vendoring/`npm install`; the anchor kata is already vendored + verified; this checkpoint is blocking-human and never auto-approvable |
| T-21-03 | Information Disclosure | RUN-GATE.md | low | mitigate | ASCII-only + allowlist-inversion (no maintainer work-email/domain); per-run transcripts git-ignored |
</threat_model>

<verification>
- Full offline battery GREEN (grade-red / tabulate-mechanical-red / merge-judge / selfcheck-red selfchecks + the separate workspace scripts `npm run check` [check-red-references] / `node extract-samples.mjs` / `node check-evals.mjs` + `claude plugin validate .` all exit 0).
- Kata git root pristine; `git status --porcelain plugins/lz-tdd` empty.
- RUN-GATE.md present with the HALT banner, the 3-arm command, target confirmation, hygiene, and the judge/oracle/unbiased path.
- The blocking-human checkpoint is reached; NO metered claude -p run occurred.
</verification>

<success_criteria>
The BUILD is closed and GREEN offline, the borrowed repos are pristine and plugins/lz-tdd is untouched,
EVL-03's BUILD sub-criteria are proven, and the phase HALTS at a blocking-human gate with the exact
ready-to-run 3-arm metered commands -- no tokens spent, awaiting fresh explicit approval.
</success_criteria>

<output>
Create `.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-04-SUMMARY.md` when done.
</output>
