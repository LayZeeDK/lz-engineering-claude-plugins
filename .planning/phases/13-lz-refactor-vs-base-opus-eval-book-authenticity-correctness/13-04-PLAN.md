---
phase: 13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - .claude/skills/lz-refactor-workspace/grading/correctness/name-layer.json
  - .claude/skills/lz-refactor-workspace/grading/correctness/behavior.json
autonomous: true
requirements: [SPEC-req-4]
user_setup: []

must_haves:
  truths:
    - "**D-07:** correctness has TWO scored sub-parts per run per arm -- (a) named refactoring at the right LAYER is correct (Fowler mechanical / Kerievsky pattern-directed / functional), graded from the diff against each target's expected_family + judgment in targets.json (single-target: nx p1->T1, p2->T2, kata gr1->G1; sweeps have NO targets.json entry so grade each applied refactoring against the constituent judgments -- nx p8 -> p7sweep + T1-T4; kata gr4 -> G1sweep); and (b) behavior-preserving, verified INDEPENDENTLY by re-applying the persisted diff.patch to a FRESH throwaway checkout and running the repo's ORIGINAL tests against the EDITED source (the Phase-12 D-14 behavior oracle), NOT trusting the in-session self-reported run: nx = differential vs the 13-01 ~15-fail baseline; kata = jest --ci vs the 13-01 pristine golden master. Record name_layer_correct y/n + tests_green y/n per run per arm."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/grading/correctness/name-layer.json"
      provides: "name_correct + layer_correct y/n per run per arm vs targets.json / constituent judgments"
      contains: "name_layer_correct"
    - path: ".claude/skills/lz-refactor-workspace/grading/correctness/behavior.json"
      provides: "tests_green y/n per run per arm from the independent behavior oracle (nx differential / kata --ci) + Pass@k"
      contains: "tests_green"
  key_links:
    - from: "results/apply/<arm>/<pN>/run-<k>/diff.patch"
      to: "grading/correctness/name-layer.json"
      via: "diff vs targets.json expected_family/judgment (sweeps: p7sweep/G1sweep constituent judgments)"
      pattern: "name_layer_correct"
    - from: "results/apply/<arm>/<pN>/run-<k>/diff.patch"
      to: "grading/correctness/behavior.json"
      via: "git apply to a fresh checkout; run ORIGINAL tests on EDITED source (nx differential / kata --ci)"
      pattern: "tests_green"
---

<objective>
Grade correctness for both arms on every graded cell, differenced vs base: (a) name/layer correctness from each run's diff against targets.json (single-target) or the constituent sweep judgments (p7sweep/G1sweep), and (b) behavior-preservation via the INDEPENDENT behavior oracle -- re-apply each persisted diff.patch to a fresh throwaway checkout and run the repo's ORIGINAL tests against the EDITED source, using the differential nx check and the pristine-seeded kata --ci check from 13-01.

Purpose: correctness (right named refactoring/layer + behavior-preserving) was only ever verified for the skill arms; there is no base delta and the in-session self-reported test runs are not trusted. This produces the correctness half of the head-to-head. Behavior grading is local test runs (no metered claude -p spend). This plan is SELF-SUFFICIENT off its declared 13-02 dependency (each run's own diff.patch/answer.md/meta.json) so it can run in parallel with 13-03 in wave 3.
Output: grading/correctness/name-layer.json (name/layer y/n per run per arm) and grading/correctness/behavior.json (tests_green y/n per run per arm + Pass@k), both tracked + ASCII-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESEARCH.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-PATTERNS.md

Correctness ground truth (OUR repo files -- read the relevant target rows):
@.claude/skills/lz-refactor-workspace/e2e-nx/targets.json
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/targets.json

Behavior-oracle baselines (from 13-01):
- nx differential reference: .claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json (~15 failed / 169 passed + failing spec names)
- kata golden master: .claude/skills/lz-refactor-workspace/e2e-gilded-rose/golden-master/approvals.spec.ts.snap + behavior-baseline.md (jest --ci reuse recipe)

Graded diffs: results/apply/{with_skill,invoke_skill,no_skill}/{p1,p2,p8,gr1,gr4}/run-{1..3}/diff.patch (+ answer.md, meta.json). These are OUR wave-2 output (declared 13-02 dependency) -- everything this plan needs is here; do NOT depend on 13-03.
Borrowed repos: nx D:/projects/github/nrwl/nx; kata D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript (throwaway branch, not protected main). Exact commands: 13-RESEARCH.md "Code Examples -> REQ-4(b)". Pass@k: run-e2e.mjs passAtK/passHatK.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grade name/layer correctness from the diff vs targets.json</name>
  <files>.claude/skills/lz-refactor-workspace/grading/correctness/name-layer.json</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/e2e-nx/targets.json (rows T1, T2 for single-target; p7sweep + T1-T4 for the p8 sweep) and e2e-gilded-rose/targets.json (G1 for gr1; G1sweep for gr4)
    - each graded run's OWN results/apply/<arm>/<pN>/run-<k>/{diff.patch, answer.md, meta.json} -- the applied-refactoring inventory comes from these (meta.json changed_files bounds the touched files; answer.md reads as a claim list). This plan does NOT require 13-03: if grading/book-authenticity/claims.json happens to already exist (13-03 ran first), it MAY be read as an optimization, but read-the-diffs-directly is the primary, sufficient path.
    - 13-PATTERNS.md "Correctness records -> Ground truth (a) name/layer" and "Sweep correctness has NO direct entry (Pitfall 6)" (grade sweep refactorings against constituent judgments; reuse the Posture A/B/C rubric in p7sweep/G1sweep verbatim)
    - 13-RESEARCH.md "Common Pitfalls -> Pitfall 6" (no pkgsweep/projsweep entry)
  </read_first>
  <action>
    For every graded run of every cell/arm, judge name/layer correctness from each run's OWN diff.patch + answer.md + meta.json (self-sufficient off the declared 13-02 dependency; if grading/book-authenticity/claims.json already exists you MAY reuse its inventory as an optimization, but do NOT hard-require it -- fall back to reading the diffs directly). Single-target: nx p1 -> targets.json T1, nx p2 -> T2, kata gr1 -> G1; a run is name_correct when the applied refactoring is in the target's expected set and layer_correct when it sits in the expected LAYER (Fowler mechanical / Kerievsky pattern-directed / functional), applying each target's judgment trap (e.g. T2 REJECTS introducing Strategy/polymorphism for the 3-value mode param). Sweeps (no targets.json row): grade each applied refactoring in nx p8 against p7sweep + T1-T4 and each in kata gr4 against G1sweep, using the Posture A/B/C (pass/timid/reckless) rubric those judgment fields already encode (behavior-preserving Fowler/functional moves pass; DECLINE speculative patterns; PAUSE on the exported-signature/blast-radius trap for nx and the Conjured/Sulfuras behavior-change trap for kata). Write grading/correctness/name-layer.json keyed by {cell, arm, run} with name_correct y/n, layer_correct y/n, a combined name_layer_correct y/n, and a one-line our-own-words rationale referencing the target id. This is main-context judgment against OUR targets.json + OUR run output -- never read .oracle/. ASCII-only; no work email. Write incrementally.
  </action>
  <verify>
    <automated>node -e "const n=require('./.claude/skills/lz-refactor-workspace/grading/correctness/name-layer.json'); const recs=n.records||n; const bad=recs.filter(r=>!r.cell||!r.arm||r.run==null||r.name_layer_correct==null); if(bad.length){console.error('missing name/layer',bad.length);process.exit(1)} console.log('name/layer graded',recs.length)"</automated>
  </verify>
  <acceptance_criteria>
    - name-layer.json has a name_correct + layer_correct + name_layer_correct verdict per run per arm for all graded cells, derived from each run's own diff/answer/meta (not dependent on 13-03's claims.json)
    - single-target rows cite T1/T2/G1; sweep rows cite p7sweep/T1-T4 (nx) or G1sweep (kata) constituent judgments incl. the DECLINE/PAUSE traps
    - graded against OUR targets.json + run output only (no .oracle/ read); ASCII-only; no work email
  </acceptance_criteria>
  <done>grading/correctness/name-layer.json records name/layer correctness for both arms on every graded cell, using targets.json + constituent sweep judgments, self-sufficient off the 13-02 diffs.</done>
</task>

<task type="auto">
  <name>Task 2: Verify behavior preservation independently (behavior oracle) and roll up Pass@k</name>
  <files>.claude/skills/lz-refactor-workspace/grading/correctness/behavior.json</files>
  <read_first>
    - 13-RESEARCH.md "Code Examples -> REQ-4(b): nx behavior oracle (differential)" and "REQ-4(b): kata behavior oracle (pristine-seeded snapshot with --ci)" (the exact reset/apply/test commands)
    - 13-PATTERNS.md "Shared Patterns -> Behavior oracle" (nx differential vs the ~15-fail baseline; kata jest --ci vs the pristine golden master; ignore test/jest/gilded-rose.spec.ts) and "Pass@k / Pass^k math"
    - .claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json + e2e-gilded-rose/behavior-baseline.md (the 13-01 baselines to compare against)
  </read_first>
  <action>
    For every graded run of every cell/arm, re-verify behavior INDEPENDENTLY (never trust the in-session self-reported run). nx (p1, p2, p8): on the nx clone, git reset --hard origin/23.0.x && git clean -fd, git apply the run's persisted diff.patch, run npx nx test eslint-plugin, filter with rg (never the grep tool), and score tests_green = no NEW failures beyond the 13-01 differential baseline (~15 failed / 169 passed); if the whole-project run dies at the pre-existing import crash, run the affected spec(s) for the touched files directly via raw jest as the prior-art workaround. Reset the clone after each run. kata (gr1, gr4): on the kata throwaway branch, restore pristine source (git checkout -- app/gilded-rose.ts or reset --hard main + clean), ensure the 13-01 golden master is present at test/jest/__snapshots__/approvals.spec.ts.snap (copy it back from golden-master/), git apply the run's diff.patch, run npx jest --ci test/jest/approvals.spec.ts (--ci refuses to write, fails on mismatch), and score tests_green = pass (ignore test/jest/gilded-rose.spec.ts). Runs with an empty diff (no-edit declines) are trivially behavior-preserving = tests_green true (no change) but are flagged as "no refactoring applied". Write grading/correctness/behavior.json keyed by {cell, arm, run} with tests_green y/n + the failure delta note for nx. Then compute per (cell, arm) a Pass@k block {n, c, passAt1, passAt3, passHat3} over tests_green using run-e2e.mjs passAtK/passHatK (do not hand-roll). After all runs, reset both borrowed repos to their base (final canonical restoration is 13-05). ASCII-only; no work email; no .oracle/ read.
  </action>
  <verify>
    <automated>node -e "const b=require('./.claude/skills/lz-refactor-workspace/grading/correctness/behavior.json'); const recs=b.records||b; const bad=recs.filter(r=>!r.cell||!r.arm||r.run==null||r.tests_green==null); if(bad.length){console.error('missing behavior verdict',bad.length);process.exit(1)} console.log('behavior graded',recs.length)"</automated>
  </verify>
  <acceptance_criteria>
    - behavior.json has a tests_green y/n per run per arm for all graded cells, produced by re-applying each diff to a FRESH checkout and running the ORIGINAL tests on the EDITED source
    - nx rows use the differential rule vs the 13-01 ~15-fail baseline (no NEW failures); kata rows use jest --ci vs the 13-01 pristine golden master
    - per (cell, arm) a Pass@k block via run-e2e.mjs passAtK/passHatK; borrowed repos reset to base after; ASCII-only; no work email; no .oracle/ read
  </acceptance_criteria>
  <done>grading/correctness/behavior.json records independent tests_green per run per arm (nx differential / kata --ci) + Pass@k, ready for 13-RESULTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| behavior verification -> the run that produced the diff | must be INDEPENDENT: fresh checkout, ORIGINAL tests on EDITED source, never the in-session self-report |
| borrowed-repo state -> behavior oracle | re-apply happens on throwaway branches; repos reset between runs and returned to base after |
| maintainer identity -> public repo commit | only the approved public gmail may appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | .oracle/ prose in correctness records | mitigate | name/layer grades against OUR targets.json + OUR run output; behavior grades against OUR baselines + repo tests; main context never reads .oracle/; scan asserts no .oracle path/prose |
| T-13-02 | Information Disclosure | maintainer work email/domain in committed correctness records | mitigate | ASCII original-words records; allowlist-inversion email scan before commit (only approved gmail); committer = approved gmail |
| T-13-05 | Tampering | behavior verdict corrupted by a wrong gate (all-green on nx; self-seeded kata snapshot) | mitigate | nx is DIFFERENTIAL vs the recorded 15-fail baseline; kata uses jest --ci against the pristine-seeded golden master (refuses to overwrite) -- both from 13-01 |
| T-13-SC | Tampering | npm/pip installs | accept | phase installs NO packages; nx/jest already present in the borrowed repos' node_modules |
</threat_model>

<verification>
- name-layer.json: name_correct + layer_correct + name_layer_correct per run per arm, single-target vs T1/T2/G1 and sweeps vs p7sweep/G1sweep constituent judgments, derived from the 13-02 diffs (no 13-03 hard dependency)
- behavior.json: tests_green per run per arm from independent re-apply (nx differential vs 13-01 baseline; kata jest --ci vs 13-01 golden master) + Pass@k via run-e2e.mjs
- both records ASCII-only, no .oracle/ path/prose, no work email; borrowed repos reset to base after grading
</verification>

<success_criteria>
Correctness (name/layer + independent behavior preservation) is graded for both arms on every graded cell, differenced vs base, tabulation-ready, self-sufficient off the wave-2 diffs.
</success_criteria>

<output>
Create `.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-04-SUMMARY.md` when done.
</output>
