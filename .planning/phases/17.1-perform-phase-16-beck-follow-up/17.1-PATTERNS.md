# Phase 17.1: Perform Phase 16 Beck follow-up - Pattern Map

**Mapped:** 2026-07-20
**Files analyzed:** 3 reference docs modified + 1 planning note (no new files)
**Analogs found:** 6 / 6 surfaces (every surface has an in-file Phase-17 owned template) + 1 / 1 provenance note

Nature: this is a PROVENANCE-TIER edit, not code. Every "pattern to copy" is an already-shipped
Phase-17 owned-source row/entry in these same files. The executor mirrors those strings token-for-token
onto the Beck-backed no-oracle surfaces. No recommendation CONTENT changes. Content-fidelity vs the
`.oracle/` essays is the oracle-reviewer's blind job downstream, NOT part of this map.

## File Classification

| Modified file | Role | Data flow | Closest analog | Match quality |
|---------------|------|-----------|----------------|---------------|
| `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md` | reference doc (prose) | provenance/tier transform | its own RCM owned blockquote + Sources entry (Phase-16); `anti-patterns.md` Beck-essay owned entries (Phase-17) | exact (in-file) |
| `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md` | reference doc (prose) | provenance/tier transform | its own RCM owned blockquote + Sources entry; `anti-patterns.md` Beck-essay owned entries | exact (in-file) |
| `plugins/lz-tdd/skills/lz-red/references/principle-backing.md` | central provenance map (prose table) | provenance/tier transform | its own Phase-17 owned rows (lines 45, 46, 49, 50, **53 = Beck essay**) | exact (in-file) |
| `.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md` | planning note (prose) | append cross-phase note | its own `## Provenance correction (...)` note (line 9) | exact (in-file) |

## Shared Patterns

### Fixed tier vocabulary (match EXACTLY -- do not paraphrase)

| Tier | Verbatim string |
|------|-----------------|
| Owned (target for an upgraded surface) | `Owned; oracle-verified against the clean-room source.` |
| No-oracle (current; kept for any un-upgraded surface) | `Unowned; high-confidence core only (no-oracle).` |

These two strings are the entire tier flip in the `principle-backing.md` Access-tier cell. In prose
(blockquotes / Sources entries) the owned phrasing appears as `... is/are owned and oracle-verified
against the clean-room source.` and the no-oracle phrasing as `... high-confidence core only ...
(no-oracle tag)` / `Unowned; high-confidence core only (no-oracle)`.

### Analog A -- surface-doc intro blockquote, owned half

In-file template (`three-laws-and-test-selection.md` lines 10-11, the RCM owned half already present):

```
The selection rationale that leans on Robert C. Martin's Clean Code
(the one-small-step / only-enough-to-fail thread) is owned and oracle-verified against the
clean-room source.
```

Richer cross-file template that groups a Kent Beck FREE ESSAY into one owned sentence
(`anti-patterns.md` lines 10-13):

```
> ... the listen-to-the-tests / keep-the-double-honest points (Sandi Metz, The Design of Tests
> talk), and the Test Desiderata lens (Kent Beck's Test Desiderata essay and video series) are owned
> and oracle-verified against the clean-room source. The implementation-detail brittleness thread
> (Vladimir Khorikov) ... are high-confidence core, authored blind with no owned source to check
> against (no-oracle tag).
```

Pattern to mirror: name the upgraded Beck move + its FREE essay, fold it into the "... are owned and
oracle-verified against the clean-room source." sentence; leave any un-upgraded move in the
"... high-confidence core only ... (no-oracle tag)" sentence.

### Analog B -- surface-doc `## Sources` block entry, owned (Beck essay)

Cross-file template for a Kent Beck FREE ESSAY as owned (`anti-patterns.md` lines 170-173):

```
- Kent Beck, Test Desiderata (essay + 12-part video series) -- the good-test properties, presented
  here as a tradeoff lens rather than a checklist. Owned; oracle-verified against the clean-room
  source. ...
```

Simpler in-file template (`three-laws-and-test-selection.md` lines 96-97, RCM owned):

```
- Robert C. Martin, Clean Code (Ch. 9, Unit Tests) -- the one-small-step / only-enough-to-fail
  rationale for shaping the next test. Owned; oracle-verified against the clean-room source.
```

Pattern to mirror: `- Kent Beck, <Free Essay Title> -- <what it backs, own words>. Owned;
oracle-verified against the clean-room source.` The Source name MUST be the free essay (Canon TDD /
First One, Then Many), NOT "Test-Driven Development by Example" (book, summary-only -> D-05 violation).

### Analog C -- `principle-backing.md` map row, owned (Beck essay = the exact template)

Best template (line 53 -- a Kent Beck row citing an ESSAY, not the book):

```
| [Test Desiderata tradeoff lens](anti-patterns.md) | Kent Beck, Test Desiderata (essay + video series); Sandi Metz, The Design of Tests, corroborates the over-testing cost | Owned; oracle-verified against the clean-room source. |
```

Simpler talk-backed templates (lines 45, 46):

```
| [Functional core, output-based assert](testing-stance/functional-core.md) | Gary Bernhardt, Boundaries (talk) | Owned; oracle-verified against the clean-room source. |
| [Query/command message matrix](testing-stance/message-matrix.md) | Sandi Metz, The Magic Tricks of Testing (talk) | Owned; oracle-verified against the clean-room source. |
```

Pattern to mirror: keep the existing `[Recommendation](doc.md)` link text UNCHANGED; change the
Source cell to `Kent Beck, <Free Essay Title>`; change the Access-tier cell to the owned string.

### Analog D -- `principle-backing.md` Sources owned-list, Beck free-essay format

The owned-sources sentence already names a Beck essay as owned (line 68 fragment):
`... Kent Beck (Test Desiderata essay and video series); and Ian Cooper.` Mirror this parenthetical
form to add the newly-owned essays, e.g. `Kent Beck (Canon TDD -- running test list and one small
step; First One, Then Many -- triangulation; Test Desiderata essay and video series)`.

### Eligibility (from `.oracle/written-content/index.md` nav Access column ONLY -- clean-room-safe)

| Essay | File | Access | Gateable? |
|-------|------|--------|-----------|
| Canon TDD | `canon-tdd.md` | free | YES (backs surfaces 1, 2; candidate for 4/5/6) |
| First One, Then Many | `tri-first-one-then-many.md` | free | YES (backs surface 3; candidate for 4) |
| Desirable Unit Tests | `desirable-unit-tests.md` | free | YES (D-04 candidate for 5/6) |
| Composable Tests | `composable-tests.md` | free | YES (D-04 candidate for 5/6) |
| Test-Driven Development: By Example | `book-tdd-by-example.md` | **book** | **NO** (primary source of 5/6; summary-only, D-05 -> those rows likely STAY no-oracle) |

`[VERIFIED: .oracle/written-content/index.md nav table -- no essay prose read]`

---

## Bundled surfaces (read this before the per-surface assignments)

Surfaces 1-4 all live inside ONE bundled Beck clause in the `three-laws` blockquote AND ONE bundled
Beck entry in the `three-laws` Sources block. Surfaces 5-6 likewise share ONE bundled clause +
entry in `test-structure`. Upgrading any subset means SPLITTING the bundle: move upgraded moves into
an owned sentence/entry, leave the rest in the no-oracle sentence/entry.

### `three-laws-and-test-selection.md` -- bundled A (blockquote, lines 9-14, verbatim current)

```
> Mixed-provenance reference. The selection rationale that leans on Robert C. Martin's Clean Code
> (the one-small-step / only-enough-to-fail thread) is owned and oracle-verified against the
> clean-room source. The Kent Beck material (the running test list, the one-step move, the starter
> case, and triangulation) is high-confidence core only, authored blind with no owned copy to check
> against (no-oracle tag). Technique NAMES are kept as plain facts; every definition below is
> written in original words, with no verbatim source prose or code (DST-04).
```

Per-surface fragment inside the bundled Beck clause: `the running test list` (surface 1),
`the one-step move` (surface 2), `and triangulation` (surface 3), `the starter case` (surface 4).

### `three-laws-and-test-selection.md` -- bundled B (Sources block, lines 98-101, verbatim current)

```
- Kent Beck, Test-Driven Development by Example -- the running test list, the one-step move, the
  starter / degenerate case, and triangulation as a test-selection move. Unowned; high-confidence
  core only (no-oracle): there is no owned copy to verify against, so correctness rests on tight core
  scope, the no-verbatim scan, and skill-reviewer review.
```

Split target: upgraded moves become their own owned entries citing the free essay (Analog B); any
un-upgraded move stays in a reduced Beck no-oracle entry naming only what remains.

### `test-structure-and-assertions.md` -- bundled A (blockquote, lines 9-15, verbatim current)

```
> Mixed-provenance reference. Two owned threads are oracle-verified against the clean-room source:
> the one-concept-per-test rationale and the F.I.R.S.T. properties (both Robert C. Martin, Clean
> Code Ch. 9). The Bill Wake (arrange-act-assert), Dan North (given-when-then), Kent Beck
> (assert-first, evident data), and Vladimir Khorikov (the four pillars and the
> output/state/communication assertion styles) material is high-confidence core only, authored blind
> with no owned copy to check against (no-oracle tag). ...
```

Per-surface fragment: `Kent Beck (assert-first, evident data)` covers surfaces 5 and 6. Only edit if
D-04 upgrades (per D-05, likely NOT -- primary source is the book; gate decides).

### `test-structure-and-assertions.md` -- bundled B (Sources block, lines 173-174, verbatim current)

```
- Kent Beck, Test-Driven Development by Example -- assert-first and evident test data. Unowned;
  high-confidence core only (no-oracle).
```

---

## Pattern Assignments -- LOCKED-ELIGIBLE upgrades (D-01, D-02; free essay, upgrade unless gate blocks)

### Surface 1 -- `## Keep a running test list` (three-laws-and-test-selection.md) [D-01, Canon TDD]

Role: SEL selection move. Owned essay: Canon TDD (`canon-tdd.md`, free).

Four provenance places (verbatim current -> mirror the owned analog):
- **A blockquote:** fragment `the running test list` in the bundled Beck clause above -> move into an
  owned sentence citing Canon TDD (Analog A).
- **B Sources entry:** fragment `the running test list` in the bundled Beck entry above -> split out as
  an owned Beck/Canon-TDD entry (Analog B).
- **C map row** (`principle-backing.md` line 29, verbatim current):
  ```
  | [Running test list](three-laws-and-test-selection.md) | Kent Beck, Test-Driven Development by Example | Unowned; high-confidence core only (no-oracle). |
  ```
  -> mirror Analog C: Source cell `Kent Beck, Canon TDD`; tier cell -> owned string. Keep link text.
- **D Sources paragraph** (`principle-backing.md` lines 64-73): add to owned-list (Analog D); remove
  `running test list` from the caveat parenthetical (see "D paragraph" section below).

Asserted tokens in edited text: NONE at risk. The `test list` token (`/test list/i`) also lives in the
heading `## Keep a running test list` (line 19) and body (line 21), which are NOT edited.

### Surface 2 -- `## Take one small step` (three-laws-and-test-selection.md) [D-01, Canon TDD]

Role: SEL selection move. Owned essay: Canon TDD (`canon-tdd.md`, free).

- **A blockquote:** fragment `the one-step move` -> owned sentence (Analog A).
- **B Sources entry:** fragment `the one-step move` -> owned entry (Analog B).
- **C map row** (`principle-backing.md` line 30, verbatim current):
  ```
  | [Take one small step](three-laws-and-test-selection.md) | Kent Beck, Test-Driven Development by Example | Unowned; high-confidence core only (no-oracle). |
  ```
  -> Source cell `Kent Beck, Canon TDD`; tier -> owned.
- **D Sources paragraph:** add to owned-list; note the caveat parenthetical currently OMITS "take one
  small step" (reconcile against the real post-gate row set, not the stale list).

**CONFLATION GUARD (from RESEARCH):** do NOT touch line 31 (`Grow the test only enough to fail` = RCM,
ALREADY owned) or the body line 36 `Distilled rationale (Robert C. Martin, owned; oracle-verified)`.
That is the RCM only-enough-to-fail thread, a DIFFERENT already-owned principle. D-01 upgrades the Beck
one-step MOVE only (its own row, line 30). The RCM attribution on line 36 stays as-is.

Asserted tokens: NONE at risk. `/one (small |single )?step/i` matches the heading `## Take one small
step` (line 30, unedited). (Note: the Sources-block text "one-step" with a hyphen does not match that
regex anyway -- the token's only home is the heading.)

### Surface 3 -- `## Triangulate to select the next test` (three-laws-and-test-selection.md) [D-02, First One, Then Many]

Role: SEL selection move. Owned essay: First One, Then Many (`tri-first-one-then-many.md`, free).

- **A blockquote:** fragment `and triangulation` -> owned sentence (Analog A).
- **B Sources entry:** fragment `and triangulation as a test-selection move` -> owned entry (Analog B).
- **C map row** (`principle-backing.md` line 33, verbatim current):
  ```
  | [Triangulate to the next test](three-laws-and-test-selection.md) | Kent Beck, Test-Driven Development by Example | Unowned; high-confidence core only (no-oracle). |
  ```
  -> Source cell `Kent Beck, First One, Then Many`; tier -> owned. Keep link text `Triangulate to the
  next test` (differs from the heading text -- pre-existing, do not "fix").
- **D Sources paragraph:** add to owned-list (First One, Then Many -- triangulation); remove
  `triangulation` from the caveat parenthetical.

Asserted tokens: NONE at risk. `/triangulat/i` matches the heading (line 74) and body, not edited.

---

## Pattern Assignments -- GATE-DECIDED candidates (D-03, D-04; flip provisionally, gate confirms or reverts)

Provisionally flip these using the same Analog A/B/C/D mirrors, THEN the orchestrator-driven
oracle-reviewer gate decides. A "blocked -- source lacks this material" verdict -> REVERT that row to
`Unowned; high-confidence core only (no-oracle).` and keep/reword its caveat. Do not force.

### Surface 4 -- `## Start from the degenerate or starter case` (three-laws-and-test-selection.md) [D-03, Canon TDD OR First One, Then Many]

Role: SEL selection move. Candidate essay: Canon TDD or First One, Then Many (both free -> gateable).

- **A blockquote:** fragment `the starter case` in the bundled Beck clause.
- **B Sources entry:** fragment `the starter / degenerate case` in the bundled Beck entry.
- **C map row** (`principle-backing.md` line 32, verbatim current):
  ```
  | [Start from the degenerate case](three-laws-and-test-selection.md) | Kent Beck, Test-Driven Development by Example | Unowned; high-confidence core only (no-oracle). |
  ```
  -> if gate PASSES: Source cell `Kent Beck, <gated essay>`; tier -> owned. If BLOCKED: leave verbatim.
- **D Sources paragraph:** the caveat parenthetical currently OMITS "start from the degenerate case";
  add/keep it in the no-oracle caveat if the row stays no-oracle.

Asserted tokens: NONE at risk. `/degenerate|starter|empty|zero|null/i` matches the heading (line 42),
body, and the `sumOf` code fence (lines 54-72) -- all unedited. **Do not delete the code fence.**

### Surface 5 -- `## Assert-first` (test-structure-and-assertions.md) [D-04, conditional]

Role: STR structure move. Primary source = book (NOT gateable, D-05). Free candidates: Canon TDD /
Desirable Unit Tests / Composable Tests. Likely STAYS no-oracle; gate decides.

- **A blockquote:** fragment `Kent Beck (assert-first, evident data)` in the test-structure bundled
  clause (lines 9-15 above). Only split if D-04 upgrades.
- **B Sources entry:** the bundled test-structure Beck entry (lines 173-174 above).
- **C map row** (`principle-backing.md` line 36, verbatim current):
  ```
  | [Assert-first](test-structure-and-assertions.md) | Kent Beck, Test-Driven Development by Example | Unowned; high-confidence core only (no-oracle). |
  ```
- **D Sources paragraph:** the caveat parenthetical names `assert-first`. If the row stays no-oracle,
  reword the caveat to explain WHY (primary source is a summary-only book, no free essay gated it).

Asserted tokens: NONE at risk. `/assert-first/i` matches the heading `## Assert-first` (line 33,
unedited). The Sources-entry text also carries it, but the heading is the guaranteed home.

### Surface 6 -- `## Evident test data` (test-structure-and-assertions.md) [D-04, conditional]

Role: STR structure move. Same source situation as surface 5.

- **A blockquote:** fragment `evident data` in the test-structure bundled clause.
- **B Sources entry:** the bundled test-structure Beck entry (lines 173-174).
- **C map row** (`principle-backing.md` line 37, verbatim current):
  ```
  | [Evident test data](test-structure-and-assertions.md) | Kent Beck, Test-Driven Development by Example | Unowned; high-confidence core only (no-oracle). |
  ```
- **D Sources paragraph:** the caveat parenthetical names `evident test data`; same treatment as 5.

Asserted tokens: NONE at risk. `/evident (test )?data/i` matches the heading `## Evident test data`
(line 44) and body (line 45), unedited.

---

## `principle-backing.md` `## Sources` paragraph (surface D, lines 64-73) -- three sub-edits (D-08)

Edit this ONE paragraph once, reconciling ALL upgraded/kept rows together.

1. **Owned-sources sentence** (verbatim current fragment ending it):
   ```
   ... Gary
   Bernhardt (Boundaries -- functional core / imperative shell); Kent Beck (Test Desiderata essay and
   video series); and Ian Cooper.
   ```
   -> extend the `Kent Beck (...)` parenthetical to name the newly-owned essays (Analog D).

2. **Caveat sentence** (verbatim current, lines 69-71):
   ```
   Beck's other rows (running test list, triangulation, assert-first, evident test data) remain
   no-oracle for now, pending a follow-up upgrade against his now-provisioned essays (Canon TDD,
   First One Then Many).
   ```
   -> remove every UPGRADED row name from the parenthetical; drop the "pending a follow-up upgrade"
   framing (this phase IS that follow-up). If ALL Beck rows upgrade -> delete the sentence. If some stay
   no-oracle (likely assert-first + evident data) -> reword to name ONLY the rows deliberately left
   no-oracle and WHY. This list is stale: it omits "take one small step" and "start from the degenerate
   case" -- reconcile against the ACTUAL post-gate row set.

3. **No-oracle-sources sentence** (verbatim current, lines 71-73) -- UNCHANGED (none are Beck):
   ```
   Unowned no-oracle sources are used at
   high-confidence core only: Bill Wake, Dan North, Roy Osherove, Vladimir Khorikov, Michael Feathers,
   Steve Freeman and Nat Pryce (GOOS), and the Vitest 4.x documentation.
   ```
   Leave this sentence intact -- it carries the asserted `Khorikov` token (see token map).

---

## Provenance note (D-09) -- `16-CONTEXT.md`

Analog (in-file, verbatim, line 9 + paragraph): a `##` heading with a date + phase parenthetical
placed after the status block and BEFORE `<domain>`, followed by one prose paragraph:

```
## Provenance correction (2026-07-19, cross-phase, added during Phase 17)

Phase 17's execute-time oracle-reviewer found the query/command **message matrix** is Sandi Metz's
_The Magic Tricks of Testing_ talk, NOT _99 Bottles of OOP_ ...
```

Mirror: add a new section AFTER the existing note (after line 18, before `<domain>` at line 20), e.g.
`## Owned-source upgrade (2026-07-20, added during Phase 17.1)`, recording that Phase 17.1 re-opened
the closed Phase-16 selection/structure surfaces for a `no-oracle -> owned` tier upgrade against Beck's
free essays (Canon TDD, First One, Then Many), that recommendation CONTENT was unchanged, and which
rows (if any) stayed no-oracle and why. ASCII only. `.planning/` is NOT scanned by the battery, so
scaffold phrases there are harmless -- but keep it ASCII anyway.

---

## Asserted-token map (what NOT to remove -- `check-red-references.mjs`)

Only two asserted tokens live in text this phase edits, and BOTH survive by construction (each tier
persists across many un-flipped rows). Every other asserted token lives in an unedited heading, body,
link, or code fence. `[VERIFIED: check-red-references.mjs FILES entries]`

| File | Token in edited text? | Detail |
|------|-----------------------|--------|
| `principle-backing.md` | `oracle-verified` (`/oracle-verified/i`) | YES in tier cells + Sources para. Survives: RCM x3, Metz x3, Bernhardt, Cooper, Test Desiderata + any newly-upgraded rows stay owned. |
| `principle-backing.md` | `no-oracle` (`/no-oracle/i`) | YES in tier cells + line 6, 23 + Sources para. Survives: >= 8 rows stay no-oracle (Wake, North, Osherove, Khorikov, Feathers, GOOS, Vitest). |
| `principle-backing.md` | `Khorikov` (`/Khorikov/i`) | In map rows 42/44/51 + the no-oracle Sources sentence (line 72). Leave that sentence UNCHANGED (D-08 part 3). |
| `principle-backing.md` | deferral `Phase 18|LAW-0|SEAM-0` | Lines 56-60, 177. Keep the Phase-18 section + its `## ...` heading. |
| `three-laws-and-test-selection.md` | none in edited text | `test list`, `one small step`, `degenerate/starter`, `triangulat`, `lz-tpp` all in unedited headings/bodies. |
| `three-laws-and-test-selection.md` | deferral + fence | Keep lines 16-17 + 88-92 Phase-18 markers; keep the `sumOf` ts fence (lines 54-72). |
| `test-structure-and-assertions.md` | none in edited text | `assert-first`, `evident test data`, etc. all in unedited headings/bodies/links. |
| `test-structure-and-assertions.md` | deferral + fences | Keep lines 17-19 + 161-165 Phase-18 markers; keep both ts fences (applyCredit, netOf). |

**Scaffold trap:** never write `TODO`, `TBD`, `placeholder`, `once it exists`, or `to be authored`
(uppercase forms) into the reference-doc prose -- `check-red-references.mjs` fails the file. (Safe in
`.planning/`.)

**Not a per-row surface:** the `principle-backing.md` intro blockquote (lines 9-14) describes the map
generally and carries NO per-source tier claim -- do NOT edit it for this phase.

## No Analog Found

None. Every surface has an exact in-file Phase-17 owned template (principle-backing lines 45/46/49/50/53;
blockquote line 10-11; Sources entries in the same files + `anti-patterns.md`). The provenance note has
its own in-file analog (`16-CONTEXT.md` line 9). RESEARCH.md is not needed as a fallback pattern source.

## Metadata

**Analog search scope:** `plugins/lz-tdd/skills/lz-red/references/` (three-laws, test-structure,
principle-backing, anti-patterns), `.claude/skills/lz-red-workspace/tools/check-red-references.mjs`,
`.oracle/written-content/index.md` (nav only), `.planning/phases/16-.../16-CONTEXT.md`.
**Files scanned:** 7 read (5 references + 1 checker + 1 nav index) + 1 planning note.
**Clean-room:** NO `.oracle/written-content/*` essay PROSE read (DST-04). Eligibility from the nav
Access column only; content fidelity is the downstream oracle-reviewer's blind job.
**Pattern extraction date:** 2026-07-20
