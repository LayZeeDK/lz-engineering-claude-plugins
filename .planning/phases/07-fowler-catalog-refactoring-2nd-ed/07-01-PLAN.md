---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/tsconfig.json
  - .claude/skills/lz-refactor-workspace/package.json
  - .claude/skills/lz-refactor-workspace/extract-samples.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-smells.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-principles.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs
  - .gitignore
autonomous: true
requirements: [FWL-01, FWL-02, FWL-03, FWL-04]
must_haves:
  truths:
    - "D-03: TS/JS samples are proven tsc --strict-clean by a bundled, re-runnable compile harness (extractor + tsconfig), not by inline fences alone"
    - "The FWL-04 harness extracts every fenced ts block as its own module and runs tsc --strict --noEmit; exit 0 == FWL-04 pass"
    - "The FWL-01 checker asserts all 66 canonical refactoring names are present once each across fowler-catalog leaves, and that the 5 print-absent entries + Split Phase carry provenance tags"
    - "The FWL-02 checker asserts 24 Fowler smell rows and that every candidate cross-link resolves to a real leaf anchor"
    - "The FWL-03 checker asserts the Ch.2 topic set is present"
    - "The hygiene checker asserts ASCII-only, work-email absence via allowlist, and a no-verbatim heuristic (DST-04)"
    - "The harness workspace lives under .claude/skills/lz-refactor-workspace/ (NON-shipped), so claude plugin validate sees only pure Markdown"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tsconfig.json"
      provides: "strict:true noEmit:true target es2021, no dom lib (FWL-04 config)"
      contains: "\"strict\": true"
    - path: ".claude/skills/lz-refactor-workspace/extract-samples.mjs"
      provides: "fence extractor -> samples/*.ts (one module per fence) + tsc driver"
      min_lines: 30
    - path: ".claude/skills/lz-refactor-workspace/tools/check-catalog.mjs"
      provides: "FWL-01 completeness + provenance checker (66 names)"
      min_lines: 30
    - path: ".claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs"
      provides: "ASCII + work-email allowlist + no-verbatim heuristic"
      min_lines: 20
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/extract-samples.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      via: "walk + extract ts fences"
      pattern: "fowler-catalog"
    - from: ".gitignore"
      to: ".claude/skills/lz-refactor-workspace/**/samples/"
      via: "ignore generated modules"
      pattern: "samples/"
---

<objective>
Build the NON-shipped compile harness and the validation checker battery that the
Fowler content plans (07-04..07-08) are graded against. No Fowler content is authored
here -- this plan produces only the instruments: the `tsc --strict` harness (FWL-04)
and the FWL-01/FWL-02/FWL-03 + hygiene checkers.

Purpose: make FWL-01..FWL-04 machine-checkable BEFORE any content lands (Nyquist
scaffold-first). Every checker is RED against the empty catalog now and turns GREEN as
content plans fill the leaves; this is the expected baseline, not a failure.

Output: `.claude/skills/lz-refactor-workspace/` harness + `tools/*.mjs` checkers + a
`.gitignore` entry for the generated `samples/` dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@.planning/research/refactoring-com-overview.md
@.claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs

<interfaces>
Reuse verbatim from verify-scaffold.mjs (same workspace, tracked precedent):
- shebang + node:fs / node:path / fileURLToPath repo-root resolution
- report(ok, label, detail) failure-counting helper -> "[PASS]/[FAIL]" lines
- scanNonAscii(p) -> first non-ASCII byte offset or -1
- walkMd(dir) -> recursive .md collector
- terminal `SUMMARY:` line + process.exit(0 on green / 1 on red)
From eval-status.mjs: exists(p) / nonEmpty(p) helpers.

Canonical facts for the checkers (public, from refactoring-com-overview.md -- NOT oracle
content): the 66 2nd-ed refactoring names, their 1st-ed aliases, and the 5 print-absent
"+" entries: Replace Magic Literal, Replace Control Flag with Break, Return Modified
Value, Replace Error Code with Exception, Replace Exception with Precheck. Split Phase's
examples are online-only. The 24 Fowler smell names are orientation only (oracle-confirmed
at 07-08); the smell checker counts Fowler-tagged rows == 24 rather than hardcoding names.
tsconfig content is supplied by 07-RESEARCH.md lines 313-327 (net-new; no in-repo analog).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FWL-04 compile harness (config + extractor + tsc driver)</name>
  <files>.claude/skills/lz-refactor-workspace/tsconfig.json, .claude/skills/lz-refactor-workspace/package.json, .claude/skills/lz-refactor-workspace/extract-samples.mjs, .gitignore</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md (Pattern 3 harness + tsconfig lines 313-327; Standard Stack: pin typescript@6.0.3; Pitfall 2 module-per-fence)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md (extract-samples.mjs sketch + "Workspace is non-shipped + gitignored-record" -- samples/ NOT yet ignored)
    - .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs (helper idiom to mirror)
    - .gitignore (existing workspace ignore rules to extend)
  </read_first>
  <action>
    Create tsconfig.json with the FWL-04 config: strict true, noEmit true, target es2021,
    module esnext, moduleResolution bundler, lib es2021 (OMIT dom -- Phase-3 Node global
    collision), skipLibCheck true, forceConsistentCasingInFileNames true, include
    samples/**/*.ts. Create package.json pinning devDependency typescript@6.0.3 (first-party
    Microsoft compiler named by FWL-04, disposition Approved in 07-RESEARCH Package
    Legitimacy Audit) plus a "typecheck" script running the extractor then tsc against
    tsconfig.json. Create extract-samples.mjs: walk plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md,
    write each fenced ts block to samples/<leaf>-<n>.ts as its OWN module (one module per
    fence so before/after symbol reuse does not collide), skip fences whose info string is
    "ts ignore" and record the count of skipped fences, append "export {}" to any module
    with no top-level import/export to force module scope, then invoke tsc --strict --noEmit
    -p tsconfig.json and end with a SUMMARY: line reporting modules-compiled / skipped and a
    non-zero exit on tsc failure. Add a .gitignore entry `.claude/skills/lz-refactor-workspace/**/samples/`
    so generated modules are never committed. Node builtins only; add NO runtime dependency
    beyond the pinned typescript (D-03, per T-07-SC).
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` runs with no error; on the empty catalog it reports 0 extracted modules and `tsc --strict --noEmit` exits 0 over the empty sample set; prints a `SUMMARY:` line.
    - `rg -n "\"strict\": true" .claude/skills/lz-refactor-workspace/tsconfig.json` matches; `rg -n "\"dom\"" .claude/skills/lz-refactor-workspace/tsconfig.json` returns no hits.
    - `rg -n "typescript" .claude/skills/lz-refactor-workspace/package.json` shows the pinned 6.0.3 devDependency.
    - `rg -n "samples/" .gitignore` matches the new workspace ignore entry.
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tsconfig.json .claude/skills/lz-refactor-workspace/package.json .claude/skills/lz-refactor-workspace/extract-samples.mjs` returns no hits (ASCII-only).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: FWL-01 catalog checker + FWL-02 smell checker</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-catalog.mjs, .claude/skills/lz-refactor-workspace/tools/check-smells.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs (report/walkMd/scanNonAscii + SUMMARY/exit idiom to copy)
    - .planning/research/refactoring-com-overview.md (the 66 names + aliases + the 5 print-absent entries -- hardcode as facts)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md (Validation Architecture FWL-01/FWL-02 rows; Pitfall 3 one-token-per-line; Pitfall 5 body-links unchecked by validate)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (per-entry content contract these checkers assert)
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (row contract + Source column extension seam)
  </read_first>
  <action>
    check-catalog.mjs (FWL-01): hardcode the 66 canonical 2nd-ed names and the 5 print-absent
    names (Replace Magic Literal, Replace Control Flag with Break, Return Modified Value,
    Replace Error Code with Exception, Replace Exception with Precheck) plus Split Phase.
    Walk fowler-catalog/*.md leaf files; for each of the 66 names assert exactly one entry
    heading matches (identity, not just cardinality -- avoid the WR-02 gap noted in Phase-6
    review); assert each entry block carries a Motivation marker, a Mechanics marker, and at
    least one ts/js fence; assert the 5 print-absent entries carry a `[web-only]` tag on the
    heading line and Split Phase carries `[web-example]`. check-smells.mjs (FWL-02): parse
    smells.md; assert the count of Fowler-tagged rows == 24; for every candidate cross-link
    of the form `](fowler-catalog/<leaf>.md#<anchor>)` compute the target leaf's heading
    anchors and assert the anchor resolves. Both scripts: report()/SUMMARY:/non-zero-exit
    idiom; against the empty catalog they MUST run and report the RED baseline (0/66 present,
    0 smell rows) -- that proves the checker works before content lands.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` executes, prints per-name PASS/FAIL and a `SUMMARY:` line, and on the empty catalog exits 1 reporting 0/66 present (expected RED baseline).
    - `node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` executes, prints a `SUMMARY:` line, and on the empty smells.md stub exits 1 reporting fewer than 24 Fowler rows (expected RED baseline).
    - `rg -n "Replace Magic Literal|Replace Control Flag with Break|Return Modified Value|Replace Error Code with Exception|Replace Exception with Precheck" .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` shows the 5 print-absent names are enumerated for the provenance assertion.
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` returns no hits.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: FWL-03 principles checker + hygiene checker (ASCII / work-email / no-verbatim)</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-principles.mjs, .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs (scanNonAscii + report + walkMd)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md (Validation Architecture FWL-03 topics + hygiene row; Security Domain DST-04 + work-email leak)
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (Ch.2 topic contract)
    - CLAUDE.md and AGENTS.md (public contact larsbrinknielsen@gmail.com only; work email must never appear)
  </read_first>
  <action>
    check-principles.mjs (FWL-03): assert each Ch.2 topic token appears (on its own line, per
    Pitfall 3) in principles.md: definition, two hats, rule of three, preparatory,
    comprehension, litter-pickup, performance, YAGNI; report()/SUMMARY:/exit idiom; RED on
    the empty stub. check-hygiene.mjs: over SKILL.md + every references/*.md leaf (walkMd) run
    (a) scanNonAscii per file, fail on any non-ASCII byte with file@offset; (b) a work-email
    allowlist gate -- enumerate all email-shaped tokens, subtract the approved
    larsbrinknielsen@gmail.com, assert the remainder is empty (this recurred twice in Phase 4,
    per the public-repo memory); (c) a no-verbatim heuristic for DST-04 -- flag any single
    quoted run over a threshold length as a candidate for human review (heuristic only; the
    authoritative no-verbatim check is the D-07 oracle checkpoint plus the Phase-10 hygiene
    scan). Combine into one SUMMARY: with non-zero exit on any hard failure (non-ASCII or a
    non-allowlisted email); the no-verbatim hits are WARN-level, not exit-failing.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-principles.mjs` executes, prints a `SUMMARY:` line, and on the empty principles.md stub exits 1 (topics absent -- expected RED baseline).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` executes over the current shipped Markdown, prints a `SUMMARY:` line, and exits 0 (the existing scaffold is ASCII-clean and email-clean today).
    - `rg -n "larsbrinknielsen@gmail.com" .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` matches (the allowlist entry is present).
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-principles.mjs .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` returns no hits.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> workspace devDependency | the single `typescript` pin enters the non-shipped harness |
| authored files -> public GitHub repo | anything committed is world-readable (IP + secrets surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | `typescript` devDependency in package.json | mitigate | Pin `typescript@6.0.3`, the first-party Microsoft compiler explicitly named by FWL-04 (disposition Approved in 07-RESEARCH audit); add NO other dependency; keep it inside the non-shipped workspace, out of the shipped skill surface |
| T-07-02 | Information Disclosure | work email in any committed harness/checker file | mitigate | check-hygiene.mjs work-email allowlist gate (Task 3); ASCII+email scan runs over authored files |
| T-07-04 | Tampering | a checker that silently passes (WR-02 identity-vs-cardinality gap from Phase-6 review) | mitigate | check-catalog.mjs asserts name IDENTITY (each of 66 present once), not just count; every checker ends with a SUMMARY: + non-zero exit so a wave merge gates on exit code |

No high-severity threats: this plan ships no runtime, no network, no user input, and no
copyrighted content (checkers hardcode only public names). Nothing rises to the
`block_on: high` bar.
</threat_model>

<verification>
- `node .../extract-samples.mjs` and all four `tools/*.mjs` checkers execute and print a `SUMMARY:` line.
- The empty-catalog RED baseline is observed: check-catalog (0/66), check-smells (<24), check-principles (topics absent) all exit 1; extract-samples + check-hygiene exit 0.
- Harness lives only under `.claude/skills/lz-refactor-workspace/`; `git status` shows no new files under `plugins/`.
- `.gitignore` ignores the generated `samples/` dir.
</verification>

<success_criteria>
- The FWL-04 harness (tsconfig + package.json + extract-samples.mjs) and the FWL-01/02/03 + hygiene checkers exist, run, and gate on exit code.
- Every checker is a throwaway Node-builtins script mirroring verify-scaffold.mjs; no new runtime dependency beyond pinned typescript.
- All authored tooling files are ASCII-only.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-01-SUMMARY.md` when done.
</output>
