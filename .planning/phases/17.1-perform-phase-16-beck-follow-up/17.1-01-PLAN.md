---
phase: 17.1-perform-phase-16-beck-follow-up
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  - .planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md
autonomous: true
requirements:
  - SEL-01
  - SEL-02
  - STR-02
  - DST-03
  - DST-04
must_haves:
  truths:
    - "D-01: running test list + take one small step are tiered owned; oracle-verified against Canon TDD across all their provenance surfaces (blockquote, Sources block, principle-backing row + Source cell)."
    - "D-02: triangulate-to-select-the-next-test is tiered owned; oracle-verified against First One, Then Many across the same surfaces."
    - "D-03: start-from-the-degenerate/starter-case is upgraded IFF the orchestrator gate confirms a free essay covers it, else it stays no-oracle with the reason recorded."
    - "D-04: assert-first + evident-test-data upgrade IFF a free essay gates them under the oracle-reviewer gate, else stay no-oracle with WHY recorded (primary source is a summary-only book)."
    - "D-05: no surface is tagged owned without a free full-text essay behind it; book-summary-only rows stay no-oracle (honesty gate)."
    - "D-06: the author edits blind -- main context reads only .oracle/*/index.md nav, never essay prose."
    - "D-07: the orchestrator (not the executor) drives oracle-reviewer per upgraded surface, converge-to-clean, reverting blocked rows."
    - "D-08: principle-backing tier cells flipped + Source cells cite the free essay (never the book) + Sources paragraph caveat reconciled to the actual post-gate row set."
    - "D-09: a Phase-16 owned-source-upgrade note records the re-open of closed Phase-16 content in 16-CONTEXT.md."
    - "D-10: all three deterministic-battery checks exit 0 after every edit; no asserted content token or code fence removed."
    - "Recommendation CONTENT is unchanged; only attribution/tier text is edited (provenance-tier upgrade to already-satisfied SEL-01/SEL-02/STR-02/DST-03/DST-04; their REQUIREMENTS.md status stays Complete, not reopened)."
  artifacts:
    - "plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (edited: blockquote + Sources block split; surfaces 1-4)"
    - "plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (edited provisionally: blockquote + Sources entry split; surfaces 5-6, conditional)"
    - "plugins/lz-tdd/skills/lz-red/references/principle-backing.md (edited: map rows Source + tier cells; Sources paragraph)"
    - ".planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md (appended: owned-source-upgrade note)"
  key_links:
    - "principle-backing map row (Source + tier cell) MUST mirror each per-doc blockquote + Sources claim -- the four consistency surfaces (A blockquote, B Sources entry, C map row, D Sources paragraph) stay consistent per upgraded row."
    - "an owned Source cell MUST name the free gated essay (Kent Beck, Canon TDD / First One, Then Many), NEVER Test-Driven Development by Example (a summary-only book) -- D-05."
    - "the orchestrator-driven oracle-reviewer verdict is the authority on the candidate/conditional rows (degenerate case; assert-first; evident data); a blocked verdict reverts that row to no-oracle before the phase closes."
---

<objective>
Upgrade the provenance TIER of the lz-red skill's currently-no-oracle, Kent-Beck-backed RED
selection/structure surfaces from `Unowned; high-confidence core only (no-oracle).` to
`Owned; oracle-verified against the clean-room source.`, now that Beck's essays are provisioned in
git-ignored `.oracle/written-content/`. This legitimately re-opens closed Phase-16 shipped content
(`three-laws-and-test-selection.md`, the `test-structure-and-assertions.md` STR slice, and the
`principle-backing.md` map). It adds NO new RED guidance and changes NO recommendation CONTENT --
only attribution/tier text.

Two upgrades are locked and eligible (test list + one small step -> Canon TDD; triangulation ->
First One, Then Many; both essays `Access: free`). Three more are candidate/conditional and decided
by the oracle-reviewer gate, not pre-committed: the degenerate/starter case, and assert-first +
evident-test-data (the latter two ride mainly on Test-Driven Development by Example, a summary-only
book, so they likely stay no-oracle).

Purpose: make the tier warrant honest (owned only when a real full-text essay backs it) and
internally consistent across the four provenance surfaces, without touching recommendation content
or the deterministic battery's asserted tokens.

Output: three edited reference docs + one appended Phase-16 note; NO new files or symbols. The
executor edits BLIND and runs the battery to GREEN; the ORCHESTRATOR then drives the oracle-reviewer
gate per upgraded surface (see the post-execution gate section) before the phase closes.

Decision coverage (CONTEXT.md, all locked D-NN tracked here):

- **D-01:** Upgrade `Keep a running test list` and `Take one small step` against Canon TDD (Task 1 A/B + Task 3 C/D).
- **D-02:** Upgrade `Triangulate to select the next test` against First One, Then Many (Task 1 A/B + Task 3 C/D).
- **D-03:** `Start from the degenerate or starter case` is a candidate -- flip provisionally, gate confirms or reverts (Task 1 + orchestrator gate).
- **D-04:** `Assert-first` + `Evident test data` are conditional (book-primary) -- flip provisionally, gate likely reverts to no-oracle (Task 2 + orchestrator gate).
- **D-05:** Owned tag ONLY when gateable against a free full-text essay; book-summary rows stay no-oracle -- Source cells never cite the book (Tasks 1/2/3 + gate).
- **D-06:** Author edits BLIND -- reads only `.oracle/*/index.md` nav, never essay prose (every task's read_first excludes essay bodies).
- **D-07:** ORCHESTRATOR drives oracle-reviewer after the executor returns; the gsd-executor has no Agent/Task tool, so the gate is NOT an executor task (post-execution gate section).
- **D-08:** Flip principle-backing tier cells + Source cells and rework the Sources paragraph caveat (Task 3).
- **D-09:** Add a Phase-16 owned-source-upgrade note mirroring the existing correction-note precedent (Task 3).
- **D-10:** Keep the battery GREEN -- touch only attribution/tier text, never asserted tokens; re-run all three checks after every edit (every task verify + Task 3 full battery).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-CONTEXT.md
@.planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-RESEARCH.md
@.planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
@plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
@plugins/lz-tdd/skills/lz-red/references/principle-backing.md
@.oracle/written-content/index.md
</context>

<clean_room_boundary>
DST-04 clean-room (D-06): main context (the executor) MUST NOT read any essay body under
`.oracle/written-content/*.md` (canon-tdd.md, tri-first-one-then-many.md, desirable-unit-tests.md,
composable-tests.md, book-*.md). Read ONLY `.oracle/written-content/index.md` (the nav table) to
confirm eligibility from its `Access` column. Verifying a surface's own-words fidelity AGAINST essay
prose is exclusively the oracle-reviewer agent's blind job, driven by the orchestrator after the
executor returns. The three reference docs being edited are the skill's OWN authored files and may
be read freely.
</clean_room_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Split and upgrade the Beck selection surfaces in three-laws-and-test-selection.md (surfaces 1-4)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md</files>
  <read_first>
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-PATTERNS.md (Analog A/B templates; verbatim current bundled blockquote lines 9-14 + bundled Sources entry lines 98-101; per-surface assignments 1-4; the "Take one small step vs Grow the test only enough to fail" CONFLATION GUARD)
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-RESEARCH.md (the four consistency surfaces; Asserted-token map; Scaffold-phrase trap)
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-CONTEXT.md (D-01, D-02, D-03, D-05, D-06)
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (the file being edited)
    - .oracle/written-content/index.md (NAV ONLY -- the Access column; NEVER open any essay body -- see clean_room_boundary)
  </read_first>
  <action>
    First confirm eligibility from the .oracle/written-content/index.md Access column only (nav,
    clean-room-safe): Canon TDD (canon-tdd.md) and First One, Then Many (tri-first-one-then-many.md)
    are both Access: free, so gateable. Then split the bundled Beck provenance in TWO places,
    mirroring the existing owned analogs token-for-token (PATTERNS Analog A for the blockquote,
    Analog B for the Sources entry).
    (A -- intro blockquote, the "Mixed-provenance reference" paragraph) Move the running test list
    (D-01), the one-step move (D-01), and triangulation (D-02) OUT of the "high-confidence core only
    ... (no-oracle tag)" Beck sentence and INTO an owned sentence stating they are owned and
    oracle-verified against the clean-room source, naming Canon TDD for the running test list and the
    one small step, and First One, Then Many for triangulation.
    (B -- ## Sources block) Split the single bundled Beck "Test-Driven Development by Example" entry
    into owned per-essay entries: one citing Kent Beck, Canon TDD for the running test list and the
    one small step; one citing Kent Beck, First One, Then Many for triangulation as a test-selection
    move -- each ending with the exact owned tier string. Leave any move NOT upgraded in a reduced
    Beck no-oracle entry that names only what remains.
    PROVISIONAL (D-03): move the starter/degenerate-case fragment into the owned sentence and owned
    Sources entry citing Canon TDD OR First One, Then Many (pick the stronger free candidate). This
    flip is PENDING the orchestrator oracle-reviewer gate, which confirms it or reverts it to
    no-oracle.
    Use the fixed owned vocabulary EXACTLY: the Sources tier string is
    "Owned; oracle-verified against the clean-room source." and the blockquote prose form is
    "... are owned and oracle-verified against the clean-room source." Cite ONLY a free essay as an
    owned source; never cite the book (D-05).
    CONFLATION GUARD (RESEARCH): do NOT touch the RCM only-enough-to-fail thread -- leave the body
    line reading "Distilled rationale (Robert C. Martin, owned; oracle-verified)" and the
    "Grow the test only enough to fail" material exactly as-is; D-01 upgrades the Beck one-step MOVE
    only, which is a different principle.
    Edit ONLY attribution/tier prose. Do NOT alter any heading, any recommendation body, or the
    sumOf code fence -- they carry asserted content tokens (test list, one small step,
    degenerate/starter, triangulat) and the Phase-18 deferral markers (RESEARCH Asserted-token map).
    Do NOT introduce any scaffold phrase the content checker rejects (RESEARCH Scaffold-phrase trap).
    ASCII only; no verbatim source prose; the only email-shaped token permitted anywhere is the
    public maintainer gmail.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs</automated>
  </verify>
  <done>three-laws blockquote + Sources block split; running test list + take one small step owned against Canon TDD, triangulation owned against First One, Then Many, degenerate case provisionally owned (pending gate); RCM line + all headings/bodies/sumOf fence/Phase-18 markers untouched; no scaffold phrase; ASCII + gmail-only; check-red-references.mjs exit 0.</done>
  <acceptance_criteria>
    - The intro blockquote and the ## Sources block are each SPLIT: running test list + one small step cited owned against Canon TDD; triangulation owned against First One, Then Many; the starter/degenerate case provisionally owned (pending the gate).
    - Both owned strings match the fixed vocabulary exactly; no owned entry cites "Test-Driven Development by Example".
    - The RCM only-enough-to-fail line is untouched; every heading, recommendation body, the sumOf code fence, and the Phase-18 markers are unchanged.
    - No scaffold phrase added; ASCII-only; no maintainer work-email or its bare domain anywhere.
    - check-red-references.mjs exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Provisionally split and flip the Beck structure surfaces in test-structure-and-assertions.md (surfaces 5-6, conditional)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md</files>
  <read_first>
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-PATTERNS.md (surfaces 5-6 assignments; verbatim current test-structure bundled blockquote lines 9-15 + bundled Sources entry lines 173-174)
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-RESEARCH.md (D-04 conditional handling; the book-primary caveat; Asserted-token map)
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-CONTEXT.md (D-04, D-05, D-06)
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (the file being edited)
    - .oracle/written-content/index.md (NAV ONLY -- confirm Canon TDD / Desirable Unit Tests / Composable Tests = free and Test-Driven Development by Example = book; NEVER open any essay body)
  </read_first>
  <action>
    These are the D-04 CONDITIONAL surfaces. Provisionally flip Assert-first (surface 5) and Evident
    test data (surface 6) to owned so the orchestrator oracle-reviewer gate has a concrete claim to
    verify -- but EXPECT them to revert: their primary source (Test-Driven Development by Example) is
    Access: book (summary-only, NOT gateable, D-05), so the gate will block unless a FREE essay
    (Canon TDD / Desirable Unit Tests / Composable Tests) actually covers the point.
    Split the bundled Beck fragment "Kent Beck (assert-first, evident data)" out of the
    "high-confidence core only ... (no-oracle tag)" sentence in the intro blockquote into an owned
    sentence citing the strongest free candidate essay (executor's pick from the free candidates;
    the gate confirms, reassigns, or reverts). Split the bundled Beck Sources entry likewise into an
    owned per-essay entry citing that free candidate, ending with the exact owned tier string. Cite
    ONLY a free essay -- NEVER "Test-Driven Development by Example" -- as the owned source (D-05).
    Leave the Bill Wake (arrange-act-assert), Dan North (given-when-then), and Vladimir Khorikov
    material in the no-oracle sentence untouched.
    Edit attribution/tier prose ONLY. Do NOT alter any heading, any recommendation body, the
    applyCredit or netOf code fences, or the Phase-18 markers -- they carry the asserted tokens
    (assert-first, evident test data, arrange-act-assert, given-when-then, four pillars, and the
    rest; RESEARCH Asserted-token map). No scaffold phrase; ASCII only; no verbatim; public gmail
    only.
    Record for the orchestrator gate: a "blocked -- source lacks this material" verdict here is the
    EXPECTED, honest outcome; the finalize step reverts these two rows to no-oracle and rewords the
    principle-backing caveat to explain WHY (book-primary, summary-only, no free essay gated them).
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs</automated>
  </verify>
  <done>assert-first + evident-test-data provisionally flipped to owned citing a free candidate essay (never the book); Wake/North/Khorikov no-oracle material + all headings/bodies/both fences/Phase-18 markers untouched; no scaffold phrase; ASCII + gmail-only; check-red-references.mjs exit 0; the expected "blocked -> revert to no-oracle" outcome recorded for the gate.</done>
  <acceptance_criteria>
    - Assert-first + evident-test-data are provisionally flipped to owned, each citing a FREE candidate essay (never the book), split cleanly out of the bundled no-oracle clause and entry.
    - The Wake / North / Khorikov no-oracle material is untouched; every heading, recommendation body, both code fences, and the Phase-18 markers are unchanged.
    - The owned tier string matches the fixed vocabulary exactly; no scaffold phrase; ASCII-only; no work-email or bare domain.
    - check-red-references.mjs exits 0.
    - The plan/SUMMARY records that a gate "blocked" verdict means revert to no-oracle at finalize (the expected outcome for these two rows).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Flip the central principle-backing map (rows + Source cells + Sources paragraph), add the Phase-16 upgrade note, and run the full battery</name>
  <files>plugins/lz-tdd/skills/lz-red/references/principle-backing.md, .planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md</files>
  <read_first>
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-PATTERNS.md (Analog C map-row template; Analog D owned-list form; the principle-backing Sources-paragraph three sub-edits; the provenance-note analog; "Not a per-row surface" note that the intro blockquote is NOT edited)
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-RESEARCH.md (the D-08 Sources-paragraph spec; Asserted-token map showing oracle-verified / no-oracle / Khorikov / Phase-18 survive; the D-09 note precedent)
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-CONTEXT.md (D-07, D-08, D-09, D-10)
    - plugins/lz-tdd/skills/lz-red/references/principle-backing.md (the file being edited)
    - .planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md (the existing "## Provenance correction" note = the append precedent + placement)
  </read_first>
  <action>
    (C -- map rows) For every row upgraded in Tasks 1-2, mirror PATTERNS Analog C: change the Source
    cell from "Kent Beck, Test-Driven Development by Example" to the FREE gated essay -- "Kent Beck,
    Canon TDD" for the Running test list and Take one small step rows; "Kent Beck, First One, Then
    Many" for the Triangulate-to-the-next-test row; the executor's chosen free candidate for the
    provisional degenerate-case (D-03) and assert-first / evident-data (D-04) rows -- AND flip that
    row's Access-tier cell to "Owned; oracle-verified against the clean-room source." KEEP each row's
    [Recommendation](doc.md) link text EXACTLY as-is, including the pre-existing "Triangulate to the
    next test" link wording that differs from the heading (do NOT "fix" it).
    (D -- Sources paragraph) Apply the three D-08 sub-edits to the one Sources paragraph: (1) extend
    the owned-sources sentence's "Kent Beck (...)" parenthetical to name the newly-owned essays
    (Analog D form: Canon TDD for the running test list and one small step; First One, Then Many for
    triangulation); (2) rework the caveat sentence -- remove every UPGRADED row name from the
    parenthetical, DROP the "pending a follow-up upgrade" framing (this phase IS that follow-up), and
    reconcile against the ACTUAL post-provisional row set (the current list is stale: it omits "take
    one small step" and "start from the degenerate case"). If some Beck rows stay no-oracle (expect
    assert-first + evident data), reword the caveat to name ONLY those and WHY (their primary source
    is a summary-only book and no free essay gated them). If ALL Beck rows end up owned, delete the
    caveat sentence. (3) Leave the no-oracle-sources sentence (Wake, North, Osherove, Khorikov,
    Feathers, GOOS, Vitest) UNCHANGED -- it carries the asserted Khorikov token.
    Do NOT edit the intro blockquote (it carries no per-source tier claim; RESEARCH "Not a per-row
    surface"). Do NOT remove any asserted token -- oracle-verified, no-oracle, Khorikov, and the
    Phase-18 deferral markers all survive by construction because both tiers persist across many
    un-flipped rows (RESEARCH Asserted-token map).
    (D-09 note) Append a new section to 16-CONTEXT.md AFTER the existing
    "## Provenance correction (2026-07-19 ...)" note and BEFORE the <domain> tag, mirroring that
    note's shape: a "## Owned-source upgrade (2026-07-20, added during Phase 17.1)" heading + one
    ASCII prose paragraph recording that Phase 17.1 re-opened the closed Phase-16 selection/structure
    surfaces for a no-oracle -> owned tier upgrade against Beck's now-provisioned free essays (Canon
    TDD; First One, Then Many), that recommendation CONTENT was unchanged, and which rows (if any)
    stayed no-oracle and why.
    Then run the full three-command deterministic battery (D-10). ASCII only; public gmail only; no
    verbatim source prose.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs</automated>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck</automated>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>principle-backing rows flipped (Source cell = free gated essay, tier = owned) with link text intact; Sources paragraph owned-list extended + caveat reconciled + no-oracle sentence unchanged; intro blockquote + oracle-verified/no-oracle/Khorikov/Phase-18 tokens intact; 16-CONTEXT.md carries the new Phase-17.1 owned-source-upgrade note; all three battery checks exit 0.</done>
  <acceptance_criteria>
    - Every upgraded row: Source cell names the FREE essay (never the book) and the tier cell equals the owned string; the [Recommendation](doc.md) link text is unchanged.
    - Sources paragraph: owned-list extended with the newly-owned Beck essays; caveat reworked/reconciled/deleted per the actual row set; no-oracle-sources sentence unchanged (Khorikov intact).
    - The principle-backing intro blockquote is untouched; oracle-verified, no-oracle, Khorikov, and the Phase-18 tokens are all still present.
    - 16-CONTEXT.md carries the new "## Owned-source upgrade (... Phase 17.1)" note after the existing correction note and before <domain>, ASCII, recording the content-unchanged claim and any rows left no-oracle with reasons.
    - All three battery checks exit 0: check-red-references.mjs, the lz-red-workspace typecheck, and check-hygiene.mjs.
  </acceptance_criteria>
</task>

</tasks>

<orchestrator_gate>
## Orchestrator post-execution gate (NOT an executor task) -- D-07

The gsd-executor has no Agent/Task tool, so the oracle-reviewer gate is run by the ORCHESTRATOR
AFTER the executor returns (never by the executor). This is the Phase-17 clean-room re-gate loop
applied to the Phase-16 Beck surfaces (17-LEARNINGS "Orchestrator drives oracle/skill/verify gates").

1. For each UPGRADED surface, the orchestrator packages the surface's own-words content and points
   the oracle-reviewer agent DIRECTLY at the correct free essay under `.oracle/written-content/`:
   - running test list + take one small step -> `canon-tdd.md`
   - triangulation -> `tri-first-one-then-many.md`
   - degenerate/starter case (D-03) -> the essay the executor cited (`canon-tdd.md` or `tri-first-one-then-many.md`)
   - assert-first + evident-test-data (D-04) -> the free candidate the executor cited (Canon TDD / Desirable Unit Tests / Composable Tests)
   The agent reads the essay to EOF in its isolated context (chunk-read, length-agnostic) and returns
   PASS, an own-words fidelity revise directive, or "blocked -- source lacks this material". No
   verbatim prose crosses back.
2. Converge-to-clean, cap 3 rounds (0.0.2 precedent; Phase 17 Test Desiderata took one round):
   - PASS -> keep the upgrade.
   - Revise -> the executor applies the own-words directive BLIND, re-runs the battery, the
     orchestrator re-gates.
   - Blocked (expected for at least assert-first + evident-data, and possibly the degenerate case) ->
     REVERT that row's tier flip to `Unowned; high-confidence core only (no-oracle).` across ALL four
     surfaces (blockquote, Sources entry, principle-backing row + Source cell), and reconcile the
     principle-backing caveat to name the row and WHY it stays no-oracle. A "blocked" verdict is a
     valid outcome, not a failure -- it stops a dishonest "owned" warrant from shipping (D-05).
   - If 3 rounds elapse without PASS or Blocked, treat as Blocked (revert to no-oracle) and note the
     unresolved gate in the phase SUMMARY for developer awareness (gates.md unconditional-escalation
     fallback after max iterations).
3. Finalize (before the phase closes): battery GREEN + the Phase-16 note present + all upgraded rows
   either PASS-confirmed or reverted-and-reconciled. Only then commit. Commit identity = public
   maintainer gmail (author AND committer).

Provisional flips (Tasks 1 surface 4, Task 2 surfaces 5-6) MUST NOT be treated as final: they are
subject to this gate and reconciled at finalize before phase completion. Assert-first (surface 5)
and evident-test-data (surface 6) are bundled into ONE blockquote clause + ONE Sources entry in
Task 2 but are INDEPENDENT rows in the principle-backing map (Task 3); the gate may resolve them
differently (one PASS, one blocked), so finalize MUST be prepared to RE-SPLIT the bundled
test-structure blockquote clause and Sources entry so each row carries its own tier independently
(the same applies to any candidate row the gate splits from a provisional bundle).
</orchestrator_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/` copyrighted source prose -> committed public-repo docs | Verbatim book/essay prose must not cross into the shipped Markdown (DST-04 clean-room). |
| maintainer identity -> public git history | Committed content, commit messages, and author/committer identity must carry only the public gmail. |
| provenance claim -> reader trust | An "owned; oracle-verified" tag asserts that a real full-text gate actually happened. |

## STRIDE Threat Register (ASVS L1; prose-only phase, no runtime/network/secret surface)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17.1-01 | Information Disclosure | `.oracle/` essay prose -> the three reference docs | low | mitigate | Executor edits BLIND (reads only `.oracle/*/index.md` nav, never essay bodies -- D-06, clean_room_boundary); orchestrator-driven oracle-reviewer clean-room gate (D-07); `check-hygiene.mjs` no-verbatim scan (>= 120-char quoted-run gate) runs in the battery. |
| T-17.1-02 | Information Disclosure | maintainer work-email / bare domain in content or commit identity | low | mitigate | `check-hygiene.mjs` email allowlist-inversion (only the public gmail permitted; forbidden value never encoded as a needle); author + committer = public gmail. |
| T-17.1-03 | Tampering / Repudiation | a dishonest "owned; oracle-verified" tag with no real full-text gate | low | mitigate | D-05 eligibility rule (free full-text essay only; book-summary rows stay no-oracle); orchestrator oracle-reviewer gate confirms or reverts each upgraded row; "blocked" -> revert to no-oracle. |
| T-17.1-04 | Tampering | accidental removal of an asserted content token or code fence breaks the deterministic gate | low | mitigate | Edit attribution/tier prose only; RESEARCH Asserted-token map lists every protected token; re-run the full battery after every edit (D-10), all three checks exit 0. |
| T-17.1-SC | Tampering | supply chain (npm/pip/cargo installs) | low | accept | No package installs in this phase; zero registry surface (RESEARCH Package Legitimacy Audit: N/A). |

All threats are low given the mitigations; the block-on-high threshold is not tripped.
</threat_model>

## Artifacts this phase produces

This phase produces NO new symbols, files, exports, or requirements. It is a provenance-tier edit of
existing shipped prose. Drift verification should EXCLUDE these as expected edits, not new surfaces:

- Modified: `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md` -- intro blockquote (the Beck clause) + the `## Sources` Beck entry, for the Running test list / Take one small step / Triangulate / (provisional) degenerate-case rows.
- Modified: `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md` -- intro blockquote (the Beck clause) + the `## Sources` Beck entry, for the (provisional, conditional) Assert-first / Evident-test-data rows.
- Modified: `plugins/lz-tdd/skills/lz-red/references/principle-backing.md` -- the map rows Source + Access-tier cells for the six Beck rows + the `## Sources` paragraph (owned-list add + caveat rework). Intro blockquote NOT touched.
- Appended: `.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md` -- one new "## Owned-source upgrade (... Phase 17.1)" note.

Requirements SEL-01, SEL-02, STR-02, DST-03, DST-04 are TOUCHED but already **Complete** for Phase
16. This phase upgrades their provenance tier only; it does NOT change delivered behavior, add
coverage, or reopen their REQUIREMENTS.md status.

<verification>
Per-edit and phase-gate checks (D-10 -- run from repo root):

- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` exits 0 (content tokens, per-file fences, no scaffold phrase intact).
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 (TS fences tsc --strict clean).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII-only; email allowlist-inversion; no-verbatim).

D-10 cadence ("re-run the full battery after every edit"): interpreted as fast-check-per-edit +
full-battery-per-plan. Tasks 1-2 run only `check-red-references.mjs` (the fast content-token / fence
/ scaffold gate) after each edit; Task 3 runs the full three-command battery. This is faithful, not
a divergence: Tasks 1-2 edit only attribution/tier prose OUTSIDE any TS code fence, so the typecheck
surface (TS fences) is provably unchanged until Task 3, and the hygiene surface (ASCII / email /
no-verbatim) is re-scanned by the full battery at plan end (Task 3) before the orchestrator gate and
commit -- catching any cumulative regression before anything ships. The executor MAY still run the
full battery per edit; it is cheap insurance (RESEARCH Deterministic Battery), not required by this
cadence.

Orchestrator gate (post-executor, D-07): oracle-reviewer PASS or reconciled-revert for every
upgraded surface; blocked rows reverted to no-oracle across all four surfaces and the caveat
reconciled. The battery is the deterministic validation surface (no test framework to add);
oracle-reviewer is the fidelity gate.
</verification>

<success_criteria>
- The locked-eligible rows (running test list, take one small step -> Canon TDD; triangulation -> First One, Then Many) are tiered `Owned; oracle-verified against the clean-room source.` consistently across their blockquote, Sources entry, and principle-backing row + Source cell, and PASS the orchestrator oracle-reviewer gate.
- Candidate/conditional rows (degenerate case; assert-first; evident data) are upgraded only where the gate confirms a free essay covers them; every other one is reverted to no-oracle with a recorded reason (honesty gate, D-05).
- No owned Source cell cites Test-Driven Development by Example (the book); recommendation CONTENT is byte-for-byte unchanged apart from attribution/tier prose.
- All three deterministic-battery checks exit 0; no asserted content token or code fence was removed.
- The Phase-16 owned-source-upgrade note exists in 16-CONTEXT.md.
- Shipped edits are ASCII-only with no verbatim source prose and no maintainer work-email/domain; commit author + committer = the public gmail.
</success_criteria>

<output>
Create `.planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-01-SUMMARY.md` when done. Record:
which rows were upgraded to owned and against which free essay; which candidate/conditional rows the
gate reverted to no-oracle and why; the final battery state (all three exit 0); and that the
Phase-16 note was added. Note that SEL-01/SEL-02/STR-02/DST-03/DST-04 were touched as a tier upgrade
only and their REQUIREMENTS.md status stays Complete (not reopened).
</output>
