---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 02
type: execute
wave: 2
depends_on: [07-01]
tags: [fowler-catalog, refactoring, skill-authoring, oracle-clean-room, typescript, pilot]
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-smells.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
  - .claude/skills/lz-refactor-workspace/package.json
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/inline-function.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-variable.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/inline-variable.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/change-function-declaration.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/encapsulate-variable.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/rename-variable.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-parameter-object.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/combine-functions-into-class.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/combine-functions-into-transform.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/split-phase.md
autonomous: false
requirements: [FWL-01, FWL-04]
must_haves:
  truths:
    - "SCOPE (07-ROUTING-ARCHITECTURE 'Scope: 62 refactorings') is 62, NOT 66; this supersedes CONTEXT D-01 (tag-group split), D-04 (smell table), D-06 (5 print-absent entries) and D-07 (AskUserQuestion oracle) -- the 4 web-only 1st-ed relics are OUT and Return Modified Value stays as the sole [web-only] entry"
    - "07-ROUTING-ARCHITECTURE 'Structure' + 'Content contracts': each refactoring is its OWN leaf file fowler-catalog/<slug>.md with # <Name> + 1st-ed alias(es) + a one-line Use when: + ## Motivation + ## Mechanics + ## Example + optional ## Watch for (D-02 per-entry content contract, honored as per-refactoring leaves)"
    - "The 07-02 SUMMARY pins the 62 canonical name->slug map as the deterministic cross-chapter cross-link-target contract, so inverse-of / see-also / candidate links resolve at check-crossrefs (wave-3 + 07-10 consume it via 07-02-SUMMARY.md)."
    - "07-ORACLE-MODEL 'Decision: Model C -- clean-room reviewer' + 'Firewall': the basic (Ch.6) leaves are authored BLIND (public knowledge + refactoring-com-overview scaffold), gated by the oracle-reviewer subagent, and revised BLIND to convergence; the executor/main context NEVER reads .oracle/ (only ls for names)"
    - "This is the CALIBRATION pilot: it runs the full blind-author -> oracle-reviewer-gate -> converge loop and the deterministic harness end-to-end on ONE chapter, and records the working rubric anchors + round counts, BEFORE the wave-3 fan-out"
    - "The Wave-1 checkers are overhauled from the superseded 66-count / tag-group / smell-table model to the LOCKED 62-count / per-refactoring-leaf / smell-leaf model (D-03 machine-checkable harness, preserved and extended per 07-ORACLE-MODEL 'Harness (deterministic)')"
    - "FWL-04 (07-ROUTING-ARCHITECTURE 'Validation'): every extracted before/after example in the basic leaves is tsc --strict-clean AND behavior-preserving, the latter now gated by the oracle-reviewer example axis, not manual-only"
    - "SKILL.md is NOT re-touched: its existing pointer to references/fowler-catalog/README.md already resolves"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/check-catalog.mjs"
      provides: "FWL-01 62-name identity + per-refactoring-leaf contract (Use when/Motivation/Mechanics/Example) + Return Modified Value [web-only] / Split Phase [web-example] + no-dupes + no-scaffolding-phrases + index-row Use-when mirror"
      contains: "Return Modified Value"
    - path: ".claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs"
      provides: "bidirectional cross-ref consistency (inverse-of pairs mutually declared, smell<->refactoring), all cross-links resolve, NO self-referential links"
      min_lines: 30
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md"
      provides: "chapter-grouped catalog index (legend + Ch.6-12 headings) with the basic-chapter rows mirroring each leaf's Use when line"
      contains: "Provenance legend"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md"
      provides: "worked pilot leaf proving the leaf content contract + the oracle loop (Extract Function is the loop's validated exemplar in 07-ORACLE-MODEL)"
      contains: "Use when:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      via: "one chapter-grouped index row per leaf, Use-when mirrored"
      pattern: "\\]\\([a-z-]+\\.md\\)"
    - from: ".claude/skills/lz-refactor-workspace/tools/check-catalog.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      via: "walk leaves + assert 62-name identity + contract fields"
      pattern: "fowler-catalog"
---

<objective>
PILOT + harness overhaul for the LOCKED 62-scope Fowler catalog. Overhaul the Wave-1
checker battery from the superseded 66-count / tag-group / smell-table model to the LOCKED
62-count / per-refactoring-leaf / smell-leaf model, scaffold the chapter-grouped catalog
index, then author the basic chapter (Fowler Ch.6, "a first set of refactorings") as
per-refactoring leaves using the clean-room blind-author -> oracle-reviewer-gate -> converge
loop.

Purpose: this is the CALIBRATION run. It proves the full execution mechanic (blind author,
oracle-reviewer verdict, blind revise, converge; deterministic harness green) on ONE chapter
and records the working rubric anchors + round counts, so the wave-3 fan-out (07-04..07-09)
inherits a proven loop and a settled harness. It runs FIRST and ALONE in wave 2 (alongside
the already-complete 07-03 principles).

Output: the overhauled/added checkers (check-catalog, check-smells, check-crossrefs,
package.json), the chapter-grouped fowler-catalog/README.md index, and the ~11 basic-chapter
refactoring leaves, all oracle-converged and harness-green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-01-SUMMARY.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-refactor/references/principles.md
@plugins/lz-tdd/skills/lz-tpp/references/transformations.md
@plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md
@.claude/skills/lz-refactor-workspace/tools/check-catalog.mjs
@.claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs

<interfaces>
CLEAN-ROOM FIREWALL (07-ORACLE-MODEL): the main/executor context NEVER opens any file under
.oracle/refactoring-2e/ -- `ls` for names only. Book content is reached ONLY through the two
subagents, which read .oracle/ in their own isolated context and return own-words results:

- `oracle-reviewer` (Task/subagent) -- GATES a drafted leaf. Input: DRAFT_PATHS (repo leaf
  paths), SOURCE = ".oracle/refactoring-2e/index.md" + a chapter/topic (e.g. "Ch.6 -- a first
  set of refactorings / the basic set"; reference index.md + chapter number/topic, NEVER a
  book chapter filename), CONTENT_TYPE = "refactoring-leaf", SCOPE = the leaf's intended
  coverage, and the canonical per-axis anchors from 07-ORACLE-MODEL "Canonical per-axis
  anchors" (mechanics / motivation / example / applicability / spirit). Returns a raw JSON
  array of per-draft verdicts: pass | revise | blocked | error (revise carries directives;
  blocked carries ambiguities / unable-to-verify axes).
- `oracle` (Task/subagent) -- OPEN-ENDED lookups (e.g. "which chapter presents refactoring X?",
  "does Ch.6 include Y?") to confirm chapter membership without the main context reading the book.

Existing harness (07-01, keep the idiom): report(ok,label,detail) + SUMMARY: line + exit(0/1)
from verify-scaffold.mjs; extract-samples.mjs already walks fowler-catalog/*.md, extracts each
```ts fence as its own module, skips ```ts ignore, runs tsc --strict --noEmit (NO change needed;
per-refactoring leaves carry their ## Example fences). check-principles.mjs + check-hygiene.mjs
are unchanged and stay green. The 62 names + 1st-ed aliases + inverse-of pairs are public facts
in refactoring-com-overview.md (orientation only; NOT oracle content).

Basic-chapter (Ch.6) expected members (confirm chapter placement via the oracle subagent; do
NOT treat this list as authoritative -- it is a sizing scaffold): Extract Function, Inline
Function, Extract Variable, Inline Variable, Change Function Declaration, Encapsulate Variable,
Rename Variable, Introduce Parameter Object, Combine Functions into Class, Combine Functions
into Transform, Split Phase. Split Phase's extended examples are online-only -> its leaf carries
[web-example].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul the harness to the LOCKED 62-scope / per-leaf / smell-leaf model</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-catalog.mjs, .claude/skills/lz-refactor-workspace/tools/check-smells.mjs, .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs, .claude/skills/lz-refactor-workspace/package.json</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs (current 66-name / level-3-heading / tag-group model to REPLACE)
    - .claude/skills/lz-refactor-workspace/tools/check-smells.mjs (current smell-TABLE model to REPLACE with the smell-leaf model)
    - .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs (report/walkMd/SUMMARY/exit idiom to reuse)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Structure", "Content contracts", "Validation (harness) implications")
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Harness (deterministic)")
    - .planning/research/refactoring-com-overview.md (62 names = the 66 table MINUS Replace Magic Literal, Replace Control Flag with Break, Replace Error Code with Exception, Replace Exception with Precheck; plus aliases + inverse-of pairs)
  </read_first>
  <action>
    Rewrite check-catalog.mjs for the LOCKED model. Replace the 66-name list with the 62
    canonical names (drop the 4 cut relics: Replace Magic Literal, Replace Control Flag with
    Break, Replace Error Code with Exception, Replace Exception with Precheck). Set the web-only
    set to ONLY Return Modified Value and the web-example set to ONLY Split Phase. Change the
    entry model from "level-3 headings inside tag-group leaves" to "one leaf file per
    refactoring": each fowler-catalog/<slug>.md (excluding README.md) has a single level-1
    `# <Name>` heading; assert each of the 62 names is present exactly once across the leaves
    (identity, not cardinality), no dupes, and flag any unknown/typo leaf heading. Per leaf
    assert the contract fields: a one-line `Use when:` selector, a `## Motivation` section, a
    `## Mechanics` section, and a `## Example` section with at least one ts/js fence; assert the
    Return Modified Value leaf carries `[web-only]` and the Split Phase leaf carries
    `[web-example]`. Add a no-draft-scaffolding scan over the leaves (fail on TODO, "once it
    exists", "to be authored", "placeholder"). Add the index-row Use-when mirror: parse
    fowler-catalog/README.md rows and assert each leaf's `Use when:` line is mirrored verbatim
    in its README row (this stays RED until 07-10 fills all rows -- that is the expected
    baseline). Create check-crossrefs.mjs (new): walk all leaves + smells + smells.md, resolve
    every ]( ... .md#anchor ) cross-link against GitHub-style heading slugs (reuse the slug idiom
    from check-smells), assert inverse-of pairs are MUTUALLY declared (if A links B as inverse,
    B links A), assert smell<->refactoring links are bidirectional where both sides exist, and
    fail on any self-referential link (a leaf linking its own anchor). Treat any link whose target file is principles.md as FILE-LEVEL: assert the file exists but do NOT resolve #anchors into it (it is a hub doc; leaves name the section in link text). check-crossrefs is a PHASE-GATE check run FULLY only at 07-10 (when all 62 catalog + 24 smell leaves exist and cross-chapter compose-links resolve); per-wave it is at most a vacuous-green sanity run, never a content gate. Rewrite check-smells.mjs
    for the smell-LEAF model: assert 24 smell leaves under references/smells/<slug>.md each with a
    one-line `Recognize by:` + a `## Candidate refactorings` section whose links resolve to real
    fowler-catalog leaves, and assert references/smells.md is a navigation index carrying a
    recognize-by line + a resolving leaf link per smell (RED until 07-10). Update package.json
    `check` script to also run tools/check-crossrefs.mjs. Every checker keeps the report()/
    SUMMARY:/non-zero-exit idiom; add NO new dependency. Run the full battery and confirm the
    RED-by-design baseline (catalog 0/62, smells 0/24, crossrefs vacuously green, principles
    still green, hygiene still green, extract-samples vacuously green).
  </action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs; test $? -eq 1 && rg -q "Return Modified Value" .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs && ! rg -q "Replace Magic Literal" .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && rg -q "check-crossrefs" .claude/skills/lz-refactor-workspace/package.json && rg -q "62" .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs</automated>
  </verify>
  <done>check-catalog encodes the 62-name identity + per-refactoring-leaf contract + Return Modified Value [web-only] / Split Phase [web-example] + no-scaffolding + index-row mirror; check-crossrefs exists and runs; check-smells is on the smell-leaf model; package.json runs all five checkers; the full battery shows the expected RED-by-design baseline (0/62, 0/24) with no crash. The 4 cut relics are absent from the name list.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the index + author the basic (Ch.6) leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/inline-function.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-variable.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/inline-variable.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/change-function-declaration.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/encapsulate-variable.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/rename-variable.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-parameter-object.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/combine-functions-into-class.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/combine-functions-into-transform.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/split-phase.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- refactoring leaf + catalog index; "principles.md: ... Leaf -> principles back-edge" for the atomic-boundary cross-ref)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Content-fidelity discipline (validated on Extract Function)", "Comprehensive review dimensions", "Canonical per-axis anchors", "Driver responsibilities + canonical anchors")
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (Phase-6 stub -- keep the thin-index framing; drop the superseded D-04 deferral section)
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (PRIMARY in-repo analog -- built + oracle-converged, SAME skill: leaf voice, ASCII hygiene, and the ../ relative cross-link idiom the leaf back-edge uses)
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md + fibonacci-worked-example.md (secondary: tsc --strict-clean fence conventions, per-unit heading+prose+code rhythm, TOC idiom)
    - .planning/research/refactoring-com-overview.md (the basic-set names + 1st-ed aliases + inverse-of pairs)
  </read_first>
  <action>
    PREREQUISITE: ensure the converged oracle agents are live -- run /reload-plugins (or restart
    the session) so `oracle` and `oracle-reviewer` are loaded before any invocation. Confirm the
    firewall: do NOT open any file under .oracle/ from this context at any point.

    First scaffold fowler-catalog/README.md as a THIN chapter-grouped index: a one-line scope,
    a Provenance legend with exactly two markers -- `[web-only]` (Return Modified Value) and
    `[web-example]` (Split Phase's online-only examples) -- and seven chapter headings (Ch.6..Ch.12,
    each labelled by number + own-words topic, e.g. "Ch.6 -- a first set of refactorings (basic)").
    Fill ONLY the Ch.6 section now (07-10 fills Ch.7..Ch.12); leave the other six headings present
    but empty. Remove the stub's "Deferred: intra-catalog split axis (D-04)" section (superseded).
    Do NOT inline any entry content in README (SKEL-04). Do NOT touch SKILL.md.

    Then author the ~11 basic-chapter leaves (one file per refactoring, kebab-case slug of the
    canonical name) via the core clean-room loop, per leaf or per small batch:
    (1) AUTHOR BLIND from public knowledge + the refactoring-com scaffold (name + 1st-ed
    alias(es) + inverse-of), in the LOCKED leaf format: `# <Name>` (with `[web-example]` on Split
    Phase), an italic aliases line if any, a one-line `Use when:` selector, `## Motivation`
    (2-4 sentences, original words), `## Mechanics` (4-8 verb-first numbered steps, original words,
    naming the test/compile checkpoint inline), `## Example` (ONE compact tsc --strict-clean TS
    before->after, 5-15 lines/side; a 2nd only for a genuinely distinct variant, hard cap 2), and
    an optional `## Watch for` (0-3 bullets). Keep the Content-fidelity discipline: preserve every
    decision branch, keep safety checkpoints as their own steps, keep follow-up/compose steps as
    cross-links to sibling leaves (use the inverse-of graph; declare inverses mutually so
    check-crossrefs passes). Where a leaf's mechanics touch a published/consumed interface (e.g.
    Change Function Declaration), author the back-edge as a link to ../principles.md that NAMES the target atomic-boundary section in the link TEXT (do not rely on a specific #anchor resolving) rather than restating the gate. ASCII only (`->`, `--`, straight quotes).
    (2) GATE: spawn `oracle-reviewer` with DRAFT_PATHS (the drafted leaves), SOURCE
    ".oracle/refactoring-2e/index.md" + "Ch.6 -- the basic/first set of refactorings",
    CONTENT_TYPE "refactoring-leaf", SCOPE (each leaf's refactoring), and the canonical per-axis
    anchors from 07-ORACLE-MODEL. Use `oracle` for any open-ended chapter-membership lookup.
    (3) REVISE BLIND from the returned directives -- never opening the source.
    (4) ITERATE to convergence (every entry `pass`, or an owner-accepted `blocked`); the DRIVER
    caps rounds at ~3 (see Task 3 for the escalation path). Apply the fan-out aggregation guard:
    dedupe/cap merged oracle output; do NOT persist a full ordered name+chapter map.
    (5) Add each converged Ch.6 leaf's `Use when:` line, mirrored verbatim, as its README Ch.6
    index row (name + alias(es) + Use-when + relative link). Run the harness: extract-samples
    (tsc --strict) + check-catalog (partial RED baseline) + check-hygiene; commit on green (check-crossrefs is a PHASE-GATE check at 07-10, not run per-wave -- its compose cross-links point to later-chapter leaves that do not exist yet).

    Record for the wave-3 fan-out: the rubric anchors that worked, the per-leaf round counts, and
    any owner escalations (this is the calibration deliverable).
  </action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "Use when:" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md && rg -q "web-example" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/split-phase.md && rg -q "Provenance legend" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md && test $(git diff --name-only plugins/lz-tdd/skills/lz-refactor/SKILL.md | wc -l) -eq 0</automated>
  </verify>
  <done>All basic-chapter leaves exist as per-refactoring files with the full leaf contract, each oracle-reviewer verdict is pass (or owner-accepted blocked); every extracted example is tsc --strict-clean and behavior-preserving per the reviewer's example axis; README.md is a thin chapter-grouped index with the two-marker legend and the Ch.6 rows mirroring each leaf's Use when; check-hygiene green; SKILL.md unchanged. No .oracle/ file was opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Owner escalation for non-converging / blocked entries (conditional)</name>
  <decision>How to resolve any Ch.6 leaf the oracle-reviewer loop cannot converge within ~3 rounds or returns as blocked with an ambiguity the source alone cannot settle?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": oracle-reviewer is adversarial (assumes drift
    until proven otherwise), so a faithful draft can false-`revise` and a too-close<->drift
    rewrite can oscillate. The driver caps rounds (~3) and, on non-convergence or a `blocked`
    ambiguity, escalates that ENTRY to the owner via AskUserQuestion instead of looping
    indefinitely. This fires ONLY for entries that do not reach `pass`; a fully converging pilot
    skips this checkpoint. It is the owner's clean-room adjudication path (the owner may open the
    source; the executor must not).
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked entry as-authored</name>
      <pros>Unblocks a faithful draft the adversarial reviewer over-flagged; records the acceptance rationale</pros>
      <cons>Bypasses the automated gate for that one entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction, then re-run the loop once</name>
      <pros>Resolves a genuine fidelity gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked): open an
    AskUserQuestion presenting the entry, the reviewer's ambiguities/unable-to-verify axes (own
    words), and the two options. Apply the owner's choice, re-run the harness, and record the
    escalation + resolution in the SUMMARY. Do NOT reveal or request any verbatim source prose.
  </action>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every Ch.6 leaf is either oracle-reviewer pass or owner-adjudicated; the escalation and resolution (if any) are recorded in the SUMMARY. If nothing escalated, this checkpoint is a no-op noted as such.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-2e/` (owner full-text book, git-ignored) -> authored public-repo leaves | copyrighted expression must never cross into committed files; the ONLY crossing is via the `oracle`/`oracle-reviewer` subagents, which read in isolated context and return own-words verdicts/answers (no prose, code, or paths) |
| authored files -> public GitHub repo | anything committed is world-readable (IP + contact surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal (DST-04) | authored leaf prose + example code | mitigate | Clean-room firewall: main/executor context NEVER reads `.oracle/` (only `ls` for names); leaves authored BLIND; `oracle-reviewer` runs the full-strength near-verbatim (`too_close_to_source`) gate; `check-hygiene` no-verbatim heuristic; Phase-10 scan |
| T-07-03 | Tampering (fabricated / unfaithful content) | mechanics / motivation / example fidelity | mitigate | `oracle-reviewer` fidelity gate (pass\|revise\|blocked) + converge-to-clean loop; driver caps rounds (~3) and escalates blocked/non-converge to the owner (Task 3); no leaf shipped until pass or owner-accepted blocked |
| T-07-05 | Tampering / firewall (fan-out aggregation) | merged oracle output reconstructing the book's curated selection | mitigate | driver dedupes/caps the aggregate; does NOT persist a full ordered name+chapter map (07-ORACLE-MODEL Driver responsibilities) |
| T-07-04 | Tampering (silently-passing checker after the 66->62 scope change) | rewritten check-catalog / check-smells / check-crossrefs | mitigate | 62-name IDENTITY + per-leaf contract + bidirectional/no-self-ref cross-ref checks; RED-by-design baseline proven before content lands; every checker exits non-zero on failure |
| T-07-02 | Information Disclosure | work email in a committed leaf/checker | mitigate | `check-hygiene` work-email allowlist gate |

No UI, no network, no secrets, no new packages (the harness pins only `typescript@6.0.3`,
already present). Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- Full battery: extract-samples (tsc --strict) GREEN over the basic leaves; check-catalog shows the RED-by-design 11/62 partial (basic leaves well-formed); check-hygiene, check-principles GREEN. check-crossrefs is NOT a wave-2 gate (phase-gate check at 07-10); Task 1 only confirms it runs (vacuous-green).
- Every basic leaf reached oracle-reviewer `pass` (or owner-accepted `blocked`); the calibration record (anchors, round counts, escalations) is in the SUMMARY.
- README is a thin chapter-grouped index; SKILL.md untouched; no `.oracle/` file opened by this context.
</verification>

<success_criteria>
- The harness is on the LOCKED 62 / per-leaf / smell-leaf model; the 4 cut relics are gone; check-crossrefs exists and is wired into the check script.
- The basic (Ch.6) chapter is authored as oracle-converged per-refactoring leaves with tsc --strict-clean, behavior-preserving examples; the chapter-grouped README index is scaffolded with Ch.6 rows.
- The clean-room loop + rubric anchors are calibrated and recorded for the wave-3 fan-out.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-02-SUMMARY.md` when done, recording the calibrated rubric anchors, per-leaf round counts, any owner escalations, and the confirmed Ch.6 membership. It MUST also pin the canonical name->slug map for ALL 62 refactorings (kebab-case of each canonical name from refactoring-com-overview.md) as the shared cross-link-target contract that 07-04..07-09 and 07-10 consume, so inverse-of / see-also / candidate cross-links resolve deterministically at the 07-10 check-crossrefs gate (which detects a dangling link but cannot fix a catalog leaf in-scope).
</output>
