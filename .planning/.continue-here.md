---
context: default
phase: 21-applied-red-eval-real-oss-repos
task: null
total_tasks: 0
status: paused
last_updated: 2026-07-26T22:46:20.724Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Each was discovered through failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: eval-run-approval-gate (D-11) -- NO metered run (claude -p / apply fan-out /
      judges) without an explicit FRESH user go-ahead. Build + zero-spend selfcheck freely; HALT at
      the run gate and present it. (The org MONTHLY spend cap was hit once mid-run during Phase-20
      EVL-02.) **This is the ONLY thing outstanding on Phase 21 -- nothing technical is blocking.**
- [ ] CONSTRAINT: --auto discuss trap -- `/gsd-core:discuss-phase 21 --analyze --auto --chain`
      auto-locks every gray area in one pass with no human checkpoint. Phase 21's scoping gray areas
      (which real OSS repo(s), how many red-test-shaped tasks, which of the 9 dimensions are GATES vs
      report-only, competitor-round timing) are HIGH-IMPACT. Auto-lock ONLY where evidence-backed;
      the ROADMAP Phase 21 design is pre-locked to steer --auto to GOOD values. Record any
      genuinely-uncertain choice (e.g. a specific OSS target that is not clearly red-test-shaped) as
      an UNRESOLVED / BLOCKER in CONTEXT.md -- do NOT self-approve a bare default. See CLAUDE.md
      memory `gsd-discuss-phase --auto` trap quadrant.

**Do not proceed past these until acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Coaching-prose-over-synthetic-scenarios | What Phase-20 EVL-02 did: over-weights the skill's house VOCABULARY, not applied substance | blocking | Phase 21 is APPLY-based in REAL OSS repos, graded on produced test code/diff; report SUBSTANCE-ONLY where any vocabulary proxy is used |
| Metered run without approval | Phase 21 apply fan-out spends real tokens; org monthly cap was hit once | blocking | Present the run set + WAIT for explicit approval before any claude -p |
| --auto locks a bad HIGH-IMPACT choice | discuss --auto has no human checkpoint | blocking | Steer with the pre-locked ROADMAP design; record uncertain choices as UNRESOLVED, don't auto-lock |
| Checker resolves a DIFFERENT toolchain than the target's | atc resolves `@angular/compiler-cli` from its OWN dependency tree, not radix's -- so the compiler doing the template check is not the one the target ships. Departs from the grade-with-the-target's-toolchain principle the per-grade toolchain copy upholds everywhere else | advisory | ACCEPTED knowingly (the alternative is installing into a borrowed repo, which is forbidden) and recorded as an OPEN residual in RUN-GATE. When choosing a checker for a target, ASK which toolchain it actually resolves against and record the answer -- never assume it inherits the target's. Read an Angular-coded `compile_error` cluster against the target's own Angular version before calling it a model failure |
| Inheriting a handoff's stated MECHANISM without re-measuring it | This very file's predecessor asserted the RXF diagnostic was caused by `tsconfig.spec.json`'s `"types"` array omitting jest-dom. FALSE -- jest-dom's types DO load; the cause is `@types/jest-axe`'s triple-slash `reference types="jest"`. The wrong mechanism sat in the handoff as settled evidence and would have been inherited. A second premise in the same session ("tsconfig.spec.json is editor-only") was also wrong -- the tagged Analog source shows it IS the runner's compile config, with semantic diagnostics merely masked | blocking | A handoff's CONCLUSIONS are reusable; its stated MECHANISMS are not. Re-derive any load-bearing mechanism with a command (`tsc --explainFiles`, reading the dependency's tagged source) before building on it. When a handoff says "verify this, do not rediscover it", verify means RUN IT |
| A cell whose verdict is decided by a DESIGN choice rather than by the dimension under test | On RXL the D-06 pass/fail tracked WHICH INJECTION TOKEN the model imagined -- an existing framework token compiles and yields assertion-red, the library's own not-yet-existent token cannot compile. Neither arm asserted the rendered output the cell's own traps require, so even the PASS was incidental. Two samples were needed to tell "systematic" from "variance" | blocking | Before funding a round, ask of each cell: could two defensible designs produce opposite verdicts? If yes, that cell measures design choice, not discipline -- scope it to the dimensions where it discriminates and say so in RUN-GATE Step 7 BEFORE the round, never after an inconvenient number |
| Gitignored resume/state files are INVISIBLE to worktree agents | `.gitignore:22` matches `.continue-here.md` at ANY depth, so the phase-level resume file is local-only. A plan instructed an executor to update it; inside the worktree it did not exist, and the task's own `! rg -q -P <pat> <missing-file>` verify clause evaluated TRUE -- it would have false-passed either way. Same class bit the workspace `node_modules`, gitignored while its manifests are tracked, so atc was unresolvable after worktree cleanup | advisory | Before naming a file in a worktree-agent plan, check `git check-ignore -v <path>`. An absence-assertion (`! rg -q ... <file>`) is VACUOUS on a missing file -- assert the file exists first, or key the check on content that must be present |
| Ponytail leaks into in-session Agent subagents | env-var PONYTAIL lock does NOT reach subagents; asides pollute output | advisory | Put an explicit "normal mode / stop ponytail" directive in every subagent prompt |
| Too many subagents in parallel | user flagged 24-in-flight; org cap hit | advisory | Fan out in SMALL waves; resume spend-limit-killed agents via SendMessage(agentId) |

<current_state>
Milestone lz-tdd@0.0.3, Phase 21 (Applied RED Eval in Real OSS Repos, multi-dimensional, 3-arm).
The instrument is BUILT, hardened and GREEN, and the CORPUS IS COMPLETE: 4 cells across 3 suites
(GRC armed smoke anchor, SRVC out-of-domain control, RXF + RXL in-domain drive discriminators).
Three user-approved pilot runs captured ($3.26; about $4.93 across all pilots to date).

The JestMatchers question this phase paused on is SETTLED and the RXF verdict is DECIDED -- see
`21-ATC-ADOPTION.md`. The radix cells now grade through `angular-typechecker` (atc) rather than tsc,
because tsc is structurally BLIND to Angular template diagnostics (MEASURED: 0 against atc's 15 on
the identical project and tsconfig). Full zero-spend battery exits 0 in 404.5 s with ELEVEN
fabricated runDirs and no skips.

NOTHING TECHNICAL IS BLOCKING. Only spend approval is outstanding.
Milestone close is HELD until Phase 21 completes (user's choice).
</current_state>

<completed_work>

- Phase 20 EVL-01 (trigger) + D-09 description widen -- done 2026-07-21 (801d0ad / 8cd13a8), live.
- Phase 20 EVL-02 coaching-prose behavior benchmark -- RAN 2026-07-22 (aa9c170): 60 coach runs +
  10 judges + merge/verify + aggregate + Pass@k; unbiased reviewer PASS-WITH-CAVEATS. Honest headline
  substance-only: with_skill Pass@1 0.97 (29/30) vs baseline 0.87 (26/30); edge concentrated on
  eval-8 + eval-9. Full-run 0.87-vs-0.50 was vocabulary-inflated (adopted correction). Record:
  .claude/skills/lz-red-workspace/EVAL-RESULTS.md.
- Phase 21 discussed, planned and BUILT across plans 21-01 through 21-04, then hardened by a series
  of quick tasks. The RED apply instrument, its 7-class D-06 classifier, RED attribution, the
  differential typecheck, the per-grade disposable toolchain copy and the containment guards are all
  in place and asserted. gsd-verifier PASSED 8/8 with the bisect run IN FULL (15 trees, 45/45
  checks, zero skips).
- Three suites wired: GRC (armed kata anchor), SRVC (h3js/srvx, out-of-domain control), and
  radix-ng/primitives @ 4a7390a2 (RXF + RXL, the in-domain drive discriminators).
- Advisory A-1 closed with two guards: a typecheck pass that did not really run can no longer read
  as clean.
- The differential made POSITION-INSENSITIVE (multiset on file + code + message) after a metered
  pilot showed 8 of 9 reported NEW errors were baseline diagnostics that merely SHIFTED.
- **SETTLED (quick 260726-uqn): the JestMatchers research.** The cause is `@types/jest-axe`'s
  triple-slash `reference types="jest"`, which pulls in `@types/jest`, whose `expect` returns
  `JestMatchers<T>` -- a type `@testing-library/jest-dom/vitest` never augments (it augments vitest's
  `Assertion`). Proven with `tsc --explainFiles`. The `types`-array premise the phase paused on is
  FALSIFIED: jest-dom's types DO load. Also verified the runner cannot fail on type errors at all
  (Analog v2.6.1 defaults `disableTypeChecking` true; radix passes no options), so nothing in the
  repo typechecks specs.
- **DECIDED (quick 260726-uqn): RXF's `compile_error` verdict STANDS**, with no matcher allowlist, no
  baseline-keyed noise rule and no target-specific tolerance. The consequence is recorded as a
  residual in RUN-GATE instead.
- **atc adopted for the radix cells only** (quick 260726-uqn), with fail-closed per-target checker
  selection, an atc JSON normalizer feeding the UNCHANGED multiset and classifier, the
  vacuous-differential guard extended to atc's own file-less synthesized faults, and TWO new
  canaries: `canary-rdxf-template` (a defect atc reports and tsc does not) and `canary-rdxf-append`
  (the first append-onto-a-dirty-file canary in the suite). Differential re-priced ~7.8 s -> ~19 s
  per radix grade.
</completed_work>

<remaining_work>

- **The `with_skill` pilot is DONE (2026-07-27, $1.9286, 2 runs on RXL) -- do NOT re-buy it.** Record:
  `21-WITHSKILL-PILOT.md`. **D-04 ANSWERED: auto-trigger fired 2/2**, corroborated four ways per run;
  first `with_skill` firing ever on SRVC/RXF/RXL. Coach-don't-drive **3/3 PERFECT** (zero production
  files every run). FINDING: **RXL's mechanical D-06 verdict is DESIGN-CONFOUNDED** -- it tracks which
  injection token the model imagined (`invoke_skill` picked the existing `LOCALE_ID` -> `genuinely_red`;
  both `with_skill` runs picked the library-mirroring `RDX_LOCALE`, absent at the pin -> `compile_error`),
  and neither arm asserted the rendered output the cell's own traps require, so even the PASS is
  incidental. **OWNER DECISION: report RXL on the DISCIPLINE DIMENSIONS ONLY**, excluded from the
  headline; no gate change, no prompt change, no verdict reclassified. Enforced in RUN-GATE Step 7.
- ONE metered item remains and needs FRESH explicit approval: the full round, 4 cells x 3 arms x k=3 =
  36 runs, ~$39 at the MEASURED $1.09/run. Read RUN-GATE Step 7 BEFORE tabulating.
- After the round: /gsd-secure-phase 21, /gsd-validate-phase 21, /gsd-extract-learnings 21.
- Then /gsd-complete-milestone lz-tdd@0.0.3 (manual archival for the @-scoped version).
</remaining_work>

<decisions_made>

- Phase 21 = apply-based, real-OSS-repo, short-human-prompt, 3-arm (no_skill / with_skill auto-trigger
  / invoke_skill force-invoke), 9-dimension eval; mattpocock:tdd competitor DEFERRED + standalone-
  confirmed (broader-scope caveat). Full rationale in .planning/HANDOFF.json `decisions` + ROADMAP.
- Report SUBSTANCE-ONLY as the headline wherever a vocabulary proxy is used (Phase-20 lesson).
- **RESOLVED -- the differential typecheck is KEPT on radix, and the decision HELD.** It was flagged
  as "REVISIT after the JestMatchers research, the runtime-registration evidence points the other
  way". The research is DONE and it did NOT overturn it. Two measurements settled it: (1) the
  differential is LOAD-BEARING -- the real `classify()` on identical runner input gives
  `compile_error`/pass:false at 1 new error and `genuinely_red`/pass:TRUE at 0, so `wrong_reason` does
  not cover the type-broken canary and dropping the typecheck would open a false pass; (2) the
  runtime-registration argument cuts the OTHER way once the mechanism is known -- the matchers are
  registered at runtime but land on `JestMatchers<T>`, a type jest-dom never augments, so the
  diagnostic is a genuine property of the program rather than a config oversight.
- **atc is the differential checker for the RADIX cells ONLY**; GRC and SRVC stay on tsc,
  byte-identically. `typecheck.checker` DEFAULTS to `tsc` so absent config means the historical
  behaviour, while every malformed form throws before the grading worktree exists -- an unknown
  checker has no knowable stronger fallback, so guessing could silently REDUCE the differential.
</decisions_made>

<blockers>
- Phase 21 metered run: needs fresh user approval (D-11). NOT technical, and it is the only one left.
</blockers>

## Required Reading (in order)
1. `.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-ATC-ADOPTION.md`
   -- the corrected root-cause mechanism, the FALSIFIED `types`-array premise, the
   load-bearing-typecheck counterfactual, the atc A/B, the RXF verdict decision and its accepted
   residual. Read this BEFORE re-opening anything about JestMatchers or the RXF verdict.
2. `.planning/HANDOFF.json` -- structured state + full decision list + context_notes (env/pitfalls).
3. `.claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md` -- the run procedure, the
   RE-PRICED per-target grading costs, and the residual list (including the atc adoption's accepted /
   closed / open parts).
4. `.planning/ROADMAP.md` Phase 21 section -- the locked design + 5 success criteria.
5. `.claude/skills/lz-red-workspace/EVAL-RESULTS.md` -- the Phase-20 EVL-02 record + reviewer verdict.
6. CLAUDE.md memories: `lz-skill-eval-design-apply-based-multidim`, `lz-red-evl02-substance-vs-vocabulary`,
   `eval02-subagent-orchestration-mechanic`, `eval-run-approval-gate`, and the `gsd-discuss-phase --auto`
   trap.

## Infrastructure State
- The RED gate: `.claude/skills/lz-red-workspace/grade-red.mjs` (+ `--selfcheck`, offline, zero spend)
  and `selfcheck-red.mjs` (the full battery; ~6.5-7 min, 404.5 s measured -- BACKGROUND it, gate on
  the PROCESS EXIT STATUS, and never shorten it: a SKIP is not a pass).
- **A FRESH WORKTREE must run `npm install` in `.claude/skills/lz-red-workspace` first.** That
  `node_modules` is gitignored while its `package.json` and `package-lock.json` are tracked, so the
  atc CLI cannot resolve until it is installed -- it fails closed with a message naming that step.
- Harness reused: `.claude/skills/lz-refactor-workspace/` (run-e2e.mjs, tabulate-mechanical.mjs,
  selfcheck-*.mjs) + oracle/oracle-reviewer for book authenticity (DST-04).
- FOUR borrowed repos are READ-ONLY and must end git-clean: the GildedRose kata (ONE worktree,
  `TypeScript/node_modules` 308 non-dotted, no named branch, `--detach` only), h3js/srvx,
  radix-ng/primitives-pin (THE EVAL SOURCE, detached at 4a7390a2), and radix-ng/primitives (the
  OWNER'S checkout -- never open, install into, or modify; its dirty files are their own work). Also
  do not modify LayZeeDK/angular-typechecker or analogjs/analog.
- Branch gsd/lz-tdd-0.0.3-lz-red, not pushed.

<context>
User reviewed the modest-but-honest Phase-20 EVL-02 result and decided the coaching-prose eval is only
a first-pass record; they want a stronger APPLIED eval (Phase 21, like lz-refactor's Phase 13/14)
before shipping 0.0.3. The design is locked in ROADMAP. The build is zero-spend; only the eventual
apply RUN is gated -- and that gate is now the single thing standing between this phase and closure.
</context>

<next_action>
Present the two GATED metered items and WAIT for a fresh explicit approval naming the scope. Do NOT
self-approve either: (1) optionally one `with_skill` cell at ~$1.10, the only way to measure the D-04
auto-trigger dimension; (2) the full 4 cells x 3 arms x k=3 = 36-run round at ~$39. Before driving,
re-read `21-ATC-ADOPTION.md` and RUN-GATE's residual list -- the radix cells now grade through atc,
the per-grade differential is re-priced from ~7.8 s to ~19 s (so a k=3 round's grading is ~22-23 min
rather than ~19-20), and a `compile_error` cluster on RXF should be read against the recorded
`new_tsc_error_lines` before attributing anything to an arm. There is no remaining research or
verdict to settle.
</next_action>
