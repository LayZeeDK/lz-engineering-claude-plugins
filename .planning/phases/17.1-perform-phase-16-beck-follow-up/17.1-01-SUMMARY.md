---
phase: 17.1-perform-phase-16-beck-follow-up
plan: 01
subsystem: testing
tags: [lz-red, provenance, clean-room, DST-04, oracle-reviewer, Kent-Beck, Canon-TDD]

# Dependency graph
requires:
  - phase: 16-source-distillation-core-red-references
    provides: the shipped three-laws / test-structure / principle-backing RED references whose Beck rows are re-tiered here
  - phase: 17-assertion-design-stance-router-ts-vitest-mechanics
    provides: the clean-room re-gate loop + the principle-backing map + the deterministic battery
provides:
  - Beck RED selection surfaces (running test list, one small step, starter case) re-tiered no-oracle -> owned against Canon TDD
  - Beck triangulation surface re-tiered no-oracle -> owned against First One, Then Many
  - Beck structure surfaces (assert-first, evident test data) provisionally flipped to owned against Canon TDD (conditional, gate-decided)
  - principle-backing map + Sources paragraph reconciled to the post-provisional row set (no owned cell cites the book)
  - a Phase-16 owned-source-upgrade note in 16-CONTEXT.md
affects: [17.1 orchestrator oracle-reviewer gate, Phase 18 coach-procedure authoring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Four-surface provenance consistency: intro blockquote + per-doc ## Sources entry + principle-backing map row/Source cell + principle-backing Sources paragraph flip together per row"
    - "Owned Source cell names the FREE gated essay (Canon TDD / First One, Then Many), never the summary-only book (D-05 honesty gate)"
    - "Provisional flip: executor flips candidate/conditional rows blind; the orchestrator oracle-reviewer gate confirms or reverts before phase close"

key-files:
  created:
    - .planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-01-SUMMARY.md
  modified:
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
    - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
    - .planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md

key-decisions:
  - "Degenerate/starter case (D-03) provisionally cited against Canon TDD (grouped with the two other canonical-loop-opening moves); First One, Then Many reserved for triangulation. Gate decides final fate."
  - "Assert-first + evident test data (D-04) provisionally cited against Canon TDD as the single free candidate for the bundle; expected to revert to no-oracle at the gate (primary source is a summary-only book)."
  - "Provisional Sources-paragraph caveat DELETED (all Beck rows provisionally owned); orchestrator re-adds a caveat naming any reverted row + WHY at finalize."

patterns-established:
  - "Split-a-bundle: upgrading a subset of a bundled Beck clause/entry means splitting owned moves into their own owned sentence/entry and leaving the rest no-oracle"
  - "Conflation guard held: the RCM only-enough-to-fail row/attribution stays owned-RCM and is never folded into the Beck one-step upgrade"

requirements-completed: [SEL-01, SEL-02, STR-02, DST-03, DST-04]

coverage:
  - id: D1
    description: "three-laws Beck selection surfaces re-tiered to owned across blockquote + Sources block (running test list, one small step, starter case -> Canon TDD; triangulation -> First One, Then Many)"
    requirement: "SEL-01"
    verification:
      - kind: automated
        ref: "node .claude/skills/lz-red-workspace/tools/check-red-references.mjs"
        status: pass
    human_judgment: true
    rationale: "The deterministic battery proves tokens/fences/scaffold intact, but the 'owned; oracle-verified' fidelity claim is decided by the pending orchestrator-driven oracle-reviewer gate against canon-tdd.md / tri-first-one-then-many.md (executor edited BLIND, DST-04)."
  - id: D2
    description: "test-structure Beck structure surfaces (assert-first, evident test data) PROVISIONALLY flipped to owned against Canon TDD"
    requirement: "STR-02"
    verification:
      - kind: automated
        ref: "node .claude/skills/lz-red-workspace/tools/check-red-references.mjs"
        status: pass
    human_judgment: true
    rationale: "Conditional per D-04/D-05: primary source is Test-Driven Development by Example (Access: book, summary-only). Expected to revert to no-oracle unless a free essay actually gates them under the orchestrator oracle-reviewer gate."
  - id: D3
    description: "principle-backing map rows (Source cell = free essay, tier = owned) + Sources paragraph reconciled; four provenance surfaces consistent; full deterministic battery GREEN"
    requirement: "DST-03"
    verification:
      - kind: automated
        ref: "node .claude/skills/lz-red-workspace/tools/check-red-references.mjs"
        status: pass
      - kind: automated
        ref: "npm --prefix .claude/skills/lz-red-workspace run typecheck"
        status: pass
      - kind: automated
        ref: "node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs"
        status: pass
    human_judgment: true
    rationale: "Row-set reconciliation depends on the gate's confirm/revert verdicts for the provisional rows; the caveat must be re-added by the orchestrator at finalize for any reverted row."
  - id: D4
    description: "Phase-16 owned-source-upgrade note appended to 16-CONTEXT.md (content-unchanged claim + provisional rows + why)"
    verification:
      - kind: automated
        ref: "git grep -n 'Owned-source upgrade (2026-07-20, added during Phase 17.1)' -- .planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md"
        status: pass
    human_judgment: false

# Metrics
duration: ~11min
completed: 2026-07-20
status: complete
---

# Phase 17.1 Plan 01: Blind owned-source tier upgrade of the Beck RED surfaces Summary

**Kent Beck's RED selection/structure surfaces re-tiered no-oracle -> owned against his now-provisioned free essays (Canon TDD for the running test list, one small step, and the starter case; First One, Then Many for triangulation), with assert-first + evident test data flipped provisionally pending the orchestrator oracle-reviewer gate; four provenance surfaces reconciled and the full battery GREEN.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-07-19T23:29:28Z
- **Completed:** 2026-07-19T23:40:22Z
- **Tasks:** 3
- **Files modified:** 4 (+1 SUMMARY created)

## Accomplishments
- Re-tiered the three locked-eligible Beck selection moves to `Owned; oracle-verified against the clean-room source.` across their intro blockquote and `## Sources` entry in three-laws-and-test-selection.md: running test list, one small step, and the starter case cited against Canon TDD; triangulation cited against First One, Then Many.
- Provisionally flipped the two D-04 structure moves (assert-first, evident test data) to owned against Canon TDD in test-structure-and-assertions.md, split cleanly out of the Wake/North/Khorikov no-oracle clause and entry (which stay no-oracle). These are EXPECTED to revert at the gate.
- Flipped all six Beck rows in the principle-backing.md map (Source cell now names the free gated essay, tier cell now owned), kept every `[Recommendation](doc.md)` link text exactly (including the pre-existing "Triangulate to the next test" wording), extended the owned-sources parenthetical, and deleted the now-stale pending-upgrade caveat. The no-oracle-sources sentence (Khorikov) and the intro blockquote were left untouched.
- Added the `## Owned-source upgrade (2026-07-20, added during Phase 17.1)` note to 16-CONTEXT.md recording the re-open of closed Phase-16 content, the content-unchanged claim, and the expected-to-revert rows.
- Held the full deterministic battery GREEN (all three checks exit 0) and the DST-04 clean-room (no essay prose read; nav Access column only).

## Task Commits

Each task was committed atomically:

1. **Task 1: Split/upgrade Beck selection surfaces in three-laws** - `06b29f5` (docs)
2. **Task 2: Provisionally flip Beck structure surfaces in test-structure** - `45d49da` (docs)
3. **Task 3: Flip principle-backing rows + Sources paragraph + add Phase-16 note + full battery** - `bb90953` (docs)

**Plan metadata:** this docs commit (SUMMARY + STATE + ROADMAP)

## Files Created/Modified
- `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md` - intro blockquote + `## Sources` Beck entry split; test list/one-step/starter -> Canon TDD owned, triangulation -> First One, Then Many owned.
- `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md` - intro blockquote + `## Sources` Beck entry split; assert-first + evident test data provisionally owned against Canon TDD (conditional).
- `plugins/lz-tdd/skills/lz-red/references/principle-backing.md` - six Beck map rows flipped (Source cell -> free essay, tier -> owned); Sources paragraph owned-list extended + caveat deleted; no-oracle sentence + intro blockquote untouched.
- `.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md` - appended the Phase-17.1 owned-source-upgrade note.

## Which rows were upgraded, and against which free essay

| Surface | Row | Free essay cited | Tier now | Fate |
|---------|-----|------------------|----------|------|
| 1 | Keep a running test list | Canon TDD | owned | locked-eligible (D-01) -- gate confirms |
| 2 | Take one small step | Canon TDD | owned | locked-eligible (D-01) -- gate confirms |
| 4 | Start from the degenerate/starter case | Canon TDD | owned (provisional) | candidate (D-03) -- gate decides |
| 3 | Triangulate to the next test | First One, Then Many | owned | locked-eligible (D-02) -- gate confirms |
| 5 | Assert-first | Canon TDD | owned (provisional) | conditional (D-04) -- gate expected to REVERT |
| 6 | Evident test data | Canon TDD | owned (provisional) | conditional (D-04) -- gate expected to REVERT |

No owned Source cell cites "Test-Driven Development by Example" (verified: 0 hits remain in the three reference docs).

## Decisions Made
- **Degenerate/starter case essay pick (D-03):** cited Canon TDD (not First One, Then Many). Rationale: the degenerate-first opening ("the empty collection, zero, null ... before any interesting case") is framed as the opening move of a new behavior -- a canonical-loop-opening practice, grouped with the running test list and the one-step move under the canonical-loop essay; First One, Then Many is reserved for its titular subject (triangulation). This is a blind provisional pick; the gate confirms, reassigns, or reverts.
- **Assert-first + evident data essay pick (D-04):** cited Canon TDD as the single strongest free candidate for the bundle (candidates were Canon TDD / Desirable Unit Tests / Composable Tests). These are EXPECTED to revert (primary source is a summary-only book).
- **Sources-paragraph caveat:** deleted in the provisional state (all Beck rows provisionally owned). The orchestrator must re-add a caveat naming any reverted row and WHY at finalize.

## Deviations from Plan

None - plan executed exactly as written (blind edits + battery GREEN; the oracle-reviewer gate and provisional reconciliation are the orchestrator's post-execution step, not an executor task).

## Issues Encountered
None. The state-update handlers (`state.record-metric`, `state.add-decision`, `state.record-session`) required `--flag` form rather than positional args; re-invoked with flags successfully.

## PENDING ORCHESTRATOR POST-EXECUTION GATE (D-07) -- REQUIRED BEFORE PHASE CLOSE

The gsd-executor has no Agent/Task tool, so the oracle-reviewer gate is the ORCHESTRATOR's job. It is NOT done. Before this phase closes the orchestrator MUST:

1. Run `oracle-reviewer` per upgraded surface, pointing it at the correct essay:
   - running test list + take one small step -> `.oracle/written-content/canon-tdd.md`
   - triangulation -> `.oracle/written-content/tri-first-one-then-many.md`
   - degenerate/starter case (D-03) -> `.oracle/written-content/canon-tdd.md` (executor's cite)
   - assert-first + evident test data (D-04) -> `.oracle/written-content/canon-tdd.md` (executor's cite)
2. Converge-to-clean (cap 3 rounds): PASS -> keep; Revise -> executor applies own-words directive BLIND + re-runs battery; Blocked -> REVERT that row to `Unowned; high-confidence core only (no-oracle).` across ALL FOUR surfaces (blockquote, Sources entry, principle-backing row + Source cell) and reconcile the principle-backing caveat to name the row + WHY.
3. Re-split note: assert-first (surface 5) and evident-test-data (surface 6) are bundled into ONE test-structure blockquote clause + ONE Sources entry but are INDEPENDENT principle-backing rows; if the gate resolves them differently, finalize MUST re-split the bundled clause/entry so each row carries its own tier. The same applies if the degenerate case is reverted while the other Canon-TDD rows PASS (the three-laws owned Sources entry and blockquote bundle test list + one-step + starter together).
4. Finalize: battery GREEN + Phase-16 note present + every upgraded row PASS-confirmed or reverted-and-reconciled, THEN the phase-complete commit. Commit identity = public maintainer gmail.

## Requirements note
SEL-01, SEL-02, STR-02, DST-03, DST-04 were TOUCHED as a provenance-tier upgrade only. They remain **Complete** for Phase 16; their REQUIREMENTS.md status is NOT reopened and delivered behavior/coverage is unchanged.

## Next Phase Readiness
- Executor deliverables complete and battery GREEN; ready for the orchestrator oracle-reviewer gate + finalize.
- No blockers. Phase 18 (coach procedure + lz-tpp seam) is unaffected by this tier edit.

## Self-Check: PASSED
- All 3 task commits exist: `06b29f5`, `45d49da`, `bb90953` (verified via git log).
- No "Test-Driven Development by Example" remains in the three edited reference docs (git grep: 0 hits) -- no owned cell cites the book.
- All six Beck principle-backing rows now cite Canon TDD or First One, Then Many (git grep verified).
- Full deterministic battery exits 0: check-red-references.mjs (RED-REFS GREEN 10/10), lz-red-workspace typecheck (7 modules tsc --strict clean), check-hygiene.mjs (198 files ASCII + email + 191 no-verbatim clean).
- SUMMARY file created at `.planning/phases/17.1-perform-phase-16-beck-follow-up/17.1-01-SUMMARY.md`.

---
*Phase: 17.1-perform-phase-16-beck-follow-up*
*Completed: 2026-07-20*
