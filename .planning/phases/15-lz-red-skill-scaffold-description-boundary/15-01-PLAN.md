---
phase: 15-lz-red-skill-scaffold-description-boundary
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/skills/lz-red/SKILL.md
  - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/naming.md
  - plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
  - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03]

must_haves:
  truths:
    - "**D-01:** references/ tree is the exact 10-file ARCHITECTURE layout -- 6 flat single-topic docs plus one testing-stance/ navigation subdir (index + 3 leaves), clustered by decision-procedure step (not per-author, not 1:1 with phases)"
    - "**D-02:** each of the 10 reference stubs is a thin content contract -- H1 title + a Scope line + a 'Populated in Phase NN' marker + the requirement IDs it satisfies + its source cluster + a scoped bullet list + a Sources placeholder -- with NO synthesized prose and NO TypeScript examples"
    - "**D-04:** SKILL.md ships the router body only (frontmatter, Two modes, three-way RED/green/refactor seam intro, a 'listen to the tests' heuristic caveat, and a Reference material pointer section); the numbered coach decision procedure is a labeled placeholder deferred to Phase 18"
    - "**D-05:** SKILL.md frontmatter is dual-mode-by-omission -- only name (== lz-red, the directory) and description; version, disable-model-invocation, user-invocable, and allowed-tools are all omitted so the skill both auto-triggers (coach) and answers /lz-tdd:lz-red (reference)"
    - "**D-06:** the v1 description leads with the positive RED-phase trigger and carries two reciprocal near-miss exclusion clauses in the tail (make a failing test pass -> lz-tpp; restructure/clean up passing code -> lz-refactor); it is language-agnostic (no Vitest/TypeScript named)"
    - "**D-07:** the description is sized to the ~1000-1536 load-bearing window with the two exclusions in the tail and the positive trigger first; folded length <= 1536 (the v1 draft measures 1083)"
    - "SKL-01: /lz-tdd:lz-red is invocable -- plugins/lz-tdd/skills/lz-red/SKILL.md exists, frontmatter carries name + description only, and claude plugin validate . exits 0"
    - "SKL-02: SKILL.md is a lean router (< 500 lines, near lz-tpp's 81) that links all 10 reference stubs, each of which exists and carries its content-contract markers"
    - "SKL-03: the description is present, folded length <= 1536, names both sibling exclusions (lz-tpp and lz-refactor), and the positive trigger precedes the exclusions"
  artifacts:
    - "plugins/lz-tdd/skills/lz-red/SKILL.md (router, ~90-120 lines)"
    - "plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (flat stub)"
    - "plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (flat stub)"
    - "plugins/lz-tdd/skills/lz-red/references/naming.md (flat stub)"
    - "plugins/lz-tdd/skills/lz-red/references/anti-patterns.md (flat stub)"
    - "plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md (flat stub)"
    - "plugins/lz-tdd/skills/lz-red/references/principle-backing.md (flat stub)"
    - "plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md (navigation-index stub)"
    - "plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md (leaf stub)"
    - "plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md (leaf stub)"
    - "plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md (leaf stub)"
  key_links:
    - "SKILL.md name field == directory name (lz-red) so Claude Code registers the skill as /lz-tdd:lz-red"
    - "every relative link in SKILL.md's Reference material section resolves to an existing stub file (case-correct: testing-stance/README.md, not readme.md)"
    - "the description's two exclusion clauses name lz-tpp and lz-refactor -- the three-way seam boundary that keeps the skill from cross-firing"
    - "skills are auto-discovered from the plugin root, so no plugin.json edit is needed for lz-red to register"
  prohibitions:
    - "**D-03:** do not create per-author stub files, and do not create .claude/skills/lz-red-workspace/ or any .oracle/ content this phase (eval workspace is Phase 20; oracle books are Phase 16)"
    - "**D-08:** do not empirically tune the description -- ship the reasoned v1; the cross-skill trigger eval and any re-tuning are Phase 20 (EVL-01)"
    - "**D-09:** do not bump plugins/lz-tdd/.claude-plugin/plugin.json -- it stays at 0.0.2 (the 0.0.3 bump is Phase 19 / DST-01)"
    - "**D-10:** do not edit .claude-plugin/marketplace.json -- it stays unchanged (version deliberately omitted; skills auto-discovered)"
    - "do not edit plugins/lz-tdd/skills/lz-tpp/SKILL.md -- the reverse lz-tpp -> lz-red pointer is SEAM-02 = Phase 18"
    - "do not author real reference prose, TypeScript, or Vitest examples in any stub -- content is Phases 16-17"
---

<objective>
Scaffold the invocable dual-mode `lz-red` skill: a lean `SKILL.md` router carrying the v1 three-way-guarded triggering description, plus ten thin reference stubs behind progressive disclosure. This mirrors the shipped lz-tpp and lz-refactor scaffolds at lz-tpp grain -- Phase 6 (lz-refactor scaffold) is the direct precedent. This is a copy-adapt phase, not a design phase: every one of the 11 files has an exact in-plugin analog.

Purpose: SKL-01/02/03 -- register `/lz-tdd:lz-red`, stand up the progressive-disclosure tree so Phases 16-18 have unambiguous fill targets, and ship a reasoned description that fires on RED intent ("choose / write the next failing test") without cross-firing lz-tpp (green step) or lz-refactor (refactor step).

Output: 11 new files under `plugins/lz-tdd/skills/lz-red/` (1 router + 10 stubs). No manifest edits (per **D-09** plugin.json stays 0.0.2; per **D-10** marketplace.json unchanged), no lz-tpp edit (SEAM-02 is Phase 18), no reference prose or TS examples (Phases 16-17), no eval workspace or .oracle content and no empirical description tuning (per **D-03** and **D-08**; those are Phases 20/16). Pulling any of these forward is the single highest-probability scope error (RESEARCH Pitfall 5) -- do not.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-lz-red-skill-scaffold-description-boundary/15-CONTEXT.md
@.planning/phases/15-lz-red-skill-scaffold-description-boundary/15-RESEARCH.md
@.planning/phases/15-lz-red-skill-scaffold-description-boundary/15-PATTERNS.md

# Direct scaffold precedents on disk -- copy-adapt these exactly:
@plugins/lz-tdd/skills/lz-tpp/SKILL.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md
</context>

## Artifacts this phase produces

All 11 files are NEW, all under `plugins/lz-tdd/skills/lz-red/`:

| # | Path | Role | Task |
|---|------|------|------|
| 1 | `SKILL.md` | dual-mode router (~90-120 lines) | 3 |
| 2 | `references/three-laws-and-test-selection.md` | flat stub | 1 |
| 3 | `references/test-structure-and-assertions.md` | flat stub | 1 |
| 4 | `references/naming.md` | flat stub | 1 |
| 5 | `references/vitest-typescript-mechanics.md` | flat stub | 1 |
| 6 | `references/principle-backing.md` | flat stub (cross-cutting) | 1 |
| 7 | `references/anti-patterns.md` | flat stub (carries RTR-03) | 2 |
| 8 | `references/testing-stance/README.md` | navigation-index stub | 2 |
| 9 | `references/testing-stance/functional-core.md` | leaf stub | 2 |
| 10 | `references/testing-stance/message-matrix.md` | leaf stub | 2 |
| 11 | `references/testing-stance/seams-and-legacy.md` | leaf stub | 2 |

UNCHANGED (do NOT touch): `plugins/lz-tdd/.claude-plugin/plugin.json` (D-09), `.claude-plugin/marketplace.json` (D-10), `plugins/lz-tdd/skills/lz-tpp/SKILL.md` (SEAM-02 = Phase 18). No `.claude/skills/lz-red-workspace/`, no `.oracle/` (D-03).

## Source Coverage Audit

Every source item is covered by a task; no gaps, no phase split needed.

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL (ROADMAP P15) | Invocable dual-mode skill whose description triggers on RED intent without colliding with siblings | Task 3 (SKILL.md + description) + Tasks 1-2 (stubs) |
| REQ | SKL-01 (invocable; dual-mode-by-omission frontmatter) | Task 3 |
| REQ | SKL-02 (lean router + progressive-disclosure stubs) | Tasks 1-2 (stubs) + Task 3 (router links) |
| REQ | SKL-03 (three-way-guarded description) | Task 3 |
| RESEARCH | 10-file tree; SKILL.md 7-section skeleton; v1 1083-char description; Wave-0 structural gate | Tasks 1-2 (tree) + Task 3 (skeleton/description) + Verification (gate) |
| CONTEXT | D-01, D-02 (tree + thin contracts) | Tasks 1-2 |
| CONTEXT | D-04, D-05, D-06, D-07 (router body, frontmatter, description) | Task 3 |
| CONTEXT | D-03, D-08, D-09, D-10 (scope fences) | Respected as prohibitions across all tasks + Verification |

Exclusions (correctly out of scope, not gaps): reference prose/TS examples (Phases 16-17), coach procedure + seam wiring (Phase 18), version bump + docs (Phase 19), eval workspace + .oracle + description tuning (Phases 20/16). These are the ROADMAP's own later phases (CONTEXT Deferred Ideas).

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 flat core test-shaping reference stubs</name>
  <files>
    plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md,
    plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md,
    plugins/lz-tdd/skills/lz-red/references/naming.md,
    plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md,
    plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  </files>
  <read_first>
    - .planning/phases/15-lz-red-skill-scaffold-description-boundary/15-PATTERNS.md (the copy-ready stub skeleton under "Flat single-topic reference stubs", plus the per-stub target table at lines 148-160)
    - .planning/phases/15-lz-red-skill-scaffold-description-boundary/15-RESEARCH.md (the per-stub content-contract map, lines 108-125)
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (confirms the reference-doc convention: NO YAML frontmatter, H1 first)
  </read_first>
  <action>
    Create these 5 flat single-topic reference stubs. Each is a THIN content contract per **D-02** -- NOT empty, NOT full content. No YAML frontmatter (reference docs are frontmatter-free; H1 first). No prose synthesis and NO TypeScript/Vitest examples (that is Phases 16-17 -- **D-03** / RESEARCH Pitfall 5). Author-name source clusters are stated as bare facts only, never as prose.

    Each stub carries, in order: (1) an H1 title; (2) a `Scope:` line naming what the doc will cover and its role in the RED coach; (3) a blockquote marker containing the exact literal string `Populated in Phase NN` with the fill phase(s) filled in (the Wave-0 gate greps for the literal `Populated in Phase`, so keep that exact casing/spelling) plus a `No verbatim source prose (DST-04)` note; (4) the requirement IDs the doc will satisfy; (5) the source cluster (author names only); (6) a scoped bullet list of the eventual sub-topics; (7) a `## Sources (placeholder)` section. Follow the copy-ready skeleton in 15-PATTERNS.md; exact wording is your discretion (**D-02**).

    Per-stub targets (from 15-PATTERNS.md / 15-RESEARCH.md -- use these exact IDs, clusters, and fill phases):

    | Stub | Requirement IDs | Source cluster | Populated in Phase |
    |------|-----------------|----------------|--------------------|
    | three-laws-and-test-selection.md | SEL-01, SEL-02, LAW-01, LAW-02, SEAM-01 | RCM, Beck | Phases 16 + 18 (co-edited) |
    | test-structure-and-assertions.md | STR-01, STR-02, ASRT-01, ASRT-02 | Wake, North, Beck, Khorikov | Phases 16 + 17 (co-edited) |
    | naming.md | NAME-01 | North, Osherove, Metz | Phase 16 |
    | vitest-typescript-mechanics.md | VIT-01, VIT-02 | Vitest 4.x, fast-check | Phase 17 |
    | principle-backing.md | cross-cutting (supports DST-03) | all sources | Phases 16-17 (co-edited) |

    Co-editing across phases is expected and correct (**D-01**): where a doc spans phases, list all contributing phases in its marker. Do NOT fragment a single decision doc per-source (ARCHITECTURE Anti-Pattern 1). ASCII-only throughout (`--` not em/en dash, `->` not arrow glyph, straight quotes, no emoji). No email-shaped tokens anywhere.
  </action>
  <acceptance_criteria>
    - All 5 files exist: `test -f` each of the 5 paths above returns success.
    - Each of the 5 stubs contains the literal marker: `git grep -F "Populated in Phase" -- plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md plugins/lz-tdd/skills/lz-red/references/naming.md plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md plugins/lz-tdd/skills/lz-red/references/principle-backing.md` hits all 5 files.
    - Each stub starts with an H1 (first non-empty line begins with `# `) and has NO `---` YAML frontmatter block.
    - Each stub names its requirement IDs (spot check: `git grep -F "SEL-01" -- .../three-laws-and-test-selection.md`, `git grep -F "NAME-01" -- .../naming.md`, `git grep -F "VIT-01" -- .../vitest-typescript-mechanics.md` each hit).
    - ASCII-only: `rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md plugins/lz-tdd/skills/lz-red/references/naming.md plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md plugins/lz-tdd/skills/lz-red/references/principle-backing.md` returns no matches (exit 1).
    - No source prose / no code fences: none of the 5 stubs contains a ``` fenced block (`git grep -F '```' -- <the 5 paths>` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>test -f plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md && test -f plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md && test -f plugins/lz-tdd/skills/lz-red/references/naming.md && test -f plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md && test -f plugins/lz-tdd/skills/lz-red/references/principle-backing.md && [ "$(git grep -l -F 'Populated in Phase' -- plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md plugins/lz-tdd/skills/lz-red/references/naming.md plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md plugins/lz-tdd/skills/lz-red/references/principle-backing.md | wc -l)" -eq 5 ]</automated>
  </verify>
  <done>All 5 flat stubs exist as thin content contracts (H1 + Scope + "Populated in Phase NN" marker + req IDs + source cluster + scoped bullets + Sources placeholder), frontmatter-free, ASCII-only, with no synthesized prose or TS examples.</done>
</task>

<task type="auto">
  <name>Task 2: Author the testing-stance navigation subdir (index + 3 leaves) and the anti-patterns stub</name>
  <files>
    plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md,
    plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md,
    plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md,
    plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md,
    plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
  </files>
  <read_first>
    - .planning/phases/15-lz-red-skill-scaffold-description-boundary/15-PATTERNS.md (the "testing-stance/README.md (navigation-index stub)" skeleton at lines 164-192, plus the flat-leaf skeleton and per-stub target table)
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (the navigation-only index STYLE to mirror: recognize-by cue + link per entry, "index is navigation only, open the leaf")
    - .planning/phases/15-lz-red-skill-scaffold-description-boundary/15-RESEARCH.md (per-stub content-contract map, lines 108-125)
  </read_first>
  <action>
    Create the one branchy part of the tree -- the `testing-stance/` subdir (1 navigation index + 3 leaves) -- plus the flat `anti-patterns.md` stub (grouped here because it carries RTR-03, the "listen to the tests" stance meta-rule). All 5 are THIN content contracts per **D-02**: frontmatter-free, H1 first, no real prose, no TS examples. Same 7-part contract shape as Task 1 (title, Scope line, `Populated in Phase NN` marker + DST-04 note, requirement IDs, source cluster, scoped bullet list, `## Sources (placeholder)`).

    `testing-stance/README.md` is a NAVIGATION-INDEX stub (**D-01**): mirror `lz-refactor/references/smells.md`. Its Scope states it is navigation only -- detection signals + a route table mapping the codebase's house testing stance to the matching leaf (functional-core / message-matrix / seams-and-legacy) -- and that it carries NO stance content, only recognize-by signals and links. Never inline stance guidance here; the three leaves carry it (Anti-Pattern 2 guard). Its per-entry contract is one route-table row = detection signal + a link to the leaf.

    Per-stub targets (exact IDs, clusters, fill phases):

    | Stub | Requirement IDs | Source cluster | Populated in Phase |
    |------|-----------------|----------------|--------------------|
    | testing-stance/README.md | RTR-02 | (router) | Phase 17 (index) + Phase 18 (SKILL.md routing step) |
    | testing-stance/functional-core.md | RTR-01, ASRT-02 | Bernhardt (FCIS) | Phase 17 |
    | testing-stance/message-matrix.md | RTR-01, ASRT-03 | Metz + Owen | Phase 17 |
    | testing-stance/seams-and-legacy.md | RTR-01, ASRT-02 | Feathers | Phase 17 |
    | anti-patterns.md | ANTI-01, ANTI-02, RTR-03 | Cooper, Khorikov, Metz, GOOS, Beck | Phase 17 |

    For `seams-and-legacy.md`, note in the scoped bullet list that when populated it will CROSS-LINK lz-refactor's `refactoring-without-tests.md` (Feathers), not copy it -- but do NOT add that outbound link yet (it is Phase-17 content; a stub carries the intent, not the link). ASCII-only; no email-shaped tokens.
  </action>
  <acceptance_criteria>
    - All 5 files exist: `test -f` each of the 5 paths returns success (confirm the `testing-stance/` subdir was created with case-correct `README.md`, not `readme.md`).
    - Each of the 5 stubs contains the literal `Populated in Phase`: `git grep -F "Populated in Phase" -- plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md plugins/lz-tdd/skills/lz-red/references/anti-patterns.md` hits all 5.
    - README.md is navigation-framed: `git grep -Fi "navigation" -- plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md` hits (it declares itself navigation-only) and it names RTR-02 (`git grep -F "RTR-02" -- .../testing-stance/README.md` hits).
    - anti-patterns.md names RTR-03: `git grep -F "RTR-03" -- plugins/lz-tdd/skills/lz-red/references/anti-patterns.md` hits.
    - Each stub starts with an H1 and has NO `---` frontmatter block.
    - ASCII-only: `rg -n '[^\x00-\x7F]'` over the 5 paths returns no matches (exit 1).
    - No code fences: `git grep -F '```' -- <the 5 paths>` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>test -f plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md && test -f plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md && test -f plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md && test -f plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md && test -f plugins/lz-tdd/skills/lz-red/references/anti-patterns.md && git grep -q -F "RTR-02" -- plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md && git grep -q -F "RTR-03" -- plugins/lz-tdd/skills/lz-red/references/anti-patterns.md</automated>
  </verify>
  <done>The testing-stance/ subdir (navigation index + 3 leaves) and anti-patterns.md exist as thin content contracts; README.md is navigation-only and names RTR-02; anti-patterns.md carries RTR-03; all frontmatter-free, ASCII-only, no prose/examples.</done>
</task>

<task type="auto">
  <name>Task 3: Author the SKILL.md router and the v1 three-way-guarded description</name>
  <files>plugins/lz-tdd/skills/lz-red/SKILL.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md (81 lines -- the size, the two-mode split, the "heuristic not law" caveat, and the single "## Reference material" bullet-list pointer section to mirror)
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (the dual-mode-by-omission frontmatter and the shipped reciprocal exclusion-clause description tail -- the direct template for the two exclusions)
    - .planning/phases/15-lz-red-skill-scaffold-description-boundary/15-PATTERNS.md ("### SKILL.md" section: the section-by-section copy-ready skeleton + the copy-ready v1 description)
    - .planning/phases/15-lz-red-skill-scaffold-description-boundary/15-RESEARCH.md ("SKILL.md Router Skeleton" table + "The v1 Three-Way-Guarded Description")
  </read_first>
  <action>
    Create the lean `SKILL.md` router. Target ~90-120 lines (near lz-tpp's 81, well under the < 500 cap). Because the coach procedure is a placeholder this phase, SKILL.md sits at the LOW end -- do not pad it. The router LINKS, it never inlines (Anti-Pattern 2): do not restate the route table, the message matrix, or the deferred coach steps.

    FRONTMATTER (per **D-05**, dual-mode-by-omission): exactly two top-level keys -- `name: lz-red` (MUST equal the directory) and `description:` rendered as a `>-` folded block scalar exactly like both siblings. Do NOT add `version`, `disable-model-invocation`, `user-invocable`, or `allowed-tools`; omitting `disable-model-invocation`/`user-invocable` is what makes the skill both auto-trigger and answer `/lz-tdd:lz-red`.

    DESCRIPTION (per **D-06/D-07**, the SKL-03 deliverable): use this exact reasoned v1 text as the folded description (measured 1083 chars folded, under the 1536 cap; positive trigger first, both exclusions in the tail; language-agnostic -- do NOT name Vitest or TypeScript). Do NOT empirically tune it (**D-08** -- that is Phase 20). The text: This skill should be used during the red step of red-green-refactor TDD to choose and write the next FAILING (red) unit test well: which test to write next, keeping a running test list, starting from the degenerate or starter case, triangulating with another concrete example to drive out the next test, structuring the test (arrange-act-assert or given-when-then), naming it for the behavior it pins, asserting observable behavior rather than implementation detail, and confirming it fails for the right reason. Use it whenever a developer asks what test to write next, how to write or structure a unit test, whether a test is a good test, or how to match the codebase's existing testing stance -- even when they only ask "what should I test here?" or "is this a good test?". Do NOT use it to make an existing failing test pass or to pick the minimal code change that turns the bar green -- that is the green/transformation step; use lz-tpp instead. Do NOT use it to restructure, clean up, or de-duplicate code whose tests already pass -- that is the refactor step; use lz-refactor instead.

    BODY (per **D-04**, router only; mirror 15-PATTERNS.md section-for-section). Seven sections:
    1. H1 `# lz-red: RED-phase TDD coach` + a one-paragraph intro (choose/write the next failing unit test; runs in two modes; making that test pass is the green step / lz-tpp; restructuring passing code is lz-refactor).
    2. `## Two modes` -- Coach mode (a codebase in view; pick/shape the next failing test) and Reference mode (explain a RED concept, or explicit `/lz-tdd:lz-red`); include an "Answer from references; do not restate here" clause and an inline note that the full coach procedure is deferred to Phase 18.
    3. `## RED vs the green step (lz-tpp) and the refactor step (lz-refactor)` -- the three-way seam intro stating BOTH exclusions and "classify the request before acting".
    4. `## Listen to the tests` -- one short heuristic-not-law caveat paragraph (test-writing pain is design feedback, not a cue to mock more; RED guidance is a heuristic, allow reasoned deviation).
    5. `## Coach decision procedure (deferred to Phase 18)` -- a LABELED PLACEHOLDER, 2-4 lines only so it does not read as real content. Name the deferred content, the fill phase (18), and the req IDs (SEAM-01, LAW-01/02, RTR-02/03), e.g. it will classify against the lz-tpp/lz-refactor seams, detect the house idiom, pick the next test, route the testing stance, structure, assert observable behavior, fail for the right reason, and hand off to lz-tpp. State "Do not infer it from this scaffold."
    6. `## Reference material` -- a single bullet-list pointer section (lz-tpp shape) linking ALL 10 stub paths so every link resolves from day one AND the Wave-0 gate can grep each path. Link the 6 flat docs and the `testing-stance/README.md` index directly; list the three leaf paths (functional-core.md, message-matrix.md, seams-and-legacy.md) as navigation-framed sub-bullets under the index pointer (the index is the coach-time navigation surface; the leaves are reached through it). Use relative links of the form `[references/naming.md](references/naming.md)`. Watch case: `testing-stance/README.md`, not `readme.md`.

    ASCII-only throughout; no email-shaped tokens.
  </action>
  <acceptance_criteria>
    - `test -f plugins/lz-tdd/skills/lz-red/SKILL.md` succeeds and `wc -l < plugins/lz-tdd/skills/lz-red/SKILL.md` is under 500 (expect ~90-120).
    - Frontmatter is dual-mode-by-omission: the YAML head (lines between the first two `---`) has exactly two top-level keys -- lines matching `^[A-Za-z][A-Za-z-]*:` inside the head are exactly `name:` and `description:`. `git grep -nE '^name: lz-red$' -- plugins/lz-tdd/skills/lz-red/SKILL.md` hits (name equals the directory).
    - Description present and both reciprocal exclusions name their sibling: `git grep -F "use lz-tpp" -- plugins/lz-tdd/skills/lz-red/SKILL.md` hits AND `git grep -F "use lz-refactor" -- plugins/lz-tdd/skills/lz-red/SKILL.md` hits.
    - Positive trigger precedes exclusions: in SKILL.md the first occurrence of `This skill should be used during the red step` appears at a lower byte offset than the first occurrence of `Do NOT use it` (satisfied by the v1 draft; verify by offset).
    - Folded description length <= 1536: extract the `description` folded scalar, join its wrapped lines with single spaces, and confirm length <= 1536 (the v1 draft measures ~1083).
    - Placeholder coach section present and labeled: `git grep -Fi "deferred to Phase 18" -- plugins/lz-tdd/skills/lz-red/SKILL.md` hits; the numbered real procedure is NOT authored (the section is a 2-4 line blockquote/placeholder).
    - All 10 stub paths are linked (every link resolves): for each of the 10 reference paths, `git grep -F "<path>" -- plugins/lz-tdd/skills/lz-red/SKILL.md` hits, and the corresponding `test -f plugins/lz-tdd/skills/lz-red/<path>` succeeds. Case-correct `testing-stance/README.md`.
    - `claude plugin validate .` exits 0.
    - ASCII-only: `rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-red/SKILL.md` returns no matches (exit 1).
  </acceptance_criteria>
  <verify>
    <automated>claude plugin validate . && test "$(wc -l < plugins/lz-tdd/skills/lz-red/SKILL.md)" -lt 500 && git grep -q -nE '^name: lz-red$' -- plugins/lz-tdd/skills/lz-red/SKILL.md && git grep -q -F "use lz-tpp" -- plugins/lz-tdd/skills/lz-red/SKILL.md && git grep -q -F "use lz-refactor" -- plugins/lz-tdd/skills/lz-red/SKILL.md</automated>
  </verify>
  <done>SKILL.md is a < 500-line dual-mode-by-omission router (name == lz-red + description only) carrying the v1 ~1083-char three-way-guarded description (positive trigger first, lz-tpp + lz-refactor exclusions in the tail), a labeled Phase-18 placeholder coach section, and resolving links to all 10 stubs; claude plugin validate . exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 15 ships Markdown only, with no executable surface (no auth, sessions, input handling, or crypto). The one relevant boundary is authored content crossing into a public repo.

| Boundary | Description |
|----------|-------------|
| authored Markdown -> public GitHub repo | Maintainer-authored files (and the commit identity) become public; the only sensitive data that could cross is maintainer PII (work email) or non-ASCII bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information disclosure | the 11 authored files + commit author/committer identity | medium | mitigate | Allowlist-inversion email scan: assert the ONLY email-shaped token present in the 11 files is `larsbrinknielsen@gmail.com` (expected: none); never write the forbidden work-email/domain even as a search needle; confirm `git config user.email` is the public gmail before committing. |
| T-15-02 | Tampering / correctness | authored Markdown (encoding hygiene) | low | mitigate | ASCII-only authoring (`--` not em/en dash, `->` not arrow glyph, straight quotes, no emoji/box-drawing); `rg -n '[^\x00-\x7F]'` over all 11 files returns nothing before commit. |
| T-15-03 | Information disclosure (verbatim source prose / copyright) | reference stubs | low | accept | Stubs carry content contracts only (author names as facts, no source prose) this phase, so DST-04 exposure is nil; the real no-verbatim gate is Phases 16-17 (own-words + oracle-reviewer). |
| T-15-SC | Tampering (supply chain) | package installs | n/a | accept | No npm/pip/cargo installs this phase (Markdown-only skill; RESEARCH Environment Availability confirms no new deps). No package-legitimacy audit or install checkpoint is required. |

No high-severity threats -- nothing to block on (`security_block_on: high`). All controls are continuous, verified at each authoring commit.
</threat_model>

<verification>
## Phase gate (Wave-0 structural gate -- inline, no committed checker per D-11 discretion / lazy path)

Run all of the following from the repo root; all must pass before `/gsd:verify-work`:

1. `claude plugin validate .` exits 0 (skill registerable; skills auto-discovered from the plugin root, no manifest edit -- **D-09**).
2. All 11 files exist (`test -f` each path in `files_modified`).
3. `wc -l < plugins/lz-tdd/skills/lz-red/SKILL.md` is under 500.
4. `git grep -l -F "Populated in Phase" -- plugins/lz-tdd/skills/lz-red/references/` lists all 10 stubs.
5. Each of the 10 stub relative paths appears in SKILL.md (`git grep -F "<path>" -- plugins/lz-tdd/skills/lz-red/SKILL.md`) and every linked path resolves to an existing file (case-correct `testing-stance/README.md`).
6. `git grep -F "use lz-tpp"` and `git grep -F "use lz-refactor"` both hit SKILL.md (both reciprocal exclusions present).
7. Folded description length <= 1536 (v1 draft ~1083).
8. Frontmatter has exactly the two top-level keys `name` and `description`; `name: lz-red` equals the directory.
9. ASCII-only across all 11 files (`rg -n '[^\x00-\x7F]'` returns nothing).
10. Email allowlist-inversion: the only email-shaped token in the 11 files is `larsbrinknielsen@gmail.com` (expected: none present); `git config user.email` == the public gmail.
11. Scope fences intact: `git status --porcelain` shows NO changes to `plugins/lz-tdd/.claude-plugin/plugin.json` (**D-09**), `.claude-plugin/marketplace.json` (**D-10**), or `plugins/lz-tdd/skills/lz-tpp/SKILL.md` (SEAM-02 = Phase 18); no `.claude/skills/lz-red-workspace/` or `.oracle/` paths created (**D-03**).

Deferred to Phase 20 (EVL-01, NOT this phase): empirical trigger recall/specificity and the cross-skill (three-way boundary) trigger eval. Do not build an eval harness here (**D-03/D-08**).
</verification>

<success_criteria>
- `/lz-tdd:lz-red` is invocable: SKILL.md exists with dual-mode-by-omission frontmatter (name == lz-red + description only), and `claude plugin validate .` exits 0 (SKL-01).
- SKILL.md is a lean router (< 500 lines) using progressive disclosure; all 10 reference stubs exist, each carries its content-contract markers, and every stub is linked from SKILL.md with a resolving relative link (SKL-02).
- The description auto-triggers on "choose / write the next failing test" intent and carries reciprocal near-miss guards naming lz-tpp and lz-refactor, positive trigger first, folded length <= 1536 (SKL-03).
- All 10 CONTEXT decisions are honored: the tree matches D-01/D-02, the router matches D-04/D-05, the description matches D-06/D-07, and the scope fences D-03/D-08/D-09/D-10 hold (plugin.json at 0.0.2, marketplace.json and lz-tpp untouched, no workspace/.oracle, no tuning).
</success_criteria>

<output>
Create `.planning/phases/15-lz-red-skill-scaffold-description-boundary/15-01-SUMMARY.md` when done.
</output>
