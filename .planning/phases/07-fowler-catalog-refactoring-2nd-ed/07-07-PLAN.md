---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 07
type: execute
wave: 3
depends_on: [07-02]
tags: [fowler-catalog, refactoring, skill-authoring, oracle-clean-room, typescript, simplify-conditional-logic]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/decompose-conditional.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/consolidate-conditional-expression.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-nested-conditional-with-guard-clauses.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-special-case.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-assertion.md
autonomous: false
requirements: [FWL-01, FWL-04]
must_haves:
  truths:
    - "07-ROUTING-ARCHITECTURE 'Content contracts': the simplify-conditional-logic (Fowler Ch.10) refactorings are authored as per-refactoring leaves fowler-catalog/<slug>.md, each # <Name> + 1st-ed alias(es) + one-line Use when: + ## Motivation + ## Mechanics + ## Example + optional ## Watch for (D-02 per-entry content contract as per-refactoring leaves)"
    - "07-ROUTING-ARCHITECTURE 'Scope: 62 refactorings': Replace Control Flag with Break is CUT (1st-ed relic) and is NOT authored here"
    - "07-ORACLE-MODEL 'The loop' + 'Firewall': each leaf is authored BLIND, gated by the oracle-reviewer subagent (CONTENT_TYPE refactoring-leaf, canonical per-axis anchors), and revised BLIND to convergence; the executor/main context NEVER reads .oracle/ (only ls for names)"
    - "FWL-04 (07-ROUTING-ARCHITECTURE 'Validation'): every before/after example is tsc --strict-clean AND behavior-preserving, the latter gated by the oracle-reviewer example axis; a smell is a design issue, never a type error"
    - "This plan does NOT touch fowler-catalog/README.md or the checker .mjs files (owned by 07-02 / 07-10); it only adds this chapter's leaf files -- parallel-safe"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/decompose-conditional.md"
      provides: "Decompose Conditional leaf (oracle-converged)"
      contains: "Use when:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-special-case.md"
      provides: "Introduce Special Case leaf (alias: Introduce Null Object)"
      contains: "Use when:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/decompose-conditional.md"
      via: "see-also cross-link (resolving)"
      pattern: "decompose-conditional\\.md"
---

<objective>
Author the simplify-conditional-logic chapter (Fowler Ch.10) as per-refactoring catalog leaves
using the clean-room blind-author -> oracle-reviewer-gate -> converge loop calibrated by the
07-02 pilot.

Purpose: fill the FWL-01 catalog for the ~6 simplify-conditional-logic refactorings with
contract-complete, oracle-converged, tsc --strict-clean leaves. Parallel with the other wave-3
chapter plans; file-disjoint by owning only this chapter's leaf files. Replace Control Flag with
Break is CUT (1st-ed relic) and is NOT authored.

Output: the ~6 simplify-conditional-logic leaf files under references/fowler-catalog/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-02-SUMMARY.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md

<interfaces>
CLEAN-ROOM FIREWALL: the executor/main context NEVER opens any file under .oracle/ -- `ls` for
names only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` (Task/subagent) gates a drafted leaf. Input: DRAFT_PATHS (this chapter's
  leaves), SOURCE = ".oracle/refactoring-2e/index.md" + "Ch.10 -- simplifying conditional logic"
  (index.md + chapter number/topic; never a book chapter filename), CONTENT_TYPE =
  "refactoring-leaf", SCOPE = each leaf's refactoring, canonical per-axis anchors from
  07-ORACLE-MODEL. Returns per-draft verdicts pass|revise|blocked|error.
- `oracle` (Task/subagent) for open-ended chapter-membership lookups.
Follow the exact loop + leaf format proven in the 07-02 pilot.

Ch.10 expected members (confirm chapter placement via `oracle`; sizing scaffold, not authoritative):
Decompose Conditional, Consolidate Conditional Expression, Replace Nested Conditional with Guard
Clauses, Replace Conditional with Polymorphism, Introduce Special Case (alias: Introduce Null
Object), Introduce Assertion. CUT (do NOT author): Replace Control Flag with Break. Aliases from
refactoring-com-overview.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the simplify-conditional-logic (Ch.10) leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/decompose-conditional.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/consolidate-conditional-expression.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-nested-conditional-with-guard-clauses.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-special-case.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-assertion.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- refactoring leaf; "Scope: 62 refactorings" -- Replace Control Flag with Break is cut)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Content-fidelity discipline", "Comprehensive review dimensions", "Canonical per-axis anchors", "Driver responsibilities")
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-02-SUMMARY.md (calibrated rubric anchors + round counts + loop mechanics)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md (leaf-contract exemplar)
    - .planning/research/refactoring-com-overview.md (Ch.10 names + 1st-ed aliases)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not).
    Do NOT open any file under .oracle/ from this context.

    Author the ~6 simplify-conditional-logic leaves (one file per refactoring, kebab-case slug)
    via the core clean-room loop, per leaf or per small batch:
    (1) AUTHOR BLIND from public knowledge + the refactoring-com scaffold, in the LOCKED leaf
    format: `# <Name>`, italic aliases line if any, one-line `Use when:`, `## Motivation` (2-4
    sentences, original words), `## Mechanics` (4-8 verb-first numbered steps, original words,
    naming the test/compile checkpoint inline), `## Example` (ONE compact tsc --strict-clean TS
    before->after, 5-15 lines/side; 2nd only for a distinct variant, hard cap 2), optional
    `## Watch for`. A smell is a design issue -- both before AND after must compile; never
    illustrate a smell with a type error. Preserve every decision branch + safety checkpoint as
    its own step; keep follow-up/compose steps as cross-links to sibling leaves (e.g. Replace
    Conditional with Polymorphism -> Decompose Conditional). Do NOT author Replace Control Flag
    with Break (CUT). ASCII only.
    (2) GATE: spawn `oracle-reviewer` with DRAFT_PATHS, SOURCE ".oracle/refactoring-2e/index.md" +
    "Ch.10 -- simplifying conditional logic", CONTENT_TYPE "refactoring-leaf", SCOPE (each leaf's
    refactoring), canonical per-axis anchors. Use `oracle` for open-ended chapter-membership
    lookups.
    (3) REVISE BLIND from the directives.
    (4) ITERATE to convergence (every entry `pass`, or owner-accepted `blocked`); DRIVER caps
    rounds at ~3 and escalates a non-converging/blocked entry to Task 2. Apply the fan-out guard
    (dedupe/cap; do NOT persist a full ordered name+chapter map).
    (5) Run the harness over the authored leaves and commit on green. Do NOT edit README.md or
    any checker .mjs; cross-links to sibling-chapter leaves resolve at the 07-10 gate.
  </action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "Use when:" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/decompose-conditional.md && rg -q "## Mechanics" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md && test ! -f plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-control-flag-with-break.md</automated>
  </verify>
  <done>All ~6 simplify-conditional-logic leaves exist as contract-complete per-refactoring files; each oracle-reviewer verdict is pass (or owner-accepted blocked); every example is tsc --strict-clean and behavior-preserving; Replace Control Flag with Break is absent (cut); check-hygiene green; README.md and checkers untouched; no .oracle/ file opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner escalation for non-converging / blocked entries (conditional)</name>
  <decision>How to resolve any Ch.10 leaf the oracle-reviewer loop cannot converge within ~3 rounds or returns as a blocked ambiguity?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate. The driver caps rounds (~3) and escalates that ENTRY to the owner via
    AskUserQuestion. Fires ONLY for entries that do not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked entry as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction, then re-run the loop once</name>
      <pros>Resolves a genuine fidelity gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked): open an
    AskUserQuestion with the entry, the reviewer's ambiguities/unable-to-verify axes (own words),
    and the two options; apply the choice; re-run the harness; record it in the SUMMARY. Do NOT
    reveal or request verbatim source prose.
  </action>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every Ch.10 leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-2e/` (owner full-text book, git-ignored) -> authored public-repo leaves | copyrighted expression must never cross; the ONLY crossing is via the `oracle`/`oracle-reviewer` subagents (isolated context, own-words verdicts, no prose/code/paths) |
| authored files -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal (DST-04) | authored leaf prose + example code | mitigate | Clean-room firewall: main context NEVER reads `.oracle/`; leaves authored BLIND; `oracle-reviewer` near-verbatim gate; `check-hygiene` no-verbatim heuristic; Phase-10 scan |
| T-07-03 | Tampering (fabricated / unfaithful content) | mechanics / motivation / example fidelity | mitigate | `oracle-reviewer` fidelity gate + converge loop; driver caps rounds (~3) and escalates to owner (Task 2) |
| T-07-05 | Tampering / firewall (fan-out aggregation) | merged oracle output reconstructing the book's selection | mitigate | driver dedupes/caps; does NOT persist a full ordered name+chapter map |
| T-07-02 | Information Disclosure | work email in a committed leaf | mitigate | `check-hygiene` work-email allowlist gate |

No UI, no network, no secrets, no new packages. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- extract-samples (tsc --strict) GREEN; check-hygiene GREEN; each Ch.10 leaf has the full contract; the cut relic is absent.
- Every Ch.10 leaf reached oracle-reviewer pass (or owner-accepted blocked).
- README.md and checker .mjs files untouched; no `.oracle/` file opened by this context.
</verification>

<success_criteria>
- All ~6 simplify-conditional-logic refactorings are contract-complete, oracle-converged, tsc --strict-clean leaves; Replace Control Flag with Break is cut.
- The plan owns only this chapter's leaf files (parallel-safe); cross-refs resolve at the 07-10 gate.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-07-SUMMARY.md` when done, recording the confirmed Ch.10 membership, per-leaf round counts, and any owner escalations.
</output>
