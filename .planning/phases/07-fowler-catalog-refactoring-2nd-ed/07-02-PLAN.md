---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
autonomous: false
requirements: [FWL-01]
must_haves:
  truths:
    - "D-01: the 66 refactorings split across fowler-catalog leaf files by Fowler's own tag-groups; the exact leaf set (7 coarse chapter-tags vs 20 fine tag-groups) and per-entry bucketing are finalized WITH the owner at the oracle checkpoint, not pre-locked"
    - "D-06: the README index carries a provenance legend distinguishing print+web, web-only (the 5 print-absent + entries), and web-example (Split Phase)"
    - "D-07: an AskUserQuestion oracle-access checkpoint is opened BEFORE authoring the index, to obtain the owner's per-entry tag data and confirm the bucketing decision"
    - "The README stays a THIN index (legend + per-entry contract + one line per leaf) -- no refactoring entry content is inlined here (SKEL-04)"
    - "SKILL.md is NOT re-touched: its pointer to fowler-catalog/README.md already resolves"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md"
      provides: "thin catalog index: provenance legend + per-entry contract + tag-group leaf link list with counts"
      contains: "Provenance legend"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      via: "one markdown link per tag-group leaf"
      pattern: "\\]\\([a-z-]+\\.md\\)"
---

<objective>
Resolve the tag-group bucketing decision WITH the owner (D-01), then author the thin
`fowler-catalog/README.md` index that fronts the leaf files the content plans
(07-04..07-07) will create. This plan writes the catalog's SHAPE (legend + leaf link
list + per-entry contract), never the entry content.

Purpose: D-01 locks the split AXIS (Fowler tag-groups) but explicitly defers the exact
leaf count and per-entry bucketing to plan time using the owner's tag data. This plan is
where that decision is made -- surfaced to the owner as a choice, not silently locked.

Output: a populated, thin `fowler-catalog/README.md` index with the provenance legend and
the finalized tag-group leaf link list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-tpp/references/transformations.md

<interfaces>
The Phase-6 stub README already carries the oracle-blocking blockquote, the per-entry
content contract, and the D-04 split-axis note. FILL against it -- do not redesign it.
The 20 Fowler tag names and the 66 names/aliases are public facts in
refactoring-com-overview.md. The provenance legend + tag-group link-list templates are in
07-RESEARCH.md lines 265-274 and 07-PATTERNS.md lines 99-116. Content plans consume the
finalized leaf set from this README.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: D-07 oracle checkpoint -- resolve tag-group bucketing</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md (D-01 axis lock + deferred bucketing; D-07 oracle mandate)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md (Open Question 1 tag-group bucketing; Pitfall 1 "the tag axis is not a partition"; A2/A3 distribution assumptions)
    - .planning/research/refactoring-com-overview.md (the 20 tag names + 66 names)
  </read_first>
  <decision>Which tag-group leaf set partitions the 66 Fowler refactorings?</decision>
  <context>
    D-01 locks the split axis to Fowler's own tag-groups but defers the exact leaf count
    and per-entry bucketing to plan time, using the owner's per-entry tag data. The public
    catalog does NOT expose per-entry tags in static HTML (client-side filter panel), so the
    owner is the source for the tag assignment. This is HIGH-impact (sets the catalog's
    shape + every content plan's file ownership) -- it MUST be decided WITH the owner, never
    auto-locked. Open the AskUserQuestion oracle-access checkpoint: ask the owner to confirm,
    from the Fowler 2nd-ed e-book/web edition (ISBN 9780135425664), the primary-tag / chapter
    placement for the 66 refactorings.
  </context>
  <options>
    <option id="coarse-7">
      <name>7 coarse chapter-tag leaves (RECOMMENDED)</name>
      <pros>Clean partition matching the book chapters (Ch.6-12) and refactoring.com; ~9-11 entries/leaf; keeps each content plan's file ownership disjoint (one plan owns whole leaves); no fine tag spans two content plans</pros>
      <cons>Uneven leaf sizes ("basic" is the largest); coarser progressive-disclosure granularity</cons>
    </option>
    <option id="fine-20">
      <name>20 fine tag-group leaves with a documented primary-tag tiebreaker</name>
      <pros>Finer disclosure; smaller leaves</pros>
      <cons>Fine operation tags (extract, inline, rename) span book chapters, so a single leaf can contain entries owned by different content plans -- requires re-slicing content-plan ownership so each leaf is authored by exactly one plan</cons>
    </option>
  </options>
  <action>
    Open the AskUserQuestion oracle-access checkpoint. Obtain the owner's per-entry tag /
    chapter data and confirm the leaf set. Recommend option coarse-7 (07-RESEARCH primary
    recommendation, cleanest ownership). If the owner picks fine-20, record the primary-tag
    tiebreaker rule and note that content-plan leaf ownership is re-derived so each fine leaf
    is authored by exactly one of 07-04..07-07 (nest fine leaves under their owning chapter
    theme; never split one leaf across plans). Do NOT author any refactoring entry content
    in this task -- this is the bucketing decision only.
  </action>
  <resume-signal>Owner confirms the leaf set (coarse-7 or fine-20 + tiebreaker) and the per-entry placement</resume-signal>
  <acceptance_criteria>
    - The AskUserQuestion oracle-access checkpoint is opened and the owner has confirmed the leaf set before any README leaf-list content is written.
    - The chosen leaf set and (if fine-20) the primary-tag tiebreaker are recorded in the SUMMARY for the content plans to consume.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the thin fowler-catalog/README.md index</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (the Phase-6 stub: keep its oracle blockquote + per-entry contract)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md (Pattern 1 thin-index template lines 259-274)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md (README index analog + provenance legend shape lines 90-116)
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (notation/legend discipline to mirror)
  </read_first>
  <action>
    Fill README.md as a THIN index only. Keep the existing oracle blockquote and the
    per-entry content contract from the stub. Add a Provenance legend (D-06) with three
    markers: `[print+web]` (both editions), `[web-only]` (the 5 print-absent + entries), and
    `[web-example]` (Split Phase examples online-only). Add a "Tag-groups" section: one line
    per leaf from the confirmed leaf set (coarse-7 or fine-20), each line linking the leaf
    file (relative link, e.g. `basic.md`) with its entry count and a one-line scope
    description (counts/scopes are facts, not oracle mechanics). Do NOT inline any refactoring
    entry (name/motivation/mechanics/example) here -- that lives in the leaves (SKEL-04).
    Remove the Phase-6 "Deferred: intra-catalog split axis (D-04)" section now that D-01
    resolves it. Do NOT touch SKILL.md.
  </action>
  <acceptance_criteria>
    - `rg -n "Provenance legend" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md` matches, and the three markers `[print+web]`, `[web-only]`, `[web-example]` each appear on their own line.
    - The Tag-groups section lists one markdown link per confirmed leaf; the link count equals the confirmed leaf count (7 or 20).
    - The per-entry content contract block from the stub is retained.
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 over the shipped Markdown (ASCII-only + no work email).
    - `rg -c "^### " plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md` returns 0 (no inlined entries -- index stays thin).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner e-book -> authored index | oracle tag data crosses into a public-repo file |
| authored index -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | catalog shape decided without the oracle | mitigate | D-07 blocking AskUserQuestion checkpoint (Task 1) resolves bucketing WITH the owner; no auto-lock even under --auto/--chain |
| T-07-01 | Information Disclosure / legal | verbatim Fowler prose leaking into the index | mitigate | DST-04: the index carries only names, counts, legend, and contract -- no book prose; check-hygiene no-verbatim heuristic + Phase-10 scan |
| T-07-02 | Information Disclosure | work email in the index | mitigate | check-hygiene work-email allowlist gate |

No high-severity threats: this plan authors an index of public names + a legend behind a
blocking human checkpoint. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The oracle checkpoint was opened and the owner confirmed the leaf set before authoring.
- README lists exactly the confirmed leaf set with the provenance legend; no entry content inlined.
- check-hygiene.mjs exits 0; SKILL.md is unchanged (`git diff --name-only` excludes SKILL.md).
</verification>

<success_criteria>
- The bucketing decision (D-01) is made with the owner and recorded.
- The thin index (D-06 legend + leaf link list) is authored; SKILL.md untouched.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-02-SUMMARY.md` when done, recording the confirmed leaf set and (if fine-20) the primary-tag tiebreaker.
</output>
