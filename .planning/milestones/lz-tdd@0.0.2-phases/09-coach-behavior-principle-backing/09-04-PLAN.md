---
phase: 09-coach-behavior-principle-backing
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/principles.md
autonomous: true
requirements: [CCH-05, PRIN-01, PRIN-02]

must_haves:
  truths:
    - "principles.md gains D-06 cross-ref pointer lines to the two Beck backing files (a pointer under 'The two hats' -> beck-tdd-by-example.md for the lz-tpp seam, CCH-05; a pointer under the when-to-refactor / economics material -> beck-tidy-first.md), using the file's existing sibling-relative [Text](file.md) link style; principles.md stays the Fowler-oracle file (pointers only, no Beck content imported)"
    - "Both new principles.md pointers resolve (the Beck files exist from 09-03) -- node tools/check-crossrefs.mjs exits 0 with principles.md now sourced (from 09-01)"
    - "The full phase gate is GREEN on the merged tree: npm run check (all checkers incl. the four rewired catalog checkers, check-backing, and check-crossrefs) exits 0, npm run typecheck exits 0, and claude plugin validate . passes"
    - "skill-reviewer PASSes on the lz-refactor skill (the authoritative DST-04 anchor for the no-oracle Beck/Feathers refs, D-07)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/principles.md"
      provides: "D-06 cross-ref pointers from the Fowler-oracle principles file to the two no-oracle Beck backing files"
      contains: "beck-tidy-first.md"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/principles.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md"
      via: "two-hats / seam pointer (CCH-05, D-06)"
      pattern: "beck-tdd-by-example.md"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/principles.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md"
      via: "when-to-refactor / economics pointer (D-06)"
      pattern: "beck-tidy-first.md"
  decisions: |
    - **D-06:** principles.md stays the Fowler-oracle file and gains cross-ref pointers to the two new Beck no-oracle backing files (beck-tdd-by-example.md under the two-hats/seam material; beck-tidy-first.md under the when-to-refactor/economics material); it imports NO Beck content -- pointers only -- preserving the oracle/no-oracle provenance separation
---

<objective>
Wire the D-06 cross-ref pointers from principles.md to the two Beck backing files, then run the phase
finalize gate. This is the Wave-3 integration plan: it depends on the parallel Wave-2 content (the
coach procedure in 09-02 and the three principle refs in 09-03) and turns the full battery GREEN on
the merged tree.

Purpose: principles.md is the Fowler-oracle principles doc; D-06 links it to the no-oracle Beck refs so
a reader following the two-hats/seam (CCH-05) or the when-to-refactor economics reaches the Beck backing
-- without importing Beck content into the oracle file. The pointers can only resolve once the Beck files
exist (09-03 output), which is why they are Wave 3. The gate then proves the whole phase GREEN and runs
first-party review (skill-reviewer), the authoritative DST-04 anchor for the no-oracle refs (D-07).

Output: an edited principles.md with two resolving Beck pointers, a full GREEN battery + typecheck +
claude plugin validate . PASS + skill-reviewer PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-coach-behavior-principle-backing/09-CONTEXT.md
@.planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md
@.planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md

The D-06 pointer placement is specified in 09-RESEARCH.md:226-231 and 09-PATTERNS.md's principles.md
section. The phase-gate sequence (full npm run check + typecheck + claude plugin validate . +
skill-reviewer) is the Validation Architecture Wave 3 in 09-RESEARCH.md:448-449.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the D-06 Beck cross-ref pointers to principles.md</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (the FILE being edited -- the two-hats section at :19-24 for the beck-tdd-by-example.md pointer, the when-to-refactor / economics material at :38-56 / :74-97 for the beck-tidy-first.md pointer, and the existing sibling-relative [Text](file.md) link style to copy)
    - plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md (the 09-03 target file -- confirm its name/existence before linking)
    - plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md (the 09-03 target file -- confirm its name/existence before linking)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (D-06 pointer placement :226-231)
    - .planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md (the principles.md cross-ref-pointers section: where to add, sibling-relative paths, "add pointers only, do not import Beck content")
  </read_first>
  <action>
    Edit plugins/lz-tdd/skills/lz-refactor/references/principles.md. Add two short cross-ref pointer lines
    in the file's existing inline [Text](file.md) link style (sibling-relative, no subdir): one under the
    "The two hats" material (:19-24) pointing to beck-tdd-by-example.md, framing it as the Beck TDD-by-Example
    backing for the lz-tpp seam (CCH-05); one under the when-to-refactor / economics material pointing to
    beck-tidy-first.md as the Beck Tidy First? backing for structural-vs-behavioral separation and refactor
    economics. Paths are bare siblings (beck-tdd-by-example.md, beck-tidy-first.md -- principles.md and the
    Beck files are all directly under references/). principles.md STAYS the Fowler-oracle file: add pointers
    only; do NOT import Beck content into it. ASCII-only; no work email.
  </action>
  <acceptance_criteria>
    - principles.md contains a Markdown link to beck-tdd-by-example.md and a Markdown link to beck-tidy-first.md, each using the sibling-relative [Text](file.md) form
    - The pointers are placed under the two-hats/seam material and the when-to-refactor/economics material respectively; no Beck prose is copied into principles.md
    - node tools/check-crossrefs.mjs exits 0 (both new pointers resolve; the Beck files exist from 09-03)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-crossrefs.mjs && node -e "const t=require('fs').readFileSync('../../../plugins/lz-tdd/skills/lz-refactor/references/principles.md','utf8');process.exit(t.includes('beck-tdd-by-example.md')&&t.includes('beck-tidy-first.md')?0:1)"</automated>
  </verify>
  <done>principles.md carries two resolving sibling-relative pointers to the Beck backing files; check-crossrefs is GREEN; no Beck content imported.</done>
</task>

<task type="auto">
  <name>Task 2: Phase finalize gate -- full battery GREEN + typecheck + validate + skill-reviewer</name>
  <read_first>
    - .claude/skills/lz-refactor-workspace/package.json (the scripts.check chain -- now 10 checkers incl. check-backing; scripts.typecheck)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (Validation Architecture: the Wave-3 phase gate :448-449; Sampling Rate; the RED->GREEN sequence so a residual RED is diagnosed correctly)
    - .planning/phases/08.2-functional-catalog-inserted/08.2-06-PLAN.md (the prior finalize-gate plan's shape: full battery + typecheck + claude plugin validate . + skill-reviewer PASS as truths)
  </read_first>
  <action>
    From .claude/skills/lz-refactor-workspace/, run the full phase gate on the merged tree and record the
    results in the SUMMARY: (a) npm run check -- every checker exits 0, INCLUDING the four IN-02-rewired
    catalog checkers, check-backing (now GREEN on the 3 authored refs), and check-crossrefs (SKILL.md coach
    links + principles.md Beck pointers + beck-tidy-first Fowler links all resolve); (b) npm run typecheck --
    exits 0 (a no-op unless a principle ref carries a live TS fence; fence-free/TS-fenced content per D-11);
    (c) from the repo root, claude plugin validate . passes with the new coach procedure and principle refs.
    Then request the plugin-dev skill-reviewer agent review of the lz-refactor skill -- it is the authoritative
    DST-04 anchor for the no-oracle Beck/Feathers refs (D-07); a PASS is required. If any checker is RED or
    validate/skill-reviewer flags an issue, diagnose against the RED->GREEN sequence (a check-backing RED means
    a missing token/tag or a residual scaffold phrase; a check-crossrefs RED means an unresolved coach/principle
    link) and fix in the owning file -- do NOT weaken a checker to force GREEN. Do not author new content beyond
    fixes needed to pass the gate.
  </action>
  <acceptance_criteria>
    - npm run check exits 0 (all 10 checkers GREEN, including check-backing and the four rewired catalog checkers)
    - npm run typecheck exits 0
    - claude plugin validate . passes from the repo root
    - skill-reviewer returns PASS on the lz-refactor skill (the DST-04 anchor for the no-oracle refs)
    - The SUMMARY records the GREEN gate; no checker was weakened to pass
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && npm run check && npm run typecheck</automated>
  </verify>
  <done>The full battery + typecheck are GREEN, claude plugin validate . passes, and skill-reviewer PASSes -- the phase is complete and DST-04-anchored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds two pointer lines to a shipped Markdown file and runs the phase gate. No runtime,
network, session, crypto, or user-input surface. The boundaries are link integrity, harness-verdict
integrity, and public-repo content hygiene.

| Boundary | Description |
|----------|-------------|
| principles.md pointers -> Beck backing files | broken pointers would dangle; must resolve |
| checker battery + skill-reviewer -> phase-complete verdict | a false-PASS here ships unbacked/verbatim content |
| repo -> published git history | committed principles.md must stay ASCII-only, work-email-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-GATE | Tampering | phase finalize verdict | mitigate | The gate is the full battery + typecheck + claude plugin validate . + skill-reviewer PASS; a residual RED must be fixed in the owning file, never by weakening a checker. |
| T-09-DST | Information Disclosure / IP | no-oracle Beck/Feathers refs shipped in the tree | mitigate | skill-reviewer PASS is the authoritative DST-04 anchor (D-07) since there is no oracle; check-hygiene no-verbatim WARN is a secondary signal. |
| T-09-EM | Information Disclosure | committed principles.md | mitigate | check-hygiene HARD-fails on non-ASCII bytes + non-allowlisted email tokens (work-email leak recurred twice in Phase 4). |
</threat_model>

<verification>
- node tools/check-crossrefs.mjs exits 0 (principles.md Beck pointers resolve).
- npm run check exits 0 (full battery incl. check-backing + the four rewired checkers).
- npm run typecheck exits 0; claude plugin validate . passes; skill-reviewer PASSes.
</verification>

<success_criteria>
principles.md links to the two Beck backing files (D-06) with both pointers resolving, and the phase is
GREEN end-to-end: full checker battery + typecheck + claude plugin validate . + skill-reviewer PASS --
closing CCH-01..06 and PRIN-01..03 with the no-oracle refs anchored by skill-reviewer and DST-04 hygiene.
</success_criteria>

<output>
Create `.planning/phases/09-coach-behavior-principle-backing/09-04-SUMMARY.md` when done.
</output>
