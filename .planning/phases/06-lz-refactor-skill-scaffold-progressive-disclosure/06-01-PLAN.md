---
phase: 06-lz-refactor-skill-scaffold-progressive-disclosure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells.md
  - plugins/lz-tdd/skills/lz-refactor/references/principles.md
  - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md
  - .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs
autonomous: true
requirements: [SKEL-01, SKEL-02, SKEL-03, SKEL-04]

must_haves:
  truths:
    - "User can invoke /lz-tdd:lz-refactor -- SKILL.md exists at plugins/lz-tdd/skills/lz-refactor/ with name == directory name (SKEL-01, D-01)"
    - "The skill is dual-mode by omission: frontmatter carries name + description only; disable-model-invocation and user-invocable are ABSENT so it auto-triggers as a coach AND is user-invocable as a reference (SKEL-01, D-02)"
    - "SKILL.md is a lean router under 500 lines (target ~90-150) carrying only dual-mode framing, the lz-tpp seam note, a marked deferred-coach placeholder, and five task-area pointers -- no catalog content, no full coach procedure (SKEL-02, D-01, D-06)"
    - "SKILL.md exposes exactly five one-level-deep references/ pointers, one per D-03 task-area group, and every pointer resolves to a file on disk (SKEL-02, SKEL-04, D-03)"
    - "The description is present, within the ~1,536-char cap (target ~750), and carries a should-be-used clause, a Do-not-use near-miss clause, and the lz-tpp seam exclusion (green/transformation step vs refactor step) (SKEL-03, D-07, D-08)"
    - "Heavy catalog material is bundled under references/ subdirs, not inlined in SKILL.md: references/fowler-catalog/ and references/kerievsky-catalog/ exist behind thin index entry-points (SKEL-04, D-04)"
    - "Each references/ stub carries its per-entry content contract and a Populated-in-Phase marker; stubs are neither empty nor populated with real catalog content (SKEL-04, D-05, D-06)"
    - "Both catalog stubs state that Phases 7/8 require an AskUserQuestion oracle-access checkpoint before authoring content (owner Fowler e-book/web ISBN 9780135425664, Kerievsky book, GoF e-book) (D-09)"
    - "Every created file is ASCII-only and contains no verbatim Fowler/Kerievsky/GoF prose or code (DST-04, ASCII-only constraint)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      provides: "Dual-mode router: minimal frontmatter + five task-area pointers"
      contains: "name: lz-refactor"
      max_lines: 500
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md"
      provides: "Thin Fowler catalog index stub + per-entry content contract + Phase 7 oracle-access note (D-05, D-09)"
      contains: "Per-entry content contract"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md"
      provides: "Thin Kerievsky catalog index stub + per-entry content contract + Phase 8 oracle-access note (D-05, D-09)"
      contains: "Per-entry content contract"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      provides: "Unified smell-taxonomy stub (coach trigger-table shape)"
      contains: "Per-entry content contract"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/principles.md"
      provides: "Fowler Ch.2 principles stub (+ note Beck backing placement finalized in Phase 9, D-03)"
      contains: "Per-entry content contract"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md"
      provides: "Feathers no-tests core-techniques stub"
      contains: "Per-entry content contract"
    - path: ".claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs"
      provides: "Wave-0 SC1-SC4 filesystem/frontmatter checker (exits 0 only when the scaffold is complete)"
      contains: "lz-refactor"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/*"
      via: "five one-level-deep markdown pointers that all resolve on disk"
      pattern: "\\]\\(references/"
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md frontmatter name"
      to: "skill directory lz-refactor"
      via: "name == directory name (command derivation)"
      pattern: "name: lz-refactor"
    - from: ".claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs"
      to: "the SC1-SC4 checks in 06-VALIDATION.md"
      via: "encoded shell/filesystem assertions, exit 0 on full pass"
      pattern: "SUMMARY"
---

<objective>
Stand up the SKELETON of the `lz-refactor` skill: a user-invocable, auto-triggering
`/lz-tdd:lz-refactor` dual-mode router (`SKILL.md`) plus a wired progressive-disclosure
`references/` tree whose files are content-contract STUBS -- ready for Phases 7-11 to fill.
This satisfies SKEL-01..04 only. No catalog content, no full coach decision procedure, no
version bump, no README/CHANGELOG, no evals (those are Phases 7-11).

Purpose: complete the red-green-refactor seam alongside the shipped `lz-tpp` (lz-tpp drives the
green step; lz-refactor will drive the refactor step) by establishing a lean router and a
splittable references architecture that later phases drop oracle-verified content into.

Output: `plugins/lz-tdd/skills/lz-refactor/SKILL.md`, five `references/` stubs (two of them thin
catalog index entry-points inside splittable subdirs), and one throwaway Wave-0 verification
checker. No `plugin.json` edit (skills are auto-discovered; the 0.0.2 version bump is Phase 10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-CONTEXT.md
@.planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-RESEARCH.md
@.planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-PATTERNS.md
@.planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-VALIDATION.md

# The shape to copy (in-repo shipped, validate --strict clean, 100/100 trigger eval):
@plugins/lz-tdd/skills/lz-tpp/SKILL.md
@plugins/lz-tdd/skills/lz-tpp/references/transformations.md
</context>

<constraints>
- ASCII-only in every created file: use `->` (not the arrow glyph), `--` (not en/em dash),
  straight quotes, `...` (not the ellipsis glyph). No emojis, no box-drawing (repo CLAUDE.md,
  RESEARCH Pitfall 4). Windows cp1252 produces mojibake otherwise.
- DST-04: NO verbatim Fowler/Kerievsky/GoF prose or code -- even in stubs. Stubs carry ONLY the
  contract template + original wording. The lz-tpp blockquote-a-blog practice does NOT extend to
  the three refactoring BOOKS.
- Public-repo hygiene: public contact larsbrinknielsen@gmail.com only; NEVER the work email.
- Do NOT edit `plugins/lz-tdd/.claude-plugin/plugin.json` -- skills are auto-discovered; the
  version bump to 0.0.2 is Phase 10 (DST-01), explicitly out of scope here.
- Do NOT author catalog content (66 Fowler / 27 Kerievsky entries, 24+ smells, Ch.2 principles),
  the full coach decision procedure, README/CHANGELOG, or evals -- Phases 7-11 (D-06).
- Do NOT create per-axis catalog leaf files -- the intra-catalog split axis is DEFERRED to Phase
  7/8 (D-04). Ship the subdir + thin index only.
- The command derives from the directory name: frontmatter `name` MUST equal `lz-refactor`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 scaffold verification checker</name>
  <files>.claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs</files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/eval-status.mjs (Node ESM checker STYLE analog: shebang,
      `import fs from "node:fs"` / `path`, small `exists`/`nonEmpty` helpers, a final `SUMMARY:`
      line, non-zero `process.exit` on failure)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-VALIDATION.md
      ("Per-Task Verification Map" -- the authoritative SC1-SC4 check list)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-RESEARCH.md
      ("Validation Architecture" -- SC -> assertion table)
  </read_first>
  <action>
    Create a single Node ESM checker at .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs
    (workspace tools/ dir, which is git-tracked; NOT inside the shipped skill dir -- a checker
    script there would pollute skill auto-discovery). Mirror the eval-status.mjs style. Encode the
    SC1-SC4 assertions against SKILL_DIR = plugins/lz-tdd/skills/lz-refactor:
    (1) SKILL.md exists;
    (2) parse the YAML frontmatter block minimally: `name` === "lz-refactor"; `description`
        present and non-empty; the keys `disable-model-invocation` AND `user-invocable` are ABSENT
        (dual-mode by omission);
    (3) `description` length <= 1536 (report the count; target ~750);
    (4) SKILL.md line count < 500;
    (5) exactly 5 DISTINCT `](references/...)` pointer targets in SKILL.md;
    (6) every extracted `](references/...)` target resolves to an existing file (resolve relative
        to SKILL_DIR);
    (7) both subdirs exist: references/fowler-catalog/ and references/kerievsky-catalog/;
    (8) the description body contains a "should be used" substring, a "Do not use" (near-miss)
        substring, and the substring "lz-tpp" (seam mention, D-08);
    (9) ASCII-only: scan SKILL.md and all references/*.md (recursively) for any byte outside
        0x00-0x7F and fail if found.
    Print one PASS/FAIL line per check and a final `SUMMARY:` line; `process.exit(0)` only when
    all checks pass, otherwise a non-zero exit. Keep it one throwaway checker -- no framework, no
    dependencies (node builtins only). ASCII-only source.
  </action>
  <acceptance_criteria>
    - File exists at .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs
    - `node --check .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs` succeeds (valid syntax)
    - Running `node .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs` now prints a
      `SUMMARY:` line and exits NON-ZERO (expected RED baseline: the skill files do not exist yet)
    - The checker encodes all nine assertions listed in the action (SC1-SC4 coverage)
    - `rg -n '[^\x00-\x7F]' .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs` returns nothing (ASCII-only)
  </acceptance_criteria>
  <verify>
    <automated>node --check .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs && node .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs; test $? -ne 0 && echo "RED baseline OK (non-zero pre-scaffold)"</automated>
  </verify>
  <done>Runnable Wave-0 checker exists, syntactically valid, encodes SC1-SC4, and exits non-zero before the scaffold is built.</done>
</task>

<task type="auto">
  <name>Task 2: references/ stub tree (five stubs + two catalog subdirs)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md, plugins/lz-tdd/skills/lz-refactor/references/smells.md, plugins/lz-tdd/skills/lz-refactor/references/principles.md, plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (stub SHAPE: title, scoped intro,
      Sources/provenance discipline -- inherit the shape, never book prose)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-PATTERNS.md
      ("Pattern Assignments" for the thin-index and single-file stubs; the exact per-file guidance)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-RESEARCH.md
      ("Code Examples" -- the D-05 Fowler and Kerievsky per-entry content contracts; the candidate
      split axes to list as a deferred comment)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-CONTEXT.md
      (D-03 five groups, D-04 splittable/axis-deferred, D-05 stub contract, D-09 oracle-access)
  </read_first>
  <action>
    Create the five references/ stub files under plugins/lz-tdd/skills/lz-refactor/references/.
    Each stub = a title, a one-line scope statement, a `Populated in Phase N` marker, and the
    per-entry content contract the later phase fills. NO catalog content, NO verbatim book prose or
    code (DST-04), ASCII-only.
    - fowler-catalog/README.md (thin index entry-point, inside the splittable subdir): scope = all
      66 Fowler refactorings, provenance-labeled; marker `Populated in Phase 7`; the Fowler
      per-entry contract (name + 1st-ed aliases / motivation (original words) / distilled mechanics
      (original words) / TS-JS before -> after (tsc --strict clean) / provenance label for the 5
      print-absent "+" entries and Split Phase online-only); a D-09 note stating Phase 7 requires
      an AskUserQuestion oracle-access checkpoint (owner Fowler e-book/web ISBN 9780135425664, GoF
      e-book) BEFORE authoring content; a deferred-split-axis comment listing the candidate axes
      from RESEARCH (Fowler's 20 tag-groups vs book-chapter grouping) WITHOUT picking one (D-04).
    - kerievsky-catalog/README.md (thin index entry-point, inside the splittable subdir): scope =
      all 27 Kerievsky pattern-directed refactorings; marker `Populated in Phase 8`; the Kerievsky
      per-entry contract (name; intent / distilled mechanics (original words) / TS-JS re-rendered
      from the book's Java (tsc --strict clean) / composed Fowler primitive(s) / direction
      To-Towards-Away with de-patterning cases called out / GoF pattern cross-reference, vocabulary
      only, no GoF text); a D-09 note stating Phase 8 requires an AskUserQuestion oracle-access
      checkpoint (owner Kerievsky book, GoF e-book) BEFORE authoring content; a deferred-split-axis
      comment (by direction vs by GoF family) WITHOUT picking one (D-04).
    - smells.md: unified smell-taxonomy stub in a coach trigger-table shape; scope = Fowler bad
      smells + Kerievsky Ch.4 smells folded and deduplicated with source tags; marker
      `Populated in Phases 7-8`; per-entry contract (smell name / source tag / candidate named
      refactorings).
    - principles.md: Fowler Ch.2 principles stub; marker `Fowler Ch.2 populated in Phase 7`; note
      that Beck principle-backing placement is finalized in Phase 9 (D-03) -- do NOT pre-split a
      principles-beck.md; per-entry contract (principle / distilled statement (original words) /
      attribution).
    - refactoring-without-tests.md: Feathers no-tests core-techniques stub; marker
      `Populated in Phase 9`; per-entry contract (technique / when-to-use / distilled mechanics
      (original words), core techniques only -- seams, characterization tests, the change
      algorithm, Sprout/Wrap Method+Class, Subclass and Override Method, Extract Interface).
    Creating the two README.md files inside their subdirs establishes references/fowler-catalog/
    and references/kerievsky-catalog/ so the SKILL.md pointers resolve and `test -d` passes. Do NOT
    create per-axis leaf files.
  </action>
  <acceptance_criteria>
    - All five files exist at their exact paths under plugins/lz-tdd/skills/lz-refactor/references/
    - `test -d plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog` and
      `test -d plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog` both pass
    - Each stub contains a per-entry content contract heading and a `Populated in Phase` marker
    - fowler-catalog/README.md and kerievsky-catalog/README.md each contain the D-09 AskUserQuestion
      oracle-access note (naming the owner Fowler e-book/web ISBN 9780135425664, Kerievsky book, GoF
      e-book as applicable) and a deferred-split-axis comment that does NOT pick an axis
    - No stub contains verbatim book prose or code (contract templates + original wording only)
    - `rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-refactor/references/` returns nothing (ASCII-only)
  </acceptance_criteria>
  <verify>
    <automated>for f in plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md plugins/lz-tdd/skills/lz-refactor/references/smells.md plugins/lz-tdd/skills/lz-refactor/references/principles.md plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done; test -d plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog && test -d plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog && ! rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-refactor/references/ && echo "STUBS OK"</automated>
  </verify>
  <done>Five ASCII-only stubs exist (two as thin catalog indexes inside splittable subdirs), each carrying its D-05 content contract and Phase marker; both catalog stubs carry the D-09 oracle-access note and an unresolved split-axis comment.</done>
</task>

<task type="auto">
  <name>Task 3: SKILL.md dual-mode router</name>
  <files>plugins/lz-tdd/skills/lz-refactor/SKILL.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md (THE shape to copy: folded `>-` description,
      name+description-only frontmatter, "Two modes" framing, the transformations-vs-refactorings
      seam block, the `[references/x.md](references/x.md)` pointer idiom, near-miss clause)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-PATTERNS.md
      ("Pattern Assignments -> SKILL.md" -- the fused lz-tpp + angular-developer body sectioning;
      the exact section shapes and the excerpted angular-developer `##`-per-task-area idiom)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-RESEARCH.md
      ("Code Examples -> SKILL.md skeleton" and "Pattern 3" -- the 4-part dual-mode description
      draft and the seam exclusion wording)
    - .planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-CONTEXT.md
      (D-01 hybrid lean router, D-02 minimal frontmatter, D-06 no catalog/no full procedure,
      D-07 description shape, D-08 seam disambiguation)
  </read_first>
  <action>
    Create plugins/lz-tdd/skills/lz-refactor/SKILL.md as a lean dual-mode router.
    Frontmatter (D-02): a folded `>-` scalar with `name: lz-refactor` and `description` ONLY. Omit
    disable-model-invocation, user-invocable, allowed-tools, version, license, and any metadata
    block. `name` MUST be exactly `lz-refactor` (== directory name).
    Description (D-07/D-08): follow the lz-tpp 4-part shape at ~750 chars (hard cap 1,536):
    (1) "This skill should be used" during the refactor step of red-green-refactor TDD to recommend
    the next NAMED refactoring for a detected code smell, and to explain refactorings / smells /
    principles on demand; (2) concrete triggers (a code smell is present, "which refactoring should
    I apply", named-refactoring lookup e.g. Extract Function / Replace Conditional with
    Polymorphism, "refactor away from a pattern" de-patterning, Fowler / Kerievsky catalog
    questions); (3) both modes named (coach + reference); (4) an explicit "Do not use it for" the
    green / transformation step (making a failing test pass -- that is lz-tpp), plain feature work,
    or generic write-code requests. The literal token "lz-tpp" must appear in the description.
    Body (D-01, D-06), ASCII-only, target ~90-150 lines, hard cap < 500:
    - Title `# lz-refactor: Refactoring coach (Fowler + Kerievsky)` + a one-paragraph purpose.
    - `## Two modes` -- two bullets (Coach mode: smell present -> recommend a NAMED refactoring,
      note full procedure deferred to Phase 9; Reference mode: explain on demand -> route to references/).
    - `## Refactoring vs the green step (the lz-tpp seam)` -- one line: refactor step
      (structure-only, behavior-preserving) = lz-refactor; green / transformation step (make a
      failing test pass) = lz-tpp.
    - `## Coach decision procedure (deferred to Phase 9)` -- a marked placeholder only. Do NOT
      author the procedure; state it lands in Phase 9 (CCH-01..05).
    - The FIVE task-area pointer sections, each a `##` heading + one-line scope + ONE one-level-deep
      pointer, using exactly these headings and targets:
      `## Fowler catalog (mechanical refactorings)` -> `[references/fowler-catalog/README.md](references/fowler-catalog/README.md)`
      `## Smell taxonomy (coach trigger table)` -> `[references/smells.md](references/smells.md)`
      `## Refactoring principles (Fowler Ch.2 + backing)` -> `[references/principles.md](references/principles.md)`
      `## Kerievsky pattern-directed refactorings` -> `[references/kerievsky-catalog/README.md](references/kerievsky-catalog/README.md)`
      `## Refactoring safely without tests (Feathers)` -> `[references/refactoring-without-tests.md](references/refactoring-without-tests.md)`
    No catalog content, no numbered 66/27-entry lists, no full coach procedure in the body (D-06).
    Exactly five `](references/...)` pointers total (the Two-modes, seam, and coach-placeholder
    sections carry NO references pointer).
  </action>
  <acceptance_criteria>
    - File exists at plugins/lz-tdd/skills/lz-refactor/SKILL.md
    - Frontmatter `name` === "lz-refactor" (== directory name); `description` present and non-empty
    - `disable-model-invocation` and `user-invocable` are BOTH ABSENT from the frontmatter (dual-mode by omission)
    - `description` length <= 1536 chars (target ~750) and contains a "should be used" clause, a
      "Do not use" near-miss clause, and the token "lz-tpp"
    - `wc -l` of SKILL.md < 500 (target ~90-150)
    - SKILL.md contains exactly 5 distinct `](references/...)` pointers, each resolving to a file
      created in Task 2
    - No catalog content is inlined (no 66/27-entry list, no full coach procedure)
    - `rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-refactor/SKILL.md` returns nothing (ASCII-only)
    - `node .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs` exits 0 (all SC1-SC4 green)
    - `claude plugin validate .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs && claude plugin validate . && echo "SCAFFOLD GREEN"</automated>
  </verify>
  <done>SKILL.md is a lean, dual-mode-by-omission router under 500 lines with a ~750-char seam-aware description and exactly five resolving references/ pointers; the Wave-0 checker and `claude plugin validate .` both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase ships zero runtime code -- pure Markdown/JSON. The only meaningful boundary is
author -> public git repository (content authored locally crosses into a public, cloneable repo).
No client/server, no input parsing, no network, no crypto.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | committed files (SKILL.md, references/*, checker) | mitigate | Work email absent; public contact larsbrinknielsen@gmail.com only. Post-commit allowlist scan per project memory ("Public-repo work-email allowlist"); recurred twice in Phase 4 -- verify absence of the work-email literal before finalizing. |
| T-06-02 | Tampering (IP/copyright, DST-04) | references/ stubs + SKILL.md | mitigate | Stubs carry ONLY the per-entry contract template + original wording; NO verbatim Fowler/Kerievsky/GoF prose or code, even in scaffold. The lz-tpp blog-quote practice does not extend to the three books. |
| T-06-03 | Tampering (data integrity) | every created file on Windows cp1252 | mitigate | ASCII-only in all files; `rg -n '[^\x00-\x7F]' <file>` must return nothing (checker assertion 9 + per-task acceptance criteria). Prevents mojibake. |
| T-06-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Not applicable -- this phase installs ZERO external packages (RESEARCH "Package Legitimacy Audit: Not applicable"). The checker uses node builtins only. If any future task proposes a dependency, halt and run the Package Legitimacy Gate first. |

## ASVS Applicability (V2-V6)

- V2 Authentication: not applicable -- no auth surface.
- V3 Session Management: not applicable -- no sessions.
- V4 Access Control: not applicable -- no runtime.
- V5 Input Validation: not applicable -- the skill ships no code that parses input.
- V6 Cryptography: not applicable -- no crypto.

No `high`-severity threat is open; nothing blocks.
</threat_model>

<verification>
Phase-level gate (after all three tasks; maps to 06-VALIDATION.md SC1-SC4):
- SC1: `test -f plugins/lz-tdd/skills/lz-refactor/SKILL.md`; frontmatter `name == lz-refactor`,
  `description` non-empty, `disable-model-invocation` + `user-invocable` ABSENT; `claude plugin validate .` exit 0.
- SC2: `wc -l SKILL.md` < 500; exactly five `##` task-area pointer sections; every
  `](references/...)` target resolves on disk.
- SC3: `description` length <= 1,536; carries should-be-used + Do-not-use + lz-tpp seam mention.
- SC4: `test -d references/fowler-catalog` and `test -d references/kerievsky-catalog`; no catalog
  content inlined in SKILL.md; catalog index files are thin stubs.
- Single-command gate: `node .claude/skills/lz-refactor-workspace/tools/verify-scaffold.mjs` exits 0
  AND `claude plugin validate .` exits 0.
- Hygiene gate: no work-email literal in any created file; `rg -n '[^\x00-\x7F]'` over the skill
  tree returns nothing; no verbatim book prose/code.
- First-party review (post-execution, per project workflow): plugin-validator + skill-reviewer PASS.
</verification>

<success_criteria>
- `/lz-tdd:lz-refactor` is invocable and dual-mode by omission (SKEL-01).
- SKILL.md is a lean router (< 500 lines) with five one-level-deep references/ pointers grouped by
  task area, all resolving (SKEL-02).
- The description is authored (not yet empirically validated) for refactor-step / code-smell /
  catalog triggers with a near-miss + lz-tpp seam exclusion, within the char cap (SKEL-03).
- Heavy catalog material is bundled in references/ subdirs behind thin index stubs; no single file
  forces the whole catalog into context (SKEL-04).
- The Wave-0 checker and `claude plugin validate .` both exit 0; all files ASCII-only with no
  verbatim book prose and no work email.
</success_criteria>

<output>
Create `.planning/phases/06-lz-refactor-skill-scaffold-progressive-disclosure/06-01-SUMMARY.md` when done.
</output>
