---
phase: 08-kerievsky-catalog-refactoring-to-patterns
plan: 06
type: execute
wave: 3
depends_on: [08-02, 08-03, 08-04, 08-05]
branching_strategy: none
tags: [kerievsky-catalog, smells, skill-authoring, oracle-clean-room, taxonomy, catalog-index, fold-dedup]
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/indecent-exposure.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/solution-sprawl.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/combinatorial-explosion.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/oddball-solution.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/conditional-complexity.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/long-function.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/primitive-obsession.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/large-class.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/repeated-switches.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/lazy-element.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/alternative-classes-with-different-interfaces.md
autonomous: false
requirements: [KRV-01, KRV-03]
must_haves:
  truths:
    - "D-05: Kerievsky's Ch.4 smells are folded into the existing unified taxonomy with source tags, deduplicated against Fowler's 24 -- Kerievsky-UNIQUE smells become new smells/<slug>.md leaves + new source-tagged navigation-only rows in smells.md; OVERLAP smells get an additive source tag + an 'also named by Kerievsky: <name>' note on the existing Fowler leaf (and, where apt, Kerievsky candidate refactorings added to that leaf's candidate map) -- NEVER rewriting the Phase-7 recognize-by cues"
    - "D-05: a Fowler / Kerievsky / both source tag is introduced (additive on existing rows/leaves); the exact unique-vs-overlap dedup MAP is settled DURING the oracle-gated loop against .oracle/ Ch.4, NOT blind -- the file list here is PROVISIONAL"
    - "D-05: check-smells (extended in 08-01) is GREEN -- new unique leaves present with the LOCKED smell-leaf contract, source tags valid, no un-deduped duplicate (a Kerievsky-tagged new leaf must not collide with an existing Fowler smell), and every candidate link resolves incl. ../kerievsky-catalog/ targets"
    - "D-01: kerievsky-catalog/README.md is finalized as a THIN index grouped by the 6 book chapters (Creation/Simplification/Generalization/Protection/Accumulation/Utilities), each row mirroring the leaf's Use when: verbatim + carrying the Direction + GoF annotations + a resolving link; the old Phase-6 'Deferred split axis' section is removed (resolved by D-01); SKILL.md is NOT re-touched"
    - "KRV-01: the full deterministic battery is GREEN at this gate -- check-kerievsky 27/27 + README-row Use-when mirror + 3 Away cases + 3 fields, check-smells (Fowler 24 + folded Kerievsky smells + candidate resolution), check-crossrefs (composed-Fowler links resolve, mutuality Fowler-scoped), extract-samples tsc --strict over every example, check-catalog 62/62 (Fowler regression), check-principles, check-hygiene -- and claude plugin validate . passes"
    - "KRV-01: if wave-2 oracle-confirmation corrected any canonical refactoring name/slug (recorded in the wave-2 SUMMARYs), 08-06 Task 2 reconciles the check-kerievsky.mjs NAMES array (data only, not logic) to the authored leaf H1 slugs in lockstep -- the single authorized post-wave-1 checker edit; the NAMES array MUST match the authored leaf H1 slugs so the 27/27 identity gate passes (D-01 identity; D-07 oracle authority over names)"
    - "D-07: each new/edited smell leaf is authored/edited BLIND, gated by the oracle-reviewer subagent over .oracle/refactoring-to-patterns/index.md scoped to 'Ch.4 -- code smells', revised BLIND to convergence; the main context NEVER reads .oracle/ (ls for names only)"
    - "D-08: this plan runs INLINE in the MAIN context (Agent + AskUserQuestion tools), NOT via gsd-executor and NOT in a worktree (branching_strategy: none); the oracle agents are read-only so there is no file conflict"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/smells/combinatorial-explosion.md"
      provides: "Kerievsky-unique smell leaf (source tag Kerievsky; candidates mostly kerievsky-catalog)"
      contains: "Recognize by:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md"
      provides: "finalized thin index: 6 chapter groups, 27 rows with mirrored Use-when + Direction + GoF + resolving links"
      contains: "Use when:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      provides: "navigation-only smell index extended with source-tagged Kerievsky-unique rows (Phase-7 rows untouched except an additive tag)"
      contains: "recognize by:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/smells/combinatorial-explosion.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md"
      via: "candidate-refactoring cross-link into the Kerievsky catalog (resolving H1 anchor)"
      pattern: "kerievsky-catalog/[a-z-]+\\.md#"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md"
      via: "one resolving row per leaf with mirrored Use-when + Direction + GoF"
      pattern: "kerievsky-catalog/[a-z-]+\\.md|\\[.*\\]\\([a-z-]+\\.md\\)"
---

<objective>
Close the Kerievsky layer: fold Kerievsky's Ch.4 smells into the unified taxonomy (source-tagged,
deduped against Fowler's 24), finalize the thin kerievsky-catalog/README.md index, then run the
FULL deterministic battery green + claude plugin validate. Runs LAST (wave 3), after all 27 catalog
leaves exist so unique-smell candidate cross-links and index rows resolve.

Purpose: KRV-03 (the Ch.4 smell fold Phase 9's coach consumes) plus the KRV-01 index finalization
and the phase-gate battery re-verify. Kerievsky-UNIQUE smells become new source-tagged leaves +
navigation-only rows; OVERLAP smells get an additive source tag + note on the existing Fowler leaf
(never a rewrite).

Output: ~4-5 new unique smell leaves, the extended navigation-only smells.md, the additive edits
to the overlap Fowler leaves, the finalized kerievsky-catalog/README.md, and a green full battery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md
@plugins/lz-tdd/skills/lz-refactor/references/smells.md
@plugins/lz-tdd/skills/lz-refactor/references/smells/repeated-switches.md
@plugins/lz-tdd/skills/lz-refactor/references/smells/primitive-obsession.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md

<interfaces>
CLEAN-ROOM FIREWALL (D-07): the executor/main context NEVER opens any file under .oracle/ -- `ls`
for names only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` gates a drafted/edited smell leaf. Input: DRAFT_PATHS (the new + edited smell
  leaves), SOURCE = ".oracle/refactoring-to-patterns/index.md" + "Ch.4 -- code smells" (index.md +
  chapter number/topic; NEVER a book chapter filename), CONTENT_TYPE = "smell-leaf", SCOPE = each
  leaf's smell, the smell-leaf canonical anchors (candidates, recognition, motivation,
  applicability, spirit) PLUS a dedup axis (is this smell genuinely unique vs an overlap of a named
  Fowler smell?). Returns pass|revise|blocked|error.
- `oracle` for open-ended lookups (the exact Ch.4 smell set + which are duplicates of which Fowler
  smells -- D-05: the dedup map is oracle-settled, NOT from research).

Ch.4 = "twelve design smells" (CITED). PROVISIONAL unique-vs-overlap map (RESEARCH -- the oracle
SETTLES the exact boundary; adjust the file set accordingly):
  UNIQUE (new leaf, tag Kerievsky): Indecent Exposure, Solution Sprawl, Combinatorial Explosion,
    Oddball Solution, and PROVISIONALLY Conditional Complexity (borderline vs Repeated Switches --
    draft as unique; the oracle may reclassify it to an overlap-tag, in which case delete the leaf
    and tag Repeated Switches instead).
  OVERLAP (additive tag + note on the existing Fowler leaf, tag becomes both): Duplicated Code;
    Long Method -> long-function; Primitive Obsession; Large Class; Switch Statements ->
    repeated-switches; Lazy Class -> lazy-element; Alternative Classes with Different Interfaces.

Candidate targets for the UNIQUE smells are mostly KERIEVSKY refactorings (now authored in wave 2):
Conditional Complexity -> Replace Conditional Logic with Strategy / Replace State-Altering
Conditionals with State / Move Embellishment to Decorator; Combinatorial Explosion -> Replace
Implicit Language with Interpreter; Oddball Solution -> Unify Interfaces with Adapter / Introduce
Null Object; Solution Sprawl -> Move Creation Knowledge to Factory / Move Accumulation to
Collecting Parameter; Indecent Exposure -> Encapsulate Classes with Factory. check-smells (extended
in 08-01) resolves both (fowler|kerievsky)-catalog candidate links.

Deterministic checkers were extended in 08-01 -- do NOT edit their LOGIC here, only run them. The
SOLE authorized checker edit in this plan is reconciling the check-kerievsky.mjs NAMES array (data,
not logic) to the authored leaf H1 slugs when wave-2 oracle-confirmation corrected a canonical
name/slug (Task 2):
check-kerievsky (27 identity + README-row mirror + 3 fields + 3 Away), check-smells (Fowler 24 +
folded Kerievsky + source tags + dedup + (fowler|kerievsky) candidate resolution), check-crossrefs
(composed-Fowler links resolve, mutuality Fowler-scoped), extract-samples (tsc --strict),
check-catalog (Fowler 62 regression), check-principles, check-hygiene.

kerievsky-catalog/README.md column format (Claude's discretion, D-01): Refactoring | Direction |
GoF pattern | Use when -- each Use when mirrored VERBATIM from its leaf; grouped under the 6 chapter
headings; delete the Phase-6 "Deferred split axis" section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold Kerievsky Ch.4 smells into the unified taxonomy via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/smells/indecent-exposure.md, plugins/lz-tdd/skills/lz-refactor/references/smells/solution-sprawl.md, plugins/lz-tdd/skills/lz-refactor/references/smells/combinatorial-explosion.md, plugins/lz-tdd/skills/lz-refactor/references/smells/oddball-solution.md, plugins/lz-tdd/skills/lz-refactor/references/smells/conditional-complexity.md, plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md, plugins/lz-tdd/skills/lz-refactor/references/smells/long-function.md, plugins/lz-tdd/skills/lz-refactor/references/smells/primitive-obsession.md, plugins/lz-tdd/skills/lz-refactor/references/smells/large-class.md, plugins/lz-tdd/skills/lz-refactor/references/smells/repeated-switches.md, plugins/lz-tdd/skills/lz-refactor/references/smells/lazy-element.md, plugins/lz-tdd/skills/lz-refactor/references/smells/alternative-classes-with-different-interfaces.md, plugins/lz-tdd/skills/lz-refactor/references/smells.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- smell leaf + navigation-only smell index)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", smell-leaf axes, "Driver responsibilities")
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md (the candidate map is where smell drafts drift most -- budget extra rounds)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-05 fold + dedup rules; D-07; D-08)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md (unique smell-leaf shape + additive overlap-edit shape + navigation-index row shape)
    - plugins/lz-tdd/skills/lz-refactor/references/smells/repeated-switches.md (smell-leaf contract exemplar)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/*.md (read the actual Kerievsky leaf headings/slugs so candidate cross-links resolve)
    - .oracle/ (ls ONLY for filenames -- NEVER open any file under .oracle/ from this context)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not). Do
    NOT open any file under .oracle/ from this context. Run this loop INLINE in the main context
    (D-08) -- NOT via gsd-executor, NOT in a worktree.

    FIRST settle the dedup map via the oracle (D-05): use `oracle` to confirm the exact Ch.4 smell
    set and which are duplicates of which Fowler smells vs genuinely unique -- do NOT rely on the
    provisional research map. Then, per the settled map:
    (1) For each Kerievsky-UNIQUE smell: AUTHOR BLIND a new smells/<slug>.md leaf in the LOCKED
    smell-leaf format -- single `# <Smell>` H1, one-line `Recognize by:` (mirrored to smells.md),
    `## How to recognize` (confirm the fuzzy match, separate near-neighbors), `## Why it's a
    problem`, `## Candidate refactorings` (each: a resolving link -- mostly ../kerievsky-catalog/
    targets -- plus "pick when <discriminator>"). Add the `Kerievsky` source tag. Add a
    navigation-only row to smells.md (recognize-by cue + resolving link + source tag), keeping the
    navigation-only shape (NO candidates in the index).
    (2) For each OVERLAP smell: on the EXISTING Fowler leaf, ADD a source tag (becomes `both`) + an
    "also named by Kerievsky: <name>" note, and (where apt) ADD Kerievsky candidate refactorings to
    that leaf's `## Candidate refactorings` map -- additive only; NEVER rewrite the Phase-7
    recognize-by cue or existing candidates. Add the `both` tag to that smell's smells.md row.
    (3) DETERMINISTIC LAYER FIRST (D-07 -- run the cheap checks BEFORE spending reviewer calls): run
    check-smells + check-crossrefs + check-hygiene over the new + edited smell leaves; fix any
    unresolved candidate link / invalid source tag / un-deduped duplicate / ASCII defect BLIND. Only
    once this layer is green do you spend an oracle-reviewer call.
    (4) GATE: spawn `oracle-reviewer` with DRAFT_PATHS (new + edited smell leaves), SOURCE
    ".oracle/refactoring-to-patterns/index.md" + "Ch.4 -- code smells", CONTENT_TYPE "smell-leaf",
    SCOPE (each leaf's smell), the smell-leaf anchors + the dedup axis. Sub-batch ~6-8; use `oracle`
    for open-ended candidate-set / dedup lookups.
    (5) REVISE BLIND from the directives (never opening the source); if the oracle reclassifies a
    provisional-unique smell (e.g. Conditional Complexity) as an overlap, DELETE its new leaf/row
    and apply the additive-tag path instead.
    (6) ITERATE to convergence (every entry `pass`, or owner-accepted `blocked`); cap rounds ~3 and
    escalate to Task 3. Fan-out guard.
    (7) RE-GATE post-oracle deterministic fixes: after applying reviewer directives, re-run
    check-smells + check-crossrefs + check-hygiene; if a post-oracle fix CHANGES a smell leaf's
    candidate map or recognition text, re-run oracle-reviewer on that leaf so no folded smell ships
    un-re-reviewed for fidelity / near-verbatim. Commit on green. ASCII only.
  </action>
  <acceptance_criteria>
    - Each settled Kerievsky-UNIQUE smell has a new smells/<slug>.md leaf with the full contract (`rg -q "^Recognize by:" <leaf> && rg -q "## Candidate refactorings" <leaf>`) + a `Kerievsky` source tag + at least one resolving candidate link (incl. ../kerievsky-catalog/ targets).
    - Each settled OVERLAP smell's existing Fowler leaf carries an additive source tag (`both`) + an "also named by Kerievsky" note, with the Phase-7 recognize-by cue unchanged.
    - smells.md has a navigation-only row (recognize-by + resolving link + source tag) for each new unique smell, and its existing Phase-7 rows are unchanged except an additive tag.
    - `node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` exits 0 (source tags valid, no un-deduped duplicate, all candidate links resolve).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
    - Each new/edited smell leaf reached oracle-reviewer `pass` (or owner-accepted `blocked` via Task 3); no file under .oracle/ was opened by this context.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "^Recognize by:" plugins/lz-tdd/skills/lz-refactor/references/smells/combinatorial-explosion.md && rg -qi "kerievsky" plugins/lz-tdd/skills/lz-refactor/references/smells.md</automated>
  </verify>
  <done>Kerievsky's Ch.4 smells are folded per the oracle-settled dedup map: unique smells are new source-tagged leaves + navigation-only rows; overlaps are additive tags + notes on the existing Fowler leaves (recognize-by cues unchanged); check-smells + check-crossrefs + check-hygiene green; every new/edited leaf oracle-reviewer pass (or owner-accepted); no .oracle/ file opened by this context.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the kerievsky-catalog/README.md index + run the full battery + claude plugin validate</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md, .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- catalog index; thin index, mirrored Use-when)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md ("Deterministic index generator that reads only public leaves" -- guarantees the Use-when mirror is exact)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (the finalized Fowler index shape to mirror)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md (the Phase-6 stub to replace -- remove the "Deferred split axis" section)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/*.md (read each leaf's Use when: / Direction: / GoF pattern: to mirror into its README row)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-01 six-chapter grouping + index annotation discretion)
  </read_first>
  <action>
    Finalize kerievsky-catalog/README.md as a THIN chapter-grouped index. Remove the Phase-6
    "Deferred split axis" section (resolved by D-01) and the AskUserQuestion oracle-checkpoint note
    for the Kerievsky book (superseded by D-07). Group the 27 rows under the six chapter headings
    (Creation Ch.6, Simplification Ch.7, Generalization Ch.8, Protection Ch.9, Accumulation Ch.10,
    Utilities Ch.11) in the confirmed chapter order recorded in the wave-2 SUMMARYs (do NOT reopen
    .oracle/). Use the column format Refactoring | Direction | GoF pattern | Use when: each row =
    a resolving relative link to the leaf + its Direction value + its GoF pattern + the leaf's
    `Use when:` line mirrored VERBATIM. Keep it thin (no leaf content inlined). Do NOT touch
    SKILL.md. NAMES RECONCILIATION (the single authorized post-wave-1 checker edit): if any wave-2
    SUMMARY records an oracle name/slug correction vs the CITED names, update the check-kerievsky.mjs
    NAMES array (data only, never logic) so it matches the authored leaf H1 slugs on disk -- else the
    27/27 identity gate false-fails unfixably; if no name was corrected, make no edit. Then run the
    FULL deterministic battery and make it green: check-kerievsky (27/27 +
    README-row Use-when mirror + 3 Away + 3 fields), check-smells (Fowler 24 + folded Kerievsky +
    candidate resolution), check-crossrefs (composed-Fowler links resolve, mutuality Fowler-scoped),
    extract-samples (tsc --strict over every example incl. walkthroughs), check-catalog (Fowler 62
    regression), check-principles, check-hygiene. Also run `claude plugin validate .` as a
    regression check (the harness stays non-shipped; only pure Markdown ships). Commit on green.
  </action>
  <acceptance_criteria>
    - kerievsky-catalog/README.md lists all 27 refactorings grouped under the 6 chapter headings, each row with a resolving link + Direction + GoF pattern + the leaf's `Use when:` mirrored verbatim; the "Deferred split axis" section is gone (`rg -q "Deferred" <readme>` returns nothing).
    - The check-kerievsky.mjs NAMES array matches the authored leaf H1 slugs on disk (reconciled from the wave-2 SUMMARYs where the oracle corrected a name; unchanged if none were corrected).
    - `node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` exits 0 (27/27 + README-row mirror + 3 Away + 3 fields).
    - The full battery is GREEN: `node extract-samples.mjs && node tools/check-kerievsky.mjs && node tools/check-catalog.mjs && node tools/check-smells.mjs && node tools/check-crossrefs.mjs && node tools/check-principles.mjs && node tools/check-hygiene.mjs` all exit 0.
    - `claude plugin validate .` passes; SKILL.md is unchanged; `git status` shows no new files outside references/ and the non-shipped workspace.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs && node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs && node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && node .claude/skills/lz-refactor-workspace/tools/check-principles.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && claude plugin validate .</automated>
  </verify>
  <done>kerievsky-catalog/README.md is the finalized thin 27-row index (6 chapter groups, mirrored Use-when + Direction + GoF + resolving links; Deferred-split-axis section removed); the full battery is GREEN (check-kerievsky 27/27, check-smells fold, check-crossrefs, extract-samples tsc, check-catalog 62/62 regression, check-principles, check-hygiene) and `claude plugin validate .` passes; SKILL.md untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Owner escalation for non-converging / blocked smell leaves + the dedup boundary (conditional)</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Driver responsibilities" -- loop bound + oscillation guard + owner escalation)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-05 dedup map is oracle-settled; D-07 escalation path)
  </read_first>
  <decision>How to resolve any Ch.4 smell leaf the oracle-reviewer loop cannot converge within ~3 rounds, or an ambiguous unique-vs-overlap dedup boundary the oracle cannot settle (e.g. Conditional Complexity vs Repeated Switches)?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate; the candidate map + dedup boundary are where smell drafts drift most. The driver
    caps rounds (~3) and escalates that ENTRY to the owner via AskUserQuestion. Fires ONLY for
    entries that do not reach `pass` or a dedup boundary the oracle leaves ambiguous.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked smell leaf / dedup classification as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (a missing candidate, or the unique-vs-overlap call), then re-run the loop once</name>
      <pros>Resolves a genuine candidate-set / dedup gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry; may add/remove a leaf</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or an unsettled dedup boundary): open
    an AskUserQuestion with the entry, the reviewer's ambiguities/unable-to-verify axes (own words),
    and the two options; apply the choice (which may add or delete a unique-smell leaf); re-run
    check-smells + check-crossrefs; record it in the SUMMARY. Do NOT reveal or request verbatim
    source prose.
  </action>
  <acceptance_criteria>
    - Every new/edited smell leaf is oracle-reviewer `pass` or owner-adjudicated, and the final unique-vs-overlap map is recorded in 08-06-SUMMARY.md; each escalation (or a no-op note if none) is logged.
  </acceptance_criteria>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry / dedup boundary</resume-signal>
  <done>Every folded smell is oracle-reviewer pass or owner-adjudicated; the final dedup map + escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-to-patterns/` (owner full-text book, git-ignored) -> authored public-repo smell leaves + indexes | copyrighted Kerievsky expression must never cross; the ONLY crossing is via the oracle/oracle-reviewer subagents (isolated context, own-words verdicts, no prose/code/paths) |
| authored files -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure / legal (DST-04) | authored smell-leaf prose + index rows | mitigate | Clean-room firewall: main context NEVER reads .oracle/; authored BLIND; oracle-reviewer full-strength near-verbatim gate; check-hygiene no-verbatim WARN + Phase-10 scan |
| T-08-04 | Tampering (fabricated smell set / wrong dedup / candidate mappings) | recognition + candidate + dedup fidelity | mitigate | oracle-reviewer smell-leaf gate (candidates/recognition + dedup axis) + converge loop; driver caps rounds (~3) and escalates to owner (Task 3) |
| T-08-06 | Tampering | dangling / resolvable-but-wrong candidate cross-links; unfinalized index rows | mitigate | check-smells + check-crossrefs assert every candidate/index link resolves; check-kerievsky README-row mirror; wave 3 runs after all leaves exist |
| T-08-05 | Information Disclosure / firewall | main context accidentally reading .oracle/ prose (incl. fan-out aggregation of the smell selection) | mitigate | firewall discipline (ls only); INLINE main-context orchestration; driver dedupes/caps, does NOT persist a full ordered name+chapter map |
| T-08-02 | Information Disclosure | work email in a smell leaf / index | mitigate | check-hygiene work-email allowlist gate |
| T-08-03 | Tampering (integrity) | non-ASCII / mojibake in a leaf/index | mitigate | check-hygiene ASCII hard-fail; ASCII-only authoring |

No UI, no network, no secrets, no new packages. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The full battery is GREEN: check-kerievsky 27/27 + README-row mirror + 3 Away + 3 fields; check-smells (Fowler 24 + folded Kerievsky + source tags + dedup + candidate resolution); check-crossrefs (composed-Fowler resolve, mutuality Fowler-scoped); extract-samples (tsc --strict); check-catalog 62/62 (Fowler regression); check-principles; check-hygiene. `claude plugin validate .` passes.
- Every new/edited smell leaf reached oracle-reviewer pass (or owner-accepted blocked); the final dedup map is recorded.
- smells.md stays navigation-only; Phase-7 recognize-by cues unchanged (additive tags only); kerievsky-catalog/README.md is the finalized thin index; SKILL.md untouched; no .oracle/ file opened by this context.
</verification>

<success_criteria>
- Kerievsky's Ch.4 smells are folded into the unified taxonomy, source-tagged and deduped against Fowler's 24 (KRV-03); the kerievsky-catalog/README.md index is finalized with all 27 mirrored rows (KRV-01).
- The phase-gate full battery is green (KRV-01..04 all machine-verified); ASCII-only; no verbatim prose; the taxonomy keeps the LOCKED navigation-only shape Phase 9 consumes.
</success_criteria>

<deferred_enhancement>
## DEFERRED to Phase 11 (harness hardening): composed-primitive field <-> Mechanics consistency check

Recorded from the 08-03 rubric review so it is not lost. NOT in this plan's scope -- do NOT add it here.

Idea: a deterministic guard in check-kerievsky asserting every `../fowler-catalog/<slug>` link in a
leaf's `Composed Fowler primitive(s):` field also appears as a link somewhere in that leaf's
`## Mechanics` (field-subset-of-mechanics, presence/link-set only, NOT semantic).

Honest limitation (why it is DEFERRED, not adopted): the Ch.7 (08-03) drift it was meant to catch was
SEMANTIC -- a wrong Fowler sibling that appeared in BOTH the field AND Mechanics and drifted together.
A field-subset-of-Mechanics presence check PASSES on exactly those cases (both places had the same
wrong link), so it would NOT have caught the Ch.7 failures; the oracle-reviewer's composed-primitive
axis already catches them. The presence check only catches a DIFFERENT, so-far-unobserved drift (a
field primitive with no Mechanics mention, or vice versa). Low, speculative value.

Disposition: DEFER to Phase 11 EVL harness hardening (or drop under YAGNI). This plan's SOLE authorized
checker edit stays the check-kerievsky.mjs NAMES-array reconciliation (data, not logic) per Task 2 --
this enhancement is a LOGIC edit and is explicitly OUT of scope here. Not a blocker for the 08-06 gate.
</deferred_enhancement>

<output>
Create `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-06-SUMMARY.md` when done, recording the settled Ch.4 smell set, the final unique-vs-overlap dedup map, per-leaf round counts, any owner escalations, and the final full-battery + claude plugin validate result.
</output>
