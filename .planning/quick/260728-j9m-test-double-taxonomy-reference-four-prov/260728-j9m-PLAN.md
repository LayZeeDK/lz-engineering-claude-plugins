---
phase: quick-260728-j9m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-red/SKILL.md
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
  - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
  - plugins/lz-tdd/skills/lz-red/references/naming.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
  - plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md
autonomous: true
requirements: [DEL-1, DEL-2, DEL-3, DEL-4, DEL-5, DEL-6, DEL-7]

must_haves:
  truths:
    - DEL-1 -- the taxonomy reference is reachable in one hop from all three SKILL.md files and is byte-identical in all three trees
    - DEL-2 -- the document's headline finding is that bare `stub` is unusable because owned sources assign it to opposite cells
    - DEL-2 -- authority is stated as PER CELL near the top; Meszaros is the reference frame for the collaborator-side term set only and is explicitly not the spine, not the production-side authority, not the valid-red authority, and not the authority on Beck's usage
    - DEL-2 -- Beck's production-side `stub` is presented as the OLDER sense of two legitimate vintages, never as a deviation from a standard that did not exist when he wrote
    - DEL-2 -- where two owned sources genuinely conflict, both are presented and the disagreement is named as inherited rather than silently resolved
    - DEL-3 -- the SUT-side transitional artifact has a named term the document openly declares coined, with the checked-and-rejected candidates listed
    - DEL-4 -- every listed caveat is present and no caveat overstates what the sources support
    - DEL-5 -- the four confirmed provenance defects are corrected at their real, content-located sites
    - DEL-6 -- the deterministic battery gates the new file's content, the three-copy byte-identity invariant, and the new red criterion
    - DEL-7 -- the red criterion is stated once, consistently, in every place it is restated; Law 2 and the hand-forward bar are disambiguated rather than one being deleted
    - DEL-7 -- no shipped statement about why a test is red remains factually false on the pinned toolchain
    - DEL-7 -- the divergence between the doctrine and the project's own stricter eval gate is recorded where a reader will find it, and the gate itself is untouched
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
    - plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
    - plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs (FILES entries, byte-identity gate, criterion guards)
  key_links:
    - each SKILL.md -> its OWN references/test-double-taxonomy.md (never a cross-skill ../ path)
    - principle-backing.md -> test-double-taxonomy.md (new backing row)
    - SKILL.md step 2 (Law 2 sizing) <-> SKILL.md step 5 (the hand-forward bar) -- disambiguated, mutually consistent
    - check-red-references.mjs [j9m] gates -> every surface the criterion is restated on
---

<objective>
One change, seven deliverables: author the cross-author test-double taxonomy reference and place a
byte-identical copy in each of the three lz-tdd skills; fix four confirmed provenance defects; and
land the owner-approved red-criterion doctrine change across every surface that restates it.

Purpose: three defects share one root -- the skill asserts provenance and doctrine it cannot support.
`stub` is assigned to opposite cells by owned sources, so every coach answer using the bare word is
ambiguous. Four backing rows claim tiers their sources do not carry. And the red criterion is stated
two contradictory ways seventeen lines apart in the same file, both backed to the same source at the
same tier.

Output: one new reference (three byte-identical copies), three SKILL.md reference lines, the
disambiguated red criterion across six lz-red surfaces, corrected provenance for all of it, and new
checker gates for every claim above.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@plugins/lz-tdd/skills/lz-red/SKILL.md
@plugins/lz-tdd/skills/lz-red/references/principle-backing.md
@plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
@plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
@plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
@plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md
@plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md
@.claude/skills/lz-red-workspace/tools/check-red-references.mjs
@.claude/skills/lz-red-workspace/tools/lib/scaffold-phrases.mjs
</context>

<research_already_supplied>
ALL research for this change is already done (roughly 20 oracle consultations across 11 authors, plus
measured toolchain behaviour on the pinned versions). Do NOT spawn a researcher. Do NOT read
`.oracle/` -- it is git-ignored copyrighted source and only dedicated oracle agents may open it. Every
fact this plan needs is stated inline below. If a fact you want is not stated here, leave it out
rather than inventing or re-deriving it. Where a source attribution cannot be confirmed from what is
stated here, tag the row no-oracle rather than guess an owned tier.
</research_already_supplied>

<scope_note>
This plan carries a scope extension: Deliverable 7 (the red-criterion doctrine change) was originally
excluded and has since been owner-approved. It stays in THIS plan, not a later one, because provenance
defect 2 IS the criterion's backing row -- the retag depends on what the criterion says, so splitting
them would edit the same lines twice.

Four tasks, above the usual 2-3, because the deliverable count doubled after the plan was scoped.
Task 3 (doctrine) must run BEFORE task 4 (provenance) for the reason above.
</scope_note>

<house_rules_that_bite_here>
Verified against the live tree while planning. Each of these will fail the battery if ignored.

1. **No fenced code blocks in the new taxonomy document, at all.** `extract-samples.mjs` typechecks
   every non-ignore TypeScript fence under `tsc --strict`; the taxonomy document must not enter that
   gate. Its checker entry sets `requireFence: false`. Prose and pipe tables only. The lz-red tree
   currently carries exactly 8 fences and `extract-samples.mjs` reports 8 modules -- that count must
   still be 8 at the end, which is the mechanical proof no fence was added or lost.
2. **No relative markdown links to sibling files inside the taxonomy document.** The three copies must
   be byte-identical, and the three skills have DIFFERENT sibling files (`testing-stance/` exists only
   under lz-red). Intra-file `#anchor` links for the table of contents are fine; anything containing a
   `/` is not. Name other documents in prose without linking them.
3. **The scaffold-phrase guard is a hard gate on every registered file.** `SCAFFOLD_RES` is
   `[/\bTODO\b/, /once it exists/i, /to be authored/i, /\bplaceholder\b/i, /\bTBD\b/]`. The live trap
   in ordinary prose here is `once it exists`, which is the natural phrasing for both the taxonomy's
   coined term and the red criterion -- phrase around it. Note that `/\bplaceholder\b/i` matches the
   SINGULAR word only; the existing plural in `vitest-typescript-mechanics.md` passes precisely
   because `placeholders` breaks the trailing word boundary. The taxonomy must name a registered
   Meszaros alias whose singular form collides; task 1 handles that with a narrow documented per-entry
   exemption, NOT by weakening the shared phrase list.
4. **`check-red-references.mjs` topic regexes are CASE-SENSITIVE where written that way.** The two
   live ones are `/right reason|AssertionError/` and `/F\.I\.R\.S\.T\./`. Both `right reason` and
   `AssertionError` must survive, at that exact casing, in `SKILL.md` and in
   `vitest-typescript-mechanics.md`. Renaming step 5 or those headings turns the checker red.
5. **`principle-backing.md` rows are parsed as EXACTLY three pipe columns** by
   `lib/provenance-honesty.mjs` (`| [Label](path.md) | Source | Access tier |`). A row whose tier cell
   starts with `Owned` while its Source cell cites Beck's Test-Driven Development by Example is a hard
   D-05 violation. Keep every new or edited row in that exact shape.
6. **`beck-tdd-by-example.md` is gated by `check-backing.mjs`**: its six topic tokens
   (`red-green-refactor`, `two rules`, `fake it`, `triangulate`, `obvious implementation`, `lz-tpp`)
   and its visible `/no-oracle/i` tag must all survive the edit, and the scaffold guard applies.
7. **ASCII only, everywhere.** No emoji, no Unicode dashes, no curly quotes. `check-hygiene.mjs`
   scans all three skill trees on this axis.
8. **Public-repo email hygiene.** The only email-shaped token permitted in authored content is the
   approved public contact. Verify by allowlist-inversion: assert the only email-shaped token present
   is the approved one. Never write a forbidden value anywhere, including as a search needle.
9. **`check-hygiene.mjs`'s no-verbatim axis trips on a double-quoted run of 120+ characters.**
   Backticked term names are unaffected. Do not quote long passages.
10. **Use `rg`, never `grep`, and never `git grep` under `.claude/skills/`** -- that tree is
    git-ignored, so `git grep` returns zero matches there with no error and no warning.
11. **No edits outside `plugins/lz-tdd/` and the one workspace checker.** In particular the RED eval
    gate is NOT to be touched (see DEL-7f).
</house_rules_that_bite_here>

<tasks>

<task type="auto">
  <name>Task 1: Extend the checker first, and assert the RED baseline (DEL-6)</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <action>
Extend `check-red-references.mjs` IN PLACE -- no sibling checker, matching the instrument-first idiom
the file's own header comment describes. The gate is currently GREEN, so every FAIL after this task is
a new gate by construction.

**Prefix the label of EVERY gate added in this task with `[j9m] `.** That makes the whole RED baseline
and each later task's progress mechanically separable from the eleven pre-existing surfaces, which is
what the verify commands below key on. Do not add the prefix to any pre-existing label.

(a) Support MULTIPLE absent guards per entry: let `absent` accept either a single object (as today) or
an array of them, and iterate. Purely additive; every existing single-object `absent` keeps working.

(b) Add a twelfth `FILES` entry for `test-double-taxonomy.md`, `requireFence: false` (house rule 1),
with these topics -- write them exactly as listed so task 2's prose and this gate cannot drift apart:

- `[j9m] table of contents` -> `/table of contents/i`
- `[j9m] lifetime axis` -> `/lifetime/i`
- `[j9m] bare-stub collision headline` -> `/collision/i`
- `[j9m] defines-vs-uses column` -> `/\bdefines\b/i`
- `[j9m] coinage declared openly` -> `/\bcoined\b/i`
- `[j9m] Self Shunt disclosure model` -> `/self shunt/i`
- `[j9m] Saboteur polarity caveat` -> `/saboteur/i`
- `[j9m] Overspecified Software citation` -> `/overspecified software/i`
- `[j9m] Cooper false-friend caveat` -> `/false friend/i`
- `[j9m] degraded-scan confidence caveat` -> `/degraded scan/i`
- `[j9m] unaudited Beck-Fake mapping caveat` -> `/unaudited/i`
- `[j9m] appendices not exhaustive` -> `/not exhaustive/i`
- `[j9m] authority is per cell` -> `/per cell|per-cell/i`
- `[j9m] Meszaros scoped as a reference frame, not the spine` -> `/reference frame/i`
- `[j9m] two senses of different vintage` -> `/vintage/i`
- `[j9m] inherited disagreement named` -> `/inherited/i`
- `[j9m] no declared precedence for the TDD content sources` -> `/no declared precedence/i`

(c) Add an OPTIONAL per-entry `scaffoldExempt` array and honor it in the existing scaffold check --
filter `SCAFFOLD_RES` by the entry's exemptions before searching. Set it on the taxonomy entry ONLY,
to `[/\bplaceholder\b/i]`, with a comment recording WHY: that pattern is a registered alias in the
source taxonomy this document maps, so for this one file it is a domain term, not a draft marker.
This mirrors the precedent already documented in `lib/scaffold-phrases.mjs` (uppercase TODO only, so a
`todos` domain example never false-fails). Do NOT edit `lib/scaffold-phrases.mjs` -- it is shared with
the lz-refactor battery, and the exemption must not reach any other file.

(d) Add a post-loop byte-identity block (same standalone idiom as the existing D-05 honesty gate and
SEAM-02 block), labelled `[j9m] test-double-taxonomy.md byte-identical across all three skills`.
Resolve `references/test-double-taxonomy.md` under each of `lz-red`, `lz-tpp` and `lz-refactor`; FAIL
loud and by name if any copy is missing; otherwise `sha256` each file's bytes via `node:crypto` and
PASS only when all three digests are equal, reporting the short digest. This invariant is what makes
the duplication safe, so it is not optional.

(e) Add the DEL-7 criterion gates. Positive topics:

- on `SKILL.md`: `[j9m] valid but blunter red` -> `/blunter/i`; `[j9m] prerequisite to clear` ->
  `/prerequisite/i`; `[j9m] characterization carve-out` -> `/characterization/i`
- on `three-laws-and-test-selection.md`: `[j9m] Law 2 sizes the test, not the hand-forward bar` ->
  `/hand forward|hand it forward|sizes the test/i`
- on `vitest-typescript-mechanics.md`: `[j9m] collection vs act-line failure` -> `/collection/i`;
  `[j9m] not-implemented throw is a valid red` -> `/not-implemented|not implemented/i`;
  `[j9m] instrument divergence recorded` -> `/stricter than/i`
- on `anti-patterns.md`: `[j9m] false-green carve-out names characterization` -> `/characterization/i`
- on `principle-backing.md`: `[j9m] test-double taxonomy backing row` ->
  `/test-double-taxonomy\.md/`; `[j9m] failure-vs-error boundary row` -> `/failure|error boundary/i`

(f) Add the DEL-7 negative sweep as absent guards. These encode the contradiction sweep mechanically,
so no ad-hoc grep is needed in later tasks. Each FAILs while the false or superseded text is still
present:

<!-- planner-discipline-allow: never reached its assertion -->
<!-- planner-discipline-allow: broken harness -->
<!-- planner-discipline-allow: in a later phase -->

- on `vitest-typescript-mechanics.md`: `[j9m] no false never-reached-the-assertion claim` ->
  `/never reached its assertion/i`; `[j9m] no bare broken-harness verdict on an unresolved name` ->
  `/broken harness/i`
- on `anti-patterns.md`: `[j9m] no stale later-phase marker` -> `/in a later phase/i`

Update the file's header comment to say twelve entries and to describe the byte-identity gate, the
scaffold exemption, the array-valued `absent` support, and the `[j9m]` label convention. Do not
weaken, delete, or loosen any pre-existing topic, flag, or gate.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1 &amp;&amp; node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg '\[FAIL\]' | rg -v '\[j9m\]' | rg -c . ; test $? -eq 1</automated>
  </verify>
  <done>
The gate exits 1 (RED baseline by design), and EVERY FAIL line carries the `[j9m]` prefix. Zero FAIL
lines come from the eleven pre-existing surfaces, the D-05 honesty gate, or the SEAM-02 block --
proving nothing pre-existing regressed and the whole RED baseline is the new work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the taxonomy reference, place three byte-identical copies, link from all three SKILL.md files (DEL-1, DEL-2, DEL-3, DEL-4)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md, plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md, plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md, plugins/lz-tdd/skills/lz-red/SKILL.md, plugins/lz-tdd/skills/lz-tpp/SKILL.md, plugins/lz-tdd/skills/lz-refactor/SKILL.md</files>
  <action>
Write the document ONCE to `plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md`, then
copy it byte-for-byte to the other two `references/` directories with `cp` (never re-type it, never
hand-edit a copy). No plugin-root shared directory, no symlinks, no cross-skill `../` links: the
placement is already decided -- bundled files are scoped within the skill directory, and the upstream
skills repository does exactly this (three byte-identical copies, zero symlinks). Duplication costs
zero extra context because only the invoked skill's copy is ever read.

Follow the existing reference house shape: `# Title`, a `Scope:` paragraph, a blockquote provenance
note, content sections, `## Sources` last. A table of contents is required at the top because the
document will exceed 100 lines.

SECTION 1 -- the two axes. Organise everything by (i) what the artifact stands in for: the SUT's own
unwritten implementation, versus a collaborator of the SUT; and (ii) lifetime: transitional (destined
to be replaced) versus permanent (a fixture of the suite).

SECTION 2 -- the AUTHORITY RULE. A short section near the top the reader cannot miss. This is a
GOVERNING rule, not a caveat: Meszaros' material is the largest and most systematic block in the
matrix, so without this section it reads as the spine. It must not be the spine. State plainly:
**authority is PER CELL; no single author arbitrates the whole taxonomy.** Then the four scopes:

- Meszaros IS the reference frame for the COLLABORATOR-SIDE term set, and only that. He coined the
  umbrella and the five kinds; Fowler credits him rather than claiming them; 99 Bottles credits him
  with standardising the scheme. So for what separates a Test Spy from a Mock Object, cite MESZAROS --
  never "Fowler's taxonomy", which is the common error. Use the phrase "reference frame".
- He is NOT the authority for the PRODUCTION side. He does not name that artifact, and he has spent
  both obvious words: the singular alias and bare `Stub` are BOTH registered aliases of `Dummy
  Object`. On the one cell that needed a name he is an obstacle rather than an authority. Say so.
- He is NOT the authority on what counts as a valid red. His outcome vocabulary has no slot for a test
  that never ran -- all three of his outcome definitions begin from the test being run, so a compile
  failure falls outside his scheme entirely. He IS citable for the narrower point that an error is a
  legitimate, equally severe red that should not be converted into a failure.
- He is NOT the authority on Beck's usage -- see the UNAUDITED caveat in section 6; do not use that
  mapping to interpret Beck.

Then the vintage framing, which must not come out backwards, because getting it backwards would
misrepresent this skill's own primary source. Beck's production-side `stub` is the OLDER sense:
`method stub` descends from remote-procedure-call stubs and predates the doubles taxonomy by roughly
two decades. The collision is an artifact of the LATER literature adopting `stub` for the test side.
So do NOT present Beck's usage as a deviation from a standard -- that standard did not exist when he
wrote, and lz-red's own step-2 mechanics rest on him. Present two legitimate senses of different
vintage, with the newer one having taken the word. Use the word "vintage".

Then one lineage line: lz-red's spine is Martin's Three Laws, its selection moves are Beck's, and its
testing stances are Metz, Bernhardt and Feathers. Meszaros is a testing-patterns author and is not in
that lineage; he is imported for vocabulary precision, not for TDD-cycle doctrine.

Then one line on precedence: this project's declared source-authority precedence covers
plugin-authoring sources only. There is NO declared precedence for the TDD content sources, which is
exactly why authority here has to be per cell. Use the phrase "no declared precedence".

SECTION 3 -- the headline finding, early, named as the reason the document exists: bare `stub` is
unusable. Owned, oracle-verified sources assign it to OPPOSITE cells. Production-side: Beck, across
four files; and Clean Code Ch. 7, which is guest-authored by Michael Feathers. Collaborator-side:
Metz, 99 Bottles, Clean Code Ch. 17 and Ch. 10, Cooper, Bernhardt, Fowler's web articles, and
Meszaros. State plainly that a coach must never use bare `stub` unqualified. Use the word "collision".

SECTION 4 -- the per-author table. Columns: Author, Term, Side, Lifetime, Defining property (in this
document's own words), Defines or uses, Source, Citability tier. The "Defines or uses" column
distinguishes a source that DEFINES a term from one that merely USES it -- write the header so the
word `defines` appears.

Two table-construction rules follow directly from the section-2 authority rule:

- EVERY row carries its OWN citability tier. Never let one author's tier propagate to a neighbouring
  row, and never let a block of rows inherit a tier from the block heading.
- Where two owned sources genuinely CONFLICT, present BOTH and name the disagreement as inherited
  rather than silently picking a winner. The two live conflicts are Clean Code versus Beck on whether a
  build failure is a valid red, and Beck versus Meszaros on which side `stub` names. Say plainly that
  this skill's own step-2 versus step-5 contradiction was a faithful transcription of the first of
  those -- which reframes that defect as inherited rather than careless. Use the word "inherited".

Rows, exactly as established:

- Beck: `stub` and `pass-through interface` are PRODUCTION-side. `impostor` and `mocking` are his
  test-side substitute words. His `fixture` (two senses) and `test bed` are test-side but are NOT
  substitution terms -- do not give them doubles rows; mention them in prose as excluded if at all.
- Metz (talks): `stub` is TEST-side. Her production-side words are `shim`, `empty method`,
  `do-nothing method`.
- 99 Bottles (Metz and Owen): the only DEFINED test-side term is `Fake`. Production side is
  `empty class`, `empty method`, `empty subclass`. Note that `shim` there means a temporary
  defaultable ARGUMENT, not an empty definition -- so it does not mean what Metz's talks mean by it.
- Bernhardt: cite for double-versus-value ONLY. He never states the mock-versus-stub split; his
  diagram supplies only a count. Do not cite him for mock-versus-stub.
- Fowler (web articles): the five kinds, the umbrella term, and `SUT` are MESZAROS' terms -- cite them
  as "Meszaros, via Fowler". Fowler's OWN contributions are state-versus-behavior verification and the
  classic/mockist naming.
- Fowler (Refactoring 2e): ZERO doubles vocabulary. Keep the book and the web articles in SEPARATE
  rows or columns, and warn explicitly that readers assume the taxonomy is in the book when it is not.
  The book does supply `failure` versus `error`.
- Cooper: LOOSE, not citable as a taxonomy. Flag that his "classical" means pre-TDD classical
  automated testing and is a FALSE FRIEND for Fowler's "classicist" -- mapping him onto that axis
  INVERTS him. This section must contain the phrase "false friend".
- Kerievsky: production side only. `skeleton` is his transitional term, and Ch. 11 explicitly calls
  such an artifact a temporary step on the way to somewhere else.
- Meszaros: the umbrella plus five kinds -- but really four-plus-one. `Dummy Object` is filed in
  Ch. 27 Value Patterns, and he says almost nothing about the other doubles applies to it.
  `Responder`, `Saboteur` and `Temporary Test Stub` are Variations of Test Stub. Both the singular
  alias word and bare `Stub` are registered aliases of `Dummy Object` -- name them.

SECTION 5 -- the coined term (DEL-3). No author in eleven names the SUT-side transitional artifact: a
real production symbol, correct signature, no implementation yet, written so a test can run, destined
to become the real implementation. Coin `signature skeleton` for it, grounded on Kerievsky's owned
`skeleton` (the only owned candidate) and disambiguated from it. Say plainly, in the document's own
voice, that the term is COINED here and is not any author's. Model that openness on Meszaros' own
`Self Shunt` naming self-criticism, and name the model. List what was checked and why each failed:

- the singular alias word -- a registered Meszaros alias of `Dummy Object`, which is
  collaborator-side, so reusing it inverts the side axis
- bare `stub` -- the collision this document exists to document
- `shim` -- Metz production-side, but 99 Bottles uses it for a temporary defaultable argument
- `empty method` / `do-nothing method` -- denote a body that STAYS empty, not one destined to be filled
- `pass-through interface` -- Beck, production-side, but denotes delegation, not absence
- `Walking Skeleton` -- a whole-system end-to-end scaffold, not a single symbol
- `impostor` -- Beck's TEST-side word
- bare `skeleton` -- Kerievsky's, owned and closest, but his denotes a transitional class-extraction
  step, so bare reuse would overload it

SECTION 6 -- the caveats (DEL-4). Every one of these, at the strength given, no stronger. The
Meszaros internal edges belong here because each one changes a citation:

- `Saboteur` polarity is PASS-when-handled: the injected failure is STIMULUS, not verdict. Cite
  Ch. 23, NOT Ch. 11 -- the two chapters draw the Responder/Saboteur line in different places.
- "The SUT can never be its own double" is NOT citable. Ch. 11 states you may make the SUT its own
  `Subclassed Test Double`, and Ch. 23 works the example. Cite only the narrow rule: never double the
  part under verification.
- Meszaros is NET PRO-SUBSTITUTION with one narrow warning. Do NOT present him as an anti-mocking
  authority.
- The correct anti-over-mocking citation is `Overspecified Software` -- a CAUSE of the `Fragile Test`
  smell, and itself a form of `Behavior Sensitivity` -- with the actual argument in Ch. 5,
  `Use the Front Door First`. `Behavior Sensitivity` alone carries NO anti-substitution content.
- Meszaros' mapping of his `Test Stub` onto Beck's `Fake` is UNAUDITED: no page, no explanatory note,
  and no discussion of Beck's `Fake It` anywhere in the volume. Use the word "unaudited".
- `Fragile Fixture` is typed THREE different ways across Ch. 2, Ch. 16 and Appendix F.
- Appendix F omits `Test Hook`; Appendix G omits two of `Self Shunt`'s aliases. Both registries are
  non-exhaustive -- say so with the phrase "not exhaustive", and do not treat either as a closed list.
- The cross-reference tables are a degraded scan, so per-column assignments other than the Beck cell
  are consistent-with rather than confirmed. Use the phrase "degraded scan".

SECTION 7 -- `## Sources`, naming each author and work with its access tier, in the house style the
sibling references already use. Per the section-2 rule, a source's tier here does not license a tier
for any row that cites it.

Then add exactly ONE reference line to each of the three SKILL.md files, pointing at THAT skill's OWN
copy with a short when-to-read gloss. It must be linked from SKILL.md DIRECTLY -- a reference reached
only from another reference gets partially read via head-style previews. Match each file's style:
`lz-red/SKILL.md` and `lz-tpp/SKILL.md` both have a `## Reference material` bullet list (append a
bullet); `lz-refactor/SKILL.md` uses short `## Topic` pointer sections (add one after "Refactoring
safely without tests (Feathers)", matching its neighbours' two-line shape). Gloss from each skill's
own vantage: lz-red, choosing what to substitute when writing the failing test; lz-tpp, what to call an
unimplemented production symbol while picking the transformation; lz-refactor, which artifacts are
transitional when restructuring. Change nothing else in any SKILL.md in this task.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg 'taxonomy|byte-identical' | rg -c 'FAIL' ; test $? -eq 1 &amp;&amp; node .claude/skills/lz-red-workspace/extract-samples.mjs | rg -q '8 module' &amp;&amp; node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs</automated>
  </verify>
  <done>
Zero FAIL lines mention the taxonomy or the byte-identity gate: every taxonomy topic PASSes and all
three copies hash identically. `extract-samples.mjs` still reports 8 modules and exits 0, proving the
new document introduced no TypeScript fence. `check-crossrefs.mjs` exits 0, proving the new
`lz-refactor/SKILL.md` pointer resolves. The remaining `[j9m]` FAILs are the DEL-7 and provenance
gates, still RED by design. No copy contains a markdown link with a `/` in it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Land the red-criterion doctrine change on every surface that restates it (DEL-7a, 7b, 7c, 7d, 7f, 7g)</name>
  <files>plugins/lz-tdd/skills/lz-red/SKILL.md, plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md, plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md, plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md, plugins/lz-tdd/skills/lz-red/references/anti-patterns.md, plugins/lz-tdd/skills/lz-red/references/naming.md, plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md, plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md</files>
  <action>
The defect: `SKILL.md` step 2 blesses stopping at a not-yet-defined symbol; step 5 forbids exactly that
by demanding an AssertionError. Both are asserted seventeen lines apart and both are currently backed
to the same source at the same tier. The fix DISAMBIGUATES; it deletes neither side.

**LOCATE EVERY SITE BY CONTENT, NEVER BY LINE NUMBER.** All line references below are approximate; a
prior spec in this repo failed review because its citations had drifted onto a reverted draft. `rg` the
quoted anchor, confirm the site, then edit.

**Sweep before you edit.** The criterion is restated in more than the two obvious places. Confirmed
sites and their anchors:

- `SKILL.md`, step 2, the clause `a reference to a not-yet-defined symbol counts as that failure`
- `SKILL.md`, step 5, the sentence `The fresh test must fail on its assertion`
- `SKILL.md`, the worked-example prose paragraph and the in-fence comment, both containing
  `red for the right reason`
- `vitest-typescript-mechanics.md`, the section `## Read the red bar: fail for the right reason`
  (both its Mechanic bullet and its Distilled note)
- `vitest-typescript-mechanics.md`, the section `## Fail-for-the-right-reason as a procedure`
  (all three paragraphs, including the closing coach paragraph)
- `three-laws-and-test-selection.md`, the small-step row containing
  `only to the first failure, then stop`
- `three-laws-and-test-selection.md`, the Law 2 bullet containing
  `code that does not compile counts as the red bar`
- `test-structure-and-assertions.md`, the `## F.I.R.S.T.` section and the blockquote line describing
  F.I.R.S.T. as the baseline the fail-for-the-right-reason step sits on -- edit ONLY if it contradicts
  the new criterion; if it is merely consistent, leave it byte-unchanged
- `anti-patterns.md`, anti-pattern 4 (`### 4. A test that passes immediately (no red)`), whose
  recognizer refers to production code not existing yet

7a -- disambiguate step 2, keep it, scope it. Law 2's clause is TRUE about Law 2, which governs when
to stop WRITING the test, and it is owned and oracle-verified to Clean Code. Do NOT delete it. Add
that this is not yet the bar you hand forward: Law 2 sizes the test; the red-bar step decides what you
hand to the green step. Two different questions that currently share the word "failure". Mirror the
same disambiguation into the `three-laws-and-test-selection.md` small-step row and Law 2 bullet.

7b -- restate step 5 so it is true in every stance the skill supports. The criterion becomes:

- the red you hand forward must have RUN and failed
- the failure must originate in the code under test -- not in module load, test collection, or setup
- an assertion failure is the SHARPEST form, because it names both expected and received
- a not-implemented throw, or an error raised inside the test body at the point of use, is a VALID but
  blunter red -- it is not a broken harness
- a build, import, module-resolution or collection failure is a prerequisite to CLEAR, not the red you
  hand forward
- a test that passes on its first run while driving new behaviour is a false green -- BUT the
  characterization test is carved out: it is green by construction and is a pin, not a driver

The characterization carve-out is REQUIRED. Without it the skill's own legacy stance is condemned by
its own gate. Keep the literals `right reason` and `AssertionError` at their exact casing (house rule
4). Restate the same criterion, consistently, in `vitest-typescript-mechanics.md`'s two sections and
in `anti-patterns.md` anti-pattern 4 (whose recognizer must acknowledge the carve-out).

7c -- correct a statement that is factually FALSE. `vitest-typescript-mechanics.md` claims a
ReferenceError, TypeError or import failure means the test never reached its assertion. MEASURED FALSE
on this project's pinned toolchain (vitest 4.1.10, tsc 6.0.3): a missing named export throws
`TypeError: x is not a function` AT THE ACT LINE, and a bare undeclared identifier throws
`ReferenceError` AT THE ACT LINE -- the body RAN in both cases. Only a missing MODULE fails at
collection with zero tests run. Fix the explanation; do not preserve the false clause, and do not
preserve any claim that the body did not run as the explanation of an unresolved name. Reserve the
prerequisite-to-clear category for collection and module-resolution failures, which is where it is
true. The `## Read the red bar` Distilled note's claim about a typo or missing import needs the same
correction.

7d -- fix the false-green examples. Six of the shipped examples pair a red-step test with a COMPLETE
CORRECT implementation, so the test passes on first run -- the false green the skill forbids as
anti-pattern 4; two were verified by execution to grade as false greens against the project's own
instrument. Required work:

- FIX the worst site: `three-laws-and-test-selection.md`, whose prose says the starter test is written
  before the code exists and then ships a full `reduce` implementation (the function `sumOf` returning
  `values.reduce(...)`). Replace that body with the not-yet-implemented form so the test genuinely
  fails at the act line, and align the surrounding prose. The fence must stay `tsc --strict` clean.
- ANNOTATE the remaining fences whose surrounding prose frames them as red-step examples: one line
  each, stating that the body is shown for context and that at red time it is the not-yet-implemented
  form. Do not rewrite them.
- LEAVE the `SKILL.md` example alone. It already ships a wrong-value stub that returns the total
  untouched, so it is a genuine red already. Judge every other fence by its own prose, not by a count.
- The lz-red tree must still hold exactly 8 fences and `extract-samples.mjs` must still report 8
  modules and exit 0. Avoid the scaffold phrases (house rule 3) in every annotation.

7f -- record the instrument divergence honestly, in `vitest-typescript-mechanics.md`, next to the
fail-for-the-right-reason procedure. This project's own RED eval gate implements the assertion-only
form, which is STRICTER than the doctrine now stated and stricter than every owned source. State that
plainly, using the phrase "stricter than", and say it is deliberate eval design rather than doctrinal
fidelity. DO NOT change the gate: a prior gate change was falsified against the negative controls, and
changing it would invalidate recorded results. Describe the gate by role; do not leak dev-tooling
paths into a shipped document. A skill that silently diverges from its own instrument is the defect
being fixed; a skill that documents the divergence is honest.

7g -- one line, in `SKILL.md` near the forward handoff. Because a not-implemented throw is now a valid
red, lz-red no longer needs to author a wrong-value stub in order to reach a valid red, which
substantially reduces the pressure on the lz-red/lz-tpp seam. Do NOT resolve the stub-authorship
question -- it stays open -- but note that the criterion change shrinks it. Exactly one line.

Remove the stale marker in `anti-patterns.md` saying the fail-for-the-right-reason procedure lands
with the coach spine in a later phase -- that is now wrong.

Do not touch `principle-backing.md` in this task; its rows are task 4's, and they depend on the
criterion text you settle here.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg '\[FAIL\]' | rg -v 'principle-backing' | rg -c . ; test $? -eq 1 &amp;&amp; node .claude/skills/lz-red-workspace/extract-samples.mjs | rg -q '8 module' &amp;&amp; node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>
Every `[j9m]` gate PASSes except the `principle-backing.md` row gates (task 4's work). The three
absent guards PASS, proving the false never-reached-the-assertion claim, the bare broken-harness
verdict, and the stale later-phase marker are all gone. The case-sensitive `right reason` and
`AssertionError` topics still PASS. `extract-samples.mjs` reports 8 modules and exits 0, so the
example fixes stayed `tsc --strict` clean and no fence was added or dropped. `check-hygiene.mjs` exits
0. Step 2's owned Law 2 clause is still present -- scoped, not deleted -- and the characterization
carve-out is stated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Correct all provenance, retag the criterion's backing, and take the phase gate GREEN (DEL-5, DEL-7e, DEL-6)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/principle-backing.md, plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md, plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md, plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md</files>
  <action>
**LOCATE EVERY SITE BY CONTENT, NEVER BY LINE NUMBER.** Anchors are given below; `rg` each one,
confirm the site, then edit.

Fix (1) -- the false "no owned Feathers source" claim, at TWO sites. Anchors: in
`principle-backing.md`, the row whose Recommendation cell is `[Seams and characterization]`; in
`testing-stance/seams-and-legacy.md`, the blockquote line containing `no owned Feathers source to
verify against`. The blanket claim is FALSE -- Clean Code Ch. 7 is guest-authored by Michael Feathers
and IS in the owned store. Correct it honestly and precisely: an owned Feathers surface EXISTS
(Clean Code Ch. 7), but it does not cover seams or characterization tests, so these recommendations
remain high-confidence core only -- no-oracle by lack of COVERAGE, not by absence of an owned author.
Do not upgrade the recommendation to oracle-verified; it is not verifiable. Keep the pipe row at
exactly three columns and keep a `/no-oracle/i` token visible in both files.

Fix (2) + DEL-7e -- retag the criterion's backing to match the criterion task 3 just settled. Anchor:
the `principle-backing.md` row whose Recommendation cell is `[Fail for the right reason]`, currently
tagged owned and oracle-verified against Clean Code Ch. 9. That chapter explicitly makes a build
failure a legitimate red and NOWHERE requires an assertion failure (checked absence across eight
chapters), so it cannot back the criterion -- it contradicts it. Retag as:

- the failure-versus-error boundary (an assertion mismatch versus an exception raised in an earlier
  phase) -- Fowler, Refactoring 2e Ch. 4; owned
- "clear the compile error, then run and fail" -- Beck's kanban-cycle step; owned. Cite it by the same
  owned Beck work name the existing owned Beck rows use. If you cannot confirm from this plan which
  owned Beck surface carries the kanban cycle, tag that row no-oracle rather than guess -- a wrong
  owned tag is the exact defect being fixed here.
- the Vitest-specific mapping -- no-oracle; this project's own work

Split into as many rows as honesty needs. Then record, in the same section, that FOUR owned sources
independently decline to require an AssertionError specifically: Clean Code (a build failure is
explicitly a legitimate red), Beck (run and fail; his own canonical stub throws), Fowler (the boundary
is an earlier phase), and Meszaros (an error is a legitimate, equally severe red that you are told NOT
to convert into a failure). Frame it the way the taxonomy's authority rule does: Clean Code and Beck
genuinely DISAGREE on whether a build failure is a valid red, the skill's step-2 versus step-5
contradiction was a faithful transcription of that disagreement, and the fix names the disagreement
rather than silently picking a winner. Never let a tier cell start with the owned marker while its
Source cell cites Beck's Test-Driven Development by Example (house rule 5).

Fix (3) -- the misattributed green-bar discipline. Anchor: in
`lz-refactor/references/beck-tdd-by-example.md`, the sentence containing `structure only ever changes
while the bar is green`. It is attributed to Beck; it is citable to Fowler, Refactoring 2e Ch. 4, and
Beck's own kanban cycle sanctions a pre-green preparatory refactoring. Re-attribute to Fowler, note
the Beck kanban exception so the claim is not overstated in the other direction, and add the Fowler
citation to that file's `## Sources`. Preserve all six `check-backing.mjs` topic tokens and the file's
visible `/no-oracle/i` tag (house rule 6).

Fix (4) -- record the now-sourced `failure` versus `error` boundary in
`vitest-typescript-mechanics.md`, alongside the red-bar mechanics task 3 rewrote: a `failure` is an
assertion mismatch; an `error` is an exception raised in an earlier phase. Source: Fowler,
Refactoring 2e Ch. 4 (owned, oracle-verified). This is the same boundary the retagged criterion rests
on, so state it once and let both sections point at it.

Fix (5) -- add the `principle-backing.md` row for the new taxonomy reference, linking
`[Test-double taxonomy](test-double-taxonomy.md)`. Its tier cell must be honest about being
per-source: tiers vary by row inside the linked table, and the coined term is lz-red's own with no
source at all.

Then run the full deterministic battery plus the email allowlist-inversion scan over every changed
file. Fix any RED before declaring done; do not weaken a checker to reach GREEN.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs &amp;&amp; node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs &amp;&amp; node .claude/skills/lz-red-workspace/extract-samples.mjs &amp;&amp; node .claude/skills/lz-refactor-workspace/tools/check-backing.mjs &amp;&amp; node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs &amp;&amp; node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs &amp;&amp; claude plugin validate .</automated>
  </verify>
  <done>
Every command above exits 0. `check-red-references.mjs` reports GREEN across all twelve surfaces
including every `[j9m]` gate, the byte-identity gate, and the D-05 honesty gate. The email
allowlist-inversion scan over the changed files reports only the approved public contact, and no
other email-shaped token, with no forbidden value written anywhere as a needle. No checker was
weakened and no pre-existing topic removed. The RED eval gate is byte-unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content -> public git remote | Everything written here becomes public on push; maintainer PII cannot be recalled once pushed. |
| bundled reference -> agent context | These documents load verbatim into a coach's context, so a wrong provenance claim or a false mechanic is asserted to users as fact. |
| shipped doctrine -> recorded eval results | The eval gate encodes the old, stricter criterion; changing it would invalidate results already recorded against negative controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j9m-01 | Information Disclosure | every authored or edited markdown surface | high | mitigate | `check-hygiene.mjs` work-email axis over all three trees, plus the task 4 allowlist-inversion scan asserting the approved public contact is the only email-shaped token; no forbidden value written as a needle. |
| T-j9m-02 | Tampering | provenance claims in `principle-backing.md` | high | mitigate | `lib/provenance-honesty.mjs` D-05 gate blocks any row tagged owned while citing the summary-only book; task 4 retags rather than upgrades; where the owned surface cannot be confirmed the row is tagged no-oracle by instruction, not guessed. |
| T-j9m-03 | Repudiation | the three byte-identical copies silently diverging | medium | mitigate | Task 1's `sha256` byte-identity gate fails loud on divergence or a missing copy; task 2 copies with `cp` rather than re-typing. |
| T-j9m-04 | Tampering | a partial criterion fix leaving one surface contradicting the rest | high | mitigate | Task 1's `[j9m]` positive topics and three absent guards encode the whole sweep mechanically; task 3 cannot pass its verify while any swept surface still carries the superseded text. |
| T-j9m-05 | Denial of Service | the RED eval gate and its recorded results | medium | mitigate | House rule 11 and DEL-7f forbid touching the gate; the divergence is documented in the shipped reference instead, and task 4's done criterion asserts the gate is byte-unchanged. |
| T-j9m-06 | Tampering | source authenticity of the new taxonomy document | low | accept | No verbatim source prose or code (DST-04 house rule); written in its own words from research already completed, with `check-hygiene.mjs`'s no-verbatim axis as the mechanical backstop. |

No package-manager install tasks in this change, so no package legitimacy gate applies.
</threat_model>

<verification>
1. `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` exits 0 -- twelve surfaces
   GREEN, every `[j9m]` gate PASS, byte-identity PASS, D-05 honesty gate PASS, SEAM-02 PASS.
2. `node .claude/skills/lz-red-workspace/extract-samples.mjs` exits 0 and still reports 8 modules --
   the taxonomy added no fence and the example fixes added or dropped none.
3. `node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs` exits 0.
4. `node .claude/skills/lz-refactor-workspace/tools/check-backing.mjs` exits 0 -- the fix (3) edit
   preserved all six topics and the no-oracle tag.
5. `node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` exits 0 -- the new
   `lz-refactor/SKILL.md` pointer resolves.
6. `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 -- ASCII, email, and
   no-verbatim axes clean across the widened file set.
7. `claude plugin validate .` passes.
8. `git diff --stat` shows only the files in `files_modified`. The RED eval gate and
   `lib/scaffold-phrases.mjs` are absent from the diff.
9. Read `SKILL.md` steps 2 and 5 together: they no longer contradict, step 2's owned Law 2 clause
   survives, and step 5 names the four-tier red hierarchy plus the characterization carve-out.
</verification>

<success_criteria>
- The taxonomy reference exists in all three skills, byte-identical under `sha256`, reachable in one
  hop from each SKILL.md, with no fenced code block and no markdown link containing a `/`.
- Authority is stated as PER CELL in a short section near the top, with Meszaros scoped to the
  collaborator-side term set and explicitly denied spine, production-side, valid-red and
  Beck-interpretation authority; the vintage framing puts Beck's `stub` as the OLDER sense; the lineage
  and no-declared-precedence lines are present.
- The bare-`stub` collision is the document's stated headline finding, with the production-side and
  collaborator-side source lists as given.
- Every table row carries its own citability tier, and both live owned-source conflicts are presented
  with the disagreement named as inherited.
- `signature skeleton` is coined openly, with the eight checked-and-rejected candidates listed and the
  `Self Shunt` disclosure model named.
- All seven DEL-4 caveats are present at the stated strength, no stronger.
- The red criterion reads the same on all eight swept surfaces: run-and-failed, originating in the
  code under test, assertion sharpest, not-implemented throw valid but blunter, build and collection
  failures a prerequisite to clear, false green forbidden with the characterization carve-out.
- No shipped sentence still claims an unresolved name means the test body did not run.
- The instrument divergence is recorded in the shipped reference and the eval gate is byte-unchanged.
- All four provenance defects are corrected at content-located sites, the criterion's backing is
  retagged to Fowler plus Beck plus a no-oracle mapping, the four-owned-sources decline is recorded,
  and the taxonomy backing row exists.
- The full battery exits 0 with no checker weakened.
</success_criteria>

<output>
Create `.planning/quick/260728-j9m-test-double-taxonomy-reference-four-prov/260728-j9m-SUMMARY.md` when done.
Do NOT commit docs artifacts (PLAN.md, SUMMARY.md, STATE.md) -- the orchestrator handles that.
</output>
