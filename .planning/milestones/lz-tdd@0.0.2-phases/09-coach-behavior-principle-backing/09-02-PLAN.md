---
phase: 09-coach-behavior-principle-backing
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
autonomous: true
requirements: [CCH-01, CCH-02, CCH-03, CCH-04, CCH-05, CCH-06]

must_haves:
  truths:
    - "The '## Coach decision procedure (deferred to Phase 9)' placeholder section (SKILL.md :38-43) is replaced by a real '## Coach decision procedure' section: a compact numbered decision tree (a tree, NOT a table dump), and the placeholder-mode parenthetical at :27 ('The full coach decision procedure is deferred to Phase 9 -- see the placeholder below.') is removed"
    - "The procedure covers all six behaviors: classify/seam gate (CCH-05), smell recognition via smells.md then open-the-leaf (CCH-01), mechanical->Fowler / repeated-complex->Kerievsky routing (CCH-01), over/under-engineering de-patterning to Kerievsky Away OR the functional-catalog with the Replace-Pipeline-with-Loop reverse note (CCH-02, CCH-06), behavior-preservation with the Feathers no-tests fallback (CCH-03), and reference-mode routing (CCH-04)"
    - "The procedure adds NO new smell->refactoring table -- it references the existing smells.md + smell leaves as the routing source of truth (D-03), and reuses the router pointer sections already in SKILL.md; all coach cross-links are FILE-LEVEL (no #anchor) and point ONLY at already-existing files (the catalog READMEs, smells.md, principles.md, refactoring-without-tests.md)"
    - "SKILL.md stays < 500 lines (INLINE, no coach-procedure.md split -- D-02), stays ASCII-only and work-email-free (check-hygiene GREEN), and its outbound links all resolve (check-crossrefs GREEN, now that 09-01 added SKILL.md as a check-crossrefs source)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      provides: "The inline dual-mode coach decision procedure (CCH-01..06)"
      contains: "## Coach decision procedure"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      via: "coach step: recognize the smell in smells.md, then open the leaf"
      pattern: "smells.md"
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md"
      via: "coach step: de-patterning routes to the functional-catalog (CCH-02/06)"
      pattern: "functional-catalog"
  decisions: |
    - **D-01:** the coach decision procedure lives INLINE in SKILL.md, replacing the "Coach decision procedure (deferred to Phase 9)" placeholder; it is the skill's core auto-trigger behavior (not heavy catalog material), and heavy content stays in references/
    - **D-02:** SKILL.md stays < 500 lines (SKEL-02); the compact inline procedure lands the file near ~120-130 lines (measured 85 before authoring), so the D-02 split-to-coach-procedure.md fallback is NOT triggered -- inline only, no new reference file
    - **D-03:** the coach routing source-of-truth is the EXISTING smells.md + smell leaves -- NO new duplicate smell->refactoring table; the procedure adds only the routing dimension (mechanical smell -> Fowler; repeated/complex-structure smell -> Kerievsky; unwarranted pattern -> functional-catalog de-patterning; missing tests -> Feathers) and preserves the navigation-only-index / open-the-leaf design
    - **D-04:** the procedure opens with the classify gate already framed in SKILL.md's "Refactoring vs the green step (the lz-tpp seam)" section -- if a red test must be made to pass, that is lz-tpp (green step), not this skill; CCH-05 extends the existing two-hats framing in principles.md rather than re-deriving it
    - **D-05:** CCH-06 functional-alternative surfacing routes to the functional-catalog -- for a dissolvable pattern the coach names "pattern X disappears via FP idiom Y / TS feature Z", follows the one-line Functional alternative: cross-links that already exist on the OO leaves, and gives the Replace-Pipeline-with-Loop reverse guidance (clarity is the default; reverse to a loop only on a measured hot path or a named house-style reason)
---

<objective>
Replace the Phase-9 coach-decision-procedure placeholder in the shipped SKILL.md router with the
real inline procedure -- a compact numbered decision tree that wires CCH-01..06 on top of the
catalogs, smell taxonomy, and router pointers that already exist. This is the skill's core
auto-trigger behavior.

Purpose: the skill has shipped as a router with a placeholder where its coaching logic belongs.
This plan makes the coach actually route: classify the request against the lz-tpp seam, recognize
the smell, route mechanical smells to Fowler and repeated/complex-structure smells to Kerievsky,
apply the de-patterning balance (Kerievsky Away OR functional-catalog dissolution), preserve
behavior with the Feathers no-tests fallback, and answer reference-mode lookups.

Output: an edited SKILL.md with an inline "## Coach decision procedure" section, the removed
placeholder-mode parenthetical, no new smell table, file-level links to existing files only, and
the file comfortably under the < 500-line budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-coach-behavior-principle-backing/09-CONTEXT.md
@.planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md
@.planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md

The exact 6-step inline procedure skeleton is specified in 09-RESEARCH.md:128-154. The sibling
lz-tpp skill's coach-procedure shape to mirror is 09-PATTERNS.md's SKILL.md section.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the inline coach decision procedure in SKILL.md (CCH-01..06)</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (the FILE being edited -- current state: 85 lines; the placeholder section is :38-43, the placeholder-mode parenthetical is inside the "Two modes" coach bullet at :27, the classify gate is "## Refactoring vs the green step (the lz-tpp seam)" at :32-36, and the router pointer sections the procedure references are :45-85: Fowler catalog, Smell taxonomy, Refactoring principles, Kerievsky, Target pattern catalogs, Functional catalog, Refactoring safely without tests)
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md (the sibling TDD skill's "## Coach decision procedure" -- the numbered-tree shape to mirror: opens with a classify gate, closes with a "coach, don't drive" line)
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (the D-03 routing source of truth -- confirm the navigation-only-index / open-the-leaf design the procedure must reference, NOT duplicate)
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (the two-hats framing at :19-24 the seam step extends -- CCH-05, D-04)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (the exact 6-step skeleton to inline, :128-154; Open Question 2 -- keep coach links file-level, no #anchor)
    - .planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md (the SKILL.md coach-procedure section: what to delete at :27 and :38-43, what pointer sections to leave untouched)
  </read_first>
  <action>
    Edit plugins/lz-tdd/skills/lz-refactor/SKILL.md. Replace the placeholder section at :38-43
    ("## Coach decision procedure (deferred to Phase 9)" + its Placeholder-only body) with a real
    "## Coach decision procedure" section: a compact numbered decision tree (~30-40 lines), copying the
    6-step structure specified in 09-RESEARCH.md:128-154. The six steps: (1) classify the request against
    the lz-tpp seam -- a red/failing test to make pass is the green step, hand off to lz-tpp and stop; a
    green suite with a structure-only smell continues here (CCH-05, pointing at the existing "Refactoring
    vs the green step" section, not restating it); (2) recognize the smell by scanning smells.md
    recognize-by cues, then OPEN the matching smell leaf for candidate refactorings -- the index is
    navigation-only, never guess from it (CCH-01, D-03); (3) route by smell kind to a NAMED refactoring --
    mechanical smell -> a Fowler refactoring; repeated/complex-structure smell -> a Kerievsky pattern-directed
    refactoring, looking the target pattern up in the GoF / extra-patterns catalogs (CCH-01); (4) apply the
    over/under-engineering balance -- a pattern that earns its keep is applied/kept; an unwarranted pattern is
    refactored AWAY, either via a Kerievsky Away refactoring (Inline Singleton, Encapsulate Composite with
    Builder, Move Accumulation to Visitor) OR dissolved to a functional idiom via the functional-catalog
    ("pattern X disappears via idiom Y / TS feature Z"), with the Replace-Pipeline-with-Loop reverse note
    (clarity is the default; reverse to a loop only on a measured hot path or a named house-style reason)
    (CCH-02, CCH-06, D-05); (5) preserve behavior -- smallest steps that keep the code working, run tests
    after each, commit on green; if the target code has NO tests, route to the refactoring-without-tests
    (Feathers) reference to pin behavior with characterization tests first (CCH-03); (6) reference mode --
    for an explain/lookup request, route to the correct references/ doc and answer from it (CCH-04).

    Also remove the placeholder-mode parenthetical inside the "Two modes" coach bullet at :27
    ("(The full coach decision procedure is deferred to Phase 9 -- see the placeholder below.)") so the
    router no longer advertises a deferred placeholder. Do NOT add a new smell->refactoring table (D-03) and
    do NOT re-add the router pointer sections -- the procedure REFERENCES them (Fowler / smells / principles /
    Kerievsky / GoF+extra / functional / Feathers already have pointer sections at :45-85). Keep every coach
    cross-link FILE-LEVEL (no #anchor) and pointing ONLY at already-existing files (the catalog READMEs,
    references/smells.md, references/principles.md, references/refactoring-without-tests.md) -- do NOT link to
    the Beck files (they are reached via principles.md, wired in 09-04). ASCII-only; no work email.
  </action>
  <acceptance_criteria>
    - SKILL.md contains a "## Coach decision procedure" heading with NO "(deferred to Phase 9)" suffix and NO "Placeholder only" body
    - SKILL.md no longer contains the string "deferred to Phase 9" anywhere (both the :27 parenthetical and the :38 heading suffix are gone)
    - The procedure names all six routing behaviors and references smells.md, fowler-catalog, kerievsky-catalog, functional-catalog, and refactoring-without-tests.md by file-level link; it adds no Markdown table
    - SKILL.md is < 500 lines (wc -l)
    - From .claude/skills/lz-refactor-workspace: node tools/check-crossrefs.mjs exits 0 (all SKILL.md outbound links resolve) and node tools/check-hygiene.mjs exits 0 (ASCII-only, work-email-free)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-crossrefs.mjs && node tools/check-hygiene.mjs && node -e "const t=require('fs').readFileSync('../../../plugins/lz-tdd/skills/lz-refactor/SKILL.md','utf8');process.exit(t.includes('## Coach decision procedure')&&!t.includes('deferred to Phase 9')&&t.split(/\r?\n/).length<500?0:1)"</automated>
  </verify>
  <done>The inline coach decision procedure replaces the placeholder; the deferred-to-Phase-9 markers are gone; no new smell table; SKILL.md is well under 500 lines; check-crossrefs and check-hygiene are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits a single shipped Markdown file (SKILL.md). No runtime, network, session, crypto, or
user-input surface. The boundaries are public-repo content hygiene and link integrity.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | SKILL.md must stay ASCII-only and work-email-free |
| coach cross-links -> catalog references | a broken/anchored link would misroute the coach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-LINK | Tampering | SKILL.md coach cross-links | mitigate | Links are file-level to already-existing files; check-crossrefs (extended in 09-01 to source SKILL.md) resolves every outbound link. |
| T-09-IP | Information Disclosure | coach-procedure prose | mitigate | The procedure is original routing prose (no book text); check-hygiene no-verbatim WARN + skill-reviewer at the 09-04 gate. |
| T-09-EM | Information Disclosure | committed SKILL.md | mitigate | check-hygiene HARD-fails on non-ASCII bytes and on non-allowlisted email tokens (work-email leak recurred twice in Phase 4). |
</threat_model>

<verification>
- Scoped to this plan's file (full-battery GREEN is asserted at the 09-04 gate, not here -- check-backing stays RED in this worktree because the principle refs are authored in the parallel 09-03).
- node tools/check-crossrefs.mjs exits 0 (SKILL.md links resolve).
- node tools/check-hygiene.mjs exits 0 (ASCII + email hygiene).
- SKILL.md < 500 lines and free of the "deferred to Phase 9" markers.
</verification>

<success_criteria>
The dual-mode coach decision procedure is inline in SKILL.md (CCH-01..06), routes on the existing
smells.md + catalog references with no duplicate table, keeps its links file-level and resolving, and
the router stays a lean file well under the < 500-line budget.
</success_criteria>

<output>
Create `.planning/phases/09-coach-behavior-principle-backing/09-02-SUMMARY.md` when done.
</output>
