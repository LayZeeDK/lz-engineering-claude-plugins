---
context: quick-task
quick_task: 260801-sc9
supersedes: 260729-x0i
phase: 21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i
task: 3
total_tasks: 3
status: quick_task_complete_owner_decision_pending
last_updated: 2026-08-01T20:24:01.783Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Every one below was discovered through an ACTUAL FAILURE, not predicted.
> Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: **THE PLANNING RECORD LIED, UNANIMOUSLY, AND IT COST MONEY.** Six artifacts agreed EVL-03's metered round was unapproved and outstanding. It had already run on 2026-07-27/28 -- 36 runs, $29.04, user-approved, fully written up. Mitigation: before acting on any "outstanding" claim, OPEN THE WORK PRODUCT and read its first page. Unanimity across planning docs is not corroboration; they copy from each other.
- [ ] CONSTRAINT: **A TRAILING `echo` LAUNDERS EXIT STATUS.** `cmd; echo "EXIT=$?"` always exits 0. Written twice by me this session, reproduced by the planner, caught twice by the plan-checker as BLOCKERS. Mitigation: `cmd; test $? -eq 0 && echo TOKEN`. Never gate on log text.
- [ ] CONSTRAINT: **THE BATTERY PRINTS "123 checks" EVEN WHEN RED.** The roster detail string is assembled from the emitted count independently of the verdict and of failures from any other guard. Mitigation: gate on the process exit code FIRST; keep the count as a separate second leg (it catches a dropped or duplicated guard, which the exit code alone would not).
- [ ] CONSTRAINT: **THE APPLY HARNESS SKIPS COMPLETED RUNS.** `run-e2e.mjs` prints "already done (exit 0), skip" and re-spends nothing without `--force`. Mitigation: pass `--force` for a genuine re-run, and never assume a fresh round overwrites stale captures in place.
- [ ] CONSTRAINT: **DIFFING A SUBTREE HIDES RELOCATIONS.** Diffing `plugins/lz-tdd/skills/lz-red/` missed that lz-red gained a Beck reference (both ends of the move sat outside that path) and missed two changed files inside the treatment surface. Mitigation: `git diff --name-status -M <base> HEAD -- plugins/lz-tdd`, whole plugin, rename detection on.
- [ ] CONSTRAINT: **DST-04 DOES NOT INHERIT.** The archived taxonomy survived four provenance remediation rounds; its distillation still reproduced source phrasing in two places on the first oracle-reviewer pass. Mitigation: treat every DERIVED artifact as new for clean-room purposes and gate it accordingly. Canonical one-line definitions are the highest-risk sentences.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting the planning record over the artifact | Six docs said EVL-03 was outstanding; the results file's first page said RUN COMPLETE. $0.78 spent re-running finished work. | blocking | Open the work product's first page before acting on any "outstanding" claim, especially before spend. |
| Fail-open verify leg | `; echo "EXIT=$?"` and `rg -c` on log text both pass on a RED run. | blocking | `test $? -eq 0 && echo TOKEN`. Gate on process status, never on printed text. |
| Assuming an overwrite | Concluded deletion was unnecessary because the re-run "would overwrite in place". The harness skips instead. | blocking | Verify the tool's actual resume semantics; add a freshness gate (mtime) before tabulating. |
| Subtree-scoped diff | Relocations in or out of the diffed path are invisible. | advisory | Diff the whole component with `-M`. |
| Reading headings as structure | Read `EVAL-RESULTS.md` headings, inferred "scaffold", moved on. It was 561 filled lines. | blocking | Read the first page, not the outline. |
| Placeholder scan false positives | "placeholder" is domain vocabulary in skill scaffolds here; the scan flags ~53 files. | advisory | Read the matches before treating any as incomplete work. |

<current_state>
Working tree CLEAN. HEAD `769a2b6` on `gsd/lz-tdd-0.0.3-lz-red`, 362 commits ahead of `origin/main`, NOTHING PUSHED. No worktrees, no async jobs.

Quick task `260801-sc9` is COMPLETE and verified (7/7, passed). It built the TREATMENT ARM of the D-12 A/B. Zero metered spend.

The session's dominant finding is unrelated to that task: **EVL-03 is already done.** The milestone is not waiting on the eval -- it is waiting on an owner decision about whether that completed round still describes the shipped skill.
</current_state>

<completed_work>

- Anchor re-verified before anything: battery 123/0, selftest 49 assertions, both scope invariants empty against `lz-tdd@0.0.2`.
- Zero-spend canary GREEN: real exit 0, zero SKIPs, crux 9 measured rather than skipped.
- **EVL-03 discovered complete**: 36 runs captured 2026-07-27, all graded, `EVAL-RESULTS.md` 561 filled lines, $29.04, user-approved at the gate.
- Re-run STOPPED after one run ($0.78). Stray capture archived; original restored byte-identical (9/9 GRC metas match). All three borrowed repos verified pristine.
- Full corpus archived to `D:/.lz-red-archive/2026-07-27-preskill-drift/` with a provenance README.
- Quick task `260801-sc9` end to end: CONTEXT (3 locked, 1 UNRESOLVED), RESEARCH, PLAN, plan-check (2 blockers -> revision -> PASSED), worktree executor (3 commits), verifier (7/7).
- DST-04 oracle-reviewer gate run by the orchestrator: first pass `too_close_to_source: true`, revised, second pass clean.
- G17 ship blocker recorded in `REQUIREMENTS.md`, figure re-derived (9-vs-10 was a units difference).
</completed_work>

<remaining_work>

1. **EVL-03 drift decision** -- owner. Gates everything else.
2. **Reconcile six stale artifacts** -- blocked on 1.
3. **`/gsd-secure-phase 21`** via `gsd-security-auditor`. Zero spend, genuinely outstanding (15-20 have SECURITY.md; 21 does not).
4. **`/gsd-validate-phase 21`** via `gsd-nyquist-auditor`. `21-VALIDATION.md` frontmatter is `status: draft`.
5. **`/gsd-extract-learnings 21`** LAST, plus the manual `~/.gsd/knowledge/` bridge.
6. **UNRESOLVED-1** -- the ambiguous-prompt corpus. Owner.
7. **D-12 A/B run** -- blocked on 6 plus spend approval.
8. **`/gsd-complete-milestone lz-tdd@0.0.3`** with MANUAL archival.
</remaining_work>

<decisions_made>

- Stopped the re-run rather than completing the approved scope -- the approval rested on a false premise, and unspent money is recoverable.
- Treatment artifact lives OUTSIDE `plugins/`; renaming to dodge guard N3 was rejected as the evasion N3 was hardened against.
- `invoke_treatment` excluded from `--arm all` so crux 6's pinned dry-run stays byte-identical.
- The G17 figure was re-derived, not copied from either prior source.
- The two oracle-reviewer ambiguities were carried to the owner rather than silently resolved -- they are coach-scope content calls.
</decisions_made>

<blockers>
- **EVL-03 drift** (human): accept the completed round, or re-run? Three costed paths -- A: re-grade only, $0, ~25 min. B: GRC + RXF, ~$14. C: all four cells, ~$50 (not the $39 previously quoted; that used a stale per-run estimate).
- **UNRESOLVED-1** (human): the ambiguous-prompt corpus. Trap-quadrant, deliberately not auto-locked.
- **DST-04 partial** (technical): Beck, Metz, 99 Bottles, Clean Code, Kerievsky, Cooper and Bernhardt attributions UNGATED -- those stores were not supplied. Disclosed in the artifact header.
- **Two coach-scope calls** (human): recorded in `260801-sc9-RUN-GATE.md` section 5.
</blockers>

## Required Reading (in order)

1. `.claude/skills/lz-red-workspace/e2e-red-gilded-rose/EVAL-RESULTS.md` -- **read the FIRST PAGE before anything else.** It is the completed EVL-03 result and it contradicts several planning docs.
2. `.planning/HANDOFF.json` -- structured state, including the three costed paths.
3. `.planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md` -- what was built, UNRESOLVED-1 verbatim, and the DST-04 gate's partial status.
4. `.planning/quick/260801-sc9-.../260801-sc9-CONTEXT.md` -- the locked decisions and why UNRESOLVED-1 was not auto-locked.
5. `D:/.lz-red-archive/2026-07-27-preskill-drift/README.md` -- why the archived corpus is not reusable as-is.

## Infrastructure State

- Borrowed repos all pristine: `GildedRose-Refactoring-Kata`, `h3js/srvx`, `radix-ng/primitives-pin` -- 0 dirty files, 1 worktree each, `node_modules` intact.
- No stranded grading worktrees in `D:/.lz-red-grade-tmp` or `os.tmpdir()`.
- `D:/.lz-red-throwaway/` holds two empty leftovers (`radix`, `probe-snapshots-moved-aside`) -- harmless, outside the repo.
- `worktree.baseRef: "head"` IS set, so subagent worktrees inherit local HEAD rather than `origin/main`. Load-bearing here at 362 unpushed commits.
- A fresh worktree has no `node_modules`; `selfcheck-red.mjs` needs `npm ci --prefix .claude/skills/lz-red-workspace` before crux 7 can run.
- Original GRC armed base `ac6a0335` confirmed still reachable, so Path A's re-grade is possible.

<context>
The plan for this session was: decide EVL-03, then run the three phase-close audits. The decision turned out to be moot -- the eval was done. That discovery, and stopping the spend it triggered, consumed most of the session. The quick task that followed went cleanly and every gate earned its place: the plan-checker caught two real fail-opens, the executor refused to wave through a selfcheck failure, and the DST-04 gate caught near-verbatim copying that four prior remediation rounds had not prevented in the derived form.

The milestone is closer than the records suggest. EVL-03's empirical half is done; what remains is a judgement call about drift, three zero-spend audits, and record hygiene.
</context>

<next_action>
Start with: read the first page of `.claude/skills/lz-red-workspace/e2e-red-gilded-rose/EVAL-RESULTS.md`, then put the EVL-03 drift question to the owner with the three costed paths. Path A (re-grade only, $0, ~25 min) is worth running regardless -- every existing grade predates the `e428d8a` attribution fix by 13 minutes, and the corpus holds exactly one `unattributable` verdict carrying that bug's signature. The three phase-21 audits are zero-spend and can proceed in parallel with that decision: secure -> validate -> extract-learnings, each reached BY its dedicated agent, never self-certified inline.
</next_action>
