---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 07
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/dealing-with-inheritance.md
autonomous: false
requirements: [FWL-01, FWL-04]
must_haves:
  truths:
    - "D-02: each of the 11 entries in this batch fills the per-entry contract exactly -- 2nd-ed name (+ 1st-ed alias), distilled motivation, distilled mechanics, a minimal focused TypeScript before->after example, and a provenance label"
    - "D-07: an AskUserQuestion oracle-access checkpoint is opened for this batch BEFORE authoring any entry"
    - "FWL-04: every before AND after ts fence in this batch compiles tsc --strict --noEmit clean"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/dealing-with-inheritance.md"
      provides: "the 11 dealing-with-inheritance-tag refactoring entries"
      contains: "Pull Up Method"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/dealing-with-inheritance.md"
      to: ".claude/skills/lz-refactor-workspace/samples/*.ts"
      via: "extract-samples.mjs pulls each ts fence into a compiled module"
      pattern: "```ts"
---

<objective>
Author the "dealing-with-inheritance" tag-group leaf -- 11 entries -- behind the mandatory
D-07 oracle checkpoint. Leaf set fixed by 07-02. This batch completes the 66-entry catalog
(21 + 14 + 20 + 11 = 66).

Batch (disjoint from other content plans):
- dealing-with-inheritance.md (11): Pull Up Method, Pull Up Field, Pull Up Constructor
  Body, Push Down Method, Push Down Field, Replace Type Code with Subclasses (aliases
  Extract Subclass / Replace Type Code with State/Strategy), Remove Subclass (alias Replace
  Subclass with Fields), Extract Superclass, Collapse Hierarchy, Replace Subclass with
  Delegate, Replace Superclass with Delegate (alias Replace Inheritance with Delegation).

Purpose: FWL-01 coverage for these 11 + FWL-04 compile-clean examples.
Output: one populated leaf file, each entry meeting the D-02 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md

<interfaces>
Same per-entry leaf template as 07-04 (D-02 contract): `### <Name>  [provenance]` heading
with the tag on the heading line, optional alias line, Motivation, Mechanics, one/two tsc
--strict-clean ts fences. All entries in this batch are `[print+web]` (no + entries here).
Class/inheritance examples need class syntax but still no `dom` lib (tsconfig omits it).
Leaf name comes from 07-02-SUMMARY. Match the leaf shape authored in 07-04.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-07 oracle checkpoint -- dealing-with-inheritance batch</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md (D-02 contract; D-07 oracle mandate)
    - .planning/research/refactoring-com-overview.md (the 11 names + aliases in this batch)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (legend + contract)
  </read_first>
  <what-needed>
    The authoritative motivation + mechanics for the 11 named refactorings in this batch,
    from the owner's Fowler Refactoring 2nd-ed e-book / web edition (ISBN 9780135425664).
    Mechanics/motivation MUST come from the oracle, not training memory (D-07).
  </what-needed>
  <how-to-provide>
    Open the AskUserQuestion oracle-access checkpoint batched by this tag-group. The owner
    confirms/provides each refactoring's substance for distillation in ORIGINAL words.
  </how-to-provide>
  <action>
    Open the AskUserQuestion oracle-access checkpoint for this batch and BLOCK: author NO
    entry until the owner confirms the batch substance (D-07). Verify in this tag-group batch,
    not one refactoring at a time. This gate survives the --auto/--chain run.
  </action>
  <resume-signal>Owner confirms the substance for the 11 refactorings in this batch</resume-signal>
  <acceptance_criteria>
    - The AskUserQuestion oracle-access checkpoint is opened and the owner has confirmed this batch's substance BEFORE any entry is written.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author dealing-with-inheritance.md (11 entries)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/dealing-with-inheritance.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (contract + legend)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md (leaf shape from 07-04)
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (tsc --strict-clean fence conventions)
  </read_first>
  <action>
    Author the 11 dealing-with-inheritance entries: Pull Up Method, Pull Up Field, Pull Up
    Constructor Body, Push Down Method, Push Down Field, Replace Type Code with Subclasses
    (aliases Extract Subclass / Replace Type Code with State/Strategy), Remove Subclass
    (alias Replace Subclass with Fields), Extract Superclass, Collapse Hierarchy, Replace
    Subclass with Delegate, Replace Superclass with Delegate (alias Replace Inheritance with
    Delegation). Each follows the D-02 contract with distilled Motivation + Mechanics
    (original words from the oracle batch) and a minimal focused TypeScript before->after
    example, both fences tsc --strict-clean. All `[print+web]`. NO verbatim Fowler prose/code
    (DST-04). ASCII only. Contents TOC if over ~100 lines.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` + `tsc --strict --noEmit` exits 0 over the batch's modules (FWL-04).
    - `node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` shows all 11 dealing-with-inheritance names PASS AND reports 66/66 present overall (this batch completes the catalog).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner e-book -> authored leaf file | oracle mechanics cross into a public-repo file |
| authored leaf -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal | verbatim Fowler prose or code in the 11 entries | mitigate | DST-04 distilled voice + ORIGINAL TS examples; check-hygiene no-verbatim heuristic + Phase-10 scan |
| T-07-03 | Tampering | fabricated mechanics bypassing the oracle | mitigate | D-07 blocking checkpoint BEFORE authoring; batched by tag-group |
| T-07-02 | Information Disclosure | work email in the leaf | mitigate | check-hygiene work-email allowlist gate |
| T-07-05 | Tampering | a "before" example differing from "after" only by a compile error | mitigate | both fences compile tsc --strict-clean (FWL-04 harness) |

No high-severity threats: prose + original-code authoring behind a blocking human oracle
checkpoint. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The oracle checkpoint was opened before any entry was authored.
- check-catalog.mjs reports 66/66 present overall; extract + tsc exits 0; check-hygiene exits 0.
</verification>

<success_criteria>
- 11 entries authored to the D-02 contract in dealing-with-inheritance.md, completing all 66.
- All examples tsc --strict-clean (FWL-04); ASCII-only; no verbatim prose.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-07-SUMMARY.md` when done.
</output>
