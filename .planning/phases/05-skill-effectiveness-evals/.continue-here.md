---
context: phase
phase: 05-skill-effectiveness-evals
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-07-02T22:36:08.636Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Each was discovered through actual failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: Upstream skill-creator run_eval is broken -- use the VENDORED harness, NOT upstream, NOT WSL. The installed `skill-creator` `run_eval.py`/`run_loop.py` crash on native Windows (`select()` on a pipe -> WinError 10038/10093) AND their slash-command probe mis-detects triggers on EVERY OS (~0 recall) -- issues #3548/#3041/#2678. WSL fixes ONLY the crash, not the detection. STRUCTURAL MITIGATION: run EVAL-01 via `.claude/skills/lz-tpp-workspace/tools/skill-creator-eval/` (`python -m scripts.run_eval` from that dir), which reimplements `run_single_query` per #3041. Never invoke the installed upstream; never use WSL.
- [ ] CONSTRAINT: grade-run.mjs is a PRE-FILTER; a real grading.json needs the LLM-judge MERGE step (not built yet). grade-run fail-closes to `grading.preliminary.json` while judge items remain; `aggregate_benchmark.py` reads `summary.pass_rate` directly and IGNORES `preliminary`/`judge_required`. STRUCTURAL MITIGATION: build the merge step (task 8) that resolves `judge_required`, recomputes `summary` over ALL expectations, refuses any grading.json with `judge_required != []` or `preliminary:true`, and treats aggregate "grading.json not found" as an ERROR (not a skip).
- [ ] CONSTRAINT: per-run metrics.json MUST be canonical nested `{tool_calls:{Edit,Write,Bash,...}}` (flat `{edits,writes,testRuns}` also accepted). A flat-vs-nested mismatch previously produced a SILENT false-pass on the "coach, don't drive" check. The grader now fails LOUD on an unrecognized shape and fail-safe (false) with no metrics. STRUCTURAL MITIGATION: the EVAL-02 runner emits the nested shape per run; any Edit/Write/MultiEdit/NotebookEdit/Bash > 0 = "drove".

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Run EVAL-01 via upstream run_eval or WSL | Upstream crashes on Windows + mis-detects triggers all-OS; WSL fixes only the crash -> meaningless ~0 results | blocking | Run only the vendored harness at `.claude/skills/lz-tpp-workspace/tools/skill-creator-eval/`; smoke-gate first (should-trigger must read 1/1) |
| Treat grade-run output as a final grade | grade-run only scores objective checks; nuance is `passed:null` (judge). An unmerged preliminary would inflate the scored-only rate | blocking | Build + run the LLM-judge merge; grade-run writes `grading.preliminary.json` (never `grading.json`) while judge items remain; aggregate must error (not skip) on a missing grading.json |
| Wrong-shaped metrics.json | Flat vs nested mismatch silently passed the safety-critical no-drive check | blocking | Emit nested `tool_calls.{Edit,Write,Bash}` per run; grader now fails loud on unrecognized shape |
| Prime the review agents | Primed reviewers confirm your assumptions and miss bugs in them (they missed the metrics-shape CRITICAL) | advisory | Include >=1 FRESH reviewer with a neutral from-scratch brief (no prior findings) that verifies against the real downstream consumer |
| lz-tpp enabled as a project skill during the probe | An enabled real skill steals the trigger -> false negatives (A3 / Pitfall 2) | advisory | Re-verify `~/.claude/plugins/config.json` + `~/.claude/settings.json` show lz-tpp NOT enabled at EVAL-01 run time |

<current_state>
Phase 5 (Skill Effectiveness Evals, optional-final + NON-BLOCKING). Task 8 (LLM-judge MERGE step + pre-aggregate GATE) is now DONE and committed (`60551dd`): `.claude/skills/lz-tpp-workspace/merge-judge.mjs` -- `--merge` (fail-closed verdict merge), `--verify <iteration-dir>` (errors if any run dir lacks a final grading.json; closes aggregate's silent-skip hole), `--selfcheck` (passes). Verified end-to-end against the real grade-run.mjs. Still nothing RUN: `.claude/skills/lz-tpp-workspace/iteration-1/eval-*/` contain only `eval_metadata.json`, no run dirs. The pause message AUTHORIZES the fresh session to run both evals (this overrides the standing eval-run approval gate for this specific work); next session was asked to reconfirm right before token spend.
</current_state>

<completed_work>
Committed this session:
- discuss-phase (--auto) -> 05-CONTEXT.md incl. D-09 native pivot
- research -> 05-RESEARCH.md (upstream run_eval Windows+all-OS bugs, #3548/#3041/#2678), 05-PATTERNS.md, 05-VALIDATION.md
- plans 05-01..04, plan-checker (0 blockers), re-synced to native + `.claude/skills/lz-tpp-workspace/` paths
- vendored native-fixed run_eval at `.claude/skills/lz-tpp-workspace/tools/skill-creator-eval/` (MIT provenance + README) + generic root `.gitignore` for `.claude/skills/*-workspace/`
- eval sets: `evals/trigger-eval.json` (27: 13 pos / 14 neg, 9 jargon-free positives, separability broken), `evals/trigger-smoke.json` (2), `evals/evals.json` (10 behavior scenarios) -- all review-revised
- `grade-run.mjs` (deterministic pre-filter, `--selfcheck` passes) + 10 `iteration-1/eval-*/eval_metadata.json`
- grader hardened over 3 review rounds; the FRESH/unbiased round caught + fixed a CRITICAL (nodrive metrics-shape false-pass), word-bounded transformRe, eval-6 realign, fail-closed output, drift-guard selfcheck
</completed_work>

<remaining_work>
1. Task 8 -- DONE (`60551dd`): merge-judge.mjs (--merge / --verify / --selfcheck), verified end-to-end.
2. Task 9 -- RUN EVAL-01 (05-03) natively: smoke (`trigger-smoke.json`, --runs-per-query 1) must read the should-trigger at 1/1; then full `trigger-eval.json` --runs-per-query 3 -> `.claude/skills/lz-tpp-workspace/trigger-results.json`; report recall/specificity vs ~90% (D-06).
3. Task 10 -- RUN EVAL-02 (05-02): 10 scenarios x (with_skill + baseline) x >=3 runs; per-run nested `metrics.json` + coach output; grade-run pre-filter -> LLM-judge merge -> `aggregate_benchmark` -> Pass@k/Pass^k (per eval + overall; wait for ALL completion notifications before grading).
4. Task 11 -- Combine -> `.claude/skills/lz-tpp-workspace/EVAL-RESULTS.md` (Pass@k/Pass^k + D-06 verdict) + at-most-one human-gated D-07 tuning (only write into `plugins/`).
</remaining_work>

<decisions_made>
- D-09: native vendored harness, WSL abandoned, artifacts under `.claude/skills/lz-tpp-workspace/`.
- Grader = deterministic pre-filter + LLM-judge for nuance; fail-closed output.
- metrics.json = canonical nested `tool_calls` (flat also accepted); wrong shape fails loud.
- At most ONE human-gated D-07 tuning; non-blocking; Phases 1-4 stay complete.
- Full decision list: `05-CONTEXT.md` D-01..D-09.
</decisions_made>

<blockers>
- None technical-blocking. The two eval RUNS spend plan tokens (authorized by the pause message). The merge step (task 8) must exist before aggregating EVAL-02 / taking Pass@k.
</blockers>

## Required Reading (in order)
1. `.claude/skills/lz-tpp-workspace/tools/skill-creator-eval/README.md` -- the vendored harness: exact `run_eval` command, provenance, A3 note.
2. `.planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md` -- D-01..D-09 (esp. D-09 pivot).
3. `.planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md` -- schemas, ready-to-use scenarios, the Windows/all-OS bug analysis, Pass@k/Pass^k formulas, `## Validation Architecture`.
4. `05-03-PLAN.md` (native EVAL-01 run) and `05-02-PLAN.md` (EVAL-02 + the GRADING MODEL note describing the pre-filter -> merge -> aggregate flow).
5. `.claude/skills/lz-tpp-workspace/grade-run.mjs` -- the grader (read the header + RUBRICS); run `node grade-run.mjs --selfcheck` to confirm it still passes.

## Infrastructure State
- Native Windows Python 3.13.6; Node v24.18.0. `grade-run.mjs --selfcheck` -> SELFCHECK OK.
- Vendored harness present + compiles: `.claude/skills/lz-tpp-workspace/tools/skill-creator-eval/scripts/{__init__,utils,run_eval}.py` (run `python -m scripts.run_eval` from the `skill-creator-eval` dir).
- No runs executed yet (no run dirs under iteration-1). `.planning/config.json` has a pre-existing (non-this-work) uncommitted change; leave it.
- A3: re-verify lz-tpp NOT enabled in `~/.claude/plugins/config.json` + `~/.claude/settings.json` before EVAL-01.

## Pre-Execution Critique Required
- Design artifact: the (to-be-built) LLM-judge merge step + the EVAL-02 run harness.
- Critique focus: does the merge refuse `judge_required != []`? does aggregate ERROR (not skip) on a missing grading.json? is the per-run metrics.json nested-shaped? Include a FRESH/unbiased reviewer.
- Gate: do not aggregate / take Pass@k until the merge step is built and its output verified.

<context>
The user gated every run and demanded independent review at each step; three review rounds on grade-run.mjs, the last (fresh/unbiased) caught the CRITICAL the primed rounds missed. The phase is optional-final + non-blocking -- the public ship (Phases 1-4) is already complete, so nothing here can regress the shipped skill except the single gated D-07 tuning.
</context>

<next_action>
Task 8 done. Next: run the EVAL-01 native SMOKE test (task 9) -- `trigger-smoke.json` --runs-per-query 1, must read the should-trigger at 1/1 -- before the full 27-query run. Re-verify A3 (lz-tpp NOT enabled in ~/.claude) right before EVAL-01. Both eval runs are authorized by the pause message but reconfirm before token spend. Grader intact this session (grade-run.mjs + merge-judge.mjs both --selfcheck OK).
</next_action>
