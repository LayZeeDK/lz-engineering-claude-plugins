# Refactoring Principles (Fowler Ch.2 + backing)

Scope: the refactoring principles that back the coach's recommendations -- Fowler Ch.2 as the core,
with Beck principle-backing (TDD by Example, Tidy First?) folded in. Reference mode explains a
principle on demand; coach mode cites a principle when justifying a recommendation.

> Populated in Phase 7 (Fowler Ch.2, oracle-verified). Beck principle-backing PLACEMENT is
> finalized in Phase 9 (D-03) -- keep it in this single file for now; do NOT pre-split a
> principles-beck.md at scaffold time. Beck is unowned -> high-confidence core only, tagged
> no-oracle. No verbatim Fowler or Beck prose or code (DST-04).

## Per-entry content contract

Each principle, when populated, carries:

- Principle -- the principle name / heading in original words.
- Distilled statement -- what it means and when it applies, in original words (no verbatim book
  prose).
- Attribution -- Fowler Ch.2, or Beck (TDD by Example / Tidy First?), tagged no-oracle where the
  source is unowned.

## Sources (placeholder)

- Fowler *Refactoring* 2nd-ed e-book / web edition (ISBN 9780135425664) Ch.2 -- oracle-verified in
  Phase 7.
- Beck (TDD by Example, Tidy First?) -- unowned; high-confidence core only, no-oracle tag; Phase 9.
