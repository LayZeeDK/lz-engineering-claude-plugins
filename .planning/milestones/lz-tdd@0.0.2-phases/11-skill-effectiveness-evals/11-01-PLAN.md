---
phase: 11-skill-effectiveness-evals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/__init__.py
  - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/utils.py
  - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/run_eval.py
  - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/LICENSE.upstream.txt
  - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/README.md
  - .claude/skills/lz-refactor-workspace/eval-status.mjs
  - .claude/skills/lz-refactor-workspace/merge-judge.mjs
  - .claude/skills/lz-refactor-workspace/run-spec-chunks.mjs
  - .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md
autonomous: true
requirements: [EVL-01, EVL-02]
user_setup: []

must_haves:
  truths:
    - "**D-01:** The vendored native-fixed skill-creator-eval harness is copied VERBATIM under .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/ (run_eval.py / utils.py / __init__.py byte-identical to the Phase-5 lz-tpp analog, preserving the Apache-2.0 provenance + run_eval reimplementation note); the locked RUN config (serial --num-workers 1, PONYTAIL_DEFAULT_MODE=off, MCP stripped, model claude-opus-4-8) is recorded in EVAL-RESULTS.md, not re-derived. This phase authors eval DATA + one grader, never eval INFRASTRUCTURE."
    - "**D-09:** All eval infrastructure lives under .claude/skills/lz-refactor-workspace/ (never under plugins/ or .planning/); the existing root .gitignore .claude/skills/*-workspace/ rules already track the eval record and drop bulky raw outputs, reused as-is with NO per-skill gitignore edits."
    - "**D-07:** EVAL-RESULTS.md is scaffolded with the soft, tunable, non-blocking pass-bar columns (trigger recall + specificity ~90% with near-misses quiet; behavior with-skill Pass@1 high AND beating baseline) and empty numbers, ready for the gated run to fill."
    - "merge-judge.mjs --selfcheck exits 0 (SELFCHECK OK) after the verbatim copy; eval-status.mjs and run-spec-chunks.mjs pass node --check."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/run_eval.py"
      provides: "Native-fixed trigger probe (3 upstream bugs fixed in run_single_query); skill-agnostic"
      contains: "run_single_query"
    - path: ".claude/skills/lz-refactor-workspace/merge-judge.mjs"
      provides: "Fail-closed LLM-judge merge + pre-aggregate completeness gate (--merge/--verify/--selfcheck)"
      contains: "selfcheck"
    - path: ".claude/skills/lz-refactor-workspace/eval-status.mjs"
      provides: "Generic run-tree walker (MISSING/PARTIAL/DONE)"
    - path: ".claude/skills/lz-refactor-workspace/run-spec-chunks.mjs"
      provides: "Optional canary-gated specificity runner (4 path constants + canary re-pointed to lz-refactor)"
    - path: ".claude/skills/lz-refactor-workspace/EVAL-RESULTS.md"
      provides: "Results-record template with the run-config caveat + empty EVL-01/EVL-02 tables"
      contains: "num-workers 1"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/run-spec-chunks.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor"
      via: "SKILL + WS path constants + an lz-refactor should-trigger CANARY query"
      pattern: "lz-refactor"
    - from: ".claude/skills/lz-refactor-workspace/tools/skill-creator-eval/README.md"
      to: "plugins/lz-tdd/skills/lz-refactor"
      via: "--skill-path example + Pitfall-2 note naming BOTH sibling skills"
      pattern: "lz-refactor"
---

<objective>
Vendor the proven Phase-5 eval infrastructure into the lz-refactor workspace, verbatim, plus the results-record template. This is the shared rig that BOTH evals run on (EVL-01 trigger probe + EVL-02 grade/merge pipeline).

Purpose: Reuse the on-disk, native-fixed harness rather than re-fixing the three upstream run_eval.py bugs (D-01). Deviating from the proven infrastructure re-opens solved problems.
Output: A copied tools/skill-creator-eval/ dir, eval-status.mjs + merge-judge.mjs (verbatim), run-spec-chunks.mjs (light-edited), and an EVAL-RESULTS.md template with blank numbers. Nothing is RUN (D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skill-effectiveness-evals/11-RESEARCH.md
@.planning/phases/11-skill-effectiveness-evals/11-PATTERNS.md

Analog rig (gitignored path -- read with Read/rg/Glob, NEVER git grep):
- .claude/skills/lz-tpp-workspace/tools/skill-creator-eval/ (README.md, LICENSE.upstream.txt, scripts/{__init__.py, utils.py, run_eval.py})
- .claude/skills/lz-tpp-workspace/eval-status.mjs
- .claude/skills/lz-tpp-workspace/merge-judge.mjs
- .claude/skills/lz-tpp-workspace/run-spec-chunks.mjs
- .claude/skills/lz-tpp-workspace/EVAL-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor tools/skill-creator-eval verbatim + README re-point</name>
  <files>.claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/__init__.py, .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/utils.py, .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/run_eval.py, .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/LICENSE.upstream.txt, .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/README.md</files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/tools/skill-creator-eval/README.md (analog to copy + edit; lines 38 and 48 are the --skill-path example and the Pitfall-2 note)
    - .claude/skills/lz-tpp-workspace/tools/skill-creator-eval/scripts/run_eval.py, utils.py, __init__.py, LICENSE.upstream.txt (copy byte-for-byte)
    - 11-PATTERNS.md "tools/skill-creator-eval/ (vendored harness, COPY VERBATIM)" section (the two exact README edits; do NOT copy __pycache__)
  </read_first>
  <action>Copy the four scripts + LICENSE from the lz-tpp analog into the lz-refactor workspace tools dir byte-for-byte (exclude __pycache__ / *.pyc). Then edit ONLY README.md per PATTERNS.md: change the --skill-path example from the lz-tpp path to plugins/lz-tdd/skills/lz-refactor, and widen the Pitfall-2 note to name BOTH sibling skills (either lz-tpp or lz-refactor can steal the trigger now that both ship in one plugin). Keep the Apache-2.0 provenance block and the run_eval reimplementation note verbatim. Do NOT re-fix any upstream bug (the native fix in run_single_query is skill-agnostic and already correct -- A2). Optionally fix the README's --num-workers example from 3 to 1 to match the locked config; the authoritative run config is EVAL-RESULTS.md regardless.</action>
  <verify>
    <automated>diff -rq --exclude=__pycache__ .claude/skills/lz-tpp-workspace/tools/skill-creator-eval/scripts .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts && rg -q "plugins/lz-tdd/skills/lz-refactor" .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/README.md</automated>
  </verify>
  <acceptance_criteria>
    - run_eval.py, utils.py, __init__.py, LICENSE.upstream.txt are byte-identical to the analog (diff reports no differences on the scripts dir).
    - README.md --skill-path example points at plugins/lz-tdd/skills/lz-refactor; the Pitfall-2 note names both siblings.
    - No lz-tpp --skill-path example remains in the copied README.
    - ASCII-only; no work-email/domain present (allowlist-inversion: only the approved gmail may appear).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Copy eval-status.mjs + merge-judge.mjs verbatim; selfcheck GREEN</name>
  <files>.claude/skills/lz-refactor-workspace/eval-status.mjs, .claude/skills/lz-refactor-workspace/merge-judge.mjs</files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/eval-status.mjs (generic run-tree walker; no skill content)
    - .claude/skills/lz-tpp-workspace/merge-judge.mjs (fail-closed judge merge + --verify gate; --selfcheck uses placeholder texts, no skill content)
    - 11-PATTERNS.md "eval-status.mjs" and "merge-judge.mjs" sections (both COPY VERBATIM)
  </read_first>
  <action>Copy both .mjs files from the lz-tpp analog into the lz-refactor workspace root byte-for-byte. No edits -- both are fully generic (config names with_skill/without_skill and artifact names are shared across skills; the selfcheck uses placeholder Q1/Q2 nuance texts). merge-judge.mjs is the Pitfall-5 protection (its --verify gate stops aggregate_benchmark from silently dropping unmerged runs); preserve it exactly.</action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/merge-judge.mjs --selfcheck && node --check .claude/skills/lz-refactor-workspace/eval-status.mjs && diff -q .claude/skills/lz-tpp-workspace/merge-judge.mjs .claude/skills/lz-refactor-workspace/merge-judge.mjs</automated>
  </verify>
  <acceptance_criteria>
    - merge-judge.mjs --selfcheck prints SELFCHECK OK and exits 0.
    - merge-judge.mjs and eval-status.mjs are byte-identical to the analog (diff reports no differences).
    - eval-status.mjs passes node --check.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Light-edit run-spec-chunks.mjs + scaffold EVAL-RESULTS.md template</name>
  <files>.claude/skills/lz-refactor-workspace/run-spec-chunks.mjs, .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md</files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/run-spec-chunks.mjs (lines 14-18 = the 4 path constants WS/TOOL_DIR/SKILL/CHUNK_DIR + the CANARY query to re-point)
    - .claude/skills/lz-tpp-workspace/EVAL-RESULTS.md (section structure + the "Harness lessons" run-config caveat text to carry as the LESSON, not the lz-tpp-specific correction prose)
    - 11-PATTERNS.md "run-spec-chunks.mjs" and "EVAL-RESULTS.md" sections; 11-RESEARCH.md "Build/Run split" (the exact run config strings)
  </read_first>
  <action>Copy run-spec-chunks.mjs from the analog, then edit ONLY the path constants: WS -> the lz-refactor-workspace absolute path, SKILL -> plugins/lz-tdd/skills/lz-refactor absolute path (TOOL_DIR and CHUNK_DIR derive from WS automatically), and CANARY -> an lz-refactor should-trigger query with should_trigger:true (use the EVL-01 T6 Extract Function catalog lookup, or an equivalent should-trigger prompt). All chunking / assess / resume / execFileSync logic is reused as-is. This component is OPTIONAL at run time (a throttle workaround). Then scaffold EVAL-RESULTS.md from the analog's structure with numbers BLANK: an intro (skill lz-refactor, model claude-opus-4-8, 3 runs per query/scenario/config), an "## EVL-01 -- Trigger accuracy (native)" recall/specificity table with the D-07 ~90% bar column, an "## EVL-02 -- Behavior / next-refactoring" per-scenario with_skill-vs-baseline (full-pass + Pass@1) table with a verdict column, a "## Harness lessons" section carrying the run-config caveat VERBATIM (serial --num-workers 1; PONYTAIL_DEFAULT_MODE=off; --strict-mcp-config drops MCP; --setting-sources project drops user plugins and keeps the ephemeral project skill; do NOT use --setting-sources "" or --bare; specificity is throttle-robust, recall is not), and a "## Reproduce / resume" section with the RUN command chain (grade -> judge -> merge -> verify -> aggregate) placeholder. Do NOT fill any measured numbers -- those come only after the gated run (D-10).</action>
  <verify>
    <automated>node --check .claude/skills/lz-refactor-workspace/run-spec-chunks.mjs && rg -q "lz-refactor-workspace" .claude/skills/lz-refactor-workspace/run-spec-chunks.mjs && rg -q "num-workers 1" .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md && rg -q "EVL-01" .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md && rg -q "EVL-02" .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - run-spec-chunks.mjs passes node --check; WS/SKILL point at lz-refactor paths; CANARY.should_trigger is true; no lz-tpp path constant remains.
    - EVAL-RESULTS.md has the intro + EVL-01 table + EVL-02 table + Harness lessons (run-config caveat verbatim) + Reproduce/resume sections, all with BLANK numbers.
    - ASCII-only (`->`, straight quotes, no emojis/em-dashes); allowlist-inversion email scan clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored files -> local git tree | New committed scripts + README + results template (local, no network) |
| vendored harness -> claude CLI subprocess | run_eval.py spawns the authenticated claude CLI at RUN time only (gated, out of this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | vendored run_eval.py + helpers | mitigate | Copy verbatim from the on-disk Phase-5 Apache-2.0 rig; diff-verify the 3 .py files byte-identical to the analog before commit; no upstream re-fix (A2). |
| T-11-02 | Information Disclosure | committed README.md + EVAL-RESULTS.md prose | mitigate | ASCII-only; allowlist-inversion email scan (only the approved gmail may appear) before commit. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; the harness is a verbatim on-disk copy, not a registry fetch (11-RESEARCH.md Package Legitimacy Audit: N/A). Low severity, no ASVS L1 install surface. |
</threat_model>

<verification>
- diff-based byte-identity of the copied harness scripts + eval-status.mjs + merge-judge.mjs against the lz-tpp analog.
- merge-judge.mjs --selfcheck GREEN; run-spec-chunks.mjs + eval-status.mjs node --check clean.
- EVAL-RESULTS.md carries the run-config caveat + empty tables.
- No eval, trigger probe, or claude -p invocation runs in this plan (D-10).
</verification>

<success_criteria>
- The full vendored harness + orchestration utils + results template exist under .claude/skills/lz-refactor-workspace/, verbatim where required and re-pointed where required.
- merge-judge selfcheck GREEN; all .mjs pass node --check.
- ASCII-only, work-email-free (allowlist-inversion), no writes into plugins/ or .planning/.
</success_criteria>

<output>
Create `.planning/phases/11-skill-effectiveness-evals/11-01-SUMMARY.md` when done.
</output>
