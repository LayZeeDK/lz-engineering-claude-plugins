---
phase: 08-kerievsky-catalog-refactoring-to-patterns
plan: 05
type: execute
wave: 2
depends_on: [08-01]
branching_strategy: none
tags: [kerievsky-catalog, refactoring, skill-authoring, oracle-clean-room, typescript, protection, accumulation, utilities, de-patterning]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-type-code-with-class.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/limit-instantiation-with-singleton.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-collecting-parameter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/chain-constructors.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/unify-interfaces.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-parameter.md
autonomous: false
requirements: [KRV-01, KRV-02, KRV-04]
must_haves:
  truths:
    - "D-01: the Ch.9 Protection + Ch.10 Accumulation + Ch.11 Utilities refactorings are authored as per-refactoring leaf files kerievsky-catalog/<slug>.md behind the thin index, mirroring the LOCKED Fowler per-refactoring-leaf model; SKILL.md is NOT re-touched"
    - "D-02: each leaf carries the LOCKED Fowler contract (single # <Name> H1 anchor + Use when: + ## Motivation (intent leads) + ## Mechanics + ## Example + optional ## Watch for) PLUS the three Kerievsky field labels in lockstep with check-kerievsky: 'Composed Fowler primitive(s):' (>=1 ../fowler-catalog/<slug>.md#<slug> cross-link), 'Direction:', 'GoF pattern:'"
    - "D-03: every leaf carries a Direction value; the 3rd named de-patterning case -- Move Accumulation to Visitor (Direction: Away, away from Iterator) -- carries an explicit 'refactor AWAY from Iterator' callout; the other 7 leaves are Towards"
    - "D-04: each leaf's 'GoF pattern:' field names the target for vocabulary only -- Class/type-safe value (Replace Type Code with Class, non-GoF), Singleton (Limit Instantiation with Singleton), Null Object (Introduce Null Object, non-classic-GoF), Collecting Parameter (non-GoF), Visitor (Move Accumulation to Visitor); the 3 Utilities (Chain Constructors, Unify Interfaces, Extract Parameter) carry the literal 'n/a -- utility'; NO reproduced GoF prose or code (DST-04)"
    - "D-06: each example is re-rendered from the book's Java into ONE compact original TS before/after (5-15 lines/side, hard cap 2), original domain + identifiers, tsc --strict-clean via extract-samples; overflow-walkthrough only if a leaf's teaching lives in intermediate states (YAGNI)"
    - "D-07: each leaf is authored BLIND, gated by the oracle-reviewer subagent over .oracle/refactoring-to-patterns/index.md scoped per-chapter ('Ch.9 Protection' / 'Ch.10 Accumulation' / 'Ch.11 Utilities'), revised BLIND to convergence; the main context NEVER reads .oracle/ (ls for names only)"
    - "D-08: this plan runs INLINE in the MAIN context (Agent + AskUserQuestion tools), NOT via gsd-executor and NOT in a worktree (branching_strategy: none); the oracle agents are read-only so there is no file conflict"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md"
      provides: "Move Accumulation to Visitor leaf (Direction: Away, away from Iterator -- de-patterning case; GoF: Visitor)"
      contains: "Direction:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/chain-constructors.md"
      provides: "Chain Constructors leaf (utility -- GoF pattern: n/a -- utility)"
      contains: "GoF pattern:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-special-case.md"
      via: "composed Fowler primitive cross-link (Introduce Special Case is the Fowler 2nd-ed name; resolving H1 anchor)"
      pattern: "fowler-catalog/[a-z-]+\\.md#"
---

<objective>
Author the tail chapters -- Ch.9 Protection (3), Ch.10 Accumulation (2), Ch.11 Utilities (3) = 8
Kerievsky pattern-directed refactorings -- as per-refactoring catalog leaves using the clean-room
blind-author -> oracle-reviewer-gate -> converge loop. Includes the 3rd named de-patterning (Away)
case (Move Accumulation to Visitor, away from Iterator) and the 3 utility refactorings that carry
"n/a -- utility" for GoF.

Purpose: fill KRV-01/02/04 for the last three chapters, completing all 27 leaves. File-disjoint by
owning only these chapters' leaf files.

Output: the 8 tail leaf files under references/kerievsky-catalog/ (the thin index README.md is
finalized later, in 08-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-special-case.md

<interfaces>
CLEAN-ROOM FIREWALL (D-07): the executor/main context NEVER opens any file under .oracle/ -- `ls`
for names only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` gates a drafted leaf. Input: DRAFT_PATHS, SOURCE =
  ".oracle/refactoring-to-patterns/index.md" + the per-leaf chapter topic ("Ch.9 -- Protection",
  "Ch.10 -- Accumulation", "Ch.11 -- Utilities"; index.md + chapter number/topic, NEVER a book
  chapter filename), CONTENT_TYPE "refactoring-leaf", SCOPE = each leaf's refactoring, canonical
  per-axis anchors PLUS direction / composed-primitive-aptness / target-pattern axes. Returns
  pass|revise|blocked|error.
- `oracle` for open-ended chapter-membership / composed-primitive lookups.
Sub-batch by chapter (3 / 2 / 3) or as one 8-leaf call (within/at the ~6-8 guidance) with per-leaf
SCOPE so the reviewer does not treat out-of-scope chapter entries as dropped; cap ~3 rounds;
escalate to Task 2. Fan-out guard: do NOT persist a full ordered name+chapter map.

Members + orientation (ASSUMED -- oracle settles direction/GoF/composed-primitives):
  Ch.9 Protection:
    20. Replace Type Code with Class -- GoF: Class / type-safe value (non-GoF); Towards;
        replace-primitive-with-object, replace-type-code-with-subclasses, encapsulate-variable.
    21. Limit Instantiation with Singleton -- GoF: Singleton; Towards;
        replace-constructor-with-factory-function, change-function-declaration.
    22. Introduce Null Object -- GoF: Null Object (Woolf, non-classic-GoF); Towards;
        introduce-special-case (the Fowler 2nd-ed name for Introduce Null Object),
        replace-conditional-with-polymorphism.
  Ch.10 Accumulation:
    23. Move Accumulation to Collecting Parameter -- GoF: Collecting Parameter (Beck, non-GoF);
        Towards; extract-function, change-function-declaration, move-statements-into-function,
        split-loop.
    24. Move Accumulation to Visitor -- GoF: Visitor; Direction AWAY (from Iterator, D-03);
        extract-class, move-function, change-function-declaration.
  Ch.11 Utilities:
    25. Chain Constructors -- GoF pattern: n/a -- utility; Towards; pull-up-constructor-body,
        remove-dead-code, substitute-algorithm, change-function-declaration.
    26. Unify Interfaces -- GoF pattern: n/a -- utility; Towards; change-function-declaration,
        extract-superclass.
    27. Extract Parameter -- GoF pattern: n/a -- utility; Towards; extract-variable,
        change-function-declaration, parameterize-function.

Leaf field LABELS (lockstep with check-kerievsky -- Pitfall 4): "Composed Fowler primitive(s):",
"Direction:", "GoF pattern:". check-kerievsky accepts a non-empty GoF value OR the literal
"n/a -- utility" (Open Question 1 default). Composed-primitive links target
../fowler-catalog/<slug>.md#<slug>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Ch.9 + Ch.10 + Ch.11 tail leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-type-code-with-class.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/limit-instantiation-with-singleton.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-collecting-parameter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/chain-constructors.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/unify-interfaces.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-parameter.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- refactoring leaf + overflow rule)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Canonical per-axis anchors", "Driver responsibilities" -- pass tight per-leaf SCOPE across the 3 chapters)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md (blind drafts fold sub-steps; too_close_to_source re-domain; no-DOM tsc; cross-ref aptness)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-01, D-02, D-03, D-04, D-06, D-07, D-08)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md (leaf skeleton + 3-field placement + Away callout + n/a-utility GoF allowance)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/introduce-special-case.md (the Fowler 2nd-ed Introduce Null Object leaf -- the composed-primitive target for #22)
    - .oracle/ (ls ONLY for filenames -- NEVER open any file under .oracle/ from this context)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not). Do
    NOT open any file under .oracle/ from this context. Run this loop INLINE in the main context
    (D-08) -- NOT via gsd-executor, NOT in a worktree.

    Author the 8 tail leaves (one file per refactoring, kebab-case slug = filename) via the core
    clean-room loop:
    (1) AUTHOR BLIND in the LOCKED leaf format: single `# <Name>` H1, one-line `Use when:`,
    `## Motivation` (intent leads, original words), `## Mechanics` (4-8 verb-first numbered steps,
    original words, naming the test/compile checkpoint inline), `## Example` (ONE compact
    tsc --strict-clean TS before->after, 5-15 lines/side, hard cap 2; pure -- no DOM/console;
    original domain + identifiers). THEN add the three Kerievsky field lines with the EXACT labels:
    `Composed Fowler primitive(s):` (>=1 ../fowler-catalog/<slug>.md#<slug> link), `Direction:`,
    `GoF pattern:` (name only; the 3 Utilities use the literal "n/a -- utility"). For Move
    Accumulation to Visitor set `Direction: Away` AND add an explicit "refactor AWAY from Iterator"
    callout in the body. Both before AND after compile (a design change, not a type error). Apply
    the overflow rule only if a leaf's teaching truly lives in intermediate states (YAGNI). ASCII
    only.
    (2) DETERMINISTIC LAYER FIRST (D-07 -- run the cheap checks BEFORE spending reviewer calls): run
    extract-samples (tsc --strict) + check-hygiene + the field-presence rg checks over the authored
    leaves; fix any compile / ASCII / missing-field / malformed-cross-link defect BLIND. Only once
    this layer is green do you spend an oracle-reviewer call.
    (3) GATE: spawn `oracle-reviewer` with DRAFT_PATHS, SOURCE ".oracle/refactoring-to-patterns/index.md"
    + the per-leaf chapter topic (Ch.9/Ch.10/Ch.11), CONTENT_TYPE "refactoring-leaf", per-leaf
    SCOPE, the canonical + pattern-directed axes. Use `oracle` for open-ended lookups.
    (4) REVISE BLIND from the directives; on too_close_to_source, fully re-domain the example blind
    and re-gate.
    (5) ITERATE to convergence (every leaf `pass`, or owner-accepted `blocked`); cap rounds ~3 and
    escalate to Task 2. Fan-out guard.
    (6) RE-GATE post-oracle deterministic fixes: after applying reviewer directives, re-run the
    deterministic layer; if a post-oracle fix CHANGES any leaf's example before/after code, re-run
    oracle-reviewer on that leaf so no example ships un-re-reviewed for behavior-preservation /
    near-verbatim. Commit on green. Do NOT edit kerievsky-catalog/README.md or any checker .mjs; the
    full check-kerievsky + crossrefs run at the 08-06 gate.
  </action>
  <acceptance_criteria>
    - All 8 tail leaf files exist; each has a single `# <Name>` H1, `Use when:`, `## Motivation`, `## Mechanics`, `## Example` with >=1 ts fence, and the three field labels (`rg -q "^Composed Fowler primitive\(s\):" <leaf> && rg -q "^Direction:" <leaf> && rg -q "^GoF pattern:" <leaf>` per leaf).
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` exits 0 (every tail example is tsc --strict-clean).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
    - The 3rd Away case carries Direction: Away + a callout: `rg -qi "away from Iterator" .../move-accumulation-to-visitor.md`.
    - The 3 utility leaves carry the literal GoF allowance: `rg -q "n/a -- utility" .../chain-constructors.md .../unify-interfaces.md .../extract-parameter.md`.
    - Each leaf reached oracle-reviewer `pass` (or owner-accepted `blocked` via Task 2), recorded in the SUMMARY; no file under .oracle/ was opened by this context.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -qi "away from Iterator" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md && rg -q "n/a -- utility" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/chain-constructors.md && rg -q "^Composed Fowler primitive\(s\):" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md && test $(ls plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/*.md | rg -v README | wc -l) -ge 8</automated>
  </verify>
  <done>All 8 tail leaves (Ch.9+10+11) exist as contract-complete files with the three Kerievsky fields; Move Accumulation to Visitor is Direction: Away + flagged; the 3 Utilities carry "n/a -- utility"; every example is tsc --strict-clean and behavior-preserving; each oracle-reviewer verdict is pass (or owner-accepted blocked); check-hygiene green; README.md and checkers untouched; no .oracle/ file opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner escalation for non-converging / blocked tail leaves (conditional)</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Driver responsibilities" -- loop bound + oscillation guard + owner escalation)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-04 GoF AskUserQuestion fallback; D-07 escalation path)
  </read_first>
  <decision>How to resolve any Ch.9/10/11 leaf the oracle-reviewer loop cannot converge within ~3 rounds, returns as a blocked ambiguity, or whose GoF target the Kerievsky oracle cannot settle (D-04 fallback)?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate. The driver caps rounds (~3) and escalates that ENTRY to the owner via
    AskUserQuestion. GoF is not in .oracle/, so an unresolvable GoF target for a specific leaf is
    also an owner question (D-04 fallback). Fires ONLY for entries that do not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked entry as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (mechanics / direction / GoF target), then re-run the loop once</name>
      <pros>Resolves a genuine fidelity or GoF-target gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked, or have an
    unresolvable GoF target): open an AskUserQuestion with the entry, the reviewer's
    ambiguities/unable-to-verify axes (own words), and the two options; apply the choice; re-run the
    deterministic layer; record it in the SUMMARY. Do NOT reveal or request verbatim source prose.
  </action>
  <acceptance_criteria>
    - Every Ch.9/10/11 leaf is oracle-reviewer `pass` or owner-adjudicated; each escalation (or a no-op note if none) is recorded in 08-05-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every tail leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-to-patterns/` (owner full-text book, git-ignored) -> authored public-repo leaves | copyrighted Kerievsky expression must never cross; the ONLY crossing is via the oracle/oracle-reviewer subagents (isolated context, own-words verdicts, no prose/code/paths) |
| GoF (owner-held, NOT in .oracle/) -> authored leaves | only the pattern NAME (vocabulary) crosses via the GoF pattern field; NO GoF prose or code (DST-04) |
| authored files -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure / legal (DST-04) | authored leaf prose + example code + GoF vocabulary | mitigate | Clean-room firewall: main context NEVER reads .oracle/; authored BLIND; oracle-reviewer full-strength near-verbatim gate; GoF name-only; check-hygiene no-verbatim WARN + Phase-10 scan |
| T-08-04 | Tampering (fabricated / unfaithful content) | mechanics / motivation / direction / GoF target / composed-primitive fidelity | mitigate | oracle-reviewer fidelity gate (+ direction / composed-primitive-aptness / target-pattern axes) + converge loop; driver caps rounds (~3) and escalates to owner (Task 2) |
| T-08-05 | Information Disclosure / firewall | main context accidentally reading .oracle/ prose (incl. fan-out aggregation) | mitigate | firewall discipline (ls only); INLINE main-context orchestration; driver dedupes/caps, does NOT persist a full ordered name+chapter map |
| T-08-06 | Tampering | a resolvable-but-wrong composed-Fowler cross-ref | mitigate | check-crossrefs (resolution, at 08-06 gate) + oracle-reviewer composed-primitive-aptness axis |
| T-08-02 | Information Disclosure | work email in a committed leaf | mitigate | check-hygiene work-email allowlist gate |
| T-08-03 | Tampering (integrity) | non-ASCII / mojibake in a leaf | mitigate | check-hygiene ASCII hard-fail; ASCII-only authoring |

No UI, no network, no secrets, no new packages. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- extract-samples (tsc --strict) GREEN over all Ch.9/10/11 examples; check-hygiene GREEN; each leaf has the full contract + the 3 Kerievsky fields.
- Move Accumulation to Visitor is Direction: Away + flagged; the 3 Utilities carry "n/a -- utility".
- Every tail leaf reached oracle-reviewer pass (or owner-accepted blocked).
- kerievsky-catalog/README.md and checker .mjs untouched; no .oracle/ file opened by this context.
</verification>

<success_criteria>
- All 8 Ch.9+10+11 refactorings are contract-complete, oracle-converged, tsc --strict-clean leaves with the three Kerievsky fields (KRV-01/04) and correct Direction incl. the 3rd Away case (KRV-02); completing all 27 catalog leaves.
- The plan owns only these chapters' leaf files (file-disjoint from sibling wave-2 plans); composed-Fowler cross-refs resolve at the 08-06 gate.
</success_criteria>

<output>
Create `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-05-SUMMARY.md` when done, recording the confirmed Ch.9/10/11 membership (including any oracle name/slug corrections vs the CITED names, for the 08-06 check-kerievsky NAMES reconciliation), per-leaf round counts, the settled directions/GoF targets, and any owner escalations.
</output>
