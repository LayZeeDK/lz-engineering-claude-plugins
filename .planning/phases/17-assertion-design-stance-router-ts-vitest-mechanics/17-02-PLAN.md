---
phase: 17-assertion-design-stance-router-ts-vitest-mechanics
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
autonomous: true
requirements: [ASRT-01, ASRT-02]
must_haves:
  truths:
    - "test-structure-and-assertions.md carries the four pillars (Khorikov, no-oracle) with resistance-to-refactoring named the load-bearing pillar, the F.I.R.S.T. properties (owned, oracle-verified attribution), and the observable-behavior framing -- authored own-words"
    - "the assertions slice carries the output-/state-/communication-based selection rule tied to the stance router (output <-> functional core; state/communication <-> Metz boundary; characterization <-> Feathers legacy), pointing at the three leaves"
    - "principle-backing.md maps every RED recommendation (selection, structure, naming, assertions, stance, anti-patterns, mechanics) to its source + access tier, using the canonical owned/no-oracle tier strings"
    - "the STR slice (Phase 16) is left intact; the Phase-18 F.I.R.S.T.-as-baseline PROCEDURE marker remains; principle-backing leaves a Phase-18 marker for the Three-Laws / seam backing rows"
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
    - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  key_links:
    - "assertions-slice selection rule -> functional-core.md / message-matrix.md / seams-and-legacy.md (the ASRT-02 spine)"
    - "principle-backing rows link each recommendation to the doc that states it"
---

<objective>
Fill the ASRT slice of `test-structure-and-assertions.md` (assertion design: the four pillars,
F.I.R.S.T., and the output/state/communication selection rule) and populate the co-edited
`principle-backing.md` source-to-recommendation map. The STR slice already filled in Phase 16 is
left untouched. This is the "assert well" surface downstream of test structure.

Purpose: close ASRT-01 (assert observable behavior via the four pillars, resistance-to-refactoring
load-bearing) and ASRT-02 (the assertion-style selection rule tied to the stance router), and record
the DST-03 traceability map.
Output: the filled assertions slice + the backing map, every fence tsc --strict clean, own-words.

Executor note (executor cannot spawn agents): author BLIND. The F.I.R.S.T. properties are an OWNED
surface (Clean Code Ch.9) but you do NOT read `.oracle/`; you author own-words from the 17-RESEARCH
planning grain (the acronym expansion is a plain fact) in original wording. The orchestrator runs the
oracle-reviewer converge-to-clean gate on the F.I.R.S.T. prose AFTER this wave (Plan 17-06); your job
is a clean own-words draft + the deterministic gates.

Decisions implemented (decision-coverage):
- **D-01:** F.I.R.S.T. is the OWNED, oracle-gated surface -- authored own-words, oracle-reviewer
  gate deferred to the orchestrator (Plan 17-06); main context never reads `.oracle/`.
- **D-02:** the four pillars (Khorikov) are NO-ORACLE -- authored blind from high-confidence core,
  no-verbatim scan only.
- **D-03:** ASRT-01 -- assert observable behavior via the four pillars (protection against
  regressions; resistance to refactoring; fast feedback; maintainability), resistance-to-refactoring
  named the load-bearing pillar; F.I.R.S.T. co-lands as the test-quality baseline.
- **D-04:** ASRT-02 -- the output-/state-/communication-based selection rule tied to the router
  (output <-> Bernhardt; state/communication <-> Metz; characterization <-> Feathers).
- **D-10:** VIT-02 is satisfied in the references, not the SKILL.md body -- the assertions slice
  carries a tsc-strict Vitest fence; the SKILL.md body is not edited here.
- **D-14:** no-verbatim gate -- F.I.R.S.T. + every fence pass the deterministic no-verbatim scan;
  the F.I.R.S.T. prose additionally clears the orchestrator oracle-reviewer.
- **D-15:** co-edit, do not split -- fill only the ASRT slice, leave the STR slice and the Phase-18
  F.I.R.S.T.-as-baseline PROCEDURE marker intact; principle-backing is co-edited (add the rows now,
  leave a Phase-18 marker for the Three-Laws / seam rows).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-RESEARCH.md
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
@plugins/lz-tdd/skills/lz-red/references/naming.md
@plugins/lz-tdd/skills/lz-red/references/principle-backing.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill the ASRT slice (four pillars, F.I.R.S.T., selection rule) in test-structure-and-assertions.md</name>
  <files>plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (the STR slice lines 19-84 is the in-file style analog; the ASRT deferral section lines 86-90 is what to REPLACE; the provenance blockquote lines 9-17 and Sources lines 92-102 to extend)
    - plugins/lz-tdd/skills/lz-red/references/naming.md (the owned-attribution bullet form + Sources access-tier strings to mirror)
    - 17-RESEARCH.md "Assertion Design" (four pillars + three assertion styles + F.I.R.S.T., lines 211-255) and 17-PATTERNS.md "test-structure-and-assertions.md -- ASRT slice" (lines 30-69) + Shared Patterns (lines 266-330)
    - Environment (worktree bootstrap): if `.claude/skills/lz-red-workspace/node_modules/` is absent (a fresh git worktree; the tree is gitignored), run `npm --prefix .claude/skills/lz-red-workspace install` BEFORE the `run typecheck` in verify. check-red-references.mjs and check-hygiene.mjs are dependency-free Node builtins -- no bootstrap needed for those.
  </read_first>
  <action>
    REPLACE the ASRT deferral section with real content. Rename the heading to drop the "(Phase 17)"
    parenthetical (e.g. "## Assert observable behavior: the four pillars") so the instrument's Phase-18
    deferral guard cannot false-match a Phase-17 literal. Author own-words, mirroring the STR slice's
    Rule/When-to-use/Distilled-rationale bullet triad exactly.

    Content to add (own-words, DST-04):
    - The four pillars of a good unit test (Khorikov, no-oracle): protection against regressions;
      resistance to refactoring; fast feedback; maintainability. Name resistance-to-refactoring as the
      LOAD-BEARING pillar -- it is precisely "assert observable behavior, not implementation" (fewest
      false failures on a behavior-preserving change). Use a "Property:" lead bullet per pillar or a
      compact table; keep the load-bearing pillar's rationale explicit.
    - The F.I.R.S.T. properties (owned, oracle-verified) as the test-quality baseline the pillars sit
      on: Fast, Independent/isolated, Repeatable, Self-validating, Timely. The acronym expansion is a
      plain fact; write every gloss in original words. Carry the owned attribution parenthetical
      "(Robert C. Martin, owned; oracle-verified)" on the distilled line, matching naming.md's form.
      Do NOT read .oracle/ -- author from the 17-RESEARCH planning grain in original wording.
    - The ASRT-02 selection rule (own-words): prefer output-based (assert the returned value; highest
      resistance to refactoring; the functional core); use state-based when the behavior is a state
      change (the Metz boundary, incoming-command side effect); use communication-based ONLY for a
      genuine outgoing command (the Metz boundary, expect-to-send); use characterization when the unit
      is untested legacy (Feathers). Link the three destinations to functional-core.md,
      message-matrix.md, and seams-and-legacy.md respectively (the assertions slice carries the
      SELECTION rule; each leaf carries its own assert rule).
    - Add ONE self-contained tsc-strict Vitest fence showing an observable-behavior (output-based)
      assertion, mirroring the fence shape at three-laws-and-test-selection.md lines 54-72 (import from
      'vitest'; define the SUT in-fence; the example compiles clean and its assertion pins a returned
      value). Self-contained module scope; no DOM / Node builtins.

    Leave a labelled Phase-18 insertion-point marker for the F.I.R.S.T.-as-baseline PROCEDURE step
    (LAW-02) with a "(Phase 18)" tag so the instrument's deferral guard passes. Extend the provenance
    blockquote to name the added material (four pillars = no-oracle; F.I.R.S.T. = owned/oracle-verified)
    and extend the "## Sources" list with a Khorikov row (no-oracle tier string) and the existing RCM /
    Clean Code Ch.9 row already covers F.I.R.S.T. -- add a note that Ch.9 also backs F.I.R.S.T. Do NOT
    edit the STR slice (lines 19-84).
  </action>
  <acceptance_criteria>
    - The file contains the tokens: four pillars, resistance to refactoring, F.I.R.S.T., observable
      behavior, and communication-based (the ASRT-02 selection style) -- all present.
    - The assertions slice links to all three stance leaves by filename -- functional-core.md,
      message-matrix.md, and seams-and-legacy.md (the ASRT-02 spine; the instrument's three
      filename-presence tokens on this entry pass).
    - The five STR tokens (arrange-act-assert, given-when-then, assert-first, evident test data, one
      concept) are unchanged and still present.
    - A "(Phase 18)" F.I.R.S.T.-as-baseline / LAW-02 marker line is present (the instrument deferral
      guard passes); no "(Phase 17)" deferral language remains.
    - At least one tsc --strict Vitest fence is present and compiles clean under `run typecheck`.
    - `## Sources` (no draft parenthetical) with a Khorikov no-oracle row + the F.I.R.S.T./Clean Code
      owned attribution; check-hygiene ASCII + no-verbatim clean.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>The assertions slice is filled own-words; typecheck + hygiene GREEN; check output shows the test-structure-and-assertions.md rows (STR + ASRT tokens, fence, Phase-18 marker, no scaffold) all PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate principle-backing.md (source-to-recommendation map + access tiers)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/principle-backing.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/principle-backing.md (its content contract lines 14-30 names the row fields Recommendation / Source / Access tier)
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (the backing voice + the inline sibling-link form for a Recommendation, lines 1-5 and 53-56)
    - plugins/lz-tdd/skills/lz-red/references/naming.md + three-laws-and-test-selection.md (the canonical tier strings: "Owned; oracle-verified against the clean-room source." and "Unowned; high-confidence core only (no-oracle).")
    - 17-PATTERNS.md "principle-backing.md" (lines 243-263)
  </read_first>
  <action>
    Populate the backing map as a Markdown table (Recommendation | Source | Access tier), one row per
    RED recommendation across all lz-red reference docs (selection, structure, naming, assertions,
    stance, anti-patterns, mechanics). Link each Recommendation to the doc that states it (inline
    sibling-relative link form, e.g. [four pillars](test-structure-and-assertions.md)). Rows to include
    (recommendation -> source -> tier):
    - test list / one small step / degenerate case / triangulation -> Kent Beck -> no-oracle;
      one-small-step rationale -> Robert C. Martin (Clean Code Ch.9) -> owned.
    - arrange-act-assert -> Bill Wake; given-when-then -> Dan North; assert-first / evident data ->
      Kent Beck (all no-oracle); one concept per test -> Robert C. Martin -> owned.
    - behavior naming -> Dan North; three-part naming -> Roy Osherove (no-oracle); name-the-behavior ->
      Metz + Owen -> owned.
    - four pillars -> Vladimir Khorikov -> no-oracle; F.I.R.S.T. -> Robert C. Martin (Clean Code Ch.9)
      -> owned; output/state/communication selection -> Khorikov -> no-oracle.
    - functional core (output-based) -> Gary Bernhardt -> no-oracle; message matrix -> Metz + Owen ->
      owned; seams + characterization -> Michael Feathers -> no-oracle.
    - anti-patterns: over-mock/test-per-class -> Ian Cooper -> owned; implementation-detail brittleness
      -> Khorikov -> no-oracle; mockist counterpoint -> GOOS (Freeman + Pryce) -> no-oracle; Test
      Desiderata -> Kent Beck -> no-oracle.
    - Vitest mechanics -> Vitest 4.x docs -> no-oracle.
    Use the canonical tier strings verbatim ("Owned; oracle-verified against the clean-room source."
    / "Unowned; high-confidence core only (no-oracle)."). Rewrite the draft `## Sources` heading to a
    plain "## Sources". Leave a labelled Phase-18 marker (a "(Phase 18)" line) for the Three Laws
    (LAW-01/02) and lz-tpp seam (SEAM-01/02) backing rows that Phase 18 will add, so the co-edit
    boundary holds. No fence (this is a cross-cutting map). ASCII-only; own-words; no source prose.
  </action>
  <acceptance_criteria>
    - A Recommendation | Source | Access tier table is present with rows spanning selection, structure,
      naming, assertions, stance, anti-patterns, and mechanics.
    - Both canonical tier strings appear (/oracle-verified/i and /no-oracle/i); at least one
      Recommendation is a Markdown link to a sibling doc.
    - Khorikov is named; a "(Phase 18)" marker line is present for the deferred Three-Laws / seam rows.
    - The draft `## Sources` heading is rewritten to a plain `## Sources` (no draft parenthetical);
      no scaffold phrase remains; check-hygiene ASCII + no-verbatim clean.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && npm --prefix .claude/skills/lz-red-workspace run typecheck</automated>
  </verify>
  <done>principle-backing.md is a filled backing table with tier strings + a Phase-18 marker; hygiene GREEN; the check output shows its rows PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| book prose (.oracle) -> shipped tree | F.I.R.S.T. (Clean Code Ch.9) is owned; verbatim prose must not cross into the shipped reference. |
| author identity -> committed content | maintainer work-email / domain must never appear; author/committer = public gmail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure (copyright, DST-04) | F.I.R.S.T. prose in the assertions slice | high | mitigate | Executor authors own-words BLIND (never reads .oracle/); deterministic no-verbatim scan (>=120-char quoted run) per commit; orchestrator oracle-reviewer converge-to-clean on F.I.R.S.T. in Plan 17-06. |
| T-17-02 | Information Disclosure (PII) | committed Markdown + commit identity | high | mitigate | check-hygiene allowlist-inversion (only public gmail permitted); verify git author/committer = public gmail before commit. |
| T-17-03 | Tampering (mojibake) | ASCII shipped tree | medium | mitigate | check-hygiene ASCII byte scan; author with `--` / `->` only. |
</threat_model>

<verification>
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 (the assertions-slice fence compiles tsc --strict).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII + email + no-verbatim).
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` shows the test-structure-and-assertions.md and principle-backing.md rows PASS (sibling stubs remain RED until their Wave-2 plans land -- expected during the wave).
</verification>

<success_criteria>
- ASRT-01 + ASRT-02 content is present own-words with the four pillars, resistance-to-refactoring load-bearing, F.I.R.S.T., and the router-tied selection rule.
- principle-backing.md maps every recommendation to a source + tier with a Phase-18 marker for the deferred rows.
- STR slice untouched; Phase-18 markers intact; typecheck + hygiene GREEN.
</success_criteria>

## Artifacts this phase produces (Plan 17-02)

- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md`
  (ASRT slice: four pillars + F.I.R.S.T. + output/state/communication selection rule + one
  tsc-strict Vitest fence + a Phase-18 F.I.R.S.T.-baseline marker; STR slice unchanged).
- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/principle-backing.md`
  (Recommendation | Source | Access tier map across all RED docs + a Phase-18 deferral marker).
- No new files. No new dependency. Owned surface authored this plan: F.I.R.S.T. (oracle-reviewer
  gated by the orchestrator in Plan 17-06).

<output>
Create `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-02-SUMMARY.md` when done.
</output>
