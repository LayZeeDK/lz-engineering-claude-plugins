---
quick_id: 260712-i5y
type: eval-measurement
autonomous: false
must_haves:
  truths:
    - "Package-scope directive prompt (no `/lz-refactor`) is the measured stimulus for BOTH suites: nx `@nx/eslint-plugin` (p8) and Gilded Rose kata (gr4). Verbatim user wording; register = directive-unscripted."
    - "Live (latest-commit skill via `--plugin-dir`) is measured FIRST; the most-relevant baseline (`no_skill` = base Opus, no plugin) is measured AFTER live."
    - "All prompts, suite configs, and the runner script are code-reviewed BEFORE any metered run; the 3 apply-mode harness fixes (I1/I3/I5) land before spending."
    - "Grading reads meta.json + diff.patch (untracked-inclusive) + transcript, filtering exit-0; NOT used_refactor for force-load (n/a here -- auto-trigger arm)."
    - "Behavior preservation verified against the ORIGINAL tests (nx jest 169-pass baseline; kata golden master); scope-contained (no test-file edits)."
  artifacts:
    - ".claude/skills/lz-refactor-workspace/e2e-nx/prompts/p8-eslint-plugin-package-directive.md"
    - ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/gr4-project-directive.md"
    - ".claude/skills/lz-refactor-workspace/e2e-{nx,gilded-rose}/results/apply/{with_skill,no_skill}/{p8,gr4}/"
  key_links:
    - from: "p8/gr4 directive prompts"
      to: "results/apply/{with_skill,no_skill}"
      via: "run-e2e.mjs apply mode, k=3, live-first then baseline"
      pattern: "e2e-eval"
---

<objective>
Measure the user's actual real-world prompt at PACKAGE scope: "Identify code smells and refactor
them away in the @nx/eslint-plugin package" (and a kata analog), no `/lz-refactor`. Determine whether
the shipped skill drives identify -> triage -> multi-round refactor to a fixpoint, and whether that is
skill-attributable (vs base Opus). Live measured first, baseline after.
</objective>

<tasks>
<task>Build p8 (nx package) + gr4 (kata project) directive prompts; register in suites.</task>
<task>Code-review gate: unbiased prompt+config review + harness code review; apply I1/I3/I5 apply-mode fixes.</task>
<task>Run live (with_skill) first, then no_skill baseline; apply mode, k=3, both suites. Grade posture from meta/diff/transcript.</task>
</tasks>

<success_criteria>
Both suites measured live + baseline; graded for auto-trigger, cross-package triage, round count,
behavior preservation, scope containment, and trap posture; live-vs-baseline delta reported.
</success_criteria>
