---
phase: 08.1-gof-design-patterns-catalog
plan: 06
type: execute
wave: 2
depends_on: [08.1-01]
branching_strategy: none
tags: [extra-patterns-catalog, kerievsky, design-patterns, skill-authoring, oracle-clean-room, typescript]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/null-object.md
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/creation-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/composed-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/collecting-parameter.md
autonomous: false
requirements: [XTR-01, GOF-03, GOF-04]
must_haves:
  truths:
    - "D-02: EXACTLY the 5 Tier-1 extra patterns (Null Object, Factory, Creation Method, Composed Method, Collecting Parameter) are authored as per-pattern leaf files extra-patterns-catalog/<slug>.md behind the thin index, one file per pattern, filename == kebab-slug of the H1; NO Tier-2 patterns (Type Object, Extension Object, DI leaf); the README rows + SKILL.md are NOT touched here"
    - "D-04: each extra-pattern leaf reuses the EXACT locked 5-section contract -- single # <Pattern> H1 (optional Also known as:), ## Intent, ## Applicability (FIRST LINE = index selector), ## Consequences, ## Example (ONE strict-TS fence naming participants + collaboration), ## Related patterns; NO synthesized Avoid when, NO invented Watch for, NO Use when: line"
    - "D-07: Consequences is present + populated; Collecting Parameter's explicit liabilities list is empty in the source, so its downside is ASSEMBLED from Kerievsky's surrounding prose + the Visitor contrast (tied to specific classes/interfaces; reads poorly across heterogeneous objects where Visitor is preferred) -- documented, NOT fabricated beyond what that prose supports"
    - "D-12: all 5 leaves are sourced/verified via the clean-room oracle loop against .oracle/refactoring-to-patterns/ (index.md + the relevant chapter number/topic only), authored BLIND with distinct example domains, deterministic layer run FIRST, oracle-reviewer gate, revised BLIND to convergence (~3-round cap, escalate on non-convergence); the main context NEVER reads .oracle/ prose (ls for names only)"
    - "D-13: this plan runs INLINE in the MAIN context (Agent + AskUserQuestion tools), NOT via gsd-executor, NOT in a worktree (branching_strategy: none); the oracle agents are read-only so there is no file conflict"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/collecting-parameter.md"
      provides: "Collecting Parameter leaf: 5-section contract; ## Consequences downside assembled from Kerievsky prose + the Visitor contrast (D-07)"
      contains: "## Consequences"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md"
      provides: "Factory leaf: 5-section contract; the extra link target for Encapsulate Classes with Factory + Move Creation Knowledge to Factory (GOF-03), NOT Factory Method"
      contains: "## Related patterns"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md"
      via: "the Wave-3 linkified Kerievsky GoF pattern: Factory token resolves into this leaf (GOF-03; NOT factory-method.md)"
      pattern: "extra-patterns-catalog/factory\\.md#"
---

<objective>
Author the 5 Tier-1 extra patterns -- Null Object, Factory, Creation Method, Composed Method,
Collecting Parameter -- as per-pattern catalog leaves on the SAME locked 5-section contract as the
GoF leaves, via the clean-room blind-author -> deterministic-checks -> oracle-reviewer-gate ->
revise-BLIND -> converge loop, sourced against .oracle/refactoring-to-patterns/. These are the link
TARGETS the Wave-3 Kerievsky linkify resolves into for the 6 extra-pattern tokens (GOF-03), and the
Factory leaf is the trap-avoidance target (Factory extra, NOT Factory Method GoF-23).

Purpose: fill XTR-01 (the 5 extra leaves) + provide the extra-catalog link targets for GOF-03.
File-disjoint by owning only these 5 leaf files.

Output: the 5 extra-pattern leaf files under references/extra-patterns-catalog/ (the README rows +
SKILL.md are finalized in Wave 3, 08.1-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-LEARNINGS.md
@plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md
@plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-collecting-parameter.md

<interfaces>
CLEAN-ROOM FIREWALL (D-12): the main context NEVER opens any file under .oracle/ -- `ls` for names
only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` gates drafted leaves. Input: DRAFT_PATHS, SOURCE = ".oracle/refactoring-to-patterns/index.md"
  + the relevant chapter number/topic per pattern (Null Object -> Ch.9 Protection; Factory + Creation
  Method -> Ch.6 Creation; Composed Method -> Ch.7 Simplification; Collecting Parameter -> Ch.10
  Accumulation -- index.md + chapter number/topic, NEVER a book chapter filename), CONTENT_TYPE
  "pattern-leaf", SCOPE = each leaf's pattern, the pattern-catalog per-axis anchors (intent /
  applicability / consequences / example / related patterns + near-verbatim). Returns
  pass|revise|blocked|error.
- `oracle` for open-ended chapter-membership / participant-vocabulary lookups.
Sub-batch all 5 in one reviewer call (within the ~6-8 guidance) with per-leaf SCOPE; cap ~3 rounds;
escalate non-convergence to Task 2. Fan-out guard: do NOT persist a full ordered name+chapter map.

The 5 slugs: null-object, factory, creation-method, composed-method, collecting-parameter. Factory here
is the extra pattern (Kerievsky's own; NOT GoF-23 Factory Method) -- keep the H1 exactly "Factory" so the
Wave-3 linkify's set-membership resolves Encapsulate Classes with Factory + Move Creation Knowledge to
Factory into extra-patterns-catalog/factory.md, never factory-method.md (Pitfall 1).

The 5-section leaf contract (D-04, IDENTICAL to the GoF leaves) -- see 08.1-PATTERNS.md leaf section for
the exact skeleton (## Intent, ## Applicability with the FIRST LINE as the index selector,
## Consequences, ## Example ONE ```ts fence, ## Related patterns).

Collecting Parameter's Consequences (D-07, oracle-flagged risk): the source's explicit liabilities list
is empty -- assemble its downside from Kerievsky's surrounding prose + the Visitor contrast (it ties the
accumulation to specific classes/interfaces and reads poorly across heterogeneous objects, where Visitor
is preferred). Document that, do NOT fabricate a liability beyond what the prose supports.

If any extra leaf's ## Example or ## Related patterns cites a Fowler refactoring, pass the 1st-ed ->
2nd-ed Fowler-name mapping to the reviewer (Extract Method -> Extract Function; Move Method -> Move
Function; Inline Method -> Inline Function; Extract Interface -> Change Function Declaration or Extract
Superclass) or it false-flags a composed-primitive demanding a nonexistent 1st-ed-named leaf (08-LEARNINGS).

These 5 leaves are link targets: the Wave-3 Kerievsky linkify resolves into null-object.md / factory.md /
creation-method.md / composed-method.md / collecting-parameter.md. gof<->extra + extra<->extra ## Related
patterns links to leaves authored by parallel Wave-2 plans will NOT resolve until Wave 3 -- do NOT gate on
check-extra-patterns 5/5 identity being clean of missing siblings or on check-crossrefs; the per-plan gate
is the per-leaf contract (rg 5 sections) + extract-samples (tsc) + check-hygiene + oracle-reviewer pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 Tier-1 extra-pattern leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/null-object.md, plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md, plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/creation-method.md, plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/composed-method.md, plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/collecting-parameter.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- leaf + thin-index model)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Canonical per-axis anchors", "Driver responsibilities")
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-LEARNINGS.md (deterministic layer before reviewer calls; too_close_to_source re-domain; the 1st-ed->2nd-ed Fowler-name mapping so composed-primitive citations do not false-flag)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-02, D-04, D-07, D-12, D-13)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (the D-04 5-section leaf skeleton -- reused verbatim by the extra leaves)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md (Oracle-Loop Application S5; Collecting Parameter Consequences risk; Pitfall 1 Factory vs Factory Method; Pitfall 3 too_close example)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md (the Kerievsky refactoring that reaches Null Object -- for Related-patterns aptness)
    - .oracle/ (ls ONLY for filenames -- NEVER open any file under .oracle/ from this context)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not). Do NOT
    open any file under .oracle/ from this context. Run this loop INLINE in the main context (D-13) --
    NOT via gsd-executor, NOT in a worktree.

    Author the 5 extra-pattern leaves (one file per pattern, kebab-slug == filename) via the clean-room
    loop:
    (1) AUTHOR BLIND in the LOCKED 5-section contract (identical to the GoF leaves): single # <Pattern>
    H1 (optional leading "Also known as:"), ## Intent, ## Applicability (FIRST LINE = a standalone
    one-sentence selector, mirrored verbatim into the Wave-3 README row), ## Consequences, ## Example
    (ONE tsc --strict-clean ```ts fence, pure -- no DOM/console; identifiers + comments name the
    pattern's participants and wire their collaboration; choose a DELIBERATELY DISTINCT domain, NOT the
    book's illustrations -- Pitfall 3), ## Related patterns. Keep the Factory H1 exactly "Factory" (the
    extra pattern; NOT Factory Method). For Collecting Parameter, assemble the ## Consequences downside
    from the source's surrounding prose + the Visitor contrast per D-07 -- do NOT fabricate a liability
    beyond that. ASCII only; no verbatim Kerievsky prose or code.
    (2) DETERMINISTIC LAYER FIRST (D-12): run extract-samples (tsc --strict) + check-hygiene + the
    per-leaf section rg checks over the 5 leaves. Fix any compile / ASCII / missing-section defect BLIND.
    Only once this layer is green do you spend an oracle-reviewer call. (Do NOT gate on
    check-extra-patterns 5/5 being clean of missing siblings or on check-crossrefs.)
    (3) GATE: spawn `oracle-reviewer` with DRAFT_PATHS, SOURCE ".oracle/refactoring-to-patterns/index.md"
    + the per-pattern chapter topic (Null Object Ch.9 / Factory + Creation Method Ch.6 / Composed Method
    Ch.7 / Collecting Parameter Ch.10), CONTENT_TYPE "pattern-leaf", per-leaf SCOPE, the pattern-catalog
    per-axis anchors. Pass the 1st-ed -> 2nd-ed Fowler-name mapping if any leaf cites a Fowler
    refactoring. Use `oracle` for open-ended lookups.
    (4) REVISE BLIND from the directives; on too_close_to_source, fully re-domain the example blind and
    re-gate.
    (5) ITERATE to convergence (every leaf `pass`, or owner-accepted `blocked`); cap rounds ~3 and
    escalate to Task 2. Fan-out guard.
    (6) RE-GATE post-oracle deterministic fixes: after applying directives, re-run the deterministic
    layer; if a fix CHANGES any leaf's ## Example code, re-run oracle-reviewer on that leaf. Commit on
    green. Do NOT edit any README, SKILL.md, or checker .mjs.
  </action>
  <acceptance_criteria>
    - All 5 leaf files exist; each has a single `# <Pattern>` H1 and the 5 sections (`rg -q "^## Intent" <leaf> && rg -q "^## Applicability" <leaf> && rg -q "^## Consequences" <leaf> && rg -q "^## Example" <leaf> && rg -q "^## Related patterns" <leaf>`) and >=1 ts fence.
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` exits 0 (every extra-pattern ## Example is tsc --strict-clean).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
    - factory.md carries the canonical "Factory" H1 so its slug round-trips as the extra target (`rg -q "^# Factory\s*$" plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md`); collecting-parameter.md's Consequences is present + populated.
    - Each leaf reached oracle-reviewer `pass` (or owner-accepted `blocked` via Task 2), recorded in the SUMMARY; no file under .oracle/ was opened; no README/SKILL.md/checker edited.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "^# Factory\s*$" plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md && for f in null-object factory creation-method composed-method collecting-parameter; do rg -q "^## Intent" plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/$f.md && rg -q "^## Consequences" plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/$f.md && rg -q "^## Related patterns" plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/$f.md || exit 1; done</automated>
  </verify>
  <done>All 5 Tier-1 extra-pattern leaves exist as contract-complete files on the D-04 5-section contract (Factory H1 exactly "Factory" so its slug is the extra target, not factory-method); Collecting Parameter's Consequences downside is assembled from prose + the Visitor contrast (not fabricated); every ## Example is tsc --strict-clean and behavior/near-verbatim-clean; each leaf is oracle-reviewer pass (or owner-accepted blocked); no README/SKILL.md/checker touched; no .oracle/ file opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner escalation for non-converging / blocked extra-pattern leaves (conditional)</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Driver responsibilities" -- loop bound + oscillation guard + owner escalation)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-12 escalation path; D-07 Collecting Parameter Consequences risk)
  </read_first>
  <decision>How to resolve any extra-pattern leaf the oracle-reviewer loop cannot converge within ~3 rounds, returns as a blocked ambiguity, or whose Consequences the source cannot settle (Collecting Parameter's empty liabilities list)?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or oscillate;
    Collecting Parameter's empty explicit-liabilities list is the flagged risk (D-07). The driver caps
    rounds (~3) and escalates that ENTRY to the owner via AskUserQuestion. Fires ONLY for entries that do
    not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked leaf as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale (esp. the assembled Collecting Parameter downside)</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (intent / applicability / the Collecting Parameter downside framing / example participants), then re-run the loop once</name>
      <pros>Resolves a genuine fidelity gap the reviewer could not phrase without leaking source prose</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked, or whose Consequences
    downside the source leaves ambiguous): open an AskUserQuestion with the entry, the reviewer's
    ambiguities/unable-to-verify axes (own words), and the two options; apply the choice; re-run the
    deterministic layer; record it in the SUMMARY. Do NOT reveal or request verbatim source prose.
  </action>
  <acceptance_criteria>
    - Every extra-pattern leaf is oracle-reviewer `pass` or owner-adjudicated; each escalation (or a no-op note if none) is recorded in 08.1-06-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every extra-pattern leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-to-patterns/` (owner full-text Kerievsky book, git-ignored) -> authored public-repo leaves | copyrighted Kerievsky expression + code must never cross; the ONLY crossing is via the oracle/oracle-reviewer subagents (isolated context, own-words verdicts, no prose/code/paths) |
| authored leaves -> public GitHub repo | world-readable (IP + hygiene surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Information Disclosure / legal (DST-04) | authored leaf prose + ## Example code | mitigate | Clean-room firewall: main context NEVER reads .oracle/; authored BLIND with distinct example domains; oracle-reviewer full-strength near-verbatim gate; check-hygiene no-verbatim WARN + Phase-10 scan |
| T-08.1-04 | Tampering (fabricated / unfaithful content) | intent / applicability / consequences (esp. the assembled Collecting Parameter downside, D-07) / example / related-patterns fidelity | mitigate | oracle-reviewer fidelity gate + converge loop; the assembled downside is documented not fabricated; driver caps rounds (~3) and escalates to owner (Task 2) |
| T-08.1-06 | Tampering | a resolvable-but-wrong ## Related patterns sibling; the Factory / Factory Method mix-up | mitigate | oracle-reviewer related-patterns aptness axis + the canonical "Factory" H1 so the Wave-3 linkify resolves to extra-patterns-catalog/factory.md + check-crossrefs resolution at the Wave-3 gate |
| T-08.1-05 | Information Disclosure / firewall | main context accidentally reading .oracle/ prose (incl. fan-out aggregation) | mitigate | firewall discipline (ls only); INLINE main-context orchestration; driver dedupes/caps, does NOT persist a full ordered name+chapter map |
| T-08.1-02 | Information Disclosure | work email in a committed leaf | mitigate | check-hygiene work-email allowlist hard-fail; public contact larsbrinknielsen@gmail.com only |
| T-08.1-03 | Tampering (integrity) | non-ASCII / mojibake in a leaf | mitigate | check-hygiene ASCII hard-fail; ASCII-only authoring |

No UI, no network, no secrets, no new packages. Nothing rises to the block_on: high bar.
</threat_model>

<verification>
- extract-samples (tsc --strict) GREEN over all 5 extra-pattern examples; check-hygiene GREEN; each leaf has the D-04 5-section contract reused verbatim.
- factory.md carries the canonical "Factory" H1 (extra target, not factory-method); Collecting Parameter's Consequences downside is assembled from prose + the Visitor contrast (not fabricated).
- Every extra-pattern leaf reached oracle-reviewer pass (or owner-accepted blocked); README rows + SKILL.md + checkers untouched; no .oracle/ file opened by this context.
</verification>

<success_criteria>
- All 5 Tier-1 extra patterns are contract-complete, oracle-converged, tsc --strict-clean leaves on the SAME 5-section contract (XTR-01), Kerievsky-grounded; NO Tier-2 patterns authored.
- These leaves are valid link targets for the Wave-3 Kerievsky linkify's 6 extra-pattern tokens (GOF-03 target existence, incl. the Factory / Factory Method disambiguation); the plan owns only these 5 leaf files (file-disjoint from sibling Wave-2 plans).
</success_criteria>

<output>
Create `.planning/phases/08.1-gof-design-patterns-catalog/08.1-06-SUMMARY.md` when done, recording the confirmed extra-pattern membership + any oracle name/slug corrections vs the locked names, per-leaf round counts, the settled Collecting Parameter Consequences downside, and any owner escalations.
</output>
