---
context: default
task: milestone-close-inherited
total_tasks: 2
status: ready
last_updated: 2026-07-16
---

# Where we are

Quick task 260716-oby (Loops/pipeline few-shot TS examples) is COMPLETE and RESOLVED.

- Mandatory review gate: PASS (2 subagents, 1 unbiased + 1 primed; 0 Critical / 0 Important).
- SHIPPED on teaching merit: commit ad56c21 (replaced the WIP pause commit).
- Held-out recognition eval (user-approved metered): EDITED (5 pairs) vs PREEDIT (1 pair) on held-out
  `getTypesOfSchema`, invoke_skill apply k=3 via `--plugin-dir` leaf toggle -> **idiom_pattern
  0/3 = 0/3, NULL lift, zero regression**. All 6 runs relocated the imperative loops into helpers;
  none used flatMap or map/filter into a Set. Root cause = recognition ceiling BEFORE routing (the
  Loops leaf never opens, so leaf examples can't reach the failure). 5th probe on the output-warrant
  axis -> recognition lever CLOSED. See heldout/RESULTS-oby.md. angular-cli restored pristine.

## Remaining (inherited milestone close)
1. User runs `/reload-plugins` to make the shipped edit live (Claude cannot; committed != live).
2. Milestone close for lz-tdd@0.0.2 (all phases 6-14 complete; all queued quick tasks resolved):
   `/gsd-audit-milestone lz-tdd@0.0.2` then `/gsd-complete-milestone lz-tdd@0.0.2`.
   Milestone-audit note (carried): the skill's gaps closed while base Opus 4.8@high is ALSO
   catalog-grade -- null applied-output delta across FIVE probes; the skill's robust value is
   auto-trigger (proven) + a narrow reference/direction-table edge.

## Anti-patterns
- Do NOT keep editing skill content to chase accumulation-loop recognition -- 5 nulls; it is a model
  recall ceiling, not a catalog/prose gap.
