---
phase: 17-assertion-design-stance-router-ts-vitest-mechanics
plan: 06
type: execute
wave: 3
depends_on: [17-02, 17-03, 17-04, 17-05]
files_modified: []
autonomous: true
requirements: [ASRT-01, ASRT-02, ASRT-03, RTR-01, RTR-03, VIT-01, VIT-02, ANTI-01, ANTI-02]
must_haves:
  truths:
    - "the full battery is GREEN on the merged tree: check-red-references exits 0 (all ten entries PASS incl. the flipped assertions slice, the six Phase-17 leaves, and principle-backing), extract-samples typecheck exits 0 (every fence tsc --strict clean), check-hygiene exits 0 (ASCII + work-email allowlist-inversion + no-verbatim)"
    - "claude plugin validate . exits 0"
    - "the four Phase-18 co-edit deferral markers remain (three-laws Phase-18 guard, assertions-slice F.I.R.S.T.-baseline marker, vitest LAW-02 marker, principle-backing Three-Laws/seam marker) -- the co-edit boundary held"
    - "the three OWNED surfaces (F.I.R.S.T. in the assertions slice, the Metz message matrix, the Cooper anti-pattern) pass the orchestrator-run oracle-reviewer converge-to-clean; skill-reviewer PASSES with >= 1 unbiased from-scratch reviewer"
  artifacts: []
  key_links:
    - "orchestrator-run oracle-reviewer (owned surfaces) + skill-reviewer gates -- NOT executor-spawned"
---

<objective>
Phase gate for Phase 17: confirm the full battery is GREEN on the merged tree, `claude plugin
validate .` exits 0, and the orchestrator-run review gates (oracle-reviewer on the three owned
surfaces + skill-reviewer with >= 1 unbiased reviewer) PASS. No content is written here -- this plan
verifies the phase deliverables and drives the gates.

Purpose: prove every Phase-17 requirement's machine signal is GREEN and the DST-04 owned-surface
+ skill-quality review gates pass before the phase is marked complete.
Output: an all-GREEN battery + PASS verdicts (no file changes unless a gate surfaces a fix, which
routes to a gap-closure plan).

CRITICAL -- executor vs orchestrator split (the gsd-executor cannot spawn subagents; its tools are
Read/Write/Edit/Bash/Grep/Glob/Skill only, NO Agent/Task):
- Task 1 (executor-runnable): run the full deterministic battery + `claude plugin validate .` and
  assert all GREEN / exit 0. The executor CAN and MUST do this.
- The oracle-reviewer and skill-reviewer gates below are ORCHESTRATOR-DRIVEN. The plan-phase /
  execute-phase orchestrator spawns them AFTER the executor returns; they are NOT executor tasks.
  If a gate flags an issue, the orchestrator opens a gap-closure edit (re-author blind + re-gate),
  NOT the executor inline.

Decisions implemented (decision-coverage):
- **D-01:** the orchestrator runs oracle-reviewer converge-to-clean on the three OWNED surfaces
  (F.I.R.S.T., the Metz matrix, the Cooper anti-pattern); main context never reads `.oracle/`.
- **D-14:** the no-verbatim scan (check-hygiene) is GREEN across the tree; owned surfaces
  additionally clear oracle-reviewer -- the phase gate confirms both.
- **D-15:** the co-edit boundary held -- all Phase-18 deferral markers remain (verified by the
  content gate's deferral guards passing).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-VALIDATION.md
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-CONTEXT.md
@.claude/skills/lz-red-workspace/tools/check-red-references.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full battery + claude plugin validate on the merged tree</name>
  <files></files>
  <read_first>
    - .planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-VALIDATION.md (the Full suite command + global gates)
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs (as extended in Plan 17-01 -- confirm all ten entries now PASS)
  </read_first>
  <action>
    On the merged Wave-2 tree, run the three deterministic gates and the first-party validator, and
    assert each is GREEN / exit 0:
    1. content gate: `npm --prefix .claude/skills/lz-red-workspace run check` exits 0 -- every entry
       PASSES (the flipped assertions slice, the six Phase-17 leaves, principle-backing), the STR/SEL/
       NAME rows still PASS, the seams cross-link guard PASSES, and every Phase-18 deferral marker
       remains (three-laws, assertions-slice, vitest, principle-backing).
    2. tsc gate: `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 -- every
       extracted Vitest fence is tsc --strict --noEmit clean.
    3. hygiene: `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 -- ASCII +
       work-email allowlist-inversion + no-verbatim across the lz-red tree.
    4. `claude plugin validate .` exits 0.
    Confirm the maintainer identity: `git config user.email` is the public gmail (public-repo hygiene)
    before any commit. Do NOT spawn agents (the executor cannot); if any gate is RED, report the exact
    failing rows so the orchestrator can route a gap-closure edit -- do not silently patch content.
  </action>
  <acceptance_criteria>
    - `npm --prefix .claude/skills/lz-red-workspace run check` exits 0 (all entries PASS; deferral
      markers intact).
    - `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0.
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
    - `claude plugin validate .` exits 0.
    - `git config user.email` is the public maintainer gmail; no work-email token anywhere in the diff.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck && npm --prefix .claude/skills/lz-red-workspace run check && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && claude plugin validate .</automated>
  </verify>
  <done>All four commands exit 0 on the merged tree; the content gate flips from the Wave-0 RED baseline to fully GREEN; maintainer identity is the public gmail.</done>
</task>

</tasks>

## Orchestrator-run gates (NOT executor tasks -- the executor lacks Agent/Task tools)

After Task 1 returns GREEN, the plan-phase / execute-phase orchestrator runs these review gates
(mirroring the Phase-16 16-03 finalize and the 0.0.2 clean-room precedent). They are documented here
so the phase-gate checklist is explicit; they are NOT executor tasks.

1. **oracle-reviewer converge-to-clean (owned surfaces, 3-round cap):** the orchestrator spawns the
   oracle-reviewer on the three OWNED Phase-17 surfaces -- F.I.R.S.T. in `test-structure-and-assertions.md`
   (Clean Code Ch.9), the Metz query/command matrix in `testing-stance/message-matrix.md` (99 Bottles
   2e JS), and the Cooper over-mock / test-per-class anti-pattern in `anti-patterns.md` (two talk
   transcripts; the oracle agents chunk-read to EOF). Verdict must be all-PASS (own-words, no
   near-verbatim). If flagged, the orchestrator opens a gap-closure edit: the executor re-authors the
   flagged surface BLIND (never reading `.oracle/`) and re-runs the deterministic gates; re-gate within
   the 3-round cap. Main context never reads `.oracle/` prose.
2. **skill-reviewer PASS (>= 1 unbiased from-scratch):** the orchestrator spawns the skill-reviewer on
   the lz-red skill (>= 1 reviewer with a neutral from-scratch brief). Apply any minor leak/accuracy
   fixes it returns, then re-run the deterministic battery.
3. **Phase-gate sign-off:** full battery GREEN + `claude plugin validate .` exit 0 + oracle-reviewer
   all-PASS + skill-reviewer PASS -> the phase goal is met. This precedes `/gsd:verify-work` and the
   downstream secure / validate / extract-learnings sequence.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owned book/talk prose (.oracle) -> shipped tree | the three owned surfaces must be own-words; the oracle-reviewer is the semantic backstop the no-verbatim scan cannot provide. |
| author identity -> committed content | maintainer work-email / domain must never appear; author/committer = public gmail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure (copyright, DST-04) | the 3 owned surfaces | high | mitigate | Deterministic no-verbatim scan (check-hygiene) GREEN + orchestrator oracle-reviewer converge-to-clean all-PASS; gap-closure loop on any flag. |
| T-17-02 | Information Disclosure (PII) | committed tree + commit identity | high | mitigate | check-hygiene allowlist-inversion exit 0; `git config user.email` = public gmail confirmed before commit. |
| T-17-03 | Tampering (mojibake) | ASCII shipped tree | medium | mitigate | check-hygiene ASCII byte scan exit 0. |
| T-17-04 | Tampering (supply chain) | shipped plugins/lz-tdd tree | medium | mitigate | claude plugin validate . exit 0; no dependency added to plugins/lz-tdd (deps stay in the dev-only workspace); git diff confirms. |
</threat_model>

<verification>
- Full suite GREEN: `run typecheck` + `run check` + `check-hygiene.mjs` all exit 0.
- `claude plugin validate .` exits 0.
- Orchestrator: oracle-reviewer all-PASS on the 3 owned surfaces + skill-reviewer PASS (>= 1 unbiased).
- All Phase-18 co-edit deferral markers remain (the content gate's deferral guards pass).
</verification>

<success_criteria>
- Every Phase-17 requirement (ASRT-01/02/03, RTR-01/03, VIT-01/02, ANTI-01/02) has a GREEN machine signal.
- The three owned surfaces pass oracle-reviewer; skill-reviewer PASSES; claude plugin validate . exit 0.
- The co-edit boundary held (Phase-18 markers intact); maintainer identity = public gmail; no work-email leak.
</success_criteria>

## Artifacts this phase produces (Plan 17-06)

- No file changes (gate only). This plan confirms the artifacts filled in Plans 17-02..17-05 and the
  instrument extended in 17-01 are all GREEN, and drives the orchestrator review gates.
- Any gate-surfaced fix routes to a gap-closure edit (not authored in this plan).

<output>
Create `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-06-SUMMARY.md` when done.
</output>
