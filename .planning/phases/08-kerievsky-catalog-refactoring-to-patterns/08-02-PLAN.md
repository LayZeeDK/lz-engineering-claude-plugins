---
phase: 08-kerievsky-catalog-refactoring-to-patterns
plan: 02
type: execute
wave: 2
depends_on: [08-01]
branching_strategy: none
tags: [kerievsky-catalog, refactoring, skill-authoring, oracle-clean-room, typescript, creation, de-patterning]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-constructors-with-creation-methods.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-creation-knowledge-to-factory.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-polymorphic-creation-with-factory-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-composite-with-builder.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md
autonomous: false
requirements: [KRV-01, KRV-02, KRV-04]
must_haves:
  truths:
    - "D-01: the Ch.6 Creation refactorings are authored as per-refactoring leaf files kerievsky-catalog/<slug>.md behind the thin index, mirroring the LOCKED Fowler per-refactoring-leaf model; SKILL.md is NOT re-touched"
    - "D-02: each Creation leaf carries the LOCKED Fowler contract (single # <Name> H1 anchor + Use when: + ## Motivation (intent leads) + ## Mechanics + ## Example + optional ## Watch for) PLUS the three Kerievsky field labels in lockstep with check-kerievsky: 'Composed Fowler primitive(s):' (>=1 ../fowler-catalog/<slug>.md#<slug> cross-link), 'Direction:', 'GoF pattern:'"
    - "D-03: every leaf carries a Direction value; the 2 Ch.6 de-patterning cases -- Encapsulate Composite with Builder (Direction: Away, away from Composite) and Inline Singleton (Direction: Away, away from Singleton) -- carry an explicit 'refactor AWAY from <pattern>' callout"
    - "D-04: each leaf's 'GoF pattern:' field names the target pattern for vocabulary only (Factory / Factory Method / Builder / Singleton; Replace Constructors with Creation Methods targets the non-GoF Creation Method) with NO reproduced GoF prose or code (DST-04)"
    - "D-06: each example is re-rendered from the book's Java into ONE compact original TS before/after (5-15 lines/side, hard cap 2), in a domain UNRELATED to the source with original identifiers to dodge too_close_to_source, tsc --strict-clean via extract-samples (pure, no DOM/console)"
    - "D-07: each leaf is authored BLIND, gated by the oracle-reviewer subagent over .oracle/refactoring-to-patterns/index.md scoped to 'Ch.6 Creation', revised BLIND to convergence; the main context NEVER reads .oracle/ (ls for names only)"
    - "D-08: this plan runs INLINE in the MAIN context (Agent + AskUserQuestion tools), NOT via gsd-executor and NOT in a worktree (branching_strategy: none); the oracle agents are read-only so there is no file conflict"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md"
      provides: "Inline Singleton leaf (Direction: Away, away from Singleton -- de-patterning case)"
      contains: "Direction:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-composite-with-builder.md"
      provides: "Encapsulate Composite with Builder leaf (Direction: Away, away from Composite -- de-patterning case)"
      contains: "Direction:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-polymorphic-creation-with-factory-method.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-constructor-with-factory-function.md"
      via: "composed Fowler primitive cross-link (resolving H1 anchor)"
      pattern: "fowler-catalog/[a-z-]+\\.md#"
---

<objective>
Author the Ch.6 Creation chapter (6 Kerievsky pattern-directed refactorings) as per-refactoring
catalog leaves using the clean-room blind-author -> oracle-reviewer-gate -> converge loop.
Includes 2 of the 3 named de-patterning (Away) cases: Encapsulate Composite with Builder (away
from Composite) and Inline Singleton (away from Singleton).

Purpose: fill KRV-01/02/04 for the Creation chapter with contract-complete, oracle-converged,
tsc --strict-clean leaves that carry the three Kerievsky fields. File-disjoint by owning only
this chapter's leaf files.

Output: the 6 Ch.6 Creation leaf files under references/kerievsky-catalog/ (the thin index
README.md is finalized later, in 08-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-constructor-with-factory-function.md

<interfaces>
CLEAN-ROOM FIREWALL (D-07): the executor/main context NEVER opens any file under .oracle/ -- `ls`
for names only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` (Task/subagent) gates a drafted leaf. Input: DRAFT_PATHS (this chapter's
  leaves), SOURCE = ".oracle/refactoring-to-patterns/index.md" + "Ch.6 -- Creation" (index.md +
  chapter number/topic; NEVER a book chapter filename), CONTENT_TYPE = "refactoring-leaf", SCOPE =
  each leaf's refactoring, canonical per-axis anchors from 07-ORACLE-MODEL PLUS the pattern-directed
  additions (direction correctness; composed-primitive aptness -- are the cited Fowler primitives
  the ones the source actually composes?; target-pattern correctness). Returns per-draft verdicts
  pass|revise|blocked|error.
- `oracle` (Task/subagent) for open-ended chapter-membership / composed-primitive lookups.
Sub-batch the 6 leaves in one reviewer call (within the ~6-8 guidance); cap ~3 rounds; escalate
non-convergence / ambiguities to Task 2. Apply the fan-out guard (do NOT persist a full ordered
name+chapter map).

Ch.6 Creation members + orientation (ASSUMED -- oracle settles direction/GoF/composed-primitives):
  1. Replace Constructors with Creation Methods -- GoF: Creation Method (Kerievsky's term, non-GoF);
     Direction Towards; likely composes replace-constructor-with-factory-function,
     change-function-declaration.
  2. Move Creation Knowledge to Factory -- GoF: Factory; Towards; move-function, extract-class,
     move-field.
  3. Encapsulate Classes with Factory -- GoF: Factory; Towards; move-function,
     change-function-declaration, hide-delegate.
  4. Introduce Polymorphic Creation with Factory Method -- GoF: Factory Method; Towards;
     pull-up-method, extract-function, replace-constructor-with-factory-function.
  5. Encapsulate Composite with Builder -- GoF: Builder; Direction AWAY (from Composite, D-03);
     extract-class, move-function, hide-delegate.
  6. Inline Singleton -- GoF: Singleton; Direction AWAY (from Singleton, D-03); inline-function,
     move-function, move-field, change-function-declaration.

Leaf field LABELS (lockstep with check-kerievsky -- Pitfall 4): "Composed Fowler primitive(s):",
"Direction:", "GoF pattern:". Composed-primitive links target ../fowler-catalog/<slug>.md#<slug>
(the Fowler leaf H1 anchor -- all 62 Fowler leaves exist and each H1 IS its own #slug anchor).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Ch.6 Creation leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-constructors-with-creation-methods.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-creation-knowledge-to-factory.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-polymorphic-creation-with-factory-method.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-composite-with-builder.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- refactoring leaf shape + overflow rule)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Content-fidelity discipline", "Canonical per-axis anchors", "Driver responsibilities")
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md (blind drafts fold sub-steps; too_close_to_source re-domain; no-DOM tsc; cross-ref aptness)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-01, D-02, D-03, D-04, D-06, D-07, D-08)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md (leaf skeleton + the 3-field placement + Away callout)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md (leaf-contract exemplar to mirror)
    - .oracle/ (ls ONLY for filenames -- NEVER open any file under .oracle/ from this context)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not). Do
    NOT open any file under .oracle/ from this context. Run this loop INLINE in the main context
    (D-08) -- NOT via gsd-executor, NOT in a worktree.

    Author the 6 Ch.6 Creation leaves (one file per refactoring, kebab-case slug = filename) via
    the core clean-room loop:
    (1) AUTHOR BLIND from public knowledge + the interfaces orientation, in the LOCKED leaf format:
    single `# <Name>` H1 (the #slug cross-link anchor), one-line `Use when:`, `## Motivation`
    (intent leads it for the pattern-directed framing, 2-4 sentences, original words),
    `## Mechanics` (4-8 verb-first numbered steps, original words, naming the test/compile
    checkpoint inline), `## Example` (ONE compact tsc --strict-clean TS before->after, 5-15
    lines/side, hard cap 2; pure -- no DOM/console; original domain + identifiers to dodge
    too_close_to_source), optional `## Watch for`. THEN add the three Kerievsky field lines with
    the EXACT labels: `Composed Fowler primitive(s):` (>=1 link to ../fowler-catalog/<slug>.md#<slug>),
    `Direction:` (To/Towards/Away; default Towards), `GoF pattern:` (name only). For the 2
    de-patterning leaves, set `Direction: Away` AND add an explicit "refactor AWAY from Composite"
    (encapsulate-composite-with-builder) / "refactor AWAY from Singleton" (inline-singleton)
    callout in the body. A refactoring is a design change, not a type error -- both before AND after
    must compile. ASCII only.
    (2) DETERMINISTIC LAYER FIRST (D-07 -- run the cheap checks BEFORE spending reviewer calls): run
    extract-samples (tsc --strict) + check-hygiene + the field-presence rg checks below over the
    authored leaves; fix any compile / ASCII / missing-field / malformed-cross-link defect BLIND.
    Only once this layer is green do you spend an oracle-reviewer call.
    (3) GATE: spawn `oracle-reviewer` with DRAFT_PATHS (the 6 leaves), SOURCE
    ".oracle/refactoring-to-patterns/index.md" + "Ch.6 -- Creation", CONTENT_TYPE "refactoring-leaf",
    SCOPE (each leaf's refactoring), the canonical per-axis anchors PLUS direction /
    composed-primitive-aptness / target-pattern axes. Use `oracle` for open-ended lookups.
    (4) REVISE BLIND from the directives (never opening the source). On a too_close_to_source
    verdict, fully re-domain that example blind and re-gate.
    (5) ITERATE to convergence (every leaf `pass`, or owner-accepted `blocked`); DRIVER caps rounds
    at ~3 and escalates a non-converging/blocked leaf to Task 2. Apply the fan-out guard.
    (6) RE-GATE post-oracle deterministic fixes: after applying reviewer directives, re-run the
    deterministic layer; if a post-oracle fix CHANGES any leaf's example before/after code, re-run
    oracle-reviewer on that leaf so no example ships un-re-reviewed for behavior-preservation /
    near-verbatim. Commit on green. Do NOT edit kerievsky-catalog/README.md or any checker .mjs
    (owned by 08-01 / 08-06); the README-mirror + full check-kerievsky + crossrefs run at the 08-06 gate.
  </action>
  <acceptance_criteria>
    - All 6 Ch.6 leaf files exist; each has a single `# <Name>` H1, a `Use when:` line, `## Motivation`, `## Mechanics`, `## Example` with >=1 ts fence, and the three field labels: `rg -q "^Composed Fowler primitive\(s\):" <leaf> && rg -q "^Direction:" <leaf> && rg -q "^GoF pattern:" <leaf>` passes for each.
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` exits 0 (every Ch.6 example is tsc --strict-clean).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII-only, no work-email; no-verbatim WARN only).
    - The 2 Away cases carry Direction: Away + a callout: `rg -qi "away from Singleton" .../inline-singleton.md && rg -qi "away from Composite" .../encapsulate-composite-with-builder.md`.
    - Every leaf's `Composed Fowler primitive(s):` line links at least one ../fowler-catalog/<slug>.md#<slug> target that exists on disk.
    - Each leaf reached oracle-reviewer `pass` (or owner-accepted `blocked` via Task 2), recorded in the SUMMARY; no file under .oracle/ was opened by this context.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "^Direction:" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md && rg -qi "away from Singleton" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md && rg -q "^GoF pattern:" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md && test $(ls plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/*.md | rg -v README | wc -l) -ge 6</automated>
  </verify>
  <done>All 6 Ch.6 Creation leaves exist as contract-complete per-refactoring files with the three Kerievsky fields; the 2 Away cases are Direction: Away + flagged; every example is tsc --strict-clean and behavior-preserving; each oracle-reviewer verdict is pass (or owner-accepted blocked); check-hygiene green; README.md and checkers untouched; no .oracle/ file opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner escalation for non-converging / blocked Ch.6 leaves (conditional)</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Driver responsibilities" -- loop bound + oscillation guard + owner escalation)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-04 GoF AskUserQuestion fallback; D-07 escalation path)
  </read_first>
  <decision>How to resolve any Ch.6 leaf the oracle-reviewer loop cannot converge within ~3 rounds, returns as a blocked ambiguity, or whose GoF target the Kerievsky oracle cannot settle (D-04 fallback)?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate. The driver caps rounds (~3) and escalates that ENTRY to the owner via
    AskUserQuestion. GoF is NOT in .oracle/, so an unresolvable GoF target for a specific leaf is
    ALSO an owner question (D-04 fallback -- for a specific target only, not pattern names
    generally). Fires ONLY for entries that do not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked entry as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (mechanics / direction / GoF target), then re-run the loop once</name>
      <pros>Resolves a genuine fidelity or GoF-target gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked, or have an
    unresolvable GoF target): open an AskUserQuestion with the entry, the reviewer's
    ambiguities/unable-to-verify axes (own words), and the two options; apply the choice; re-run the
    deterministic layer; record it in the SUMMARY. Do NOT reveal or request verbatim source prose.
  </action>
  <acceptance_criteria>
    - Every Ch.6 leaf is oracle-reviewer `pass` or owner-adjudicated; each escalation (or a no-op note if none) is recorded in 08-02-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every Ch.6 leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-to-patterns/` (owner full-text book, git-ignored) -> authored public-repo leaves | copyrighted Kerievsky expression must never cross; the ONLY crossing is via the oracle/oracle-reviewer subagents (isolated context, own-words verdicts, no prose/code/paths) |
| GoF (owner-held, NOT in .oracle/) -> authored leaves | only the pattern NAME (vocabulary) crosses, via the leaf's GoF pattern field; NO GoF prose or code (DST-04) |
| authored files -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure / legal (DST-04) | authored leaf prose + example code + GoF vocabulary | mitigate | Clean-room firewall: main context NEVER reads .oracle/; leaves authored BLIND; oracle-reviewer full-strength near-verbatim gate; GoF is name-only (no prose/code); check-hygiene no-verbatim WARN + Phase-10 scan |
| T-08-04 | Tampering (fabricated / unfaithful content) | mechanics / motivation / direction / GoF target / composed-primitive fidelity | mitigate | oracle-reviewer fidelity gate (+ direction / composed-primitive-aptness / target-pattern axes) + converge loop; driver caps rounds (~3) and escalates to owner (Task 2) |
| T-08-05 | Information Disclosure / firewall | main context accidentally reading .oracle/ prose (incl. fan-out aggregation) | mitigate | firewall discipline (ls only); INLINE main-context orchestration; driver dedupes/caps, does NOT persist a full ordered name+chapter map |
| T-08-06 | Tampering | a resolvable-but-wrong composed-Fowler cross-ref (a plausible-but-wrong sibling primitive) | mitigate | check-crossrefs (resolution, at 08-06 gate) + oracle-reviewer mechanics/composed-primitive-aptness axis (aptness) |
| T-08-02 | Information Disclosure | work email in a committed leaf | mitigate | check-hygiene work-email allowlist gate |
| T-08-03 | Tampering (integrity) | non-ASCII / mojibake in a leaf | mitigate | check-hygiene ASCII hard-fail; ASCII-only authoring |

No UI, no network, no secrets, no new packages. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- extract-samples (tsc --strict) GREEN; check-hygiene GREEN; each Ch.6 leaf has the full contract + the 3 Kerievsky fields; the 2 Away cases are Direction: Away + flagged.
- Every Ch.6 leaf reached oracle-reviewer pass (or owner-accepted blocked).
- kerievsky-catalog/README.md and checker .mjs files untouched; no .oracle/ file opened by this context.
</verification>

<success_criteria>
- All 6 Ch.6 Creation refactorings are contract-complete, oracle-converged, tsc --strict-clean leaves with the three Kerievsky fields (KRV-01/04) and correct Direction incl. the 2 Away cases (KRV-02).
- The plan owns only this chapter's leaf files (file-disjoint from sibling wave-2 plans); composed-Fowler cross-refs resolve at the 08-06 gate.
</success_criteria>

<output>
Create `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-02-SUMMARY.md` when done, recording the confirmed Ch.6 membership (including any oracle name/slug corrections vs the CITED names, for the 08-06 check-kerievsky NAMES reconciliation), per-leaf round counts, the settled directions/GoF targets, and any owner escalations.
</output>
