---
phase: 10-distribution-hygiene
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - plugins/lz-tdd/.claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [DST-01, DST-03]

must_haves:
  truths:
    - "plugins/lz-tdd/.claude-plugin/plugin.json version is 0.0.2 (was 0.0.1); its description names BOTH skills (lz-tpp AND lz-refactor); its keywords keep the existing 7 TDD terms and add refactoring vocabulary (at minimum refactoring, code-smells, design-patterns, gang-of-four); author.email / homepage / repository / license are unchanged (author.email stays the public gmail)"
    - ".claude-plugin/marketplace.json plugins[0].description names both skills (two-skill marketplace-listing wording); NO version field is added anywhere in marketplace.json (plugin.json is the sole version source); $schema / name / owner / source / category unchanged"
    - "README.md documents lz-refactor alongside lz-tpp: the lead paragraph no longer claims the plugin ships only lz-tpp (it frames the two-skill pair via the red-green-refactor seam -- lz-tpp at the green step, lz-refactor at the refactor step); the 'What this is' bullet list is a two-skill listing (adds an lz-refactor bullet mirroring the lz-tpp /lz-tdd:<skill> invocation shape); a new '## What lz-refactor does' section mirrors the '## What lz-tpp does' two-mode Coach/Reference shape; a refactoring primer + book sources (LINK ONLY) + a references/ pointer mirror the '## Transformation Priority Premise' block"
    - "README.md Installation block (the two /plugin commands) and the License block are UNCHANGED; the optional catalog-inventory line uses counts RE-VERIFIED against the live tree at write time (62 Fowler refactorings, 27 Kerievsky pattern-directed refactorings, 23 GoF + 5 extra patterns, 19 functional de-patterning idioms, 28 code smells) and does NOT imply a complete Beck or Feathers catalog"
    - "CHANGELOG.md gains a '## [lz-tdd@0.0.2] - <date>' entry ABOVE the 0.0.1 entry in the existing Keep a Changelog shape: a one-paragraph lead, an '### Added' list of bold-led bullets covering the lz-refactor skill (dual-mode coach + reference, /lz-tdd:lz-refactor) / the five reference catalogs / the unified smell taxonomy / the coach decision procedure / the three no-oracle principle-backing refs (tagged core-only), and a bottom link-ref [lz-tdd@0.0.2]: .../releases/tag/lz-tdd%400.0.2 (URL-encoded %40, tag not yet created)"
    - "All four edited files are ASCII-only and work-email-free; the new README primer, CHANGELOG entry, and manifest descriptions are ORIGINAL prose (link, don't inline) and pass the widened+hardened check-hygiene from 10-01"
  artifacts:
    - path: "plugins/lz-tdd/.claude-plugin/plugin.json"
      provides: "version 0.0.2 + two-skill description + refactoring keywords"
      contains: "0.0.2"
    - path: ".claude-plugin/marketplace.json"
      provides: "two-skill marketplace-listing description; no version field"
      contains: "lz-refactor"
    - path: "README.md"
      provides: "the lz-refactor section, two-skill lead + What-this-is, refactoring primer + sources + references pointer"
      contains: "## What lz-refactor does"
    - path: "CHANGELOG.md"
      provides: "the lz-tdd@0.0.2 Keep-a-Changelog entry"
      contains: "[lz-tdd@0.0.2]"
  key_links:
    - from: "README.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/"
      via: "the refactoring section points readers into references/ for the catalogs (link, don't inline)"
      pattern: "plugins/lz-tdd/skills/lz-refactor/references"
    - from: "CHANGELOG.md"
      to: "github.com/LayZeeDK/lz-engineering-claude-plugins/releases/tag/lz-tdd%400.0.2"
      via: "bottom link-ref mirroring the 0.0.1 %40-encoded release-tag URL"
      pattern: "releases/tag/lz-tdd%400.0.2"
  decisions: |
    - **D-05:** the README gains an lz-refactor section MIRRORING the lz-tpp shape (two-mode Coach/Reference block + original refactoring primer + book sources link-only + references/ pointer); the "What this is" bullets become a two-skill listing and the lead stops claiming the plugin ships only lz-tpp; section ordering + wording are Claude's discretion
    - **D-06:** link the sources, never inline them (carry Phase-4 D-02 forward) -- cite Fowler Refactoring 2nd ed + Kerievsky Refactoring to Patterns by title/author/publisher-or-ISBN link; point at references/; do NOT inline catalog entries, mechanics, smell tables, or examples; DST-04 applies to the README itself so the primer is ORIGINAL prose
    - **D-07:** the README MAY carry a one-line catalog inventory (counts + names are FACTS, not protected expression) RE-COUNTED against the live tree at write time; it MUST NOT imply a complete Beck or Feathers catalog (those three refs are no-oracle, high-confidence-core-only)
    - **D-08:** bump plugin.json version 0.0.1 -> 0.0.2 AND correct the two stale one-skill strings in the same file (description, keywords), plus correct marketplace.json plugins[0].description -- all three currently mis-describe a two-skill plugin; factual inaccuracy is FIX-in-phase
    - **D-09:** do NOT add a version field to marketplace.json (plugin.json wins silently, the marketplace value is masked, the validator warns); version lives in plugin.json only
    - **D-15:** add "## [lz-tdd@0.0.2] - <date>" in the existing Keep a Changelog shape mirroring the 0.0.1 entry (one-paragraph lead + "### Added" list + bottom link-ref); content = the lz-refactor skill, five reference catalogs, unified smell taxonomy, coach decision procedure, and three no-oracle principle-backing refs (tagged core-only, no complete Beck/Feathers catalog claimed); same link-don't-inline rule
    - **D-16:** the bottom link-ref points at .../releases/tag/lz-tdd%400.0.2 -- a tag that does NOT exist yet at write time (correct; matches what the 0.0.1 entry did when authored); cutting the tag/Release is NOT this phase
    - **D-17:** micro-decisions inside the locked boundaries land here -- README section ordering / tagline / value-prop wording / optional copy-paste usage snippet / minimal ASCII badges; the exact keywords list added to plugin.json; the exact CHANGELOG bullet granularity
---

<objective>
Author the Phase-10 content edits under the widened+hardened hygiene gate from 10-01: bump the manifest
metadata to a truthful two-skill 0.0.2 (DST-01/D-08/D-09), document lz-refactor in the README alongside
lz-tpp (DST-01/D-05/06/07), and add the lz-tdd@0.0.2 CHANGELOG entry (DST-03/D-15/16).

Purpose: plugin.json version is the update trigger -- an unchanged version makes /plugin update SKIP the
plugin, so existing installs never see lz-refactor; and the README/CHANGELOG/manifests all currently
describe a one-skill (TPP-only) plugin, which is a factual inaccuracy that must be corrected before the
public ship. Every edit mirrors an analog that already lives in the same file (10-PATTERNS.md).

Output: four edited files (plugin.json, marketplace.json, README.md, CHANGELOG.md), each ASCII-only,
work-email-free, original-prose, and passing the widened check-hygiene as it lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-distribution-hygiene/10-CONTEXT.md
@.planning/phases/10-distribution-hygiene/10-RESEARCH.md
@.planning/phases/10-distribution-hygiene/10-PATTERNS.md

Each edit is a diff-in-words against an intra-file analog: the README lz-tpp section (README:29-61),
the CHANGELOG 0.0.1 entry (CHANGELOG:8-32), and the current manifest fields -- all excerpted with exact
line refs in 10-PATTERNS.md. The live files carry the analog shapes directly (both were written under
the same DST-02 hygiene rules).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump plugin.json to 0.0.2 + two-skill descriptions + refactoring keywords; keep marketplace.json version-free (D-08, D-09)</name>
  <files>plugins/lz-tdd/.claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <read_first>
    - plugins/lz-tdd/.claude-plugin/plugin.json (the FILE being edited -- version at line 3 = "0.0.1"; description at line 4 names only lz-tpp/TPP; keywords at lines 12-20 = the 7 TDD terms with no refactoring vocabulary; author.email at line 7 = the public gmail, UNCHANGED)
    - .claude-plugin/marketplace.json (the FILE being edited -- plugins[0].description at line 13 names only "TPP coaching"; NO version key present today; $schema/name/owner/source/category UNCHANGED)
    - .planning/phases/10-distribution-hygiene/10-PATTERNS.md (the plugin.json + marketplace.json sections: exact current field values and the "leave unchanged" list)
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md (Open Question 2 keyword recommendation; the version-collision trap D-09)
  </read_first>
  <action>
    Edit plugins/lz-tdd/.claude-plugin/plugin.json: set version to "0.0.2"; rewrite description to
    two-skill wording that names lz-tpp (TPP coach/reference) AND lz-refactor (the Fowler + Kerievsky
    refactoring coach/reference for the red-green-refactor refactor step) as ORIGINAL prose; extend
    keywords to keep the existing 7 and ADD refactoring vocabulary -- at minimum refactoring, code-smells,
    design-patterns, gang-of-four (optionally fowler, kerievsky); D-17 owns the exact list. Leave
    author.email, homepage, repository, and license byte-for-byte unchanged. Edit
    .claude-plugin/marketplace.json: rewrite plugins[0].description to two-skill marketplace-listing
    wording consistent with the plugin.json description. Do NOT add a version field to marketplace.json
    (D-09) and leave $schema, top-level name/description/owner, source, and category unchanged. Keep both
    files valid JSON and ASCII-only; do NOT write the work-email literal.
  </action>
  <acceptance_criteria>
    - plugin.json line 3 is "version": "0.0.2"
    - plugin.json description contains both "lz-tpp" and "lz-refactor"; keywords array contains at least refactoring, code-smells, design-patterns, gang-of-four in addition to the original 7
    - marketplace.json plugins[0].description contains "lz-refactor" and has NO "version" key anywhere in the file
    - plugin.json author.email is unchanged (the public gmail); both files parse as valid JSON
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (both manifests now in the widened (a)/(b) set)
  </acceptance_criteria>
  <verify>
    <automated>git grep -qF '"version": "0.0.2"' -- plugins/lz-tdd/.claude-plugin/plugin.json && git grep -qF 'lz-refactor' -- plugins/lz-tdd/.claude-plugin/plugin.json && git grep -qF 'gang-of-four' -- plugins/lz-tdd/.claude-plugin/plugin.json && git grep -qF 'lz-refactor' -- .claude-plugin/marketplace.json && ! git grep -qE '"version"' -- .claude-plugin/marketplace.json && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>plugin.json is at 0.0.2 with a truthful two-skill description + refactoring keywords; marketplace.json's listing description names both skills and carries no version field; both parse as JSON and pass the widened hygiene gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add the README lz-refactor section, two-skill lead + What-this-is, primer + sources + references pointer (D-05, D-06, D-07)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the FILE being edited -- the one-skill lead at :3-5; the single-skill "What this is" bullets at :11-13; the UNCHANGED Installation block at :21-27; the "## What lz-tpp does" two-mode shape to mirror at :29-40; the "## Transformation Priority Premise" primer+sources+pointer shape to mirror at :41-61; the UNCHANGED License block at :63-65)
    - .planning/phases/10-distribution-hygiene/10-PATTERNS.md (README Changes 1-6: exact lead reword, the two-skill bullet, the mirrored sections, and the "unchanged" install + license blocks)
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md (the README lz-refactor section spec; the recommended inventory line; the seam one-sentence pitch)
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (confirm the /lz-tdd:lz-refactor invocation and the dual-mode coach/reference framing the README summarizes -- do NOT copy catalog content out of it; link, don't inline)
  </read_first>
  <action>
    Edit README.md. (1) Reword the lead paragraph (:3-5) so it no longer says the plugin ships only
    lz-tpp -- frame the two-skill pair via the red-green-refactor seam (lz-tpp at the green step,
    lz-refactor at the refactor step), original prose. (2) Add an lz-refactor bullet to the "What this
    is" list (:11-13) mirroring the lz-tpp bullet's /lz-tdd:<skill> invocation shape. (3) Leave the
    Installation block (:21-27) verbatim. (4) Add a "## What lz-refactor does" section mirroring "## What
    lz-tpp does" (:29-40): the same two-bullet Coach (auto-triggers at the REFACTOR step) / Reference
    (invoked via /lz-tdd:lz-refactor) structure, original prose. (5) Add a refactoring primer + sources +
    references pointer mirroring "## Transformation Priority Premise" (:41-61): a brief ORIGINAL
    refactoring primer, then an "Authoritative sources:" LINK-ONLY list citing Fowler Refactoring 2nd ed
    and Kerievsky Refactoring to Patterns by title/author/publisher-or-ISBN link (never inline catalog
    text), then a pointer into plugins/lz-tdd/skills/lz-refactor/references/. Optionally add a one-line
    catalog inventory using counts RE-VERIFIED against the live tree at write time -- run the recount (ls
    the six references catalog dirs, subtract README + the 3 Kerievsky walkthroughs) and use the verified
    figures (expected 62 Fowler / 27 Kerievsky / 23 GoF + 5 extra / 19 functional / 28 smells); the line
    must NOT imply a complete Beck or Feathers catalog. (6) Leave the License block (:63-65) unchanged.
    Section ordering, tagline wording, and whether to include a usage snippet or minimal ASCII badges are
    Claude's discretion (D-17). ASCII-only; original prose; no work email.
  </action>
  <acceptance_criteria>
    - README.md contains a "## What lz-refactor does" heading and the string "/lz-tdd:lz-refactor"
    - The "What this is" list has an lz-refactor bullet; the lead paragraph no longer states the plugin ships only lz-tpp
    - The Installation block still contains both verbatim commands (/plugin marketplace add LayZeeDK/lz-engineering-claude-plugins and /plugin install lz-tdd@lz-engineering-claude-plugins)
    - The refactoring sources are LINK-ONLY (no inlined mechanics/smell tables/examples) and a references/ pointer is present; any inventory counts match a fresh recount of the live tree
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (README now in the widened (a)/(b) set AND the axis (c) verbatim set -- the primer is original prose)
  </acceptance_criteria>
  <verify>
    <automated>git grep -qF '## What lz-refactor does' -- README.md && git grep -qF '/lz-tdd:lz-refactor' -- README.md && git grep -qF '/plugin marketplace add LayZeeDK/lz-engineering-claude-plugins' -- README.md && git grep -qF 'plugins/lz-tdd/skills/lz-refactor/references' -- README.md && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>The README documents lz-refactor alongside lz-tpp (two-skill lead + What-this-is, a mirrored What-lz-refactor-does section, and an original primer + link-only sources + references pointer); the install + license blocks are untouched; any inventory counts are recounted; the widened hygiene gate is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Add the lz-tdd@0.0.2 CHANGELOG entry mirroring the 0.0.1 entry (D-15, D-16)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the FILE being edited -- the Keep a Changelog + SemVer header at :1-6; the "## [lz-tdd@0.0.1] - 2026-07-04" entry at :8-32 with its one-paragraph lead (:10-11), 6 bold-led "### Added" bullets (:13-30), and the bottom %40-encoded link-ref (:32) -- the exact shape to mirror)
    - .planning/phases/10-distribution-hygiene/10-PATTERNS.md (the CHANGELOG section: the four structural parts to mirror and the content rules)
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md (Open Question 2 bullet-granularity recommendation; D-15 content list; D-16 tag-not-yet-created note)
  </read_first>
  <action>
    Edit CHANGELOG.md. Insert a new "## [lz-tdd@0.0.2] - <date>" entry ABOVE the 0.0.1 entry (use today's
    date, 2026-07-09), mirroring the 0.0.1 entry's four structural parts: (1) the heading; (2) a
    one-paragraph lead summarizing the lz-refactor skill addition; (3) an "### Added" list of bold-led
    bullets -- one per deliverable (the lz-refactor dual-mode coach + reference skill via
    /lz-tdd:lz-refactor; the five reference catalogs -- Fowler, Kerievsky, GoF, extra-patterns,
    functional; the unified smell taxonomy; the coach decision procedure; and the three no-oracle
    principle-backing references, tagged core-only) -- bullet granularity is D-17; (4) a bottom link-ref
    [lz-tdd@0.0.2]: https://github.com/LayZeeDK/lz-engineering-claude-plugins/releases/tag/lz-tdd%400.0.2
    with the URL-encoded %40 for @, mirroring the 0.0.1 ref verbatim. Same link-don't-inline rule as D-06
    (no inlined catalog entries/mechanics/examples). The entry MUST NOT imply a complete Beck or Feathers
    catalog (tag the principle refs core-only, FUT-01 bound). D-16: the release tag does not exist yet --
    that is correct and matches the 0.0.1 precedent. ASCII-only; original prose; no work email.
  </action>
  <acceptance_criteria>
    - CHANGELOG.md contains a "## [lz-tdd@0.0.2] -" heading positioned ABOVE the "## [lz-tdd@0.0.1]" heading
    - The entry has a one-paragraph lead, an "### Added" list of bold-led bullets, and a bottom link-ref containing "releases/tag/lz-tdd%400.0.2" (the %40 encoding is present)
    - The entry links (does not inline) the catalogs; it tags the three principle-backing refs as core-only and does not claim a complete Beck/Feathers catalog
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (CHANGELOG now in the widened (a)/(b) set AND the axis (c) verbatim set)
  </acceptance_criteria>
  <verify>
    <automated>git grep -qF '[lz-tdd@0.0.2]' -- CHANGELOG.md && git grep -qF 'releases/tag/lz-tdd%400.0.2' -- CHANGELOG.md && git grep -qF '[lz-tdd@0.0.1]' -- CHANGELOG.md && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>CHANGELOG.md has an lz-tdd@0.0.2 entry above the 0.0.1 entry, mirroring its shape (lead + Added bullets + %40 link-ref), linking not inlining the catalogs, claiming no complete Beck/Feathers catalog; the widened hygiene gate is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

New human/AI-authored prose (README primer, CHANGELOG entry, manifest descriptions) crosses into the
committed public repo. No runtime/network/session/crypto/input surface -- the threats are content
hygiene and factual/manifest correctness.

| Boundary | Description |
|----------|-------------|
| new authored prose -> committed public README/CHANGELOG/manifests | must be ASCII-only, work-email-free, original (not inlined book text) |
| README/CHANGELOG claims -> live tree + manifests | install strings, invocations, contact, license, and catalog counts must not drift from reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-EM | Information Disclosure | work/PII email in new README/CHANGELOG/manifest prose | mitigate | The widened check-hygiene (a)/(b) from 10-01 gates all four files as each edit lands; the full-tree work-email git grep runs at the 10-04 gate |
| T-10-ASCII | Integrity | non-ASCII in new prose | mitigate | check-hygiene axis (a) over the widened set; author ASCII-only (-- , straight quotes, ->) |
| T-10-IP | Information Disclosure / IP | verbatim book prose inlined into the README primer / CHANGELOG | mitigate | Link-don't-inline (D-06/D-15); primer is original prose; check-hygiene axis (c) now scans README + CHANGELOG (D-04/D-01 L1) |
| T-10-DRIFT | Tampering / Integrity | README/manifest/CHANGELOG stating a wrong version, install string, invocation, or catalog count | mitigate | Lift install strings + email/URL/license verbatim from the live files; recount the inventory against the tree (D-07); claude plugin validate at the 10-04 gate confirms manifest correctness |
</threat_model>

<verification>
- After each task, node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (widened (a)/(b) + hardened (c) GREEN over the edited files).
- plugin.json version == 0.0.2; both descriptions name lz-refactor; marketplace.json has no version key.
- README has "## What lz-refactor does" + "/lz-tdd:lz-refactor" + an unchanged install block + a references/ pointer; inventory counts match a fresh recount.
- CHANGELOG has the lz-tdd@0.0.2 entry above 0.0.1 with the %40 link-ref.
- The full battery + claude plugin validate + review agents run at the 10-04 gate, not here.
</verification>

<success_criteria>
The manifests truthfully describe a two-skill 0.0.2 plugin, the README documents lz-refactor alongside
lz-tpp with original prose and link-only sources, and the CHANGELOG records the 0.0.2 release -- all
four files ASCII-only, work-email-free, and GREEN under the widened hygiene gate.
</success_criteria>

<output>
Create `.planning/phases/10-distribution-hygiene/10-02-SUMMARY.md` when done.
</output>
