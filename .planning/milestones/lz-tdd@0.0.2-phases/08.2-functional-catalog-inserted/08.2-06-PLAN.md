---
phase: 08.2-functional-catalog-inserted
plan: 06
type: execute
wave: 4
depends_on: [08.2-02, 08.2-03, 08.2-04, 08.2-05]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
autonomous: true
requirements: [FUN-03, FUN-04]

must_haves:
  truths:
    - "Every one of the 55 OO leaves (23 gof + 27 kerievsky + 5 extra; the 3 kerievsky *-walkthrough companions are NOT leaves and get no link) carries exactly one Functional alternative: line resolving to its idiom leaf, and that idiom leaf's Correspondence: names it back (forward-subset mutuality)"
    - "SKILL.md gains a thin functional-catalog router pointer AFTER the existing catalog pointers, mirroring the Phase 08.1 D-14 pointer style; no coach behavior (that is Phase 9)"
    - "The full battery is GREEN: npm run check (all 9 checkers incl. check-functional) + npm run typecheck + claude plugin validate . all pass; skill-reviewer PASSes"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      provides: "The functional-catalog router pointer section"
      contains: "functional-catalog/README.md"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/strategy.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/first-class-function.md"
      via: "Functional alternative: link (mutual with first-class-function's Correspondence)"
      pattern: "functional-catalog/first-class-function.md#"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/null-object.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/option-and-either.md"
      via: "Functional alternative: link (mutual with option-and-either)"
      pattern: "functional-catalog/option-and-either.md#"
  decisions: |
    - **D-09:** each existing gof/kerievsky/extra leaf gains exactly one one-line Functional alternative: cross-link to its idiom leaf, contract-preserving (same one-line metadata mechanism as the Direction:Away links); Fowler leaves get NO such line
    - **D-05:** the phase closes only when check-functional (C1-C8) + the full npm run check battery + npm run typecheck are GREEN and claude plugin validate . passes -- the no-oracle correctness anchor
---

<objective>
Wire the bidirectional cross-links and pass the phase gate: add the 55 `Functional
alternative:` lines to the OO leaves, add the SKILL.md router pointer, then turn the full
battery GREEN and run the first-party review.

Purpose: FUN-04 (bidirectional link integrity + the check-functional gate) and FUN-03's
native cross-refs (the mutual Null Object / Extract Parameter back-links complete here). This
is the phase gate -- check-crossrefs and check-functional C4 have been RED-by-design since the
leaves landed without their forward links; they go GREEN here.

Output: 55 one-line link edits, the SKILL.md pointer, a GREEN battery + validate + skill-reviewer PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-functional-catalog-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md

The forward-link mapping (each OO leaf -> its idiom leaf) is RESEARCH.md Deliverable 2:
the GoF-23 -> leaf, Kerievsky-27 -> leaf, and extra-5 -> leaf tables. The link format and
placement are RESEARCH.md "The Cross-Link Contract (Deliverable 3)".
</context>

<tasks>

<task type="auto">
  <name>Task 1: 23 GoF + 5 extra Functional alternative: links</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (the "GoF 23 -> idiom leaf" and "Extra patterns 5 -> idiom leaf" tables; the Cross-Link Contract link format + gof/extra placement guidance)
    - .planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md (the "Functional alternative: cross-link on 55 OO leaves" section -- placement between H1/Also-known-as and ## Intent; check-gof safety)
    - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/strategy.md and references/extra-patterns-catalog/null-object.md (the current header shape: H1 -> optional Also known as: -> ## Intent)
  </read_first>
  <action>
    For each of the 23 gof-catalog leaves and 5 extra-patterns-catalog leaves, insert exactly ONE line
    `Functional alternative: [<idiom name>](../functional-catalog/<slug>.md#<anchor>)` as a single metadata line between the H1
    (and the `Also known as:` line if present) and `## Intent`. Use the exact idiom target from the RESEARCH GoF->leaf /
    Extra->leaf tables (e.g. Strategy/Bridge/Adapter -> first-class-function; Decorator/Chain of Responsibility/Template
    Method/Builder -> function-composition; Composite/Interpreter/State/Visitor -> discriminated-union-and-fold;
    Abstract Factory/Factory Method -> factory-function; Singleton/Facade -> module-namespace; Prototype -> structural-clone;
    Flyweight -> memoization-and-interning; Proxy/Command -> thunk-and-lazy-value; Iterator -> generator; Mediator ->
    reducer-and-store; Observer -> observer-callbacks; Memento -> immutable-snapshot; Null Object -> option-and-either;
    Factory/Creation Method -> factory-function; Composed Method/Collecting Parameter -> function-composition). For a target
    that is an N:1 leaf, point the `#anchor` at that pattern's `### <Pattern>` residual anchor
    (`discriminated-union-and-fold.md#visitor`); for a 1:1 leaf, point at the leaf's own H1 anchor. ASCII-only.
    Then COMMIT this batch (the 28 gof + extra Functional alternative: link edits) with specific-file staging and a `docs(08.2): ...` message BEFORE moving to Task 2, so a mid-plan context exhaustion never loses committed link work.
  </action>
  <acceptance_criteria>
    - All 23 gof + 5 extra leaves carry exactly one `Functional alternative:` line each
    - node tools/check-gof.mjs exits 0 (23/23 contract intact) and node tools/check-extra-patterns.mjs exits 0 (5/5)
    - node tools/check-functional.mjs C4 recognizes the gof/extra forward links (the kerievsky links land in Task 2)
    - The 28 gof + extra link edits are committed (specific-file staging, `docs(08.2): ...`) BEFORE Task 2 begins -- batch-1 commit checkpoint
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-gof.mjs && node tools/check-extra-patterns.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>28 gof+extra leaves carry their Functional alternative: link; the gof/extra contracts stay GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: 27 Kerievsky Functional alternative: links + SKILL.md router pointer</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (the "Kerievsky 27 -> idiom leaf" table, incl. the A4 utilities; the Cross-Link Contract kerievsky placement -- inside the existing metadata header block)
    - .planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md (the SKILL.md router pointer section -- mirror the "Target pattern catalogs" block lines 66-72; GSD note: SKILL.md needs subagent review)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-logic-with-strategy.md (metadata header block placement) and move-accumulation-to-visitor.md (Away metadata line)
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (the existing "Target pattern catalogs" pointer block to place the new one after)
  </read_first>
  <action>
    For each of the 27 kerievsky-catalog LEAF files (NOT the 3 *-walkthrough companions), add one
    `Functional alternative: [<idiom name>](../functional-catalog/<slug>.md#<anchor>)` line inside the existing metadata header
    block (alongside `Direction:` / `GoF pattern:` / `Composed Fowler primitive(s):`, between the `Use when:` line and the first
    `## `). Use the exact idiom target from the RESEARCH Kerievsky->leaf table, incl. the A4 utilities: Chain Constructors ->
    factory-function, Unify Interfaces -> first-class-function, Extract Parameter -> currying-and-partial-application; and the
    mutual pairs Introduce Null Object -> option-and-either, Replace Type Code with Class -> branded-type. Point N:1 targets at
    the served pattern's residual anchor; 1:1 targets at the leaf H1 anchor.
    Then add a thin `## Functional catalog (...)` router pointer to SKILL.md AFTER the existing "Target pattern catalogs" block:
    one `## ` heading + a 2-3 line "when to consult" sentence + the bare link
    `[references/functional-catalog/README.md](references/functional-catalog/README.md)`. Keep it a pure pointer -- NO coach
    behavior (that is Phase 9). ASCII-only.
    Then COMMIT this batch (the 27 kerievsky Functional alternative: links + the SKILL.md router pointer) with specific-file staging and a `docs(08.2): ...` message BEFORE running the Task 3 phase gate, so committed link work survives a mid-plan context exhaustion.
  </action>
  <acceptance_criteria>
    - All 27 kerievsky LEAF files carry exactly one `Functional alternative:` line; the 3 *-walkthrough companions carry none
    - node tools/check-kerievsky.mjs exits 0 (27/27; the added line does not break its field checks)
    - SKILL.md has a new `## Functional catalog` pointer section after the "Target pattern catalogs" block, pointing at functional-catalog/README.md, with no coach-behavior content
    - The 27 kerievsky link edits + the SKILL.md pointer are committed (specific-file staging, `docs(08.2): ...`) BEFORE Task 3's phase gate runs -- batch-2 commit checkpoint
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-kerievsky.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>27 kerievsky leaves carry their link; SKILL.md carries the thin functional-catalog pointer; kerievsky contract stays GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate -- full battery GREEN + validate + skill-reviewer PASS</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md ("Validation Architecture" -- the phase-gate sampling rate; the RED-baseline assertion set that must now be GREEN)
    - .planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md (the SKILL.md subagent-review GSD note)
    - .claude/skills/lz-refactor-workspace/package.json (the full check + typecheck scripts)
  </read_first>
  <action>
    Turn the full battery GREEN and pass first-party review:
    (a) From `.claude/skills/lz-refactor-workspace/`, run `npm run check` (all 9 checkers incl. check-functional: expect
    check-functional C1-C8 GREEN -- 19/19 leaves, 55/55 Functional alternative: links mutual, selector-mirror, notes, Sources,
    per-pattern residual caps; check-crossrefs GREEN -- every forward + Correspondence + intra-leaf-anchor link resolves; the
    other 7 checkers GREEN) and `npm run typecheck` (every functional Example compiles tsc --strict). Resolve any residual
    failure by fixing the offending leaf/README/link (a dangling anchor, a >1-line residual, a missing mutual back-link).
    (b) Run `claude plugin validate .` from the repo root -- must PASS.
    (c) Spawn the skill-reviewer agent (per the project rule that SKILL.md edits need subagent review, and FUN-04's
    skill-reviewer PASS): review the SKILL.md router-pointer change AND the functional-catalog for progressive-disclosure +
    DST-04 hygiene. Address any blocking finding. Record the PASS in the SUMMARY.
    Do NOT open an AskUserQuestion oracle-access checkpoint (D-06: no owned book this phase).
  </action>
  <acceptance_criteria>
    - `npm run check` exits 0 -- all 9 checkers GREEN, including check-functional C1-C8 (19/19 + 55/55 mutual links)
    - `npm run typecheck` exits 0 -- every functional Example compiles tsc --strict
    - `claude plugin validate .` PASSes
    - skill-reviewer returns PASS on the SKILL.md change + the functional-catalog; the PASS is recorded in the SUMMARY
    - FUN-01..04 are satisfied (documented in the SUMMARY with the closing evidence)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && npm run check && npm run typecheck</automated>
  </verify>
  <done>Full battery + typecheck + claude plugin validate GREEN; skill-reviewer PASS; FUN-01..04 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown reference content + a thin SKILL.md pointer -- no runtime/network/user-input surface.
Boundaries are repo-hygiene / licensing + the skill-instruction-change review gate.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | the 55 link edits + SKILL.md must be ASCII-only, work-email-free, no verbatim book prose/code |
| SKILL.md (agent instruction) -> Claude behavior | a skill-instruction change; must be subagent-reviewed before acceptance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-SKILL | Tampering | plugins/lz-tdd/skills/lz-refactor/SKILL.md | mitigate | skill-reviewer subagent review before acceptance (project rule); the pointer is thin, no coach behavior. |
| T-08.2-IP | Information Disclosure | the 55 link edits + SKILL.md | mitigate | ASCII-only, one-line links only; check-hygiene no-verbatim heuristic + claude plugin validate. |
| T-08.2-EM | Information Disclosure | committed files | mitigate | Work-email allowlist enforced by check-hygiene. |
| T-08.2-SC | Tampering | typescript@6.0.3 | accept | No new installs; the gate runs the pinned tsc. |
</threat_model>

<verification>
- `npm run check` exits 0 (9 checkers GREEN, incl. check-functional C1-C8 + check-crossrefs).
- `npm run typecheck` exits 0 (every functional Example compiles tsc --strict).
- `claude plugin validate .` PASSes.
- skill-reviewer PASSes on the SKILL.md change + the functional-catalog.
</verification>

<success_criteria>
55 OO leaves carry a resolving, mutual Functional alternative: link; SKILL.md carries the thin
functional-catalog pointer; the full battery + typecheck + claude plugin validate are GREEN;
skill-reviewer PASSes; FUN-01..04 closed.
</success_criteria>

<output>
Create `.planning/phases/08.2-functional-catalog-inserted/08.2-06-SUMMARY.md` when done
(record the skill-reviewer PASS and the FUN-01..04 closing evidence).
</output>
