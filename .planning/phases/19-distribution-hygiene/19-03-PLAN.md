---
phase: 19-distribution-hygiene
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified: []
autonomous: true
requirements: [DST-02, DST-03]

must_haves:
  truths:
    - "The first-party CLI gate passes on the FINAL tree (post-19-01 content + post-19-02 GA-7): claude plugin validate . exits 0 AND claude plugin validate . --strict exits 0 -- both are REGRESSION gates (exit 0 pre-edit; re-run after the manifest / README / CHANGELOG landed)."
    - "The full deterministic battery is GREEN on the final tree: the lz-red workspace check (check-red-references, 11/11 surfaces) exits 0, the lz-red workspace typecheck (extract-samples tsc --strict, 8 modules) exits 0, and the lz-refactor workspace check (10 checkers incl. the widened check-hygiene on all three axes) + typecheck exit 0."
    - "DST-03 hygiene holds on the final tree: check-hygiene axes (a) ASCII + (b) work-email allowlist-inversion + (c) no-verbatim all GREEN; every TypeScript sample is tsc --strict clean; and the full-tree email allowlist-inversion is clean -- enumerating every email-shaped token and subtracting the approved public gmail (larsbrinknielsen@gmail.com) leaves an empty remainder."
    - "D-08: the two plugin-dev review agents are ORCHESTRATOR-run after the executor's battery returns (gsd-executor has no Agent/Task tool) -- the plugin-validator PASSes on the lz-tdd plugin (structure / manifest / security / path-traversal), and the skill-reviewer reaches an explicit PASS on lz-red (lz-tpp / lz-refactor re-confirmed only if a shared edit touched them -- 19-01 did not)."
    - "D-01/D-02: the targeted DST-04 clean-room re-sweep (orchestrator oracle-reviewer only) re-confirms the handful of OWNED lz-red surfaces (RCM test-selection + F.I.R.S.T.; Metz message-matrix; Cooper over-mock / test-per-class anti-pattern; the RCM Three-Laws spine) all-pass on the final tree; the main context NEVER reads .oracle/; the layer-3 attestation cites the standing 16-03 / 17-06 (+ 17-VERIFICATION) / 18-06 owned-surface verdicts and is recorded in a durable phase artifact (an undocumented attestation is not an attestation)."
    - "D-09: findings are triaged -- FIX-in-phase = structural / manifest errors, security / path-traversal, anything breaking install or /lz-tdd:lz-red|lz-tpp|lz-refactor invocation, ASCII violations, broken reference links, malformed frontmatter, factual inaccuracies, any DST-04 verbatim hit; DEFER-to-Phase-20 = description-triggering effectiveness, over/under-triggering, the three-way cross-skill boundary, coach-routing accuracy (the expected lz-red 1091-char description + body-short flags are DEFER items, NOT ship blockers -- do not shorten the description or inflate the body)."
    - "D-10: any edit forced onto a SKILL.md by skill-reviewer or the DST-04 re-sweep gets its OWN >= 1-unbiased-from-scratch subagent review before acceptance, and /reload-plugins is a human ship action afterward; README / CHANGELOG / manifest / checker edits are NOT skill-instruction files and need no such review."
  artifacts:
    - path: ".planning/phases/19-distribution-hygiene/19-DST-04-ATTESTATION.md"
      provides: "the recorded DST-04 layer-1 (deterministic) result + layer-2 targeted re-sweep scope/result + layer-3 attestation citations for the owned + no-oracle lz-red surfaces"
      contains: "oracle-reviewer"
    - path: ".planning/phases/19-distribution-hygiene/19-03-SUMMARY.md"
      provides: "the recorded gate results, the two agent verdicts, the DST-04 re-sweep outcome, and the D-09 FIX/DEFER triage for the public-ship gate"
      contains: "plugin-validator"
  key_links:
    - from: "claude plugin validate . --strict"
      to: "plugins/lz-tdd/.claude-plugin/plugin.json + .claude-plugin/marketplace.json"
      via: "the first-party hard gate validates the edited manifests + the three-skill structure on the final tree"
      pattern: "plugin validate"
    - from: "the orchestrator"
      to: ".claude/agents/oracle-reviewer.md"
      via: "the DST-04 layer-3 re-sweep is expressed as oracle-reviewer subagent invocations run BY the orchestrator; the main context never diffs the tree against .oracle/"
      pattern: "oracle-reviewer"
  decisions: |
    - **D-01:** the three-layer DST-04 model, scaled to the lz-red tree -- layer 1 the deterministic no-verbatim gate (re-run GREEN in the battery); layer 2 the standing owned-surface attestation; layer 3 the TARGETED clean-room oracle-reviewer re-sweep over the OWNED surfaces only (not the whole tree); the no-oracle leaves rest on the deterministic gate + no-verbatim scan.
    - **D-02:** the clean-room rule is non-negotiable -- the main context NEVER reads .oracle/; every draft-vs-source comparison goes through the read-only oracle-reviewer subagent (own-words verdicts, never echoes source); "just diff the tree against the book" is impossible by construction.
    - **D-08:** three review gates on the FINAL tree -- claude plugin validate . + --strict (HARD regression gates), the plugin-dev plugin-validator agent, and the plugin-dev skill-reviewer agent on lz-red (explicit PASS); plus npm run check + npm run typecheck GREEN. The agent gates are ORCHESTRATOR-run (executor has no Agent/Task tool).
    - **D-09:** triage findings -- FIX ship-blockers in Phase 19; DEFER to Phase 20 the description-triggering / over-under-triggering / three-way-boundary / coach-routing findings (record, do not act; the known lz-red >500-char description + body-short flags are DEFER items).
    - **D-10:** a skill-reviewer- or re-sweep-forced edit to any SKILL.md needs its own >= 1-unbiased subagent review before acceptance, and /reload-plugins is a human ship action; README / CHANGELOG / manifest / checker edits do not.
---

<objective>
Run the DST-02 / DST-03 public-ship gate on the FINAL tree after the content edits (19-01) and the GA-7
forward-fix (19-02) have landed. The EXECUTOR runs the deterministic battery (first-party CLI validation,
both workspace check + typecheck batteries, the full-tree email allowlist-inversion) and authors the
DST-04 attestation from the standing owned-surface verdicts; the ORCHESTRATOR then runs the three agent
gates the gsd-executor cannot (it has no Agent/Task tool) -- plugin-validator, skill-reviewer on lz-red,
and the targeted DST-04 clean-room oracle-reviewer re-sweep. Every finding is triaged per D-09.

Purpose: this is the last gating step before the public three-skill 0.0.3 ship. Phase 20 (evals) is
explicitly non-blocking and runs after. The gates ARE the tests for this docs / config phase -- there is
no unit-test framework. Output: a machine-clean, agent-PASS final tree, a recorded DST-04 attestation, and
a 19-03-SUMMARY capturing every gate result, agent verdict, re-sweep outcome, and triage decision. The
executor authors no shipped file here (the battery is read-only); any content fix is a D-09 gap-closure
edit driven by a gate outcome, routed through D-10 if it lands on a SKILL.md.

## Decisions implemented (D-NN)

- **D-01:** DST-04 three-layer model scaled to lz-red -- layer 1 deterministic gate re-run GREEN; layer 2 standing owned-surface attestation cited; layer 3 targeted clean-room re-sweep over the owned surfaces only.
- **D-02:** the main context never reads .oracle/; the re-sweep runs only through the read-only oracle-reviewer subagent.
- **D-08:** the three review gates on the final tree (validate + --strict; plugin-validator; skill-reviewer on lz-red) plus GREEN batteries; the agent gates are orchestrator-run.
- **D-09:** FIX ship-blockers now; DEFER the triggering / boundary / routing findings to Phase 20 (the lz-red 1091-char description + body-short flags are DEFER, not blockers).
- **D-10:** any forced SKILL.md edit gets its own >= 1-unbiased review before acceptance; /reload-plugins is a human ship action.

## Artifacts produced

- 19-DST-04-ATTESTATION.md: the layer-1 deterministic result + the layer-2 targeted re-sweep scope/outcome + the layer-3 citations (16-03 / 17-06 + 17-VERIFICATION / 18-06) for the owned + no-oracle lz-red surfaces.
- 19-03-SUMMARY.md: the recorded gate results, the plugin-validator + skill-reviewer verdicts, the DST-04 re-sweep outcome, and the full D-09 FIX/DEFER triage.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-distribution-hygiene/19-CONTEXT.md
@.planning/phases/19-distribution-hygiene/19-RESEARCH.md
@.planning/phases/19-distribution-hygiene/19-PATTERNS.md
@.claude/agents/oracle-reviewer.md

RESEARCH "Validation Architecture" gives the full-suite commands and the Phase-Requirements -> Validation
map; Finding 3 lists the owned-surface attestation citations (16-03 / 17-06 + 17-VERIFICATION / 18-06);
Finding 4 gives the targeted re-sweep scope (the four owned surfaces) + per-invocation spec; Finding 7
names the expected skill-reviewer DEFER items (the lz-red 1091-char description; body-short). The
oracle-reviewer agent is the ONLY sanctioned .oracle/ reader and chunk-reads to EOF. The executor runs
ONLY the deterministic battery + the attestation authoring; the three agent gates are in
<orchestrator_gates> below and run AFTER the executor returns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the deterministic finalize battery on the final tree -- validate + --strict, both workspace batteries, full-tree email allowlist-inversion (D-08, D-09)</name>
  <files></files>
  <read_first>
    - .planning/phases/19-distribution-hygiene/19-RESEARCH.md ("Verified Baseline" -- the exact gate commands + expected exit 0; "Validation Architecture" -- the full-suite command; A1 -- validate exits 0 today, a regression is a D-09 FIX-now)
    - .claude/skills/lz-red-workspace/package.json (the check + typecheck scripts -- check-red-references + the extract-samples tsc extractor)
    - .claude/skills/lz-refactor-workspace/package.json (the check battery incl. check-hygiene + the typecheck script)
  </read_first>
  <action>
    On the FINAL tree (after 19-01 content + 19-02 GA-7 have landed), run the deterministic gates and
    confirm every one is GREEN: (1) the lz-red workspace check (node check-red-references.mjs -- 11/11
    surfaces) exits 0; (2) the lz-red workspace typecheck (tsc --strict over every fenced sample -- 8
    modules) exits 0; (3) the lz-refactor workspace check (the 10-checker battery INCLUDING the widened
    check-hygiene on axes ASCII + work-email allowlist-inversion + no-verbatim) exits 0; (4) the lz-refactor
    workspace typecheck exits 0 (regression -- the lz-refactor tree is unchanged this phase); (5) claude
    plugin validate . exits 0 AND claude plugin validate . --strict exits 0; (6) the full-tree email
    allowlist-inversion is clean -- enumerate every email-shaped token across the tracked tree, subtract the
    approved public gmail (larsbrinknielsen@gmail.com), and assert the remainder is empty (NEVER write the
    forbidden work-email or its bare domain; verify by asserting only the approved gmail is present). If any
    gate fails, triage per D-09: a structural / manifest error, security / path-traversal, broken install or
    /lz-tdd:lz-red|lz-tpp|lz-refactor invocation, ASCII violation, broken link, malformed frontmatter,
    factual inaccuracy, or a DST-04 verbatim hit is a ship-blocker -- FIX it in place (edit the offending
    file, re-run the gate); if the fix lands on any SKILL.md, it goes through the D-10 >= 1-unbiased review
    before acceptance. Do NOT self-certify the orchestrator gates below -- they run after this task returns.
    Record each gate's result for the SUMMARY.
  </action>
  <acceptance_criteria>
    - The lz-red workspace check and typecheck both exit 0; the lz-refactor workspace check (incl. check-hygiene all three axes) and typecheck both exit 0
    - claude plugin validate . exits 0 AND claude plugin validate . --strict exits 0 on the final tree
    - The full-tree email allowlist-inversion leaves an empty remainder (only the approved public gmail present across the tracked tree); the forbidden value is not written anywhere
    - Any gate failure was triaged per D-09 and any ship-blocker fixed in place with the gate re-run GREEN; any SKILL.md fix went through the D-10 review
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run check && npm --prefix .claude/skills/lz-red-workspace run typecheck && npm --prefix .claude/skills/lz-refactor-workspace run check && npm --prefix .claude/skills/lz-refactor-workspace run typecheck && claude plugin validate . && claude plugin validate . --strict && test -z "$(rg --hidden --glob '!.git/**' -o -i -N --no-filename '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' . | sort -u | rg -iv 'larsbrinknielsen@gmail\.com')"</automated>
  </verify>
  <done>The final tree passes claude plugin validate . and --strict, both workspace check + typecheck batteries (incl. check-hygiene on all three axes), and the full-tree email allowlist-inversion is clean; any ship-blocker was fixed and re-verified, any SKILL.md fix D-10-reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Author the DST-04 attestation artifact -- layer-1 result + layer-2 re-sweep scope + layer-3 citations (D-01, D-02)</name>
  <files>.planning/phases/19-distribution-hygiene/19-DST-04-ATTESTATION.md</files>
  <read_first>
    - .planning/phases/19-distribution-hygiene/19-RESEARCH.md (Finding 3 -- the owned-surface attestation citations and what each stands for; Finding 4 -- the four owned surfaces in the targeted re-sweep scope + the no-oracle leaves that rest on the deterministic gate)
    - .planning/phases/19-distribution-hygiene/19-CONTEXT.md (D-01 layer 2/3 -- "an undocumented attestation is not an attestation"; the owned-vs-no-oracle split)
    - .planning/phases/16-source-distillation-core-red-references/16-03-SUMMARY.md (the Beck/RCM selection + F.I.R.S.T. owned-surface oracle-reviewer verdict to cite)
    - .planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-06-SUMMARY.md and .planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-VERIFICATION.md (the Metz matrix + Cooper + Clean Code F.I.R.S.T. owned-surface verdicts to cite)
    - .planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-06-SUMMARY.md (the RCM Three-Laws spine owned-surface verdict to cite)
  </read_first>
  <action>
    Create .planning/phases/19-distribution-hygiene/19-DST-04-ATTESTATION.md recording the three DST-04
    layers for the lz-red tree. (1) Layer 1 (deterministic): state that check-hygiene axis (c) no-verbatim
    is GREEN over the lz-red tree + root prose on the final tree (from Task 1). (2) Layer 2 (targeted
    re-sweep SCOPE): list the four OWNED surfaces that the orchestrator oracle-reviewer re-sweep re-confirms
    on the final tree -- the RCM Beck-lineage test-selection rows + the F.I.R.S.T. block
    (three-laws-and-test-selection.md / test-structure-and-assertions.md), the Metz query/command message
    matrix (testing-stance/message-matrix.md), the Cooper over-mock / test-per-class anti-pattern
    (anti-patterns.md), and the RCM Three-Laws spine (three-laws-and-test-selection.md + the SKILL.md
    compact spine) -- and note that this re-sweep is an ORCHESTRATOR post-execution gate (below), its
    all-pass result to be recorded on completion; the main context never reads .oracle/. (3) Layer 3
    (attestation): cite by filename the standing owned-surface oracle-reviewer verdicts -- 16-03-SUMMARY.md
    (Beck/RCM selection + F.I.R.S.T.), 17-06-SUMMARY.md + 17-VERIFICATION.md (Metz matrix + Cooper + Clean
    Code F.I.R.S.T.), and 18-06-SUMMARY.md (RCM Three-Laws spine) -- as standing evidence for the surfaces
    the re-sweep does not re-open. State explicitly that the no-oracle leaves (functional-core.md /
    seams-and-legacy.md / vitest-typescript-mechanics.md / naming.md / the STR/AAA-GWT structure content /
    principle-backing.md) were authored BLIND with no .oracle/ read and rest on the deterministic gate +
    no-verbatim scan, OUTSIDE the DST-04 book-prose re-sweep axis. Keep the file ASCII-only, carry only the
    approved public gmail among email-shaped tokens, and quote NO .oracle/ source text.
  </action>
  <acceptance_criteria>
    - 19-DST-04-ATTESTATION.md exists and records all three layers (layer-1 deterministic GREEN; layer-2 the four owned surfaces + the orchestrator-re-sweep note; layer-3 the standing citations)
    - It cites, by filename, 16-03-SUMMARY.md, 17-06-SUMMARY.md (and 17-VERIFICATION.md), and 18-06-SUMMARY.md as the layer-3 standing evidence
    - It states the no-oracle lz-red leaves are outside the DST-04 book-prose axis (authored blind, no .oracle/ read)
    - The file is ASCII-only, carries only the approved public gmail among email-shaped tokens, and quotes no .oracle/ source prose
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8"); for(const c of ["16-03","17-06","18-06","oracle-reviewer"]){if(!s.includes(c))process.exit(1);} if(/[^\x00-\x7F]/.test(s))process.exit(1);' ".planning/phases/19-distribution-hygiene/19-DST-04-ATTESTATION.md"</automated>
  </verify>
  <done>19-DST-04-ATTESTATION.md records the DST-04 layer-1 deterministic result, the layer-2 targeted re-sweep scope (four owned surfaces, orchestrator-run), and the layer-3 citations (16-03 / 17-06 + 17-VERIFICATION / 18-06), notes the no-oracle leaves as outside the axis, and is ASCII-only + carries only the approved gmail + quotes no source prose.</done>
</task>

</tasks>

<orchestrator_gates>
## Orchestrator gates (run AFTER the executor's Task-1 battery + Task-2 attestation return -- NOT executor tasks)

gsd-executor has ONLY Read/Write/Edit/Bash/Grep/Glob/Skill (no Agent/Task tool -- memory
gsd-executor-cannot-spawn-subagents), so the three agent gates below are ORCHESTRATOR-driven and run after
the deterministic tasks return GREEN. Do NOT plan or run them as executor steps. Each is reached BY its
dedicated agent (never self-certified inline). This mirrors 16-03 / 17-06 / 18-06.

1. **plugin-dev plugin-validator on the lz-tdd plugin** (D-08). Structure / manifest / security /
   path-traversal on the final tree. A plugin-root README/LICENSE "missing" note is by-design for a
   single-plugin marketplace (both live at repo root) -- triage minor.
2. **plugin-dev skill-reviewer on lz-red** (D-08). MUST reach an explicit PASS. Re-confirm lz-tpp /
   lz-refactor only if a shared edit touched them (19-01 did not). D-09 triage: the expected 1091-char
   description ("too long") and possible "body short" (147 lines) flags are triggering-effectiveness /
   content-organization concerns -> RECORD and DEFER to Phase 20 (EVL-01/02). Do NOT shorten the
   description or inflate the body (that pulls Phase-20 empirical work forward and fights the intentional
   lean design). Neither blocks the ship.
3. **Targeted DST-04 clean-room oracle-reviewer re-sweep** (D-01 layer 3, D-02). Over the four OWNED
   surfaces only (RCM test-selection + F.I.R.S.T.; Metz message-matrix; Cooper anti-pattern; RCM
   Three-Laws spine), re-confirming on the FINAL tree. Per-invocation spec (Finding 4): agent =
   oracle-reviewer; DRAFT input = only the shipped surface (not whole leaves); source scope = the book via
   index.md (the reviewer resolves the file); rubric = the DST-04 near-verbatim axis only; round cap = 3;
   batched-or-per-surface is the orchestrator's call (D-13). The main context NEVER reads .oracle/. On any
   revise: reword that surface BLIND from the reviewer's short directives and re-gate; non-convergence
   escalates to the owner. Record the all-pass result into 19-DST-04-ATTESTATION.md on completion.

If any gate returns NEEDS-CHANGES, close it via a scoped D-09 gap-closure edit and re-run the full
deterministic battery (Task 1) before advancing; if the edit lands on any SKILL.md it goes through the
D-10 >= 1-unbiased review first, and /reload-plugins is the human ship action. Requirement closure
(DST-02, DST-03) + the milestone traceability flip happen only after all three gates are clear and
gsd-verifier passes across the three ROADMAP Success Criteria.
</orchestrator_gates>

<threat_model>
## Trust Boundaries

The final public-ship tree crosses into the published marketplace. The first-party validator + agents
verify structure / manifest safety; the deterministic gates + full-tree allowlist-inversion verify
hygiene; the clean-room re-sweep verifies no-verbatim; residual verification of the earlier plans'
threats happens here on the assembled tree.

| Boundary | Description |
|----------|-------------|
| plugin manifest source -> /plugin marketplace add install | relative ./plugins/lz-tdd source only; no ../ or absolute path traversal |
| assembled tree -> published public repo | ASCII-only, carries only the approved public gmail (incl. .planning/), no verbatim book prose / talk transcripts |
| .oracle/ book prose -> main context / committed tree | FORBIDDEN; only the orchestrator's oracle-reviewer reads .oracle/ and returns own-words verdicts |

## STRIDE Threat Register

ASVS level 1; block on `high`. None is left OPEN -- every threat has an existing GREEN mitigation. The
shipped artifacts are Markdown that execute no code; example fences are compiled, never run.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-STRUCT | Tampering | manifest path-traversal / malformed structure breaking install | high | mitigate | claude plugin validate . --strict (HARD gate) + the plugin-dev plugin-validator agent; the relative ./plugins/lz-tdd source is already compliant |
| T-19-EM | Information Disclosure (PII) | work/PII email anywhere in the repo incl. .planning/ | high | mitigate | check-hygiene axes (a)/(b) over the shippable surface + the full-tree email allowlist-inversion post-content (only the approved gmail present) |
| T-19-IP | Information Disclosure / IP | residual verbatim book prose / talk transcript in the shipped lz-red tree | high | mitigate | check-hygiene axis (c) in the battery + the targeted DST-04 oracle-reviewer re-sweep over the owned surfaces + the layer-3 attestation; a verbatim hit is a D-09 ship-blocker |
| T-19-ASCII | Integrity | non-ASCII (cp1252 mojibake) anywhere in the shipped tree | medium | mitigate | check-hygiene axis (a) ASCII byte-scan GREEN in the battery |
| T-19-SKILLEDIT | Tampering | a skill-instruction edit slipping in unreviewed | medium | mitigate | any SKILL.md fix routes through the D-10 >= 1-unbiased subagent review before acceptance; /reload-plugins is a human ship action |
| T-19-SC | Tampering (supply chain) | npm/pip/cargo package installs | low | accept | No dependency added this phase (RESEARCH Package Legitimacy Audit: Not applicable); claude plugin validate . confirms structural integrity |
</threat_model>

<verification>
- claude plugin validate . and --strict exit 0 on the final tree; both workspace check + typecheck batteries exit 0.
- The full-tree email allowlist-inversion leaves an empty remainder (only the approved public gmail present).
- Orchestrator gate 1: plugin-validator PASSes on the lz-tdd plugin.
- Orchestrator gate 2: skill-reviewer PASSes on lz-red; the 1091-char description + body-short flags are recorded as Phase-20 DEFER, not acted on.
- Orchestrator gate 3: the targeted DST-04 oracle-reviewer re-sweep is all-pass on the four owned surfaces (main context never read .oracle/); recorded in 19-DST-04-ATTESTATION.md.
- Findings triaged FIX-or-DEFER in the SUMMARY; no description shortened / body inflated; any SKILL.md fix D-10-reviewed.
- gsd-verifier confirms goal achievement across the three ROADMAP Success Criteria.
</verification>

<success_criteria>
The final three-skill 0.0.3 tree passes the first-party CLI (plain + --strict), both workspace check +
typecheck batteries (incl. check-hygiene on all three axes), the full-tree email allowlist-inversion, and
both plugin-dev review agents (plugin-validator + skill-reviewer PASS on lz-red); the targeted DST-04
clean-room re-sweep is all-pass on the owned surfaces with the attestation recorded; every finding is
triaged per D-09 with triggering / boundary / routing findings deferred to Phase 20 and any SKILL.md edit
reviewed per D-10 -- so DST-02 and DST-03 are ready for gsd-verifier and the milestone traceability flip.
</success_criteria>

<output>
Create `.planning/phases/19-distribution-hygiene/19-03-SUMMARY.md` when done, recording the GREEN battery,
the two agent verdicts, the DST-04 re-sweep outcome, and the full D-09 FIX/DEFER triage.
</output>
