---
phase: 20-skill-effectiveness-evals
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - .claude/skills/lz-red-workspace/EVAL-RESULTS.md
autonomous: true
requirements:
  - EVL-01
  - EVL-02
must_haves:
  truths:
    - "EVAL-RESULTS.md carries the results structure (trigger recall/specificity, the reciprocal RED spot-check, RED-behavior with-skill vs baseline, and Pass@k/Pass^k tables for k=1,3,5,total) with the locked run-config caveat and all numbers left blank until the gated run (D-07)."
    - "The full deterministic build battery is GREEN on the merged tree: check-evals.mjs exit 0, grade-run.mjs --selfcheck exit 0, merge-judge.mjs --selfcheck exit 0, and the existing content gate (check-red-references.mjs + extract-samples.mjs) unregressed."
    - "The phase presents exact ready-to-run commands for EVL-01 forward (lz-red), EVL-01 reciprocal (lz-tpp + lz-refactor via direct run_eval), and EVL-02 behavior, then HALTS before any metered claude -p run (D-11)."
    - "The EVL-02 subagent fan-out, the LLM judge, and the >=1 unbiased-from-scratch reviewer are documented as post-approval ORCHESTRATOR steps, not executor work (D-04, D-07, gsd-executor-cannot-spawn-subagents)."
    - "The at-most-one D-09 tuning pass is documented as gated/conditional -- applied only if a soft bar (100% recall / 100% specificity; behavior clearly beats baseline) is missed on a demonstrated defect, and the only write-back into plugins/ (D-08, D-09)."
  artifacts:
    - .claude/skills/lz-red-workspace/EVAL-RESULTS.md
  key_links:
    - "EVAL-RESULTS.md reproduce section <-> the built harness/runners/grader (20-01 + 20-02)"
    - "ready-to-run commands <-> the vendored tools/skill-creator-eval/ + evals/*.json + the two runners"
---

<objective>
Finalize the build: scaffold the EVAL-RESULTS.md record, prove the full deterministic battery GREEN
on the merged tree, and present the exact ready-to-run eval commands -- then HALT before any metered
run (D-11 HARD GATE). This plan is the build/run boundary of the phase.

Purpose: the phase goal is an EMPIRICALLY validated lz-red skill, but the measurement spends
claude -p tokens and is user-gated. This plan produces the ready-to-run record and the command set,
documents the orchestrator-run gates (behavior subagents, LLM judge, unbiased reviewer) that fire
only after approval, and stops. The actual run + Pass@k/Pass^k fill-in + optional D-09 tuning happen
after the user approves, outside this plan.

Output: EVAL-RESULTS.md (structure + run-config caveat + Pass@k/Pass^k tables, numbers blank) and a
presented, verified command set. No metered run; no subagent spawn.

## Locked decisions honored
- **D-07:** EVAL-RESULTS.md scaffolds the Pass@k and Pass^k tables (k=1,3,5,total) per eval and overall, records the wait-for-all-completion-notifications rule, and reserves a slot for the >=1 unbiased-from-scratch reviewer verdict. Numbers are filled only after the gated run.
- **D-08:** Document the SOFT, non-blocking pass bars -- trigger 100% recall / 100% specificity with both sibling near-misses quiet AND the reciprocal RED spot-check clean; behavior with-skill Pass@1 high AND clearly beating the unaided baseline. Missing a bar triggers AT MOST the D-09 tuning pass; it NEVER reopens Phases 15-19.
- **D-09:** Document the at-most-one, tightly-bounded tuning pass (description widen only if it beats the current one on a held-out set; a bounded coach-wording tweak only on a real routing/RED-move defect; NO new reference files, NO re-authoring locked content). Any SKILL.md edit gets its OWN >=1 unbiased subagent review; /reload-plugins is a human ship action. Do NOT apply tuning in this plan (it is post-run, gated, conditional).
- **D-11 (HARD GATE):** Present the ready-to-run commands and HALT. Run NOTHING (no run_eval, no run_loop, no subagent, no claude -p).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-skill-effectiveness-evals/20-CONTEXT.md
@.planning/phases/20-skill-effectiveness-evals/20-RESEARCH.md
@.planning/phases/20-skill-effectiveness-evals/20-PATTERNS.md
@AGENTS.md

Depends on 20-01 (harness + runners + check-evals + trigger/reciprocal/negatives data + gitignore)
and 20-02 (grade-run.mjs + evals/evals.json). Both must be merged before this plan runs its battery.

Read-blindness note (RESEARCH Pitfall 7): `git grep` is a SILENT zero inside
.claude/skills/*-workspace/. Use `rg` or Read/Glob there.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold EVAL-RESULTS.md and prove the full deterministic battery GREEN on the merged tree</name>
  <files>.claude/skills/lz-red-workspace/EVAL-RESULTS.md</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md (analog; COPY the heading/table STRUCTURE + the run-config caveat text; numbers blank)
    - .claude/skills/lz-red-workspace/check-evals.mjs, grade-run.mjs, merge-judge.mjs, evals/*.json (the built assets this record describes; from 20-01 + 20-02)
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs, extract-samples.mjs (the existing content gate that must stay unregressed)
    - CLAUDE.md ("skill-creator workflow" -- MUST compute/report Pass@k and Pass^k per eval and overall; formulas + k values; wait for all completion notifications; >=1 unbiased reviewer)
    - .planning/phases/20-skill-effectiveness-evals/20-PATTERNS.md ("EVAL-RESULTS.md (report, COPY STRUCTURE)")
  </read_first>
  <action>
    Copy the EVAL-RESULTS.md heading/table STRUCTURE from the lz-refactor analog into lz-red-workspace, adapted to the
    three lz-red measurements and with ALL numbers left blank (filled only after the gated run). Include:
    (a) a header naming the skill under test (lz-red), the run config caveat text VERBATIM in spirit (serial
    --num-workers 1, PONYTAIL_DEFAULT_MODE=off, MCP stripped + --setting-sources project, model claude-opus-4-8), and
    the standing note that these numbers come only from a user-approved run;
    (b) an EVL-01 forward section (recall over should_trigger:true, specificity over should_trigger:false, threshold
    trigger_rate >= 0.5 = fired) with an empty per-query table;
    (c) an EVL-01 reciprocal RED spot-check section (lz-tpp specificity + lz-refactor specificity over the RED positives)
    with empty tables and the note that both siblings must stay quiet on RED intent;
    (d) an EVL-02 section (with-skill vs baseline per scenario x dimension) with empty tables and the SATURATION
    limitation note (claude-opus-4-8 saturates most leaf-sourced scenarios; the discriminating signal rests on the
    classify-first / over-mock / false-green cases -- an eval-design limitation to document, not tune away);
    (e) Pass@k and Pass^k tables (k = 1, 3, 5, total) per eval and overall, with the formulas, per the CLAUDE.md rule;
    (f) a reserved slot for the >=1 unbiased-from-scratch reviewer verdict.
    ASCII-only (->, straight quotes); no work-email / bare domain anywhere.

    Then run the full deterministic build battery on the merged tree and confirm GREEN: check-evals.mjs exit 0,
    grade-run.mjs --selfcheck exit 0, merge-judge.mjs --selfcheck exit 0, AND the existing content gate
    (tools/check-red-references.mjs + extract-samples.mjs) still exit 0 (the eval additions must not disturb the
    Phase-16/17/18 gate). Do NOT run any eval, run_eval, run_loop, or subagent.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/check-evals.mjs && echo checkevals-ok</automated>
    <automated>node .claude/skills/lz-red-workspace/grade-run.mjs --selfcheck && node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck && echo graders-ok</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-red-workspace/extract-samples.mjs && echo contentgate-ok</automated>
    <automated>rg -c "Pass\^k|Pass@k" .claude/skills/lz-red-workspace/EVAL-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `node .claude/skills/lz-red-workspace/check-evals.mjs` exits 0; `node .claude/skills/lz-red-workspace/grade-run.mjs --selfcheck` exits 0; `node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck` exits 0 (full deterministic eval battery GREEN on the merged tree).
    - `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` and `node .claude/skills/lz-red-workspace/extract-samples.mjs` both exit 0 (existing content gate unregressed).
    - EVAL-RESULTS.md contains sections for EVL-01 forward, EVL-01 reciprocal (lz-tpp + lz-refactor), and EVL-02, plus Pass@k AND Pass^k tables for k=1,3,5,total (rg count >= 1 for each keyword), all with blank numbers.
    - EVAL-RESULTS.md states the locked run-config caveat and the saturation limitation note; no non-ASCII byte, no work-email / bare domain (allowlist-inversion clean).
  </acceptance_criteria>
  <done>EVAL-RESULTS.md is scaffolded with the three-measurement structure + Pass@k/Pass^k tables + caveats and blank numbers, and the full deterministic battery is GREEN on the merged tree with the content gate unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the ready-to-run command set + the orchestrator-gate + D-09 tuning notes into EVAL-RESULTS.md, then HALT (no metered run)</name>
  <files>.claude/skills/lz-red-workspace/EVAL-RESULTS.md</files>
  <read_first>
    - .claude/skills/lz-red-workspace/EVAL-RESULTS.md (this plan's Task 1 output -- append the reproduce/run section)
    - .claude/skills/lz-red-workspace/run-recall-chunks.mjs, run-spec-chunks.mjs, tools/skill-creator-eval/scripts/run_eval.py (the exact commands to cite)
    - .planning/phases/20-skill-effectiveness-evals/20-RESEARCH.md (## Build / Run Split -> the RUN (GATED) command list; ## Common Pitfalls -> Pitfall 1 canary asymmetry, Pitfall 9 no run in-phase, Pitfall 10 executor cannot spawn subagents)
    - CLAUDE.md + memory (eval-run-approval-gate; eval02-subagent-orchestration-mechanic; unbiased-review-beats-primed; gsd-executor-cannot-spawn-subagents; reload-plugins-after-oracle-agent-changes)
  </read_first>
  <action>
    Append a "How to run (GATED -- user approval required)" section to EVAL-RESULTS.md that lists the EXACT ready-to-run
    commands and documents the gates. Do NOT execute any of them.

    EVL-01 forward (lz-red): the canary-gated runners `node run-recall-chunks.mjs` and `node run-spec-chunks.mjs` from
    the workspace root (preferred under any active/rate-limited session), OR the direct probe `python -m
    scripts.run_eval --eval-set ../../evals/trigger-eval.json --skill-path <repo>/plugins/lz-tdd/skills/lz-red --model
    claude-opus-4-8 --runs-per-query 3 --num-workers 1` from tools/skill-creator-eval/ with PONYTAIL_DEFAULT_MODE=off.
    Note the recall/specificity asymmetry (recall is throttle-sensitive, specificity is throttle-robust) and that a
    chunk is trusted only when its appended positive canary fired.

    EVL-01 reciprocal (D-03.2): run the DIRECT probe TWICE with --eval-set ../../evals/reciprocal-red.json and
    --skill-path pointed at lz-tpp, then lz-refactor. Call out explicitly that the canary-gated runners are NOT used
    for the reciprocal probe -- their canary is a lz-red positive that would not fire on a sibling skill-path; the
    reciprocal probe is a specificity measurement (both siblings must stay quiet on the RED prompts, ~100% specificity).
    If a sibling fires on a RED positive, that is a bounded, non-blocking D-09 tuning candidate on THAT sibling's
    description.

    EVL-02 behavior (ORCHESTRATOR steps, post-approval -- the executor cannot spawn subagents): spawn UNNAMED /
    fire-and-forget subagents per scenario x config (with_skill vs no_skill) x run (>=3) in an isolated scratch cwd each;
    wait for ALL completion notifications before grading (persist timing as each arrives); then grade-run.mjs -> the
    skill-creator grader agent (LLM judge on the two dimensions) -> merge-judge.mjs --merge -> merge-judge.mjs --verify
    iteration-1 (pre-aggregate gate) -> python -m scripts.aggregate_benchmark iteration-1 --skill-name lz-red ->
    eval-viewer/generate_review.py. Then compute Pass@k/Pass^k, fill EVAL-RESULTS.md, and run >=1 unbiased-from-scratch
    reviewer over the grader + results (unbiased-beats-primed). Note the judge-string provenance rule (read
    judge_required from grading.preliminary.json; never transcribe judge strings by hand) and that these spawns are
    driven by the ORCHESTRATOR after approval, never by the gsd-executor.

    Document the D-08 soft bars and the at-most-one D-09 tuning pass (conditional, the only write-back into plugins/;
    any SKILL.md edit gets its own >=1 unbiased review; /reload-plugins is a human ship action afterward). End with an
    explicit HALT line: build complete, no eval was run, await user approval before executing any command above.

    Keep the section ASCII-only; do NOT write the work-email or bare domain. This task WRITES commands and STOPS -- it
    runs NONE of them.
  </action>
  <verify>
    <automated>rg -c "GATED|user approval|HALT" .claude/skills/lz-red-workspace/EVAL-RESULTS.md</automated>
    <automated>rg -c "reciprocal-red.json" .claude/skills/lz-red-workspace/EVAL-RESULTS.md</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.claude/skills/lz-red-workspace/EVAL-RESULTS.md','utf8');const nonascii=[...s].some(c=>c.charCodeAt(0)>127);console.log('nonascii',nonascii)"</automated>
  </verify>
  <acceptance_criteria>
    - EVAL-RESULTS.md contains a "How to run (GATED)" section with the exact EVL-01 forward, EVL-01 reciprocal (lz-tpp + lz-refactor, direct run_eval), and EVL-02 orchestrator-driven commands, plus an explicit HALT / await-approval line (rg matches GATED / user approval / HALT).
    - The reciprocal section explicitly states the canary-gated runners are NOT used for the reciprocal probe and names the reason (`rg -c "reciprocal-red.json"` >= 1; the canary caveat is present).
    - The EVL-02 section documents the subagent fan-out + LLM judge + merge/verify/aggregate + >=1 unbiased reviewer as ORCHESTRATOR post-approval steps and states the executor cannot spawn subagents.
    - NO eval, run_eval, run_loop, subagent, or claude -p was executed by this task (the SUMMARY records that nothing metered ran).
    - `node -e` non-ASCII scan of EVAL-RESULTS.md reports false; no work-email / bare domain token present (allowlist-inversion clean).
  </acceptance_criteria>
  <done>EVAL-RESULTS.md documents the exact gated run commands (EVL-01 forward + reciprocal + EVL-02 orchestrator gates), the soft bars, and the conditional D-09 tuning, and the phase HALTS with nothing metered executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored results doc -> committed public repo | EVAL-RESULTS.md is new committed prose (command examples, caveats) in a public repo |
| gated run commands -> claude CLI (token spend) | The documented commands spawn the already-authenticated claude only when a human runs them post-approval; this plan runs none |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | committed EVAL-RESULTS.md | high | mitigate | ASCII + allowlist-inversion email scan of EVAL-RESULTS.md (only the approved public gmail may appear); never write the work-email / bare domain, even in a command example; re-run the allowlist scan before commit (AGENTS.md) |
| T-20-02 | Information Disclosure | committed run-config / command examples | low | mitigate | ASCII-only authoring (->, straight quotes; no emojis / em-dashes / smart quotes) |
| T-20-03 | Denial of Service (token spend) | gated eval run | high | mitigate | D-11 HARD GATE: this plan documents commands and HALTS; the run is user-approved (eval-run-approval-gate). No run_eval / run_loop / subagent / claude -p executed in-phase |
| T-20-SC | Tampering (supply chain) | npm/pip installs | n/a | accept | No package install in this plan or phase; all tooling is on-disk (vendored harness copied byte-identical, workspace devDeps pre-exist). Package-legitimacy gate vacuously satisfied (RESEARCH). |
</threat_model>

## Artifacts this plan produces

- `.claude/skills/lz-red-workspace/EVAL-RESULTS.md` (SCAFFOLD: three-measurement structure + Pass@k/Pass^k tables + run-config + saturation caveats + the gated "How to run" command set + orchestrator-gate + D-09 tuning notes; numbers blank until the gated run)

<verification>
Deterministic, zero-spend, on the merged tree (from repo root):
- `node .claude/skills/lz-red-workspace/check-evals.mjs` -> exit 0.
- `node .claude/skills/lz-red-workspace/grade-run.mjs --selfcheck` -> exit 0.
- `node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck` -> exit 0.
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` and `node .claude/skills/lz-red-workspace/extract-samples.mjs` -> exit 0 (content gate unregressed).
- EVAL-RESULTS.md: ASCII-only, allowlist-inversion clean, carries the three measurement sections + Pass@k/Pass^k tables + the gated command set + HALT line.

HARD GATE (D-11): NO metered run, NO subagent spawn, NO claude -p in this plan. The EVL-01 forward + reciprocal runs and the EVL-02 behavior benchmark are ORCHESTRATOR steps that fire only after explicit user approval, documented in EVAL-RESULTS.md.
</verification>

<success_criteria>
- The full deterministic battery is GREEN on the merged tree and the content gate is unregressed.
- EVAL-RESULTS.md is a complete ready-to-run record (three measurements + Pass@k/Pass^k + caveats + gated command set), numbers blank.
- The phase halts before any metered run; the orchestrator gates + conditional D-09 tuning are documented, not executed.
</success_criteria>

<output>
Create `.planning/phases/20-skill-effectiveness-evals/20-03-SUMMARY.md` when done.

After the executor returns and the deterministic battery is GREEN, the ORCHESTRATOR presents the
EVAL-RESULTS.md "How to run (GATED)" command set to the user and awaits explicit approval before any
metered eval run (EVL-01 forward + reciprocal, EVL-02 behavior). The subagent fan-out, the LLM judge,
the >=1 unbiased-from-scratch reviewer, and the conditional at-most-one D-09 tuning pass are
orchestrator-run post-approval steps, never executor tasks.
</output>
