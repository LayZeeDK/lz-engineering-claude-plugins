---
phase: 14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/tabulate-mechanical.mjs
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/fixtures/meta.sample.json
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/fixtures/answer.sample.md
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/summary.template.json
autonomous: true
requirements: [D-04, D-07]
must_haves:
  truths:
    - "A deterministic tabulator reads phase-14 cell meta.json + answer.md and emits per-(cell,arm) mechanical dimensions: token/cost totals, tool_calls histogram + sub-agent spawn count, distinct-named-refactoring lift count, and Pass@k/Pass^k"
    - "The tabulator reuses the shipped name matcher (nameRe) and the run-e2e Pass@k helpers rather than reinventing them"
    - "node tabulate-mechanical.mjs --selfcheck exits 0 against a fixture with known token/tool/name values (zero spend, no real runs required)"
    - "A summary.template.json fixes the machine-readable head-to-head rollup shape (arms lz-refactor|code_review, one block per D-04 dimension)"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/grading/head-to-head/tabulate-mechanical.mjs"
      provides: "deterministic mechanical-dimension tabulator for the head-to-head, with a --selfcheck mode"
    - path: ".claude/skills/lz-refactor-workspace/grading/head-to-head/summary.template.json"
      provides: "the per-(cell,arm)-per-dimension rollup skeleton Wave 2 fills"
  key_links:
    - from: "tabulate-mechanical.mjs"
      to: "grade-run.mjs nameRe + run-e2e.mjs passAtK/passHatK"
      via: "import/copy the shipped matcher + Pass@k helpers (do not reinvent)"
      pattern: "passAtK|nameRe"
  decisions: |
    - **D-04:** Score the ROADMAP dimensions plus the owner-added TS/FP/OOP idiom+pattern axis, each tagged MECHANICAL (tokens, tools, lift-count -- from meta/transcripts, deterministic here) or GRADED (book authenticity, output quality, over/under-engineering, idiom/pattern fidelity -- oracle/judge in Wave 2). The ~139-vs-12 vocabulary-breadth asymmetry is EXPECTED and reported, not normalized away.
    - **D-07:** Consume the token/cost (total_cost_usd + per-model modelUsage) and per-tool tool_calls fields added to meta.json; report total_cost_usd/modelUsage as the headline cost (rolls up sub-agents) and usage as main-context-only; count sub-agent spawns from the Agent/Task tool_use.
---

<objective>
Build the zero-spend grading scaffolding: a deterministic tabulator that turns the phase-14 run meta.json +
answer.md into the MECHANICAL dimension tables (token/cost, tool usage incl. sub-agent spawn count, lift
count of distinct named refactorings/smells) plus Pass@k/Pass^k, and a machine-readable rollup template for
the full head-to-head. It reuses the shipped `nameRe` matcher and the `run-e2e.mjs` Pass@k helpers, and
self-checks against a fixture so the deterministic dimensions are provably correct BEFORE any real run
exists.

Purpose: separate the deterministic, reusable scoring (tokens/tools/lift) from the Wave-2 oracle/judge work,
and make it self-checkable offline.
Output: `tabulate-mechanical.mjs` (+ `--selfcheck`), fixtures, and `summary.template.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESEARCH.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-PATTERNS.md

meta.json field contract (produced by 14-01; build the tabulator against THIS documented schema so it stays
independent of that plan's file): existing fields (arm, prompt_id, target, run_idx, answer_chars, ...) PLUS
`input_tokens`, `output_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens`, `total_cost_usd`,
`num_turns`, `model_usage: {<model>:{input,output,costUSD}}`, `tool_calls: {<ToolName>:<count>}`.
Arms in this phase: `invoke_skill` (= the lz-refactor arm) and `code_review`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical tabulator + fixtures + --selfcheck</name>
  <files>.claude/skills/lz-refactor-workspace/grading/head-to-head/tabulate-mechanical.mjs, .claude/skills/lz-refactor-workspace/grading/head-to-head/fixtures/meta.sample.json, .claude/skills/lz-refactor-workspace/grading/head-to-head/fixtures/answer.sample.md</files>
  <read_first>
    - grade-run.mjs lines 51-127 (`nameRe` word-bounded matcher + FOWLER/KERIEVSKY/KERIEVSKY_AWAY name arrays) and line 44-45 (`--selfcheck` usage). run-e2e.mjs lines 442-468 (`comb`/`passAtK`/`passHatK`) and 481-548 (the existing `report()` meta-walk it models on). 14-PATTERNS.md "grading/*" section + "Shared Patterns / Pass@k". 14-RESEARCH.md "Meta Capture Detail" (sub-agent spawn count = the Agent/Task tool_use count; total_cost_usd/modelUsage roll up sub-agents).
  </read_first>
  <action>
    Author `tabulate-mechanical.mjs` that walks a results dir (default the two phase-14 recommend trees, `--suite`/`--results` overridable), reads every `meta.json` for arms `invoke_skill` and `code_review`, and emits per (cell, arm): token/cost totals (headline = `total_cost_usd` + `model_usage` opus row; also `input_tokens`/`output_tokens` as main-context-only), the merged `tool_calls` histogram, the sub-agent SPAWN count (sum of `tool_calls` entries whose name is the Agent/Task spawn tool -- expected ~2 for code_review, 0 for lz-refactor; count by name, do NOT hardcode a single spelling), and a LIFT count = distinct canonical refactoring/smell NAMES matched in `answer.md` via the reused `nameRe` over the FOWLER+KERIEVSKY name arrays (import from grade-run.mjs or copy the arrays; mark lift as a mechanical heuristic -- the graded lift refinement is oracle-owned in Wave 2). Reuse `passAtK`/`passHatK` (import or copy verbatim per PATTERNS Pattern F) and report k=1,3. Write the rollup to `grading/head-to-head/mechanical.json`. Add a `--selfcheck` mode (model on grade-run.mjs --selfcheck) that runs against the two committed fixtures (`fixtures/meta.sample.json` with known token/tool values incl. an Agent-spawn entry; `fixtures/answer.sample.md` naming two known Fowler refactorings) and asserts the tabulator computes the expected token totals, spawn count, lift count, and Pass@k -- exit 1 on mismatch, exit 0 with an OK line on success. ASCII-only, fail-closed.
  </action>
  <acceptance_criteria>
    - `node grading/head-to-head/tabulate-mechanical.mjs --selfcheck` exits 0 and prints an OK line.
    - The fixtures deterministically pin the expected spawn count, lift count, token totals, and Pass@k the selfcheck asserts.
    - The script imports/copies `nameRe` + `passAtK`/`passHatK` rather than reimplementing them; ASCII-only; fails closed on a missing/garbled meta.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/grading/head-to-head/tabulate-mechanical.mjs --selfcheck</automated>
  </verify>
  <done>The deterministic mechanical dimensions (tokens/tools/spawn/lift/Pass@k) compute correctly against a fixture with zero spend.</done>
</task>

<task type="auto">
  <name>Task 2: Head-to-head rollup template</name>
  <files>.claude/skills/lz-refactor-workspace/grading/head-to-head/summary.template.json</files>
  <read_first>
    - grading/summary.json (the Phase-13 per-(cell,arm) rollup shape + the `passk_source` note) and grading/book-authenticity/claims.json lines 1-19 (per-claim record shape). 14-PATTERNS.md "grading/*" section + "14-RESULTS.md" dimension list. 14-RESEARCH.md "Grading Architecture (D-04/D-05)".
  </read_first>
  <action>
    Author `summary.template.json`: the machine-readable head-to-head rollup skeleton with arms `lz-refactor` (= invoke_skill) and `code_review`, one block per D-04 dimension -- `lift`, `token_usage`, `tool_usage`, `output_quality`, `book_authenticity`, `over_under_engineering`, `idiom_pattern` -- each block a per-(cell,arm) row with `{n,c,passAt1,passAt3,passHat3}` where run counts allow, plus a top-level `passk_source` note pointing at run-e2e.mjs's helpers and a `dimension_kind` map tagging each dimension MECHANICAL or GRADED. Leave numeric cells as null placeholders (Wave 2 / the tabulator fill them). Include the ~139-vs-12 vocabulary-breadth headline as a documented field on the `idiom_pattern` block. ASCII-only.
  </action>
  <acceptance_criteria>
    - `summary.template.json` parses as JSON, lists both arms, one block per the seven D-04 dimensions, each tagged MECHANICAL or GRADED, with null numeric placeholders.
    - The `idiom_pattern` block carries the vocabulary-breadth headline field; ASCII-only.
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('node:fs').readFileSync('.claude/skills/lz-refactor-workspace/grading/head-to-head/summary.template.json','utf8'))"</automated>
  </verify>
  <done>The head-to-head rollup skeleton fixes the Wave-2 output shape across all seven dimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grading code -> committed workspace | New tabulator + fixtures + template land in a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | committed tabulator + fixtures + template | mitigate | ASCII-only; no work email; allowlist-inversion hygiene at commit; fixtures carry synthetic values only |
| T-14-07 | Tampering (measurement validity) | a silent tabulation bug over-/under-counting a dimension | mitigate | `--selfcheck` fixture with known values asserts token/spawn/lift/Pass@k; fail-closed on garbled meta |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | N/A -- no packages installed; Node stdlib + reused in-repo helpers only |
</threat_model>

<verification>
- `node grading/head-to-head/tabulate-mechanical.mjs --selfcheck` exits 0.
- `summary.template.json` parses and covers all seven D-04 dimensions with a MECHANICAL/GRADED tag each.
- No metered `claude -p` run occurs in this plan.
</verification>

<success_criteria>
The deterministic mechanical scoring (tokens, tools, sub-agent spawn count, lift, Pass@k) is built and self-checked against a fixture, and the head-to-head rollup shape is fixed -- all zero-spend, before the D-12 gate.
</success_criteria>

<output>
Create `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-03-SUMMARY.md` when done.
</output>
