---
phase: 14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
  - .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs
autonomous: true
requirements: [D-06, D-07, D-02]
must_haves:
  truths:
    - "node run-e2e.mjs --dry-run --arm code_review --synthetic-base composes /mattpocock-skills:code-review <ROOT_SHA> with --plugin-dir at the mattpocock cache and Bash NOT in --disallowedTools"
    - "node run-e2e.mjs --dry-run --arm invoke_skill still blocks Bash (recommend profile unchanged)"
    - "A per-target throwaway branch (empty-root -> target-file-only commit) builds from applyBase, three-dot diff resolves scoped to the target path, and teardown leaves the borrowed repo git-status clean with no review-* branch/worktree"
    - "extractResult() returns token/cost/tool-histogram fields; meta.json carries them alongside the existing fields"
    - "node e2e-nx/selfcheck-code-review.mjs exits 0 over all three cruxes (composition, synthetic-branch build/teardown+scope, transcript parse)"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs"
      provides: "code_review competitor arm + token/tool meta capture + synthetic-base worktree setup/teardown, extended in place"
      contains: "code_review"
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs"
      provides: "offline zero-spend Wave-0 self-check for the three harness-extension cruxes"
  key_links:
    - from: "e2e-nx/selfcheck-code-review.mjs"
      to: "e2e-nx/run-e2e.mjs"
      via: "spawns --dry-run --arm code_review and calls the synthetic-branch helper + extractResult over an on-disk transcript"
      pattern: "run-e2e"
  decisions: |
    - **D-02:** Feed code-review the SAME target file(s) as a whole-file diff against a synthetic empty-root baseline (empty-root commit -> target-file-only commit; fixed point = ROOT), built from the suite applyBase (origin/23.0.x / main), NOT HEAD; both arms run in one worktree for byte-identical input. The empty-tree hash is NOT usable as the fixed point (breaks code-review's three-dot flow).
    - **D-06:** Extend run-e2e.mjs with a code_review arm that loads the mattpocock plugin via --plugin-dir and invokes /mattpocock-skills:code-review <ROOT_SHA>, with its OWN tool profile (allow Bash + the sub-agent-spawn tool; block Edit/Write); ADD the arm, do not rewrite.
    - **D-07:** Extend meta.json to record token usage (input/output/cache + total_cost_usd + per-model modelUsage + num_turns) and a per-tool tool_calls histogram, read from the stream-json result event and the tool_use blocks.
---

<objective>
Extend the existing recommend harness `run-e2e.mjs` in place (D-06 "ADD the arm, do not rewrite") with three
things needed for the lz-refactor-vs-code-review head-to-head, then gate them offline with a zero-spend
self-check:

1. A `code_review` competitor arm that loads the mattpocock plugin and invokes
   `/mattpocock-skills:code-review <ROOT_SHA>` with its own tool profile (D-06).
2. A `--synthetic-base` path that builds a per-target throwaway branch (whole target file as a diff against
   an empty-root baseline) so both arms see byte-identical pristine input (D-02).
3. Token/cost/tool-histogram fields in `meta.json` (D-07).

Purpose: this is the load-bearing zero-spend build for the phase. Nothing metered runs until Wave 1, and the
self-check here is the Wave-0 gate that proves the harness composes correctly and leaves borrowed repos
pristine BEFORE any spend.
Output: an extended `run-e2e.mjs` + a new `selfcheck-code-review.mjs`, both self-checked offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESEARCH.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-PATTERNS.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-VALIDATION.md

Verified facts (do not re-derive):
- mattpocock plugin root (the --plugin-dir for code_review): `C:/Users/LarsGyrupBrinkNielse/.claude/plugins/cache/mattpocock/mattpocock-skills/1.2.0` (contains `.claude-plugin/{plugin,marketplace}.json`). Make it a module-level const `MATTPOCOCK_DIR` with a `process.env.MATTPOCOCK_DIR` override, and record the resolved dir in meta for drift detection (RESEARCH A4).
- code-review's command form: `/mattpocock-skills:code-review <fixed-point>`; it runs `git rev-parse`/`git diff <fp>...HEAD`/`git log` (needs Bash) and spawns sub-agents (needs the Agent/Task tool). It never edits.
- The empty-tree object `4b825dc642cb6eb9a060e54bf8d69288fbee4904` is NOT a valid code-review fixed point (three-dot diff exits 128). Use a synthetic empty-root commit as ROOT (RESEARCH Pattern 3 + Anti-Patterns).
- nx pristine target blob is under `origin/23.0.x`; the nx working clone is on a leftover `lz-refactor-e2e-smoke` branch, so build synthetic trees from applyBase, not HEAD (RESEARCH Pitfall 3).
- kata git root is the PARENT of the suite `repo` (TypeScript subdir); from the git root the target is `TypeScript/app/gilded-rose.ts` (RESEARCH Pitfall 4).
- The `result` event carries `usage`/`total_cost_usd`/`modelUsage`/`num_turns`; tool usage = count of top-level `tool_use` blocks by `name` (RESEARCH "Meta Capture Detail").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture token/cost/tool meta (D-07)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs</files>
  <read_first>
    - run-e2e.mjs lines 221-277 (`extractResult`, the existing result-event branch at 237-239 and the tool_use loop at 247-267) and lines 411-431 (the `meta` object). 14-PATTERNS.md "Pattern E". 14-RESEARCH.md "Meta Capture Detail (D-07)".
  </read_first>
  <action>
    In `extractResult`, inside the existing `tool_use` loop (247-267) add a `toolCalls[block.name] = (toolCalls[block.name] || 0) + 1;` histogram; in the existing result-event branch (237-239) also read `ev.usage`, `ev.total_cost_usd`, `ev.modelUsage`, `ev.num_turns` from the same `result` event. Return the new values alongside the current ones (per D-07): `input_tokens`, `output_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens`, `total_cost_usd`, `num_turns`, `model_usage` (map `modelUsage` to `{<model>:{input,output,costUSD}}`), and `tool_calls` (the histogram). In `runOne`'s `meta` object (411-431) add exactly those fields, KEEPING every existing field. Do NOT recompute tokens from transcript text (RESEARCH "Don't Hand-Roll") -- read the CLI's reported usage. Report `total_cost_usd`/`model_usage` as the headline cost (rolls up sub-agents); `usage` (input/output) is main-context-only.
  </action>
  <acceptance_criteria>
    - `extractResult` returns the 8 new fields; parsing an existing Phase-13 transcript yields `input_tokens > 0`, `total_cost_usd > 0`, and a non-empty `tool_calls` map.
    - Every pre-existing `meta.json` field (prompt_id..answer_chars) is still written.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs</automated>
  </verify>
  <done>meta capture emits token/cost/tool fields from the result event; the selfcheck transcript-parse crux passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the code_review arm + --synthetic-base worktree (D-06, D-02)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs</files>
  <read_first>
    - run-e2e.mjs lines 86-139 (`parseArgs`, the `--arm` allowlist at 126-128), 179-189 (`composePrompt`, the `invoke_skill` branch), 191-216 (`buildCmd`, tool profile at 203-206 + `--plugin-dir` push at 211-213), 282-292 (`git()` helper), 337-353 + 399-409 (apply-mode guarded worktree discipline), 551-647 (`main` loop + dry-run). 14-PATTERNS.md "Patterns A/B/C/D". 14-RESEARCH.md "Pattern 3" (the VERIFIED plumbing sequence) + Pitfalls 3/4/6 + "Pattern 1" (tool-profile asymmetry).
  </read_first>
  <action>
    Add `MATTPOCOCK_DIR` + a `CODE_REVIEW_COMMAND = '/mattpocock-skills:code-review'` module const. (A) `parseArgs`: add `code_review` to the `--arm` allowlist (line 126); add a boolean `--synthetic-base` flag; do NOT add `code_review` to the `both`/`all` fan-outs (it runs only when explicitly selected). (B) `composePrompt`: add an `arm === 'code_review'` branch returning `${CODE_REVIEW_COMMAND} ${promptEntry.root_sha}` with NO preamble/body; thread `root_sha` via `promptEntry` (set during setup) so the signature stays the same. (C) `buildCmd`: give `code_review` its own tool profile -- `--disallowedTools Edit Write MultiEdit NotebookEdit` (DROP `Bash`, leave the Agent/Task spawn tool allowed) while keeping the existing recommend profile (Bash blocked) for the other arms; push `--plugin-dir MATTPOCOCK_DIR` for `code_review` and `PLUGIN_DIR` for `with_skill`/`invoke_skill`. Do NOT equalize the profiles (RESEARCH Anti-Patterns; the asymmetry is the D-04 finding). (D) Synthetic-base helper + main() wiring: when `--synthetic-base` is set, before the run loop build a per-target throwaway branch from the suite `applyBase` using the VERIFIED sequence in 14-RESEARCH.md "Pattern 3" (`EMPTY -> commit-tree ROOT`; `read-tree --empty` + `update-index --add --cacheinfo <blob from applyBase:path>,path` into a temp `GIT_INDEX_FILE` -> `write-tree TREE` -> `commit-tree TREE -p ROOT` -> `branch review-<target>` -> `worktree add`); the target `file` comes from `targets.json[promptEntry.target].file`; resolve the git root via `git -C <suite.repo> rev-parse --show-toplevel` and prepend the subdir prefix for kata (`TypeScript/`, Pitfall 4); set `promptEntry.root_sha = ROOT` and run BOTH selected arms with `cwd` = `<worktree>/<suite.repo-subdir>` (the worktree git root for nx; explicitly `<worktree>/TypeScript` for kata, Pitfall 4) so lz-refactor resolves the subdir-relative target the prompt names (e.g. kata `app/gilded-rose.ts`) and code_review still diffs ROOT...HEAD there. Reuse `git()` with `mustSucceed: true` on every state-changing step; refuse `PROTECTED_BRANCHES`; teardown finally-style (`worktree remove --force` + `branch -D review-*`) so a failed run still cleans up (Pitfall 6). Assert `code_review` is only used together with `--synthetic-base`. In `--dry-run`, build only ROOT via `commit-tree` (a loose object, no branch/worktree) to get `root_sha` for command composition, leaving the borrowed repo clean; the full worktree is built only for real runs.
  </action>
  <acceptance_criteria>
    - `--dry-run --arm code_review --synthetic-base --prompt p1` prints `cmd:` containing `/mattpocock-skills:code-review <40-hex ROOT>`, `--plugin-dir` = the mattpocock cache, and NO `Bash` in `--disallowedTools`; the borrowed repo `git status --porcelain` is empty afterward.
    - `--dry-run --arm invoke_skill --prompt p1` still shows `--disallowedTools` INCLUDING `Bash` and `--plugin-dir` = `plugins/lz-tdd`.
    - A full synthetic-base build for one target yields a three-dot `git diff ROOT...TIP --stat` that is non-empty and `git ls-tree -r --name-only TIP` equal to exactly the target path(s); teardown removes the branch + worktree and `git status --porcelain` is empty.
    - `code_review` without `--synthetic-base` throws a clear error.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs</automated>
  </verify>
  <done>code_review arm composes on-grain, the synthetic worktree builds from applyBase and tears down clean, and the tool-profile asymmetry is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Offline Wave-0 self-check (selfcheck-code-review.mjs)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs</files>
  <read_first>
    - check-evals.mjs lines 13-40 (`import.meta.url` here-dir + read-or-fail), 21-24 (`fail()` + `process.exit(1)` fail-closed), 84-91 (ASCII assertion), 136-141 (OK + exit 0). 14-PATTERNS.md "selfcheck-code-review.mjs" section (the 3 cruxes) + "Shared Patterns / Fail-closed offline self-check". 14-VALIDATION.md "Wave 0 Requirements". 14-RESEARCH.md "Pitfall 4" (kata path-prefix mismatch: the kata git root is the PARENT of the `TypeScript/` suite; the cr-gr prompt names the subdir-relative `app/gilded-rose.ts` while the synthetic tree stores the root-relative `TypeScript/app/gilded-rose.ts`).
  </read_first>
  <action>
    Author a self-contained Node `assert`-style, fail-closed, ASCII-clean self-check (copy check-evals.mjs's skeleton) covering the three RESEARCH cruxes, spending nothing: (1) COMPOSITION -- `spawnSync` `node e2e-nx/run-e2e.mjs --dry-run --arm code_review --synthetic-base --prompt p1` and assert the composed prompt is `/mattpocock-skills:code-review <40-hex>`, `--plugin-dir` is the mattpocock cache, and `Bash` is NOT in `--disallowedTools`; also `--dry-run --arm invoke_skill --prompt p1` asserts `Bash` IS blocked. (2) SYNTHETIC BRANCH -- call the runner's synthetic-base build for BOTH a nx target (`packages/eslint-plugin/src/rules/enforce-module-boundaries.ts` from `origin/23.0.x`) AND the kata target (`TypeScript/app/gilded-rose.ts` from the kata `applyBase` `main`) -- git-only, zero-spend. For EACH, assert `git diff ROOT...TIP --stat` non-empty AND `git ls-tree -r --name-only TIP` equals the ROOT-relative target path (for kata that is exactly `TypeScript/app/gilded-rose.ts`, Pitfall 4), then teardown and assert `git status --porcelain` empty + no `review-*` branch/worktree in that repo. The kata case exercises the path-prefix resolution (the kata git root is the PARENT of the `TypeScript/` suite): also assert the synthetic-base helper computed the kata arm `cwd` as `<worktree>/TypeScript`, so the bug surfaces here (git-only) instead of during METERED kata spend. (3) TRANSCRIPT PARSE -- run the extended `extractResult()` over the existing on-disk transcript `e2e-nx/results/apply/no_skill/p1/run-1/outputs/transcript.stream.jsonl` and assert `input_tokens > 0`, `total_cost_usd > 0`, `tool_calls` non-empty. Fail loudly (exit 1) with a clear message on any violation; print `selfcheck-code-review: OK` + exit 0 when all three pass. Confirm each borrowed repo is clean at the end. Do NOT wire this into `npm run check` (it touches borrowed repos); it runs explicitly.
  </action>
  <acceptance_criteria>
    - `node e2e-nx/selfcheck-code-review.mjs` exits 0 and prints the three-crux OK line.
    - Crux 2 builds+tears down BOTH a nx target AND the kata target (`TypeScript/app/gilded-rose.ts` from `main`), asserting for each a non-empty three-dot diff, `git ls-tree -r --name-only TIP` equal to the ROOT-relative target path, and -- for kata -- that the helper set the arm `cwd` to `<worktree>/TypeScript` (Pitfall 4).
    - After it runs, BOTH borrowed repos are clean: `git -C D:/projects/github/nrwl/nx status --porcelain` and `git -C D:/projects/github/emilybache/GildedRose-Refactoring-Kata status --porcelain` are empty with no `review-*` branch/worktree remaining in either.
    - The script is ASCII-only and fails closed (missing transcript / dirty repo / Bash-in-code_review-profile all exit 1).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs</automated>
  </verify>
  <done>The zero-spend Wave-0 gate is green over all three cruxes and leaves the borrowed repos pristine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner -> borrowed repos (nx, kata) | The runner's own git child mutates throwaway branches/worktrees in repos it does not own |
| harness code -> committed workspace | New/edited files are committed to a public repo (maintainer PII + ASCII hygiene) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | synthetic-base setup writing to borrowed repos | mitigate | Build from applyBase into a THROWAWAY `review-*` branch/worktree only; recommend tool profile blocks Edit/Write on both arms (code_review keeps Bash but not Edit/Write); refuse PROTECTED_BRANCHES |
| T-14-02 | Tampering | leftover branch/worktree after a run | mitigate | Finally-style teardown (`worktree remove --force` + `branch -D`); selfcheck asserts `git status --porcelain` empty + no `review-*` remains |
| T-14-03 | Information Disclosure | committed run-e2e.mjs / selfcheck source | mitigate | ASCII-only; no work email; allowlist-inversion hygiene at commit (AGENTS.md) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | N/A -- this plan installs NO packages (RESEARCH Package Legitimacy Audit); Node stdlib + system git + already-cached plugin only |
</threat_model>

<verification>
- `node .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs` exits 0 (all three cruxes).
- `node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --dry-run --arm code_review --synthetic-base --prompt p1` and `--arm invoke_skill --prompt p1` both compose cleanly, zero spend.
- Borrowed repos (`D:/projects/github/nrwl/nx`, `D:/projects/github/emilybache/GildedRose-Refactoring-Kata`) are `git status` clean.
- No metered `claude -p` run occurs in this plan.
</verification>

<success_criteria>
The harness composes the code_review arm on-grain, builds+tears down the synthetic whole-file-diff baseline from applyBase leaving borrowed repos pristine, captures token/tool meta, and the offline self-check gate is green -- all with zero spend.
</success_criteria>

<output>
Create `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-01-SUMMARY.md` when done.
</output>
