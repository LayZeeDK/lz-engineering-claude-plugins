---
phase: 10-distribution-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs
autonomous: true
requirements: [DST-02, DST-04]

must_haves:
  truths:
    - "check-hygiene.mjs axes (a) ASCII-only and (b) work-email allowlist scan the WIDE target set: both skill trees (lz-refactor SKILL.md + references, lz-tpp SKILL.md + references), the repo-root README.md / CHANGELOG.md / LICENSE, and both manifests (plugins/lz-tdd/.claude-plugin/plugin.json + .claude-plugin/marketplace.json); the printed 'files:' count reflects the widened set"
    - "The non-.md targets (LICENSE has no extension; both manifests are .json) are added as explicit targets.push entries because walkMd is .md-only (line 63) and would skip them vacuously; the repo-root README.md / CHANGELOG.md are also explicit pushes because walkMd only recurses the lz-refactor references directory"
    - "The no-verbatim axis (c) is promoted from warn() to a HARD report() gate (drives the non-zero exit) and scans the NARROWER verbatim target set -- the lz-refactor tree + the repo-root README.md + CHANGELOG.md ONLY -- EXCLUDING lz-tpp (its transformations.md is cited FibTPP by design), LICENSE (verbatim OSI MIT), and both manifests (short quoted JSON)"
    - "The email allowlist shape is unchanged: enumerate every email-shaped token, subtract the approved public gmail larsbrinknielsen@gmail.com, assert the remainder is empty; the work-email literal is NOT written into the checker in any form"
    - "Regression baseline: node check-hygiene.mjs exits 0 on today's pre-content tree -- the widened (a)/(b) set is ASCII-clean and work-email-absent, and the hardened (c) gate finds no verbatim-looking quoted run at or above the 120-char threshold over its target set"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs"
      provides: "The widened, per-axis-split, hardened hygiene gate (this phase's instrument)"
      contains: "verbatimTargets"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs"
      to: ".claude/skills/lz-refactor-workspace/package.json"
      via: "the npm run check battery invokes check-hygiene.mjs (8th of 10 checkers); the checker is EXTENDED in place, not replaced by a sibling, so the chain is unchanged"
      pattern: "check-hygiene.mjs"
  decisions: |
    - **D-01:** layer 1 of the three-layer DST-04 scan -- promote check-hygiene.mjs axis (c) from a WARN to a HARD report() gate; the deterministic, source-free layer that runs in the normal battery (this phase cannot defer back to the checker: check-hygiene.mjs:8 names the Phase-10 scan as the authoritative no-verbatim gate)
    - **D-04:** the no-verbatim book-prose axis is scoped to the lz-refactor tree plus NEW Phase-10 prose (root README primer + CHANGELOG entry); axis (c) EXCLUDES lz-tpp (verbatim cited FibTPP by design), which is why the checker carries a per-axis target-set split rather than one shared array
    - **D-10:** widen the ASCII + work-email walk from lz-refactor-only to both skill trees + root README.md/CHANGELOG.md/LICENSE + both manifests -- the exact files this phase writes were previously UNGATED for both axes; the leak recurred twice in Phase 4
    - **D-11:** keep the allowlist-subtraction shape of the email check (enumerate email-shaped tokens, subtract the approved public gmail, assert remainder empty); NEVER write the work-email literal into the checker
    - **D-17:** micro-decision resolved here -- the hardened DST-04 detector EXTENDS check-hygiene.mjs (lower surface, reuses the existing repoRoot anchor) rather than landing as a sibling checker; the per-axis split is implemented as two internal target arrays
---

<objective>
Harden and widen check-hygiene.mjs into this phase's instrument BEFORE any content prose is authored
(instrument-first / Nyquist). Two changes to one file: (1) widen the ASCII + work-email walk to the
full shippable surface with a per-axis target-set split (D-10, D-04, D-11); (2) promote the no-verbatim
axis (c) from a WARN to a HARD gate over the narrower verbatim set (D-01 layer 1, D-17 extend-not-sibling).

Purpose: the files this phase writes (README, CHANGELOG, both manifests) and the whole lz-tpp skill are
UNGATED today for ASCII and work-email; and check-hygiene.mjs:8 names the Phase-10 scan as the
authoritative no-verbatim gate, so this phase must realize it. Landing the instrument first means every
byte of new Phase-10 prose is authored UNDER the widened gate and any violation is caught as it lands.

Output: an edited check-hygiene.mjs that runs GREEN on today's tree (a regression baseline, because the
tree is already clean), ready to gate Wave 2's content edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-distribution-hygiene/10-CONTEXT.md
@.planning/phases/10-distribution-hygiene/10-RESEARCH.md
@.planning/phases/10-distribution-hygiene/10-PATTERNS.md

The exact in-file hooks (report()/warn() split at lines 30-41; the (c) block at lines 125-137; the
explicit targets.push pattern at lines 83-89; the .md-only walkMd filter at line 63; the repoRoot
anchor at line 17; the SKILL_DIR/SKILL_MD/REFERENCES constants at lines 18-20) are excerpted in
10-PATTERNS.md under "check-hygiene.mjs". The per-axis split has no in-file analog (10-PATTERNS.md
"No Analog Found") -- build two internal target arrays.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the ASCII + work-email walk with a per-axis target-set split (D-10, D-04, D-11)</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs (the FILE being edited -- current state: 148 lines; repoRoot at :17; SKILL_DIR/SKILL_MD/REFERENCES at :18-20; the single shared `targets` array built at :83-89 with an explicit targets.push(SKILL_MD) then walkMd(REFERENCES); walkMd's .md-only filter at :63; axis (a) at :96-107 and axis (b) at :109-123 both looping over targets)
    - .planning/phases/10-distribution-hygiene/10-PATTERNS.md (Hook 3 explicit-file push, Hook 4 walkMd .md-only trap, and the "No Analog Found" per-axis split spec)
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md (Pitfall 1: non-.md targets silently skipped; the per-axis target-set split under "Layer 1"; the widened D-10 baseline commands and expected counts)
  </read_first>
  <action>
    Edit check-hygiene.mjs. Add new path constants next to SKILL_DIR/SKILL_MD/REFERENCES (all resolved
    from the existing repoRoot at :17): the lz-tpp skill dir (plugins/lz-tdd/skills/lz-tpp), its SKILL.md
    and references dir; the repo-root README.md, CHANGELOG.md, and LICENSE; and the two manifest paths
    (plugins/lz-tdd/.claude-plugin/plugin.json and .claude-plugin/marketplace.json). Replace the single
    shared targets array with two arrays: wideTargets for axes (a)+(b) and verbatimTargets for axis (c).
    Build wideTargets = lz-refactor SKILL.md (explicit push) + walkMd(lz-refactor references) + lz-tpp
    SKILL.md (explicit push) + walkMd(lz-tpp references) + explicit pushes of README.md, CHANGELOG.md,
    LICENSE, plugin.json, marketplace.json. Add the non-.md targets (LICENSE, both manifests) and the
    root-level README.md/CHANGELOG.md as explicit targets.push entries mirroring the existing
    targets.push(SKILL_MD) shape -- do NOT route them through walkMd (its .endsWith(".md") filter at :63
    skips LICENSE + the JSON manifests, and walkMd only recurses the references dir so it never reaches
    the root files). Point axis (a) ASCII (:96-107) and axis (b) email (:109-123) loops at wideTargets.
    Update the two console.log lines (:92-93) so the printed "files:" count is the wideTargets length and
    the label no longer says "skill dir" only. Leave APPROVED_EMAILS / EMAIL_RE (:23-24) and the
    allowlist-subtraction logic UNCHANGED (D-11) -- widen the target set only, and do NOT write the
    work-email literal anywhere in the file. Also define verbatimTargets here (Task 2 consumes it):
    lz-refactor SKILL.md + walkMd(lz-refactor references) + explicit README.md + CHANGELOG.md pushes,
    EXCLUDING lz-tpp, LICENSE, and both manifests.
  </action>
  <acceptance_criteria>
    - check-hygiene.mjs defines explicit path constants for the lz-tpp skill (SKILL.md + references), the repo-root README.md / CHANGELOG.md / LICENSE, and both manifests, all resolved from repoRoot
    - The non-.md targets (LICENSE, plugin.json, marketplace.json) and the root README.md/CHANGELOG.md are added via explicit targets.push, NOT via walkMd
    - Axes (a) and (b) loop over the wide target set; the printed "files:" count increased by 9 relative to the lz-refactor-only baseline (lz-tpp SKILL.md + 3 lz-tpp references + README.md + CHANGELOG.md + LICENSE + 2 manifests)
    - APPROVED_EMAILS and EMAIL_RE are unchanged; the string "@consensus" appears nowhere in the file
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (ASCII + email PASS over the widened set)
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -uu -q "marketplace.json" .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -uu -q "lz-tpp" .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && ! rg -uu -q "@consensus" .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>check-hygiene.mjs scans the widened (a)/(b) target set via explicit pushes for the non-.md and root files; verbatimTargets is defined; the checker exits 0 with an increased "files:" count; the work-email literal is absent from the file.</done>
</task>

<task type="auto">
  <name>Task 2: Promote axis (c) no-verbatim from WARN to a HARD gate over verbatimTargets (D-01 layer 1, D-17)</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs (the FILE being edited -- the report()/warn() split at :30-41; the (c) block at :125-137 currently calling warn() per long-quote hit; QUOTE_THRESHOLD=120 at :25; the (a)/(b) single-report() call shape at :107 and :123; the header comment at :6-9 that says "(c) is WARN-level only"; the SUMMARY lines at :142/:146 mentioning "no-verbatim WARN(s)")
    - .planning/phases/10-distribution-hygiene/10-PATTERNS.md (Hook 1 report()/warn() split, Hook 2 the (c) block to harden)
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md (Layer 1 hardening; Pitfall 2 false-fail on a legitimate 120+ quote and the widen-never-weaken rule; A3 "expected GREEN on today's tree")
  </read_first>
  <action>
    Edit check-hygiene.mjs. Change the (c) loop (:125-137) to iterate verbatimTargets (not the wide set)
    and to COLLECT every run whose inner length is >= QUOTE_THRESHOLD into a hits array instead of calling
    warn() per hit. After the loop, emit a single HARD gate line via report(hits.length === 0, "no
    verbatim-looking quoted runs (DST-04)", detail) -- mirroring the single-report() shape axes (a) and
    (b) already use at :107 and :123; when hits exist, the detail lists file:charcount per hit and report()
    increments failures so the process exits non-zero. Update the header comment (:6-9) so it states (c)
    is now a HARD failure alongside (a) and (b) -- remove the "(c) is WARN-level only ... the authoritative
    no-verbatim gate is the D-07 oracle checkpoint plus the Phase-10 hygiene scan" wording and say (c) is
    the realized Phase-10 no-verbatim gate. Update the two SUMMARY console.log lines (:142/:146) so they no
    longer describe (c) as a WARN. Then run the checker against today's tree: it MUST exit 0 (regression
    baseline; the lz-refactor tree already passed the per-leaf oracle gate with DST-04 as an axis and
    reported 0 WARN in the Phase-9 battery, and README/CHANGELOG carry no 120+ char double-quoted run). If
    a LEGITIMATE 120+ char quoted run trips it (an original quoted sentence, or a long quoted error string
    in a TS example), REFINE the heuristic (raise/scope the threshold, or restrict detection to prose
    outside code fences) so it keeps its verbatim-detection value -- do NOT weaken it below detection value
    and do NOT revert (c) to a WARN (widen a gate, never weaken one).
  </action>
  <acceptance_criteria>
    - The (c) DST-04 long-quote hit routes through report(false, ...) (HARD, increments failures), NOT warn(); the old warn("long quoted run ...") call no longer exists in the file
    - Axis (c) iterates verbatimTargets (lz-refactor tree + root README.md + CHANGELOG.md), NOT the wide set; lz-tpp, LICENSE, and both manifests are absent from axis (c)'s input
    - The header comment (:6-9) no longer says axis (c) is WARN-level-only; it describes (c) as a HARD Phase-10 no-verbatim gate
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 on today's tree (regression baseline GREEN as a HARD gate)
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -uu -q "verbatimTargets" .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && ! rg -uu -q 'warn\("long quoted' .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>Axis (c) is a HARD report() gate over verbatimTargets; the old warn() long-quote call is gone; the header comment reflects the realized Phase-10 gate; the checker exits 0 as a HARD-gate regression baseline on today's tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits ONE non-shipped Node checker. No runtime, network, session, crypto, or user-input
surface. The instrument itself is the mitigation surface for the phase's real threats (PII leak,
verbatim IP leak, mojibake) over the shippable public tree.

| Boundary | Description |
|----------|-------------|
| authored content -> committed public repo | new Phase-10 prose (README/CHANGELOG) and both skill trees must be ASCII-only + work-email-free before commit |
| shipped lz-refactor prose -> published tree | no verbatim Fowler/Kerievsky/GoF prose or code (D-04 axis c) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-EM | Information Disclosure | work/PII email in a public file | mitigate | Axis (b) allowlist-subtraction widened (D-10) to both skill trees + root README/CHANGELOG/LICENSE + both manifests; allowlist shape unchanged (D-11); leak recurred twice in Phase 4 |
| T-10-ASCII | Information Disclosure / Integrity | non-ASCII mojibake (cp1252) in committed output | mitigate | Axis (a) ASCII byte-scan widened (D-10) to the same wide target set |
| T-10-IP | Information Disclosure / IP | verbatim book prose/code in the lz-refactor tree + new prose | mitigate | Axis (c) hardened from WARN to a HARD gate over verbatimTargets (D-01 layer 1); the semantic clean-room sweep is 10-03; excludes lz-tpp/LICENSE/manifests (D-04) |
| T-10-SC | Tampering | npm/pip/cargo package installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: Not applicable); the pinned typescript@6.0.3 devDependency is unchanged from Phase 7 |
</threat_model>

<verification>
- node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (widened (a)/(b) + hardened (c) all GREEN on today's tree -- a regression baseline).
- The printed "files:" count reflects the widened set (increased by 9 over the lz-refactor-only baseline).
- Source assertions: verbatimTargets defined; the manifests + lz-tpp appear in wideTargets construction; the old warn() long-quote call is removed; "@consensus" absent from the file.
- No other checker weakened; the npm run check chain still lists check-hygiene.mjs (verified at the 10-04 gate, not here).
</verification>

<success_criteria>
The hygiene instrument is widened and hardened in place: axes (a)/(b) scan the full shippable surface;
axis (c) is a HARD no-verbatim gate over the lz-refactor tree + new root prose only; the tree runs
GREEN as a regression baseline; and no existing HARD check was relaxed.
</success_criteria>

<output>
Create `.planning/phases/10-distribution-hygiene/10-01-SUMMARY.md` when done.
</output>
