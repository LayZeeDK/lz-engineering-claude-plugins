---
phase: 09-coach-behavior-principle-backing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/lib/heading-scan.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-gof.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-functional.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-backing.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
  - .claude/skills/lz-refactor-workspace/package.json
autonomous: true
requirements: [PRIN-01, PRIN-02, PRIN-03]

must_haves:
  truths:
    - "tools/lib/heading-scan.mjs exports a fence-aware collectH1Lines(text); check-gof, check-kerievsky, check-extra-patterns, and check-functional each import it and call it in place of the former fence-blind H1 filter"
    - "The IN-02 refactor is behavior-preserving: every pre-existing checker (check-catalog, check-kerievsky, check-gof, check-extra-patterns, check-smells, check-crossrefs, check-principles, check-hygiene, check-functional) still exits 0 individually -- no leaf carries a fenced column-0 hash so the counts are identical"
    - "node tools/check-backing.mjs exits 1 with a RED summary against the phase-open tree: beck-tdd-by-example.md and beck-tidy-first.md are absent, and refactoring-without-tests.md trips the SCAFFOLD guard on its Sources (placeholder) heading"
    - "check-crossrefs.mjs sourceFiles now also includes SKILL.md, principles.md, beck-tdd-by-example.md, beck-tidy-first.md, and refactoring-without-tests.md (each pushed under an existsSync guard); check-crossrefs still exits 0 (the two absent Beck files are skipped; every current outbound link resolves)"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/lib/heading-scan.mjs"
      provides: "Shared fence-aware level-1 heading collector for all four catalog checkers (IN-02 / D-10)"
      contains: "collectH1Lines"
    - path: ".claude/skills/lz-refactor-workspace/tools/check-backing.mjs"
      provides: "PRIN-01/02/03 structural gate; RED baseline by design; line-oriented topic tokens + no-oracle tag + SCAFFOLD guard + Fowler-link presence"
      contains: "no-oracle"
      min_lines: 70
    - path: ".claude/skills/lz-refactor-workspace/package.json"
      provides: "check battery chain extended with check-backing.mjs"
      contains: "check-backing.mjs"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/tools/check-gof.mjs"
      to: ".claude/skills/lz-refactor-workspace/tools/lib/heading-scan.mjs"
      via: "import { collectH1Lines }"
      pattern: "heading-scan"
    - from: ".claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      via: "sourceFiles push under existsSync (path.join(REFERENCES, '..', 'SKILL.md'))"
      pattern: "SKILL.md"
  decisions: |
    - **D-09:** a structural checker gate is added following the instrument-first / Nyquist pattern (RED baseline before content, GREEN when authored); it is a NEW sibling check-backing.mjs (not an overload of the FWL-03-specific check-principles.mjs) asserting the 3 principle refs exist, each carries its required core-topic tokens (line-oriented), each carries the no-oracle tag, and beck-tidy-first.md carries at least one fowler-catalog link; it is wired into the package.json npm run check battery; coach + Beck link resolution is delegated to the extended check-crossrefs
    - **D-10:** the carried-in Phase 08.2 tech debt IN-02 is folded in ONCE as a shared tools/lib/heading-scan.mjs helper imported by check-gof / check-kerievsky / check-extra-patterns / check-functional alongside githubSlug; a per-checker patch is forbidden (it re-introduces the divergence the 08.2 fixer declined); the swap is behavior-preserving and the still-GREEN battery is the proof
    - **D-12:** the HOW micro-decisions land as recommended: filenames beck-tdd-by-example.md / beck-tidy-first.md; a NEW sibling check-backing.mjs rather than extending check-principles.mjs
---

<objective>
Instrument the phase FIRST (instrument-first / Nyquist scaffold), before any coach or
principle content is authored. Two harness moves land in Wave 1:

1. Retire the carried-in IN-02 tech debt (D-10): one shared fence-aware collectH1Lines
   helper, imported by all four catalog checkers in place of their identical fence-blind H1
   scan. This is a behavior-preserving refactor -- the still-GREEN battery is the proof.
2. Stand up the PRIN-01/02/03 structural gate (D-09): a NEW check-backing.mjs driven to a
   RED baseline, wired into the npm run check battery, plus the check-crossrefs source-set
   extension so the coming coach cross-links and Beck->Fowler links get resolution-checked.

Purpose: give the Wave-2 authoring plans a machine-checkable target. There is NO owned book
oracle for Beck/Feathers (D-07); check-backing + skill-reviewer + DST-04 hygiene are the
correctness anchor. PRIN-01/02/03 close only when Wave 2 turns this gate GREEN.

Output: tools/lib/heading-scan.mjs, four rewired catalog checkers, tools/check-backing.mjs
(RED), the extended check-crossrefs.mjs source set, the package.json chain entry, and an
explicitly asserted RED baseline with the rest of the battery GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-coach-behavior-principle-backing/09-CONTEXT.md
@.planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md
@.planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md

Carried-in tech debt IN-02 is described at .planning/ROADMAP.md:174 (the Phase 9
"Carried-in tech debt" note). The exact collectH1Lines implementation is specified in
09-RESEARCH.md:319-365; the check-backing topic-token map is at 09-RESEARCH.md:466-479.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared fence-aware heading helper + rewire the four catalog checkers (IN-02 / D-10)</name>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/lib/github-slug.mjs (the ONLY existing lib file; copy its shape: single named export const, pure function, node-builtin-free, a doc comment stating the single-source-of-truth rationale)
    - .claude/skills/lz-refactor-workspace/tools/check-gof.mjs (imports node builtins only at :27-29; the fence-blind H1 scan is line :160)
    - .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs (imports node builtins only at :20-22; the fence-blind H1 scan is line :113)
    - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs (already imports githubSlug from ./lib/ at :36; the fence-blind H1 scan is line :199)
    - .claude/skills/lz-refactor-workspace/tools/check-functional.mjs (already imports githubSlug from ./lib/ at :44; the fence-blind H1 scan is line :198, preceded by the stale ponytail note at :192-197 that names this exact upgrade)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (Code Examples: the exact collectH1Lines fence-tracking implementation, :319-365)
    - .planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md (the heading-scan.mjs and four-catalog-checker-edits sections: exact per-file import wiring)
  </read_first>
  <action>
    Create .claude/skills/lz-refactor-workspace/tools/lib/heading-scan.mjs exporting a single pure
    function collectH1Lines(text) that returns the level-1 ATX heading lines that sit OUTSIDE fenced
    code blocks. Copy the fence-tracking implementation from 09-RESEARCH.md Code Examples (CommonMark-ish:
    a fence opens on a line starting with 3-or-more backticks or 3-or-more tildes and closes on a later
    line with the same char, a run at least the opening length, and an empty info string). Node builtins
    only; ASCII-only; mirror the github-slug.mjs doc-comment style stating why this is one shared helper
    (single source of truth, zero divergence across the four checkers).

    Rewire the four catalog checkers so each replaces its single fence-blind H1-scan line
    (the lines.filter that tests /^#\s+\S/) with a call to collectH1Lines(text) -- the local text
    variable already exists at each call site (check-gof :160, check-kerievsky :199, check-extra-patterns :113,
    check-functional :198). Import wiring differs per file: check-kerievsky and check-functional already
    import from ./lib/ (githubSlug) -- ADD a second import line for collectH1Lines from ./lib/heading-scan.mjs.
    check-gof and check-extra-patterns import only node builtins -- ADD the same collectH1Lines import beside
    their node-builtin imports. In check-functional.mjs, also replace/trim the now-stale ponytail note at
    :192-197 (it documented this gap as carried-in Phase 9 debt) with a one-line note that the fence-aware
    scan now routes through the shared helper. Do NOT touch check-crossrefs.mjs slugsFor/linkRe (out of D-10
    scope -- kept dormant by D-11 authoring discipline). This swap is behavior-preserving: no leaf currently
    carries a fenced column-0 hash, so every checker H1 count is identical and the battery stays GREEN.
  </action>
  <acceptance_criteria>
    - .claude/skills/lz-refactor-workspace/tools/lib/heading-scan.mjs exists and exports collectH1Lines
    - All four catalog checkers import collectH1Lines from ./lib/heading-scan.mjs and call collectH1Lines(text) at their former H1-scan line; none still contains a lines.filter for /^#\s+\S/ as its leaf-H1 scan
    - check-functional.mjs no longer carries the stale :192-197 "carried-in Phase 9 debt" ponytail note
    - Each of the 9 checkers (check-catalog, check-kerievsky, check-gof, check-extra-patterns, check-smells, check-crossrefs, check-principles, check-hygiene, check-functional) exits 0 individually (behavior-preserving; counts unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-gof.mjs && node tools/check-kerievsky.mjs && node tools/check-extra-patterns.mjs && node tools/check-functional.mjs && node tools/check-catalog.mjs && node tools/check-smells.mjs && node tools/check-crossrefs.mjs && node tools/check-principles.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>heading-scan.mjs exists; all four catalog checkers route their leaf-H1 scan through collectH1Lines; the whole pre-existing battery still exits 0 (behavior-preserving IN-02 retirement).</done>
</task>

<task type="auto">
  <name>Task 2: check-backing.mjs RED gate + check-crossrefs source extension + package.json wiring (D-09)</name>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-principles.mjs (FULL file -- the template to copy: shebang + doc header, repoRoot resolution path.resolve(here, "..","..","..",".."), the TOPICS array, the report() closure, the lines.some((line) => re.test(line)) presence loop, and the SUMMARY GREEN/RED + process.exit(0|1) tail)
    - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs (dir consts at :27-34; the sourceFiles construction + SMELLS_INDEX existsSync push at :107-114; principles.md is already special-cased as a file-level hub TARGET at :165-173)
    - .claude/skills/lz-refactor-workspace/package.json (the scripts.check &&-chain at :8)
    - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md (the stub -- its "## Sources (placeholder)" heading at :29 is what the SCAFFOLD guard trips on to keep Feathers RED until Wave 2 populates it)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (Validation Architecture: the check-backing topic-token map :466-479, the SCAFFOLD guard set, Pitfall 3 + Pitfall 4)
    - .planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md (the check-backing.mjs and check-crossrefs source-set-extension sections)
  </read_first>
  <action>
    Create .claude/skills/lz-refactor-workspace/tools/check-backing.mjs as a faithful sibling of
    check-principles.mjs. Copy its import block, repoRoot resolution, report() closure, line-oriented
    lines.some() presence loop, and SUMMARY GREEN/RED + process.exit(0|1) tail. Define the three target
    file paths under references/: beck-tdd-by-example.md, beck-tidy-first.md, refactoring-without-tests.md.
    Assert, per file (topic tokens verbatim from 09-RESEARCH.md :466-479, case-insensitive, line-oriented):
    beck-tdd-by-example.md -> red-green-refactor, two rules, fake it, triangulate, obvious implementation, lz-tpp;
    beck-tidy-first.md -> structural, behavioral, coupling, cohesion, options, PLUS a Fowler-link presence
    check matching a link URL of shape ../fowler-catalog/<slug>.md (regex the ]( ... fowler-catalog/<slug>.md form);
    refactoring-without-tests.md -> seam(s), characterization test, change algorithm (or the step tokens), sprout,
    wrap, subclass and override, extract interface. For ALL THREE additionally assert: the no-oracle tag is
    present (match /no-oracle/i) AND the SCAFFOLD guard does NOT match (/\bTODO\b/, /\bTBD\b/, /\bplaceholder\b/i,
    /once it exists/i, /to be authored/i). A missing file is a FAIL (RED), mirroring check-principles handling of
    an absent file. Do NOT resolve links inside check-backing (no second resolver) -- assert PRESENCE/format only;
    resolution is check-crossrefs's job. End with a SUMMARY: ... GREEN/RED line and process.exit(0|1).

    Extend check-crossrefs.mjs sourceFiles: define path consts beside the existing dir consts (:27-34) for
    SKILL.md as path.join(REFERENCES, "..", "SKILL.md"), and principles.md / beck-tdd-by-example.md /
    beck-tidy-first.md / refactoring-without-tests.md as path.join(REFERENCES, "<name>.md"). Push each onto
    sourceFiles under an existsSync guard, mirroring the existing SMELLS_INDEX push at :112-114. Do NOT alter the
    inverse-of mutuality guard (it stays Fowler-catalog-scoped) and do NOT touch the principles.md file-level-hub
    TARGET special-case at :165-173 (adding principles.md as a SOURCE is orthogonal and safe -- its outbound links
    get checked; nothing resolves anchors INTO it).

    Wire the gate: append " && node tools/check-backing.mjs" to the end of the package.json scripts.check chain
    (after check-functional.mjs). Leave scripts.typecheck unchanged. ASCII-only throughout; node builtins only.
  </action>
  <acceptance_criteria>
    - node tools/check-backing.mjs exits 1 and prints a SUMMARY: ... RED line (baseline: 2 Beck files absent + Feathers stub trips the placeholder SCAFFOLD guard)
    - check-backing.mjs contains the no-oracle presence check, the SCAFFOLD guard set, and the fowler-catalog link presence check for beck-tidy-first.md; it defines no second link resolver
    - check-crossrefs.mjs sourceFiles includes SKILL.md, principles.md, beck-tdd-by-example.md, beck-tidy-first.md, refactoring-without-tests.md (each existsSync-guarded); node tools/check-crossrefs.mjs still exits 0
    - package.json scripts.check ends with "&& node tools/check-backing.mjs"; scripts.typecheck is unchanged
    - The RED baseline is documented in the SUMMARY as the EXPECTED instrument-first Wave-1 state (check-backing is the sole RED; every other checker is GREEN)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-crossrefs.mjs && (node tools/check-backing.mjs; test $? -eq 1) && node -e "process.exit(require('fs').readFileSync('package.json','utf8').includes('check-backing.mjs')?0:1)"</automated>
  </verify>
  <done>check-backing.mjs exits 1 (RED baseline) with the 3-file topic map + no-oracle + SCAFFOLD guard; check-crossrefs sources are extended and still GREEN; package.json chain carries check-backing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only the NON-shipped checker harness (node-builtin `.mjs` scripts + package.json).
There is no runtime, network, session, crypto, or user-input surface. The only boundary that matters
is harness integrity: a checker that silently mis-counts would let a defect ship GREEN.

| Boundary | Description |
|----------|-------------|
| checker harness -> phase GREEN/RED verdict | a false-PASS here would let unbacked/verbatim content pass the gate |
| repo -> published git history | committed `.mjs` + package.json must stay ASCII-only and work-email-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-FP | Tampering | IN-02 collectH1Lines swap in the 4 catalog checkers | mitigate | The refactor is behavior-preserving: Task 1 asserts all 9 checkers still exit 0 individually (identical counts). A regression that mis-counts headings fails safe (false-FAIL/RED, never a silent false-PASS) because no leaf carries a fenced column-0 hash. |
| T-09-RED | Tampering | check-backing.mjs baseline | mitigate | The RED baseline is asserted explicitly (exit 1) so the gate is proven to work BEFORE Wave-2 content turns it GREEN -- a checker that passed vacuously against absent content would be a false-PASS. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages added this plan; the only devDependency (typescript@6.0.3) is unchanged and was audited in Phase 7. No install task exists, so no [ASSUMED]/[SUS] legitimacy checkpoint is required. |
| T-09-EM | Information Disclosure | committed harness files | accept | check-hygiene guards ASCII-only + work-email absence over references/ recursively; the harness `.mjs` are plain ASCII with no email tokens. |
</threat_model>

<verification>
- Task 1: all 9 pre-existing checkers exit 0 individually (behavior-preserving IN-02 retirement).
- Task 2: node tools/check-backing.mjs exits 1 (RED baseline); node tools/check-crossrefs.mjs exits 0; package.json check chain carries check-backing.mjs.
- The phase-open baseline is unambiguously: check-backing RED, everything else GREEN.
</verification>

<success_criteria>
The shared fence-aware helper serves all four catalog checkers with the battery still GREEN (IN-02
retired, D-10); check-backing.mjs exists as a RED gate wired into npm run check (D-09); check-crossrefs
now sources SKILL.md + principles.md + the 3 backing files; the RED baseline is asserted and recorded.
</success_criteria>

<output>
Create `.planning/phases/09-coach-behavior-principle-backing/09-01-SUMMARY.md` when done.
</output>
