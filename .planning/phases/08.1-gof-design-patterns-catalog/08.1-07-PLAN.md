---
phase: 08.1-gof-design-patterns-catalog
plan: 07
type: execute
wave: 3
depends_on: [08.1-02, 08.1-03, 08.1-04, 08.1-05, 08.1-06]
branching_strategy: none
tags: [gof-catalog, extra-patterns-catalog, kerievsky-linkify, catalog-index, skill-wiring, validation, phase-gate]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/limit-instantiation-with-singleton.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-composite-with-builder.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-adapter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/unify-interfaces-with-adapter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-composite.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-tree-with-composite.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-one-many-distinctions-with-composite.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-polymorphic-creation-with-factory-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-embellishment-to-decorator.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-dispatcher-with-command.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-logic-with-strategy.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-hard-coded-notifications-with-observer.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-state-altering-conditionals-with-state.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/compose-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-creation-knowledge-to-factory.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-collecting-parameter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-constructors-with-creation-methods.md
autonomous: true
requirements: [GOF-01, GOF-02, GOF-03, GOF-04, XTR-01]
must_haves:
  truths:
    - "D-08: every Kerievsky GoF pattern: token that names a real pattern is retrofitted into a resolvable link -- 17 GoF-23 tokens -> [Name](../gof-catalog/<slug>.md#<slug>), 6 extra tokens -> [Name](../extra-patterns-catalog/<slug>.md#<slug>) (incl. Factory -> extra-patterns-catalog/factory.md, never factory-method); the 4 no-real-pattern tokens (chain-constructors, unify-interfaces, extract-parameter = n/a -- utility; replace-type-code-with-class = Class / type-safe value) stay free text with NO gof/extra link"
    - "D-05: gof-catalog/README.md is finalized as a THIN index grouped by the 3 families (Creational / Structural / Behavioral), each row = a bare [Name](slug.md) link (no #anchor) + the leaf's ## Applicability FIRST LINE mirrored verbatim; extra-patterns-catalog/README.md is finalized flat (5 rows, same mirror); NO per-leaf Category label"
    - "D-09: the 3 required Away links resolve -- Singleton -> Inline Singleton, Composite -> Encapsulate Composite with Builder, Iterator -> Move Accumulation to Visitor (authored into the gof leaves' ## Related patterns in Wave 2; check-gof REQUIRED_AWAY presence + check-crossrefs resolution both GREEN here)"
    - "D-14: SKILL.md gains ONE lean pointer section immediately AFTER the Kerievsky pointer, pointing to both new catalog READMEs (references/gof-catalog/README.md for the GoF-23 target patterns; references/extra-patterns-catalog/README.md for the non-GoF target patterns); the Phase-9 coach placeholder is NOT touched"
    - "D-15: this is the single Wave-3 gate plan -- after all 5 Wave-2 authoring plans land, it applies the linkify + finalizes both READMEs + wires SKILL.md, then turns the full battery GREEN (npm run check across all 8 checkers incl. check-gof 23/23 + check-extra-patterns 5/5 + check-kerievsky linkify 27/27 + check-crossrefs) + npm run typecheck (all fences tsc --strict) + claude plugin validate ."
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md"
      provides: "finalized thin GoF index: 3 family groups, 23 rows with mirrored Applicability first line + resolving bare-slug links"
      contains: "Creational"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md"
      provides: "finalized thin extra-patterns index: 5 rows with mirrored Applicability first line"
      contains: "Factory"
    - path: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      provides: "one lean pointer section to both new catalog READMEs (D-14), after the Kerievsky pointer"
      contains: "gof-catalog/README.md"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md"
      via: "linkified GoF pattern: token (D-08)"
      pattern: "gof-catalog/singleton\\.md#singleton"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md"
      via: "linkified GoF pattern: Factory token -> extra catalog (NOT factory-method), D-08"
      pattern: "extra-patterns-catalog/factory\\.md#factory"
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md"
      via: "D-14 pointer section"
      pattern: "gof-catalog/README\\.md"
---

<objective>
Close the GoF layer: apply the D-08 linkify to the 27 Kerievsky GoF pattern: tokens (17 GoF-23 + 6
extra links; 4 free-text sentinels untouched), finalize both catalog READMEs (family grouping + the
Applicability-first-line mirror rows + the 3 Away links resolving), wire the SKILL.md pointer section
(D-14), then run the FULL deterministic battery GREEN + npm run typecheck + claude plugin validate .
Runs LAST (Wave 3), after all 23 GoF + 5 extra leaves exist so the linkify targets, README rows, and
Away links all resolve.

Purpose: GOF-03 (the linkify + Away links Phase 9's coach navigates) plus the GOF-01/XTR-01 README
finalization, D-14 wiring, and the phase-gate battery re-verify. This is deterministic (no oracle
loop): the links are applied from the fixed census and the README selectors are mirrored from the
already-authored public leaves.

Output: the linkified Kerievsky leaves, the finalized gof-catalog + extra-patterns-catalog READMEs,
the SKILL.md pointer section, and a green full battery + plugin validate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-VALIDATION.md
@plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md

<interfaces>
This is a DETERMINISTIC finalize job -- NO oracle loop, NO content authoring. The links come from the
fixed census; the README selectors are mirrored verbatim from the already-authored public gof/extra
leaves (reading shipped references/, NEVER .oracle/).

LINKIFY CENSUS (D-08; full table in 08.1-RESEARCH.md S1b). For each Kerievsky leaf's `GoF pattern:`
line, replace the plain name with a link, preserving any trailing parenthetical qualifier as free text
after the link. 17 GoF-23 tokens -> ../gof-catalog/<slug>.md#<slug>:
  inline-singleton, limit-instantiation-with-singleton -> singleton
  encapsulate-composite-with-builder -> builder
  extract-adapter, unify-interfaces-with-adapter -> adapter
  extract-composite, replace-implicit-tree-with-composite, replace-one-many-distinctions-with-composite -> composite
  form-template-method -> template-method
  introduce-polymorphic-creation-with-factory-method -> factory-method
  move-accumulation-to-visitor -> visitor
  move-embellishment-to-decorator -> decorator
  replace-conditional-dispatcher-with-command -> command
  replace-conditional-logic-with-strategy -> strategy
  replace-hard-coded-notifications-with-observer -> observer
  replace-implicit-language-with-interpreter -> interpreter
  replace-state-altering-conditionals-with-state -> state
6 extra tokens -> ../extra-patterns-catalog/<slug>.md#<slug>:
  compose-method -> composed-method
  encapsulate-classes-with-factory, move-creation-knowledge-to-factory -> factory
  introduce-null-object -> null-object
  move-accumulation-to-collecting-parameter -> collecting-parameter
  replace-constructors-with-creation-methods -> creation-method
4 FREE TEXT (do NOT add a link): chain-constructors, unify-interfaces, extract-parameter
(n/a -- utility); replace-type-code-with-class (Class / type-safe value).

Example transform: `GoF pattern: Singleton` -> `GoF pattern: [Singleton](../gof-catalog/singleton.md#singleton)`;
`GoF pattern: Factory` (in encapsulate-classes-with-factory) -> `GoF pattern: [Factory](../extra-patterns-catalog/factory.md#factory)`.

README ROW FORMAT (D-05, mirror the Fowler/Kerievsky index): each row = a BARE [Name](slug.md) link
(NO #anchor -- readmeRowsBySlug matches bare .md only) + the leaf's ## Applicability FIRST LINE mirrored
VERBATIM as the selector column. gof-catalog README groups the 23 rows under ## Creational / ## Structural
/ ## Behavioral; extra-patterns README is flat (5 rows). NO per-leaf Category label.

The 3 Away links (D-09) were authored into the gof leaves' ## Related patterns in Wave 2 -- this plan
only VERIFIES they resolve (check-gof REQUIRED_AWAY presence + check-crossrefs).

SKILL.md pointer (D-14): add ONE lean section IMMEDIATELY AFTER the existing "## Kerievsky pattern-directed
refactorings" section, pointing to references/gof-catalog/README.md (GoF-23 target patterns) and
references/extra-patterns-catalog/README.md (non-GoF target patterns). Do NOT touch the Phase-9 coach
placeholder.

If any Wave-2 SUMMARY recorded an oracle name/slug correction vs the locked names, the check-gof /
check-extra-patterns NAMES arrays are data -- reconcile them to the authored leaf H1 slugs on disk (the
single authorized post-Wave-1 checker data edit) so the identity gate matches; if none were corrected,
make no checker edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the D-08 linkify to the 27 Kerievsky GoF pattern: tokens</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/limit-instantiation-with-singleton.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-composite-with-builder.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-adapter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/unify-interfaces-with-adapter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-composite.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-tree-with-composite.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-one-many-distinctions-with-composite.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-polymorphic-creation-with-factory-method.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-embellishment-to-decorator.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-dispatcher-with-command.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-logic-with-strategy.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-hard-coded-notifications-with-observer.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-state-altering-conditionals-with-state.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/compose-method.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-creation-knowledge-to-factory.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/introduce-null-object.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-collecting-parameter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-constructors-with-creation-methods.md</files>
  <read_first>
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md (S1b linkify branch + the full 27-row token census table -- the source of truth for each leaf's target)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (check-kerievsky MODIFY section: the exact GoF pattern: line transform + the 4 free-text exceptions)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-08 the three linkify outcomes; the H1-anchor ../<catalog>/<slug>.md#<slug> convention)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md (a representative leaf -- confirm the current GoF pattern: line shape before editing)
    - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md (confirm the 23 gof leaf slugs authored in Wave 2 so link targets round-trip)
  </read_first>
  <action>
    For each of the 23 linkify-target Kerievsky leaves, edit ONLY its `GoF pattern:` line, replacing the
    plain pattern name with the census link and preserving any trailing parenthetical qualifier as free
    text after the link: 17 GoF-23 tokens become [Name](../gof-catalog/<slug>.md#<slug>) and 6 extra
    tokens become [Name](../extra-patterns-catalog/<slug>.md#<slug>), per the census in the interfaces
    block. Do NOT touch the 4 free-text leaves (chain-constructors, unify-interfaces, extract-parameter,
    replace-type-code-with-class) -- they must carry NO gof/extra link. Do NOT edit any other line of any
    leaf (Direction, Use when:, Motivation, Mechanics, Example, Composed Fowler primitive(s), Related --
    all unchanged). Critical: Factory (in encapsulate-classes-with-factory + move-creation-knowledge-to-factory)
    links to extra-patterns-catalog/factory.md, NEVER gof-catalog/factory-method.md. ASCII only. Then run
    check-kerievsky + check-crossrefs to confirm the linkify branch is GREEN (27/27 on the GoF-pattern
    axis: 23 correct links + 4 free text) and all links resolve.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` exits 0 (27/27 incl. the linkify branch: 23 correct-catalog+slug links, 4 free-text sentinels).
    - `node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` exits 0 (every linkified ../gof-catalog and ../extra-patterns-catalog target + H1 anchor resolves).
    - The Factory tokens resolve to the extra catalog, not GoF-23: `rg -q "extra-patterns-catalog/factory.md#factory" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md && ! rg -q "gof-catalog/factory-method" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md`.
    - The 4 free-text tokens carry no gof/extra link: `! rg -q "gof-catalog|extra-patterns-catalog" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/chain-constructors.md plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-type-code-with-class.md`.
    - `rg -n "[^\x00-\x7F]"` over the 23 edited leaves returns no hits (ASCII-only).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && rg -q "extra-patterns-catalog/factory.md#factory" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/encapsulate-classes-with-factory.md && ! rg -q "gof-catalog|extra-patterns-catalog" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/chain-constructors.md</automated>
  </verify>
  <done>All 23 real-pattern GoF pattern: tokens are linkified to the correct catalog + slug (17 GoF-23, 6 extra; Factory -> extra, not factory-method); the 4 no-real-pattern tokens stay free text; check-kerievsky 27/27 (linkify branch GREEN) + check-crossrefs all resolve; only the GoF pattern: line of each target leaf changed; ASCII-only.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize both catalog READMEs (family grouping + Applicability-first-line rows) + wire the SKILL.md pointer</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md, plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md, plugins/lz-tdd/skills/lz-refactor/SKILL.md</files>
  <read_first>
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-05 README family grouping + Applicability-first-line mirror; D-14 SKILL.md pointer; D-09 the 3 Away links)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (README row shape: bare [Name](slug.md) link + mirrored selector; readmeRowsBySlug matches bare .md only; SKILL.md append-1-section spec, lines 60-64 mirror)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (the finalized Fowler index shape to mirror)
    - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/*.md (read each authored leaf's ## Applicability FIRST LINE to mirror verbatim into its README row + confirm the 3 Away links present)
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/*.md (same, for the 5 extra rows)
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (the existing Kerievsky pointer section, lines 60-64, to append after)
  </read_first>
  <action>
    Finalize references/gof-catalog/README.md as a THIN family-grouped index: keep the Wave-1 intro +
    the 3 family headings ## Creational / ## Structural / ## Behavioral; under each heading add one
    Markdown-table row per leaf in that family = a BARE [Name](slug.md) link (NO #anchor) + the leaf's
    ## Applicability FIRST LINE mirrored VERBATIM as the selector column. Finalize
    references/extra-patterns-catalog/README.md flat (5 rows, same bare-link + mirrored-selector shape,
    no family split). NO per-leaf Category label; no leaf content inlined. Then add ONE lean pointer
    section to SKILL.md IMMEDIATELY AFTER the "## Kerievsky pattern-directed refactorings" section
    (D-14), pointing to references/gof-catalog/README.md (for the GoF-23 target patterns) and
    references/extra-patterns-catalog/README.md (for the non-GoF target patterns); mirror the lean shape
    of the existing catalog pointer sections. Do NOT touch the Phase-9 coach placeholder. If any Wave-2
    SUMMARY recorded an oracle name/slug correction, reconcile the check-gof / check-extra-patterns NAMES
    arrays (data only) to the authored leaf H1 slugs; else make no checker edit. Run check-gof +
    check-extra-patterns + check-crossrefs to confirm the row mirror + family grouping + Away-link
    resolution are all GREEN. ASCII only.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-gof.mjs` exits 0 (23/23: identity + 5-section contract + Applicability-first-line mirror + 3 family headings + REQUIRED_AWAY 3 present + Singleton-DI).
    - `node .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs` exits 0 (5/5: identity + contract + row mirror).
    - `node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` exits 0 (all README rows + the 3 Away links + gof<->gof Related links resolve).
    - SKILL.md carries the D-14 pointer after the Kerievsky section and does not alter the coach placeholder: `rg -q "gof-catalog/README.md" plugins/lz-tdd/skills/lz-refactor/SKILL.md && rg -q "extra-patterns-catalog/README.md" plugins/lz-tdd/skills/lz-refactor/SKILL.md && rg -q "Coach decision procedure \(deferred to Phase 9\)" plugins/lz-tdd/skills/lz-refactor/SKILL.md`.
    - `rg -n "[^\x00-\x7F]"` over the two READMEs + SKILL.md returns no hits (ASCII-only).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-gof.mjs && node .claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && rg -q "gof-catalog/README.md" plugins/lz-tdd/skills/lz-refactor/SKILL.md && rg -q "extra-patterns-catalog/README.md" plugins/lz-tdd/skills/lz-refactor/SKILL.md && ! rg -n "[^\x00-\x7F]" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/README.md plugins/lz-tdd/skills/lz-refactor/SKILL.md</automated>
  </verify>
  <done>gof-catalog/README.md is the finalized thin 23-row family-grouped index (bare-slug links + Applicability-first-line mirror); extra-patterns-catalog/README.md is the finalized flat 5-row index; SKILL.md has the D-14 pointer to both new READMEs after the Kerievsky section (coach placeholder untouched); check-gof 23/23 + check-extra-patterns 5/5 + check-crossrefs all GREEN; ASCII-only.</done>
</task>

<task type="auto">
  <name>Task 3: Full-battery phase gate -- npm run check + npm run typecheck + claude plugin validate</name>
  <files>.claude/skills/lz-refactor-workspace/package.json</files>
  <read_first>
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-VALIDATION.md (the GREEN gate + per-requirement signal map + full-suite command)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md (Validation Architecture: the GREEN gate after Wave 3)
    - .claude/skills/lz-refactor-workspace/package.json (the check + typecheck scripts)
  </read_first>
  <action>
    Run the full deterministic battery and make it GREEN (no logic edits here -- only run + confirm):
    `cd .claude/skills/lz-refactor-workspace && npm run check` must exit 0 across all 8 checkers
    (check-catalog 62/62 Fowler regression, check-kerievsky 27/27 incl. linkify, check-gof 23/23,
    check-extra-patterns 5/5, check-smells, check-crossrefs all resolve, check-principles 8/8,
    check-hygiene). Then `npm run typecheck` must exit 0 (every Fowler + Kerievsky + gof + extra ## Example
    fence compiles tsc --strict via extract-samples). Then run `claude plugin validate .` at the repo
    root -- it must pass (the harness stays non-shipped; only pure Markdown ships). If any checker is RED,
    fix the underlying content/link/README defect in the owning file (NOT by weakening a checker); the
    only permitted checker edit remains a NAMES-array data reconciliation to the authored leaf H1 slugs.
    Confirm `git status` shows no new/modified files outside references/ and the non-shipped workspace.
    ASCII-only across all touched artifacts.
  </action>
  <acceptance_criteria>
    - `cd .claude/skills/lz-refactor-workspace && npm run check` exits 0 (all 8 checkers GREEN: check-catalog 62/62, check-kerievsky 27/27 linkify, check-gof 23/23, check-extra-patterns 5/5, check-smells, check-crossrefs, check-principles, check-hygiene).
    - `cd .claude/skills/lz-refactor-workspace && npm run typecheck` exits 0 (all gof + extra + fowler + kerievsky fences tsc --strict clean).
    - `claude plugin validate .` passes at the repo root.
    - `git status` shows changes only under plugins/lz-tdd/skills/lz-refactor/references/, plugins/lz-tdd/skills/lz-refactor/SKILL.md, and .claude/skills/lz-refactor-workspace/ (no stray files); no non-ASCII in any touched artifact.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && npm run check && npm run typecheck && cd - >/dev/null && claude plugin validate .</automated>
  </verify>
  <done>The full battery is GREEN (check-catalog 62/62, check-kerievsky 27/27 linkify, check-gof 23/23, check-extra-patterns 5/5, check-smells, check-crossrefs, check-principles 8/8, check-hygiene), npm run typecheck is clean (all fences tsc --strict), and claude plugin validate . passes; changes are confined to references/, SKILL.md, and the non-shipped workspace; ASCII-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored / linkified files -> public GitHub repo | world-readable; the linkify + README finalize touch only shipped Markdown (original prose, public pattern names, intra-repo links) |
| non-shipped harness -> claude plugin validate | the harness stays under .claude/skills/lz-refactor-workspace/; validate sees only pure Markdown under plugins/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Information Disclosure / legal (DST-04) | the finalized README rows mirror the leaves' Applicability first lines | mitigate | selectors are mirrored from the already-oracle-gated public leaves (no new source contact); check-hygiene no-verbatim WARN over references/**; the main context reads shipped references/, NEVER .oracle/ |
| T-08.1-06 | Tampering | a wrong linkify target (Factory -> factory-method), a dangling README row, or an unresolved Away link | mitigate | check-kerievsky linkify branch (exact-name set membership) + check-crossrefs (all links + anchors resolve) + check-gof REQUIRED_AWAY presence, all GREEN at this gate |
| T-08.1-02 | Information Disclosure | work email in a README / SKILL.md / linkified leaf | mitigate | check-hygiene work-email allowlist hard-fail over references/**; public contact larsbrinknielsen@gmail.com only |
| T-08.1-03 | Tampering (integrity) | non-ASCII / mojibake in a finalized README / SKILL.md / leaf | mitigate | check-hygiene ASCII hard-fail; ASCII-only acceptance checks on the touched files |
| T-08.1-07 | Tampering (unsafe canonical idiom) | an incorrect / non-compiling TS ## Example shipped as canonical | mitigate | npm run typecheck (extract-samples tsc --strict) over every fence at the gate; oracle-reviewer behavior-preservation gated the code in Wave 2 |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | NO new package installs (RESEARCH Package Legitimacy Audit: N/A); sole dep typescript@6.0.3 pre-existing + verified |

No UI, no network, no secrets, no new packages. Nothing rises to the block_on: high bar.
</threat_model>

<verification>
- All 23 real-pattern Kerievsky GoF pattern: tokens are linkified (17 gof + 6 extra; Factory -> extra, not factory-method); the 4 free-text tokens carry no link; check-kerievsky 27/27 + check-crossrefs GREEN.
- gof-catalog/README.md is the finalized thin family-grouped 23-row index and extra-patterns-catalog/README.md the flat 5-row index (bare-slug links + Applicability-first-line mirror); check-gof 23/23 + check-extra-patterns 5/5 GREEN; the 3 Away links resolve.
- SKILL.md has the D-14 pointer to both new READMEs after the Kerievsky section; the coach placeholder is untouched.
- npm run check (all 8 checkers) + npm run typecheck GREEN; claude plugin validate . passes; changes confined to references/, SKILL.md, and the non-shipped workspace; ASCII-only.
</verification>

<success_criteria>
- Every Kerievsky GoF pattern: token that names a real pattern resolves to a link (GoF-23 -> gof-catalog, extra -> extra-patterns-catalog); non-pattern tokens stay free text; the 3 Direction:Away links resolve (GOF-03).
- Both catalog READMEs are finalized thin indexes (family-grouped GoF + flat extra, Applicability-first-line selector mirror); SKILL.md points to both (GOF-01 / XTR-01 / D-14).
- The full deterministic battery + npm run typecheck + claude plugin validate . are GREEN across all of GOF-01..04 + XTR-01 (GOF-04 hygiene + tsc); the phase is gate-verified.
</success_criteria>

<output>
Create `.planning/phases/08.1-gof-design-patterns-catalog/08.1-07-SUMMARY.md` when done, recording the applied linkify counts (17 gof + 6 extra + 4 free text), the finalized README row counts, the SKILL.md pointer diff, any check-gof/check-extra NAMES reconciliation, and the final full-battery + npm run typecheck + claude plugin validate . results.
</output>
