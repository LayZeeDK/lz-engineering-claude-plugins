---
context: research
phase: lz-refactor-skill-improvement (post Phase-12 e2e)
task: 1
total_tasks: 4
status: paused
last_updated: 2026-07-13T22:46:15.507Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Each was discovered through failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **`--force` on improved `with_skill` re-runs** -- idempotent resume skips exit-0 metas and would hand back the OLD-skill with_skill results, silently measuring the wrong skill. The re-measure MUST pass `--force`.
- [ ] CONSTRAINT: **Do NOT re-run `no_skill`** -- prompts are unchanged and `no_skill` loads no plugin, so base is identical. Reuse the existing `no_skill` baselines. (User stated this explicitly; re-running wastes metered spend.)
- [ ] CONSTRAINT: **Same-model taste-reviewer is research-disqualified** -- if L1's residual over-engineering warrants escalation, use a DETERMINISTIC structure-count check or a CROSS-FAMILY reviewer, NEVER a same-session same-model "is this over-engineered?" reviewer (shared blind spots; see `260712-n5o-RESEARCH-review-regimes.md`).
- [ ] CONSTRAINT: **Grade over-engineering from SOURCE, not the snapshot or `used_refactor`** -- count added class/hierarchy lines in `app/gilded-rose.ts`; the kata `.snap` `+Conjured` lines are the golden master (existing behavior), NOT added behavior. `used_refactor` undercounts `invoke_skill`. Grade from `meta.json` + `diff.patch` + transcript.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Prose veto doesn't bind | Iteration-1 forced-verdict veto was rationalized past (run-1 built a 5-class hierarchy, gamed "duplication across 2 blocks"). | advisory | L1 is a COUNTABLE net-cost test, not a taste veto. Don't expect prose to hard-gate; the re-measure judges whether counting helps. |
| Confirmation-directed research | I primed a research agent toward the reviewer-agent idea; it "confirmed" it. The unbiased re-run (neutral + skeptic) reversed the conclusion. | blocking | Any mechanism/architecture research MUST include a neutral-brief and an adversarial pass (unbiased-review-beats-primed). |
| Measuring the wrong skill | Runner reads `--plugin-dir` from the repo tree on disk. Old with_skill results linger unless `--force`d. | blocking | `--force` improved with_skill; confirm the plugin path in a `--dry-run` shows the repo tree. |
| Session-limit fragility | Hit the 5-hour/spend cap ~5x; runs 429 fast (exit 1, ~99 chars). | advisory | Run in background; idempotent resume re-runs exit-1 metas on re-invoke. Live/priority arm first. |

<current_state>
The lz-refactor skill improvement is at iteration 2 (L1), COMMITTED (98cf482) and review-clean but
UN-MEASURED. L1 rewrote step-4's warrant into one countable net-cost test (count what a pattern
REMOVES: duplication sites, obscurity -- vs ADDS: classes/types/files/indirection; APPLY only if
removed > added, else DECLINE). It generalizes across all refactorings/patterns (polymorphism-over-
trivial-types falls out as an instance) and the unbiased review's branch-double-counting loophole was
closed. Nothing metered has run against L1 yet. The user paused here to hand the re-measure to a fresh
session.
</current_state>

<completed_work>
See `.planning/HANDOFF.json` completed_tasks for the full trail. In brief: Phase-12 e2e (null skill
value-delta vs base Opus; auto-trigger the only win) -> two quick tasks (i5y 4c45570, n5o b215219,
null delta generalizes) -> skill v1 (9832c74: revert 12-02 + veto + guard + sweep; re-measured, veto
failed 1/3 kata over-engineering) -> warrant/enforcement/review-regimes research (98cf482: verifiability
is the binding lever; same-model reviewer NOT sound) -> L1 countable warrant (98cf482, review-clean).
</completed_work>

<remaining_work>
1. RE-MEASURE L1 (the immediate task -- see Pre-Execution / commands below).
2. Escalate-or-accept based on the residual over-engineering rate.
3. OPTIONAL: the REFERENCE use-case eval (the untested value lever).
4. Cleanup (deferred; destructive git needs user OK).
</remaining_work>

<decisions_made>
- L1 = one countable net-cost warrant (refactoring-agnostic), because verifiability is the dominant
  binding lever (research).
- No same-model reviewer-agent / hook now; escalation (if needed) = deterministic-count or cross-family.
- Reuse no_skill baselines; only re-run improved with_skill (--force).
- Honest ceiling: base Opus 4.8@high is excellent; the skill's realizable value = auto-trigger (proven)
  + reference catalog (untested). Output-gating to beat base is likely not worth the machinery.
</decisions_made>

<blockers>
- Metered runs user-gated + session-limit-fragile (workaround: background + idempotent resume).
- /reload-plugins user-only (interactive only; runner reads L1 from disk, unaffected).
- git reset --hard on nx/kata classifier-gated (cleanup only).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` -- machine-readable state (supersedes the stale Phase-12 handoff).
2. `plugins/lz-tdd/skills/lz-refactor/SKILL.md` step 4 (the L1 countable warrant under test).
3. `.planning/quick/260712-n5o-.../260712-n5o-RESEARCH-review-regimes.md` + `-enforcement-eval.md` -- the escalation decision (deterministic-count / cross-family, NOT same-model review).
4. `.planning/quick/260712-i5y-.../260712-i5y-SUMMARY.md` -- the package eval + multi-aspect (parity + efficiency) findings.
5. `.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs` (arms/modes; --force; grade from meta/diff/transcript) + `targets.json` (gr4 = G1sweep over-engineering trap; T2/p2 validateEntry type-code is a candidate 2nd pattern-probe).

## Infrastructure State
- L1 committed: `98cf482` on branch `gsd/lz-tdd-0.0.2-lz-refactor`. Runner reads it via `--plugin-dir`.
- nx repo `D:/projects/github/nrwl/nx`: branch `lz-refactor-e2e-smoke` @ origin/23.0.x, 1 stash, dirty (apply resets per-run).
- kata `D:/projects/github/emilybache/GildedRose-Refactoring-Kata`: branch `lz-refactor-e2e-kata`, 1 stash. TS project at `.../TypeScript`.
- Worktrees (baseline evidence, deferred): `lz-plugins-baseline` + `lz-plugins-oldold` (both @ e535070).
- Deferred eval results: 16 untracked `results/` trees under `.claude/skills/lz-refactor-workspace/**` incl. `_archive_12-02/` (old with_skill p8/gr4 preserved). Transcripts gitignored under `outputs/`.
- Existing no_skill baselines to reuse: kata gr4 k=3; nx p8 k=3, p9-p12 k=2.

## Pre-Execution Critique Required
- Design under test: L1 step-4 countable net-cost warrant (SKILL.md, committed 98cf482).
- **Exact re-measure commands** (from repo root):
  ```bash
  R=".claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs"
  KS=".claude/skills/lz-refactor-workspace/e2e-gilded-rose"
  KATA="D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript"
  # (a) kata over-engineering testbed -- the primary probe:
  node $R --suite $KS --mode apply --prompt gr4 --arm with_skill --cwd "$KATA" --runs 3 --force
  ```
- **Grade (a):** for each `results/apply/with_skill/gr4/run-*/`: count added `abstract class|class ...|extends|implements` lines in the `app/gilded-rose.ts` hunk of `diff.patch` (NOT the .snap); read `answer.md` for the `APPLY:`/`DECLINE:` verdict + whether it declined polymorphism. TARGET: 0/3 hierarchies (vs iteration-1's 1/3; base no_skill is 0/3). If improved is 0/3 with the countable verdict emitted, L1 binds.
- **Second probe (generalization -- author + REVIEW before running):** build one pattern-tempting probe that is NOT polymorphism-over-type-codes (candidate: a package/prompt exercising the `validateEntry` 3-value `mode` type-code in `@nx/eslint-plugin` nx-plugin-checks, which tempts Strategy/State -- see targets.json T2; or a small State/Builder temptation). It is a NEW instrument -> must pass the unbiased prompt-review gate first. Then run improved with_skill k=3 --force and grade the same way. Purpose: confirm the countable warrant generalizes, not a re-fit of the kata.
- Gate: do NOT escalate to any mechanism (deterministic-count / cross-family) until the re-measure shows the residual over-engineering rate actually matters.

<context>
The arc: the skill adds no measurable coach-OUTPUT value over base Opus (6+ e2e scenarios); its only
proven win is auto-triggering the workflow. We are trying to make the over-engineering veto BIND so the
skill outperforms base on that one axis. Iteration 1 (per-case veto) failed 1/3. L1 (countable
net-cost) is iteration 2. The re-measure is the judge: if L1 declines the hierarchy 0/3 (and on the 2nd
probe), the countable warrant works; if not, the evidence says stop tuning output and either escalate to
a deterministic-count/cross-family gate (only if the rate matters) or accept auto-trigger + pursue the
untested REFERENCE-catalog value instead. Do not re-open the reviewer-agent-as-same-model idea (research
disqualified it).
</context>

<next_action>
Start with: read this file + HANDOFF.json + SKILL.md step 4. Confirm nx/kata are on their throwaway
branches. Then run the kata gr4 improved with_skill k=3 --force (command above) in the background; when
it lands, grade over-engineering from source diffs (target 0/3) against the existing no_skill gr4
baseline. Report, then decide the 2nd probe / escalate-or-accept with the user. Do NOT re-run no_skill.
</next_action>
