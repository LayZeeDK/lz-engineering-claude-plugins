---
phase: 12-autonomous-multi-round-refactoring-for-whole-package-sweeps
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified: []
autonomous: false
requirements: [SC1, SC2, SC4]
user_setup: []

must_haves:
  truths:
    - "**D-18:** the phase HALTS here before any metered run; /reload-plugins is a HARD GATE only the USER can clear (committed != live). Build + review + commit (Plans 01/02) are done; this task presents the exact ready-to-run before/after commands, asks the user to /reload-plugins, and WAITS. NO eval, trigger probe, or e2e run is executed in-phase. Readiness is confirmed with build-time selfchecks ONLY (check-evals.mjs, claude plugin validate .) -- none spend claude -p."
    - "**D-16:** the presented protocol is the {trigger gap, drive gap} x {nx, kata} x {before, after} matrix. Baseline is captured against the CURRENT shipped skill FIRST: because the SKILL.md edit is already committed, the operator captures the pre-edit baseline by pointing the runners at the pre-edit skill (a git worktree / checkout at the commit BEFORE the Plan-02 SKILL.md commit). INVARIANT: that pre-edit baseline tree MUST contain the committed 12-01 instruments (3 sweep positives + 3 dual-written negatives + both e2e sweep scenarios) AND the pre-edit (OLD) SKILL.md -- guaranteed because 12-01 commits its instruments in a dedicated commit (12-01 Task 3) and 12-02 commits ONLY SKILL.md, so the commit before the 12-02 SKILL.md commit is exactly {OLD SKILL.md + NEW instruments}; without the 12-01 instruments in that tree the Gap-1 sweep-trigger baseline cannot reproduce. Isolated trigger -> baseline/trigger/; sweep-trigger e2e --arm with_skill (expect ~0 activations, reproduces Gap 1); drive e2e --arm invoke_skill (force-load, isolates DRIVE from the trigger gap) + --arm no_skill (base-Opus control) in apply mode (expect stop-and-ask, reproduces Gap 2). Then /reload-plugins the edited tree -> delete stale chunks -> capture after/. A gap is CLOSED when the after-rate crosses the bar while before was at/near 0. The existing 18/18 coach-recommend triggering + the 11/11-quiet specificity set are re-run on the new surface to prove NO regression (SC1)."
    - "**D-12:** the presented trigger commands use the LOCKED run config -- canary-gated chunk runners (run-recall-chunks.mjs / run-spec-chunks.mjs), --runs-per-query 3 --num-workers 1 --model claude-opus-4-8, PONYTAIL_DEFAULT_MODE=off; and BEFORE re-running after adding queries, DELETE the stale trigger-results-d07-{recall,spec}-chunk-*.json AND evals/d07-chunks/{recall,spec}-chunk-*.json (chunk boundaries shift). The spec runner reads d07-chunks/negatives.json (the dual-write reason)."
    - "**D-14:** the presented e2e grading reads posture from meta.json + diff.patch + transcript (NOT used_refactor, which undercounts invoke_skill in apply sessions): fired, rounds_completed (>= 2), tests_green_each_round, behavior_preserved (original unmodified tests against the edited source: kata golden master; nx jest 46/46 or nx-plugin-checks 7/7), scope_contained, plus pauses / stopped_at_seeded_risk / drove_through_risk."
    - "**D-15:** the headline metric AUTONOMY-PRECISION is reported per suite + combined (fraction of runs that drive all N_safe rounds unasked, pause at most once, land ONLY on the seeded should-pause item, behavior preserved); over-pausing => posture B, driving through the seeded risk => posture C, high precision => refined Option A correct. Report Pass@k / Pass^k per suite + pooled (k = 1, 3; k = 1 acceptable for the no_skill control, tradeoff noted)."
    - "SC4: the presented protocol proves both gaps CLOSED before/after in BOTH suites -- not assertion alone; the run itself is the behavioral proof, gated on the user reload."
  artifacts: []
  key_links:
    - from: ".planning/phases/12 (present-and-halt readiness gate)"
      to: ".claude/skills/lz-refactor-workspace (trigger runners + e2e suites + baseline/ + after/)"
      via: "readiness selfchecks + the presented ready-to-run before/after commands (no execution) + the /reload-plugins ask"
      pattern: "reload-plugins"
---

<objective>
Confirm all Phase-12 instruments and the edited skill are built and READY, present the exact ready-to-run before/after eval + e2e protocol with the locked run config, ask the user to run /reload-plugins, and HALT. This is the D-18 hard-gate boundary: everything up to the metered run is done autonomously; the run itself is user-gated (metered claude -p) and the reload is a user-only action.

Purpose: The metered run spends claude -p and requires the skill to be LIVE (committed != live), which only /reload-plugins achieves. This task proves readiness without spending tokens, lays out the full before/after matrix + no-regression re-run, and stops.
Output: A presented command block (baseline-first-then-after, both suites, with the no-regression re-run) + a blocking checkpoint. No eval is executed; no file under plugins/ is written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-CONTEXT.md
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-RESEARCH.md

Built assets to confirm ready (from 12-01/12-02):
- .claude/skills/lz-refactor-workspace/evals/trigger-eval.json (+3 sweep positives, +3 sweep negatives) and evals/d07-chunks/negatives.json (dual-written)
- .claude/skills/lz-refactor-workspace/run-recall-chunks.mjs, run-spec-chunks.mjs, check-evals.mjs
- .claude/skills/lz-refactor-workspace/e2e-nx/ (p7cmd sweep scenario) and e2e-gilded-rose/ (gr3cmd sweep scenario), run-e2e.mjs, baseline/ + after/ archive dirs
- .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/run_eval.py (the vendored native-fixed probe)
- plugins/lz-tdd/skills/lz-refactor/SKILL.md (committed edit from 12-02; NOT yet live)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify readiness, present the before/after protocol, ask for /reload-plugins, HALT</name>
  <read_first>
    - 12-RESEARCH.md "Code Examples" (the exact native trigger-eval invocation + the e2e sweep-command run commands) and "Measurement" / Pitfalls 3/4/5 (stale-chunk deletion, used_refactor undercount, reload-plugins gate)
    - 12-CONTEXT.md D-12 (locked run config + dual-write + stale-chunk deletion), D-14 (per-run pass criteria), D-15 (autonomy-precision + Pass@k), D-16 (before/after protocol + baseline-first), D-18 (the hard gate + present-and-wait)
    - .claude/skills/lz-refactor-workspace/e2e-nx/suite.json + e2e-gilded-rose/suite.json (skillCommand, applyBase, protectedBranches, the new p7cmd/gr3cmd rows) and run-e2e.mjs (--mode / --arm / --report flags, apply-branch protection, applyBase reset)
    - .claude/skills/lz-refactor-workspace/run-recall-chunks.mjs + run-spec-chunks.mjs (canary gating, CHUNK sizes, the exact stale-cache filenames to delete)
  </read_first>
  <what-built>
    Plans 12-01 and 12-02 built everything up to the metered run: the trigger-eval set gained 3 sweep positives + 3 dual-written sweep negatives (check-evals green); both e2e suites gained one multi-round sweep-command scenario (p7cmd / gr3cmd) each seeded with N safe rounds + one should-pause trap; and the shipped SKILL.md gained the broadened sweep-trigger description + the sweep-drive loop-to-fixpoint cluster, subagent-reviewed (incl. unbiased) and committed clean. The skill is committed but NOT live.
  </what-built>
  <action>
    Confirm build-time readiness with the two free selfchecks (check-evals.mjs green; claude plugin validate . pass). Present the full baseline-first-then-after protocol shown in the how-to-verify block below with the LOCKED run config (canary-gated chunk runners; --runs-per-query 3 --num-workers 1 --model claude-opus-4-8; PONYTAIL_DEFAULT_MODE=off; stale-chunk deletion; spec source = d07-chunks/negatives.json), name the D-14 grading signals and the D-15 autonomy-precision + Pass@k headline, then ask the user to run /reload-plugins and HALT. Execute NO metered claude -p and write NO file under plugins/ (D-18).
  </action>
  <how-to-verify>
    Confirm build-time readiness (free, no claude -p): run node check-evals.mjs (from the workspace dir) -> green; claude plugin validate . (repo root) -> pass. Then present, do not execute, the before/after protocol below and WAIT.

    Present this exact protocol (baseline FIRST, then after), with the locked config, for the operator to run after reload:

    A. BASELINE (against the CURRENT shipped/pre-edit skill; expect the gaps to reproduce):
       - Because SKILL.md is already committed with the new content, capture the true baseline against the PRE-EDIT skill: create a git worktree (or checkout) at the commit BEFORE the 12-02 SKILL.md commit and point the trigger probe / e2e --plugin-dir at that pre-edit tree. INVARIANT (verify before running): that pre-edit tree must contain the OLD SKILL.md PLUS the committed 12-01 instruments (the sweep positives/negatives + both e2e sweep scenarios) -- it does, because 12-01 commits the instruments separately (12-01 Task 3) and 12-02 commits ONLY SKILL.md; if the checked-out tree lacks the 12-01 instruments you picked the wrong commit (go one commit forward to the 12-01 instrument commit, still before the SKILL.md commit).
       - Isolated trigger baseline -> baseline/trigger/ : the canary-gated chunk runners against the pre-edit skill (recall + spec), locked config below.
       - Sweep-trigger e2e baseline: run-e2e.mjs --mode apply --arm with_skill on the p7cmd (nx) + gr3cmd (kata) scenarios (expect ~0 sweep activations = Gap 1).
       - Drive baseline: run-e2e.mjs --mode apply --arm invoke_skill (force-load, isolates DRIVE from the trigger gap; expect stop-and-ask = Gap 2) + --arm no_skill (base-Opus control, k=1 acceptable).
    B. /reload-plugins  <-- USER ONLY. Ask the user to run it now; Claude cannot.
    C. AFTER (against the edited, now-live skill):
       - DELETE stale caches first: trigger-results-d07-{recall,spec}-chunk-*.json AND evals/d07-chunks/{recall,spec}-chunk-*.json (chunk boundaries shifted when queries were added, Pitfall 3).
       - Isolated trigger after -> after/trigger/ : run-recall-chunks.mjs + run-spec-chunks.mjs, locked config: --runs-per-query 3 --num-workers 1 --model claude-opus-4-8, PONYTAIL_DEFAULT_MODE=off, --strict-mcp-config, --setting-sources project.
       - Sweep e2e after: run-e2e.mjs --mode apply --arm with_skill (trigger) + --arm invoke_skill (drive) on p7cmd + gr3cmd, k=3.
       - NO-REGRESSION re-run (SC1): the existing 18/18 coach-recommend triggering (nx p1-p4 + kata gr1/gr2, --mode recommend) and the 11/11-quiet specificity set on the new description.
       - run-e2e.mjs --report for Pass@1/@3/^3 per (mode,arm,prompt) + pooled.

    Grade posture from meta.json + diff.patch + transcript, NOT used_refactor (undercounts invoke_skill in apply sessions, Pitfall 4): fired, rounds_completed (>=2), tests_green_each_round, behavior_preserved (ORIGINAL unmodified tests against the EDITED source: kata golden master; nx jest 46/46 or nx-plugin-checks 7/7), scope_contained; plus pauses / stopped_at_seeded_risk / drove_through_risk. Report AUTONOMY-PRECISION (D-15) per suite + combined and the {trigger, drive} x {nx, kata} x {before, after} matrix. A gap is CLOSED when the after-rate crosses the bar while before was at/near 0. WAIT for ALL run completions before grading (skill-creator workflow rule).
  </how-to-verify>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node check-evals.mjs</automated>
    <note>Also run claude plugin validate . from the repo root. Build-time readiness only. The behavioral before/after run is the user's to execute after /reload-plugins; it is NOT run in-phase (D-18).</note>
  </verify>
  <acceptance_criteria>
    - Build-time readiness confirmed: check-evals.mjs exits 0 and claude plugin validate . passes.
    - The full before/after protocol is presented (baseline-first via the pre-edit tree; then reload; then after + no-regression re-run) with the LOCKED run config (canary-gated chunk runners, --runs-per-query 3 --num-workers 1 --model claude-opus-4-8, PONYTAIL off, stale-chunk deletion, dual-write spec source).
    - The presentation names the D-14 grading signals (from transcript/diff/meta, not used_refactor) and the D-15 headline metric (autonomy-precision + Pass@k/Pass^k, k=1 acceptable for no_skill).
    - The /reload-plugins ask is explicit and the task HALTS -- NO metered claude -p (trigger probe, chunk runner, or e2e) is executed in-phase; no file under plugins/ is written.
  </acceptance_criteria>
  <done>Readiness selfchecks green; the before/after protocol + locked config + grading signals are presented; the /reload-plugins ask is explicit; the phase halts with zero metered spend.</done>
  <resume-signal>User runs /reload-plugins, then types "approved" (or runs the presented before/after protocol themselves and shares results). Do not proceed to any metered run until then.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed-but-not-live skill -> metered eval subprocess | The after-measurement is only valid once /reload-plugins makes the edit live; a stale-cache or un-reloaded run silently measures the OLD skill (false result, not an exploit) |
| e2e apply arm -> real target repos (nx / kata working trees) | The apply run edits real code, but on a throwaway branch reset to applyBase between k runs; apply mode refuses protected branches; changes are reversible and uncommitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Repudiation / measurement integrity | before/after run against a stale or un-reloaded skill | mitigate | The protocol mandates /reload-plugins before the after-run and deletion of stale chunk caches (D-12/D-16, Pitfall 3/5); baseline is captured against the explicit pre-edit tree so before/after is not conflated |
| T-12-05 | Tampering (real-repo edits) | e2e apply arm on nx / kata | accept | apply mode resets to applyBase between runs and refuses protected branches; edits are uncommitted and reversible; this is the intended behavioral proof, not a defect |
| T-12-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package install occurs in this phase (audit N/A); nothing to verify |
</threat_model>

<verification>
- check-evals.mjs green and claude plugin validate . passes (build-time readiness).
- The before/after protocol is presented with the locked config, baseline-first, both suites, plus the no-regression re-run; grading signals and the autonomy-precision headline are named.
- The /reload-plugins ask is explicit; the task halts; zero metered claude -p executed; no plugins/ file written.
</verification>

<success_criteria>
The phase reaches a clean, present-and-halt readiness gate: all instruments + the reviewed skill edit are built and committed, the exact before/after protocol (that will prove both gaps CLOSED in both suites, SC4, without regressing SC1) is laid out with the locked run config, and the phase waits for the user to /reload-plugins and approve the metered run.
</success_criteria>

<output>
Create `.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-03-SUMMARY.md` when done.
</output>
