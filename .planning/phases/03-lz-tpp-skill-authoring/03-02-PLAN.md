---
phase: 03-lz-tpp-skill-authoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md
autonomous: true
requirements: [TPP-06]

must_haves:
  truths:
    - "A reader sees a full Fibonacci example applied test-by-test in monotonic TPP priority order, each step tagged with the NAMED transformation and its canonical list position (TPP-06; D-06)"
    - "The walk reaches (statement -> tail-recursion) #9 (preferred over plain (statement -> recursion) #11), then unwinds to (if -> while) #10 + (variable -> assignment) #13 for stack safety because JS/TS lacks reliable TCO (D-06)"
    - "The walk demonstrates that (case) at position 14 keeps a degenerate switch solution as the last resort, and Word Wrap appears ONLY as a referenced impasse illustration, not a second full walk (D-06)"
    - "All code is TypeScript with ASCII-only arrows (->) and the file opens with a table of contents because it exceeds 100 lines (SKILL-06 progressive-disclosure rule); this file is one of the two focused reference files added under references/ ALONGSIDE the LOCKED, unmodified transformations.md (D-01)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md"
      provides: "The canonical FibTPP Fibonacci walk translated to TypeScript, applied test-by-test in monotonic priority order, each step tagged with its transformation and list position, ending with the iterative unwind for stack safety"
      contains: "## Contents"
      min_lines: 100
  key_links:
    - from: "plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md"
      to: "(self-contained)"
      via: "no markdown link to any other reference file (one-level-deep rule: reference files must not link to other reference files)"
      pattern: "^(?!.*\\]\\(.*(transformations|typescript-and-tco)\\.md\\)).*$"
---

<objective>
Author the bundled worked-example reference file: the canonical FibTPP Fibonacci walk
translated to TypeScript, applied test-by-test in monotonic TPP priority order, with each step
tagged by the named transformation and its position on the canonical 14-item list, ending with
the JS/TS unwind from tail-recursion to an iterative loop for stack safety.

Purpose: TPP-06 requires at least one full worked example applied test-by-test in monotonic
priority order. This file is the on-demand tier the SKILL.md body points at; it grounds the
coach's "name the transformation" behavior in a concrete, faithful walk and demonstrates the
recursion/iteration ordering where language-specificity bites.
Output: plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md (TOC-headed,
ASCII-only, TypeScript, each step tagged, self-contained -- no links to other reference files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md
@.planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md
@.planning/research/FEATURES.md

<interfaces>
Canonical list positions this walk must tag against (from references/transformations.md,
LOCKED -- do not modify): 1 ({} -> nil), 2 (nil -> constant), 4 (constant -> scalar),
6 (unconditional -> if), 9 (statement -> tail-recursion), 10 (if -> while),
11 (statement -> recursion), 13 (variable -> assignment), 14 (case).

Progressive-disclosure rule (RESEARCH, VERIFIED 2026-07-02): a reference file over 100 lines
opens with a table of contents; a reference file must NOT link to another reference file (this
file is reached one level deep from SKILL.md and links to no sibling reference).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the TypeScript Fibonacci worked-example file</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md</files>
  <read_first>
    - .planning/research/FEATURES.md (Part 2 "Worked Example -- Fibonacci (from FibTPP)": the 5-step Java walk to translate to TS; the test suite of(0)..of(6); the key lesson about (case) preventing the degenerate switch; and Part 2 "Worked Example -- Word Wrap impasse" for the impasse illustration)
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md ("Canonical transformation priority list": the exact names and positions 1-14 to tag each step against; the amended RGR process)
    - .planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md (D-06: the exact monotonic walk shape and that Word Wrap is a referenced illustration only, not a second full walk)
    - .planning/research/TPP-TYPESCRIPT.md (Kata 1 sum + Pattern 4 iterative form: idiomatic stack-safe TS loop shape to model the final unwind; TS typing guidance)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md (Pitfall 6 "worked example that does not follow priority order"; the TS lift-targets note for the Fibonacci file)
  </read_first>
  <action>
    Create references/fibonacci-worked-example.md. Open with a short intro naming the source
    (FibTPP post) and a "## Contents" table of contents (the file will exceed 100 lines). Present
    the Fibonacci test suite (of(0)=0, of(1)=1, of(2)=1, of(3)=2, of(4)=3, of(5)=5, of(6)=8) in
    TypeScript, then walk it test-by-test. Each step is a subsection that (a) states the newly red
    test, (b) shows the minimal TypeScript change in a fenced ts block, and (c) TAGS the applied
    transformation by name AND canonical list position.

    Follow the canonical monotonic walk: step 1 = ({} -> nil) #1 then (nil -> constant) #2 (return
    0); step 2 = (unconditional -> if) #6 refactored with (constant -> scalar) #4 for the base case
    (which coincidentally passes the next test); step 3 = note the "obvious" naive (statement ->
    recursion) #11 move and why it is dispreferred (exponential, not tail-recursive, no reliable
    TCO); step 4 = prefer (statement -> tail-recursion) #9 with an accumulator helper, then a
    structure-only refactor to drop the redundant outer if; step 5 = because JS/TS lacks reliable
    TCO, unwind the tail recursion to an iterative form via (if -> while) #10 plus a few (variable
    -> assignment) #13, giving a stack-safe loop. State in ONE sentence (self-contained, no link to
    a sibling reference file) that the "no reliable TCO in JS/TS" reason and the full stack-safe
    pattern catalog live in the TypeScript/TCO reference. Close with the FibTPP key lesson: (case)
    sits at position 14 as the last resort, which is what prevents the degenerate switch(n) solution;
    and a short paragraph referencing Word Wrap as the impasse illustration (the escape is to pose a
    simpler test / backtrack, NOT to power through) -- do NOT re-walk Word Wrap.

    Keep every arrow as ASCII "->". Every fenced code block is TypeScript (```ts). Do not add a
    markdown link to references/transformations.md or references/typescript-and-tco.md (one-level-deep
    rule).
  </action>
  <verify>
    <automated>rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md` succeeds.
    - File exceeds 100 lines (`rg -c '' <file>` > 100) AND opens with a table of contents: `rg -n '^## Contents'` matches near the top.
    - Each canonical step is tagged: `rg -F` finds every token `({} -> nil)`, `(nil -> constant)`, `(constant -> scalar)`, `(unconditional -> if)`, `(statement -> tail-recursion)`, `(if -> while)`, `(variable -> assignment)`, and `(statement -> recursion)`.
    - Monotonic-order intent is explicit: tail-recursion is presented as preferred over plain recursion, and the iterative unwind is presented as the JS/TS stack-safety step (manual fidelity confirm against FEATURES Part 2 + Pitfall 6; each step subsection names its list position).
    - `(case)` last-resort lesson present: `rg -F '(case)'` matches and `rg -in 'last (resort|option)'` matches.
    - Word Wrap referenced as impasse illustration only: `rg -in 'word wrap'` matches; the file does NOT contain a second full step-by-step walk for Word Wrap (manual confirm).
    - Test suite present in TS: `rg -F 'of(6)'` (or the chosen call form) matches inside a ```ts block; all fenced code blocks are ```ts (`rg -n '^```' <file>` shows only ts-tagged or closing fences).
    - Self-contained (one-level-deep rule): the file contains NO markdown link to another reference file -- `rg -n '\]\((references/)?(transformations|typescript-and-tco)\.md' <file>` returns nothing.
    - ASCII-only: `rg -n '[^\x00-\x7F]' <file>` returns nothing.
    - OPTIONAL confirmatory type-check (the Fibonacci TS is newly translated, not pre-verified): extract each fenced ts block to a separate temp file and run `tsc --strict --noEmit` per block (compile blocks individually to avoid cross-block redeclaration errors). Confirmatory, not a hard gate.
  </acceptance_criteria>
  <done>fibonacci-worked-example.md exists, is TOC-headed and ASCII-only, walks the canonical FibTPP Fibonacci suite test-by-test in monotonic priority order with each step tagged by named transformation and list position, ends with the iterative unwind, notes (case) as last resort and Word Wrap as impasse illustration, and links to no sibling reference file.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical verification and fidelity gate for the Fibonacci file</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md (the file just authored)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md ("Validation Architecture": the mechanical check list and the Wave 0 gaps that define these commands; Pitfall 6/8)
    - .planning/phases/02-tpp-source-distillation/02-LEARNINGS.md (mechanical-check pattern; the line-wrap brittleness note -- keep asserted tokens on one line; work-email allowlist gate that never spells the work email; verified-no-op recorded in SUMMARY)
  </read_first>
  <action>
    Run the full mechanical gate battery over fibonacci-worked-example.md: ASCII gate, TOC
    presence, transformation-token presence for every tagged step, (case)/last-resort and
    Word-Wrap-illustration checks, the no-sibling-link guard, the work-email allowlist gate, and
    the transformations.md-unchanged git guard. Then run `claude plugin validate ./plugins/lz-tdd`
    to confirm the skill tree still validates with the new reference file present. Fix any failure
    in place (formatting/wording only; keep the walk monotonic and faithful). If a token-presence
    check misses only because a line-wrap split the token, reflow it onto one line (Phase-2 lesson).
    If everything passes first time, record the passing checks in the SUMMARY (verified-no-op).
  </action>
  <verify>
    <automated>claude plugin validate ./plugins/lz-tdd</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 acceptance checks re-run green (ASCII gate, TOC, every transformation token, (case)/last-resort, Word Wrap illustration, no-sibling-link guard).
    - Work-email allowlist gate green (never spells the work email): `rg -oIN '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' <file> | rg -v '^larsbrinknielsen@gmail\.com$'` returns nothing.
    - `claude plugin validate ./plugins/lz-tdd` exits 0 with the new file present.
    - transformations.md unchanged: `git diff --exit-code -- plugins/lz-tdd/skills/lz-tpp/references/transformations.md` exits 0; transformations.md is absent from this plan's files_modified.
  </acceptance_criteria>
  <done>All mechanical, fidelity, and hygiene gates pass for fibonacci-worked-example.md; the skill tree validates; failures (if any) were fixed in place and verified-no-op recorded in SUMMARY if nothing changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored reference file -> public git repo | Committed worked example becomes a public artifact; fidelity or hygiene errors ship publicly. |
| worked-example guidance -> reader's mental model of TPP | A walk that violates priority order would teach TPP wrong and undermine the skill's core value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering (source-fidelity drift) | The Fibonacci walk order | mitigate | Translate the canonical FibTPP walk from FEATURES Part 2; tag each step with its transformation and list position; manual fidelity confirm against Pitfall 6 that each step is the highest-priority-that-passes. |
| T-03-02-02 | Information Disclosure | Committed reference content | mitigate | Work-email allowlist gate (Task 2) that filters out the public gmail; check never spells the work-email literal. |
| T-03-02-03 | Tampering (integrity/hygiene) | Non-ASCII bytes / arrow glyphs | mitigate | ASCII-only gate; arrows as `->`; keep asserted tokens on one line (Phase-2 line-wrap lesson). |
| T-03-02-04 | Tampering (locked-source integrity) | transformations.md | mitigate | Git `--exit-code` guard proves transformations.md is byte-unchanged; it is excluded from files_modified. |
| T-03-02-05 | Elevation of Privilege | Worked-example prose | accept | The file is a static walk; it recommends but issues no auto-edit/auto-run instructions. No coach-drive surface here (that guard lives in SKILL.md, plan 03-01). |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (Markdown + fenced TS only; RESEARCH Package Legitimacy Audit = not applicable). |
</threat_model>

<verification>
- File exists, exceeds 100 lines, opens with a TOC, and is ASCII-only.
- Every canonical step token present; monotonic order confirmed against FEATURES Part 2; (case) last-resort and Word Wrap illustration present.
- No link to any sibling reference file (one-level-deep rule).
- Work-email absent (allowlist gate); transformations.md unchanged; `claude plugin validate ./plugins/lz-tdd` passes.
</verification>

<success_criteria>
- ROADMAP Phase 3 SC3 (at least one full worked example applied test-by-test in monotonic priority order) is satisfied by this file.
- TPP-06 is addressed by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-lz-tpp-skill-authoring/03-02-SUMMARY.md` when done.
</output>
