---
phase: 05-skill-effectiveness-evals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-skill-effectiveness-evals/evals/trigger-eval.json
  - .planning/phases/05-skill-effectiveness-evals/evals/trigger-smoke.json
  - .planning/phases/05-skill-effectiveness-evals/evals/evals.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-0-fib-nothing-to-constant/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-1-fib-split-base-case/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-2-fib-prefer-tail-recursion/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-3-sum-deep-input-stacksafe/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-4-flatten-tree-no-tail/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-5-wordwrap-impasse-backtrack/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-6-refactor-not-transformation/eval_metadata.json
  - .planning/phases/05-skill-effectiveness-evals/grade-run.mjs
  - .gitignore
autonomous: true
requirements: [EVAL-01, EVAL-02]
user_setup: []

must_haves:
  truths:
    - "trigger-eval.json is a JSON array of >=18 {query, should_trigger} objects (8-10 should-trigger + 8-10 genuinely-tricky near-misses); trigger-smoke.json is a 2-query subset (D-02)"
    - "evals.json is a valid {skill_name, evals[]} object holding the 7 named-transformation behavior scenarios, each with a coach-dont-drive assertion (D-03)"
    - "the deterministic grader emits grading.json with exactly the fields text/passed/evidence per expectation plus summary{passed,failed,total,pass_rate}, using ground truth lifted from the locked references (D-04)"
    - "eval sets and workspace live under .planning/phases/05-skill-effectiveness-evals/ (never under plugins/), and raw run transcripts are excluded from the committed surface via .gitignore (D-08)"
  artifacts:
    - path: ".planning/phases/05-skill-effectiveness-evals/evals/trigger-eval.json"
      provides: "EVAL-01 trigger-eval set (>=18 queries), input to run_loop.py (D-02)"
      contains: "should_trigger"
    - path: ".planning/phases/05-skill-effectiveness-evals/evals/trigger-smoke.json"
      provides: "2-query WSL2 interop smoke subset (D-01, D-02)"
      contains: "should_trigger"
    - path: ".planning/phases/05-skill-effectiveness-evals/evals/evals.json"
      provides: "EVAL-02 behavior set of 7 scenarios (D-03, D-04)"
      contains: "\"skill_name\": \"lz-tpp\""
    - path: ".planning/phases/05-skill-effectiveness-evals/grade-run.mjs"
      provides: "Deterministic grader writing grading.json (text/passed/evidence) (D-04)"
      min_lines: 60
    - path: ".gitignore"
      provides: "Raw-transcript / trigger-workspace exclusion (D-08)"
      contains: "lz-tpp-workspace"
  key_links:
    - from: "evals/trigger-eval.json"
      to: "scripts.run_loop / scripts.run_eval"
      via: "array of {query, should_trigger}"
      pattern: "should_trigger"
    - from: "grade-run.mjs"
      to: "aggregate_benchmark.py"
      via: "grading.json summary.pass_rate + expectations[].text/passed"
      pattern: "pass_rate"
    - from: "lz-tpp-workspace/iteration-1/eval-<N>-<slug>/eval_metadata.json"
      to: "aggregate_benchmark.py glob(eval-*)"
      via: "eval_id matches the -<N>- dir prefix"
      pattern: "eval_id"
---

> **POST-PIVOT -- see `05-CONTEXT.md` D-09 before executing.** Two blanket corrections supersede the details below:
> 1. **Paths:** every `.planning/phases/05-skill-effectiveness-evals/...` / `PHASE_DIR` eval path (evals, lz-tpp-workspace, trigger-workspace, EVAL-RESULTS.md, grade-run.mjs) is superseded by **`.claude/skills/lz-tpp-workspace/`** (NOT `plugins/`, NOT `.planning/`). The eval sets (trigger-eval.json, trigger-smoke.json, evals.json) are ALREADY authored + committed at `.claude/skills/lz-tpp-workspace/evals/`; the eval record is tracked via generic root `.gitignore` rules, so the per-plan `.gitignore` task is DONE at root level. Remaining 05-01 work: the deterministic grader (`grade-run.mjs`) and the 7 `eval_metadata.json`, under `.claude/skills/lz-tpp-workspace/`.
> 2. **EVAL-01 runs NATIVELY** via the vendored harness at `.claude/skills/lz-tpp-workspace/tools/skill-creator-eval/` (see 05-03) -- NOT under WSL (upstream #3548/#3041 made WSL insufficient).

<objective>
Author every eval DATA artifact and the deterministic grader that Waves 2-3 consume. This is Wave 0: no eval RUNS here, only the authored inputs. The skill-creator harness (run_loop.py, run_eval.py, aggregate_benchmark.py, generate_review.py, grader.md, schemas) is fully shipped and installed; this phase authors DATA, not infrastructure.

Purpose: Give the trigger loop (05-03, WSL2) and the behavior benchmark (05-02, native) valid, schema-correct inputs plus a reusable deterministic grader, so both eval runs are meaningful and repeatable.
Output: trigger-eval.json + trigger-smoke.json (EVAL-01 data), evals.json + 7 eval_metadata.json (EVAL-02 data), grade-run.mjs (deterministic grader), and a .gitignore raise for raw transcripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md
@.planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md
@.planning/phases/05-skill-effectiveness-evals/05-PATTERNS.md

Constants (native path forms; WSL forms live in 05-03):
- PHASE_DIR = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals
- LZTPP = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp
- SKILL_CREATOR_DIR = C:/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator

Ready-to-use source material (lift, do not re-derive):
- 20-query trigger set: 05-RESEARCH.md lines 280-303
- 7 behavior scenarios with expected NAMED transformations + discriminating assertions: 05-RESEARCH.md lines 365-378
- evals.json / eval_metadata.json / grading.json field names: 05-RESEARCH.md lines 307-349; 05-PATTERNS.md lines 74-195

Canonical transformation numbering (verified against transformations.md): #1 ({} -> nil), #2 (nil -> constant), #4 (constant -> scalar), #6 (unconditional -> if), #9 (statement -> tail-recursion), #10 (if -> while), #11 (statement -> recursion), #12 (expression -> function), #13 (variable -> assignment), #14 (case). LOWER number = HIGHER priority.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the trigger eval sets (EVAL-01 data)</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/evals/trigger-eval.json
    .planning/phases/05-skill-effectiveness-evals/evals/trigger-smoke.json
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 223-305 (trigger-set schema + the ready-to-use 20-query candidate set + rationale)
    - .planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md D-02 (set design: 8-10 should-trigger, 8-10 genuinely-tricky near-misses; concrete/detailed queries; session model id)
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md lines 3-12 (the description under test - the trigger target)
    - .claude-plugin/marketplace.json (in-repo JSON formatting analog: 2-space indent, straight quotes, ASCII)
  </read_first>
  <action>
    Write trigger-eval.json as a plain JSON ARRAY (not an object) of {query, should_trigger} objects, lifting the 20-query candidate set verbatim from 05-RESEARCH.md lines 280-303: 10 should_trigger:true (failing-test + "which transformation / minimal change", explicit TPP mentions, reference-mode asks like what does (constant -> scalar) mean) and 10 should_trigger:false near-misses (write-a-test, plain refactor-with-all-tests-green, explain SOLID, mock fetch, reduce complexity, memoized fibonacci, debug a failing test, rename a variable, add pagination, complexity analysis). Keep every query concrete and multi-step exactly as researched; keep the casual phrasing/typos (they matter for realism). Then write trigger-smoke.json as a 2-object subset: one obvious should_trigger:true query (use the "red test expect(of(2)).toBe(1) ... smallest change to green it" query) and one should_trigger:false near-miss (use the "refactor this 80-line processOrder ... all tests already pass" query). ASCII only: arrows as -> inside query strings, straight quotes, 2-space indent, trailing newline. Per D-08 both files go under PHASE_DIR/evals/ (create the dir), never under plugins/.
  </action>
  <verify>
    <automated>node -e "const a=require('D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/evals/trigger-eval.json'); const s=require('D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/evals/trigger-smoke.json'); const ok=Array.isArray(a)&&a.length>=18&&a.every(q=>typeof q.query==='string'&&typeof q.should_trigger==='boolean')&&a.filter(q=>q.should_trigger).length>=8&&a.filter(q=>!q.should_trigger).length>=8&&Array.isArray(s)&&s.length===2&&s.some(q=>q.should_trigger)&&s.some(q=>!q.should_trigger); if(!ok){console.error('FAIL');process.exit(1);} console.log('OK', a.length, 'queries');"</automated>
  </verify>
  <acceptance_criteria>
    - trigger-eval.json parses as a JSON array of >=18 objects, each with a string `query` and boolean `should_trigger`; >=8 are true and >=8 are false.
    - trigger-smoke.json parses as a JSON array of exactly 2 objects, containing one should_trigger:true and one should_trigger:false.
    - No non-ASCII byte in either file (arrows are the two characters `->`); grep for the Unicode arrow returns nothing: `rg -c $'\u2192' <files>` == 0.
  </acceptance_criteria>
  <done>Both trigger sets exist under PHASE_DIR/evals/, parse cleanly, and satisfy the should/should-not counts.</done>
</task>

<task type="auto">
  <name>Task 2: Author the behavior eval set + per-scenario metadata (EVAL-02 data)</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/evals/evals.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-0-fib-nothing-to-constant/eval_metadata.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-1-fib-split-base-case/eval_metadata.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-2-fib-prefer-tail-recursion/eval_metadata.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-3-sum-deep-input-stacksafe/eval_metadata.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-4-flatten-tree-no-tail/eval_metadata.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-5-wordwrap-impasse-backtrack/eval_metadata.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-6-refactor-not-transformation/eval_metadata.json
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 307-378 (evals.json + eval_metadata.json field names; the 7-scenario table with expected NAMED transformations and discriminating assertions)
    - .planning/phases/05-skill-effectiveness-evals/05-PATTERNS.md lines 74-134 (evals.json authoritative shape; the eval-<N>-<slug> dir-prefix / eval_id contract enforced by aggregate_benchmark.py)
    - plugins/lz-tdd/skills/lz-tpp/references/fibonacci-worked-example.md (ground truth for scenarios 0,1,2,5)
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (ground truth for scenarios 3,4 - Kata 1 sum, Kata 2 flatten, the TS/JS overlay)
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md lines 42-66 (the 7-step coach procedure = behavior ground truth for scenarios 5,6)
  </read_first>
  <action>
    Write evals.json as an OBJECT with skill_name:"lz-tpp" and evals:[7 objects]. Each eval object: id (0..6), prompt (a red test + current code snippet, inline), expected_output (one-line success description), files:[], expectations:[strings]. Build the 7 scenarios from the 05-RESEARCH.md table (lines 365-378), preserving the discriminating asserts and the expected NAMED transformations:
    - eval 0 fib-nothing-to-constant: no code yet, red of(0)===0 -> names ({} -> nil) #1 then (nil -> constant) #2, presents `return 0` (not a full algorithm).
    - eval 1 fib-split-base-case: `return 0`, red of(1)===1 -> names (unconditional -> if) #6 + (constant -> scalar) #4; does NOT jump to recursion.
    - eval 2 fib-prefer-tail-recursion: returns 1 for n>1, red of(3)===2 -> names (statement -> tail-recursion) #9; explicitly REJECTS plain (statement -> recursion) #11 as the pick (higher priority + no-reliable-TCO).
    - eval 3 sum-deep-input-stacksafe: Kata 1, base done, red sum(1000000)===500000500000 -> states the deep-input test makes stack safety BEHAVIORAL; names the TS/JS overlay (if -> while) #10 + (variable -> assignment) #13 (or a named trampoline); does NOT offer a plain V8 tail-recursive form as the final answer.
    - eval 4 flatten-tree-no-tail: Kata 2, naive tree recursion green shallow, red on deeply nested -> states (statement -> tail-recursion) #9 is NOT AVAILABLE for tree recursion; names the explicit-stack (if -> while) #10 iteration (or generator-as-state-machine).
    - eval 5 wordwrap-impasse-backtrack: Word Wrap, current hard red only passable by low-priority (expression -> function) #12 -> BACKTRACK: pose a simpler test passable by a higher-priority transformation and/or check for a structure-only refactoring FIRST; does NOT immediately apply #12. Keep the code snippet compact and INLINE (do NOT author a new worked-example reference file - out of scope per A4/D-07).
    - eval 6 refactor-not-transformation: all tests GREEN, request "extract a helper / clean up" -> classify as a REFACTORING (structure-only, behavior-preserving, NOT priority-ranked); declines to name a numbered transformation.
    EVERY scenario's expectations[] MUST also include the cross-cutting "coach, don't drive" assertion: the coach does not edit the provided code and does not run tests. Then write one eval_metadata.json per scenario under lz-tpp-workspace/iteration-1/eval-<N>-<slug>/ with fields eval_id (== the -<N>- prefix int), eval_name (the slug), prompt (same as evals.json), assertions[] (the same expectation strings). ASCII only; 2-space indent. All under PHASE_DIR (D-08).
  </action>
  <verify>
    <automated>node -e "const e=require('D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/evals/evals.json'); const fs=require('fs'); const base='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1'; const slugs=['eval-0-fib-nothing-to-constant','eval-1-fib-split-base-case','eval-2-fib-prefer-tail-recursion','eval-3-sum-deep-input-stacksafe','eval-4-flatten-tree-no-tail','eval-5-wordwrap-impasse-backtrack','eval-6-refactor-not-transformation']; let ok=e.skill_name==='lz-tpp'&&Array.isArray(e.evals)&&e.evals.length===7&&e.evals.every(v=>Array.isArray(v.expectations)&&v.expectations.length>=2); slugs.forEach((s,i)=>{const m=JSON.parse(fs.readFileSync(base+'/'+s+'/eval_metadata.json','utf8')); if(m.eval_id!==i||!Array.isArray(m.assertions)){ok=false;}}); if(!ok){console.error('FAIL');process.exit(1);} console.log('OK 7 scenarios + 7 metadata');"</automated>
  </verify>
  <acceptance_criteria>
    - evals.json parses as an object with skill_name === "lz-tpp" and exactly 7 evals, each having a `prompt` and an `expectations[]` of >=2 strings including a coach-dont-drive assertion.
    - All 7 eval_metadata.json exist at eval-<N>-<slug>/ with eval_id matching the -<N>- prefix (0..6) and an assertions[] mirroring the scenario expectations.
    - Scenario 2 expectations include both the string `(statement -> tail-recursion)` and an explicit non-recommendation of `(statement -> recursion)`; scenario 3 includes both `(if -> while)` and `(variable -> assignment)` (or a named trampoline); grep confirms: `rg -c "statement -> tail-recursion" evals.json` >= 1.
  </acceptance_criteria>
  <done>evals.json (7 scenarios) and 7 eval_metadata.json exist under the workspace, parse cleanly, and carry the named-transformation + coach-dont-drive assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the deterministic grader (grade-run.mjs) + raise .gitignore for raw transcripts</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/grade-run.mjs
    .gitignore
  </files>
  <behavior>
    - Given a run dir with outputs/transcript.md that contains the required named-transformation tokens and a metrics.json with Edit=0/Write=0 and no test run, the grader writes grading.json with all expectations passed and summary.pass_rate == 1.0.
    - Given a transcript missing a required token (e.g. scenario 2 without "(statement -> tail-recursion)"), that expectation is passed:false with evidence naming the missing token, and summary.pass_rate < 1.0.
    - Given a scenario-2 transcript that recommends "(statement -> recursion)" AS the pick, the absent-as-pick expectation is passed:false.
    - Given a metrics.json with Edit>0 or Write>0, the coach-dont-drive expectation is passed:false with evidence citing the counts.
    - Output uses exactly the keys text/passed/evidence per expectation and summary{passed,failed,total,pass_rate}; ASCII-only evidence strings.
  </behavior>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-PATTERNS.md lines 138-194 (grader role, the LOAD-BEARING grading.json output contract, the coach-dont-drive metrics.json check, the deliberate minimal-schema simplification)
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 340-349 (grading.json verbatim fields) and lines 209-219 (Pitfall 4: field names are load-bearing)
    - C:/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/references/schemas.md (grading.json + metrics.json shapes)
    - C:/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/scripts/aggregate_benchmark.py (the consumer: it reads summary.pass_rate/passed/failed/total and warns if an expectation lacks text/passed)
    - .gitignore (current 19-line file; keep its section/blank-line style)
  </read_first>
  <action>
    Write grade-run.mjs as a Node.js ESM script (project default for permanent/reusable scripts; Node built-ins only - fs, path - NO npm install). It takes one argv: a run dir path (e.g. .../eval-2-.../with_skill/run-1). It loads that run's outputs/transcript.md, the sibling metrics.json (tool_calls.Edit/Write counts + a scan of the transcript for a Bash test invocation), and the parent eval dir's eval_metadata.json (for eval_id + the assertion text). Embed a per-eval-id RUBRIC const mapping eval_id -> { present:[tokens that MUST appear in the transcript], absentAsPick:[tokens that must NOT be recommended as the pick], coachDontDrive:true }, using the canonical tokens: eval0 present ["({} -> nil)","(nil -> constant)"]; eval1 present ["(unconditional -> if)","(constant -> scalar)"], absentAsPick ["(statement -> recursion)","(statement -> tail-recursion)"]; eval2 present ["(statement -> tail-recursion)"], absentAsPick ["(statement -> recursion)"]; eval3 present ["(if -> while)","(variable -> assignment)"] with an OR-alternative token "trampoline"; eval4 present ["(if -> while)"] with OR-alternative "generator", and a required phrase indicating tail-recursion is NOT available; eval5 present ["backtrack"] OR "simpler test", absentAsPick ["(expression -> function)"]; eval6 present ["refactoring"], and it must NOT name a numbered transformation as the required move. Normalize whitespace/case for token matching. Emit one grading.json expectation per assertion in eval_metadata (text = the assertion string) with passed = the predicate result and evidence = the matched transcript line OR the missing-token note OR the tool-call counts. Compute summary{passed,failed,total,pass_rate} (pass_rate = passed/total, rounded to 2 dp). Mark the OR-condition / "states X" checks (eval3/eval4 stack-safety-behavioral claims) as a deterministic best-effort match; note in a `// ponytail:` comment that a grader.md judged pass (D-04) can refine those two rationale claims if a string match proves insufficient. Then add a section to .gitignore (keep the existing header style) excluding the bulky raw run transcripts and the trigger workspace: `.planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/**/outputs/` and `.planning/phases/05-skill-effectiveness-evals/trigger-workspace/`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),os=require('os'),path=require('path'); const d=fs.mkdtempSync(path.join(os.tmpdir(),'grade')); const ev=path.join(d,'eval-2-fib-prefer-tail-recursion'); const run=path.join(ev,'with_skill','run-1'); fs.mkdirSync(path.join(run,'outputs'),{recursive:true}); fs.writeFileSync(path.join(ev,'eval_metadata.json'),JSON.stringify({eval_id:2,eval_name:'fib-prefer-tail-recursion',prompt:'x',assertions:['names (statement -> tail-recursion)','does NOT recommend (statement -> recursion)','coach does not edit code or run tests']})); fs.writeFileSync(path.join(run,'outputs','transcript.md'),'Apply (statement -> tail-recursion) #9 with an accumulator helper; do not use (statement -> recursion) #11.'); fs.writeFileSync(path.join(run,'metrics.json'),JSON.stringify({tool_calls:{Edit:0,Write:0}})); const {execFileSync}=require('child_process'); execFileSync('node',['D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/grade-run.mjs',run],{stdio:'inherit'}); const g=JSON.parse(fs.readFileSync(path.join(run,'grading.json'),'utf8')); const ok=Array.isArray(g.expectations)&&g.expectations.every(e=>'text'in e&&'passed'in e&&'evidence'in e)&&typeof g.summary.pass_rate==='number'&&g.summary.pass_rate===1; if(!ok){console.error('FAIL',JSON.stringify(g));process.exit(1);} console.log('OK grader pass_rate',g.summary.pass_rate);"</automated>
  </verify>
  <acceptance_criteria>
    - Running `node grade-run.mjs <run-dir>` writes grading.json whose expectations each use exactly text/passed/evidence and whose summary has passed/failed/total/pass_rate.
    - On the self-check fixture (scenario 2, correct transcript, Edit=0/Write=0) summary.pass_rate === 1.0; flipping the transcript to omit `(statement -> tail-recursion)` yields pass_rate < 1.0.
    - `.gitignore` contains a line matching `lz-tpp-workspace/**/outputs/` and a line matching `trigger-workspace/`; the eval SETS and eval_metadata/grading/benchmark files remain tracked (not ignored).
  </acceptance_criteria>
  <done>grade-run.mjs runs against a run dir and emits schema-correct grading.json; .gitignore excludes only the bulky raw transcripts and the trigger workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored JSON -> skill-creator harness | Author-supplied eval sets are parsed and fed to run_loop / aggregate; malformed JSON breaks the run |
| repo working tree -> committed public surface | Files staged here become public; must stay lean and email-clean (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (V5 input validation) | evals/*.json, eval_metadata.json | mitigate | Each task's automated verify does `JSON.parse` / require() on every authored JSON before it can be fed to the harness; a parse failure fails the task. |
| T-05-03 | Information disclosure | committed eval artifacts + .gitignore | mitigate | .gitignore excludes bulky raw transcripts (D-08); public email allowlist enforced in 05-04 (only larsbrinknielsen@gmail.com permitted). Stage files by name, never `git add .`. |
| T-05-SC | Tampering (supply chain) | grade-run.mjs runtime | accept | No package installs this phase: harness ships with skill-creator; grade-run.mjs uses Node built-ins only (fs, path). No npm/PyPI slopcheck surface. |
</threat_model>

<verification>
- All authored JSON parses (Task 1-2 automated checks).
- grade-run.mjs self-check produces schema-correct grading.json (Task 3 automated check).
- ASCII-only: `rg -c $'\u2192' .planning/phases/05-skill-effectiveness-evals/evals/` returns 0 (arrows are the two chars `->`).
- `.gitignore` excludes raw transcripts + trigger-workspace; eval sets remain tracked (`git status` shows them as new tracked files).
</verification>

<success_criteria>
- trigger-eval.json (>=18 queries), trigger-smoke.json (2), evals.json (7 scenarios), 7 eval_metadata.json, and grade-run.mjs all exist under PHASE_DIR and pass their parse/self-check verifications.
- Ground truth is lifted from the locked references (not re-derived); named-transformation tokens match the verified numbering.
- .gitignore keeps raw transcripts out of the committed surface (D-08).
</success_criteria>

<output>
Create `.planning/phases/05-skill-effectiveness-evals/05-01-SUMMARY.md` when done, recording: the final query counts, the 7 scenario slugs + expected transformations, the grader's rubric tokens per eval_id, and confirmation that all artifacts live under PHASE_DIR (nothing under plugins/).
</output>
