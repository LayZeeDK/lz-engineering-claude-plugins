---
phase: 01-marketplace-plugin-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - plugins/lz-tdd/.claude-plugin/plugin.json
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
  - .gitignore
autonomous: true
requirements: [MKT-01, MKT-02, MKT-03, MKT-04, MKT-05, DIST-04]

must_haves:
  truths:
    - "Running `claude plugin validate .` reports no errors"
    - "The local `claude plugin marketplace add .` resolves the marketplace and lists lz-tdd via its ./plugins/lz-tdd source"
    - "version 0.0.1 appears only in plugin.json, absent from the marketplace entry (no version masking)"
    - "A second skill under lz-tdd or a second plugin can be added by creating new directories only, with no edits to existing manifests"
    - "A .gitignore (Node/OS noise, not ignoring .planning/) and a valid placeholder SKILL.md with frontmatter both exist, so the repo commits clean"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog: name lz-engineering-claude-plugins, owner, one plugins[] entry with source ./plugins/lz-tdd, no version field"
      contains: "\"source\": \"./plugins/lz-tdd\""
    - path: "plugins/lz-tdd/.claude-plugin/plugin.json"
      provides: "Plugin manifest: name lz-tdd, version 0.0.1, MIT license, author (public gmail), keywords; no skills/commands path fields"
      contains: "\"version\": \"0.0.1\""
    - path: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      provides: "Placeholder skill with YAML frontmatter name: lz-tpp + non-triggering description + stub body"
      contains: "name: lz-tpp"
    - path: ".gitignore"
      provides: "Node + OS ignore rules; does NOT ignore .planning/"
      contains: "node_modules/"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "plugins/lz-tdd/.claude-plugin/plugin.json"
      via: "plugins[].source relative string"
      pattern: "\\./plugins/lz-tdd"
    - from: "plugins/lz-tdd/"
      to: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      via: "skills/ auto-discovery (no path field in plugin.json)"
      pattern: "skills/lz-tpp/SKILL\\.md"
    - from: "plugins/lz-tdd/skills/lz-tpp/ (dir name)"
      to: "SKILL.md frontmatter name: lz-tpp"
      via: "namespace derivation -> /lz-tdd:lz-tpp"
      pattern: "name:\\s*lz-tpp"
---

<objective>
Scaffold a valid, installable Claude Code marketplace repo hosting the `lz-tdd` plugin
with a placeholder `lz-tpp` skill. Create exactly four static files -- the three-level
manifest hierarchy plus a repo-hygiene `.gitignore` -- then gate the result on the
first-party `claude plugin validate` CLI and the plugin-dev `plugin-validator` agent.

Purpose: Stand up an installable, validating manifest hierarchy with an additive-by-design
layout so later phases (TPP source distillation, skill authoring, distribution) are pure
additions with no restructuring. This is the foundation phase; nothing ships without it.

Output:
- `.claude-plugin/marketplace.json` (marketplace catalog)
- `plugins/lz-tdd/.claude-plugin/plugin.json` (plugin manifest)
- `plugins/lz-tdd/skills/lz-tpp/SKILL.md` (placeholder skill)
- `.gitignore` (Node/OS hygiene)

This phase is pure JSON + Markdown. No runtime code, no build step, no external packages,
no `package.json`. `claude plugin validate` is the gate, not npm (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source-of-truth for THIS phase (read before implementing):
@.planning/phases/01-marketplace-plugin-scaffold/01-CONTEXT.md
@.planning/phases/01-marketplace-plugin-scaffold/01-RESEARCH.md
@.planning/phases/01-marketplace-plugin-scaffold/01-VALIDATION.md
@.planning/research/ARCHITECTURE.md

<constraints>
Repo-wide constraints that override generic habits (from CLAUDE.md + CONTEXT.md):
- ASCII-only in EVERY committed file. No emoji, no Unicode punctuation. Use `->` not
  arrows, `--` not em/en dashes, straight quotes. Windows cp1252 mojibake otherwise.
- Public contact `larsbrinknielsen@gmail.com` ONLY. The work email (from global CLAUDE.md;
  not repeated here to avoid committing it to a public repo) must appear NOWHERE in this
  public repo.
- Every committed manifest/doc uses the FINAL PLURAL name `lz-engineering-claude-plugins`,
  even though the local working directory is the singular `lz-engineering-claude-plugin`
  (D-11). Never write the singular name into a committed file.
- Strict JSON in both manifests: no comments, no trailing commas (Pitfall 8).
- The exact, copy-ready content of all four files is specified in 01-RESEARCH.md
  "Concrete File Specs (buildable)" sections 1-4. Use those specs verbatim (with the
  version correction below). Do not re-derive field sets from STACK.md.
- VERSION GOTCHA: STACK.md's sample plugin.json shows `version: "0.1.0"`. That is WRONG
  for this milestone. Use `"0.0.1"` (D-04 / MKT-02). Do not copy 0.1.0.
- SCOPE FENCES (D-12): the `license: "MIT"` FIELD in plugin.json is in scope; the LICENSE
  FILE and README content are Phase 4 (out of scope here). No commands/agents/hooks/.mcp.json.
  No package.json / build / lint tooling. Keep `.planning/` TRACKED (do NOT gitignore it).
  Do NOT add a stub README.md (Phase 4 owns DIST-01; omitting keeps the phase boundary clean).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the lz-tdd plugin subtree (plugin.json + placeholder SKILL.md)</name>
  <files>plugins/lz-tdd/.claude-plugin/plugin.json, plugins/lz-tdd/skills/lz-tpp/SKILL.md</files>
  <read_first>
    - .planning/phases/01-marketplace-plugin-scaffold/01-RESEARCH.md -- "Concrete File Specs" section 3 (plugin.json) and section 4 (SKILL.md placeholder), including the field-provenance tables
    - .planning/phases/01-marketplace-plugin-scaffold/01-CONTEXT.md -- decisions D-02, D-04, D-07, D-08, D-09, D-11, D-12
    - .planning/research/ARCHITECTURE.md -- "Recommended Project Structure" (plugin subtree), Anti-Pattern 2 (components at plugin root, not inside .claude-plugin/), Anti-Pattern 5 (name alignment)
  </read_first>
  <action>
    Create the plugin subtree that the marketplace `source` will point at (built first per
    the RESEARCH build order: the referenced target exists before the referencer).

    plugins/lz-tdd/.claude-plugin/plugin.json (per D-07, exact content in RESEARCH spec #3):
    strict JSON object with `name` "lz-tdd" (kebab-case; drives the /lz-tdd: namespace),
    `version` "0.0.1" (full X.Y.Z semver -- NOT 0.1.0, per D-04), `description`, `author`
    object with `name` "Lars Gyrup Brink Nielsen" and `email` "larsbrinknielsen@gmail.com"
    (public gmail ONLY, never the work email), `license` "MIT" (machine-readable field only;
    the LICENSE file is Phase 4 per D-12), and `keywords` array. Include both `homepage` and
    `repository` as string URLs pointing at https://github.com/LayZeeDK/lz-engineering-claude-plugins
    (discretion, recommended by RESEARCH). Include NO `skills`/`commands`/`agents`/`hooks`/`mcpServers`
    path fields -- rely on auto-discovery of skills/ (D-02). keywords set is discretion: use the
    RESEARCH-recommended tdd, test-driven-development, transformation-priority-premise, tpp,
    red-green-refactor, clean-code, typescript.

    plugins/lz-tdd/skills/lz-tpp/SKILL.md (per D-08/D-09, exact content in RESEARCH spec #4):
    YAML frontmatter with `name: lz-tpp` (MUST equal the directory name lz-tpp so the namespace
    resolves to /lz-tdd:lz-tpp -- Anti-Pattern 5) plus a short `description`. The description is
    a plain STUB that reads clearly as a placeholder and is deliberately NOT trigger-tuned for
    TDD (D-09 -- trigger tuning is Phase 3/5; a non-triggering stub avoids auto-firing during
    development). Do NOT add a `version` field (not a valid skill frontmatter field). Do NOT set
    `disable-model-invocation` or `user-invocable` -- keep frontmatter defaults (D-08). Body is a
    short stub noting the dual-mode coach + reference is authored in Phase 3. Create NO references/
    directory yet (D-08).

    ASCII-only throughout. Strict JSON in plugin.json (no comments, no trailing commas).
  </action>
  <verify>
    <automated>python -m json.tool plugins/lz-tdd/.claude-plugin/plugin.json &amp;&amp; rg -n "\"version\": \"0.0.1\"" plugins/lz-tdd/.claude-plugin/plugin.json &amp;&amp; rg -n "^name: lz-tpp$" plugins/lz-tdd/skills/lz-tpp/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `python -m json.tool plugins/lz-tdd/.claude-plugin/plugin.json` exits 0 (strict-JSON well-formed)
    - `rg -n '"version": "0.0.1"' plugins/lz-tdd/.claude-plugin/plugin.json` matches (version is 0.0.1, not 0.1.0)
    - `rg -n '"name": "lz-tdd"' plugins/lz-tdd/.claude-plugin/plugin.json` matches
    - `rg -n '"license": "MIT"' plugins/lz-tdd/.claude-plugin/plugin.json` matches
    - `rg -n '"skills"|"commands"|"agents"|"hooks"|"mcpServers"' plugins/lz-tdd/.claude-plugin/plugin.json` returns NO matches (auto-discovery only; MKT-04/D-02)
    - `rg -n "^name: lz-tpp$" plugins/lz-tdd/skills/lz-tpp/SKILL.md` matches AND the file has a `description:` frontmatter key (frontmatter present; success criterion 5)
    - `rg -n "^version:" plugins/lz-tdd/skills/lz-tpp/SKILL.md` returns NO matches (no version field in skill frontmatter, D-08)
    - No `references/` directory exists under plugins/lz-tdd/skills/lz-tpp/ (D-08)
    - Email allowlist (work email absent): `rg -uNo -e '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]+' plugins/lz-tdd/.claude-plugin/plugin.json plugins/lz-tdd/skills/lz-tpp/SKILL.md | rg -v 'larsbrinknielsen@gmail\.com'` returns NO output (the ONLY email present is the public gmail)
  </acceptance_criteria>
  <done>
    plugin.json is valid strict JSON with name lz-tdd, version 0.0.1, MIT license, public-gmail
    author, keywords, and no component path fields. SKILL.md is a valid placeholder with
    frontmatter name: lz-tpp and a non-triggering stub description. No references/ dir. All ASCII.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the root marketplace manifest and the .gitignore hygiene file</name>
  <files>.claude-plugin/marketplace.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-marketplace-plugin-scaffold/01-RESEARCH.md -- "Concrete File Specs" section 1 (.gitignore) and section 2 (marketplace.json), including the field-provenance table and the [ASSUMED] note about the optional top-level description
    - .planning/phases/01-marketplace-plugin-scaffold/01-CONTEXT.md -- decisions D-01, D-03, D-05, D-06, D-10, D-11
    - .planning/research/ARCHITECTURE.md -- "source Reference Forms", Anti-Pattern 1 (do not flatten the plugin to repo root), Anti-Pattern 4 (relative ./ paths, no ../ or absolute)
    - plugins/lz-tdd/.claude-plugin/plugin.json (created in Task 1 -- the target the marketplace source resolves to)
  </read_first>
  <action>
    Create the marketplace catalog (the referencer) now that the plugin subtree it points at
    exists (D-01 three-level hierarchy; the plugins/lz-tdd container is kept separate from the
    repo-root .claude-plugin/ so a second plugin is a pure addition -- Anti-Pattern 1).

    .claude-plugin/marketplace.json (per D-05/D-06, exact content in RESEARCH spec #2): strict
    JSON with `$schema` "https://anthropic.com/claude-code/marketplace.schema.json" (documentary;
    the validator uses its own internal schema regardless -- D-05), `name`
    "lz-engineering-claude-plugins" (PLURAL, matches the target GitHub repo -- D-06/D-11),
    `owner` object with `name` "Lars Gyrup Brink Nielsen" (and public-gmail `email`), and a
    `plugins` array with ONE entry: `name` "lz-tdd", `source` "./plugins/lz-tdd" (relative
    string, ./-prefixed, forward slashes, never ../ or absolute -- D-03/Anti-Pattern 4),
    `description`, and `category` "development". Include a top-level `description`
    (RESEARCH-recommended, clears the strict "missing metadata" warning; [ASSUMED] A1, verify in
    Task 3). CRITICAL: include NO `version` field anywhere in the marketplace entry (D-04/MKT-05
    -- version lives ONLY in plugin.json; declaring it here would trigger the version-masking trap).

    .gitignore at repo root (per D-10, exact content in RESEARCH spec #1): Node + OS baseline --
    node_modules/, build output (dist/, build/, out/), *.log and npm-debug.log*, OS noise
    (.DS_Store, Thumbs.db), and minimal editor entries (.idea/, *.swp). MUST NOT contain any
    `.planning` entry -- .planning/ stays TRACKED (D-10). Extra editor entries are discretion;
    keep minimal.

    ASCII-only throughout. Strict JSON in marketplace.json (no comments, no trailing commas).
  </action>
  <verify>
    <automated>python -m json.tool .claude-plugin/marketplace.json &amp;&amp; rg -n "\"source\": \"\./plugins/lz-tdd\"" .claude-plugin/marketplace.json &amp;&amp; test -f .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `python -m json.tool .claude-plugin/marketplace.json` exits 0 (strict-JSON well-formed)
    - `rg -n '"name": "lz-engineering-claude-plugins"' .claude-plugin/marketplace.json` matches (plural marketplace name; D-06/D-11)
    - `rg -n '"source": "./plugins/lz-tdd"' .claude-plugin/marketplace.json` matches (relative source; MKT-01/D-03)
    - `rg -n '"version"' .claude-plugin/marketplace.json` returns NO matches (no version in the marketplace entry; MKT-05/D-04)
    - `rg -n '"\$schema"' .claude-plugin/marketplace.json` matches the Anthropic schema URL (D-05)
    - `.gitignore` exists at the repo root and `rg -n "^node_modules/" .gitignore` matches
    - `rg -n "\.planning" .gitignore` returns NO matches (.planning/ stays tracked; D-10)
    - Singular-name-leak check (word-boundary, excludes the plural): `rg -uNo -e 'lz-engineering-claude-plugin\b' .claude-plugin plugins | rg -v 'lz-engineering-claude-plugins'` returns NO output (no SINGULAR working-dir name leaked; D-11)
    - Email allowlist (work email absent): `rg -uNo -e '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]+' .claude-plugin/marketplace.json .gitignore | rg -v 'larsbrinknielsen@gmail\.com'` returns NO output (the ONLY email present is the public gmail)
  </acceptance_criteria>
  <done>
    marketplace.json is valid strict JSON named lz-engineering-claude-plugins with one plugins[]
    entry resolving to ./plugins/lz-tdd and NO version field. .gitignore covers Node/OS noise and
    does not ignore .planning/. All committed identifiers use the plural repo name; all ASCII.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate the full scaffold (CLI gate + local install loop + hygiene)</name>
  <files>.claude-plugin/marketplace.json, plugins/lz-tdd/.claude-plugin/plugin.json, plugins/lz-tdd/skills/lz-tpp/SKILL.md, .gitignore</files>
  <read_first>
    - .planning/phases/01-marketplace-plugin-scaffold/01-RESEARCH.md -- "Validation Architecture" (CLI signatures, Success Criterion -> Validation Point Map, Sampling Rate), "Build Order and Ordering Constraints" (do NOT validate until plugin.json AND SKILL.md exist), "Open Questions" (strict-warning-vs-error nuance on the placeholder description)
    - .planning/phases/01-marketplace-plugin-scaffold/01-VALIDATION.md -- "Per-Task Verification Map" and "Manual-Only Verifications" (remote resolution is DEFERRED)
    - .planning/phases/01-marketplace-plugin-scaffold/01-CONTEXT.md -- decision D-13 (verification caveat: LOCAL path proxy only; remote name resolution is ship-time, do NOT claim it here)
    - all four created files (marketplace.json, plugin.json, SKILL.md, .gitignore)
  </read_first>
  <action>
    Run the full validation gate now that all four files exist (hard ordering constraint: the
    marketplace `source` resolution and skills/ auto-discovery both fail against an incomplete
    subtree -- create all, then validate once).

    1. Primary gate: run `claude plugin validate .` from the repo root -- MUST exit 0 with no
       errors. Then run `claude plugin validate . --strict`. Per RESEARCH Open Question 1: a
       strict ERROR is a real gate failure to fix; a strict WARNING on the placeholder skill's
       intentionally-short non-triggering description is EXPECTED and non-blocking (D-09).
       Confirm [ASSUMED] A1 empirically (whether strict flags a missing top-level marketplace
       description) and note the observed behavior in the SUMMARY.
    2. Local install proxy for MKT-01 (D-13 -- PATH-based, so the singular working-dir name is
       irrelevant): `claude plugin marketplace add .` MUST succeed (this parses marketplace.json
       AND resolves the ./plugins/lz-tdd source); `claude plugin marketplace list` MUST show
       lz-engineering-claude-plugins; `claude plugin list` MUST surface lz-tdd. Then clean up with
       `claude plugin marketplace remove lz-engineering-claude-plugins`. Do NOT run or claim the
       REMOTE `/plugin marketplace add LayZeeDK/lz-engineering-claude-plugins` -- it is DEFERRED to
       ship-time (the GitHub repo + physical rename do not exist yet); record it as deferred, not
       passing (D-13).
    3. Secondary structural review for MKT-03: run the plugin-dev `plugin-validator` agent against
       the lz-tdd plugin and confirm no structural findings. If the agent is unavailable,
       `claude plugin validate . --strict` clean is the sufficient minimum fallback (per RESEARCH
       Environment Availability); note which path was used.
    4. Version-mask assertion (MKT-05): confirm `version` appears in plugin.json ONLY, never in
       marketplace.json.
    5. Public-repo hygiene: confirm the work email appears nowhere and all four committed files
       are ASCII-only.

    Fix any error surfaced by steps 1-5 by editing the offending file, then re-run the gate. Do
    not write bespoke validation scripts -- the first-party CLI + plugin-dev agent are the gate.
  </action>
  <verify>
    <automated>claude plugin validate . &amp;&amp; rg -n "\"version\"" plugins/lz-tdd/.claude-plugin/plugin.json &amp;&amp; ! rg -n "\"version\"" .claude-plugin/marketplace.json &amp;&amp; ! (rg -uNo -e '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]+' .claude-plugin plugins .gitignore | rg -v 'larsbrinknielsen@gmail\.com')</automated>
  </verify>
  <acceptance_criteria>
    - `claude plugin validate .` exits 0 with no errors (MKT-03, success criterion 1)
    - `claude plugin validate . --strict` produces no ERRORS; any WARNING is only on the placeholder skill description and is documented as expected/non-blocking (D-09)
    - `claude plugin marketplace add .` succeeds; `claude plugin marketplace list` shows lz-engineering-claude-plugins; `claude plugin list` surfaces lz-tdd; cleanup `claude plugin marketplace remove lz-engineering-claude-plugins` succeeds (MKT-01 LOCAL proxy, success criterion 2)
    - plugin-dev `plugin-validator` agent returns no structural findings (or, if unavailable, `--strict` clean is recorded as the fallback per RESEARCH) (MKT-03)
    - `rg -n '"version"' plugins/lz-tdd/.claude-plugin/plugin.json` matches AND `rg -n '"version"' .claude-plugin/marketplace.json` returns NO matches (MKT-05, success criterion 3)
    - plugin.json has no `skills`/`commands`/`agents`/`hooks`/`mcpServers` path fields and marketplace.json has exactly one plugins[] entry -- confirming a second skill/plugin is addable by new directories only (MKT-04, success criterion 4 -- by inspection)
    - `.gitignore` exists and does not list `.planning/`; SKILL.md frontmatter is valid (confirmed by `claude plugin validate .`) (DIST-04, success criterion 5)
    - Email allowlist over the committed manifests: `rg -uNo -e '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]+' .claude-plugin plugins .gitignore | rg -v 'larsbrinknielsen@gmail\.com'` returns NO output (the ONLY email present is the public gmail; work email absent; hygiene)
    - No committed file references the singular working-dir name `lz-engineering-claude-plugin` (as a standalone token, not the plural); all committed content is ASCII-only (D-11, hygiene)
    - Remote resolution is recorded as DEFERRED (D-13), NOT claimed as passing
  </acceptance_criteria>
  <done>
    `claude plugin validate .` passes clean, `--strict` has no errors, the local marketplace
    add/list/remove loop resolves and lists lz-tdd via ./plugins/lz-tdd, the plugin-dev validator
    (or --strict fallback) reports no structural findings, version is confirmed to live only in
    plugin.json, and repo hygiene (no work email, ASCII-only, plural name) holds. Remote
    resolution is documented as deferred to ship-time, not claimed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub | Static JSON + Markdown scaffold published to a public repository. The only crossing is human-authored manifest content becoming publicly readable. No runtime input, no code execution, no network calls, no external packages. |

## STRIDE Threat Register

This phase creates only static JSON + Markdown scaffold files. There is NO code execution,
auth, session handling, cryptography, input parsing, network I/O, or external package
installation. ASVS V2-V6 do not apply to a manifest scaffold. No threat at or above the
HIGH block threshold exists for this phase. The relevant surface is public-repo hygiene only.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | author.email / owner.email in manifests | mitigate | Use `larsbrinknielsen@gmail.com` ONLY; the work email (from global CLAUDE.md) appears nowhere. Verifiable via an email allowlist: extract every email from the committed manifests and assert the only match is the public gmail (Task 3). Severity: LOW (public gmail is intended contact). |
| T-01-02 | Tampering | non-ASCII / smart punctuation in committed files | mitigate | ASCII-only in every committed file (`->` not arrows, `--` not em/en dashes, straight quotes) to prevent Windows cp1252 mojibake. Verifiable by inspection in Task 3. Severity: LOW. |
| T-01-03 | Tampering | `source` path traversal in manifests | mitigate | `source` is a `./`-relative string with forward slashes, never `../` or absolute (D-03). `claude plugin validate` rejects path traversal (Task 3). Severity: LOW. |
| T-01-04 | Repudiation | ambiguous reuse rights | mitigate | `license: "MIT"` field set in plugin.json this phase (the LICENSE FILE is Phase 4, D-12). Verifiable: `rg -n '"license": "MIT"'` (Task 1). Severity: LOW. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Not applicable -- this phase installs NO external packages (pure JSON + Markdown, no package.json, no build; D-12). No supply-chain surface exists; no legitimacy checkpoint required. |

No STRIDE threat at or above the ASVS L1 / HIGH block threshold applies to this static
manifest scaffold. The hygiene controls above are the mitigations; all are verified by
`rg`/inspection in Task 3.
</threat_model>

<verification>
Phase-level checks (run after all four files exist; see 01-VALIDATION.md for the full map):

- `claude plugin validate .` exits 0 with no errors (MKT-03).
- `claude plugin validate . --strict` produces no errors (warnings only on the placeholder
  description are expected per D-09).
- Local install loop: `claude plugin marketplace add .` -> `claude plugin marketplace list`
  (shows lz-engineering-claude-plugins) -> `claude plugin list` (surfaces lz-tdd) ->
  `claude plugin marketplace remove lz-engineering-claude-plugins` (MKT-01, LOCAL proxy only).
- plugin-dev `plugin-validator` agent: no structural findings (or `--strict` clean fallback).
- `version` in plugin.json only, absent from marketplace.json (MKT-05).
- plugin.json has no component path fields; one marketplace entry per plugin (MKT-04, by inspection).
- `.gitignore` present, does not ignore `.planning/`; SKILL.md frontmatter valid (DIST-04).
- Hygiene: no work email anywhere (email allowlist); ASCII-only committed files; plural repo name throughout.
- Remote `/plugin marketplace add LayZeeDK/...` is DEFERRED to ship-time (D-13) -- NOT claimed here.
</verification>

<success_criteria>
- All four files exist: `.claude-plugin/marketplace.json`, `plugins/lz-tdd/.claude-plugin/plugin.json`,
  `plugins/lz-tdd/skills/lz-tpp/SKILL.md`, `.gitignore`.
- `claude plugin validate .` passes clean and `--strict` has no errors.
- The local marketplace add/list/remove loop resolves the marketplace and lists lz-tdd via
  its `./plugins/lz-tdd` source.
- `version: "0.0.1"` appears only in plugin.json (absent from the marketplace entry).
- The layout is additive: plugin.json has no component path fields (auto-discovery); adding a
  second skill dir or a second plugin subtree + one marketplace entry needs no edits to existing files.
- `.gitignore` covers Node/OS noise and keeps `.planning/` tracked; the placeholder SKILL.md
  has valid frontmatter so the repo commits clean.
- Repo hygiene: work email absent, ASCII-only, plural repo name everywhere.
- All six phase requirements satisfied: MKT-01, MKT-02, MKT-03, MKT-04, MKT-05, DIST-04.
</success_criteria>

<output>
Create `.planning/phases/01-marketplace-plugin-scaffold/01-01-SUMMARY.md` when done.
Record: the four files created, the exact `claude plugin validate .` / `--strict` output
(including any expected placeholder-description warning and the A1 top-level-description
finding), the local marketplace add/list/remove loop result, which secondary review path was
used (plugin-dev agent vs --strict fallback), and an explicit note that remote name resolution
is DEFERRED to ship-time (D-13) and was not claimed.
</output>
