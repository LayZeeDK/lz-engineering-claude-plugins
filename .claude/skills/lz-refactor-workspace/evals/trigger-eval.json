[
  {"query": "all tests pass, but this 60-line `processOrder()` does five different things and i keep scrolling to follow it. whats the refactoring here?", "should_trigger": true},
  {"query": "green bar. i've got the same date-formatting block copy-pasted across three services - which named refactoring cleans up that duplication?", "should_trigger": true},
  {"query": "tests are green. `Invoice.describe()` only ever reads a Customer's name, tier and address and barely touches Invoice itself. is there a named refactoring for that?", "should_trigger": true},
  {"query": "i'm on the refactor step of red-green-refactor and the tests are green. whats the right named move for a function that takes 7 positional args?", "should_trigger": true},
  {"query": "green. this pricing class has a giant switch on customerType that also flips discount flags and picks the next state. feels like it wants a pattern - which refactoring do i reach for?", "should_trigger": true},
  {"query": "what does Extract Function actually do, and when should i reach for it instead of inlining?", "should_trigger": true},
  {"query": "what does Replace Conditional with Polymorphism mean, and how is it different from just tidying a switch in place?", "should_trigger": true},
  {"query": "i have a `Config` Singleton that everything reaches into globally and it's making my tests painful. should i refactor away from the Singleton?", "should_trigger": true},
  {"query": "whats the 'rule of three' about - the guideline for when you should actually refactor duplicated code?", "should_trigger": true},
  {"query": "tests pass. i've got ~12 near-identical methods each handling one combination of {currency, taxRegion, rounding}, and adding a currency means adding a dozen more. what refactoring gets me out of this?", "should_trigger": true},

  {"query": "red test `expect(of(2)).toBe(1)`, my `of` is `return n <= 1 ? n : 1;`. whats the smallest edit to get it passing? doing tdd", "should_trigger": false},
  {"query": "my failing test wants fib(3) to be 2 but the code returns 1 for anything above 1. whats the minimal transformation to green it without writing the whole recursion yet?", "should_trigger": false},
  {"query": "which change makes this failing test pass - is there a smaller step than writing the full algorithm?", "should_trigger": false},
  {"query": "refactor this to be faster - it's O(n^2) and i need it under 50ms", "should_trigger": false},
  {"query": "write a jest test for this `formatCurrency(cents)` helper covering negatives and zero", "should_trigger": false},
  {"query": "write a debounce helper in typescript", "should_trigger": false},
  {"query": "add pagination to the GET /users endpoint in this express app", "should_trigger": false},
  {"query": "explain the SOLID principles with a short typescript example for each", "should_trigger": false},
  {"query": "my CSV parse step throws on empty rows and one of my tests is red - help me find the bug", "should_trigger": false},
  {"query": "whats the time and space complexity of this recursive merge sort?", "should_trigger": false}
]
