---
phase: 16-source-distillation-core-red-references
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/naming.md
autonomous: true

requirements: [SEL-01, SEL-02, STR-01, STR-02, NAME-01]

must_haves:
  decisions: [D-04, D-10, D-12]
  truths:
    - The full validation battery is GREEN -- check-red-references (all three slices), extract-samples (tsc --strict over every fence), and check-hygiene (ASCII + email + no-verbatim) all exit 0.
    - claude plugin validate . exits 0 with the three filled references and the dev-only workspace present.
    - A skill-reviewer (including at least one unbiased from-scratch reviewer) has reviewed the lz-red RED references and returned PASS (any surfaced fixes applied and re-gated).
    - Every owned surface (RCM in SEL/STR, Metz in NAME) is confirmed oracle-reviewer all-pass, and the no-verbatim scan is clean over the whole lz-red tree.
    - The two co-edited stubs retain their later-phase markers (Phase 17 ASRT, Phase 18 LAW/SEAM) -- no deferred slice was over-filled.
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (reviewer fixes applied if any)
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (reviewer fixes applied if any)
    - plugins/lz-tdd/skills/lz-red/references/naming.md (reviewer fixes applied if any)
  key_links:
    - The finalize gate re-runs the exact Wave-1 checker commands over the merged tree, confirming no cross-slice regression.
    - Any reviewer-driven change to an owned surface re-enters the oracle-reviewer converge-to-clean loop before it ships.
  prohibitions:
    - NO verbatim book/talk prose survives the final no-verbatim scan; the executor NEVER reads .oracle/ prose during any re-draft.
    - NO maintainer work-email or its bare domain in any committed file (allowlist-inversion; only larsbrinknielsen@gmail.com); confirm git author/committer = the public gmail before the phase-gate commit.
    - NO weakening of any checker to force GREEN -- fix the content, not the gate.
    - NO over-fill of the Phase 17 (ASRT) / Phase 18 (LAW/SEAM) slices during reviewer fixups.
---

<objective>
Close the phase: run the full validation battery GREEN over the merged tree, run a skill-reviewer
(at least one unbiased from-scratch) on the three RED references, confirm every owned surface passed
the oracle-reviewer gate and the no-verbatim scan, and confirm claude plugin validate . exits 0. This
is the Wave-2 finalize/gate plan.

Purpose: the per-task checks in Wave-1 verify each slice as it lands; this plan confirms the whole
lz-red reference set holds together, that the load-bearing SEL-02 firewall and the AAA/GWT one-skeleton
framing read correctly to a fresh reviewer, and that no deferred slice was over-filled -- before the
phase is marked complete.

Output: a GREEN full battery, a skill-reviewer PASS, and a confirmed clean phase gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md
@.planning/phases/16-source-distillation-core-red-references/16-RESEARCH.md
@.planning/phases/16-source-distillation-core-red-references/16-VALIDATION.md
</context>

## Decisions implemented

- **D-04:** the finalize gate confirms the two co-edited stubs still carry their Phase 17 (ASRT) and Phase 18 (LAW/SEAM) deferral markers -- no over-fill.
- **D-10:** the phase gate confirms every TypeScript sample is tsc --strict clean via the extract-samples gate over all fences.
- **D-12:** the phase gate confirms every owned surface passed the oracle-reviewer converge-to-clean loop and the deterministic no-verbatim scan is clean across the whole lz-red tree.

<tasks>

<task type="auto">
  <name>Task 1: Skill-reviewer PASS on the RED references + owned-surface fidelity confirmation</name>
  <files>plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md, plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md, plugins/lz-tdd/skills/lz-red/references/naming.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (filled by 16-02; confirm the SEL-02 firewall and the preserved Phase 18 marker)
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (confirm AAA/GWT one-skeleton framing and the preserved Phase 17 marker)
    - plugins/lz-tdd/skills/lz-red/references/naming.md (confirm should-primary + Osherove alternative + match-house-stance)
    - .planning/phases/16-source-distillation-core-red-references/16-RESEARCH.md (the Pitfalls: DST-04 near-verbatim, SEL-02 firewall bleed) so the reviewer brief targets the real risks
  </read_first>
  <action>
    Spawn a skill-reviewer over the three filled lz-red references, including at least ONE unbiased from-scratch reviewer whose brief carries NO prior findings (a neutral read catches leaks a primed reviewer confirms past). The review brief must check: the SEL-02 triangulation prose stays on the RED (test-selection) side of the lz-tpp seam and does not drift into GREEN generalization; AAA and GWT read as ONE skeleton in TWO vocabularies (no imposed school); "should"/behavior naming is primary with Osherove three-part as the alternative; no near-verbatim canonical one-liner survives (Osherove three-part, BDD "should", F.I.R.S.T./AAA glosses); every fence is minimal-surface and tsc-strict; the Phase 17/18 markers are intact. Apply any surfaced fixes. If a fix touches an OWNED surface (RCM or Metz rationale), re-enter the oracle-reviewer converge-to-clean loop for that surface (draft path + the relevant .oracle/<book>/index.md + CONTENT_TYPE=other + anchors) and re-draft BLIND on any revise -- the executor NEVER reads .oracle/ prose. Do NOT weaken any checker to pass. Confirm from the 16-02 outputs that every owned surface already returned an oracle-reviewer all-pass verdict; record the confirmation.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The skill-reviewer (>= 1 unbiased from-scratch) returns PASS; any surfaced fixes are applied and committed.
    - check-red-references exits 0 (GREEN) and check-hygiene exits 0 (ASCII + email + no-verbatim clean) after fixes.
    - Any reviewer fix to an owned surface was re-gated through the oracle-reviewer (all-pass) with a BLIND re-draft on revise.
    - The SEL-02 firewall, the AAA/GWT one-skeleton framing, and the should-primary/Osherove-alternative framing are confirmed correct by the reviewer.
    - No checker was weakened; the Phase 17/18 markers remain intact.
  </acceptance_criteria>
  <done>skill-reviewer (>= 1 unbiased from-scratch) PASS on the three RED references; fixes applied and any owned-surface change re-gated; completeness + hygiene GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Full-battery GREEN + claude plugin validate phase gate</name>
  <files>plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md, plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md, plugins/lz-tdd/skills/lz-red/references/naming.md</files>
  <read_first>
    - .planning/phases/16-source-distillation-core-red-references/16-VALIDATION.md (the full-suite command + sign-off checklist)
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs (the completeness gate to confirm GREEN)
    - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs (the hygiene gate to confirm GREEN)
  </read_first>
  <action>
    Run the full validation battery over the merged tree and confirm the phase gate. Run, in order: node .claude/skills/lz-red-workspace/extract-samples.mjs (tsc --strict --noEmit over every fence in the three refs); node .claude/skills/lz-red-workspace/tools/check-red-references.mjs (completeness GREEN for all three slices, deferral markers intact); node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs (ASCII + work-email allowlist + no-verbatim GREEN over the widened target set); claude plugin validate . (exit 0). If any gate is RED, fix the CONTENT (never weaken the gate) and re-run; an owned-surface fix re-enters the oracle-reviewer loop. Before the phase-gate commit, confirm git author and committer email = larsbrinknielsen@gmail.com (allowlist-inversion; the maintainer work-email/domain must be absent from every committed file and the commit identity). This task modifies files only if a final gate fix is required; otherwise it is a confirmation run.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/extract-samples.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - extract-samples.mjs exits 0: every TypeScript fence in the three references is tsc --strict --noEmit clean.
    - check-red-references.mjs exits 0: all three slices complete, each with >= 1 ts fence, no draft marker, and the Phase 17/18 deferral markers preserved.
    - check-hygiene.mjs exits 0: ASCII-only, only the approved gmail present, no verbatim-looking quoted run over the lz-red tree.
    - claude plugin validate . exits 0.
    - git author/committer email is the public gmail; the maintainer work-email/domain is absent everywhere.
  </acceptance_criteria>
  <done>Full battery GREEN (extract-samples tsc --strict, check-red-references, check-hygiene) + claude plugin validate . exit 0; commit identity = public gmail; phase gate confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .oracle/ prose -> executor main context | Any reviewer-driven re-draft of an owned surface goes back through the oracle / oracle-reviewer agents; the executor never Reads .oracle/. |
| author -> shipped lz-red tree | The final gate confirms only ASCII, own-words, work-email-free content shipped. |
| commit identity | The author/committer email must be the public gmail before the phase-gate commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-verbatim | Information Disclosure | the three filled references | high | mitigate | Final no-verbatim scan (>= 120-char quoted run) GREEN over the whole lz-red tree + confirmation that every owned surface passed the oracle-reviewer gate; skill-reviewer (>= 1 unbiased) catches residual near-verbatim canon; any re-draft is BLIND. |
| T-16-email | Information Disclosure | any committed file / commit identity | high | mitigate | check-hygiene allowlist-inversion (only larsbrinknielsen@gmail.com) + explicit git author/committer verification before the phase-gate commit; the work-email/domain is never written as a needle. |
| T-16-ascii | Tampering (data corruption) | shipped reference Markdown | medium | mitigate | check-hygiene axis (a) ASCII-only over the merged tree. |
| T-16-tsc | Tampering (integrity of guidance) | all Vitest example fences | medium | mitigate | extract-samples tsc --strict --noEmit over every fence at the gate. |
| T-16-scope | Scope integrity | the two co-edited refs | low | mitigate | check-red-references deferral guard confirms the Phase 17/18 markers survived any reviewer fixups. |
| T-16-gate | Integrity of verification | the checker battery | high | mitigate | Fix content, never weaken a gate; the finalize task re-runs the exact Wave-1 commands and claude plugin validate . -- no gate is edited to force GREEN. |

Block-on-high posture (ASVS L1, block on high): a verbatim leak, a non-allowlisted email, or a weakened gate is a HARD failure and blocks phase completion.
</threat_model>

<verification>
- node .claude/skills/lz-red-workspace/extract-samples.mjs -> exit 0.
- node .claude/skills/lz-red-workspace/tools/check-red-references.mjs -> exit 0 (GREEN, all three slices).
- node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs -> exit 0 (all three axes).
- claude plugin validate . -> exit 0.
- skill-reviewer PASS (>= 1 unbiased from-scratch); every owned surface oracle-reviewer all-pass.
- git author/committer email = larsbrinknielsen@gmail.com.
</verification>

<success_criteria>
The phase is closed cleanly: the full battery is GREEN (completeness, tsc --strict, hygiene), claude
plugin validate . exits 0, a skill-reviewer (with an unbiased from-scratch reviewer) has PASSed the
three RED references, every owned surface passed the oracle-reviewer gate, the no-verbatim scan is
clean, the deferred Phase 17/18 slices are untouched, and the commit identity is the public gmail.
</success_criteria>

## Artifacts this plan produces

- No new files. This plan confirms the phase gate and applies only reviewer-driven fixes (if any) to the three already-authored references:
  - `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md`
  - `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md`
  - `plugins/lz-tdd/skills/lz-red/references/naming.md`

<output>
Create `.planning/phases/16-source-distillation-core-red-references/16-03-SUMMARY.md` when done.
</output>
