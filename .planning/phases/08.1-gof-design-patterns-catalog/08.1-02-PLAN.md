---
phase: 08.1-gof-design-patterns-catalog
plan: 02
type: execute
wave: 2
depends_on: [08.1-01]
branching_strategy: none
tags: [gof-catalog, creational, design-patterns, skill-authoring, oracle-clean-room, typescript, de-patterning]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/abstract-factory.md
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/builder.md
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/factory-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/prototype.md
  - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md
autonomous: false
requirements: [GOF-01, GOF-02, GOF-03, GOF-04]
must_haves:
  truths:
    - "D-01: the 5 GoF Creational patterns (Abstract Factory, Builder, Factory Method, Prototype, Singleton) are authored as per-pattern leaf files gof-catalog/<slug>.md behind the thin index, one file per pattern, filename == kebab-slug of the H1; the README rows + SKILL.md are NOT touched here"
    - "D-04: each leaf uses the locked 5-section contract -- single # <Pattern> H1 (optional leading Also known as:), ## Intent, ## Applicability (FIRST LINE = the one-sentence index selector), ## Consequences, ## Example (ONE strict-TS fence whose identifiers/comments name the participants + wire their collaboration), ## Related patterns; NO synthesized Avoid when, NO invented Watch for, NO Use when: line"
    - "D-06: modern-status caveats are folded INTO ## Consequences (not a new section) for Singleton (design smell; prefer DI) and Factory Method (Gamma would generalize to Factory); Singleton's ## Consequences MUST cite Dependency Injection (Fowler DI/IoC article) as the preferred alternative"
    - "D-07: Consequences is present + populated; a liability is stated ONLY where the source states one -- Builder and Singleton are among the 8 benefits-only patterns and carry benefits faithfully (Singleton + its modern caveat); no invented liabilities"
    - "D-09: the Singleton leaf's ## Related patterns carries the required Away back-link to the Kerievsky de-patterning refactoring -- [Inline Singleton](../kerievsky-catalog/inline-singleton.md#inline-singleton) -- so check-gof's REQUIRED_AWAY presence gate passes for Singleton"
    - "D-12: all 5 leaves are sourced/verified via the clean-room oracle loop against .oracle/design-patterns/ (index.md + Ch.3 Creational, by number/topic only), authored BLIND with distinct example domains, deterministic layer run FIRST, oracle-reviewer gate, revised BLIND to convergence (~3-round cap, escalate on non-convergence); the main context NEVER reads .oracle/ prose (ls for names only)"
    - "D-13: this plan runs INLINE in the MAIN context (Agent + AskUserQuestion tools), NOT via gsd-executor, NOT in a worktree (branching_strategy: none); the oracle agents are read-only so there is no file conflict"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md"
      provides: "Singleton leaf: 5-section contract; Consequences cites Dependency Injection (D-06); ## Related patterns Away link to Inline Singleton (D-09)"
      contains: "Dependency Injection"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/factory-method.md"
      provides: "Factory Method leaf: 5-section contract; Consequences folds the Gamma would-generalize-to-Factory caveat (D-06)"
      contains: "## Consequences"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md"
      via: "## Related patterns Away back-link (D-09 required-3)"
      pattern: "kerievsky-catalog/inline-singleton\\.md#"
---

<objective>
Author the 5 GoF Creational patterns -- Abstract Factory, Builder, Factory Method, Prototype,
Singleton -- as per-pattern catalog leaves on the locked 5-section contract, via the clean-room
blind-author -> deterministic-checks -> oracle-reviewer-gate -> revise-BLIND -> converge loop.
Includes the Singleton modern-status caveat (cite Dependency Injection, D-06), the Factory Method
caveat, and the Singleton -> Inline Singleton Away back-link (D-09).

Purpose: fill GOF-01/02 for the Creational family (5 of 23) and land the first of the 3 required
Away links. File-disjoint by owning only these 5 leaf files.

Output: the 5 Creational leaf files under references/gof-catalog/ (the README rows + SKILL.md are
finalized in Wave 3, 08.1-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md
@.planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-LEARNINGS.md
@plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md

<interfaces>
CLEAN-ROOM FIREWALL (D-12): the main context NEVER opens any file under .oracle/ -- `ls` for names
only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` gates drafted leaves. Input: DRAFT_PATHS, SOURCE = ".oracle/design-patterns/index.md"
  + the chapter topic "Ch.3 -- Creational" (index.md + chapter number/topic, NEVER a book chapter
  filename), CONTENT_TYPE "pattern-leaf", SCOPE = each leaf's pattern, the per-axis anchors adapted for
  the pattern-catalog content type (intent / applicability / consequences / example / related patterns +
  full-strength DST-04 near-verbatim). Returns pass|revise|blocked|error.
- `oracle` for open-ended chapter-membership / participant-vocabulary lookups.
Sub-batch all 5 in one reviewer call (within the ~6-8 guidance) with per-leaf SCOPE; cap ~3 rounds;
escalate non-convergence to Task 2. Fan-out guard: do NOT persist a full ordered name+chapter map.

The 5 slugs (filename == slugFor(H1)): abstract-factory, builder, factory-method, prototype, singleton.

The 5-section leaf contract (D-04) -- see 08.1-PATTERNS.md leaf section for the exact skeleton:
  # <Pattern>            (single H1; optional leading "Also known as: <alias>")
  ## Intent             (what it does + the problem it targets)
  ## Applicability      (FIRST LINE = one-sentence index selector, mirrored verbatim into the Wave-3 README row)
  ## Consequences       (benefits AND liabilities where sourced; modern caveat folded here, D-06)
  ## Example            (ONE ```ts fence; identifiers/comments name the participants + wire collaboration)
  ## Related patterns   (cross-links + distinctions)

Modern caveats to fold into ## Consequences (D-06): Singleton -> a design smell; prefer Dependency
Injection (cite Fowler's DI/IoC article as the preferred alternative -- the deterministic Singleton-DI
assist in check-gof asserts /dependency injection/i in Singleton's Consequences). Factory Method ->
Gamma would generalize it to a broader "Factory". Benefits-only (D-07): Builder and Singleton carry
benefits faithfully (+ Singleton's caveat); do NOT invent a liability for them.

Away link (D-09): the Singleton leaf's ## Related patterns MUST include the exact link
[Inline Singleton](../kerievsky-catalog/inline-singleton.md#inline-singleton).

gof<->gof ## Related patterns links between sibling Creational leaves (and to Structural/Behavioral
leaves authored by parallel Wave-2 plans) will NOT resolve until Wave 3 -- so do NOT gate this plan on
check-gof 23/23 or check-crossrefs; the per-plan gate is the per-leaf contract (rg 5 sections) +
extract-samples (tsc) + check-hygiene + oracle-reviewer pass. (RESEARCH: check-crossrefs is RED-by-design
during Wave 2, GREEN at the Wave-3 gate.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 GoF Creational leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/abstract-factory.md, plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/builder.md, plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/factory-method.md, plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/prototype.md, plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- leaf + thin-index model the GoF leaves mirror with the D-04 5-section contract substituted)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Canonical per-axis anchors", "Driver responsibilities" -- pass tight per-leaf SCOPE)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-LEARNINGS.md (blind drafts fold sub-steps; too_close_to_source re-domain; deterministic layer before reviewer calls; a resolvable link can still be the wrong sibling; Ch.5/Ch.4 chapter truncation risk)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-01, D-04, D-06, D-07, D-09, D-12, D-13)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-PATTERNS.md (the D-04 5-section leaf skeleton + the gof leaf file mechanics)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-RESEARCH.md (Oracle-Loop Application S5 + the per-axis rubric anchors for the pattern-catalog content type; Pitfall 3 blind draft converges on GoF's canonical example)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/inline-singleton.md (the Away back-link target -- confirm its H1 anchor #inline-singleton)
    - .oracle/ (ls ONLY for filenames -- NEVER open any file under .oracle/ from this context)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not). Do NOT
    open any file under .oracle/ from this context. Run this loop INLINE in the main context (D-13) --
    NOT via gsd-executor, NOT in a worktree.

    Author the 5 Creational leaves (one file per pattern, kebab-slug == filename) via the clean-room
    loop:
    (1) AUTHOR BLIND in the LOCKED 5-section contract: single # <Pattern> H1 (optional leading
    "Also known as:"), ## Intent, ## Applicability (make the FIRST LINE a standalone one-sentence
    selector -- it is mirrored verbatim into the Wave-3 README row), ## Consequences, ## Example (ONE
    tsc --strict-clean ```ts fence, pure -- no DOM/console; identifiers + comments name the pattern's
    participants and wire their collaboration; choose a DELIBERATELY DISTINCT domain, NOT the book's
    maze/MapSite/document-editor illustrations -- Pitfall 3), ## Related patterns (cross-links +
    distinctions). Fold the modern caveats into ## Consequences per D-06: Singleton -> a design smell,
    prefer Dependency Injection (cite Fowler's DI/IoC article by name); Factory Method -> Gamma would
    generalize it to a broader Factory. Keep Builder + Singleton benefits-faithful (D-07 -- do NOT
    invent a liability). Add the Singleton ## Related patterns Away link
    [Inline Singleton](../kerievsky-catalog/inline-singleton.md#inline-singleton) (D-09). ASCII only;
    no verbatim GoF prose or code.
    (2) DETERMINISTIC LAYER FIRST (D-12 -- run the cheap checks BEFORE spending reviewer calls): run
    extract-samples (tsc --strict) + check-hygiene + the per-leaf section rg checks over the 5 leaves;
    for the Singleton leaf also confirm /dependency injection/i in its Consequences and the Inline
    Singleton Away link. Fix any compile / ASCII / missing-section / missing-caveat / malformed-link
    defect BLIND. Only once this layer is green do you spend an oracle-reviewer call. (Do NOT gate on
    check-gof 23/23 or check-crossrefs -- both are RED-by-design mid-Wave-2.)
    (3) GATE: spawn `oracle-reviewer` with DRAFT_PATHS, SOURCE ".oracle/design-patterns/index.md" +
    "Ch.3 -- Creational", CONTENT_TYPE "pattern-leaf", per-leaf SCOPE, the pattern-catalog per-axis
    anchors (intent / applicability / consequences [the D-06/D-07 policing axis] / example / related
    patterns + near-verbatim). Use `oracle` for open-ended lookups.
    (4) REVISE BLIND from the directives; on too_close_to_source, fully re-domain the example blind and
    re-gate.
    (5) ITERATE to convergence (every leaf `pass`, or owner-accepted `blocked`); cap rounds ~3 and
    escalate to Task 2. Fan-out guard.
    (6) RE-GATE post-oracle deterministic fixes: after applying directives, re-run the deterministic
    layer; if a fix CHANGES any leaf's ## Example code, re-run oracle-reviewer on that leaf so no
    example ships un-re-reviewed for behavior/near-verbatim. Commit on green. Do NOT edit any README,
    SKILL.md, or checker .mjs.
  </action>
  <acceptance_criteria>
    - All 5 leaf files exist; each has a single `# <Pattern>` H1 and the 5 sections (`rg -q "^## Intent" <leaf> && rg -q "^## Applicability" <leaf> && rg -q "^## Consequences" <leaf> && rg -q "^## Example" <leaf> && rg -q "^## Related patterns" <leaf>` per leaf) and >=1 ts fence.
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` exits 0 (every Creational ## Example is tsc --strict-clean).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII + email + no-verbatim over references/**).
    - Singleton's Consequences cites DI and carries the Away link: `rg -qi "dependency injection" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md && rg -q "inline-singleton.md#inline-singleton" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md`.
    - Each leaf reached oracle-reviewer `pass` (or owner-accepted `blocked` via Task 2), recorded in the SUMMARY; no file under .oracle/ was opened by this context; no README/SKILL.md/checker edited.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -qi "dependency injection" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md && rg -q "inline-singleton.md#inline-singleton" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md && for f in abstract-factory builder factory-method prototype singleton; do rg -q "^## Intent" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/$f.md && rg -q "^## Related patterns" plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/$f.md || exit 1; done</automated>
  </verify>
  <done>All 5 Creational leaves exist as contract-complete files on the D-04 5-section contract; Singleton's Consequences cites Dependency Injection and its Related patterns carries the Inline Singleton Away link; Factory Method folds the generalize-to-Factory caveat; every ## Example is tsc --strict-clean and behavior/near-verbatim-clean; each leaf is oracle-reviewer pass (or owner-accepted blocked); no README/SKILL.md/checker touched; no .oracle/ file opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner escalation for non-converging / blocked Creational leaves (conditional)</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Driver responsibilities" -- loop bound + oscillation guard + owner escalation)
    - .planning/phases/08.1-gof-design-patterns-catalog/08.1-CONTEXT.md (D-12 escalation path; AskUserQuestion survives only for reviewer escalations)
  </read_first>
  <decision>How to resolve any Creational leaf the oracle-reviewer loop cannot converge within ~3 rounds, or that returns as a blocked ambiguity?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate. The driver caps rounds (~3) and escalates that ENTRY to the owner via AskUserQuestion.
    Fires ONLY for entries that do not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked leaf as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (intent / applicability / consequences caveat / example participants), then re-run the loop once</name>
      <pros>Resolves a genuine fidelity gap the reviewer could not phrase without leaking source prose</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked): open an
    AskUserQuestion with the entry, the reviewer's ambiguities/unable-to-verify axes (own words), and
    the two options; apply the choice; re-run the deterministic layer; record it in the SUMMARY. Do
    NOT reveal or request verbatim source prose.
  </action>
  <acceptance_criteria>
    - Every Creational leaf is oracle-reviewer `pass` or owner-adjudicated; each escalation (or a no-op note if none) is recorded in 08.1-02-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every Creational leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/design-patterns/` (owner full-text GoF book, git-ignored) -> authored public-repo leaves | copyrighted GoF expression + code must never cross; the ONLY crossing is via the oracle/oracle-reviewer subagents (isolated context, own-words verdicts, no prose/code/paths) |
| authored leaves -> public GitHub repo | world-readable (IP + hygiene surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Information Disclosure / legal (DST-04) | authored leaf prose + ## Example code | mitigate | Clean-room firewall: main context NEVER reads .oracle/; authored BLIND with distinct example domains; oracle-reviewer full-strength near-verbatim gate; check-hygiene no-verbatim WARN + Phase-10 scan |
| T-08.1-04 | Tampering (fabricated / unfaithful content) | intent / applicability / consequences (incl. the D-06 caveats + D-07 no-invented-liability) / example / related-patterns fidelity | mitigate | oracle-reviewer fidelity gate (consequences axis polices D-06/D-07) + converge loop; driver caps rounds (~3) and escalates to owner (Task 2) |
| T-08.1-06 | Tampering | a resolvable-but-wrong ## Related patterns sibling / a wrong Away link | mitigate | oracle-reviewer related-patterns aptness axis + check-gof REQUIRED_AWAY presence + check-crossrefs resolution at the Wave-3 gate |
| T-08.1-05 | Information Disclosure / firewall | main context accidentally reading .oracle/ prose (incl. fan-out aggregation) | mitigate | firewall discipline (ls only); INLINE main-context orchestration; driver dedupes/caps, does NOT persist a full ordered name+chapter map |
| T-08.1-02 | Information Disclosure | work email in a committed leaf | mitigate | check-hygiene work-email allowlist hard-fail; public contact larsbrinknielsen@gmail.com only |
| T-08.1-03 | Tampering (integrity) | non-ASCII / mojibake in a leaf | mitigate | check-hygiene ASCII hard-fail; ASCII-only authoring |

No UI, no network, no secrets, no new packages. Nothing rises to the block_on: high bar.
</threat_model>

<verification>
- extract-samples (tsc --strict) GREEN over all 5 Creational examples; check-hygiene GREEN; each leaf has the D-04 5-section contract.
- Singleton's Consequences cites Dependency Injection and its Related patterns carries the Inline Singleton Away link; Factory Method folds the generalize-to-Factory caveat.
- Every Creational leaf reached oracle-reviewer pass (or owner-accepted blocked); README rows + SKILL.md + checkers untouched; no .oracle/ file opened by this context.
</verification>

<success_criteria>
- All 5 GoF Creational patterns are contract-complete, oracle-converged, tsc --strict-clean leaves (GOF-01), with the Singleton DI caveat + the Factory Method caveat folded into Consequences (GOF-02) and the Singleton -> Inline Singleton Away back-link present (GOF-03 required-3, 1 of 3).
- The plan owns only these 5 leaf files (file-disjoint from sibling Wave-2 plans); gof<->gof cross-refs + README rows resolve at the Wave-3 gate.
</success_criteria>

<output>
Create `.planning/phases/08.1-gof-design-patterns-catalog/08.1-02-SUMMARY.md` when done, recording the confirmed Creational membership + any oracle name/slug corrections vs the locked names (for the check-gof NAMES reconciliation, if any), per-leaf round counts, the settled Consequences caveats, and any owner escalations.
</output>
