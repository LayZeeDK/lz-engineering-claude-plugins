# Phase 8.1: GoF Design Patterns Catalog - Pattern Map

**Mapped:** 2026-07-07
**Files analyzed:** 33 (2 catalog READMEs + 28 leaves + 2 new checkers + 5 harness/skill edits) collapsed to 11 distinct pattern targets
**Analogs found:** 11 / 11 (every target is a near-exact clone or self-edit of shipped Phase-7/8 code)

This phase is a structural clone of shipped Phases 7 (Fowler) and 8 (Kerievsky). Every analog
lives in this repo and is GREEN today. The planner should point executors at the exact analog
file + line range below and name the single delta (leaf field set, or an allow-list widen), not
re-derive structure.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/README.md` | doc index | static content | `references/kerievsky-catalog/README.md` | exact (family-grouped table) |
| `references/gof-catalog/<slug>.md` (x23) | doc leaf | static content | kerievsky leaf shape + GoF 5-section contract (D-04) | role-match (leaf sections differ) |
| `references/extra-patterns-catalog/README.md` | doc index | static content | `references/kerievsky-catalog/README.md` | exact (thin, no family split) |
| `references/extra-patterns-catalog/<slug>.md` (x5) | doc leaf | static content | same as gof leaf | role-match |
| `.claude/skills/lz-refactor-workspace/tools/check-gof.mjs` | checker/utility | batch file-I/O + validate | `tools/check-catalog.mjs` | exact (clone, swap field set) |
| `.claude/skills/lz-refactor-workspace/tools/check-extra-patterns.mjs` | checker/utility | batch file-I/O + validate | `tools/check-catalog.mjs` | exact (small clone) |
| `tools/check-kerievsky.mjs` (MODIFY) | checker/utility | batch file-I/O + validate | itself (lines 272-277) | self-edit (replace presence-only branch) |
| `tools/check-crossrefs.mjs` (MODIFY) | checker/utility | batch file-I/O + validate | itself (lines 26-30, 102) | self-edit (widen source set) |
| `extract-samples.mjs` (MODIFY) | compile harness | batch transform + spawn tsc | itself (lines 21-24) | self-edit (append CATALOGS) |
| `package.json` (MODIFY) | config | n/a | itself (line 8) | self-edit (insert 2 commands) |
| `plugins/lz-tdd/skills/lz-refactor/SKILL.md` (MODIFY) | doc router | static content | itself (lines 60-64) | self-edit (append 1 pointer section) |

## Pattern Assignments

### `tools/check-gof.mjs` (checker, batch file-I/O + validate) -- CLONE `check-catalog.mjs`

**Analog:** `.claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` (337 lines, the clone target).

**Transfer VERBATIM (do not rewrite):** the whole skeleton -- imports + `here`/`repoRoot`
resolution (lines 20-26), `report` (110-116), `isDir` (118-124), `slugFor` (127), `walkMd`
(129-147), `collectLeaves` (152-180 -- includes the single-H1 assertion + `[tag]` strip),
`readmeRowsBySlug` (184-208), the `byName` build (210-223), the per-name present/dup loop head
(234-249), the "unknown leaf heading" sweep (321-327), `SCAFFOLD_RES` (106), and the exit-code
contract (331-337). Only `CATALOG` (line 27) repoints to `gof-catalog`.

The repoRoot resolution comment is load-bearing (4 levels up from `tools/`):
```
const here = path.dirname(fileURLToPath(import.meta.url));
// tools -> lz-refactor-workspace -> skills -> .claude -> repo root
const repoRoot = path.resolve(here, "..", "..", "..", "..");
```

**Delta 1 -- NAMES grouped by family** (replaces the flat 62-name array at lines 32-95). Model on
`check-kerievsky.mjs` NAMES (lines 42-76) which already groups by chapter with comment headers.
Define 3 arrays + flat union + a `FAMILY_OF` map (D-10):
- `CREATIONAL` (5): Abstract Factory, Builder, Factory Method, Prototype, Singleton
- `STRUCTURAL` (7): Adapter, Bridge, Composite, Decorator, Facade, Flyweight, Proxy
- `BEHAVIORAL` (11): Chain of Responsibility, Command, Interpreter, Iterator, Mediator, Memento, Observer, State, Strategy, Template Method, Visitor
- `NAMES = [...CREATIONAL, ...STRUCTURAL, ...BEHAVIORAL]`; `EXPECTED = 23`.

**Delta 2 -- 5-section contract** (replaces the Fowler `Use when:`/Motivation/Mechanics block at
lines 256-274). New per-leaf checks:
```
if (!/^##\s+Intent\s*$/m.test(leaf.text)) missing.push("## Intent");
if (!/^##\s+Applicability\s*$/m.test(leaf.text)) missing.push("## Applicability");
if (!/^##\s+Consequences\s*$/m.test(leaf.text)) missing.push("## Consequences");
if (!/^##\s+Example\b/m.test(leaf.text)) missing.push("## Example");
if (!/^##\s+Related patterns\s*$/m.test(leaf.text)) missing.push("## Related patterns");
if (!/```(ts|typescript)\b/.test(leaf.text)) missing.push("ts fence");
```
DELETE the Fowler provenance logic entirely (WEB_ONLY/WEB_EXAMPLE sets lines 101-102, checks
276-294). `Also known as:` is OPTIONAL -- do NOT assert it.

**Delta 3 -- Applicability-first-line selector mirror** (replaces the `Use when:` mirror at lines
303-315). The analog extracts `Use when:` then requires it verbatim in a README row; do the same
but source the selector from the Applicability first line:
```
const app = leaf.text.match(/^##\s+Applicability\s*$\r?\n+([^\r\n]+)/m);
const selector = app ? app[1].trim() : null;
// then: rows.get(expectedSlug) must contain a line that .includes(selector)  (RED until Wave 3)
```
`readmeRowsBySlug`'s regex `/\]\(([a-z0-9][a-z0-9-]*)\.md\)/g` (line 191) matches BARE `](slug.md)`
links only -- README rows MUST link with no `#anchor`, same as Fowler/Kerievsky.

**Delta 4 -- Consequences present + populated** (D-07). Presence is covered by Delta 2; ADD a
non-empty-body check (at least one non-blank line between `## Consequences` and the next `## `). Do
NOT assert a liability exists (8 benefits-only patterns would false-fail). "liability only where
sourced" is SEMANTIC -> oracle-reviewer, not the checker.

**Delta 5 -- Singleton-cites-DI assist** (recommended, A1/Open-Q2, confirm at plan review). For the
Singleton leaf assert `/dependency injection/i` appears in its Consequences body.

**Delta 6 -- Related -> Away required-3** (D-09). Model on `check-kerievsky.mjs`'s `AWAY` set
(lines 82-86). PRESENCE only (resolution is check-crossrefs's job):
```
const REQUIRED_AWAY = new Map([
  ["Singleton",  "../kerievsky-catalog/inline-singleton.md#inline-singleton"],
  ["Composite",  "../kerievsky-catalog/encapsulate-composite-with-builder.md#encapsulate-composite-with-builder"],
  ["Iterator",   "../kerievsky-catalog/move-accumulation-to-visitor.md#move-accumulation-to-visitor"],
]);
// for those 3 names: assert the exact link string appears in leaf.text
```

**Delta 7 -- README family grouping** (D-05). Minimum: assert the 3 family headings
(`Creational`/`Structural`/`Behavioral`) exist in the README. Stronger (recommended, A3): section-scan
tracking the current family heading and assert each present leaf's row falls under its `FAMILY_OF`
heading -- model on `check-smells.mjs` context tracking if adopted.

**SUMMARY label:** `GOF-01 GREEN -- N/23 patterns present with 5-section contract + family index`
(analog line 332).

---

### `tools/check-extra-patterns.mjs` (checker) -- small clone of the same

Same skeleton as `check-gof.mjs`. Changes: `CATALOG = references/extra-patterns-catalog`;
`NAMES = ["Null Object","Factory","Creation Method","Composed Method","Collecting Parameter"]`;
`EXPECTED = 5`; SAME 5-section contract + Consequences present+populated + Applicability-first-line
mirror. NO family grouping (flat 5-row index). NO `REQUIRED_AWAY` (Away links are gof-only). Assert
name IDENTITY not cardinality (D-10). SUMMARY label: `XTR-01 GREEN -- N/5`.

---

### `references/gof-catalog/README.md` + `extra-patterns-catalog/README.md` (doc index)

**Analog:** `references/kerievsky-catalog/README.md` (79 lines).

**Copy the structure:** H1 title + "index" suffix; a Scope intro paragraph noting it is a THIN index
(entry content lives in leaves, never inlined); family/chapter H2 section headers; one Markdown table
per group; a `## Sources` footer. The Kerievsky README groups by 6 book chapters
(`## Ch.6 -- Creation` etc., lines 14, 25, 36, 48, 56, 63); GoF groups by the 3 families
(`## Creational` / `## Structural` / `## Behavioral`, D-05).

**Row shape to mirror** (Kerievsky lines 16-23):
```
| Refactoring | Direction | GoF pattern | Use when |
|---|---|---|---|
| [Inline Singleton](inline-singleton.md) | Away | Singleton | a class is a Singleton, but ... |
```
GoF drops the Direction/GoF-pattern columns; the selector column = the leaf's Applicability first
line mirrored verbatim (D-05). The link target MUST be a BARE `[Name](slug.md)` (no `#anchor`) so
`readmeRowsBySlug` matches it.

**CRITICAL (Pitfall 4 / A2):** Wave-1 READMEs carry family headings + intro ONLY -- NO leaf-linking
rows. Rows link to not-yet-authored leaves would false-RED check-crossrefs. Rows land in Wave 3
(D-05 "Wave 3 finalize"), same as Fowler/Kerievsky.

---

### `references/gof-catalog/<slug>.md` (x23) + `extra-patterns-catalog/<slug>.md` (x5) (doc leaf)

**Analog for FILE MECHANICS:** any kerievsky leaf, e.g. `kerievsky-catalog/inline-singleton.md`
(75 lines). **Analog for SECTION SHAPE:** the LOCKED GoF 5-section contract in CONTEXT D-04 (NOT the
Kerievsky sections -- those differ).

**What transfers from the Kerievsky leaf:**
- Single `# <Pattern Name>` H1 = the identity the checker keys on; filename = `slugFor(H1)`
  (e.g. `# Inline Singleton` -> `inline-singleton.md`). "Chain of Responsibility" ->
  `chain-of-responsibility.md`, "Factory Method" -> `factory-method.md`.
- `## Example` carries fenced ```ts blocks (inline-singleton lines 34-51, 55-67) that
  `extract-samples.mjs` compiles `tsc --strict`. Use distinct example domains per leaf to avoid the
  `too_close_to_source` DST-04 flag (Pitfall 3).
- Cross-links use `[Name](../<catalog>/<slug>.md#<slug>)` H1-anchor form (inline-singleton line 7).

**What is NEW (D-04 5-section contract, replaces the Kerievsky
`Use when:`/Direction/GoF pattern/Composed Fowler primitive/Motivation/Mechanics/Watch-for shape):**
```
# <Pattern Name>
Also known as: <alias>        (OPTIONAL leading line)

## Intent
<what it does + the problem it targets>

## Applicability
<FIRST LINE = the one-sentence index selector, mirrored verbatim into the README row>
<positive situations + symptoms>

## Consequences
<benefits AND liabilities where the source states them; modern caveat folded in here (D-06)>

## Example
```ts
// ONE strict-TS fence; identifiers/comments name the participants + wire their collaboration
```

## Related patterns
<cross-links + distinctions; the 3 Away leaves add a Kerievsky back-link (D-09)>
```
NO synthesized `Avoid when`, NO invented `Watch for`, NO `Use when:` selector line (D-04).

---

## Shared Patterns

### Repo-root path resolution (every checker)
**Source:** `check-catalog.mjs` lines 24-27 (`tools/*` = 4 levels up); `extract-samples.mjs` lines
15-17 (workspace root = 3 levels up -- it lives one level higher than `tools/`).
**Apply to:** `check-gof.mjs`, `check-extra-patterns.mjs` copy the `tools/*` 4-level form verbatim.

### Name-identity contract (every catalog checker)
**Source:** `check-catalog.mjs` lines 210-327 (build `byName`, loop `NAMES` asserting exactly-one
leaf, then sweep for unknown headings).
**Apply to:** both new checkers. Each canonical name must appear EXACTLY once as a leaf H1
(identity, not cardinality); filename == `slugFor(name)`; single H1; no unknown/typo heading; no
`SCAFFOLD_RES` phrase. This is the "assert name IDENTITY, not cardinality" clause of D-10.

### README-row selector mirror (every catalog checker + README)
**Source:** `check-catalog.mjs` `readmeRowsBySlug` (184-208) + the mirror check (303-315); regex
`/\]\(([a-z0-9][a-z0-9-]*)\.md\)/g` matches BARE `.md` links only.
**Apply to:** GoF/extra READMEs must link rows with no `#anchor`; the mirrored text is the
Applicability first line (not `Use when:`).

### Exit-code + SUMMARY contract (every checker)
**Source:** `check-catalog.mjs` lines 331-337 -- `failures === 0` -> print `... GREEN ...`, exit 0;
else print `... RED ...`, exit 1. `package.json` `check` chain relies on this (`&&` short-circuits).
**Apply to:** both new checkers keep the same shape so a RED check-gof halts the chain.

### Away-set membership (gof required-3 + kerievsky de-patterning)
**Source:** `check-kerievsky.mjs` `AWAY` Set (82-86) + its use (286-294).
**Apply to:** `check-gof.mjs` `REQUIRED_AWAY` map (presence-only assertion of the exact Kerievsky
back-link string).

### DST-04 clean-room firewall (all content authoring)
**Source:** enforced by `check-hygiene.mjs` (unchanged; walks `references/**`) + the oracle loop.
**Apply to:** all 28 leaves + 2 READMEs. ASCII-only, no verbatim GoF/Kerievsky prose or code, public
contact only. Main context NEVER reads `.oracle/` prose (only `ls` for names).

## Pattern Assignments (MODIFY targets -- exact excerpts of what changes)

### `tools/check-kerievsky.mjs` -- replace the presence-only `GoF pattern:` branch
**Current code to replace (lines 272-277):**
```
  // GoF pattern: name-only vocabulary (KRV-04). A non-empty value satisfies it; the three Utilities
  // (Chain Constructors, Unify Interfaces, Extract Parameter) may carry the literal "n/a -- utility"
  // (Open Question 1) -- that is a non-empty value, so the same presence check accepts it.
  if (!/^GoF pattern:\s*\S/m.test(leaf.text)) {
    missing.push("GoF pattern: field");
  }
```
**Replace with the link-parse + set-membership linkify branch (D-08, D-11).** Duplicate the GoF-23
(23) and extra-5 (5) name arrays into this file (or a shared token map). For each leaf's
`GoF pattern:` line: parse the first `[text](../<catalog>/<slug>.md#<anchor>)` link if present
(name = link text) else the leading token before ` (` (name = plain token). Then:
- name in GoF-23 set -> REQUIRE link to `../gof-catalog/<slugFor(name)>.md#<slugFor(name)>`;
- name in extra-5 set -> REQUIRE link to `../extra-patterns-catalog/<slugFor(name)>.md#<slugFor(name)>`;
- name in NEITHER set -> REQUIRE NO gof/extra link (free text).

CRITICAL (Pitfall 1): exact-name set lookup, NEVER substring. `Factory` (extra) must map to
`extra-patterns-catalog/factory.md`, NOT `Factory Method` (`gof-catalog/factory-method.md`).
The `slugFor` helper (line 111) is already in this file. This self-produces the Wave-1 RED baseline
(23 un-linkified tokens FAIL; 4 free-text sentinels PASS) -- EXPECTED, not a regression.

**Token census (all 27 leaves verified via `git grep "^GoF pattern:"`):** 17 GoF-23 links + 6 extra
links = 23 leaves require a link; 4 stay free text (chain-constructors, unify-interfaces,
extract-parameter = `n/a -- utility`; replace-type-code-with-class = `Class / type-safe value`).
Live example of a token that gets linkified -- `kerievsky-catalog/inline-singleton.md` line 6 today:
```
GoF pattern: Singleton
```
becomes `GoF pattern: [Singleton](../gof-catalog/singleton.md#singleton)` in Wave 3.

### `tools/check-crossrefs.mjs` -- widen the source set
**Current source-set declaration (lines 26-30 + 102):**
```
const REFERENCES = path.join(repoRoot, "plugins", "lz-tdd", "skills", "lz-refactor", "references");
const CATALOG = path.join(REFERENCES, "fowler-catalog");
const KERIEVSKY = path.join(REFERENCES, "kerievsky-catalog");
const SMELLS_DIR = path.join(REFERENCES, "smells");
...
const sourceFiles = [...collectLeafFiles(CATALOG), ...collectLeafFiles(KERIEVSKY), ...collectLeafFiles(SMELLS_DIR)];
```
**Add two dirs and include them** (mirroring the `KERIEVSKY` line):
```
const GOF   = path.join(REFERENCES, "gof-catalog");
const EXTRA = path.join(REFERENCES, "extra-patterns-catalog");
const sourceFiles = [...collectLeafFiles(CATALOG), ...collectLeafFiles(KERIEVSKY),
                     ...collectLeafFiles(GOF), ...collectLeafFiles(EXTRA),
                     ...collectLeafFiles(SMELLS_DIR)];
```
The `inverse-of` mutuality guard stays `CATALOG` (Fowler)-scoped (line 180), so one-directional
Kerievsky<->gof links are correctly excluded -- NO change there.

### `extract-samples.mjs` -- append 2 CATALOGS entries
**Current `CATALOGS` array (lines 21-24):**
```
const CATALOGS = [
  { dir: path.join(REFERENCES, "fowler-catalog"), prefix: "fowler" },
  { dir: path.join(REFERENCES, "kerievsky-catalog"), prefix: "kerievsky" },
];
```
**Append:**
```
  { dir: path.join(REFERENCES, "gof-catalog"),            prefix: "gof" },
  { dir: path.join(REFERENCES, "extra-patterns-catalog"), prefix: "extra" },
```
The per-catalog `prefix` prevents sample-module collisions (each fence -> `<prefix>-<leaf>-<n>.ts`,
line 131), so a gof `factory-method` fence and an extra `factory` fence never overwrite each other.
Empty-catalog GREEN guard (141-146) and `export {}` module-scoping (126) transfer as-is. (SUMMARY
label `FWL-04` is cosmetic; relabel optional.)

### `package.json` -- insert 2 commands after check-kerievsky
**Current `check` chain (line 8):**
```
"check": "node tools/check-catalog.mjs && node tools/check-kerievsky.mjs && node tools/check-smells.mjs && node tools/check-crossrefs.mjs && node tools/check-principles.mjs && node tools/check-hygiene.mjs"
```
**Insert `check-gof` + `check-extra-patterns` AFTER `check-kerievsky.mjs` (D-11):**
```
"check": "node tools/check-catalog.mjs && node tools/check-kerievsky.mjs && node tools/check-gof.mjs && node tools/check-extra-patterns.mjs && node tools/check-smells.mjs && node tools/check-crossrefs.mjs && node tools/check-principles.mjs && node tools/check-hygiene.mjs"
```

### `plugins/lz-tdd/skills/lz-refactor/SKILL.md` -- append 1 pointer section
**Existing Kerievsky pointer section to mirror (lines 60-64):**
```
## Kerievsky pattern-directed refactorings

When routing a structural / pattern-directed smell or looking up a named Kerievsky refactoring
(including de-patterning), consult the catalog index:
[references/kerievsky-catalog/README.md](references/kerievsky-catalog/README.md)
```
**Add ONE lean section IMMEDIATELY AFTER it** (D-14) pointing to BOTH new READMEs
(`references/gof-catalog/README.md` = GoF-23 targets; `references/extra-patterns-catalog/README.md`
= non-GoF targets). Do NOT touch the Phase-9 coach placeholder (lines 38-43).

## No Analog Found

None. Every target is a clone of `check-catalog.mjs`, a self-edit of an existing harness/skill file,
or a leaf/index following the shipped Kerievsky catalog shape with the D-04 5-section contract
substituted. The only genuinely NEW logic is (a) the 5-section field set + Applicability-first-line
mirror in `check-gof`, and (b) the link-parse + set-membership linkify branch in `check-kerievsky` --
both specified concretely above.

## Metadata

**Analog search scope:** `.claude/skills/lz-refactor-workspace/` (harness), `.claude/skills/lz-refactor-workspace/tools/` (checkers), `plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/` (leaf/index shape), `plugins/lz-tdd/skills/lz-refactor/SKILL.md` (router).
**Files scanned:** check-catalog.mjs, check-kerievsky.mjs, check-crossrefs.mjs, extract-samples.mjs, package.json, kerievsky-catalog/README.md, kerievsky-catalog/inline-singleton.md, SKILL.md; `.oracle/` names via `ls` only (firewall-safe, no prose read).
**Pattern extraction date:** 2026-07-07
