---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/encapsulation.md
autonomous: false
requirements: [FWL-01, FWL-04]
must_haves:
  truths:
    - "D-02: each of the 21 entries in this batch fills the per-entry contract exactly -- 2nd-ed name (+ 1st-ed alias), distilled motivation, distilled mechanics, a minimal focused TypeScript before->after example, and a provenance label"
    - "D-06: Replace Magic Literal carries [web-only] and Split Phase carries [web-example]"
    - "D-07: an AskUserQuestion oracle-access checkpoint is opened for this batch BEFORE authoring any entry"
    - "FWL-04: every before AND after ts fence in this batch compiles tsc --strict --noEmit clean"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md"
      provides: "the 12 basic-tag refactoring entries (incl. Split Phase, Replace Magic Literal)"
      contains: "Extract Function"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/encapsulation.md"
      provides: "the 9 encapsulation-tag refactoring entries"
      contains: "Encapsulate Record"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      to: ".claude/skills/lz-refactor-workspace/samples/*.ts"
      via: "extract-samples.mjs pulls each ts fence into a compiled module"
      pattern: "```ts"
---

<objective>
Author the "basic" and "encapsulation" tag-group leaves of the Fowler catalog -- 21
entries -- behind the mandatory D-07 oracle checkpoint. This is the first content batch;
the leaf set was fixed by 07-02.

Batch (from refactoring-com-overview.md, disjoint from other content plans):
- basic.md (12): Extract Function, Inline Function, Extract Variable, Inline Variable,
  Change Function Declaration, Encapsulate Variable, Rename Variable, Introduce Parameter
  Object, Combine Functions into Class, Combine Functions into Transform, Split Phase,
  Replace Magic Literal.
- encapsulation.md (9): Encapsulate Record, Encapsulate Collection, Replace Primitive with
  Object, Replace Temp with Query, Extract Class, Inline Class, Hide Delegate, Remove
  Middle Man, Substitute Algorithm.

Purpose: FWL-01 coverage for these 21 + FWL-04 compile-clean examples.
Output: two populated leaf files, each entry meeting the D-02 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md

<interfaces>
Per-entry leaf template (D-02 contract, structural only -- from 07-RESEARCH.md lines
276-296 and 07-PATTERNS.md lines 44-86): a `### <2nd-ed Name>  [provenance-tag]` heading
(tag on the heading line so the rg gate matches), an optional `_Aliases (1st-ed): ..._`
line, a Motivation section, a Mechanics section, and one or two ts fences (before / after).
Both fences must compile tsc --strict-clean. A smell is a design issue, never a type error.
Any leaf over ~100 lines gets a top-of-file Contents TOC. TS style: braces always, blank
line before return, blank lines around control flow (CLAUDE.md). Bucketing/leaf names come
from 07-02-SUMMARY (if 07-02 chose fine-20, use that leaf set for this batch's themes).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-07 oracle checkpoint -- basic + encapsulation batch</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md (D-02 content contract; D-06 provenance; D-07 oracle mandate)
    - .planning/research/refactoring-com-overview.md (the 21 names + aliases in this batch)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (legend + contract this batch fills)
  </read_first>
  <what-needed>
    The authoritative motivation + mechanics for the 21 named refactorings in this batch,
    from the owner's Fowler Refactoring 2nd-ed e-book / web edition (ISBN 9780135425664).
    Names/aliases are public facts; the mechanics and motivation MUST come from the oracle,
    not from training memory (D-07). Split Phase's examples are online-only; Replace Magic
    Literal is a print-absent + entry.
  </what-needed>
  <how-to-provide>
    Open the AskUserQuestion oracle-access checkpoint batched by these two tag-groups (D-07:
    verify in batches, not one refactoring at a time). The owner confirms/provides each
    refactoring's substance so it can be distilled in ORIGINAL words with an ORIGINAL TS
    example.
  </how-to-provide>
  <action>
    Open the AskUserQuestion oracle-access checkpoint for this batch and BLOCK: author NO
    entry until the owner confirms the batch substance (D-07). Verify in this tag-group batch,
    not one refactoring at a time. This gate survives the --auto/--chain run.
  </action>
  <resume-signal>Owner confirms the substance for the 21 refactorings in this batch</resume-signal>
  <acceptance_criteria>
    - The AskUserQuestion oracle-access checkpoint is opened and the owner has confirmed this batch's substance BEFORE any entry is written.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author basic.md (12 entries)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (contract + legend)
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (tsc --strict-clean fenced-TS conventions to mirror)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md (per-entry heading + interleaved prose + fence rhythm)
  </read_first>
  <action>
    Author the 12 basic-tag entries: Extract Function (alias Extract Method), Inline Function
    (alias Inline Method), Extract Variable (alias Introduce Explaining Variable), Inline
    Variable (alias Inline Temp), Change Function Declaration (aliases Add Parameter / Change
    Signature / Remove Parameter / Rename Function / Rename Method), Encapsulate Variable
    (aliases Encapsulate Field / Self-Encapsulate Field), Rename Variable, Introduce
    Parameter Object, Combine Functions into Class, Combine Functions into Transform, Split
    Phase, Replace Magic Literal (alias Replace Magic Number with Symbolic Constant). Each
    entry follows the D-02 contract: `### <Name>  [provenance]` heading, alias line where one
    exists, distilled Motivation, distilled Mechanics (original words from the oracle batch),
    and a minimal focused TypeScript before->after example. Mark Split Phase `[web-example]`
    and Replace Magic Literal `[web-only]`; all others `[print+web]`. Add a Contents TOC if
    the file exceeds ~100 lines. NO verbatim Fowler prose or code (DST-04). ASCII only.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` extracts this leaf's fences and `tsc --strict --noEmit` exits 0 over the batch's modules (FWL-04).
    - `node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` shows all 12 basic-tag names PASS (present, with Motivation + Mechanics + a ts/js fence each).
    - `rg -n "Split Phase.*\[web-example\]" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md` and `rg -n "Replace Magic Literal.*\[web-only\]" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md` each match (D-06).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII-only + no work email).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author encapsulation.md (9 entries)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/encapsulation.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (contract + legend)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md (match the leaf shape authored in Task 2)
    - .planning/research/refactoring-com-overview.md (the 9 encapsulation names + aliases)
  </read_first>
  <action>
    Author the 9 encapsulation-tag entries: Encapsulate Record (alias Replace Record with
    Data Class), Encapsulate Collection, Replace Primitive with Object (aliases Replace Data
    Value with Object / Replace Type Code with Class), Replace Temp with Query, Extract
    Class, Inline Class, Hide Delegate, Remove Middle Man, Substitute Algorithm. Same D-02
    contract and provenance rules (all `[print+web]` in this leaf). Minimal focused
    TypeScript before->after, both fences tsc --strict-clean. NO verbatim Fowler prose/code
    (DST-04). ASCII only. Add a Contents TOC if over ~100 lines.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` + `tsc --strict --noEmit` exits 0 over the batch's modules (FWL-04).
    - `node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` shows all 9 encapsulation-tag names PASS.
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner e-book -> authored leaf files | oracle mechanics cross into public-repo files |
| authored leaves -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal | verbatim Fowler prose or code in the 21 entries | mitigate | DST-04 distilled voice + ORIGINAL TS examples; refactoring.com excerpts are orientation only; check-hygiene no-verbatim heuristic + Phase-10 scan; owner confirms distilled-not-copied |
| T-07-03 | Tampering | fabricated mechanics bypassing the oracle | mitigate | D-07 blocking checkpoint BEFORE authoring; batched by tag-group; blocking under --auto/--chain |
| T-07-02 | Information Disclosure | work email in a leaf | mitigate | check-hygiene work-email allowlist gate |
| T-07-05 | Tampering | a "before" example differing from "after" only by a compile error | mitigate | both fences compile tsc --strict-clean (FWL-04 harness) -- a smell is a design issue, not a type error |

No high-severity threats: prose + original-code authoring behind a blocking human oracle
checkpoint. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The oracle checkpoint was opened before any entry was authored.
- check-catalog.mjs shows all 21 batch names PASS; extract + tsc exits 0; check-hygiene exits 0.
- Provenance tags present for Split Phase and Replace Magic Literal.
</verification>

<success_criteria>
- 21 entries authored to the D-02 contract across basic.md + encapsulation.md.
- All examples tsc --strict-clean (FWL-04); provenance labeled (D-06); ASCII-only; no verbatim prose.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-04-SUMMARY.md` when done.
</output>
