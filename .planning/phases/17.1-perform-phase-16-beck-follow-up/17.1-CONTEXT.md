# Phase 17.1: Perform Phase 16 Beck follow-up - Context

**Gathered:** 2026-07-20
**Status:** Ready for planning

<domain>
## Phase Boundary

Owned-source **tier upgrade** of the lz-red skill's currently-no-oracle, Kent-Beck-backed RED
selection/structure surfaces from `Unowned; high-confidence core only (no-oracle)` to
`Owned; oracle-verified`, now that Beck's essays are provisioned in `.oracle/written-content/`.

This is a legitimate re-open of closed Phase-16 shipped content (`three-laws-and-test-selection.md`,
`test-structure-and-assertions.md` STR slice, and the `principle-backing.md` map). It follows the
exact clean-room re-gate loop Phase 17 proved: edit attribution/tier -> orchestrator-driven
oracle-reviewer gate against the correct `.oracle` essay -> converge-to-clean.

**Explicitly NOT in scope:** authoring new RED guidance, changing recommendation CONTENT, touching
non-Beck surfaces, altering asserted instrument tokens, or the three other open follow-ups
(broader internal-ID/DST-04 3-skill polish; FUT-METZ-REFACTOR lz-refactor Metz layer; Phase 18
coach procedure + lz-tpp seam).

</domain>

<decisions>
## Implementation Decisions

### Upgrade scope (which surfaces flip to owned)
- **D-01:** Upgrade `Keep a running test list` and `Take one small step` (both in
  `three-laws-and-test-selection.md`) against Canon TDD (`.oracle/written-content/canon-tdd.md`).
- **D-02:** Upgrade `Triangulate to select the next test` (`three-laws-and-test-selection.md`)
  against First One, Then Many (`.oracle/written-content/tri-first-one-then-many.md`).
- **D-03:** `Start from the degenerate or starter case` (`three-laws-and-test-selection.md`) is a
  CANDIDATE upgrade -- the handoff did not name it, but First One, Then Many / Canon TDD plausibly
  back it. Upgrade it too IF (and only if) a full-text essay actually covers it under the oracle
  gate; otherwise leave it no-oracle and say so. Do not force it.
- **D-04:** `Assert-first` and `Evident test data` (`test-structure-and-assertions.md` STR slice)
  are CONDITIONAL. These live mainly in TDD-by-Example, which is `Access: book` (summary-only, NOT
  gateable). Upgrade ONLY if a free essay (candidates: Canon TDD, Desirable Unit Tests, Composable
  Tests) actually covers the point under the oracle gate; otherwise leave no-oracle and record why.

### Gateability rule (the honesty gate)
- **D-05:** A surface may be tagged `Owned; oracle-verified` ONLY when it can be gated against
  real full-text source (`Access: free` in `.oracle/written-content/index.md`). Book-summary
  entries (`Access: book` -- 1-2 sentence summaries: `book-tdd-by-example.md`,
  `book-xp-explained.md`, `book-implementation-patterns.md`, `book-tidy-first.md`) are NOT
  gateable; surfaces backed only by those stay no-oracle. A tier upgrade is only honest if the
  surface can actually be verified against source text.

### Clean-room re-gate process (DST-04)
- **D-06:** The author edits BLIND -- main context never reads `.oracle/` essay PROSE; it reads
  only the `.oracle/*/index.md` navigation files. Content is likely already correct (well-known
  Beck TDD); the gate confirms and may request a wording revise.
- **D-07:** The ORCHESTRATOR drives the `oracle-reviewer` agent on each upgraded surface vs its
  Beck essay (converge-to-clean). The gsd-executor has no Agent/Task tool, so any plan task must
  frame review as "orchestrator gates after executor returns," never "executor spawns the reviewer."

### Provenance record + battery integrity
- **D-08:** In `principle-backing.md`, flip the Access-tier cell for each upgraded row
  (no-oracle -> owned) and update the Sources paragraph -- remove the "remain no-oracle for now,
  pending a follow-up upgrade against his now-provisioned essays (Canon TDD, First One Then Many)"
  caveat for exactly the rows that were upgraded (leave it for any row deliberately left no-oracle).
- **D-09:** Add a Phase-16 provenance/upgrade note (mirrors the correction notes already added to
  16-CONTEXT / 17-CONTEXT) recording that this re-opened closed Phase-16 content for an
  owned-source upgrade.
- **D-10:** Keep the deterministic battery GREEN. `check-red-references.mjs` asserts CONTENT tokens
  (e.g. `ADV-0`); touch only attribution/tier text, never asserted tokens. Re-run the full battery
  after every edit (all three checks must stay exit 0).

### Claude's Discretion
- Exact wording of upgraded provenance/Source lines (subject to the oracle-reviewer gate).
- Placement/format of the Phase-16 upgrade note, following existing correction-note precedent.
- Whether the candidate/conditional rows (D-03, D-04) upgrade or stay -- decided by the gate, per
  the locked rule D-05, not pre-committed here.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The follow-up specification (authoritative task definition)
- `.planning/.continue-here.md` -- the queued Follow-up #2 spec: remaining_work items 1-7,
  decisions_made, required-reading order, infrastructure state.
- `.planning/HANDOFF.json` -- structured task list (7 remaining tasks) + decisions + context_notes.

### Surfaces to upgrade (skill's own authored files)
- `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md` -- headings:
  `## Keep a running test list`, `## Take one small step`, `## Start from the degenerate or starter
  case`, `## Triangulate to select the next test`; plus its `## Sources` block.
- `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md` -- headings
  `## Assert-first`, `## Evident test data`; plus its `## Sources` block.
- `plugins/lz-tdd/skills/lz-red/references/principle-backing.md` -- the source-to-recommendation
  tier map (rows for the surfaces above) + the `## Sources` paragraph carrying the "pending
  follow-up upgrade" caveat to update.

### Oracle sources (nav-only in main context; essay prose read ONLY by oracle-reviewer)
- `.oracle/written-content/index.md` -- nav index; Access-tier column decides eligibility (D-05).
- `.oracle/written-content/canon-tdd.md` -- Canon TDD (backs test list + one-step; candidate for
  degenerate-case / assert-first / evident-data).
- `.oracle/written-content/tri-first-one-then-many.md` -- First One, Then Many (backs triangulation;
  candidate for degenerate-case).
- `.oracle/written-content/desirable-unit-tests.md`, `.oracle/written-content/composable-tests.md`
  -- additional free-essay candidates for the STR-slice conditional (D-04).

### Pattern + hygiene references
- `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-LEARNINGS.md` -- the
  clean-room re-gate loop, the provenance-conflation lesson, and the instrument-token caveat.
- `.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md` -- Phase-16 decisions
  + the existing provenance-correction note (precedent for D-09).
- `./CLAUDE.md` + `./AGENTS.md` -- clean-room DST-04, gsd-executor-can't-spawn, ASCII-only, `rg`
  not `grep`, public-gmail identity, allowlist-inversion email hygiene.

### Requirements touched (provenance-tier upgrade only; all already Complete for Phase 16)
- `.planning/REQUIREMENTS.md` -- SEL-01, SEL-02 (three-laws surfaces), STR-02 (assert-first /
  evident-data), DST-03 / DST-04 (source-to-recommendation traceability + clean-room). No NEW
  requirements; this upgrades provenance tiers, it does not change delivered behavior.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Deterministic battery (dev-only, git-ignored workspace) -- must stay exit 0 after every edit:
  - `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs`
  - `npm --prefix .claude/skills/lz-red-workspace run typecheck`
  - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs`
- The `oracle-reviewer` agent + the Phase-17 clean-room re-gate loop -- reuse verbatim; this phase
  is that loop applied to the Phase-16 Beck surfaces.

### Established Patterns
- Access-tier vocabulary is fixed: `Owned; oracle-verified against the clean-room source.` vs
  `Unowned; high-confidence core only (no-oracle).` Match the existing string exactly when flipping.
- Provenance is stated per-surface in each reference doc's `## Sources` block AND centrally in
  `principle-backing.md`. An upgrade must update BOTH places for consistency.

### Integration Points / Caveats
- `check-red-references.mjs` asserts content tokens (e.g. `ADV-0`) and per-file fences -- editing
  attribution/tier text is safe; removing an asserted token breaks the gate (instrument caveat).
- gsd-executor cannot spawn agents -> plan oracle-reviewer gates as orchestrator steps after the
  executor's blind edits return.

</code_context>

<specifics>
## Specific Ideas

- Content is expected to already be correct (well-known Beck TDD); the oracle gate primarily
  CONFIRMS the existing own-words text against Beck's essays and may request a wording revise. Do
  not rewrite recommendations wholesale.
- The `principle-backing.md` Sources paragraph currently names the exact caveat sentence to remove
  once the Canon-TDD / First-One-Then-Many rows are upgraded -- edit that sentence surgically.

</specifics>

<deferred>
## Deferred Ideas

- **ROADMAP goal placeholder:** Phase 17.1's ROADMAP entry still reads "[Urgent work - to be
  planned]" with Requirements TBD. plan-phase (or `/gsd-phase edit`) should set the real goal/scope
  from this CONTEXT before or during planning.
- **Follow-up #1** -- broader internal-ID / DST-04 3-skill polish. Separate task.
- **Follow-up #3 (FUT-METZ-REFACTOR)** -- lz-refactor Metz layer. Separate milestone/task.
- **Phase 18** -- coach procedure on the Three Laws spine + lz-red <-> lz-tpp seam wiring. Next
  roadmap phase; the `principle-backing.md` Three-Laws/seam rows land there, not here.

None of the above are in this phase's scope.

</deferred>

---

*Phase: 17.1-perform-phase-16-beck-follow-up*
*Context gathered: 2026-07-20*
