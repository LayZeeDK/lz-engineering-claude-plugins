---
phase: 08.2-functional-catalog-inserted
plan: 04
type: execute
wave: 2
depends_on: [08.2-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/option-and-either.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/functor-and-monad.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/lens-and-optics.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/currying-and-partial-application.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/transducers.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/normalized-store.md
autonomous: true
requirements: [FUN-01, FUN-03]

must_haves:
  truths:
    - "Six native-FP alternative-to idiom leaves exist FLAT in functional-catalog/ on the locked template, each with a paraphrased ## Idiom, a tsc-strict-clean Before->After ## Example, and a single un-split ## When each fits"
    - "Every Correspondence: value is alternative-to; option-and-either and currying-and-partial-application carry MUTUAL back-links (Null Object, Extract Parameter), the other four target Iterator/Builder/Fowler analogs as resolution-only references"
    - "lens-and-optics, transducers, and normalized-store may target fowler-catalog leaves in Correspondence (A3 resolution: fowler targets accepted, resolution-only, outside the gof/kerievsky/extra mutuality set)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/option-and-either.md"
      provides: "Option/Result native leaf; MUTUAL with Null Object (extra)"
      contains: "Correspondence: alternative-to"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/normalized-store.md"
      provides: "The D-10 sixth native leaf (id + Map<Id,Entity>); Change Value to Reference [fowler] + Mediator [gof]"
      contains: "## Example"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/lens-and-optics.md"
      provides: "The selector/lens leaf; Hide Delegate + Remove Middle Man [fowler] resolution-only"
      contains: "alternative-to"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/option-and-either.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/null-object.md"
      via: "Correspondence: alternative-to link (mutual with Null Object's Functional alternative:)"
      pattern: "extra-patterns-catalog/null-object.md#null-object"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/currying-and-partial-application.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-parameter.md"
      via: "Correspondence: alternative-to link (mutual with Extract Parameter, A4)"
      pattern: "kerievsky-catalog/extract-parameter.md#extract-parameter"
  decisions: |
    - **D-03:** ONE unified functional-catalog/ covers native FP patterns alongside the de-patterning idioms, distinguished only by the Correspondence value (alternative-to here vs dissolves-from in plans 02/03)
    - **D-08:** the five native FP patterns (Option/Either, functor/monad, lens/optics, currying/partial application, transducers) are alternative-to leaves cross-referencing their OO alternative (Option<->Null Object, functor<->Iterator, currying<->Builder/Extract Parameter, transducers<->Replace Loop with Pipeline)
    - **D-10:** lens-and-optics (selector/lens from Hide Delegate/Remove Middle Man) and normalized-store (from Change Value to Reference) are the two new Fowler-sourced idiom leaves; the Fowler participation is resolution-only via Correspondence, not a bidirectional link
    - **D-11:** native leaves live FLAT in functional-catalog/ (no native/ subdir) -- the alternative-to tag plus the README map already distinguish them
    - **D-01:** each leaf uses the locked template; NO duplicated ## Mechanics
---

<objective>
Author the six native-FP "alternative-to" idiom leaves -- FP patterns with no single OO
ancestor, offered ALONGSIDE an OO alternative rather than dissolving one: option-and-either,
functor-and-monad, lens-and-optics, currying-and-partial-application, transducers,
normalized-store. Flat in functional-catalog/ (D-11). Completes the 19-leaf inventory.

Purpose: FUN-03 -- native FP patterns cross-referencing their OO alternative. Two of them
(option-and-either, currying-and-partial-application) form mutual back-links with an OO leaf
(Null Object, Extract Parameter); the other four reference their OO analog resolution-only
(functor->Iterator; lens/transducers/normalized-store -> Fowler analogs, A3).

Output: 6 alternative-to leaf files on the locked D-01 template, every Example tsc-strict-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-functional-catalog-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md

Native-set source: `.planning/research/functional-depatterning-ts.md` Section 12.4 (native FP
items + their OO alternatives) and Section 12.3 (Hide Delegate -> selector/lens; Change Value
to Reference -> normalized store; Replace Loop with Pipeline -> transducers).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: option-and-either + currying-and-partial-application (the two MUTUAL-back-link natives)</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (Alternative-to inventory rows N1, N4; the Cross-Link Contract "Mutuality" note; A4 -- Extract Parameter maps to currying)
    - .planning/research/functional-depatterning-ts.md Section 12.1/12.4 (Null Object -> no-op fn / Option; currying <-> Builder/constructor injection)
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/null-object.md (the OO leaf that will link back to option-and-either)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-parameter.md (the OO leaf that will link back to currying, A4)
  </read_first>
  <behavior>
    - option-and-either: Option/Result-style discriminated unions replace null-checks and exceptions. Correspondence: alternative-to -> Null Object [extra] (MUTUAL: Null Object's Functional alternative: will point here in plan 06) + Introduce Null Object [kerievsky]. Single ## When each fits paragraph.
    - currying-and-partial-application: fix some arguments now, supply the rest later. Correspondence: alternative-to -> Builder [gof] (resolution-only; Builder dissolves to function-composition) + Extract Parameter [kerievsky] (MUTUAL, A4). Single paragraph.
    - Examples compile tsc --strict; pure/same-I/O.
  </behavior>
  <action>
    Create `option-and-either.md` and `currying-and-partial-application.md` on the D-01 template. Header:
    `Correspondence: alternative-to -> [links]`. option-and-either links `../extra-patterns-catalog/null-object.md#null-object`
    and `../kerievsky-catalog/introduce-null-object.md#introduce-null-object`. currying-and-partial-application links
    `../gof-catalog/builder.md#builder` and `../kerievsky-catalog/extract-parameter.md#extract-parameter`. Paraphrased ## Idiom,
    tsc-strict-clean ## Example (+ the one-line mechanics cross-link), single un-split ## When each fits. ASCII-only.
  </action>
  <acceptance_criteria>
    - Both files exist FLAT in functional-catalog/; check-functional C1 recognizes both; filename == slug
    - `node extract-samples.mjs` exits 0
    - Each Correspondence: begins `Correspondence: alternative-to ->`
    - option-and-either links to null-object.md; currying-and-partial-application links to extract-parameter.md; node tools/check-crossrefs.mjs shows no dangling link
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-crossrefs.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>Both mutual-back-link natives compile, link to their OO counterparts, pass hygiene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: functor-and-monad + normalized-store</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (Alternative-to rows N2, N6; A3 -- fowler targets accepted resolution-only; the "Native 6 -> OO alternative" note on resolution-only refs)
    - .planning/research/functional-depatterning-ts.md Section 12.3 (Change Value to Reference -> normalized store; same substrate as Mediator) and Section 12.4 (functor <-> Iterator)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/change-value-to-reference.md (the Fowler analog target for normalized-store; resolution-only)
  </read_first>
  <behavior>
    - functor-and-monad: a mappable/chainable container (map/flatMap). Correspondence: alternative-to -> Iterator [gof] (resolution-only: Iterator's own Functional alternative: points to generator, so this is NOT mutual). Single paragraph.
    - normalized-store: id + Map<Id,Entity> for shared entity references. Correspondence: alternative-to -> Change Value to Reference [fowler] (A3 resolution-only) + Mediator [gof] (resolution-only; Mediator's Functional alternative: points to reducer-and-store). Single paragraph.
    - Examples compile tsc --strict; pure/same-I/O.
  </behavior>
  <action>
    Create `functor-and-monad.md` and `normalized-store.md` on the D-01 template. functor-and-monad
    `Correspondence: alternative-to -> [Iterator](../gof-catalog/iterator.md#iterator)`. normalized-store
    `Correspondence: alternative-to -> [Change Value to Reference](../fowler-catalog/change-value-to-reference.md#change-value-to-reference), [Mediator](../gof-catalog/mediator.md#mediator)`.
    Record in the SUMMARY that these Correspondence refs are RESOLUTION-ONLY (not mutual): the OO leaves' own Functional
    alternative: links point at their dissolution leaves (Iterator->generator, Mediator->reducer-and-store), and Fowler leaves
    never receive Functional alternative: links (A3). Paraphrased ## Idiom; tsc-strict-clean ## Example; single ## When each fits.
    ASCII-only.
  </action>
  <acceptance_criteria>
    - Both files exist FLAT in functional-catalog/; check-functional C1 recognizes both
    - `node extract-samples.mjs` exits 0
    - normalized-store Correspondence includes a fowler-catalog link and node tools/check-crossrefs.mjs resolves it (A3 accepted)
    - node tools/check-hygiene.mjs exits 0
    - SUMMARY records the resolution-only classification for these Correspondence refs
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-crossrefs.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>Both natives compile; the fowler-target Correspondence resolves; A3 recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lens-and-optics + transducers</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (Alternative-to rows N3, N5; A3 fowler resolution-only; the D-10 selector/lens sourcing)
    - .planning/research/functional-depatterning-ts.md Section 12.3 (Hide Delegate -> selector/lens; Remove Middle Man inverse) and Section 12.7 (transducers vs Replace Loop with Pipeline; intermediate-array avoidance)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/hide-delegate.md and remove-middle-man.md and replace-loop-with-pipeline.md (the Fowler analog targets, resolution-only)
  </read_first>
  <behavior>
    - lens-and-optics: a composable getter/setter pair (a selector) over immutable data. Correspondence: alternative-to -> Hide Delegate [fowler] + Remove Middle Man [fowler] (A3 resolution-only). Single paragraph.
    - transducers: compose collection operations without intermediate arrays. Correspondence: alternative-to -> Replace Loop with Pipeline [fowler] (A3 resolution-only). Single paragraph (note: the Replace-Pipeline-with-Loop reverse note lives IN the Fowler leaf per D-14, added in plan 05 -- do not duplicate it here).
    - Examples compile tsc --strict; pure/same-I/O.
  </behavior>
  <action>
    Create `lens-and-optics.md` and `transducers.md` on the D-01 template with the fowler-catalog Correspondence links above
    (resolution-only, A3). Paraphrased ## Idiom; tsc-strict-clean ## Example; single un-split ## When each fits. ASCII-only.
    Do NOT add a Functional alternative: line to any fowler leaf (Fowler participates via README rows only).
  </action>
  <acceptance_criteria>
    - Both files exist FLAT in functional-catalog/; check-functional C1 recognizes both; 19 idiom leaves now exist
    - `node extract-samples.mjs` exits 0
    - Correspondence links to fowler-catalog leaves resolve via node tools/check-crossrefs.mjs (no dangling)
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-crossrefs.mjs && node tools/check-hygiene.mjs && (node tools/check-functional.mjs; true)</automated>
  </verify>
  <done>Both natives compile and resolve; all 19 idiom leaves now exist (13 dissolves-from + 6 alternative-to).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown reference content only -- no runtime/network/user-input surface. Boundaries are
repo-hygiene / licensing invariants.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | committed leaves must be ASCII-only, work-email-free, no verbatim book prose/code |
| research artifact -> leaf prose | paraphrase near-verbatim source quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-IP | Information Disclosure | the 6 native leaf files | mitigate | DST-04 paraphrase discipline; check-hygiene no-verbatim heuristic. |
| T-08.2-EM | Information Disclosure | committed files | mitigate | Work-email allowlist enforced by check-hygiene. |
| T-08.2-SC | Tampering | typescript@6.0.3 | accept | No new installs; compilation uses the pinned tsc. |
</threat_model>

<verification>
- `node extract-samples.mjs` exits 0 (all 6 Examples compile tsc --strict).
- `node tools/check-crossrefs.mjs` exits 0 (Correspondence links resolve, incl. the fowler-catalog A3 targets).
- `node tools/check-hygiene.mjs` exits 0.
- check-functional advances on C1 (19/19 idiom leaves recognized); C3/C4/C7/C8 still RED until plans 05/06.
</verification>

<success_criteria>
Six native-FP alternative-to leaves exist FLAT on the locked template, every Example
tsc-strict-clean; the two mutual back-links (Null Object, Extract Parameter) and the four
resolution-only refs (Iterator, Builder, Fowler analogs) are in place; 19-leaf inventory complete.
</success_criteria>

<output>
Create `.planning/phases/08.2-functional-catalog-inserted/08.2-04-SUMMARY.md` when done
(record A3 fowler-target resolution + the resolution-only vs mutual classification).
</output>
