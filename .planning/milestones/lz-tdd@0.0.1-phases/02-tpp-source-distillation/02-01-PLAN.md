---
phase: 02-tpp-source-distillation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md
autonomous: true
requirements: [TPP-03]
user_setup: []

must_haves:
  truths:
    - "The NDC 2011 talk (video id B93QezwTQpI) is transcribed with the local youtube-to-markdown tool, falling back to the markitdown CLI only when the primary output is 0-segment or metadata-only (D-07)"
    - "The transcript is retained git-tracked at .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md and is NOT bundled under the shipped plugins/lz-tdd/skills/lz-tpp/references/ (D-08)"
    - "The retained transcript is ASCII-only committed content and contains no work-email literal (repo hygiene)"
  artifacts:
    - path: .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md
      provides: "Retained NDC 2011 TPP talk transcript as reconciliation source material for 02-02 (D-07, D-08)"
      min_lines: 1
  key_links:
    - from: ".planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md"
      to: "plugins/lz-tdd/skills/lz-tpp/references/transformations.md (reconciliation section)"
      via: "manual reconciliation performed in plan 02-02"
      pattern: "reconcil"
---

<objective>
Produce and retain the NDC 2011 "Transformation Priority Premise" talk (video id
`B93QezwTQpI`) as a git-tracked transcript at the D-08 retention path, so plan 02-02
can reconcile it against the two Clean Code blog posts.

Purpose: The transcript is a tertiary provenance/reconciliation input (blogs take
precedence over the talk). It is source material only -- NOT a shipped skill asset --
so ASR quality just needs to be usable for reconciliation, not clean prose (D-07).
Output: `.planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md`
(ASCII-only, git-tracked, non-empty), never placed under the shipped `references/` dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tpp-source-distillation/02-CONTEXT.md
@.planning/phases/02-tpp-source-distillation/02-RESEARCH.md

Tool facts (verified in RESEARCH.md "Tool invocation" + "Fallback trigger"):
- Primary tool: youtube-to-markdown.mjs at
  D:\projects\github\LayZeeDK\lz-cybernetics-ai-plugins\tools\youtube-to-markdown\youtube-to-markdown.mjs
  (present; node_modules incl. youtubei.js installed; Node v24.18.0).
- `--output <prefix>` is passed through path.resolve and the tool writes `<prefix>.md`.
  Pass the prefix WITHOUT the `.md` extension so the file lands exactly at the D-08 path.
- Do NOT pass `--json` (the .json sidecar would be untracked noise; D-08 retains only .md).
- Success prints `[OK] Markdown saved: ...` plus a segment count. Failure signal is
  `[WARN] No transcript was available. Markdown contains metadata only.` OR a 0 segment count.
- Fallback (ONLY on the failure signal above): markitdown CLI at
  C:\Users\LarsGyrupBrinkNielse\AppData\Local\Temp\markitdown-mcp-npx\venv\Scripts\markitdown.exe
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the NDC 2011 transcript via youtube-to-markdown (markitdown fallback)</name>
  <files>.planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md</files>
  <read_first>
    - .planning/phases/02-tpp-source-distillation/02-CONTEXT.md (D-07, D-08)
    - .planning/phases/02-tpp-source-distillation/02-RESEARCH.md ("Tool invocation (transcript, D-07)", "Fallback trigger (D-07)", Open Question Q1, Assumptions A1/A3)
  </read_first>
  <action>
    Run the primary tool with node, passing the video id `B93QezwTQpI` and
    `--output` set to the D-08 retention PREFIX (the retention path minus the `.md`
    extension): `.planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript`.
    Do not append `.md` to the prefix; do not pass `--json`. Read the tool's stdout:
    on `[OK] Markdown saved` with a non-zero segment count the primary output is
    usable -- stop here. ONLY if it prints the metadata-only WARN or reports 0
    segments, trigger the markitdown fallback (D-07) against the talk URL
    https://youtu.be/B93QezwTQpI and write its Markdown output to the same D-08 path.
    Do NOT over-invest in ASR cleanup -- the bar is "usable for reconciliation", not
    clean prose (D-07). Do NOT copy the file anywhere under
    plugins/lz-tdd/skills/lz-tpp/references/ (D-08: retained, not shipped). If BOTH
    tools yield only metadata/0 segments, record that outcome in the task notes and
    still retain whatever metadata file was produced (reconciliation in 02-02 will
    then note "no usable transcript track available" rather than fabricate content).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md` succeeds (file exists at the exact D-08 path).
    - The file is non-empty: `test -s .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md` succeeds.
    - No transcript artifact was written under the shipped skill: `find plugins/lz-tdd/skills/lz-tpp/references -name '*transcript*' 2>/dev/null` returns nothing (references/ may not exist yet -- absent is also a pass).
    - No stray `.json` sidecar was retained in the phase dir: `find .planning/phases/02-tpp-source-distillation -name 'ndc-2011-tpp-transcript.json'` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md && test -s .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md && echo TRANSCRIPT_RETAINED</automated>
  </verify>
  <done>A non-empty transcript .md exists at the D-08 path (produced by the primary tool, or by the markitdown fallback if the primary returned metadata-only/0 segments), with no copy under references/ and no .json sidecar.</done>
</task>

<task type="auto">
  <name>Task 2: ASCII-normalize and hygiene-verify the retained transcript</name>
  <files>.planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md</files>
  <read_first>
    - .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md (the file produced in Task 1)
    - .planning/research/PITFALLS.md (Security Mistakes row: ASCII-only committed content; work-email hygiene)
    - .planning/phases/02-tpp-source-distillation/02-RESEARCH.md ("Security Domain" table)
  </read_first>
  <action>
    Perform a bounded MECHANICAL encoding normalization only (NOT prose cleanup, per
    D-07): replace any non-ASCII bytes surviving from ASR/auto-caption output with
    their ASCII equivalents -- curly single/double quotes to straight ' and ", en/em
    dashes to `-`/`--`, ellipsis to `...`, non-breaking spaces to plain spaces, any
    special arrow/dash glyph to `->`. Do not rewrite wording, fix grammar, or re-time
    segments. Then confirm repo hygiene: the file must contain no email contact at all
    (a talk transcript should have none); if any email string appears it may only be
    the public gmail, never the work-email literal. Leave the transcript otherwise
    as-produced.
  </action>
  <acceptance_criteria>
    - ASCII gate returns zero matches: `rg -n '[^\x00-\x7F]' .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md` prints nothing.
    - Work-email absence via allowlist (never spells the work email; set no literal): `rg -uNo -e '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]+' .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md | rg -v 'larsbrinknielsen@gmail\.com'` returns no output.
    - The file is still non-empty after normalization (`test -s` succeeds).
  </acceptance_criteria>
  <verify>
    <automated>rg -n '[^\x00-\x7F]' .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md; test $? -eq 1 && echo ASCII_CLEAN</automated>
  </verify>
  <done>The retained transcript is ASCII-only (zero non-ASCII matches), carries no work-email literal (allowlist check clean), and remains non-empty -- normalization was mechanical encoding only, no prose editing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External video/ASR source -> committed repo file | Untrusted third-party caption/ASR text crosses into a public, git-tracked artifact |
| Local tool output -> Windows cp1252 toolchain | Non-ASCII bytes from ASR can corrupt diffs/rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | Retained transcript in public `.planning/` | mitigate | Work-email allowlist scan (Task 2) asserts only the public gmail can appear; the check never spells the work-email literal |
| T-02-01-02 | Tampering (encoding) | Non-ASCII ASR bytes in committed `.md` | mitigate | Mechanical ASCII normalization + `rg -n '[^\x00-\x7F]'` gate returns zero (Task 2) |
| T-02-01-03 | Tampering (of source truth) | ASR transcript treated as canonical over the blogs | accept | Low risk: transcript is retained as tertiary reconciliation input only; precedence (blogs > talk > secondary) is enforced in 02-02, not here |
| T-02-01-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs this phase; only pre-existing local tooling is invoked (RESEARCH "Package Legitimacy Audit: Not applicable") |
</threat_model>

<verification>
- File exists and is non-empty at the exact D-08 path; no copy under the shipped `references/`; no `.json` sidecar.
- ASCII gate (`rg -n '[^\x00-\x7F]'`) returns zero matches.
- Work-email allowlist check returns no output.
</verification>

<success_criteria>
- TPP-03 (transcription + retention half): the NDC 2011 talk is transcribed via the
  local tool (markitdown fallback path defined) and retained git-tracked at the D-08
  path as source material, ASCII-clean and hygiene-clean. The reconciliation half of
  TPP-03 is completed in 02-02.
</success_criteria>

<output>
Create `.planning/phases/02-tpp-source-distillation/02-01-SUMMARY.md` when done.
</output>
