---
phase: 08.2-functional-catalog-inserted
plan: 03
type: execute
wave: 2
depends_on: [08.2-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/reducer-and-store.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/observer-callbacks.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/generator.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/memoization-and-interning.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/immutable-snapshot.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/structural-clone.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/branded-type.md
autonomous: true
requirements: [FUN-01, FUN-02]

must_haves:
  truths:
    - "Seven 1:1 dissolves-from idiom leaves exist on the locked template, each with a paraphrased ## Idiom, a tsc-strict-clean Before->After ## Example, and a single un-split ## When each fits paragraph (NO per-pattern ### sub-headings)"
    - "Every ts fence sits INSIDE ## Example and compiles under npm run typecheck; every Correspondence: value is dissolves-from linking to its single served OO leaf"
    - "Prototype and Replace Type Code with Class both resolve to a real LEAF (structural-clone, branded-type) so every GoF-23 pattern and its Kerievsky refactoring maps to a leaf, not a note"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/generator.md"
      provides: "The Iterator dissolution leaf (function*/Symbol.iterator)"
      contains: "Correspondence: dissolves-from"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/branded-type.md"
      provides: "The Replace Type Code with Class / Replace Primitive with Object leaf (opaque/branded type)"
      contains: "## Example"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/structural-clone.md"
      provides: "The Prototype leaf keeping the 23-GoF -> leaf invariant clean (A7)"
      contains: "Keep the OO form when:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/generator.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/iterator.md"
      via: "Correspondence: dissolves-from link"
      pattern: "gof-catalog/iterator.md#iterator"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/reducer-and-store.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/mediator.md"
      via: "Correspondence: dissolves-from link"
      pattern: "gof-catalog/mediator.md#mediator"
  decisions: |
    - **D-01:** each leaf uses the locked template (Use when: / Correspondence: dissolves-from -> [link] / Keep the OO form when: header, then ## Idiom / ## Example with the one-line mechanics cross-link / ## When each fits); 1:1 leaves carry a single un-split ## When each fits paragraph -- no ### per-pattern anchors; NO duplicated ## Mechanics
    - **D-06:** no owned oracle -- research artifact + check-functional + tsc --strict are the anchor; DST-04 clean-room (paraphrase, original tsc-clean code) applies
---

<objective>
Author the seven 1:1 "dissolves-from" idiom leaves -- each serves a single OO pattern, so
each has a single un-split `## When each fits` paragraph (no residual `###` split):
reducer-and-store (Mediator), observer-callbacks (Observer), generator (Iterator),
memoization-and-interning (Flyweight), immutable-snapshot (Memento), structural-clone
(Prototype), branded-type (Replace Type Code with Class / Replace Primitive with Object).

Purpose: complete the 13 dissolves-from leaves. structural-clone and branded-type exist as
real leaves (not notes) so every GoF-23 pattern resolves to a linkable leaf, keeping the
bidirectional check (C4) uniform.

Output: 7 leaf files on the locked D-01 template, every Example tsc-strict-clean, every
## Idiom paraphrased.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-functional-catalog-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md

Idiom content source: `.planning/research/functional-depatterning-ts.md` Sections 10-11
(Mediator reducer+store, Observer callbacks, Iterator generators, Flyweight
memoization/interning, Memento immutable snapshot, Prototype structural clone; PARAPHRASE).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reducer-and-store + observer-callbacks + generator</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (inventory rows 5, 6, 7; the GoF/Kerievsky N:1 rows for Mediator, Observer + Replace Hard-Coded Notifications with Observer, Iterator)
    - .planning/research/functional-depatterning-ts.md Section 10 Q1 (Observer callbacks, State/Mediator reducer), Q4 (State-as-reducer corroboration), Section 3.4 (Iterator generators), Section 11 (Mediator, Observer, Iterator verdicts + tiers)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-logic-with-strategy.md (header/body shape)
  </read_first>
  <behavior>
    - reducer-and-store: a central pure reducer + store/subscribe (Redux/Elm). Correspondence: dissolves-from -> Mediator [gof]. Single ## When each fits paragraph (God-object risk transfers to the reducer; coordinating long-lived stateful actors relocates to an effect layer).
    - observer-callbacks: a set of listener callbacks + an unsubscribe closure / event stream. Correspondence: dissolves-from -> Observer [gof] + Replace Hard-Coded Notifications with Observer [kerievsky]. Single paragraph (complex event graphs want reactive streams -- a library, not a built-in).
    - generator: a generator function / lazy sequence (function*, Symbol.iterator). Correspondence: dissolves-from -> Iterator [gof]. Single paragraph (laziness as the value-add; infinite sequences).
    - Examples compile tsc --strict; pure/same-I/O.
  </behavior>
  <action>
    Create `reducer-and-store.md`, `observer-callbacks.md`, `generator.md` on the D-01 template. Header: `Use when:` one line;
    `Correspondence: dissolves-from -> [link]` with the served OO leaf link(s) above; `Keep the OO form when:` from the locked
    guardrail list. Body: paraphrased `## Idiom`, tsc-strict-clean `## Example` (Before OO -> After idiom + the one-line
    `Same behavior; mechanics: [<refactoring>](../<catalog>/<slug>.md#<anchor>), run tests between steps.`), and a SINGLE
    un-split `## When each fits` paragraph (no `###` sub-headings -- these are 1:1 leaves). ASCII-only.
  </action>
  <acceptance_criteria>
    - Three files exist; check-functional C1 recognizes each; filename == slug
    - `node extract-samples.mjs` exits 0
    - Each ## When each fits is a single paragraph with NO `### ` sub-heading
    - Correspondence: lines begin `Correspondence: dissolves-from ->` and links resolve
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>Three 1:1 leaves compile, single-paragraph When-each-fits, pass hygiene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: memoization-and-interning + immutable-snapshot + structural-clone</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (inventory rows 10, 11, 12; A2 -- memoization-and-interning classified dissolves-from Flyweight; A7 -- Prototype gets a leaf not a note)
    - .planning/research/functional-depatterning-ts.md Section 11 "C resolved" (Flyweight memoization/interning), Section 10 Q3 (Object.freeze/readonly/as const for Memento; structural clone via spread/structuredClone for Prototype), Section 11 Creational/Structural tables
  </read_first>
  <behavior>
    - memoization-and-interning: a memoized/interning factory (closure over a Map/WeakMap), Symbol.for. Correspondence: dissolves-from -> Flyweight [gof]. Single paragraph (fully dissolves for immutable primitives; a closure-held cache remains for heavy compound values).
    - immutable-snapshot: keep the previous immutable value (structural sharing); readonly/freeze/as const. Correspondence: dissolves-from -> Memento [gof]. Single paragraph (readonly is erased at runtime; Object.freeze is shallow -- name the gotcha).
    - structural-clone: a structural clone via spread / structuredClone / Object.create. Correspondence: dissolves-from -> Prototype [gof]. Single paragraph.
    - Examples compile tsc --strict; pure/same-I/O.
  </behavior>
  <action>
    Create `memoization-and-interning.md`, `immutable-snapshot.md`, `structural-clone.md` on the D-01 template with the
    dissolves-from Correspondence links above and single un-split ## When each fits paragraphs. Paraphrase every ## Idiom line.
    ASCII-only. In the SUMMARY note A2 (memoization-and-interning is dissolves-from Flyweight, not a native alternative-to leaf)
    and A7 (structural-clone is a leaf, not a moot note, to keep the 23-GoF -> leaf invariant).
  </action>
  <acceptance_criteria>
    - Three files exist; check-functional C1 recognizes each; filename == slug
    - `node extract-samples.mjs` exits 0
    - Each Correspondence: begins `Correspondence: dissolves-from ->`; links resolve
    - No `### ` sub-heading in any of the three ## When each fits sections
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>Three 1:1 leaves compile and pass hygiene; A2/A7 recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: branded-type</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (inventory row 13; the N:1 rows -- Replace Type Code with Class [kerievsky] + Replace Primitive with Object [fowler via README, not a bidirectional link])
    - .planning/research/functional-depatterning-ts.md Section 12.3 (Replace Primitive with Object -> branded type)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-type-code-with-class.md (the served leaf; its Functional alternative: link will target branded-type in plan 06)
  </read_first>
  <behavior>
    - branded-type: a branded/opaque type wrapping a primitive (a phantom-tagged nominal type). Correspondence: dissolves-from -> Replace Type Code with Class [kerievsky]. (Replace Primitive with Object is a Fowler analog reached via the README map only -- do NOT add a Fowler bidirectional link.) Single un-split ## When each fits paragraph. Example compiles tsc --strict.
  </behavior>
  <action>
    Create `branded-type.md` on the D-01 template. `Correspondence: dissolves-from -> [Replace Type Code with Class](../kerievsky-catalog/replace-type-code-with-class.md#replace-type-code-with-class)`.
    Do NOT list a fowler-catalog link here (Fowler participates via README rows only, D-10). Paraphrased ## Idiom; a
    tsc-strict-clean branded/opaque-type Example; single un-split ## When each fits. ASCII-only.
  </action>
  <acceptance_criteria>
    - branded-type.md exists; check-functional C1 recognizes it; filename == slug
    - `node extract-samples.mjs` exits 0
    - Correspondence: links ONLY to the kerievsky Replace Type Code with Class leaf (no fowler link)
    - node tools/check-hygiene.mjs exits 0; node tools/check-crossrefs.mjs shows no new dangling link
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-crossrefs.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>branded-type compiles, links only to its kerievsky served leaf, passes hygiene; 13 dissolves-from leaves now exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown reference content only -- no runtime/network/user-input surface. Boundaries are
repo-hygiene / licensing invariants.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | committed leaves must be ASCII-only, work-email-free, no verbatim book prose/code |
| research artifact -> leaf prose | paraphrase the artifact's near-verbatim source quotes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-IP | Information Disclosure | the 7 leaf files | mitigate | DST-04 paraphrase discipline on every ## Idiom; check-hygiene no-verbatim heuristic. |
| T-08.2-EM | Information Disclosure | committed files | mitigate | Work-email allowlist enforced by check-hygiene. |
| T-08.2-SC | Tampering | typescript@6.0.3 | accept | No new installs; compilation uses the pinned tsc. |
</threat_model>

<verification>
- `node extract-samples.mjs` exits 0 (all 7 Examples compile tsc --strict).
- `node tools/check-hygiene.mjs` exits 0.
- `node tools/check-crossrefs.mjs` no new dangling links.
- check-functional advances on C1 (13/19 idiom leaves recognized); overall RED until plans 05/06.
</verification>

<success_criteria>
Seven 1:1 dissolves-from leaves exist on the locked template, single-paragraph When-each-fits,
every Example tsc-strict-clean, every ## Idiom paraphrased; 13 dissolves-from leaves complete.
</success_criteria>

<output>
Create `.planning/phases/08.2-functional-catalog-inserted/08.2-03-SUMMARY.md` when done
(record A2/A7).
</output>
