---
phase: 08-kerievsky-catalog-refactoring-to-patterns
plan: 04
type: execute
wave: 2
depends_on: [08-01]
branching_strategy: none
tags: [kerievsky-catalog, refactoring, skill-authoring, oracle-clean-room, typescript, generalization]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-composite.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-one-many-distinctions-with-composite.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-hard-coded-notifications-with-observer.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/unify-interfaces-with-adapter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-adapter.md
  - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md
autonomous: false
requirements: [KRV-01, KRV-02, KRV-04]
must_haves:
  truths:
    - "D-01: the Ch.8 Generalization refactorings are authored as per-refactoring leaf files kerievsky-catalog/<slug>.md behind the thin index, mirroring the LOCKED Fowler per-refactoring-leaf model; SKILL.md is NOT re-touched"
    - "D-02: each Generalization leaf carries the LOCKED Fowler contract (single # <Name> H1 anchor + Use when: + ## Motivation (intent leads) + ## Mechanics + ## Example + optional ## Watch for) PLUS the three Kerievsky field labels in lockstep with check-kerievsky: 'Composed Fowler primitive(s):' (>=1 ../fowler-catalog/<slug>.md#<slug> cross-link), 'Direction:', 'GoF pattern:'"
    - "D-03: every leaf carries a Direction value (all Towards for this chapter -- no de-patterning cases here); oracle-reviewer settles To-vs-Towards"
    - "D-04: each leaf's 'GoF pattern:' field names the target pattern for vocabulary only (Template Method / Composite / Observer / Adapter / Interpreter) with NO reproduced GoF prose or code (DST-04)"
    - "D-06: each example is re-rendered from the book's Java into ONE compact original TS before/after (5-15 lines/side, hard cap 2), original domain + identifiers, tsc --strict-clean via extract-samples; Replace Implicit Language with Interpreter is the primary overflow candidate -- use the LOCKED overflow rule (push an evolving <slug>-walkthrough.md, explain-in-depth only, ALSO compiled) and keep the compact before/after in the leaf"
    - "D-07: each leaf is authored BLIND, gated by the oracle-reviewer subagent over .oracle/refactoring-to-patterns/index.md scoped to 'Ch.8 Generalization', revised BLIND to convergence; the main context NEVER reads .oracle/ (ls for names only)"
    - "D-08: this plan runs INLINE in the MAIN context (Agent + AskUserQuestion tools), NOT via gsd-executor and NOT in a worktree (branching_strategy: none); the oracle agents are read-only so there is no file conflict"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md"
      provides: "Form Template Method leaf (GoF: Template Method)"
      contains: "Direction:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md"
      provides: "Replace Implicit Language with Interpreter leaf (GoF: Interpreter; overflow-walkthrough candidate)"
      contains: "GoF pattern:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/pull-up-method.md"
      via: "composed Fowler primitive cross-link (resolving H1 anchor)"
      pattern: "fowler-catalog/[a-z-]+\\.md#"
---

<objective>
Author the Ch.8 Generalization chapter (7 Kerievsky pattern-directed refactorings) as
per-refactoring catalog leaves using the clean-room blind-author -> oracle-reviewer-gate ->
converge loop. All Towards-direction; Replace Implicit Language with Interpreter is the primary
overflow-walkthrough candidate.

Purpose: fill KRV-01/02/04 for the Generalization chapter with contract-complete, oracle-converged,
tsc --strict-clean leaves that carry the three Kerievsky fields. File-disjoint by owning only this
chapter's leaf files.

Output: the 7 Ch.8 Generalization leaf files (+ optional walkthrough for Interpreter) under
references/kerievsky-catalog/ (the thin index README.md is finalized later, in 08-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md

<interfaces>
CLEAN-ROOM FIREWALL (D-07): the executor/main context NEVER opens any file under .oracle/ -- `ls`
for names only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` gates a drafted leaf. Input: DRAFT_PATHS (this chapter's leaves + any
  walkthrough), SOURCE = ".oracle/refactoring-to-patterns/index.md" + "Ch.8 -- Generalization"
  (index.md + chapter number/topic; NEVER a book chapter filename), CONTENT_TYPE
  "refactoring-leaf", SCOPE = each leaf's refactoring, canonical per-axis anchors PLUS direction /
  composed-primitive-aptness / target-pattern axes. Returns pass|revise|blocked|error.
- `oracle` for open-ended chapter-membership / composed-primitive lookups.
7 leaves is at the upper edge of one sub-batch -- one reviewer call is fine; cap ~3 rounds;
escalate to Task 2. Fan-out guard: do NOT persist a full ordered name+chapter map.

Ch.8 Generalization members + orientation (ASSUMED -- oracle settles direction/GoF/composed-primitives):
  13. Form Template Method -- GoF: Template Method; Towards; pull-up-method, extract-function,
      slide-statements, change-function-declaration.
  14. Extract Composite -- GoF: Composite; Towards; extract-superclass, pull-up-field, pull-up-method.
  15. Replace One/Many Distinctions with Composite -- GoF: Composite; Towards; extract-class,
      change-function-declaration, encapsulate-collection. (slug drops the slash:
      replace-one-many-distinctions-with-composite)
  16. Replace Hard-Coded Notifications with Observer -- GoF: Observer; Towards; extract-class,
      move-function, change-function-declaration.
  17. Unify Interfaces with Adapter -- GoF: Adapter; Towards; extract-superclass,
      change-function-declaration.
  18. Extract Adapter -- GoF: Adapter; Towards; extract-class, move-function, extract-superclass.
  19. Replace Implicit Language with Interpreter -- GoF: Interpreter; Towards; extract-class,
      replace-conditional-with-polymorphism, move-function. (pattern-heavy -- overflow-walkthrough)

Leaf field LABELS (lockstep with check-kerievsky -- Pitfall 4): "Composed Fowler primitive(s):",
"Direction:", "GoF pattern:". Composed-primitive links target ../fowler-catalog/<slug>.md#<slug>.
Overflow rule (D-06, YAGNI): a <slug>-walkthrough.md ONLY where the compact before/after loses the
lesson (Interpreter first); extract-samples compiles walkthrough fences too.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Ch.8 Generalization leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-composite.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-one-many-distinctions-with-composite.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-hard-coded-notifications-with-observer.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/unify-interfaces-with-adapter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-adapter.md, plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- refactoring leaf + overflow rule)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", "Canonical per-axis anchors", "Driver responsibilities")
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md (blind drafts fold sub-steps; too_close_to_source re-domain; no-DOM tsc; cross-ref aptness; Pitfall 5 pattern-heavy overflow)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-01, D-02, D-03, D-04, D-06, D-07, D-08)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md (leaf skeleton + 3-field placement + walkthrough shape)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md (leaf-contract exemplar)
    - .oracle/ (ls ONLY for filenames -- NEVER open any file under .oracle/ from this context)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not). Do
    NOT open any file under .oracle/ from this context. Run this loop INLINE in the main context
    (D-08) -- NOT via gsd-executor, NOT in a worktree.

    Author the 7 Ch.8 Generalization leaves (one file per refactoring, kebab-case slug = filename;
    Replace One/Many Distinctions with Composite drops the slash in its slug) via the core
    clean-room loop:
    (1) AUTHOR BLIND in the LOCKED leaf format: single `# <Name>` H1, one-line `Use when:`,
    `## Motivation` (intent leads, original words), `## Mechanics` (4-8 verb-first numbered steps,
    original words, naming the test/compile checkpoint inline), `## Example` (ONE compact
    tsc --strict-clean TS before->after, 5-15 lines/side, hard cap 2; pure -- no DOM/console;
    original domain + identifiers). THEN add the three Kerievsky field lines with the EXACT labels:
    `Composed Fowler primitive(s):` (>=1 ../fowler-catalog/<slug>.md#<slug> link), `Direction:`
    (Towards), `GoF pattern:` (name only). For Replace Implicit Language with Interpreter (and any
    other leaf whose teaching lives in intermediate states) apply the overflow rule: keep the
    compact before/after in the leaf and push the evolving multi-step example to
    <slug>-walkthrough.md (explain-in-depth only; pure + tsc-clean). Both before AND after compile.
    ASCII only.
    (2) GATE: spawn `oracle-reviewer` with DRAFT_PATHS (the 7 leaves + any walkthrough), SOURCE
    ".oracle/refactoring-to-patterns/index.md" + "Ch.8 -- Generalization", CONTENT_TYPE
    "refactoring-leaf", SCOPE (each leaf's refactoring), the canonical + pattern-directed axes. Use
    `oracle` for open-ended lookups.
    (3) REVISE BLIND from the directives; on too_close_to_source, fully re-domain the example blind
    and re-gate.
    (4) ITERATE to convergence (every leaf `pass`, or owner-accepted `blocked`); cap rounds ~3 and
    escalate to Task 2. Fan-out guard.
    (5) Run the deterministic layer (extract-samples tsc + check-hygiene + the field-presence rg
    checks) and commit on green. Do NOT edit kerievsky-catalog/README.md or any checker .mjs; the
    full check-kerievsky + crossrefs run at the 08-06 gate.
  </action>
  <acceptance_criteria>
    - All 7 Ch.8 leaf files exist; each has a single `# <Name>` H1, `Use when:`, `## Motivation`, `## Mechanics`, `## Example` with >=1 ts fence, and the three field labels (`rg -q "^Composed Fowler primitive\(s\):" <leaf> && rg -q "^Direction:" <leaf> && rg -q "^GoF pattern:" <leaf>` per leaf).
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` exits 0 (every Ch.8 example -- leaf + any walkthrough -- is tsc --strict-clean).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
    - Every leaf's `Composed Fowler primitive(s):` line links at least one ../fowler-catalog/<slug>.md#<slug> target that exists on disk.
    - Each leaf reached oracle-reviewer `pass` (or owner-accepted `blocked` via Task 2), recorded in the SUMMARY; no file under .oracle/ was opened by this context.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "^GoF pattern:" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-implicit-language-with-interpreter.md && rg -q "^Direction:" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/form-template-method.md && rg -q "^Composed Fowler primitive\(s\):" plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/extract-adapter.md</automated>
  </verify>
  <done>All 7 Ch.8 Generalization leaves exist as contract-complete files with the three Kerievsky fields; Interpreter uses the overflow-walkthrough where warranted; every example (leaf + walkthrough) is tsc --strict-clean and behavior-preserving; each oracle-reviewer verdict is pass (or owner-accepted blocked); check-hygiene green; README.md and checkers untouched; no .oracle/ file opened by this context.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Owner escalation for non-converging / blocked Ch.8 leaves (conditional)</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("Driver responsibilities" -- loop bound + oscillation guard + owner escalation)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-04 GoF AskUserQuestion fallback; D-07 escalation path)
  </read_first>
  <decision>How to resolve any Ch.8 leaf the oracle-reviewer loop cannot converge within ~3 rounds, returns as a blocked ambiguity, or whose GoF target the Kerievsky oracle cannot settle (D-04 fallback)?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate. The driver caps rounds (~3) and escalates that ENTRY to the owner via
    AskUserQuestion. GoF is not in .oracle/, so an unresolvable GoF target for a specific leaf is
    also an owner question (D-04 fallback). Fires ONLY for entries that do not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked entry as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (mechanics / direction / GoF target), then re-run the loop once</name>
      <pros>Resolves a genuine fidelity or GoF-target gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked, or have an
    unresolvable GoF target): open an AskUserQuestion with the entry, the reviewer's
    ambiguities/unable-to-verify axes (own words), and the two options; apply the choice; re-run the
    deterministic layer; record it in the SUMMARY. Do NOT reveal or request verbatim source prose.
  </action>
  <acceptance_criteria>
    - Every Ch.8 leaf is oracle-reviewer `pass` or owner-adjudicated; each escalation (or a no-op note if none) is recorded in 08-04-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated entry</resume-signal>
  <done>Every Ch.8 leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-to-patterns/` (owner full-text book, git-ignored) -> authored public-repo leaves | copyrighted Kerievsky expression must never cross; the ONLY crossing is via the oracle/oracle-reviewer subagents (isolated context, own-words verdicts, no prose/code/paths) |
| GoF (owner-held, NOT in .oracle/) -> authored leaves | only the pattern NAME (vocabulary) crosses via the GoF pattern field; NO GoF prose or code (DST-04) |
| authored files -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure / legal (DST-04) | authored leaf/walkthrough prose + example code + GoF vocabulary | mitigate | Clean-room firewall: main context NEVER reads .oracle/; authored BLIND; oracle-reviewer full-strength near-verbatim gate; GoF name-only; check-hygiene no-verbatim WARN + Phase-10 scan |
| T-08-04 | Tampering (fabricated / unfaithful content) | mechanics / motivation / direction / GoF target / composed-primitive fidelity | mitigate | oracle-reviewer fidelity gate (+ direction / composed-primitive-aptness / target-pattern axes) + converge loop; driver caps rounds (~3) and escalates to owner (Task 2) |
| T-08-05 | Information Disclosure / firewall | main context accidentally reading .oracle/ prose (incl. fan-out aggregation) | mitigate | firewall discipline (ls only); INLINE main-context orchestration; driver dedupes/caps, does NOT persist a full ordered name+chapter map |
| T-08-06 | Tampering | a resolvable-but-wrong composed-Fowler cross-ref | mitigate | check-crossrefs (resolution, at 08-06 gate) + oracle-reviewer composed-primitive-aptness axis |
| T-08-02 | Information Disclosure | work email in a committed leaf | mitigate | check-hygiene work-email allowlist gate |
| T-08-03 | Tampering (integrity) | non-ASCII / mojibake in a leaf/walkthrough | mitigate | check-hygiene ASCII hard-fail; ASCII-only authoring |

No UI, no network, no secrets, no new packages. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- extract-samples (tsc --strict) GREEN over all Ch.8 examples + walkthroughs; check-hygiene GREEN; each leaf has the full contract + the 3 Kerievsky fields.
- Every Ch.8 leaf reached oracle-reviewer pass (or owner-accepted blocked).
- kerievsky-catalog/README.md and checker .mjs untouched; no .oracle/ file opened by this context.
</verification>

<success_criteria>
- All 7 Ch.8 Generalization refactorings are contract-complete, oracle-converged, tsc --strict-clean leaves with the three Kerievsky fields (KRV-01/04) and correct Direction (KRV-02); Interpreter uses an overflow-walkthrough where warranted.
- The plan owns only this chapter's leaf files (file-disjoint from sibling wave-2 plans); composed-Fowler cross-refs resolve at the 08-06 gate.
</success_criteria>

<output>
Create `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-04-SUMMARY.md` when done, recording the confirmed Ch.8 membership, per-leaf round counts, the settled directions/GoF targets, which leaves used a walkthrough, and any owner escalations.
</output>
