---
context: research
phase: lz-refactor-skill-improvement (post Phase-12 e2e)
task: L1 accepted; output axis closed
status: paused
last_updated: 2026-07-14
---

# lz-refactor skill-improvement -- L1 ACCEPTED, output-warrant axis CLOSED

## Where this landed (2026-07-14)

The L1 rewrite of SKILL.md step 4 into ONE countable net-cost warrant (committed `98cf482`) is
**ACCEPTED and validated**:

- **gr4 re-measure** (kata, apply, `--force`, invoked via `--plugin-dir`=committed L1, k=3):
  **0/3 over-engineered hierarchies, 3/3 explicit countable DECLINE, 3/3 routed the Conjured
  behaviour-change trap to lz-tpp, tests green.** Fixes iteration-1's 1/3 over-build regression.
  Base `no_skill` gr4 is also 0/3 -> L1 = parity + a legible warrant, not an output win over base.

- **Positive-control axis CLOSED (conceptually foreclosed).** Step-4's REMOVES side has NO
  extensibility/growth term BY DESIGN (worked example: "4 type codes -> polymorphism adds 4 classes,
  removes 0 duplication -> DECLINE"). So a faithful L1 must decline a speculative-growth pattern; if it
  endorses, it overrode step-4 with base instinct -> a PASS is indistinguishable from "skill inert."
  The probe can only show the warrant harmless or harmful, never HELPFUL. Declining patterns justified
  only by future growth is intended YAGNI / anti-Speculative-Generality, not a bug.

- **Two probes built + unbiased-reviewed (neutral + adversarial) + disqualified BEFORE any spend:**
  (B) `@nx/vite` `add*Target` -- vacuous behaviour gate (`configuration.spec.ts` uses `addPlugin:true`
  everywhere, skipping the `!hasPlugin` block that calls them; no test executes them) + break-even
  net-cost (DECLINE defensible) + wrong path (step-3 Fowler, not L1's step-4). (A) kata grown-type-set
  -- foreclosed as above. p2/validateEntry reuse also disqualified (already run; base declines on taste;
  same family as the worked example). The unbiased-review gate paid off 3x this turn.

Findings recorded in `.claude/skills/lz-refactor-workspace/e2e-gilded-rose/GR-RESULTS.md` + memory
`lz-refactor-output-warrant-axis-exhausted`.

## Remaining work

1. **OPTIONAL -- reference-catalog eval** (the one untested VALUE lever): on-demand explain/lookup
   (explain a refactoring / Direction / de-patterning; recommend mode; no repo), with_skill vs no_skill,
   where a curated oracle-verified catalog could beat base recall. New instrument -> unbiased-review the
   prompt set first; honor eval-run-approval-gate for the metered run. DEFERRED.
2. Cleanup done this turn (borrowed repos restored; worktrees removed; raw results discarded --
   findings live in the committed docs, per the "results never committed" convention).

## Do NOT re-open
- The positive-control quest (conceptually foreclosed above).
- A same-model reviewer-agent / hook (research-disqualified: shared blind spots; escalation, if ever,
  is deterministic-count or cross-family -- and residual over-engineering is 0/3, so unwarranted).
- Do not re-run `no_skill` for existing prompts; only for NEW query prompts (user rule).

## Infrastructure state (post-cleanup)
- L1 committed `98cf482` on branch `gsd/lz-tdd-0.0.2-lz-refactor`.
- Borrowed repos restored to pristine: nx `D:/projects/github/nrwl/nx` @ `23.0.x`;
  kata `D:/projects/github/emilybache/GildedRose-Refactoring-Kata` @ `main`. Throwaway branches +
  stashes dropped; `lz-plugins-baseline` / `lz-plugins-oldold` worktrees removed.
- e2e harness (suites, prompts, targets, run-e2e.mjs, checkers, findings docs) stays committed;
  raw `results/` run trees are local-only byproducts (0 ever tracked; `outputs/` gitignored).

## Next action
Optional: build the reference-catalog eval (start with the instrument + unbiased review; halt before the
metered run). Otherwise the milestone work resumes: Phase 12 is superseded by this loop -- reconcile
Phase-12 status and continue the lz-tdd@0.0.2 roadmap (phases 6-11; 10/11 remain per ROADMAP).
