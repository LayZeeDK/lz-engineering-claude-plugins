---
context: phase
phase: 08-kerievsky-catalog-refactoring-to-patterns
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-07-06T14:30:00.000Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Discovered through failure (Phase 7). Acknowledge each explicitly before proceeding.

- [ ] CONSTRAINT: INLINE main-context orchestration (D-08) -- the Kerievsky authoring loop MUST run in the MAIN context, NOT via `gsd-executor`, NOT in a worktree -- because it needs the `Agent` tool (to spawn `oracle`/`oracle-reviewer`) and `AskUserQuestion` (owner escalation), which `gsd-executor` lacks. Structural mitigation: drive author -> deterministic-check -> oracle-gate -> revise -> converge yourself; set no worktree; branching_strategy is none.
- [ ] CONSTRAINT: CLEAN-ROOM FIREWALL (DST-04) -- the main context NEVER reads any file under `.oracle/` (only `ls`/`wc` for names/sizes). Only the isolated `oracle`/`oracle-reviewer` subagents read book prose; their verdicts carry no source expression. Structural mitigation: reach book facts ONLY through those subagents.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd-executor for authoring plans | Spawning gsd-executor for 08-03..08-06 fails: it cannot spawn oracle agents or ask the owner. This is how Phase 7 first failed. | blocking | Run the clean-room loop INLINE in the main context; the plans set branching_strategy: none + autonomous: false. |
| Main context reads `.oracle/` prose | Reading the copyrighted book into the main context would leak it into the world-readable repo (DST-04). | blocking | Only oracle/oracle-reviewer read `.oracle/`; main context uses `ls`/`wc` (names/sizes) only. |
| oracle-reviewer truncates a long chapter | Read tool caps ~25K tok; Ch.7 (~40K) and Ch.8 (~39K) exceed it. A single Read drops the tail -> false source-only / missed drops. | advisory (FIXED) | Fixed in the agent contracts (always chunk-read to EOF, advance by actual lines, stop on zero) + RELOADED live. Still sanity-check big-chapter verdicts for incomplete-read signatures. |
| Editing an agent/skill instruction without review | Instruction files are behavioral contracts; a subtle edit can break the firewall or a step reference. | advisory | STANDING RULE: agent-review any agent/skill instruction change before accepting; after review+commit, escalate to owner to run /reload-plugins and wait. |

<current_state>
Phase 8 (Kerievsky Catalog) at the FINAL gate. Wave 1 (harness) + Wave 2 plans 08-02..08-05 are DONE and
committed; ALL 27 catalog leaves are authored + oracle-converged (+ 3 overflow walkthroughs: State,
Interpreter, Move Accumulation to Visitor). Next and LAST plan is 08-06 (the phase gate). Owner is pacing
one plan per session (checked in after 08-05).

LOCKED Refactoring Directions convention (owner-approved this session; see 08-REFACTORING-DIRECTIONS.md):
the book's Refactoring Directions table (Inside Front Cover) is the AUTHORITATIVE Direction source and
outranks oracle chapter-prose. Values: `To` = full pattern; `To/Towards` (compound) = both-listed dual
placement; `Away` = de-pattern; `n/a` = table-absent utility. Settled via 3 oracle direction-inference
consults + a unanimous 6-member Opus board (peer-deliberated, no chair). 08-05 already set its OWN leaves
(3 To-only -> `To`; Visitor -> `Away`; 4 utilities -> `n/a`); the COMMITTED Ch.6/7/8 leaves are reconciled
at 08-06 (see remaining_work).

AGENT CHANGE THIS SESSION (committed 9a877d7, RELOADED): oracle + oracle-reviewer were made permanently
aware of the Refactoring Directions table as the authoritative direction source. Agent-reviewed
(firewall+correctness PASS; consistency PASS after fixes). No further /reload-plugins pending.

08-04 provenance lesson (still applies at 08-06): the `Composed Fowler primitive(s)` field cross-links to
our Fowler 2ND-ed catalog; map Kerievsky's 1st-ed primitive names (Extract Method->Extract Function, etc.;
"Extract Interface" -> Change Function Declaration / Extract Superclass). Pass this to oracle-reviewer for
any smell-fold candidate cross-refs.
</current_state>

<completed_work>

- 08-01 harness extension -- DONE (585552c). check-kerievsky.mjs (RED baseline 0/27 by design); extract-samples/check-crossrefs/check-smells extended, GREEN.
- 08-02 Ch.6 Creation (6 leaves) -- DONE (7629ab6). Oracle-converged 6/6 (R1 1 pass/5 revise, R2 5 pass, 0 owner escalations). 2 of the 3 Away cases here (Encapsulate Composite with Builder, Inline Singleton).
- 08-03 Ch.7 Simplification (6 leaves) -- DONE (fa72f8e). Oracle-converged 6/6 (R1 0 pass/6 revise, R2 6 pass, 0 owner escalations). All Towards. One overflow walkthrough (State only). See 08-03-SUMMARY.md.
- 08-04 Ch.8 Generalization (7 leaves) -- DONE (6d9478d). Oracle-converged 7/7 (Extract Composite R1 pass; five leaves R2; Form Template Method R3), 0 owner escalations. GoF Template Method/Composite x2/Observer/Adapter x2/Interpreter. One overflow walkthrough (Interpreter). See 08-04-SUMMARY.md. (Directions To/To-only reconciled at 08-06 per the locked convention.)
- 08-05 Ch.9+10+11 (8 leaves) -- DONE (8243563). Oracle-converged per-chapter (R1->R2, 0 blocked). Completes ALL 27 leaves. Move Accumulation to Visitor = Away (from Iterator) + walkthrough (3rd de-patterning case). Directions settled against the authoritative Refactoring Directions table; the 4 table-absent utilities = `n/a` sentinel (6-member Opus board). See 08-05-SUMMARY.md + 08-REFACTORING-DIRECTIONS.md.
- oracle/oracle-reviewer Refactoring-Directions-table awareness -- DONE (9a877d7); agent-reviewed (firewall PASS + consistency PASS after fixes); RELOADED.
- oracle/oracle-reviewer chunked-read fix -- DONE (88f1513 then corrected a01f51b); agent-reviewed (firewall SAFE + correctness PASS); RELOADED.
- RUBRIC LOCK + VALIDATION (this session, all committed):
  - 08-ORACLE-MODEL.md created (efd2fb5) -- locks the 3 Kerievsky anchors (direction / composed-primitive / target-pattern) + the RETARGETED spirit/judgment axis (owns ONLY the over-application counterweight; applicability barred from it) + example/walkthrough clause + mechanics/composed-primitive split. Agent-reviewed by 2 subagents (consistency + unbiased); their fixes applied.
  - Pilot-hardened the spirit axis (0e5acbe) -- A/B/C gate returned correct/partial/wrong in step; closed the spirit<->applicability double-score.
  - Spirit-only re-gate of all 12 shipped Kerievsky leaves (ea87b12) -- 12/12 correct after one blind fix (Command gained an over-application counterweight; its old Watch-for was behavior-preservation only). Closed the pre-retarget two-tier gap.
</completed_work>

<remaining_work>

- 08-06 (phase GATE) -- NOT started:
  - Ch.4 smell fold + dedup (source-tagged) into smells.md/smells/ (KRV-03), via the clean-room oracle loop (smell-leaf axes; per-smell dedup map oracle-settled against .oracle/ Ch.4).
  - Finalize the kerievsky-catalog README index (27 rows + Direction annotations) + ship a SINGLE shared Direction gloss line (`To`/`To/Towards`/`Away`/`n/a` meanings) -- NOT per-leaf.
  - DIRECTION RECONCILIATION (per 08-REFACTORING-DIRECTIONS.md, owner-approved): compound `To/Towards` on the 6 both-listed committed leaves (Unify Interfaces with Adapter, Replace Conditional Dispatcher with Command, Move Embellishment to Decorator, Replace Hard-Coded Notifications with Observer, Replace State-Altering Conditionals with State, Replace Conditional Logic with Strategy); 11 committed To-only leaves `Towards` -> `To`; verify utilities `n/a` + Visitor `Away`.
  - check-kerievsky: reconcile NAMES to authored slugs AND widen the Direction allow-list (+`n/a` 4th token, +compound `To/Towards` leading-token); keep the coach's `Away` routing untouched. Then FULL battery GREEN (check-kerievsky flips green) + `claude plugin validate`.
- Phase close: verify_phase_goal -> secure-phase -> validate-phase -> extract-learnings.
</remaining_work>

<decisions_made>

- Execution is INLINE in the main context (D-08); no gsd-executor, no worktrees.
- oracle/oracle-reviewer always chunk-read `.oracle/` to EOF (length-agnostic); fix is in the agent contracts, invocations pass NO sizes.
- STANDING RULE: agent/skill instruction changes must be agent-reviewed before acceptance.
- STANDING RULE: after an oracle/agent change is reviewed+committed, escalate to owner to run /reload-plugins and WAIT.
- Oracle-settled Ch.6 GoF targets: Move Creation Knowledge to Factory + Encapsulate Classes with Factory target a plain Factory (NOT Abstract Factory).
- RETARGETED spirit/judgment axis (08-ORACLE-MODEL.md): it owns ONLY the over-application counterweight (a pattern-specific guard against applying the pattern speculatively; for Away leaves, why the pattern was premature). Applicability is BARRED from scoring that guard; direction owns the label; motivation owns the reasons. EVERY Ch.8-11 leaf MUST carry this counterweight (a behavior-preservation caveat does NOT count -- that is what tripped Command).
- The 3 Kerievsky anchors are LOCKED driver-side; pass them on EVERY remaining chapter (read 08-ORACLE-MODEL.md first).
- The composed-primitive-field <-> Mechanics deterministic check is DEFERRED to Phase 11 (a subset check would not catch the semantic co-drift the oracle axis already handles).
- Do NOT reopen committed, converged leaves on a lone adversarial re-pass (oracle-reviewer oscillates on mechanics/composed-primitive); re-gate ONLY changed axes, as the spirit-only re-gate did.
</decisions_made>

<blockers>
- None technical. Open: re-confirm pacing for the remaining 4 plans on resume.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md` -- D-01..D-08 (authoritative decisions).
2. `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md` -- LOCKED leaf/index/harness contracts to mirror.
3. `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md` -- clean-room loop, per-axis anchors, driver responsibilities.
   3a. `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-ORACLE-MODEL.md` -- Kerievsky addendum (driver-side, agent-reviewed): the per-axis anchors (direction / composed-primitive / target-pattern) + the retargeted spirit/judgment axis + mechanics/composed-primitive split; pass these every remaining chapter.
4. `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-LEARNINGS.md` -- blind-draft pitfalls (folded sub-steps; too_close re-domain; no-DOM tsc; cross-ref aptness).
5. The next plan `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-04-PLAN.md` (then 08-05/06) -- exact members, fields, and per-plan threat model.
6. `plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-conditional-with-polymorphism.md` -- leaf-shape exemplar.
7. Auto-memory: oracle-reviewer-large-chapter-read-cap; agent-skill-instruction-changes-need-review; reload-plugins-after-oracle-agent-changes.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: authoring via gsd-executor -> run INLINE in the main context (D-08).
- ANTI-PATTERN: main context reading `.oracle/` prose -> `ls`/`wc` only; book prose only via oracle agents.
- ANTI-PATTERN: passing chapter sizes/line-numbers into oracle invocations -> the reloaded agent self-chunks to EOF; invocations stay generic (SOURCE = index.md + chapter number/topic).
- ANTI-PATTERN: reopening committed, converged leaves on a lone adversarial oracle re-pass (it oscillates on mechanics/composed-primitive, contradicting its own R2 pass) -> honor the round cap + converged verdict; re-gate ONLY a changed axis (as the spirit-only re-gate did).
- ANTI-PATTERN: authoring a Ch.8-11 leaf without a pattern-specific over-application counterweight in `## Watch for` (the retargeted spirit obligation) -> a behavior-preservation caveat does not satisfy it.

## Infrastructure State
- oracle + oracle-reviewer agents: LIVE + RELOADED. This session they were made aware of the Refactoring Directions table (Inside Front Cover) as the authoritative Direction source (committed 9a877d7; agent-reviewed; reloaded). Base always-chunk-to-EOF contract (a01f51b) intact. NO /reload-plugins pending.
- Direction convention: LOCKED in 08-REFACTORING-DIRECTIONS.md (owner-approved). Authoritative source = the book's Refactoring Directions table. 08-06 applies the committed-leaf reconciliation + widens check-kerievsky's Direction allow-list (+`n/a`, +compound).
- Rubric reference: `08-ORACLE-MODEL.md` (the Ch.4 smell fold uses the SMELL-LEAF axes, not the retargeted spirit).
- Harness (non-shipped, `.claude/skills/lz-refactor-workspace/`): check-kerievsky RED-by-design (needs 08-06 README rows + NAMES reconcile + Direction allow-list widen); extract-samples 185 modules tsc --strict GREEN; check-hygiene/check-crossrefs/check-smells GREEN.
- Working tree: clean except `.planning/config.json` (auto-chain runtime flag -- do NOT commit).
- `.oracle/refactoring-to-patterns/` fully provisioned (Ch.4 smells + Ch.6-11 catalog + index.md + Inside Front Cover Refactoring Directions table).

<context>
Phase 8 is a near-mechanical clone of Phase 7's shipped-green clean-room model, applied to the
Kerievsky book. Per chapter: author the leaves BLIND (public knowledge + the plan's orientation, never
`.oracle/`) in the LOCKED Fowler leaf format + the 3 Kerievsky fields; run the deterministic layer
FIRST (cheap: tsc + hygiene + field presence + link resolution); then gate the chapter's leaves in one
oracle-reviewer call; revise blind from the directives; converge to all-pass (~3 round cap); escalate
only non-convergence/unresolvable-GoF to the owner via AskUserQuestion. Commit the chapter + its
SUMMARY. Examples must be tsc --strict clean, pure (no DOM/console), behavior-preserving, in domains
unrelated to the source with original identifiers (dodge too_close_to_source). The 3 Kerievsky field
labels are lockstep with check-kerievsky: `Composed Fowler primitive(s):` (>=1 ../fowler-catalog/<slug>.md#<slug>),
`Direction:` (To/Towards/Away), `GoF pattern:`.
</context>

<next_action>
Continue INLINE (D-08). 08-06 is the phase GATE (last content plan). Read 08-06-PLAN.md +
08-REFACTORING-DIRECTIONS.md (the locked direction convention) + 08-ORACLE-MODEL.md FIRST. Tasks:
1. Ch.4 smell fold (KRV-03): fold Kerievsky's Ch.4 smells into smells.md/smells/ with source tags, deduped
   against Fowler's 24, via the clean-room loop (author blind -> deterministic -> oracle-reviewer
   SMELL-LEAF gate [candidates/recognition/motivation/applicability/spirit -- NOT the retargeted spirit] ->
   converge). The per-smell dedup map is oracle-settled against .oracle/ Ch.4.
2. Direction reconciliation (owner-approved, see 08-REFACTORING-DIRECTIONS.md): compound `To/Towards` on the
   6 both-listed committed leaves; 11 committed `Towards` -> `To`; verify utilities `n/a` + Visitor `Away`.
   Trivial one-line field edits on COMMITTED leaves (08-06 owns cross-leaf reconciliation).
3. check-kerievsky: reconcile NAMES to authored slugs AND widen the Direction allow-list (+`n/a`,
   +compound `To/Towards` leading-token). Editing a .mjs checker is EXEMPT from the agent-review rule
   (normal verify path). Then run the FULL battery (check-kerievsky flips GREEN) + `claude plugin validate`.
4. Finalize the kerievsky-catalog README index + ship the SINGLE shared Direction gloss line.
Commit + 08-06-SUMMARY. Then phase close: verify_phase_goal -> secure-phase -> validate-phase ->
extract-learnings. Owner paces one plan per session -- 08-06 is the last content plan.
</next_action>
