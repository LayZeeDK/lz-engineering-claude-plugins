---
phase: 08.2-functional-catalog-inserted
plan: 02
type: execute
wave: 2
depends_on: [08.2-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/first-class-function.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/thunk-and-lazy-value.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/function-composition.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/discriminated-union-and-fold.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/module-namespace.md
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/factory-function.md
autonomous: true
requirements: [FUN-01, FUN-02]

must_haves:
  truths:
    - "Six N:1 dissolves-from idiom leaves exist on the locked template, each with a paraphrased ## Idiom, a tsc-strict-clean Before->After ## Example, and a ## When each fits with exactly one `### <Pattern>` sub-heading + one line per served pattern"
    - "Every ts fence sits INSIDE ## Example and compiles under npm run typecheck; every Correspondence: value is dissolves-from with links to the served gof/kerievsky/extra leaves"
    - "Each ## Idiom one-liner is original wording -- not the Norvig/Wlaschin phrasing the research artifact quotes"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/discriminated-union-and-fold.md"
      provides: "The canonical N:1 leaf serving Visitor/State/Interpreter/Composite (+ Kerievsky ADT refactorings)"
      contains: "Correspondence: dissolves-from"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/function-composition.md"
      provides: "N:1 leaf serving Decorator/CoR/Template Method/Builder + Composed Method/Collecting Parameter"
      contains: "## When each fits"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/first-class-function.md"
      provides: "N:1 leaf serving Strategy/Bridge/Adapter (1-method) + the Kerievsky Strategy/Adapter refactorings"
      contains: "Keep the OO form when:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/discriminated-union-and-fold.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/visitor.md"
      via: "Correspondence: dissolves-from link + a ### Visitor residual anchor"
      pattern: "gof-catalog/visitor.md#visitor"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/first-class-function.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/strategy.md"
      via: "Correspondence: dissolves-from link"
      pattern: "gof-catalog/strategy.md#strategy"
  decisions: |
    - **D-01:** each leaf uses the locked template -- Use when: / Correspondence: dissolves-from -> [links] / Keep the OO form when: header, then ## Idiom / ## Example (Before -> After tsc-strict-clean + a one-line "mechanics: <cross-link>, run tests between steps") / ## When each fits; NO duplicated ## Mechanics section
    - **D-02:** organized BY IDIOM -- these 6 leaves each serve multiple OO patterns (e.g. discriminated-union-and-fold serves Visitor/State/Interpreter/Composite), not one leaf per pattern
    - **D-06:** no owned oracle -- the research artifact + check-functional + tsc --strict are the correctness anchor; DST-04 clean-room (original prose, paraphrase the artifact's Norvig/Wlaschin quotes, original tsc-clean code) still applies
    - **D-13:** the 6 slugs, their dissolves-from Correspondence enum value, and their served-pattern sets are the canonical inventory from RESEARCH.md Deliverable 1
---

<objective>
Author the six N:1 "dissolves-from" idiom leaves -- the heaviest content in the phase,
because each serves several OO patterns and therefore carries per-pattern residual
anchors (C5): first-class-function, thunk-and-lazy-value, function-composition,
discriminated-union-and-fold, module-namespace, factory-function.

Purpose: these are the leaves the N:1 README rows (plan 05) target by intra-leaf anchor,
and the 55 OO leaves (plan 06) link to. They must exist before those plans run.

Output: 6 leaf files on the locked D-01 template, every Example tsc-strict-clean, every
## Idiom paraphrased (DST-04), each served pattern a `### <Pattern>` sub-heading + one line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-functional-catalog-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md

The design source of record for idiom content: `.planning/research/functional-depatterning-ts.md`
Sections 10-11 (the disappearance map + per-pattern idioms; PARAPHRASE, do not quote).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The two ADT / composition mega-leaves (discriminated-union-and-fold, function-composition)</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md ("Canonical Idiom-Leaf Inventory" rows 3 and 4; "The N:1 Coverage Map" GoF/Kerievsky/extra tables; "Pattern: the locked leaf template"; Pitfall 1/2/4)
    - .planning/research/functional-depatterning-ts.md Section 10 Q1 (Visitor/State/Interpreter fold, Template Method HOF, Chain of Responsibility reduce) and Section 11 (verdicts)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/replace-conditional-logic-with-strategy.md (the header + body shape to mirror)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/move-accumulation-to-visitor.md (the Direction: Away metadata-line form the Correspondence: line mirrors)
  </read_first>
  <behavior>
    - discriminated-union-and-fold: a tagged (`kind`/`tag`) union + an exhaustive `switch` folded by a function (with `default: assertNever`). Correspondence: dissolves-from -> Visitor, State, Interpreter, Composite [gof] + Replace State-Altering Conditionals with State, Replace Implicit Tree with Composite, Extract Composite, Replace One/Many Distinctions with Composite, Replace Implicit Language with Interpreter, Move Accumulation to Visitor [kerievsky]. `## When each fits`: one `### Visitor` / `### State` / `### Interpreter` / `### Composite` sub-heading, each one line carrying that pattern's expression-problem flip (new operation cheap vs new case edits every fold).
    - function-composition: compose same-signature functions / a collection pipeline / middleware. Correspondence: dissolves-from -> Decorator, Chain of Responsibility, Template Method, Builder [gof] + Composed Method, Collecting Parameter [extra] + Encapsulate Composite with Builder, Compose Method, Move Embellishment to Decorator, Form Template Method, Move Accumulation to Collecting Parameter [kerievsky]. One `### <Pattern>` residual line per served gof/extra pattern.
    - Both Examples compile tsc --strict (one module per fence, inside ## Example) and are pure/same-I/O (behavior-preserving by construction).
  </behavior>
  <action>
    Create `discriminated-union-and-fold.md` and `function-composition.md`. Header block per D-01: `Use when:` (one line,
    to be mirrored verbatim in the README), `Correspondence: dissolves-from -> [links]` (the exact served-leaf link set from
    RESEARCH.md, `../gof-catalog/<slug>.md#<anchor>` / `../kerievsky-catalog/<slug>.md#<anchor>` / `../extra-patterns-catalog/<slug>.md#<anchor>`),
    `Keep the OO form when:` (identity | coordinated mutable state | measured hot path | new-variant churn (expression problem) |
    house style). Body: `## Idiom` (1-2 ORIGINAL lines -- paraphrase; the DU+fold line must not read like the research
    artifact's Norvig/Wlaschin quote), `## Example` (Before OO -> After idiom, tsc-strict-clean, plus the one-line
    `Same behavior; mechanics: [<Fowler/Kerievsky refactoring>](../<catalog>/<slug>.md#<anchor>), run tests between steps.`),
    `## When each fits` with one `### <Served pattern>` sub-heading + EXACTLY ONE content line per served gof/extra pattern
    (Kerievsky refactorings reach the leaf via README rows, not necessarily their own residual anchor -- the residual anchors
    are per SERVED PATTERN as listed in RESEARCH.md's residual set). For discriminated-union-and-fold, each residual line
    carries the per-pattern expression-problem flip. ASCII-only; `->` for arrows, `--` for dashes.
  </action>
  <acceptance_criteria>
    - Both files exist with a single H1 matching the slug; check-functional C1 recognizes both idiom names
    - `node extract-samples.mjs` exits 0 (both Examples compile tsc --strict)
    - discriminated-union-and-fold has `### Visitor`, `### State`, `### Interpreter`, `### Composite` each followed by exactly one non-blank line
    - Each Correspondence: line begins `Correspondence: dissolves-from ->` and links resolve (node tools/check-crossrefs.mjs shows no new dangling link from these two files)
    - node tools/check-hygiene.mjs exits 0 (ASCII + no verbatim + email allowlist)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>Both mega-leaves compile, carry their per-pattern residual anchors, and pass hygiene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: first-class-function + factory-function</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (inventory rows 1 and 9; the N:1 map GoF/Kerievsky/extra rows for Strategy/Bridge/Adapter and the Factory family; A4 resolution -- Unify Interfaces -> first-class-function, Chain Constructors -> factory-function)
    - .planning/research/functional-depatterning-ts.md Section 3.1/3.2/3.5 (Strategy, Abstract Factory, one-method interface) and Section 11 Creational/Structural tables
    - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/strategy.md (a target leaf shape for the Correspondence link)
  </read_first>
  <behavior>
    - first-class-function: a function-valued variable/parameter; a one-method interface is just a function. Correspondence: dissolves-from -> Strategy, Bridge, Adapter [gof] + Replace Conditional Logic with Strategy, Unify Interfaces with Adapter, Extract Adapter, Unify Interfaces [kerievsky]. `### Strategy` / `### Bridge` / `### Adapter` residual lines (Adapter's line notes the N-method-interface residual -> object). A4: the Unify Interfaces utility maps here (nearest idiom).
    - factory-function: a factory function (+ lookup map / record of factories). Correspondence: dissolves-from -> Abstract Factory, Factory Method [gof] + Factory, Creation Method [extra] + Replace Constructors with Creation Methods, Move Creation Knowledge to Factory, Encapsulate Classes with Factory, Introduce Polymorphic Creation with Factory Method, Chain Constructors [kerievsky]. `### Abstract Factory` / `### Factory Method` (+ `### Factory` / `### Creation Method` for the extra patterns) residual lines. A4: Chain Constructors maps here.
    - Examples compile tsc --strict; pure/same-I/O.
  </behavior>
  <action>
    Create `first-class-function.md` and `factory-function.md` on the D-01 template (same header + body structure as Task 1).
    Correspondence enum value = dissolves-from with the exact served-leaf link sets above. Author one `### <Served pattern>`
    sub-heading + one line per served gof/extra pattern under `## When each fits`. Paraphrase every ## Idiom line (the
    "a one-method interface is just a function" and "the strategy is a variable whose value is a function" phrasings are the
    exact Wlaschin/Norvig quotes the research artifact reproduces -- restate in original wording). State the A4 resolution in
    the SUMMARY: the 2 Kerievsky utilities Unify Interfaces (-> first-class-function) and Chain Constructors
    (-> factory-function) map to the nearest idiom leaf so every kerievsky leaf gets a resolvable Functional alternative: link
    (keeps C4 uniform; no checker exemption). ASCII-only.
  </action>
  <acceptance_criteria>
    - Both files exist; check-functional C1 recognizes both idiom names; filename == slug
    - `node extract-samples.mjs` exits 0
    - first-class-function has `### Strategy`, `### Bridge`, `### Adapter`; factory-function has `### Abstract Factory`, `### Factory Method`, `### Factory`, `### Creation Method` (one line each)
    - node tools/check-hygiene.mjs exits 0
    - The A4 utility-mapping resolution is recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>Both leaves compile, carry residual anchors for every served pattern, pass hygiene; A4 recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: thunk-and-lazy-value + module-namespace</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (inventory rows 2 and 8; the N:1 map rows for Command/Proxy and Singleton/Facade)
    - .planning/research/functional-depatterning-ts.md Section 10 Q1 (Command closure/thunk), Q3 (modules-as-singletons), Section 11 (Command, Proxy, Singleton, Facade verdicts)
    - plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/singleton.md (its Consequences already cite Dependency Injection -- mirror that vocabulary in module-namespace's residual line, do not quote)
  </read_first>
  <behavior>
    - thunk-and-lazy-value: a nullary closure `() => T` defers an action or memoizes a value. Correspondence: dissolves-from -> Command, Proxy [gof] + Replace Conditional Dispatcher with Command [kerievsky]. `### Command` (invoke-later/queue/macro; serialization is the residual boundary -> tagged data command) + `### Proxy` (virtual=lazy-thunk; guard=composition; remote=async residual lines fold into one capped line naming the sub-kinds) residual lines.
    - module-namespace: an ES module's single-evaluated bindings; a module of functions. Correspondence: dissolves-from -> Singleton, Facade [gof] + Inline Singleton, Limit Instantiation with Singleton [kerievsky]. `### Singleton` (module value is a process singleton; DI for testability) + `### Facade` (a module of functions over the subsystem) residual lines.
    - Examples compile tsc --strict; pure/same-I/O (for module-namespace keep the Example free of real side effects).
  </behavior>
  <action>
    Create `thunk-and-lazy-value.md` and `module-namespace.md` on the D-01 template. Correspondence enum = dissolves-from with
    the exact served-leaf link sets above. Under `## When each fits`, one `### <Served pattern>` + one line per served gof
    pattern (Command, Proxy for thunk-and-lazy-value; Singleton, Facade for module-namespace). Paraphrase all ## Idiom lines.
    ASCII-only.
  </action>
  <acceptance_criteria>
    - Both files exist; check-functional C1 recognizes both; filename == slug
    - `node extract-samples.mjs` exits 0
    - thunk-and-lazy-value has `### Command` and `### Proxy`; module-namespace has `### Singleton` and `### Facade` (one line each)
    - Correspondence: lines begin `Correspondence: dissolves-from ->` with resolving links
    - node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node extract-samples.mjs && node tools/check-hygiene.mjs && (node tools/check-functional.mjs; true)</automated>
  </verify>
  <done>Both leaves compile, carry residual anchors, pass hygiene; six N:1 leaves now exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown reference content only -- no runtime/network/user-input surface in the shipped
plugin. The only boundaries are repo-hygiene / licensing invariants.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | committed leaves must be ASCII-only, work-email-free, no verbatim book prose/code |
| research artifact -> leaf prose | the artifact quotes Norvig/Wlaschin near-verbatim; those quotes must be paraphrased, not copied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-IP | Information Disclosure | the 6 leaf files (## Idiom lines especially) | mitigate | DST-04: paraphrase every ## Idiom one-liner from the first draft (Pitfall 1 / MEMORY near-verbatim trap); check-hygiene no-verbatim heuristic gates it. |
| T-08.2-EM | Information Disclosure | committed files | mitigate | Work-email allowlist enforced by check-hygiene. |
| T-08.2-SC | Tampering | typescript@6.0.3 | accept | No new installs; example compilation uses the already-pinned tsc. |
</threat_model>

<verification>
- `node extract-samples.mjs` exits 0 (all 6 Examples compile tsc --strict).
- `node tools/check-hygiene.mjs` exits 0 (ASCII + no verbatim + email allowlist).
- `node tools/check-crossrefs.mjs` shows no new dangling link from these 6 leaves (their Correspondence targets are existing OO leaves).
- check-functional advances on C1 (6/19 idiom leaves recognized); overall still RED until plans 05/06 -- expected.
</verification>

<success_criteria>
Six N:1 dissolves-from leaves exist on the locked template, every Example tsc-strict-clean,
every ## Idiom paraphrased, each served pattern a one-line `### <Pattern>` residual anchor.
</success_criteria>

<output>
Create `.planning/phases/08.2-functional-catalog-inserted/08.2-02-SUMMARY.md` when done
(record the A4 utility-mapping resolution).
</output>
