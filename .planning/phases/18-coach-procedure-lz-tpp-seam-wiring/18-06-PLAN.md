---
phase: 18-coach-procedure-lz-tpp-seam-wiring
plan: 06
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05]
files_modified: []
autonomous: true
requirements: [LAW-01, LAW-02, RTR-02, SEAM-01, SEAM-02, VIT-02]
must_haves:
  truths:
    - "The full deterministic battery is GREEN on the merged tree: check-red-references exit 0, typecheck exit 0 (incl. SKILL-1.ts), check-hygiene exit 0, provenance-honesty self-test 3/3, claude plugin validate . exit 0."
    - "D-14: no stale deferral marker survives anywhere in the shipped lz-red tree (the checker `absent` guards confirm this)."
    - "D-05: the owned Three-Laws surface is cleared by the orchestrator oracle-reviewer gate; if a Law statement is downgraded to no-oracle, the three-laws prose tag AND the principle-backing LAW row are reconciled together and the battery re-run."
    - "D-10: the shipped lz-tpp reverse-pointer edit is cleared by >= 1 unbiased from-scratch orchestrator review."
    - "The lz-red SKILL.md coach procedure is cleared by >= 1 unbiased orchestrator review (coach-don't-drive, override honored, no leaf content restated)."
  artifacts:
    - .planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-06-SUMMARY.md
  key_links:
    - "the three orchestrator gates run AFTER the executor's battery task returns -- gsd-executor cannot spawn them"
---

<objective>
Finalize Phase 18. The executor runs the full deterministic battery on the merged tree and confirms
GREEN; the ORCHESTRATOR then runs the three agent gates that gsd-executor cannot (it has no Agent/Task
tool). This plan is the phase gate: requirement closure (LAW-01/02, RTR-02, SEAM-01/02, VIT-02) awaits
both the GREEN battery AND the three orchestrator gates, then gsd-verifier.

Purpose: prove the artifacts landed, are well-formed, and are own-words / faithful. Output: a GREEN
battery + three cleared gates recorded in the SUMMARY. No file is authored by the executor here (the
battery is read-only); any content fix is a gap-closure edit driven by a gate outcome.

## Decisions implemented (D-NN)

- **D-05:** the owned Three-Laws surface goes through the orchestrator oracle-reviewer gate (converge-to-clean, 3-round cap); the gate DECIDES the owned-vs-no-oracle tier per Law statement.
- **D-10:** the shipped lz-tpp edit is cleared by >= 1 unbiased from-scratch orchestrator review before acceptance; /reload-plugins is a Phase-19 human action.
- **D-12:** the clean-room gates (oracle-reviewer on the owned surface + the no-verbatim scan) are cleared; main context never read .oracle/ prose.
- **D-14:** the checker `absent` guards confirm no stale deferral marker remains in the shipped lz-red tree.

## Artifacts produced

- The GREEN deterministic battery result + the three cleared orchestrator gates, recorded in 18-06-SUMMARY.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-CONTEXT.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-VALIDATION.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full deterministic battery on the merged tree and confirm GREEN</name>
  <files></files>
  <read_first>.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-VALIDATION.md</read_first>
  <action>
Run the full deterministic battery on the merged Phase-18 tree and confirm every gate is GREEN:
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` exits 0 -- all four flipped
  guards GREEN, the SKILL.md entry GREEN (coach-procedure topics + non-ignore ts fence + no-stale-marker),
  the SEAM-02 block GREEN (both reverse pointers), and the D-05 honesty gate holds.
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 -- every fence including
  SKILL-1.ts is tsc --strict --noEmit clean.
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 -- ASCII + work-email
  allowlist-inversion + no-verbatim over both skill trees.
- `node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs` reports 3/3.
- `claude plugin validate .` exits 0.

Confirm via the checker's `absent`-guard output that no stale deferral marker remains in the shipped
lz-red tree (the guards cover SKILL.md + the four references). Do NOT self-certify the three
orchestrator gates below -- those are run by the orchestrator after this task returns.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs exits 0 (all guards GREEN, SEAM-02 block GREEN, D-05 honesty gate holds).
    - `npm run typecheck` exits 0 with SKILL-1.ts among the compiled modules.
    - check-hygiene.mjs exits 0; provenance-honesty.selftest.mjs reports 3/3; `claude plugin validate .` exits 0.
    - The checker `absent` guards all PASS (no stale deferral marker anywhere in the shipped lz-red tree).
  </acceptance_criteria>
  <done>The full deterministic battery is GREEN on the merged tree; requirement closure now awaits the three orchestrator gates.</done>
</task>

</tasks>

<orchestrator_gates>
## Orchestrator gates (run AFTER the executor's battery task returns -- NOT executor tasks)

gsd-executor has ONLY Read/Write/Edit/Bash/Grep/Glob/Skill (no Agent/Task tool), so the three gates
below are ORCHESTRATOR-driven and run after Task 1 returns GREEN. Do NOT plan or run them as executor
steps. Each is reached BY its dedicated agent (never self-certified inline).

1. **oracle-reviewer on the owned Three-Laws surface** (D-05/D-12). Target: the
   `three-laws-and-test-selection.md` "The Three Laws spine and classify-first" section authored in
   18-02. Converge-to-clean, 3-round cap. The gate DECIDES the owned-vs-no-oracle tier per Law
   statement against Clean Code Ch. 9. If a Law statement is under-covered, downgrade it to no-oracle
   high-confidence core -- and RECONCILE both the leaf prose tier tag AND the matching
   principle-backing LAW row so they stay tier-consistent and the D-05 honesty gate stays GREEN (a
   gap-closure edit; re-run the battery after). Main context must NOT read .oracle/ prose.
2. **>= 1 unbiased from-scratch review of the shipped lz-tpp/SKILL.md edit** (D-10, SC4). The reverse
   pointer is EXCLUDED from the no-verbatim scan, so this review is the verbatim-safety backstop; it
   also checks own-words, pointer-only (no restatement), ASCII, and work-email absence. Going live
   needs a human /reload-plugins at the Phase-19 ship.
3. **>= 1 unbiased review / skill-reviewer of the lz-red SKILL.md coach procedure** (standing rule;
   17-06 precedent). Checks coach-don't-drive (QUESTION vs COMMAND; no auto-edit / no running the
   suite), the natural-language override honored, no leaf content restated, and the 6-step Three Laws
   spine present.

If any gate returns NEEDS-CHANGES, close it via a scoped gap-closure edit and re-run the full
deterministic battery before advancing. Requirement closure (LAW-01/02, RTR-02, SEAM-01/02, VIT-02)
and the milestone traceability flip happen only after all three gates are clear and gsd-verifier
passes.
</orchestrator_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .oracle/ book prose -> distilled Markdown | the oracle-reviewer gate proves the owned surface is own-words and faithful without main context reading .oracle/ |
| authored content -> shipped LIVE skill | the lz-tpp edit review + the deterministic gates prove the shipped tree is clean before the Phase-19 reload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Info disclosure (IP) | owned Three-Laws surface + all authored prose | medium | mitigate | oracle-reviewer gate (gate 1) + check-hygiene no-verbatim scan; verbatim leak blocks acceptance. |
| T-18-04 | Info disclosure (IP) | lz-tpp/SKILL.md (out of the no-verbatim scan) | medium | mitigate | gate 2 (>= 1 unbiased review) is the sole verbatim backstop for lz-tpp. |
| T-18-02 | Info disclosure | all authored Markdown | medium | mitigate | check-hygiene allowlist-inversion work-email scan GREEN. |
| T-18-03 | Tampering (integrity) | all authored Markdown | low | mitigate | check-hygiene ASCII scan GREEN. |
| T-18-SC | Tampering (supply chain) | shipped plugins/lz-tdd tree | low | accept | No dependency added this phase; `claude plugin validate .` confirms structural integrity. |

No high-severity runtime threats: the shipped artifacts are Markdown that execute no code; example
fences are compiled, never run. ASVS level 1; block on `high` (none exist).
</threat_model>

<verification>
- Full deterministic battery GREEN: check-red-references (exit 0) + typecheck (exit 0) + check-hygiene (exit 0) + provenance-honesty self-test (3/3) + `claude plugin validate .` (exit 0).
- Orchestrator gate 1: oracle-reviewer clears the owned Three-Laws surface (tier gate-decided).
- Orchestrator gate 2: >= 1 unbiased review clears the shipped lz-tpp edit.
- Orchestrator gate 3: >= 1 unbiased review / skill-reviewer clears the lz-red SKILL.md coach procedure.
- gsd-verifier confirms goal achievement across the 4 ROADMAP Success Criteria.
</verification>

<success_criteria>
The full deterministic battery is GREEN on the merged tree, no stale deferral marker remains, and all
three orchestrator gates are cleared (owned Three-Laws surface, shipped lz-tpp edit, lz-red SKILL.md
coach procedure), so LAW-01/02, RTR-02, SEAM-01/02, and the VIT-02 SKILL.md clause are ready for
gsd-verifier and the milestone traceability flip.
</success_criteria>

<output>
Create `.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-06-SUMMARY.md` when done, recording
the GREEN battery and the outcome of each of the three orchestrator gates.
</output>
