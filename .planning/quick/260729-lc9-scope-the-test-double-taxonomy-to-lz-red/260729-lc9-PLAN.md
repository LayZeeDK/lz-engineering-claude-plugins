---
phase: quick-260729-lc9
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12]
files_modified:
  - .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
  - .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
  - plugins/lz-tdd/skills/lz-red/SKILL.md
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  - plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
  - plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
  - .planning/research/test-double-taxonomy.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md

must_haves:
  truths:
    - "D-01 D-02: no file named test-double-taxonomy.md exists anywhere under plugins/, and the battery FAILS by name if one reappears."
    - "D-03 D-12: lz-red ships NO test-double reference; the never-bare-stub rule survives inline in lz-red/SKILL.md and lz-tpp/SKILL.md, each carrying the phrase which side they mean."
    - "D-08: deleting a data cell together with its pipe from any table in the shipped tree or in the archived copy makes the battery FAIL naming the table's start line and the ragged row's line."
    - "Deleting or mistyping any relative markdown link target under plugins/ makes the battery FAIL by file:line -- a class that does not exist in the battery today."
    - "D-04: the archived copy states it is an inert record, not accepted, not an agent input, and names what is still open in it."
    - "D-05: the record-then-inspect double in message-matrix.md is named a Test Spy; the string Mock rule occurs nowhere under plugins/; the mockist label is attributed to Fowler at both its sites."
    - "D-07: the Beck side of the live conflict cites the surface that actually carries it, not the rows above it."
    - "check-red-references.mjs and row-guards.selftest.mjs both exit 0; claude plugin validate . passes; git status clean."
  artifacts:
    - .planning/research/test-double-taxonomy.md
    - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md
    - .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
  key_links:
    - "EXPECTED_CHECKS, NEW_LABELS and RETIRED_LABELS must move in lockstep with every guard added and retired, or the roster gate fails for the wrong reason."
    - "row-guards.selftest.mjs asserts RETIRED_LABELS length AND shape, ROW_SCOPED_GUARDS key set, COUNT_GUARDS key set, and OLD_NEEDLES key parity -- all four break when the taxonomy guards retire."
    - "G17 stays scoped to plugins/ while the shape guard reaches the one .planning/ path; the two scopes must stay textually separate (H8)."
    - "scanTables and parseRows must split cells through ONE shared helper, or the shape guard and the row parser can disagree about a row's width."
---

<objective>
Scope the test-double taxonomy to lz-red only by removing it from the shipped plugin entirely,
archive it as an inert record, harden the table instrument with two general guards, fix three
content correctness errors, and re-scope the checker battery to match.

Purpose: the artifact violated a standing owner constraint at its introduction (D-01) and survived
three acceptance gates because nothing enforced the constraint. This task removes the artifact from
the deliverable, makes the constraint machine-enforced (D-02), and closes the dead-link class that
the removal itself creates.

Output: 4 atomic commits. Commits 1 and 2 leave the battery deliberately RED with an exactly
enumerated failure set; commit 3 returns it to GREEN; commit 4 finishes the archive.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md
@.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RESEARCH.md
@.planning/.continue-here.md
@AGENTS.md
</context>

<decisions_coverage>
## Locked decisions and where each is implemented

- **D-01:** the taxonomy is used by lz-red only. Task 3 deletes all three shipped copies; guard N3
  (Task 1) makes the constraint machine-enforced instead of prose-enforced.
- **D-02:** no byte-identical copies ever again. Task 3 deletes the sha256 gate OUTRIGHT (not
  narrowed, not archived) and replaces the three-copies rationale at BOTH duplicated comment sites
  with the forward constraint.
- **D-03:** the how-and-why leaves the shipped tree. Task 3 moves the whole 522-line document out of
  plugins/ to .planning/research/.
- **D-04:** the archived copy is an inert record, never an agent input. Task 4 writes the header. No
  agent definition under .claude/agents/ is edited by any task in this plan.
- **D-05:** narrow reading plus the three real correctness errors. Task 2 fixes exactly those three
  and adds no vocabulary guard.
- **D-06:** authoritative pattern names stay verbatim. Task 2 changes only the label word and the one
  mislabelled example; the eight correct "one warranted double" sites and every framework API name
  are left alone.
- **D-07:** fix content blocker C-B1 now. Task 4 re-sources the Beck side of the live conflict.
- **D-08:** the general table-shape guard replaces the narrow row-count repair. Task 1, guard N1.
- **D-09:** the lz-refactor "refactor is overloaded" parallel is an observation, not a work order.
  Nothing in lz-refactor changes beyond the one deletion D-01 requires.
- **D-10:** do not investigate the shared-reference mechanism. Tasks 3 and 4 RECORD the forward
  constraint in prose and in guard N3. No spike, no prototype, no validation of the mechanism.
- **D-11:** the deleted absence rule is reinstated in the archived copy. Task 4.
- **D-12:** the end state is the no-artifact baseline arm of a future A/B. Task 3 leaves lz-red with
  the inline rule and no reference file. That is not a regression to patch. Authoring the runtime
  intent-disambiguation artifact and its A/B is OUT of scope for this task.
</decisions_coverage>

<ordering_resolution>
## The two ordering hazards this plan resolves explicitly

**H1 -- the link guard's RED proof is destroyed by the deletion.** The only two failing cases today
are `plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md:289` and
`plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md:289`, both pointing at
`principle-backing.md`, which exists only under `lz-red/references/`. Task 3 deletes both files.
**Resolution: Task 1 builds and runs the link guard and records those two hits as its RED baseline,
before any deletion.** Re-measured this session, fence-aware: 1014 inline links across 197 files,
0 inside code fences, exactly 2 unresolved.

**H2 -- step 4 says "verbatim" while steps 1, 6 and D-11 all edit that same text.** VERBATIM MEANS
"COPIED WITHOUT REWRITING", NOT "restored to its pre-edit bytes". **Resolution: the move happens
FIRST (Task 3), as a pure `git mv` rename with no content change; every content edit to that
document then lands on the moved copy in place (Task 4).** Do NOT re-apply the C-B1 fix, the D-11
reinstatement or the S1-S4 sweep to the shipped copy before moving it -- editing one shipped copy
alone breaks the byte-identity gate, and editing all three is waste on files that are deleted in the
same commit. This is the single most likely executor misreading; if you find yourself editing
`plugins/**/test-double-taxonomy.md`, stop.

**H3, H6 -- the deletion and the battery re-scope are ONE commit.** Deleting the taxonomy makes 43
checks vanish SILENTLY (the FILES loop skips a missing file); only 15 fail loud. A battery run in
that window is not a signal. Likewise `COUNT_GUARDS` empties and its import, its export and its
selftest assertion must move together. Task 3 is therefore large by necessity, not by preference.

**H4 -- the D-05 fix is over-wide by default.** "the one warranted double" has NINE sites and EIGHT
are correct. `check-red-references.mjs:209` has six `expect-to-send` sites, so the battery will NOT
catch an over-wide rename. Task 2 names the exact target and the exact non-targets.

**H8 -- keep the two tree scopes textually separate.** G17 walks `plugins/` only and its
`TAXONOMY_BASENAME` filter is deleted. The shape guard walks `plugins/` plus the ONE
`.planning/research/test-double-taxonomy.md` path. Do not reuse one constant for both.

**Deliberate RED windows.** After Task 1 the battery exits 1 with EXACTLY 8 failures; after Task 2,
EXACTLY 4. Both sets are enumerated in the task's `<done>`. This is the instrument-first discipline
this workspace already uses (see `260728-wev-RED-BASELINE.md`), and the exact-count assertion is
what stops a real failure hiding inside a designed one.
</ordering_resolution>

<tasks>

<task type="auto">
  <name>Task 1: Instrument first -- add nine guards, prove each can fail, record the RED baseline</name>

  <files>
.claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
.claude/skills/lz-red-workspace/tools/check-red-references.mjs
.claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md
  </files>

  <action>
Add NINE emitted checks. Nothing in `plugins/` is edited by this task. `EXPECTED_CHECKS` moves
174 -> 183 with the arithmetic written into the comment above it, in the style already there.

**A. `lib/pipe-table.mjs` -- one shared splitter and two new exports.**

1. Extract the cell split into ONE internal helper used by BOTH `parseRows` and the new
   `scanTables`. It must be aware of a backslash-escaped pipe, which today's `line.split("|")` at
   `:47` miscounts. Measured: `plugins/**/*.md` has ZERO escaped pipes so behaviour is unchanged
   today, but `.planning/research/STACK.md` has three rows that this exact bug would report ragged,
   which is proof the class is live in this repo. Load-bearing reason for sharing: if the shape
   guard and the row parser disagree about a row's width, the guard passes while the parser silently
   SKIPS the row -- the delimiter-evasion defect class this instrument exists to close.
2. Export `scanTables(text) -> { tables, offenders }`. Algorithm, exactly as measured in RESEARCH
   Q1: split on `/\r?\n/`; fence-aware via `/^\s{0,3}(`{3,}|~{3,})/` where the same fence character
   closes and a fenced line also CLOSES any open table; a line is a table row iff it starts with a
   pipe (the same predicate `parseRows` uses); width via the shared splitter; the first pipe row of
   a run sets the width; any later row of a different width is an offender; a non-pipe line closes
   the table. Offender string VERBATIM:
   `table at line 233: header is 9 wide, but 1 row(s) differ -- line 237 is 8`.
   Return the table COUNT alongside the offenders -- that is what makes the checker's anti-vacuity
   leg testable.
3. Export `findLinkTargets(text) -> {line, raw, kind}[]`, pure and disk-free so classification is
   fixture-testable. Extract with `/\[[^\]]*\]\(([^)\s]+)(?:\s+"[^"]*")?\)/g` per line. Kinds:
   `anchor` for a target starting with `#`; `scheme` for `/^[a-z][a-z0-9+.-]*:/`; `absolute` for a
   leading `/`; `relative` otherwise. Use the SAME fence helper -- measured 0 fenced links today, so
   this costs nothing and closes the class permanently.
4. Update the module header. The measurement recorded at `:16-18` cites the taxonomy as one of two
   targets; that file stops being a target of this module in Task 3. State the new scope. The
   `principle-backing.md` measurement at `:19-23` survives and stays.

**B. `check-red-references.mjs` -- five post-loop blocks and three FILES-entry guards.**

Place N1, N2, N3 next to the G17 block and REUSE its existing `collectMarkdown(dir)` helper. Do not
write a second directory walker. Each is ONE `report()` call with a by-file-and-line detail string,
the G17 idiom. Each FAILS CLOSED on an unreadable file or tree -- push a hit, never filter.

- **N1** label `[lc9] no ragged pipe table`. Scope `plugins/` ONLY for now; Task 3 adds the one
  `.planning/` path. Anti-vacuity leg, and it is load-bearing: also FAIL when `tablesSeen === 0`.
  Without it a wrong path yields 0 files -> 0 offenders -> permanent PASS, the guard-that-cannot-fail
  class this file's own header condemns. Say in the comment that the pure function CANNOT fail on
  empty text -- "no ragged tables" is true of an empty document -- so the anti-vacuity control lives
  here on the table count and not in the selftest, unlike every guard in `row-guards.mjs`.
- **N2** label `[lc9] every relative markdown link resolves`. Scope `plugins/` only: the exit gate's
  own wording is "no dead relative links in any shipped SKILL.md or reference", and the archived
  copy's own link is handled as CONTENT in Task 4. Skip `anchor`, `scheme` and `absolute` kinds;
  split a relative target at its first `#` and resolve the file part against `path.dirname(file)`
  with `fs.existsSync`. The fragment split is REQUIRED, not optional -- the lz-refactor catalogs
  carry file-plus-fragment links. Anti-vacuity leg: FAIL when `linksSeen === 0`. Record in the
  comment that an allowlist was MEASURED unnecessary rather than forgotten: 1014 inline links, 984
  relative, 30 anchor-only, 0 external-scheme, 0 reference-style definitions, 0 inside fences.
- **N3** label `[lc9] no test-double taxonomy copy in the shipped tree`. FAIL if any file under
  `plugins/` has that basename, listing each. This is what makes D-01 and D-02 machine-enforced
  rather than prose-enforced; the constraint was violated once already and survived three acceptance
  reviews precisely because nothing checked it.
- **N7** label `[lc9] mockist label attributed to Fowler at both sites`. ONE report over TWO files:
  require the phrase `Fowler's label` in `anti-patterns.md` AND in `principle-backing.md`. One
  report because it is one claim restated in a dependent, the same reasoning the existing
  `owned-source count re-derived across both files` check records.
- **N8** label `[lc9] lz-tpp/SKILL.md carries the side-qualification rule inline`. Require the phrase
  `which side they mean` in the shipped `lz-tpp/SKILL.md`. Purpose: G17 is an ABSENCE gate, so
  deleting the rule outright passes it. This makes the rule's PRESENCE mandatory in the skill that
  fills the empty production symbol -- measured, `lz-tpp/SKILL.md:93,95` are the ONLY occurrences of
  the contested word or of `production-side` in that entire tree.

Three FILES-entry guards:

- **N4** on the `testing-stance/message-matrix.md` entry, a `topics` guard labelled
  `[lc9] Test Spy named for the record-then-inspect double`, needle `/test spy/i`.
- **N5** on the same entry, an `absent` guard labelled `[lc9] no Mock rule label`, needle
  `/Mock rule/i`.
- **N6** on the `testing-stance/functional-core.md` entry, an `absent` guard with the same label text
  and needle.
- **N9** on the `SKILL.md` entry (the lz-red router, `dir`-overridden), a `topics` guard labelled
  `[lc9] side-qualification rule inline in the coach procedure`, needle `/which side they mean/i`.

Add all nine composed labels to `NEW_LABELS`. Compose each EXACTLY as emitted: the FILES loop emits
`<entry name>: <label>`, so the message-matrix and functional-core labels carry the
`testing-stance/` path segment, and a post-loop label is verbatim.

**C. `row-guards.selftest.mjs` -- the fixture proof for what has no baseline failure.**

Extend the existing file; do NOT create a second selftest. Its stated invariant, IN-MEMORY FIXTURES
ONLY, is exactly right for these. Add two sections:

- `lib/pipe-table.mjs -- scanTables`, six cases: (1) a well-formed 3-column table -> 0 offenders;
  (2) header 3 wide with a data row 2 wide because a pipe was DELETED -> exactly 1 offender with the
  verbatim string `table at line 1: header is 3 wide, but 1 row(s) differ -- line 3 is 2`; (3) a row
  whose cell is BLANKED with the pipe KEPT -> 0 offenders, because content emptiness is a different
  guard's job and the two mutations render identically in GFM; (4) a ragged-looking table inside a
  fence -> 0 offenders; (5) two tables of different widths separated by a blank line -> 0 offenders;
  (6) `scanTables("")` -> `{ tables: 0, offenders: [] }`, with a comment stating this is why the
  anti-vacuity control lives on the table count in the checker.
- `lib/pipe-table.mjs -- findLinkTargets`, one case per kind plus a file-with-fragment target and a
  link inside a fence.
- One more `parseRows` case: a row containing a backslash-escaped pipe parses to the intended width.

**D. `260729-lc9-RED-BASELINE.md`** in this task's directory. One row per new guard: label, PROOF
KIND (`RED-at-baseline` or `invariant-GREEN + fixture set`), and the evidence. For a RED-at-baseline
guard the evidence is a `file:line`. For N1 the evidence is the fixture list -- do NOT manufacture a
baseline failure for it; measured, `plugins/**` has 31 tables, 405 pipe rows and 0 ragged, so the
guard correctly starts GREEN and the fixture set IS its proof. Record the exact 8-failure set this
commit leaves behind.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && ! node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > /tmp/lc9-t1.txt && test "$(rg -c '^\s+\[FAIL\]' /tmp/lc9-t1.txt)" = "8" && rg -q '183 checks, equal to the PREDICTED literal' /tmp/lc9-t1.txt && test "$(rg -c 'lz-refactor.references.test-double-taxonomy.md:289' /tmp/lc9-t1.txt)" = "1" && test "$(rg -c 'lz-tpp.references.test-double-taxonomy.md:289' /tmp/lc9-t1.txt)" = "1" && rg -q 'no ragged pipe table' /tmp/lc9-t1.txt && test "$(rg -c '\[PASS\].*no ragged pipe table' /tmp/lc9-t1.txt)" = "1" && test -f .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md</automated>
  </verify>

  <done>
- `row-guards.selftest.mjs` exits 0, including the pipe-DELETION fixture that makes `scanTables`
  return exactly one offender with the verbatim string.
- `check-red-references.mjs` exits 1 with EXACTLY 8 `[FAIL]` lines, and they are exactly:
  `[lc9] every relative markdown link resolves` (2 hits, both `:289`),
  `[lc9] no test-double taxonomy copy in the shipped tree` (3 hits),
  `testing-stance/message-matrix.md: [lc9] Test Spy named for the record-then-inspect double`,
  `testing-stance/message-matrix.md: [lc9] no Mock rule label`,
  `testing-stance/functional-core.md: [lc9] no Mock rule label`,
  `[lc9] mockist label attributed to Fowler at both sites`,
  `[lc9] lz-tpp/SKILL.md carries the side-qualification rule inline`,
  `SKILL.md: [lc9] side-qualification rule inline in the coach procedure`.
- The roster gate PASSES at 183, proving no guard was dropped or duplicated while nine were added.
- `[lc9] no ragged pipe table` PASSES -- invariant-GREEN, proof is the fixture set, no baseline
  failure is manufactured for it.
- Nothing under `plugins/` changed: `git status --short plugins/` is empty.
- Commit: `test(quick-260729-lc9): add nine guards with proofs, RED baseline recorded`
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix the three D-05 correctness errors, narrowly</name>

  <files>
plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  </files>

  <action>
Per D-05 this settles NO new vocabulary and adds NO vocabulary guard. It fixes three defects that are
wrong regardless of any vocabulary question. Per D-06, authoritative pattern, smell and refactoring
names stay verbatim: do not touch `vi.fn`, `vi.spyOn`, `vi.mock`, "over-mocking", "mockist" as
Fowler's own term, "doubles as documentation", "double duty", or "One skeleton" at
`test-structure-and-assertions.md:24,28`.

**1. The mislabelled Test Spy.** `message-matrix.md:131-138` demonstrates a `vi.fn()` that records a
call which the test inspects afterwards with `toHaveBeenCalledWith`. Per the taxonomy that is a Test
Spy; a Mock Object is reserved for a double that CARRIES THE EXPECTATION ITSELF. Name it. Edit the
bullet at `:57` and the in-fence comment at `:137` so the artifact is called a Test Spy and the
distinction from a Mock Object is stated in one clause.

**BLAST RADIUS, MEASURED -- do not exceed it.** The phrase "the one warranted double" has NINE sites
(`anti-patterns.md:38,128,166`; `test-structure-and-assertions.md:130`;
`message-matrix.md:57,73,79,137`; `vitest-typescript-mechanics.md:66`) and EIGHT are CORRECT --
`double` is the right umbrella term there, and a Test Spy IS a test double. Only the `:131-138`
example, its `:57` bullet and its `:137` comment need the Test Spy name. Renaming the other sites is
a fresh error, and `check-red-references.mjs:209` will NOT catch it because `expect-to-send` has six
sites.

**2. The six `Mock rule:` labels whose bodies say "double".** Replace the label word at all six --
`message-matrix.md:38,47,57,67` and `functional-core.md:42,44`. Note that only
`functional-core.md:42` is an actual H2 HEADING; the other five are BULLET LABELS. Do not
restructure any section. `Double rule:` is the natural replacement. Two live topic needles must stay
green: `functional-core.md`'s `no doubles in the core` topic is `/no (test )?doubles?/i`, satisfied
by both `:42` and `:44` after the change, and `message-matrix.md`'s
`/expect[ -]to[ -]send|expect to send/i` has six sites.

**3. The mockist attribution.** RECORDED STRENGTH IS PLAUSIBLE, NOT CONFIRMED -- read strictly, the
current clause names the school's proponents rather than its namers, which is defensible. Clarify
it; do not describe it as a fabrication and do not remove the GOOS counterpoint. At
`anti-patterns.md:123` and in the mirrored row at `principle-backing.md:52`, attribute the label to
Fowler. The taxonomy row records `classicist` and `mockist` as his OWN contribution, and the
counterpoint POSITION is still correctly sourced to GOOS. Both edits must carry the exact phrase
`Fowler's label`, which is guard N7's needle. Keep `principle-backing.md:52` at three cells -- the
row parser skips a row of the wrong width, so a stray pipe there silently removes it from every
guard that reads that table.

Run the battery before proceeding, so the diff for this fix is legible and separate from the much
larger Task 3 diff.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && ! node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > /tmp/lc9-t2.txt && test "$(rg -c '^\s+\[FAIL\]' /tmp/lc9-t2.txt)" = "4" && rg -q '183 checks, equal to the PREDICTED literal' /tmp/lc9-t2.txt && test "$(git grep -c 'Mock rule' -- plugins/ | wc -l)" = "0" && test "$(git grep -c -i 'test spy' -- plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md | wc -l)" = "1" && test "$(git grep -l "Fowler's label" -- plugins/ | wc -l)" = "2" && test "$(rg -c 'warranted double' plugins/lz-tdd/skills/lz-red/references/anti-patterns.md)" = "2" && test "$(rg -c 'warranted double' plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md)" = "1" && test "$(rg -c '^\|' plugins/lz-tdd/skills/lz-red/references/principle-backing.md)" = "37"</automated>
  </verify>

  <done>
- `check-red-references.mjs` exits 1 with EXACTLY 4 `[FAIL]` lines, and they are exactly the two
  taxonomy-deletion guards (`[lc9] every relative markdown link resolves`,
  `[lc9] no test-double taxonomy copy in the shipped tree`) and the two inline-rule guards
  (`[lc9] lz-tpp/SKILL.md ...`, `SKILL.md: [lc9] ...`). N4, N5, N6 and N7 have flipped to PASS.
- `Mock rule` occurs in ZERO files under `plugins/`.
- `Test Spy` occurs in `message-matrix.md` and the `:131-138` example is labelled accordingly.
- `Fowler's label` occurs in exactly TWO files under `plugins/`.
- The correct "warranted double" sites are UNTOUCHED at their measured baseline counts:
  `anti-patterns.md` still has 2 and `test-structure-and-assertions.md` still has 1. This is the H4
  over-wide-sweep protection, and it is the ONLY protection -- `check-red-references.mjs:209` has six
  `expect-to-send` sites, so the battery cannot catch a sweep on its own.
- `principle-backing.md` still has 37 pipe-leading lines, so the mockist row edit did not change the
  table's shape. Load-bearing: the row parser SKIPS a row of the wrong width, so a stray or missing
  pipe would silently remove that row from every guard reading that table.
- Commit: `fix(quick-260729-lc9): name the Test Spy, drop the Mock rule label, attribute mockist`
  </done>
</task>

<task type="auto">
  <name>Task 3: Move the taxonomy out, delete all three copies, re-scope the battery -- one commit</name>

  <files>
plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
.planning/research/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-red/SKILL.md
plugins/lz-tdd/skills/lz-tpp/SKILL.md
plugins/lz-tdd/skills/lz-refactor/SKILL.md
plugins/lz-tdd/skills/lz-red/references/principle-backing.md
.claude/skills/lz-red-workspace/tools/check-red-references.mjs
.claude/skills/lz-red-workspace/tools/lib/row-guards.mjs
.claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
.claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
  </files>

  <action>
ONE commit. Splitting it is not available: deleting the taxonomy before re-scoping makes 43 checks
vanish silently, retiring the count guards before deleting makes them fail on a present file, and
removing a router link without deleting the file (or the reverse) trips N2 or N3 (H3, H6).

**A. Move, then delete. `git mv` FIRST, no content change (H2).**

`git mv plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md .planning/research/test-double-taxonomy.md`,
then `git rm` the lz-tpp and lz-refactor copies. Moving the canonical lz-red copy gives git one clean
rename plus two deletions instead of three deletions and an addition, so `git log --follow` keeps the
D-02 history legible. Keep the basename. `.planning/research/` already exists and its five files are
tracked, so this adds a file to a tracked directory with no gitignore question. Do NOT edit the moved
file in this task.

**B. Replace the three router blocks (D-12: lz-red ends with the rule inline and NO reference).**

- `lz-red/SKILL.md`: DELETE the appendix bullet at `:154-159` entirely, and add the rule INLINE in
  step 6 of the coach procedure, adjacent to the two existing side-qualified uses at `:99-100`.
  Suggested wording, adjust freely but keep the two constraints below:

  > Always qualify the contested word by side -- a production-side stub is the symbol's own unwritten
  > implementation, a collaborator-side stub stands in for something else. Owned sources assign the
  > bare word to opposite sides, so when a developer uses it unqualified, establish which side they
  > mean before answering.

- `lz-tpp/SKILL.md`: replace the bullet at `:92-96` with ONE inline bullet. lz-tpp is the skill that
  FILLS the empty production symbol and `:93,:95` are the ONLY occurrences of the contested word in
  its whole tree, so the replacement must CARRY THE NAMING RULE, not merely drop the pointer:

  > - A production symbol with no implementation yet is a production-side stub. Always qualify it by
  >   side, because owned sources assign the bare word to opposite sides; when a developer says it
  >   unqualified, establish which side they mean before answering.

- `lz-refactor/SKILL.md`: replace the H2 section at `:182-186`. MEASURED: that tree has ZERO
  occurrences of the contested word or of either side qualifier outside the deleted copy, so there is
  no naming rule to preserve here and the replacement must say nothing about the contested word
  (D-09 -- change nothing else in lz-refactor):

  > ## Transitional versus permanent stand-ins
  >
  > When a request turns on whether an artifact is a temporary step or a permanent fixture of the
  > design, say which it is before refactoring it: a temporary step is meant to be replaced and a
  > permanent fixture is part of the shape you are refactoring toward.

TWO CONSTRAINTS ON ALL THREE, both load-bearing rather than cosmetic. (i) G17's allowlist is
per-line and anchored to end-of-slice, so a line wrap BETWEEN the side qualifier and the contested
word DEFEATS it -- keep every qualifier and its word on ONE line. (ii) `lz-red/SKILL.md` and
`lz-tpp/SKILL.md` must each contain the phrase `which side they mean` (guards N9 and N8).

**C. `principle-backing.md` -- remove every reference to the departed document.**

Delete the whole `[Test-double taxonomy](test-double-taxonomy.md)` row at `:71` -- every cell in it
describes a document no longer in the tree, including a count restated from it. Delete the
cross-reference sentence at `:98-99`. Then sweep the file for any remaining mention and remove it;
N2 catches a surviving LINK but not a surviving prose mention, which is what the sweep is for.

**D. Re-scope the battery. Retire 58, keep 2, RETARGET 0.**

Retargeting is rejected for three reasons, recorded so the choice is not silently re-opened: a
frozen archive has no regression surface, so a guard on it can only fail on a deliberate archive edit
(the guard-that-cannot-fail class in a new costume); pointing 44 checks of the lz-red SKILL gate at a
planning artifact makes a planning-doc edit able to redden the skill gate, which is the wrong
direction of coupling; and NO `.planning/` subdirectory survives a milestone close, so every
retargeted guard would break at the next one.

In `check-red-references.mjs`:

1. Delete the entire `test-double-taxonomy.md` FILES entry (29 topics, 14 `absent`, 1 auto scaffold
   check = 44 emitted checks). `FILES.length` goes 12 -> 11.
2. Delete the sha256 byte-identity block OUTRIGHT -- not narrowed, not archived (D-02). Narrowing it
   to one copy makes its predicate `digests.every(d => d === digests[0])` VACUOUS on a one-element
   array, reproducing the guard-that-cannot-fail class this file's own header condemns.
3. Delete the two taxonomy row-scoped entries and all NINE count entries from `TWO_IG_GUARDS`; it
   drops to four principle-backing row-scoped guards. Delete `taxonomyRowBacked` too -- it asserts
   the very row deleted in step C, so it must retire, not retarget.
4. Delete the chronology post-loop block, `taxonomyText`, `TAXONOMY_BASENAME` and the
   `path.basename(file) !== TAXONOMY_BASENAME` filter in G17's target list. Deleting the filter is
   SELF-ENFORCING: if a copy is ever re-added to a shipped reference tree, G17 fires loudly on it,
   which is the correct outcome under D-01/D-02. Keeping a vacuous filter would preserve the retired
   mechanism's shape, which is exactly what D-02 forbids. G17 keeps its scope otherwise -- measured
   10/3/177 files before and after; it never scanned the copies.
5. Drop `COUNT_GUARDS` from the `:89` import.
6. Prune `NEW_LABELS` to the survivors -- the four remaining `TWO_IG_GUARDS` labels plus the nine
   `[lc9]` labels = 13. Set `EXPECTED_CHECKS = 125` and write the arithmetic into the comment above
   it in the existing style: `174 baseline + 9 [lc9] additions - 58 retirements = 125`.
7. Add `.planning/research/test-double-taxonomy.md` to guard N1's target list as ONE extra path
   alongside the `plugins/` walk, failing closed if it is unreadable. Keep this textually separate
   from G17's scope (H8) -- do not introduce a shared basename constant.
8. **Step 8, first half (D-02).** The three-copies rationale is DUPLICATED: `:35-37` in the file
   header and `:632-637` in the byte-identity block, and the second states it in MORE detail
   including a clause about there being no plugin-root shared dir, which the forward architecture
   contradicts. The block deletion removes the second; the header comment at `:35-37` must be
   REPLACED with the forward constraint, not merely deleted. Also correct `:20-23` (twelve FILES
   entries -> eleven), `:669-679` (G17's scope comment still cites the basename exclusion), and the
   final SUMMARY string, which names the taxonomy and asserts byte-identity across three skills.
   The forward constraint to record: if a future milestone needs this material in the plugin it goes
   in as ONE plugin-wide shared reference, never as byte-identical per-skill copies. Per D-10, record
   the constraint and do NOT investigate, spike or prove the mechanism.

In `lib/row-guards.mjs`:

9. Grow `RETIRED_LABELS` from 6 to 64 by appending the 58 labels enumerated in RESEARCH Q2, composed
   EXACTLY as emitted. Three have an unusual shape and are the likely transcription errors: the
   scaffold label puts its bracket prefix FIRST, the byte-identity label has no colon after the
   filename, and the owned-source label has no filename prefix at all. If a retired label is
   mistyped, the roster gate cannot see that the check really went -- transcribe from a captured run,
   not from memory.
10. Delete the dead surface, so the retirement does not leave a large unreferenced export area the
    roster gate cannot see: `TAXONOMY_COLUMNS`, `TAXONOMY_HEADER`, `TAX`, `taxonomyRows`, the entire
    count-guard section including `NUM_WORDS`, `CARDINAL_ALT`, `statedCardinals`, `assertStatedTotal`,
    `sourceBulletCount`, `axisValueCounts`, `productionOwnTransitionalRows`, `enumerationRuns` and all
    nine guards, the `COUNT_GUARDS` export itself, and the three retired `ROW_SCOPED_GUARDS` entries
    with their matching `OLD_NEEDLES` entries.

In `row-guards.selftest.mjs` -- FOUR assertions break and each is named because an executor that runs
only the main battery sees GREEN and ships a red selftest (H5):

11. `RETIRED_LABELS carries exactly six composed labels` -> 64.
12. The `RETIRED_LABELS are COMPOSED strings` assertion regex `/^[a-z-]+\.md: /` FAILS on exactly
    THREE of the 58 (the three unusual shapes above), and would also fail a `testing-stance/` path
    label. Do NOT just delete it -- its purpose is catching a bare-label transcription error, which
    is a live hazard given those shapes. REPLACE it with one that still has teeth: every entry is
    non-empty, the list is duplicate-free, and every entry contains either `.md` or a bracketed tag
    prefix, so a bare `label` value from the checker can never slip in.
13. `ROW_SCOPED_GUARDS exports exactly the seven named row-scoped guards` -> four; the
    `COUNT_GUARDS exports exactly the nine` assertion is deleted with the export; `OLD_NEEDLES`
    parity must shrink in lockstep.
14. Delete the count-guard fixture block and the two retired taxonomy row-scoped guard blocks.
    **KEEP `TAXONOMY_LINES`** -- it is an in-memory 9-column fixture and it is the ONLY fixture
    exercising `parseRows`'s header-dropped-BY-VALUE and width-mismatch-SKIPS cases, plus the new
    `scanTables` cases. Add a comment saying it is a synthetic fixture, not a copy of any shipped
    document. Remove from `BACKING_LINES` the taxonomy prose line and the taxonomy table ROW; that
    drops the fixture from 5 data rows to 4, so the `backing fixture parses to 5 data rows`
    assertion literal must move to 4. `mutate()` asserts its replacement actually happened, so any
    fixture mutation still referencing a deleted line will throw -- fix those, do not weaken
    `mutate`.

In `lib/pipe-table.mjs`: confirm the header updated in Task 1 no longer names the taxonomy as a
target of the module.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > /tmp/lc9-t3.txt && rg -q '125 checks, equal to the PREDICTED literal' /tmp/lc9-t3.txt && rg -q 'none of the 64 retired labels survive' /tmp/lc9-t3.txt && rg -q 'all 13 new labels present' /tmp/lc9-t3.txt && test "$(git grep -c 'test-double-taxonomy' -- plugins/ | wc -l)" = "0" && test "$(git grep -c -i 'taxonom' -- plugins/lz-tdd/skills/lz-red/references/principle-backing.md | wc -l)" = "0" && test -f .planning/research/test-double-taxonomy.md && test "$(git grep -c 'which side they mean' -- plugins/lz-tdd/skills/lz-red/SKILL.md plugins/lz-tdd/skills/lz-tpp/SKILL.md | wc -l)" = "2" && test "$(git grep -c 'production-side stub' -- plugins/lz-tdd/skills/lz-tpp/SKILL.md | wc -l)" = "1" && test "$(rg -c 'COUNT_GUARDS' .claude/skills/lz-red-workspace/tools/check-red-references.mjs | wc -l)" = "0" && test "$(rg -c 'TAXONOMY_BASENAME' .claude/skills/lz-red-workspace/tools/check-red-references.mjs | wc -l)" = "0" && claude plugin validate .</automated>
  </verify>

  <done>
- Battery exits 0 at 125 emitted checks plus the roster gate, with all 13 new labels present and none
  of the 64 retired labels surviving.
- `row-guards.selftest.mjs` exits 0 with all four broken assertions repaired, not deleted.
- ZERO files under `plugins/` mention `test-double-taxonomy`, and `principle-backing.md` has no
  remaining taxonomy mention of any kind.
- `.planning/research/test-double-taxonomy.md` exists and `git status` shows it as a RENAME of the
  lz-red copy with no content change.
- Both `lz-red/SKILL.md` and `lz-tpp/SKILL.md` carry the inline rule with `which side they mean`;
  lz-tpp still carries `production-side stub`, so the naming rule survives in the skill that fills
  the empty symbol.
- `COUNT_GUARDS` and `TAXONOMY_BASENAME` appear nowhere in the checker.
- `claude plugin validate .` passes.
- Commit: `refactor(quick-260729-lc9): scope the test-double taxonomy to planning, re-scope the battery`
  </done>
</task>

<task type="auto">
  <name>Task 4: Make the archived copy an honest inert record</name>

  <files>
.planning/research/test-double-taxonomy.md
  </files>

  <action>
Every edit lands on the MOVED copy. Nothing under `plugins/` is touched. This is where step 1 (C-B1),
step 6 (the sweep) and D-11 finally apply -- see the H2 resolution above for why they run after the
move and not before.

**A. The inert-record header (D-04).** Above the existing `# ` title, add a header that states: the
document is an ARCHIVED RECORD, not a deliverable and not an agent input; it shipped as a bundled
reference in three lz-tdd skills and was removed from all of them by this quick task; no skill, agent
or command reads it; it must NOT be wired into `oracle.md`, `oracle-reviewer.md` or any SKILL.md.
State WHY it is kept: the per-author map is the useful part -- it records which author uses which
test-double term, on which side, and which source to cite -- and its job is to help a HUMAN or the
ORCHESTRATOR understand the oracle's sources and frame a question to the oracle, never to be an
authority a coach quotes. State that it FAILED THREE CONSECUTIVE ACCEPTANCE GATES, with round three's
independent unprimed review returning ACCEPT WITH FIXES at 2 BLOCKING, 15 IMPORTANT, 10 MINOR, and
point at
`.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-ACCEPTANCE-REVIEW.md`.
Name what remains OPEN: the IMPORTANT and MINOR findings are not all closed; absence claims here rest
on a degraded scan and nobody has verified a whole-book negative, because the oracle agent has no
search tool; and statements about how this document was distributed, gated or enforced describe a
state that no longer exists. Close with the forward constraint (D-02, step 8 second half): if a
future milestone needs this material in the plugin it goes in as ONE plugin-wide shared reference,
never as byte-identical per-skill copies. Per D-10 the header RECORDS the constraint and does not
investigate or prove the mechanism.

Do NOT edit any file under `.claude/agents/`. Measured: `git grep -ln "test-double-taxonomy" --
.claude/agents/` returns nothing today and both oracle agents are `tools: Read, Glob`. Wiring the map
in would be a NEW data path, not a preserved one, and D-04 forbids it.

**B. Fix content blocker C-B1 (D-07, step 1).** At `:285-287` the Beck side of the Clean-Code-versus-
Beck conflict is sourced to "the report and essays cited in his rows above" -- rows `:235-238`, which
give a 1994 report plus essays from 2007-2010, February 2022, August 2008 and January 2022. None of
those carries the position. The surface that does is named correctly by the dependents at
`principle-backing.md:67` and `:85` and at `lz-refactor/references/beck-tdd-by-example.md:26` and
`:83`. The dependents are right and this map is wrong, and TWO QUALIFIERS THE DOCUMENT ITSELF MARKS
NOT OPTIONAL hang off the wrong work. Re-source the Beck side to the surface the dependents name.

**C. Reinstate the deleted absence rule (D-11).** The hard rule in section 1, before its deletion
last round, said the document states only what POPULATES each cell, never about the literature at
large and NEVER ABOUT WHAT SOME AUTHOR DOES OR DOES NOT NAME. Restore the struck clause. Then scope
the claim it would have caught, at `:106` ("He does not name that artifact"), to what was ACTUALLY
READ -- nobody can verify a whole-book negative here. That claim is the entire warrant for section
2's ruling that this author is not the authority for the production side, so scope it rather than
delete it, and make the scoping visible.

**D. The step-6 sweep -- four surviving claims the removal makes false.** All four are BLOCKING.

- `:7` claims the document SETTLES what to call things and which author to cite for each cell. That
  is a claim to operative authority, and it would sit on the same screen as a header saying the
  document is not accepted and not an agent input. Rewrite it to describe what the document RECORDS.
- `:81` justifies omitting a matrix diagram on the ground that the page is read mid-cycle and a list
  is cheaper to scan than a grid. After the removal the page is read mid-cycle by nobody. It was
  already weak: `lz-red/SKILL.md:52-100` routes steps 2-5 to four other references and never routes
  to this document at all. Drop the justification or restate it without the readership claim.
- `:211-215` claims a gate checks the never-bare-word rule across all three skills' reference trees
  and all three routers, exempting the three copies of this document. There are no three copies and
  the exemption is deleted. The prior acceptance review certified this sentence TRUE, which is
  exactly why step 6 exists. Restate it in the past tense as a record of what the gate did while the
  document shipped, or delete it.
- `:289` declares the two NOT-OPTIONAL qualifiers "stated in full" behind a relative
  `[principle-backing.md](principle-backing.md)` link. That link already dead-ended in two of three
  copies and dead-ends always from here. This is the document's ONLY non-anchor relative link
  (verified). Either state the two qualifiers inline or name the dependent by its repository path
  without a relative link.

Advisory, and a reviewer will flag it: `:171` and `:174` make present-tense claims of an operative
role ("this is the reason the document exists", "what this page adds is"). Not false, but they sit
under a header calling the document inert. Adjust the tense if it is cheap; it is not blocking.

**E. Leave the planning history alone.** `.planning/.continue-here.md` and `.planning/HANDOFF.json`
describe the merged three-copy state. They are dated planning records, not shipped claims, and
rewriting history is worse than a dated record. This omission is DELIBERATE and is stated here so a
reviewer does not read it as a miss.

**F. Keep the archive shape-clean.** Guard N1 now scans this file. Do not introduce a ragged row
while editing, and do not introduce a backslash-escaped pipe inside a table cell.

Finally, re-run the public-repo hygiene scan by ALLOWLIST-INVERSION over everything this task wrote:
assert the only email-shaped token present is the approved public contact `larsbrinknielsen@gmail.com`
and flag anything else. Never write a forbidden value as a search needle -- the needle is the leak.
  </action>

  <verify>
    <automated>test -f .planning/research/test-double-taxonomy.md && rg -q 'INERT RECORD' .planning/research/test-double-taxonomy.md && rg -q 'plugin-wide shared reference' .planning/research/test-double-taxonomy.md && rg -q 'does or does not name' .planning/research/test-double-taxonomy.md && ! rg -q 'settles what to CALL things' .planning/research/test-double-taxonomy.md && ! rg -q 'This page is read mid-cycle' .planning/research/test-double-taxonomy.md && ! rg -q 'exempting the three copies of this document' .planning/research/test-double-taxonomy.md && ! rg -q '\]\(principle-backing\.md\)' .planning/research/test-double-taxonomy.md && test "$(git grep -c -l -E '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' -- .planning/research/test-double-taxonomy.md | wc -l)" = "0" && node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && claude plugin validate . && test "$(git status --porcelain | wc -l)" = "0"</automated>
  </verify>

  <done>
- The archived copy opens with an inert-record header naming its status, its purpose, its three
  failed gates, what remains open, and the D-02 forward constraint.
- The C-B1 Beck attribution points at the surface the dependents name.
- The absence hard rule carries the reinstated clause, and the `:106` claim is scoped to what was
  read.
- None of the four falsified claims survives: the settles-authority lead, the read-mid-cycle
  justification, the three-copies gate sentence, and the dead relative link are all gone.
- No email-shaped token appears in the archived copy at all, so allowlist-inversion is trivially
  satisfied there; the same scan over the rest of the task's output finds only the approved contact.
- Battery exits 0, selftest exits 0, `claude plugin validate .` passes, `git status` is clean.
- No file under `.claude/agents/` was modified.
- Commit: `docs(quick-260729-lc9): archive the taxonomy as an inert record, fix C-B1, sweep the claims`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub | This is a public repo; committed content and commit identity are published. |
| planning artifact -> agent context | An archived document that an agent might later be wired to read. |
| guard source -> guarded content | A guard whose needle no longer has a subject reports a false GREEN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-lc9-01 | Information disclosure | archived copy + 4 commit messages | high | mitigate | Task 4 runs allowlist-inversion over the task's output and asserts zero email-shaped tokens in the archived copy; never encode a forbidden value as a needle. |
| T-lc9-02 | Tampering | guard retirement | high | mitigate | 58 retirements recorded BY NAME in `RETIRED_LABELS` and asserted absent by the roster gate; without this a deliberate retirement and an accidental drop are the same green run. |
| T-lc9-03 | Repudiation | guard-that-cannot-fail | high | mitigate | The byte-identity gate is deleted outright rather than narrowed to a vacuous one-element predicate; N1 and N2 each carry an explicit anti-vacuity leg on their own seen-count. |
| T-lc9-04 | Elevation of privilege | oracle agent inputs | medium | mitigate | D-04: no file under `.claude/agents/` is edited; Task 4's `<done>` asserts it. Wiring the map in would be a new data path. |
| T-lc9-05 | Denial of service | dead shipped links | medium | mitigate | Guard N2 is added BEFORE the deletion that creates five dead links, with its RED-at-baseline proof captured first. |
| T-lc9-06 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; no dependency is added. Node builtins only, matching the existing modules. |
</threat_model>

<verification>
Run at the end of Task 4, all four legs, `&&`-chained:

```
node .claude/skills/lz-red-workspace/tools/check-red-references.mjs \
  && node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs \
  && claude plugin validate . \
  && test "$(git status --porcelain | wc -l)" = "0"
```

Plus the two proofs that cannot be re-derived after the fact and must therefore be READ from
`260729-lc9-RED-BASELINE.md`:

- guard N2's RED baseline, two hits with `file:line`, captured in Task 1 before the deletion;
- guard N1's fixture set, including the pipe-DELETION case that returns exactly one offender.

**A green battery is not acceptance.** This document has gone fully green over a blocking defect
three times. The content of the archived copy (Task 4) has no machine gate beyond table shape; an
independent unprimed review is the only gate on it, and the orchestrator owns that step because the
executor has no Agent tool.
</verification>

<success_criteria>
- No file named `test-double-taxonomy.md` exists under `plugins/`, and the battery fails by name if
  one reappears.
- `check-red-references.mjs` exits 0 at 125 emitted checks plus the roster gate.
- `row-guards.selftest.mjs` exits 0, including the pipe-deletion fixture that makes the shape guard
  fail and the link-classification fixtures.
- `claude plugin validate .` passes; no dead relative link in any shipped SKILL.md or reference.
- lz-red and lz-tpp each carry the side-qualification rule inline, with the qualifier and the word on
  the same line so G17's allowlist still applies.
- The archived copy carries the inert-record header and none of the four falsified claims.
- Four atomic commits, `git status` clean, no stray worktrees or branches.
- Zero spend: no `claude -p`, no metered run, no eval. The runtime intent-disambiguation artifact and
  its A/B (D-12) are NOT authored.
</success_criteria>

<orchestrator_followup>
The executor has NO Agent or Task tool and cannot spawn a reviewer. After the executor returns, the
ORCHESTRATOR must:

1. Spawn at least one WHOLLY UNPRIMED reviewer (no prior findings in its brief, firewalled from
   `.oracle/`) over the archived copy and the three replacement router sentences. Three consecutive
   rounds of this work stream shipped a blocking defect behind a green battery; the unprimed review
   is the gate, and a primed reviewer confirms the assumptions instead of testing them.
2. Spawn an instrument auditor over the 58 retirements: for each, confirm the label string matches
   what the pre-change run actually emitted, and confirm no surviving guard was weakened in place
   (a weakened guard leaves the roster count unchanged, so the roster gate is blind to it).
3. Confirm the executor did not edit any file under `.claude/agents/` (D-04).
</orchestrator_followup>

<output>
Create `.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-SUMMARY.md`
when done. Record, per task: the commit SHA, the emitted-check count, and any hole you found rather
than banked -- the four holes the last two executors self-reported were all CONFIRMED, so a
self-reported weakness is worth more than a banked pass.
</output>
