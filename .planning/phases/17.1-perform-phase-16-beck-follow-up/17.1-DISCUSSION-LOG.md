# Phase 17.1: Perform Phase 16 Beck follow-up - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-07-20
**Phase:** 17.1-perform-phase-16-beck-follow-up
**Mode:** `--analyze --auto --chain` (autonomous discuss; all gray areas auto-selected; recommended
option auto-locked per option; auto-advance to plan-phase)
**Areas discussed:** Upgrade scope, Gateability rule, Clean-room re-gate process, Provenance record + battery integrity

> All four gray areas were pre-decided in `.planning/HANDOFF.json` / `.planning/.continue-here.md`
> (queued Follow-up #2). None fell in the high-impact/low-confidence trap quadrant (low-risk,
> well-known Beck TDD, re-open of correct shipped content), so auto-locking the recommended option
> was appropriate. No user prompts were issued.

---

## Upgrade scope

| Option | Description | Selected |
|--------|-------------|----------|
| Locked-set + conditional | Test list + one-step -> Canon TDD; triangulation -> First One Then Many; degenerate-case + assert-first + evident-data upgrade ONLY if a full-text essay backs them under the gate | x |
| Upgrade every Beck row | Force all Beck no-oracle rows to owned regardless of source coverage | |
| Named rows only | Upgrade exactly the three handoff-named rows, ignore degenerate-case candidacy | |

**Choice:** Locked-set + conditional (D-01..D-04). The gate decides the conditional rows per the honesty rule.
**Notes:** "First One, Then Many" plausibly also backs the degenerate/starter case, so it is a candidate the oracle-reviewer evaluates rather than a pre-commit.

---

## Gateability rule

| Option | Description | Selected |
|--------|-------------|----------|
| Full-text only | Tag `owned; oracle-verified` only when a full-text (Access: free) essay backs it; book-summary (Access: book) surfaces stay no-oracle | x |
| Summaries count | Allow book-summary entries to justify an owned tag | |

**Choice:** Full-text only (D-05). A tier upgrade is honest only when the surface can actually be gated against real source text.
**Notes:** TDD-by-Example is `Access: book` (summary-only) -> not gateable; assert-first/evident-data ride on the free-essay conditional instead.

---

## Clean-room re-gate process

| Option | Description | Selected |
|--------|-------------|----------|
| Blind author + orchestrator gate | Author edits blind (main context reads only .oracle index nav); orchestrator drives oracle-reviewer per surface, converge-to-clean | x |
| Executor spawns reviewer | Plan tasks have the executor invoke oracle-reviewer directly | |

**Choice:** Blind author + orchestrator gate (D-06, D-07). Matches DST-04 and the gsd-executor-has-no-Agent-tool constraint.
**Notes:** Reuse the Phase-17 loop verbatim.

---

## Provenance record + battery integrity

| Option | Description | Selected |
|--------|-------------|----------|
| Flip tiers both places + note + green battery | Update principle-backing tiers/Sources AND per-doc Sources blocks; add a Phase-16 upgrade note; keep the battery exit 0 without touching asserted tokens | x |
| Central map only | Update principle-backing.md only, skip per-doc Sources | |

**Choice:** Flip tiers both places + note + green battery (D-08, D-09, D-10).
**Notes:** `check-red-references.mjs` asserts content tokens (e.g. ADV-0) - touch attribution/tier text only.

## Claude's Discretion

- Exact wording of upgraded provenance/Source lines (subject to the oracle-reviewer gate).
- Whether the conditional rows (degenerate-case; assert-first; evident-data) upgrade or stay no-oracle - decided by the gate per rule D-05.
- Placement/format of the Phase-16 upgrade note, following existing correction-note precedent.

## Deferred Ideas

- ROADMAP goal placeholder for Phase 17.1 ("[Urgent work - to be planned]", Requirements TBD) - set the real goal during plan-phase / `/gsd-phase edit`.
- Follow-up #1 (internal-ID / DST-04 3-skill polish); Follow-up #3 (FUT-METZ-REFACTOR lz-refactor Metz layer); Phase 18 (coach procedure + lz-tpp seam). All out of scope here.
