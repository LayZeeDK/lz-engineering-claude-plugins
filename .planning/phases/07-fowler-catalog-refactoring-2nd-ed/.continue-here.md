# Continue Here -- Phase 7 (Fowler Catalog, Refactoring 2nd ed)

**Paused:** 2026-07-05. **Status:** planning COMPLETE + rigor-checked; ready to EXECUTE Wave 2.
Everything is committed and clean (only `.planning/config.json` is modified -- the `_auto_chain_active`
flag; leave it). Nothing is half-done.

## Where we are -- START EXECUTION

All planning is done. The next session's job is to **execute Phase 7** (author the Fowler catalog
via the clean-room oracle loop). Resume with **`/gsd-execute-phase 7`** (Wave 2 first), or drive the
waves manually.

**Fully DONE + committed (do NOT redo):**
- Wave 1: `07-01` harness + FWL checkers (07-01-SUMMARY.md exists).
- `07-03` `principles.md` authored + oracle-CONVERGED (SUMMARY still pending -- record one when the
  phase closes).
- **Clean-room agents CONVERGED (round-14) + LIVE** (`/reload-plugins` was run this session):
  `.claude/agents/oracle.md`, `.claude/agents/oracle-reviewer.md`. 5 blind rounds x 3 lenses; final
  verdicts firewall "sound" / logic "no HIGH" / authoring "ship-as-is". Verdict partition
  `pass|blocked|error|revise`.
- **Scope-correction RE-PLAN done + full-rigor-checked:** REQUIREMENTS/ROADMAP/PROJECT/07-CONTEXT/
  07-VALIDATION/STATE synced to 62 + clean-room oracle. 10 plans regenerated (07-01/07-03 preserved).
  RESEARCH + PATTERNS refreshed (research -> pattern-map order) -- both confirm the locked design, no
  change. `gsd-plan-checker` (final re-run): READY-WITH-NOTES, 0 blockers; W1/W2 + 3 warnings applied.

## Execution mechanic every content plan runs (the clean-room oracle loop)

For each leaf (refactoring or smell):
1. **AUTHOR BLIND** -- from public knowledge + `.planning/research/refactoring-com-overview.md`
   (names/aliases/tags/inverse-of), in the LOCKED leaf/smell format (07-ROUTING-ARCHITECTURE
   "Content contracts"). The executor/main context **NEVER reads `.oracle/`** (only `ls` for names).
2. **GATE via `oracle-reviewer`** -- pass DRAFT_PATHS + SOURCE (`.oracle/refactoring-2e/index.md`, by
   chapter/topic) + CONTENT_TYPE (`refactoring-leaf`|`smell-leaf`) + the canonical per-axis anchors
   (07-ORACLE-MODEL "Driver responsibilities + canonical anchors") + SCOPE. Verdict per draft.
3. **REVISE BLIND** from the directives; use the `oracle` subagent for open-ended lookups.
4. **CONVERGE** -- every entry `pass` (or an owner-accepted `blocked`). The DRIVER caps rounds (~3)
   and escalates a `blocked`/ambiguity to the OWNER via AskUserQuestion -- never loop forever.
5. Run the harness checkers + `tsc --strict`; commit on green.

## Wave order (pilot -> parallel -> smells)

- **Wave 2 = `07-02` PILOT (do first, alone).** Overhauls the harness (66->62, per-leaf, smell-leaf,
  new `check-crossrefs`) + scaffolds `fowler-catalog/README.md` + authors the basic chapter (Ch.6, 11
  leaves) + **pins the canonical 62 name->slug map in its SUMMARY** (the shared cross-link-target
  contract wave-3 + 07-10 consume) + records the working rubric anchors/round-counts. Calibrate the
  loop here. (`07-03` principles is already done -- Wave 2 has no other work.)
- **Wave 3 = `07-04..07-09` (parallel, file-disjoint).** One book chapter each (Ch.7-12). They
  `@`-ref `07-02-SUMMARY.md` for the slug map. `check-crossrefs` is NOT a per-wave gate here.
- **Wave 4 = `07-10` (last).** 24 smell leaves + navigation-only `smells.md` + finalize
  `fowler-catalog/README.md` + run the FULL checker battery GREEN (the phase gate).

**After execution (per CLAUDE.md GSD rules):** verify_phase_goal (gsd-verifier) -> secure-phase ->
validate-phase -> extract-learnings. Do not skip the verifier.

## Locked decisions (durable in committed docs -- do not re-litigate)

- **Scope: 62 refactorings** (not 66). CUT 4 web-only 1st-ed relics (Replace Magic Literal, Replace
  Control Flag with Break, Replace Error Code with Exception, Replace Exception with Precheck). KEEP
  Return Modified Value `[web-only]`; Split Phase examples `[web-example]`.
- **Routing (07-ROUTING-ARCHITECTURE.md):** per-refactoring leaves `fowler-catalog/<slug>.md` +
  chapter-grouped `README.md` index; smell leaves `smells/<slug>.md` + navigation-only `smells.md`;
  `principles.md`. Leaf = `Use when:` + Motivation + Mechanics (every branch + safety checkpoints) +
  1 compact tsc-clean before/after example + optional Watch-for. Smell leaf = Recognize-by + how-to-
  recognize + why + candidate refactorings (discriminators + links). SKILL.md NOT re-touched.
- **Oracle model (07-ORACLE-MODEL.md):** clean-room; see the execution mechanic above. Availability:
  oracle agents ONLY for `.oracle`-present books (Fowler now, Kerievsky Phase 8); AskUserQuestion for
  GoF/Beck/RCM (not in `.oracle`). NOTE: 07-ORACLE-MODEL's early "Firewall"/"Verdict schema" blocks
  are STALE (pre-round-2: `Read, Grep, Glob` + old schema) -- the live agents + the round-10 "Driver
  responsibilities + canonical anchors" section are authoritative.
- **Pacing:** basic pilot first, then parallel fan-out, smells last (encoded in the waves above).

## Critical Anti-Patterns

| Anti-pattern | Severity | How it is prevented |
|---|---|---|
| Executor/main context reads `.oracle/` copyrighted book files | blocking | Only `oracle`/`oracle-reviewer` subagents read `.oracle/` (isolated context, own-words verdicts). Main context uses `ls` for names only (DST-04). |
| Using AskUserQuestion as the oracle path for Fowler/Kerievsky | warning | Those are in `.oracle/` -> use the clean-room agents. AskUserQuestion only for GoF/Beck/RCM (and for owner-adjudicating `blocked` ambiguities). |
| Fabricating catalog/smell content instead of the oracle loop | blocking | Author blind, gate via `oracle-reviewer` to convergence; no verbatim (DST-04). |
| Failing check-crossrefs before 07-10 | warning | It is a PHASE-GATE check (07-10 only), RED-by-design earlier. Per-wave verify uses tsc + check-hygiene + spot-checks. Also check-catalog (62/62) + check-smells (24/24) are RED until 07-10. |
| Wave-3 chapters drift on a leaf's slug | warning | 07-02's SUMMARY pins the canonical 62 name->slug map; wave-3 + 07-10 consume it so cross-links resolve. |

## Notes
- Agents are CONVERGED + LIVE -- no reload needed unless a new session's registry is stale (then
  `/reload-plugins`).
- Per-chapter refactoring membership in the plans is APPROXIMATE; the executor confirms placement via
  the `oracle` subagent (check-catalog asserts name-identity across ALL leaves, so the 62 total is
  invariant to chapter shifts).
- Harness lives at `.claude/skills/lz-refactor-workspace/` (gitignored-record pattern; `samples/`
  gitignored). Big chapters (~11 leaves: 07-02/07-08/07-09) -- the executor sub-batches the
  author/gate loop within the plan to stay in context (`oracle-reviewer` takes multiple DRAFT_PATHS).
- This milestone's phases 6-11 are Markdown skill authoring, NO UI (skip UI-SPEC).
