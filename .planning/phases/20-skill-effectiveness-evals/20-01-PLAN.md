---
phase: 20-skill-effectiveness-evals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/run_eval.py
  - .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/utils.py
  - .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/__init__.py
  - .claude/skills/lz-red-workspace/tools/skill-creator-eval/LICENSE.upstream.txt
  - .claude/skills/lz-red-workspace/tools/skill-creator-eval/README.md
  - .claude/skills/lz-red-workspace/eval-status.mjs
  - .claude/skills/lz-red-workspace/merge-judge.mjs
  - .claude/skills/lz-red-workspace/run-recall-chunks.mjs
  - .claude/skills/lz-red-workspace/run-spec-chunks.mjs
  - .claude/skills/lz-red-workspace/check-evals.mjs
  - .claude/skills/lz-red-workspace/evals/trigger-eval.json
  - .claude/skills/lz-red-workspace/evals/reciprocal-red.json
  - .claude/skills/lz-red-workspace/evals/d07-chunks/negatives.json
  - .gitignore
autonomous: true
requirements:
  - EVL-01
must_haves:
  truths:
    - "The native-fixed skill-creator-eval harness (run_eval.py + utils.py + __init__.py + LICENSE.upstream.txt + README.md) exists under lz-red-workspace/tools/skill-creator-eval/, copied byte-identical from the lz-refactor rig (D-01)."
    - "merge-judge.mjs --selfcheck exits 0 and eval-status.mjs + both runners pass node --check after the verbatim/light copy (D-01)."
    - "check-evals.mjs lints the trigger set GREEN: schema, >=8 positives + >=8 near-miss negatives, >=2 lz-tpp green-step AND >=2 lz-refactor refactor-step seam negatives, ASCII-only, email-allowlist-inversion (D-02, D-03)."
    - "evals/reciprocal-red.json holds every trigger-eval should_trigger:true positive re-tagged should_trigger:false, byte-consistent with those positives (D-03 reciprocal RED spot-check)."
    - "evals/d07-chunks/negatives.json is byte-consistent with the trigger-eval should_trigger:false slice (dual-write invariant)."
    - "run-recall-chunks.mjs and run-spec-chunks.mjs point --skill-path at lz-red and their CANARY_PREFIX matches a real should_trigger:true query in trigger-eval.json (WR-01 derive-from-set)."
    - "Per-run byproducts (run-*/, results*/, trigger-results-*.json) under lz-red-workspace are git-ignored; the shipped plugins/lz-tdd tree gains no build dep (D-10 corrected per RESEARCH Pitfall 8)."
  artifacts:
    - .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/run_eval.py
    - .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/utils.py
    - .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/__init__.py
    - .claude/skills/lz-red-workspace/tools/skill-creator-eval/LICENSE.upstream.txt
    - .claude/skills/lz-red-workspace/tools/skill-creator-eval/README.md
    - .claude/skills/lz-red-workspace/eval-status.mjs
    - .claude/skills/lz-red-workspace/merge-judge.mjs
    - .claude/skills/lz-red-workspace/run-recall-chunks.mjs
    - .claude/skills/lz-red-workspace/run-spec-chunks.mjs
    - .claude/skills/lz-red-workspace/check-evals.mjs
    - .claude/skills/lz-red-workspace/evals/trigger-eval.json
    - .claude/skills/lz-red-workspace/evals/reciprocal-red.json
    - .claude/skills/lz-red-workspace/evals/d07-chunks/negatives.json
    - .gitignore (3 added lz-red lines)
  key_links:
    - "check-evals.mjs <-> evals/trigger-eval.json + evals/reciprocal-red.json + evals/d07-chunks/negatives.json (dual-write byte-consistency + both-seam coverage)"
    - "run-recall-chunks.mjs / run-spec-chunks.mjs CANARY_PREFIX <-> a real should_trigger:true query in trigger-eval.json"
---

<objective>
Build the EVL-01 (trigger eval) half of the lz-red eval harness: copy the proven, native-fixed
vendored rig into the existing dev-only lz-red-workspace, adapt the build-time trigger lint to the
three-way boundary, author the RED trigger data + the reciprocal RED spot-check set, and add the
missing gitignore lines. This plan BUILDS assets only; it runs NO metered eval (D-11).

Purpose: EVL-01 needs (a) the skill-agnostic native-fixed probe copied verbatim (never re-fixed),
(b) a fail-closed lint proving the trigger set covers BOTH sibling seams + a reciprocal dual-write
invariant, and (c) the RED trigger data grounded in the shipped lz-red description. This is the
cross-skill-boundary delta that no prior phase produced (D-03).

Output: the vendored harness + generic scripts + light-edited runners + adapted check-evals.mjs +
trigger-eval.json / reciprocal-red.json / d07-chunks/negatives.json + 3 gitignore lines. All
build-time; the trigger + reciprocal RUNS are gated to the finalize plan (20-03).

## Locked decisions honored
- **D-01:** Copy the locally vendored, bug-fixed skill-creator-eval harness verbatim from the freshest working rig (.claude/skills/lz-refactor-workspace/); preserve Apache-2.0 provenance + the run_eval reimplementation note; author eval DATA, not eval INFRASTRUCTURE; additive under the existing lz-red-workspace, do NOT disturb check-red-references.mjs / extract-samples.mjs.
- **D-02:** Hand-author a ~20-24-query trigger set (~12 should-trigger RED prompts + ~12 genuinely-tricky near-miss should-not-trigger queries) grounded in the shipped lz-red description; near-misses cover BOTH the lz-tpp green-step and the lz-refactor refactor-step, plus generic negatives; concrete/varied register with code snippets and typos.
- **D-03:** Prove the three-way boundary. Forward (lz-red-centric) near-misses draw from BOTH sibling territories; author evals/reciprocal-red.json (the RED positives re-tagged should_trigger:false) so the finalize plan can run it against the lz-tpp AND lz-refactor skill-paths and assert both stay quiet on RED intent. Bounded spot-check, not a 3x3 matrix.
- **D-10 (corrected):** Everything lives under .claude/skills/lz-red-workspace/; NOT under plugins/lz-tdd/skills/lz-red/ and NOT under .planning/. RESEARCH Pitfall 8 OVERRIDES the CONTEXT phrasing: a per-skill gitignore edit IS required (the run-capture rules are hardcoded to lz-refactor).
- **D-11 (HARD GATE):** Build all EVL-01 assets; run NOTHING. No run_eval / claude -p invocation in this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-skill-effectiveness-evals/20-CONTEXT.md
@.planning/phases/20-skill-effectiveness-evals/20-RESEARCH.md
@.planning/phases/20-skill-effectiveness-evals/20-PATTERNS.md
@AGENTS.md

Ground truth for the trigger data (LOCKED skill under test -- do NOT modify):
@plugins/lz-tdd/skills/lz-red/SKILL.md
@plugins/lz-tdd/skills/lz-tpp/SKILL.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md

Read-blindness note (RESEARCH Pitfall 7): `git grep` returns a SILENT zero inside
.claude/skills/*-workspace/ (partially gitignored prefix). Use `rg` or Read/Glob for anything
under the workspace; never conclude absence from a git grep zero there.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the vendored harness + generic scripts verbatim, light-edit the two runners, add the gitignore lines</name>
  <files>.claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/run_eval.py, .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/utils.py, .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/__init__.py, .claude/skills/lz-red-workspace/tools/skill-creator-eval/LICENSE.upstream.txt, .claude/skills/lz-red-workspace/tools/skill-creator-eval/README.md, .claude/skills/lz-red-workspace/eval-status.mjs, .claude/skills/lz-red-workspace/merge-judge.mjs, .claude/skills/lz-red-workspace/run-recall-chunks.mjs, .claude/skills/lz-red-workspace/run-spec-chunks.mjs, .gitignore</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/run_eval.py, utils.py, __init__.py (VERBATIM sources)
    - .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/LICENSE.upstream.txt, README.md (README = COPY + EDIT)
    - .claude/skills/lz-refactor-workspace/eval-status.mjs, merge-judge.mjs (VERBATIM sources)
    - .claude/skills/lz-refactor-workspace/run-recall-chunks.mjs (analog; PATTERNS EDIT 1 SKILL line 19, path resolution lines 17-20, run config lines 109-113), run-spec-chunks.mjs (SKILL line 20)
    - .gitignore (lines 29-36 generic; lines 43-45 lz-refactor-specific run-capture block to mirror)
    - .planning/phases/20-skill-effectiveness-evals/20-PATTERNS.md ("tools/skill-creator-eval/ (harness, VERBATIM)" + "run-recall-chunks.mjs + run-spec-chunks.mjs (LIGHT EDIT)" + "<repo>/.gitignore (config, EDIT)")
  </read_first>
  <action>
    Copy the reusable rig from .claude/skills/lz-refactor-workspace/ into .claude/skills/lz-red-workspace/,
    additive alongside the existing Phase-16 content gate (never overwrite extract-samples.mjs, tools/check-red-references.mjs,
    tools/lib/*, package.json, tsconfig.json).

    VERBATIM copies (use `cp` to preserve bytes; do NOT hand-retype): the entire tools/skill-creator-eval/scripts/
    directory (run_eval.py, utils.py, __init__.py), tools/skill-creator-eval/LICENSE.upstream.txt,
    eval-status.mjs, and merge-judge.mjs. The native fix in run_eval.py is skill-agnostic (Windows select-on-pipe,
    whole-turn detection, ephemeral project-skill probe, --strict-mcp-config + --setting-sources project baked in) --
    do NOT re-fix it. Do NOT copy __pycache__/ or any *.pyc.

    COPY + EDIT tools/skill-creator-eval/README.md: copy it, then re-point the usage --skill-path example to
    .../plugins/lz-tdd/skills/lz-red, and edit the Pitfall-2 sibling note to name ALL THREE siblings (lz-tpp,
    lz-refactor, lz-red now ship in one plugin = triple steal-risk). Keep the Phase-5 provenance line honest and
    add a one-line note that this copy serves the lz-red eval.

    LIGHT EDIT the two runners (copy run-recall-chunks.mjs and run-spec-chunks.mjs, then change exactly two things):
    EDIT 1 -- re-point the SKILL constant from "lz-refactor" to "lz-red" (recall runner ~line 19, spec runner ~line 20);
    keep the import.meta.url path resolution (lines 17-20) and the locked run config (execFileSync args lines 109-113:
    serial --num-workers 1, PONYTAIL_DEFAULT_MODE=off, model claude-opus-4-8) VERBATIM. Leave the CANARY_PREFIX as a
    documented placeholder for now (Task 3 re-derives it from the authored trigger set); the CANARY only throws at RUN
    time, not during this build.

    Add the three lz-red-specific gitignore lines. Read .gitignore; the lz-refactor run-capture block is at lines 43-45
    (results*/, run-*/, trigger-results-*.json under lz-refactor-workspace). Add the mirrored lz-red block immediately
    after it (per RESEARCH Pitfall 8): the same three patterns scoped to .claude/skills/lz-red-workspace/**/. Prefer the
    additive lz-red lines over generalizing to *-workspace (which would newly-ignore already-tracked lz-tpp captures).
  </action>
  <verify>
    <automated>git diff --no-index --quiet .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/scripts/run_eval.py .claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/run_eval.py; echo run_eval=$?</automated>
    <automated>node --check .claude/skills/lz-red-workspace/eval-status.mjs && node --check .claude/skills/lz-red-workspace/run-recall-chunks.mjs && node --check .claude/skills/lz-red-workspace/run-spec-chunks.mjs && echo syntax-ok</automated>
    <automated>node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck; echo mergejudge=$?</automated>
    <automated>rg -c "lz-red-workspace" .gitignore</automated>
    <automated>rg -c "[^\x00-\x7F]" .claude/skills/lz-red-workspace/tools/skill-creator-eval/README.md || echo readme-ascii-ok</automated>
    <automated>rg -oN "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}" .claude/skills/lz-red-workspace/tools/skill-creator-eval/README.md | rg -v "larsbrinknielsen@gmail.com" || echo readme-email-ok</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --no-index --quiet` exits 0 for each of run_eval.py, utils.py, __init__.py, LICENSE.upstream.txt, eval-status.mjs, merge-judge.mjs (byte-identical to the lz-refactor source).
    - `node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck` exits 0 (verbatim copy intact).
    - `node --check` exits 0 for eval-status.mjs, run-recall-chunks.mjs, run-spec-chunks.mjs.
    - `rg -n "lz-red" .claude/skills/lz-red-workspace/run-recall-chunks.mjs` shows the SKILL constant re-pointed to lz-red (and same for the spec runner); `rg -c "lz-refactor" .claude/skills/lz-red-workspace/run-recall-chunks.mjs` returns 0.
    - README --skill-path names lz-red and the Pitfall-2 note names all three siblings (rg confirms lz-tpp, lz-refactor, lz-red all present).
    - README.md (the one COPY + EDIT file inside tools/skill-creator-eval/) is ASCII-only and allowlist-inversion clean: `rg "[^\x00-\x7F]"` finds no non-ASCII byte, and the only email-shaped token present is the approved public gmail. This closes the hygiene-coverage gap left by 20-03's directory-scoped exclusion of the vendored rig (the 4 byte-verbatim files carry only upstream provenance and are out of maintainer-identity scope per AGENTS.md; README is maintainer-edited, so it IS covered here at authoring time).
    - `.gitignore` contains exactly the three added patterns `.claude/skills/lz-red-workspace/**/results*/`, `.claude/skills/lz-red-workspace/**/run-*/`, `.claude/skills/lz-red-workspace/**/trigger-results-*.json` (rg count >= 3).
    - No file under __pycache__/ or any *.pyc was copied; the existing content gate files are untouched.
  </acceptance_criteria>
  <done>The vendored native-fixed harness, generic scripts, and lz-red-pointed runners exist under lz-red-workspace byte-faithful to their sources; merge-judge --selfcheck is GREEN; the gitignore drops lz-red per-run captures.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt check-evals.mjs to the three-way boundary (both seams + reciprocal dual-write + email-allowlist), asserted RED baseline</name>
  <files>.claude/skills/lz-red-workspace/check-evals.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/check-evals.mjs (analog; PATTERNS KEEP :42-59 schema, :61-71 split, :84-91 ASCII byte scan, :93-134 dual-write; ADAPT 1 seam :73-82; ADAPT 2 reciprocal mirror :117-134)
    - .claude/skills/lz-red-workspace/check-evals.mjs (this file, once copied)
    - AGENTS.md ("Detection: allowlist-inversion" -- assert only the approved gmail is present; never encode the forbidden value as a needle)
    - .planning/phases/20-skill-effectiveness-evals/20-PATTERNS.md ("check-evals.mjs (lint, ADAPT)")
    - .planning/phases/20-skill-effectiveness-evals/20-RESEARCH.md (## Common Pitfalls -> Pitfall 8; ## Security Domain seam-negative design note)
  </read_first>
  <action>
    Copy check-evals.mjs from the lz-refactor rig into lz-red-workspace, then adapt (keep the fail-closed spine,
    widen the seam rule, add two checks). This is the instrument-first step: it goes RED now (the eval data is absent)
    and Task 3 turns it GREEN.

    KEEP verbatim: the schema lint (:42-59), the >=8 should-trigger / >=8 should-not split (:61-71), the ASCII-only
    byte scan (:84-91), and the dual-write invariant that compares evals/d07-chunks/negatives.json against the
    trigger-eval negatives (:93-134).

    ADAPT 1 -- BOTH-seam negatives (replace the single-seam rule at :73-82): keep the lz-tpp green-step regex
    (failing test / make ... pass / minimal transformation / go green / green it / smallest edit-or-step) and ADD a
    second lz-refactor refactor-step regex (clean up / de-duplicate / refactor / restructure / tidy / messy / code
    smell, combined with a tests-green cue). Require >=2 negatives matching EACH seam independently; fail closed with a
    clear message naming which seam is short. This is the D-02/D-03 both-territories rule.

    ADAPT 2 -- reciprocal dual-write: add a check that evals/reciprocal-red.json exists, is an array, every entry is
    should_trigger:false, and its query strings are byte-consistent with the should_trigger:true positives of
    trigger-eval.json (the reciprocal set is the RED positives flipped). Mirror the existing dual-write comparison
    loop (:117-134).

    ADAPT 3 -- email-allowlist (repo hygiene; fold beside the ASCII scan at :84-91): sweep every present eval JSON
    file (evals/trigger-eval.json, evals/reciprocal-red.json, evals/d07-chunks/negatives.json, and evals/evals.json IF
    it exists -- tolerate its absence, it is authored in a parallel plan) and assert by ALLOWLIST-INVERSION that the
    only email-shaped token present is the approved public gmail (declared in AGENTS.md); flag anything else. NEVER
    write the forbidden work-email or its bare domain into the script, even as a search needle.

    Adjust the eval-set path constants only if the analog hardcodes lz-refactor filenames; the schema is identical
    ([{query, should_trigger}]) so the filenames match.
  </action>
  <verify>
    <automated>node --check .claude/skills/lz-red-workspace/check-evals.mjs && echo syntax-ok</automated>
    <automated>node .claude/skills/lz-red-workspace/check-evals.mjs; echo checkevals=$?</automated>
  </verify>
  <acceptance_criteria>
    - `node --check .claude/skills/lz-red-workspace/check-evals.mjs` exits 0 (valid syntax).
    - `node .claude/skills/lz-red-workspace/check-evals.mjs` exits NON-zero at this point (RED baseline: the trigger/reciprocal/negatives data does not exist yet). This proves the instrument fails closed.
    - `rg -n "should_trigger" .claude/skills/lz-red-workspace/check-evals.mjs` shows the reciprocal all-false + dual-write assertion added.
    - The script contains TWO seam regexes (a lz-tpp green-step matcher and a lz-refactor refactor-step matcher) each with a >=2 requirement (rg confirms two distinct filters).
    - The allowlist-inversion email check covers the three files 20-01 authors (evals/trigger-eval.json, evals/reciprocal-red.json, evals/d07-chunks/negatives.json) as REQUIRED inputs -- check-evals fails closed if any is absent, so its GREEN (Task 3) guarantees all three are swept clean -- while evals/evals.json (authored by the parallel 20-02) is scanned only opportunistically "if present", never as the sole guarantee for that file.
    - The check asserts only the approved public gmail may appear and flags any other email-shaped token; the forbidden work-email / bare domain appears NOWHERE in the file (the script encodes the allowlist, never the forbidden value as a needle).
  </acceptance_criteria>
  <done>check-evals.mjs is syntactically valid, fails closed on the absent data (RED baseline), and encodes the both-seam + reciprocal dual-write + email-allowlist adaptations.</done>
</task>

<task type="auto">
  <name>Task 3: Author the trigger-eval, reciprocal-red, and d07-chunks negatives sets; re-derive the runner CANARY; flip check-evals GREEN</name>
  <files>.claude/skills/lz-red-workspace/evals/trigger-eval.json, .claude/skills/lz-red-workspace/evals/reciprocal-red.json, .claude/skills/lz-red-workspace/evals/d07-chunks/negatives.json, .claude/skills/lz-red-workspace/run-recall-chunks.mjs, .claude/skills/lz-red-workspace/run-spec-chunks.mjs</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/SKILL.md (lines 3-15 the description = should/should-not ground truth incl. the lz-tpp + lz-refactor "Do NOT use" seam clauses)
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md + plugins/lz-tdd/skills/lz-refactor/SKILL.md (sibling green-step / refactor-step territory)
    - .claude/skills/lz-refactor-workspace/evals/trigger-eval.json + evals/d07-chunks/negatives.json (schema shapes to mirror)
    - .claude/skills/lz-red-workspace/check-evals.mjs, run-recall-chunks.mjs, run-spec-chunks.mjs (the gate + runners this data feeds)
    - .planning/phases/20-skill-effectiveness-evals/20-RESEARCH.md (## EVL-01 Query Candidates T1..T12 + N1..N12; ## Reciprocal RED spot-check set; seam-negative design note)
  </read_first>
  <action>
    Author evals/trigger-eval.json as a top-level array of {query, should_trigger}. Target ~12 should_trigger:true
    RED-phase prompts and ~12 should_trigger:false near-misses, grounded in the shipped lz-red description (SKILL.md:3-15).
    Draw wording from the RESEARCH candidate tables (T1..T12 positives spanning selection / structure / assertion-target /
    naming / stance / fail-for-the-right-reason / low-signal catch-all; N1..N12 negatives). The negatives MUST include
    >=2 lz-tpp green-step near-misses (make the failing test pass / minimal transformation / smallest edit), >=2
    lz-refactor refactor-step near-misses (tests green, clean up / de-duplicate / refactor), and generic negatives
    (plain feature work, SOLID explanation, debugging, complexity analysis, run-the-suite). Keep negatives clearly on a
    sibling's or a non-TDD facet -- avoid a "write a test" phrasing that reads as RED intent. Concrete and varied
    register: some with file paths, code snippets, casual speech, and typos. ASCII-only (->, straight quotes; no emojis,
    em-dashes, or smart quotes). Do NOT write the work-email or its bare domain anywhere.

    Author evals/d07-chunks/negatives.json as the exact should_trigger:false slice of trigger-eval.json, same entries in
    the same order (the check-evals dual-write invariant enforces byte-consistency; run-spec-chunks.mjs reads this file).

    Author evals/reciprocal-red.json as the trigger-eval should_trigger:true positives re-tagged should_trigger:false,
    reusing the positive query strings VERBATIM (check-evals asserts byte-consistency) so it is provably the exact RED
    prompts -- the set the finalize plan runs against the lz-tpp and lz-refactor skill-paths (D-03.2).

    Re-derive the runner CANARY (WR-01): choose ONE should_trigger:true positive as the canary and set CANARY_PREFIX in
    run-recall-chunks.mjs (~lines 34-39) and run-spec-chunks.mjs (~lines 27-30) to that query's leading substring, so the
    existing `all.find(q => q.should_trigger && q.query.startsWith(CANARY_PREFIX))` guard resolves to a real positive and
    the throw-guard is satisfied. Derive FROM the set; never hand-type a twin that can drift.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/check-evals.mjs; echo checkevals=$?</automated>
    <automated>node -e "const t=require('./.claude/skills/lz-red-workspace/evals/trigger-eval.json');const pos=t.filter(q=>q.should_trigger).length,neg=t.length-pos;console.log('pos',pos,'neg',neg)"</automated>
    <automated>node -e "const t=require('./.claude/skills/lz-red-workspace/evals/reciprocal-red.json');console.log('recip',t.length,'allfalse',t.every(q=>q.should_trigger===false))"</automated>
  </verify>
  <acceptance_criteria>
    - `node .claude/skills/lz-red-workspace/check-evals.mjs` exits 0 (GREEN: schema, >=8/>=8 split, both-seam >=2 each, reciprocal all-false + byte-consistent, negatives dual-write, ASCII, email-allowlist all pass).
    - trigger-eval.json has >=8 (target ~12) should_trigger:true and >=8 (target ~12) should_trigger:false entries.
    - reciprocal-red.json length equals the trigger positive count, every entry should_trigger:false, and each query byte-matches a trigger positive.
    - d07-chunks/negatives.json byte-matches the trigger-eval should_trigger:false slice (dual-write invariant holds -- proven by check-evals exit 0).
    - Both runners' CANARY_PREFIX is the leading text of a real should_trigger:true positive: `node -e` loading trigger-eval.json and calling `.find(q=>q.should_trigger && q.query.startsWith(PREFIX))` returns a truthy entry for each runner's prefix.
    - check-evals GREEN proves the allowlist-inversion + ASCII sweep is clean over the three files 20-01 authors -- evals/trigger-eval.json, evals/reciprocal-red.json, evals/d07-chunks/negatives.json: no non-ASCII byte and no email-shaped token other than the approved public gmail in any of them (self-covering; does not rely on the parallel 20-02's evals.json).
  </acceptance_criteria>
  <done>The EVL-01 trigger, reciprocal, and negatives data are authored, grounded in the shipped description, cover both seams, and pass the adapted check-evals GREEN; the runner canary is derived from a real positive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored eval content -> committed public repo | New committed JSON/prose (trigger queries, reciprocal set, negatives) crosses into a public repo |
| eval scripts -> local filesystem / claude CLI | Node/Python scripts read+write only under lz-red-workspace; they spawn the already-authenticated claude only at the GATED run (out of this plan's scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | committed eval JSON (queries, reciprocal set, negatives) | high | mitigate | check-evals.mjs email-allowlist-inversion (assert only the approved public gmail present, flag anything else) + re-run the allowlist scan before commit; never encode the forbidden work-email or its bare domain as a needle (AGENTS.md) |
| T-20-02 | Information Disclosure | committed eval prose (queries with code + typos) | low | mitigate | check-evals.mjs ASCII-only byte scan (->, straight quotes; no emojis / em-dashes / smart quotes) |
| T-20-SC | Tampering (supply chain) | npm/pip installs | n/a | accept | No package install in this plan; the vendored harness is copied byte-identical from the on-disk Phase-11 rig (Apache-2.0), workspace devDeps pre-exist. Package-legitimacy gate vacuously satisfied (RESEARCH). |
| T-20-03 | Denial of Service (token spend) | gated claude -p run | n/a (out of scope) | mitigate | D-11 HARD GATE: this plan runs nothing; the trigger + reciprocal runs are user-approved and presented in 20-03 |
</threat_model>

## Artifacts this plan produces

- `.claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/run_eval.py` (VERBATIM)
- `.claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/utils.py` (VERBATIM)
- `.claude/skills/lz-red-workspace/tools/skill-creator-eval/scripts/__init__.py` (VERBATIM)
- `.claude/skills/lz-red-workspace/tools/skill-creator-eval/LICENSE.upstream.txt` (VERBATIM)
- `.claude/skills/lz-red-workspace/tools/skill-creator-eval/README.md` (COPY + EDIT: skill-path + triple-sibling note)
- `.claude/skills/lz-red-workspace/eval-status.mjs` (VERBATIM)
- `.claude/skills/lz-red-workspace/merge-judge.mjs` (VERBATIM)
- `.claude/skills/lz-red-workspace/run-recall-chunks.mjs` (LIGHT EDIT: SKILL -> lz-red, CANARY re-derived)
- `.claude/skills/lz-red-workspace/run-spec-chunks.mjs` (LIGHT EDIT: SKILL -> lz-red, CANARY re-derived)
- `.claude/skills/lz-red-workspace/check-evals.mjs` (ADAPT: both seams + reciprocal dual-write + email-allowlist)
- `.claude/skills/lz-red-workspace/evals/trigger-eval.json` (AUTHOR: ~12 positives + ~12 near-misses)
- `.claude/skills/lz-red-workspace/evals/reciprocal-red.json` (AUTHOR NEW: RED positives re-tagged should_trigger:false)
- `.claude/skills/lz-red-workspace/evals/d07-chunks/negatives.json` (AUTHOR: negative slice, dual-write)
- `.gitignore` (EDIT: 3 added lz-red-workspace run-capture lines)

<verification>
Deterministic, zero-spend build-time gates (from repo root):
- `node .claude/skills/lz-red-workspace/check-evals.mjs` -> exit 0 (schema, both-seam split, reciprocal + negatives dual-write, ASCII, email-allowlist).
- `node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck` -> exit 0 (verbatim copy intact).
- `node --check` on eval-status.mjs + both runners -> exit 0.
- The existing content gate is unregressed: `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` and `node .claude/skills/lz-red-workspace/extract-samples.mjs` still exit 0 (this plan adds files, does not touch them).

NO metered run in this plan (D-11). The trigger + reciprocal RUN commands are presented in 20-03 and gated on user approval.
</verification>

<success_criteria>
- The vendored harness + generic scripts are byte-identical copies; merge-judge --selfcheck GREEN.
- check-evals.mjs GREEN over the authored trigger + reciprocal + negatives sets (both seams >=2 each; reciprocal all-false + byte-consistent; ASCII; email-allowlist).
- Both runners point at lz-red with a CANARY derived from a real positive.
- The gitignore drops lz-red per-run captures; no new build dep in plugins/lz-tdd.
</success_criteria>

<output>
Create `.planning/phases/20-skill-effectiveness-evals/20-01-SUMMARY.md` when done.
</output>
