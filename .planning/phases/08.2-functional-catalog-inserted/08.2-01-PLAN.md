---
phase: 08.2-functional-catalog-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/check-functional.mjs
  - .claude/skills/lz-refactor-workspace/extract-samples.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
  - .claude/skills/lz-refactor-workspace/package.json
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md
autonomous: true
requirements: [FUN-01, FUN-04]

must_haves:
  truths:
    - "Running node tools/check-functional.mjs exits 1 with a RED summary against the empty catalog (0/19 leaves; 55 missing Functional alternative: lines; no README rows)"
    - "The 8 pre-existing checkers still exit 0 individually (no regression); npm run typecheck exits 0 (0 functional modules, vacuously GREEN)"
    - "check-functional owns the 19-name LEAVES inventory, OO_LEAF_DIRS, and NOTE_ITEMS constants and imports the shared github-slug helper (no second slug/resolver implementation)"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/check-functional.mjs"
      provides: "The C1-C8 functional-catalog gate; RED baseline by design"
      contains: "githubSlug"
      min_lines: 120
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md"
      provides: "Scaffold N:1 map (empty table + ## Sources) so the selector-mirror check has a target to be RED against"
      contains: "## Sources"
    - path: ".claude/skills/lz-refactor-workspace/package.json"
      provides: "check chain extended with check-functional"
      contains: "check-functional.mjs"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/tools/check-functional.mjs"
      to: ".claude/skills/lz-refactor-workspace/tools/lib/github-slug.mjs"
      via: "import { githubSlug }"
      pattern: "import.*github-slug"
    - from: ".claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog"
      via: "collectLeafFiles(FUNCTIONAL) added to sourceFiles"
      pattern: "FUNCTIONAL"
  decisions: |
    - **D-05:** check-functional enforces the selector-mirror (C3), the Correspondence closed enum (C2), bidirectional + intra-leaf-anchor link integrity (C4/C5), the one-line-per-served-pattern cap (C5), and tsc --strict on every Example (C6)
    - **D-12:** check-functional.mjs lives in .claude/skills/lz-refactor-workspace/tools/ and is wired into package.json check + the npm run typecheck extractor
    - **D-13:** the canonical 19-leaf idiom inventory (13 dissolves-from + 6 alternative-to), their slugs, Correspondence enum values, and served-pattern sets are encoded as the checker LEAVES constant (derived by the researcher in RESEARCH.md Deliverable 1)
---

<objective>
Build the correctness anchor FIRST (instrument-first / Nyquist scaffold): a new
`check-functional.mjs` gate driven to a RED baseline, plus the three one-line harness
extensions and a scaffold README, BEFORE any leaf content is authored. This is the
Phase 7/8/8.1 pattern -- FUN-01..04 close only when the full battery turns GREEN in
later waves.

Purpose: give every downstream authoring plan a machine-checkable target. There is NO
owned book oracle this phase (D-06); check-functional + `tsc --strict` + the committed
research artifact ARE the correctness anchor.

Output: check-functional.mjs (C1-C8), extended extract-samples.mjs + check-crossrefs.mjs,
package.json chain entry, functional-catalog/README.md scaffold, and an explicitly
asserted RED baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md

Design source of record (the check-functional spec): `.planning/research/functional-depatterning-ts.md` Section 13.4.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author check-functional.mjs to a RED baseline (C1-C8)</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (the "check-functional.mjs Specification (Deliverable 4)" sub-check table C1-C8; the "Canonical Idiom-Leaf Inventory" tables for the 19 LEAVES constant; "Don't Hand-Roll")
    - .planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md (the check-functional.mjs section: exact copy sources by line number)
    - .claude/skills/lz-refactor-workspace/tools/check-gof.mjs (skeleton to copy: imports+repoRoot, report(), isDir/walkMd, collectLeaves(), readmeRowsBySlug(), sectionBody(), slugFor, SCAFFOLD_RES, NAMES/REQUIRED_AWAY, SUMMARY/exit tail)
    - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs (the linkify-gate parse-link + exact-name-set membership pattern for C2/C4)
    - .claude/skills/lz-refactor-workspace/tools/lib/github-slug.mjs (the anchor slug helper to import)
  </read_first>
  <action>
    Create `.claude/skills/lz-refactor-workspace/tools/check-functional.mjs` as a faithful sibling of check-gof.mjs.
    Copy the import block + `const repoRoot = path.resolve(here, "..", "..", "..", "..")`, `report()`, `isDir`, `walkMd`,
    `collectLeaves()` (also skip readme.md), `readmeRowsBySlug()`, `sectionBody()`, `slugFor`, and the SCAFFOLD_RES draft-phrase
    guard. Import `githubSlug` from `./lib/github-slug.mjs` (FILENAME uses slugFor; `#anchor` uses githubSlug -- keep both).
    Declare three owned constants: LEAVES = the 19 idiom entries (H1 name + expected slug + Correspondence enum value in
    {dissolves-from, alternative-to} + served-pattern set) from RESEARCH.md Deliverable 1; OO_LEAF_DIRS = ["gof-catalog",
    "kerievsky-catalog", "extra-patterns-catalog"]; NOTE_ITEMS = the Fowler moot/data-modeling/light note source items.
    Implement C1-C8 as report() lines per the RESEARCH.md Deliverable 4 table: C1 (19 idiom H1 identity + filename==slug +
    Use when:/Correspondence:/Keep the OO form when:/## Idiom/## Example with a ts fence inside it/## When each fits; reject
    unknown H1), C2 (Correspondence value is the closed enum + >=1 link whose URL matches
    `../{gof,kerievsky,extra-patterns,fowler}-catalog/<slug>.md#<anchor>` -- INCLUDE fowler-catalog as an accepted
    Correspondence target dir per A3/D-13 resolution: lens-and-optics/transducers/normalized-store link to Fowler analogs
    resolution-only), C3 (selector-mirror N:1 + one-line note-cell cap), C4 (each gof/kerievsky/extra leaf carries exactly one
    Functional alternative: link resolving to an idiom leaf whose Correspondence names it back -- forward-subset mutuality,
    modeled on check-gof REQUIRED_AWAY; C4's leaf enumeration over OO_LEAF_DIRS MUST mirror check-kerievsky.mjs collectLeaves -- skip readme.md AND any -walkthrough.md companion -- so it demands the line on EXACTLY 55 leaves (23 gof + 27 kerievsky + 5 extra), never the 3 kerievsky -walkthrough.md companions), C5 (per N:1 leaf, exactly one `### <Pattern>` sub-heading per served pattern under
    `## When each fits`, each followed by exactly ONE non-blank content line; README row targets `<slug>.md#<pattern-anchor>`;
    1:1 leaves get no split), C6 (assert each leaf has >=1 non-ignore ts fence inside `## Example`; the compile itself is
    delegated to npm run typecheck), C7 (every NOTE_ITEMS source appears as a one-line README note with NO functional-catalog
    leaf link), C8 (README `## Sources` cites `.planning/research/functional-depatterning-ts.md`). DELEGATE all file+anchor
    RESOLUTION to check-crossrefs (do not re-implement a resolver -- assert PRESENCE/format only). End with the
    `SUMMARY: ... GREEN/RED` + `process.exit(0|1)` tail exactly as check-gof does. Node builtins + github-slug only; ASCII-only.
  </action>
  <acceptance_criteria>
    - `.claude/skills/lz-refactor-workspace/tools/check-functional.mjs` exists and imports githubSlug from ./lib/github-slug.mjs
    - The file declares LEAVES (19 entries), OO_LEAF_DIRS (3), and NOTE_ITEMS constants
    - `node .claude/skills/lz-refactor-workspace/tools/check-functional.mjs` runs without a thrown exception and prints a `SUMMARY: ... RED` line and exits 1 (RED baseline: 0/19 leaves present)
    - The C2 URL-shape acceptance set includes `fowler-catalog` (A3 resolution)
    - No second slug function and no second link resolver are defined (resolution delegated to check-crossrefs)
    - C4's leaf enumeration excludes readme.md and -walkthrough.md companions (mirrors check-kerievsky collectLeaves); EXACTLY 55 leaves (23 gof + 27 kerievsky + 5 extra) must carry the Functional alternative: line, so the RED baseline reads 55 missing
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-functional.mjs; test $? -eq 1</automated>
  </verify>
  <done>check-functional.mjs exits 1 (RED) with the expected 0/19 baseline and the C1-C8 sub-check lines print.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the three harness extensions + scaffold the README</name>
  <read_first>
    - .claude/skills/lz-refactor-workspace/package.json (the check script chain, line 8)
    - .claude/skills/lz-refactor-workspace/extract-samples.mjs (the CATALOGS array, lines 22-27)
    - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs (dir consts lines 28-33; the sourceFiles array lines 104-106; the inverse-of guard comment lines 99-103)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md (the ## Sources + intro-prose shape to mirror for the scaffold)
  </read_first>
  <action>
    (a) package.json: append ` && node tools/check-functional.mjs` to the end of the existing `check` script chain (after
    check-hygiene.mjs). Leave `typecheck` unchanged.
    (b) extract-samples.mjs: add exactly one entry to the CATALOGS array:
    `{ dir: path.join(REFERENCES, "functional-catalog"), prefix: "functional" }`.
    (c) check-crossrefs.mjs: define `const FUNCTIONAL = path.join(REFERENCES, "functional-catalog");` beside the other dir
    consts, and add `...collectLeafFiles(FUNCTIONAL)` to the `sourceFiles` array. Do NOT add FUNCTIONAL to the inverse-of
    mutuality guard (functional links are one-directional; the guard stays CATALOG/Fowler-scoped, mirroring the existing
    kerievsky<->gof exclusion).
    (d) Scaffold `plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md`: an intro paragraph declaring the
    N:1 pattern->idiom contract (siblings are 1:1 selector-mirrors; this one fans many rows into one leaf), an empty map table
    header, and a `## Sources` block that cites `.planning/research/functional-depatterning-ts.md` with a DST-04 no-verbatim
    disclaimer. ASCII-only. This is a scaffold (rows are finalized in plan 08.2-05); it exists so C3/C8 have a target to be RED
    against.
  </action>
  <acceptance_criteria>
    - `package.json` check chain ends with `&& node tools/check-functional.mjs`
    - `extract-samples.mjs` CATALOGS contains a functional-catalog entry with prefix "functional"
    - `check-crossrefs.mjs` defines FUNCTIONAL and includes collectLeafFiles(FUNCTIONAL) in sourceFiles; the inverse-of guard is unchanged
    - `functional-catalog/README.md` exists, declares the N:1 contract, and contains `## Sources` citing the research artifact
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node -e "process.exit(require('fs').readFileSync('package.json','utf8').includes('check-functional.mjs')?0:1)"</automated>
  </verify>
  <done>All three harness files carry their one-line extension and the scaffold README exists with a ## Sources block.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the RED baseline and no regression</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md (the "RED-baseline assertion set" bullet list)
    - .planning/STATE.md (the Phase 08.1-01 RED-baseline assertion precedent -- what must FAIL vs STAY GREEN)
  </read_first>
  <action>
    From `.claude/skills/lz-refactor-workspace/`, verify and record the RED baseline in the SUMMARY:
    (a) `node tools/check-functional.mjs` exits 1 -- RED: 0/19 idiom leaves present, 55 gof/kerievsky/extra leaves missing the
    `Functional alternative:` line, no README rows. This is the EXPECTED instrument-first baseline, not a defect.
    (b) `npm run typecheck` (node extract-samples.mjs) exits 0 -- 0 functional modules extracted (vacuously GREEN; the tsc
    signal only becomes meaningful once Examples exist).
    (c) Each of the 8 pre-existing checkers (check-catalog, check-kerievsky, check-gof, check-extra-patterns, check-smells,
    check-crossrefs, check-principles, check-hygiene) still exits 0 individually -- no regression. (Note: `npm run check` as a
    whole now exits 1 because it includes check-functional; that is expected. Assert the 8 pre-existing checkers pass, and
    check-functional is the sole RED.)
    Do NOT author any leaf content, README rows, or Functional alternative: links in this plan -- those are waves 2-4.
  </action>
  <acceptance_criteria>
    - `node tools/check-functional.mjs` exits 1 with a RED summary
    - `node extract-samples.mjs` exits 0
    - check-crossrefs.mjs exits 0 (no Functional alternative: links added yet, so nothing dangles)
    - The other 7 pre-existing checkers each exit 0
    - The SUMMARY.md records the RED baseline explicitly as the expected Wave-1 state
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-crossrefs.mjs && node tools/check-gof.mjs && node tools/check-kerievsky.mjs && node extract-samples.mjs && (node tools/check-functional.mjs; test $? -eq 1)</automated>
  </verify>
  <done>RED baseline asserted and documented; the 8 pre-existing checkers + typecheck stay GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only the NON-shipped checker harness + a scaffold Markdown README. There is
no runtime, network, session, crypto, or user-input surface in the shipped plugin. The only
boundaries are repo-hygiene invariants.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | committed files must stay ASCII-only, work-email-free, and free of verbatim book expression |
| devDependency -> local build | the pinned typescript@6.0.3 the extractor runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-SC | Tampering | typescript@6.0.3 devDependency | accept | No new package installs this plan; typescript@6.0.3 was pinned + audited in Phase 7. package.json devDependencies unchanged. |
| T-08.2-IP | Information Disclosure | committed checker + README text | mitigate | ASCII-only; no book prose. check-hygiene already walks references/ recursively and will gate the README. |
| T-08.2-EM | Information Disclosure | committed files | mitigate | Work-email allowlist enforced by check-hygiene (MEMORY: work-email allowlist recurred twice). |
</threat_model>

<verification>
- `node tools/check-functional.mjs` exits 1 with the documented RED baseline.
- The 8 pre-existing checkers and `npm run typecheck` exit 0 (no regression).
- No leaf content authored; README is a scaffold only.
</verification>

<success_criteria>
check-functional.mjs exists with C1-C8 and the 19-name LEAVES inventory; the three harness
extensions are wired; the scaffold README exists; the RED baseline is asserted and recorded.
</success_criteria>

<output>
Create `.planning/phases/08.2-functional-catalog-inserted/08.2-01-SUMMARY.md` when done.
</output>
