---
phase: 17-assertion-design-stance-router-ts-vitest-mechanics
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md
autonomous: true
requirements: [RTR-01, ASRT-02]
must_haves:
  truths:
    - "testing-stance/README.md is a NAVIGATION-ONLY index: detection signals + a route table linking each signal to functional-core.md / message-matrix.md / seams-and-legacy.md, carrying no stance content of its own"
    - "functional-core.md (Bernhardt FCIS, no-oracle) carries the Signal / Assert rule (value-based over the core) / Mock rule (no doubles in the core), scoped to when the code IS value-in/value-out, with a tsc-strict Vitest fence"
    - "seams-and-legacy.md (Feathers, no-oracle) carries seam / characterization / sequencing as the RED-step framing and CROSS-LINKS ../../../lz-refactor/references/refactoring-without-tests.md rather than copying it"
    - "no README stance content leak (Pitfall 4); FCIS is not presented as the default (Pitfall 3, brownfield routes to Feathers)"
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md
  key_links:
    - "README route table -> the three leaves (the RTR-01 navigation spine)"
    - "seams-and-legacy.md -> ../../../lz-refactor/references/refactoring-without-tests.md (cross-skill link, not copy)"
---

<objective>
Author the testing-stance navigation index and its two NO-ORACLE leaves: the README route table
(detection signals -> leaf), Bernhardt's functional-core leaf (output-based, no doubles), and the
Feathers seams-and-legacy leaf (characterization; cross-linked to lz-refactor, not copied). This is
the differentiator's navigation half plus the two blind-authored stance leaves; the owned Metz
message-matrix leaf is Plan 17-04.

Purpose: close RTR-01 (the stance subdir: README + 3 leaves, Feathers cross-linked) and the ASRT-02
per-leaf assert rules for the functional-core and legacy routes.
Output: a nav-only README + two own-words leaves, the functional-core fence tsc --strict clean.

Executor note: all three files here are NO-ORACLE -- authored blind from high-confidence core (no
oracle gate; no `.oracle/` read). The no-verbatim scan is the only DST-04 backstop for these.

Decisions implemented (decision-coverage):
- **D-02:** Bernhardt (FCIS) and Feathers (seams/characterization) are NO-ORACLE -- authored blind
  from high-confidence core, no-verbatim scan only.
- **D-04:** ASRT-02 per-leaf assert rules -- functional-core carries the output-based rule; the
  legacy leaf carries the characterization (pin current behavior) rule.
- **D-06:** RTR-01 -- author `testing-stance/` at content grain (README nav-only + leaves); the
  Feathers leaf CROSS-LINKS `refactoring-without-tests.md` (RED-step framing only), does not copy.
- **D-14:** no-verbatim gate -- own-words, deterministic no-verbatim scan (no owned surface here).
- **D-15:** co-edit, do not split -- fill the RTR-01 index/leaf content; the RTR-02 SKILL.md wiring
  is Phase 18 (not touched); the README's Phase-18 wiring note stays out of the nav content.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-RESEARCH.md
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md
@plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
@plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md
@plugins/lz-tdd/skills/lz-refactor/references/smells.md
@plugins/lz-tdd/skills/lz-red/references/naming.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author testing-stance/README.md (navigation-only route table)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md (its own nav-only content contract lines 14-27; the Phase-15 Scope paragraph declares "NAVIGATION ONLY")
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (the nav-index analog: recognize-by cue + leaf link, zero stance content -- lines 1-24)
    - 17-RESEARCH.md "Testing-Stance Router" README route table (lines 284-293) + 17-PATTERNS.md "testing-stance/README.md" (lines 72-103)
    - Environment (worktree bootstrap): if `.claude/skills/lz-red-workspace/node_modules/` is absent (a fresh git worktree; the tree is gitignored), run `npm --prefix .claude/skills/lz-red-workspace install` BEFORE any `run typecheck` in this plan (Tasks 2-3). check-red-references.mjs and check-hygiene.mjs are dependency-free Node builtins -- no bootstrap needed for those.
  </read_first>
  <action>
    Fill the README as a NAVIGATION-ONLY index (Pitfall 4: never accrete stance content here). Keep
    the existing Scope paragraph. Add a 3-row route table (Detection signal | Route to):
    - value-in / value-out code; pure functions; no mocking needed -> functional-core.md
    - object with collaborators; a query/command message split -> message-matrix.md
    - untested legacy; hidden I/O / statics; no obvious place to assert -> seams-and-legacy.md
    Each "Route to" cell is a Markdown link to the leaf. Carry recognize-by signals + links ONLY; the
    assert/mock guidance lives in the leaves. Rewrite the draft `## Sources` heading to a plain
    "## Sources" (router-only: note the leaves carry the sourced stance content). No fence (nav-only;
    the extractor skips READMEs). Leave the Phase-18 SKILL.md-wiring (RTR-02) out of the nav content --
    it is a one-line pointer at most, not stance guidance. ASCII-only.
  </action>
  <acceptance_criteria>
    - A route table with the tokens "detection signal" and "route table" (or "route to") is present.
    - Links to functional-core.md, message-matrix.md, and seams-and-legacy.md are all present.
    - No assert/mock stance content is inlined (the table carries recognize-by + link only).
    - The draft `## Sources` heading is a plain `## Sources` (no draft parenthetical); no scaffold
      phrase remains; check-hygiene ASCII clean.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>README is a nav-only route table linking the three leaves; hygiene GREEN; the check output shows the README row PASS (requireFence false).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author functional-core.md (Bernhardt FCIS, output-based, no doubles)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md (content contract lines 13-26: Signal / Assert rule / Mock rule)
    - plugins/lz-tdd/skills/lz-red/references/naming.md (bullet triad + Sources access-tier form) and three-laws-and-test-selection.md lines 54-72 (the fence shape)
    - 17-RESEARCH.md functional-core (lines 295-302) + Pitfall 3 brownfield trap (lines 554-562) + 17-PATTERNS.md "functional-core.md" (lines 106-124)
  </read_first>
  <action>
    Fill the leaf own-words (no-oracle). Sections carrying the contract fields Signal / Assert rule /
    Mock rule:
    - Functional core vs imperative shell: decision logic in pure functions, I/O in a thin shell.
    - Signal: value-in / value-out code; pure functions; no mocking needed.
    - Assert rule (ASRT-02, output-based): feed inputs, assert the returned value; this is the
      highest-resistance-to-refactoring style.
    - Mock rule: the core needs NO doubles -- purity removes the need to mock; the shell gets a thin
      band of integration tests at the boundary.
    Scope strictly to "when the code IS value-in/value-out" (Pitfall 3): do NOT present FCIS as the
    default; note that no-seam legacy routes to seams-and-legacy.md, not here. Add ONE self-contained
    tsc-strict Vitest fence: an output-based example (pure function; assert the returned value; no
    doubles), mirroring the three-laws fence shape; module-scoped; no DOM / Node builtins. Rewrite the
    draft `## Sources` heading to `## Sources` with a Bernhardt no-oracle row (canonical tier string).
    ASCII-only.
  </action>
  <acceptance_criteria>
    - Tokens present: functional core, imperative shell, value-based (or output-based), no doubles.
    - The leaf scopes FCIS to value-in/value-out code and points no-seam legacy to seams-and-legacy.md
      (not a default-everywhere framing).
    - At least one tsc --strict Vitest fence present and compiling under `run typecheck`.
    - `## Sources` (no draft parenthetical) with a Bernhardt no-oracle row; check-hygiene clean.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>functional-core.md carries Signal/Assert/Mock rules + a compiling output-based fence, scoped to a functional core; typecheck + hygiene GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author seams-and-legacy.md (Feathers, characterization, cross-linked to lz-refactor)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md (content contract lines 14-29: Technique / When-to-use / Assert rule; line 27-29 defers the cross-link to fill time)
    - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md (the CROSS-LINK TARGET -- link, do NOT copy its technique bodies)
    - 17-RESEARCH.md seams-and-legacy (lines 303-318, the verified relative path) + 17-PATTERNS.md "seams-and-legacy.md" (lines 157-182)
  </read_first>
  <action>
    Fill the leaf own-words (no-oracle), RED-step framing ONLY (write the pinning test), with the
    contract fields Technique / When-to-use / Assert rule:
    - Seam: a place where behavior can be altered without editing in place -- the substitution/
      observation point where a test can assert.
    - Characterization test: exercise the code, observe the ACTUAL output, assert that observed value
      to pin CURRENT behavior (not desired behavior) so a later change becomes detectable. Assert rule:
      it pins current behavior.
    - Sequencing: find a seam -> write a characterization test to pin current behavior -> THEN drive
      the new behavior test-first.
    Do NOT duplicate the refactor-step technique bodies (seam taxonomy, change algorithm, Sprout/Wrap,
    Extract Interface, Subclass and Override) -- they live in lz-refactor. Add a CROSS-LINK to
    [refactoring-without-tests.md](../../../lz-refactor/references/refactoring-without-tests.md) for
    those techniques (the verified relative path up out of testing-stance/ -> references/ -> lz-red/ ->
    skills/, then down into lz-refactor/references/). Rewrite the draft `## Sources` heading to
    `## Sources` with a Feathers no-oracle row noting "cross-linked to, not copied from, lz-refactor's
    Feathers reference". No fence required (prose-heavy). ASCII-only.
  </action>
  <acceptance_criteria>
    - Tokens present: seam, characterization, sequencing.
    - The exact string refactoring-without-tests.md appears in a cross-link (the instrument cross-link
      presence guard passes); the leaf does NOT reproduce the refactor-step technique bodies.
    - The assert rule states the characterization test pins CURRENT behavior, not desired behavior.
    - `## Sources` (no draft parenthetical) with a Feathers no-oracle row; check-hygiene no-verbatim
      clean (no duplicated lz-refactor prose).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && npm --prefix .claude/skills/lz-red-workspace run typecheck</automated>
  </verify>
  <done>seams-and-legacy.md carries seam/characterization/sequencing + the refactoring-without-tests.md cross-link, no copied prose; hygiene + typecheck GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lz-refactor prose -> lz-red leaf | the Feathers technique bodies must be LINKED, not copied -- duplication would trip the no-verbatim scan and blur the RED/refactor seam. |
| author identity -> committed content | maintainer work-email / domain must never appear; author/committer = public gmail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure (copyright, DST-04) | Feathers / Bernhardt leaves | high | mitigate | No-oracle authoring blind from high-confidence core; no `.oracle/` read; deterministic no-verbatim scan (>=120-char quoted run) per commit; skill-reviewer PASS at the phase gate (Plan 17-06). The Feathers leaf cross-LINKS lz-refactor rather than copying (also caught by the no-verbatim scan). |
| T-17-02 | Information Disclosure (PII) | committed Markdown + commit identity | high | mitigate | check-hygiene allowlist-inversion; author/committer = public gmail. |
| T-17-03 | Tampering (mojibake) | ASCII shipped tree | medium | mitigate | check-hygiene ASCII byte scan; `--` / `->` only. |
</threat_model>

<verification>
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 (functional-core fence compiles tsc --strict).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` shows the README, functional-core.md, and seams-and-legacy.md rows PASS, incl. the seams cross-link guard (sibling stubs RED until their Wave-2 plans land -- expected).
</verification>

<success_criteria>
- RTR-01 navigation index + the two no-oracle stance leaves are authored own-words; the README is nav-only; the Feathers leaf cross-links, not copies.
- ASRT-02 per-leaf assert rules (output-based; characterization) are present.
- typecheck + hygiene GREEN; Phase-18 RTR-02 wiring untouched.
</success_criteria>

## Artifacts this phase produces (Plan 17-03)

- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md`
  (nav-only route table -> 3 leaves).
- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md`
  (Bernhardt FCIS: Signal/Assert/Mock + one tsc-strict output-based fence).
- MODIFIED (filled): `plugins/lz-tdd/skills/lz-red/references/testing-stance/seams-and-legacy.md`
  (Feathers: seam/characterization/sequencing + the refactoring-without-tests.md cross-link).
- No new files. No new dependency. No owned surface in this plan (all no-oracle).

<output>
Create `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-03-SUMMARY.md` when done.
</output>
