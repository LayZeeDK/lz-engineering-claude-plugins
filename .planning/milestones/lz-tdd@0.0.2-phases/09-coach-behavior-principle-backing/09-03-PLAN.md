---
phase: 09-coach-behavior-principle-backing
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md
  - plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md
  - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md
autonomous: true
requirements: [PRIN-01, PRIN-02, PRIN-03]

must_haves:
  truths:
    - "beck-tdd-by-example.md (PRIN-01) exists and covers the red-green-refactor cycle, the two rules, and the Fake It / Triangulate / Obvious Implementation green-bar strategies, scoped to the seam and explicitly naming lz-tpp; it carries the no-oracle provenance tag"
    - "beck-tidy-first.md (PRIN-02) exists and covers structural-vs-behavioral change separation + refactor economics (coupling, cohesion, options); it cross-references overlapping Fowler refactorings BY LINK (>=1 ../fowler-catalog/<slug>.md link) rather than restating their mechanics, claims no complete tidyings catalog, and carries the no-oracle tag"
    - "refactoring-without-tests.md (PRIN-03) is populated against its declared per-entry contract for seams, characterization tests, the change algorithm, Sprout/Wrap Method+Class, Subclass and Override Method, and Extract Interface; the scaffold markers are removed (the 'Populated in Phase 9.' blockquote and the '(placeholder)' in the '## Sources (placeholder)' heading) and it carries the no-oracle tag"
    - "All three refs are DST-04-clean (original prose; the famous Beck/Feathers one-liners are paraphrased, not quoted; technique/strategy NAMES are the only verbatim facts); node tools/check-backing.mjs exits 0 (GREEN) once all three are authored"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md"
      provides: "PRIN-01 no-oracle Beck TDD-by-Example backing (cycle, two rules, green-bar strategies), seam-scoped"
      contains: "no-oracle"
      min_lines: 25
    - path: "plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md"
      provides: "PRIN-02 no-oracle Beck Tidy First? backing (structural/behavioral separation + economics), Fowler cross-links by link"
      contains: "fowler-catalog/"
      min_lines: 25
    - path: "plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md"
      provides: "PRIN-03 populated Feathers no-tests backing (seams, characterization tests, change algorithm, sprout/wrap, subclass-and-override, extract interface)"
      contains: "no-oracle"
      min_lines: 40
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog"
      via: "cross-reference overlapping Fowler refactorings by link (PRIN-02)"
      pattern: "fowler-catalog/[a-z0-9-]+\\.md"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md"
      to: "self (per-entry content contract)"
      via: "each core technique authored as Technique / When-to-use / Distilled mechanics"
      pattern: "Distilled mechanics"
  decisions: |
    - **D-06:** Beck TDD by Example (PRIN-01) and Beck Tidy First? (PRIN-02) each get their OWN no-oracle reference file (beck-tdd-by-example.md, beck-tidy-first.md), mirroring the standalone Feathers file; PRIN-03 populates the existing refactoring-without-tests.md stub; principles.md stays the Fowler-oracle file (its Beck cross-ref pointers are added in 09-04)
    - **D-07:** all three refs are tagged NO-ORACLE, high-confidence CORE only; Beck/Feathers have no owned oracle AND no committed research artifact, so the correctness anchor is tight core scope + skill-reviewer PASS + DST-04 hygiene; the Phase 6 D-09 AskUserQuestion oracle-access checkpoint is NOT opened for these; each ref names only the book (no research artifact to cite)
    - **D-08:** PRIN core scope is fixed and NOT expanded -- Beck TDD = cycle + two rules + Fake It / Triangulate / Obvious Implementation (seam-scoped); Beck Tidy First? = structural-vs-behavioral separation + economics (coupling / cohesion / options), Fowler cross-refs by link, no complete tidyings catalog claimed (FUT-01); Feathers = seams, characterization tests, the change algorithm, Sprout/Wrap Method+Class, Subclass and Override Method, Extract Interface
    - **D-11:** principle-reference content is authored fence-safe -- TS fences (comments are //, never a column-0 hash) or fence-free prose; no non-TS fenced block (bash/yaml/toml/dockerfile) with a column-0 hash line (keeps the check-crossrefs slugsFor fence gap dormant)
---

<objective>
Author the three no-oracle principle-backing references on top of the RED check-backing gate from
09-01. Two are new files (Beck TDD by Example, Beck Tidy First?); the third populates the existing
Feathers stub. They turn check-backing GREEN.

Purpose: PRIN-01/02/03 -- the coach's recommendations need principle backing, but the three books are
unowned. There is no oracle and no research artifact (D-07); correctness rests on tight core scope
(D-08), skill-reviewer PASS, and DST-04 hygiene. The refs are cross-referenced FROM the coach
(via principles.md, wired in 09-04), not inlined into the router.

Output: beck-tdd-by-example.md, beck-tidy-first.md (both new, no-oracle-tagged), a populated
refactoring-without-tests.md (scaffold markers removed), and check-backing GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-coach-behavior-principle-backing/09-CONTEXT.md
@.planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md
@.planning/phases/09-coach-behavior-principle-backing/09-PATTERNS.md

The per-file core-topic outlines (bounded by D-08) are in 09-RESEARCH.md:181-215; the no-oracle
provenance-marker convention is 09-RESEARCH.md:217-224; the DST-04 paraphrase risk list is
09-RESEARCH.md Pitfall 1 (:258-272). The check-backing topic tokens these files must satisfy are
09-RESEARCH.md:466-479.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author beck-tdd-by-example.md (PRIN-01, no-oracle)</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md (the sibling no-oracle file's shape: top scope paragraph + a top no-oracle blockquote + a ## Sources block -- copy the STRUCTURE, not the text)
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md (its ## Sources no-oracle note at :173-180 -- the in-repo provenance-marker precedent; here name ONLY the book, no research artifact)
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (the two-hats / seam framing at :19-24 this file backs -- CCH-05; do not duplicate, complement)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (PRIN-01 outline :183-189; no-oracle marker convention :217-224; Pitfall 1 paraphrase list :258-272; check-backing tokens :466-479)
    - .claude/skills/lz-refactor-workspace/tools/check-backing.mjs (the exact topic-token regexes and the no-oracle + SCAFFOLD-guard assertions this file must satisfy)
  </read_first>
  <action>
    Create plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md as a no-oracle backing
    reference scoped to the seam (D-08 -- do NOT expand beyond this). Content in original prose: the
    red-green-refactor cycle (write a failing test, make it pass, refactor), scoped to how the green step
    is lz-tpp's job and the refactor step is lz-refactor's; the two rules (write new code only when a test
    is failing; then eliminate duplication) -- paraphrased, NOT quoted; and the three green-bar strategies
    by NAME with original one-line definitions: Fake It, Triangulate, Obvious Implementation. Explicitly name
    lz-tpp (the check-backing token keys on it) as the owner of the green/transformation step. Add the
    no-oracle provenance marker per 09-RESEARCH.md:217-224: a top blockquote stating this is a no-oracle
    reference, high-confidence CORE only, no owned book to verify against, original prose, no verbatim
    Beck prose or code (DST-04); and a ## Sources section (NOT "(placeholder)") naming Beck, Test-Driven
    Development by Example, and the no-oracle posture. Keep any code example TS-fenced or omit it (D-11).
    DST-04: paraphrase every famous one-liner from the first draft; keep only NAMES verbatim. ASCII-only;
    no work email. The token surface must satisfy check-backing: red-green-refactor, two rules, fake it,
    triangulate, obvious implementation, lz-tpp, and a present /no-oracle/i tag, with no SCAFFOLD phrase.
  </action>
  <acceptance_criteria>
    - beck-tdd-by-example.md exists and contains lines matching (case-insensitive): red-green-refactor, two rules, fake it, triangulate, obvious implementation, lz-tpp
    - It contains a no-oracle provenance tag (/no-oracle/i) and a ## Sources section naming the Beck book, with NO "(placeholder)" / TODO / TBD / "to be authored" / "once it exists" scaffold phrase
    - The prose is original (no verbatim Beck sentences); NAMES (Fake It / Triangulate / Obvious Implementation) are the only verbatim facts
    - Any code block is a TS fence (or there is none) -- no non-TS fenced block with a column-0 hash
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node -e "const t=require('fs').readFileSync('../../../plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md','utf8').toLowerCase();process.exit(['red-green-refactor','two rules','fake it','triangulate','obvious implementation','lz-tpp','no-oracle'].every(s=>t.includes(s))&&!/\bplaceholder\b/.test(t)?0:1)"</automated>
  </verify>
  <done>beck-tdd-by-example.md exists, carries the six PRIN-01 tokens + the no-oracle tag, is DST-04-clean and fence-safe, and has no scaffold phrase.</done>
</task>

<task type="auto">
  <name>Task 2: Author beck-tidy-first.md (PRIN-02, no-oracle, Fowler cross-links by link)</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/principles.md (the existing inline [Text](fowler-catalog/<slug>.md) link style at :46-53 -- copy this exact link form; also its two-hats/economics framing)
    - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md (sibling no-oracle file structure + provenance marker)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (PRIN-02 outline + the VERIFIED-existing Fowler link targets :190-205: extract-variable, extract-function, extract-class, combine-functions-into-class, inline-function, slide-statements, replace-nested-conditional-with-guard-clauses, decompose-conditional, rename-variable; the "Explaining Constants has no Fowler 2nd-ed leaf -- do not fabricate a link" note; no-oracle marker :217-224)
    - .claude/skills/lz-refactor-workspace/tools/check-backing.mjs (the beck-tidy-first token set + the fowler-catalog link-presence regex it asserts)
  </read_first>
  <action>
    Create plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md as a no-oracle backing reference
    scoped by D-08 (do NOT expand; no complete tidyings catalog -- that is FUT-01). Content in original prose:
    the separation of structural change from behavioral change (keep them in separate steps/commits); refactor
    economics -- coupling, cohesion, and options (the value of deferring decisions); and a brief when-to-tidy
    note (before vs after vs later). CROSS-REFERENCE overlapping Fowler refactorings BY LINK using the
    principles.md link form ([Name](fowler-catalog/<slug>.md)) -- do NOT restate their mechanics (PRIN-02 core
    constraint). Link only VERIFIED-existing slugs from 09-RESEARCH.md:196-205 (extract-variable, extract-function,
    extract-class, combine-functions-into-class, inline-function, slide-statements,
    replace-nested-conditional-with-guard-clauses, decompose-conditional, rename-variable). Cover "Explaining
    Constants" in prose or point at extract-variable.md -- do NOT fabricate a link for a non-existent leaf.
    Add the same no-oracle provenance marker (top blockquote + ## Sources naming Beck, Tidy First?, no-oracle
    posture, no research artifact). ASCII-only; no work email; fence-safe (D-11). The token surface must satisfy
    check-backing: structural, behavioral, coupling, cohesion, options, at least one ../fowler-catalog/<slug>.md
    link, a present /no-oracle/i tag, and no SCAFFOLD phrase.
  </action>
  <acceptance_criteria>
    - beck-tidy-first.md exists and contains lines matching (case-insensitive): structural, behavioral, coupling, cohesion, options
    - It contains >=1 Markdown link whose URL matches fowler-catalog/<slug>.md, and every such slug is one of the verified-existing Fowler leaves (no fabricated slug)
    - It contains a no-oracle tag and a ## Sources section naming Beck Tidy First?; no scaffold phrase; it does NOT restate Fowler mechanics (links only)
    - Fence-safe (TS fence or none); ASCII-only
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-crossrefs.mjs && node -e "const t=require('fs').readFileSync('../../../plugins/lz-tdd/skills/lz-refactor/references/beck-tidy-first.md','utf8');const l=t.toLowerCase();process.exit(['structural','behavioral','coupling','cohesion','options','no-oracle'].every(s=>l.includes(s))&&/\]\(\.\.?\/?fowler-catalog\/[a-z0-9-]+\.md/.test(t)&&!/\bplaceholder\b/i.test(t)?0:1)"</automated>
  </verify>
  <done>beck-tidy-first.md exists, carries the PRIN-02 tokens + >=1 resolving Fowler link + the no-oracle tag, restates no Fowler mechanics, and is fence-safe with no scaffold phrase.</done>
</task>

<task type="auto">
  <name>Task 3: Populate refactoring-without-tests.md (PRIN-03, Feathers, remove scaffold markers)</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md (the FILE being populated -- its declared per-entry content contract at :10-27 (Technique / When-to-use / Distilled mechanics) and the core-techniques list; the scaffold markers to REMOVE: the "> Populated in Phase 9." blockquote at :7-8 and the "## Sources (placeholder)" heading at :29)
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md (the ## Sources no-oracle note at :173-180 -- the provenance-marker precedent)
    - .planning/phases/09-coach-behavior-principle-backing/09-RESEARCH.md (PRIN-03 outline + technique list :207-215; DST-04 paraphrase list for Feathers one-liners in Pitfall 1 :258-272; no-oracle marker :217-224; check-backing tokens :466-479)
    - .claude/skills/lz-refactor-workspace/tools/check-backing.mjs (the refactoring-without-tests token set + the SCAFFOLD guard the "(placeholder)" heading currently trips)
  </read_first>
  <action>
    Populate plugins/lz-tdd/skills/lz-refactor/references/refactoring-without-tests.md against its OWN declared
    per-entry content contract (Technique / When-to-use / Distilled mechanics), authoring each core technique in
    original prose: Seams (and the idea of a seam as a place to alter behavior without editing in place --
    paraphrase, do NOT quote the canonical seam definition); Characterization tests (pin current behavior before
    changing it); the change algorithm (identify change points, find test points, break dependencies, write tests,
    make the change); Sprout Method / Sprout Class; Wrap Method / Wrap Class; Subclass and Override Method; Extract
    Interface. Keep NAMES verbatim (facts); paraphrase Feathers' famous lines ("legacy code is code without tests"
    -> original wording). REMOVE the scaffold markers so the SCAFFOLD guard clears: delete the "> Populated in
    Phase 9." blockquote, and rename "## Sources (placeholder)" to "## Sources" (the word "placeholder" trips
    /\bplaceholder\b/i). Fold the existing no-oracle framing into a retained top blockquote per the provenance
    convention (no-oracle, high-confidence CORE only, no owned book, original prose, no verbatim Feathers prose or
    code -- DST-04). ASCII-only; no work email; fence-safe (D-11). The token surface must satisfy check-backing:
    seam(s), characterization test, change algorithm (or the step tokens), sprout, wrap, subclass and override,
    extract interface, a present /no-oracle/i tag, and NO SCAFFOLD phrase remaining.
  </action>
  <acceptance_criteria>
    - refactoring-without-tests.md contains lines matching (case-insensitive): seam, characterization test, change algorithm (or the change-algorithm step tokens), sprout, wrap, subclass and override, extract interface
    - The "> Populated in Phase 9." blockquote is gone and the Sources heading is "## Sources" (no "(placeholder)"); no TODO / TBD / "to be authored" / "once it exists" phrase remains
    - A no-oracle tag is present; each technique follows the Technique / When-to-use / Distilled mechanics contract; the seam definition and other famous Feathers lines are paraphrased (DST-04)
    - Fence-safe (TS fence or none); ASCII-only
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && (node tools/check-backing.mjs; test $? -eq 0)</automated>
  </verify>
  <done>refactoring-without-tests.md is populated to its contract, scaffold markers removed, no-oracle-tagged and DST-04-clean; check-backing exits 0 GREEN across all three refs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan authors three shipped Markdown references from unowned books. No runtime, network, session,
crypto, or user-input surface. The dominant risk is copyright/IP integrity of the authored content.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | committed prose must be original (no verbatim Beck/Feathers prose or code) and ASCII-only, work-email-free |
| no-oracle content -> shipped skill | with no oracle, tight core scope + skill-reviewer + hygiene are the only correctness anchors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-DST | Information Disclosure / IP | Beck/Feathers famous one-liners (seam definition, "legacy code is code without tests", Fake It/Triangulate/Obvious Implementation definitions) | mitigate | DST-04: paraphrase every canonical line from the first draft (09-RESEARCH.md Pitfall 1); keep only NAMES verbatim; skill-reviewer PASS at the 09-04 gate is the authoritative DST-04 anchor; check-hygiene no-verbatim WARN heuristic. |
| T-09-SCOPE | Tampering | no-oracle core scope | mitigate | D-08 fixes the scope; do not expand beyond the SC #6 / REQUIREMENTS core -- tight scope is a correctness anchor in the absence of an oracle. |
| T-09-EM | Information Disclosure | committed refs | mitigate | check-hygiene HARD-fails on non-ASCII bytes + non-allowlisted email tokens (work-email leak recurred twice in Phase 4). |
</threat_model>

<verification>
- Scoped to this plan (SKILL.md coach edits + principles.md pointers land in 09-02 / 09-04; full-battery GREEN is the 09-04 gate).
- node tools/check-backing.mjs exits 0 (GREEN) once all three refs are authored and the Feathers scaffold marker is removed.
- node tools/check-crossrefs.mjs exits 0 (beck-tidy-first.md Fowler links resolve).
</verification>

<success_criteria>
Three no-oracle principle references exist -- Beck TDD by Example (PRIN-01), Beck Tidy First? (PRIN-02,
Fowler cross-links by link), Feathers (PRIN-03, populated) -- each tagged no-oracle, scoped to the fixed
core (D-08), DST-04-clean, fence-safe, and turning check-backing GREEN.
</success_criteria>

<output>
Create `.planning/phases/09-coach-behavior-principle-backing/09-03-SUMMARY.md` when done.
</output>
