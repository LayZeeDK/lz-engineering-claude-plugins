---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 10
type: execute
wave: 4
depends_on: [07-02, 07-04, 07-05, 07-06, 07-07, 07-08, 07-09]
tags: [fowler-catalog, smells, skill-authoring, oracle-clean-room, taxonomy, catalog-index]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/smells.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/mysterious-name.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/long-function.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/long-parameter-list.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/global-data.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/mutable-data.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/divergent-change.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/shotgun-surgery.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/feature-envy.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/data-clumps.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/primitive-obsession.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/repeated-switches.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/loops.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/lazy-element.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/speculative-generality.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/temporary-field.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/message-chains.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/middle-man.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/insider-trading.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/large-class.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/alternative-classes-with-different-interfaces.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/data-class.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/refused-bequest.md
  - plugins/lz-tdd/skills/lz-refactor/references/smells/comments.md
autonomous: false
requirements: [FWL-01, FWL-02, FWL-04]
must_haves:
  truths:
    - "07-ROUTING-ARCHITECTURE 'Structure' + 'Content contracts' (smell leaf): the 24 Fowler bad smells are authored as leaves references/smells/<slug>.md, each # <Smell> + one-line Recognize by: + ## How to recognize + ## Why it's a problem + ## Candidate refactorings (each: link to a fowler-catalog leaf + 'pick when <discriminator>') -- this is where the smell->refactoring map lives (FWL-02)"
    - "07-ROUTING-ARCHITECTURE 'Content contracts' (smell index): references/smells.md is a NAVIGATION-only recognize-by index -- per smell a recognize-by line + a resolving link to its leaf, with NO candidates (deliberately, so it cannot shortcut the confirm-via-leaf step)"
    - "07-ROUTING-ARCHITECTURE 'Content contracts' (catalog index): fowler-catalog/README.md is finalized -- all 62 refactorings grouped under their chapter headings (Ch.6-12), each row = name + 1st-ed alias(es) + the leaf's Use when: line mirrored verbatim + a resolving link"
    - "07-ORACLE-MODEL 'The loop' + smell-leaf axes (candidates, recognition, motivation, applicability, spirit): each smell leaf is authored BLIND, gated by the oracle-reviewer subagent (CONTENT_TYPE smell-leaf), and revised BLIND to convergence; the executor/main context NEVER reads .oracle/ (only ls for names)"
    - "07-ORACLE-MODEL 'Harness (deterministic)': the FULL battery is green at this gate -- check-catalog 62/62 + index-row Use-when mirror, check-smells 24 leaves + smells.md index + candidate links resolve, check-crossrefs (bidirectional smell<->refactoring + inverse-of + no self-ref + all links resolve), extract-samples (tsc --strict) over all examples, check-hygiene, check-principles"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md"
      provides: "Duplicated Code smell leaf (Recognize by + How to recognize + Why + Candidate refactorings with discriminators)"
      contains: "Candidate refactorings"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      provides: "navigation-only recognize-by smell index (24 smells, no candidates)"
      contains: "recognize by:"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md"
      provides: "finalized chapter-grouped catalog index with all 62 rows and mirrored Use-when lines"
      contains: "Use when:"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md"
      via: "candidate-refactoring cross-link (resolving)"
      pattern: "fowler-catalog/[a-z-]+\\.md#"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/smells/*.md"
      via: "one resolving link per smell (navigation only, no candidates)"
      pattern: "smells/[a-z-]+\\.md"
---

<objective>
Close the Fowler layer: author the 24 bad-smell leaves + the navigation-only smell index, and
finalize the chapter-grouped catalog index, then run the FULL deterministic battery green. Runs
LAST (wave 4), after all 62 catalog leaves exist, so smell candidate cross-links and README index
rows resolve.

Purpose: FWL-02 -- the smell taxonomy the Phase-9 coach consumes -- plus the FWL-01 catalog-index
finalization and the phase-gate FWL-04 compile re-verify. The smell leaves hold the
smell->refactoring map (candidates + discriminators); the smell index is navigation-only so it
cannot shortcut the confirm-via-leaf step.

Output: references/smells/<24 leaves>, the recognize-by references/smells.md index, the finalized
fowler-catalog/README.md, and a green full battery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-02-SUMMARY.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/smells.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md

<interfaces>
CLEAN-ROOM FIREWALL: the executor/main context NEVER opens any file under .oracle/ -- `ls` for
names only. Book content is reached ONLY via the subagents:
- `oracle-reviewer` (Task/subagent) gates a drafted smell leaf. Input: DRAFT_PATHS (smell
  leaves), SOURCE = ".oracle/refactoring-2e/index.md" + "Ch.3 -- bad smells in code" (index.md +
  chapter number/topic; never a book chapter filename), CONTENT_TYPE = "smell-leaf", SCOPE = each
  leaf's smell, and the smell-leaf canonical anchors from 07-ORACLE-MODEL (candidates,
  recognition, motivation, applicability, spirit). Returns per-draft verdicts
  pass|revise|blocked|error.
- `oracle` (Task/subagent) for open-ended lookups (e.g. "which refactorings does the source name
  as candidates for smell X?").

Deterministic checkers were rebuilt for this model by the 07-02 pilot (do NOT edit them here, only
run them): check-catalog (62-name identity + index-row Use-when mirror), check-smells (24 leaves +
smells.md index + candidate-link resolution), check-crossrefs (bidirectional smell<->refactoring +
inverse-of + no self-ref + all links resolve), extract-samples (tsc --strict), check-hygiene,
check-principles.

The 24 Fowler bad-smell names (orientation only; oracle-confirms exact 2nd-ed set/wording): Mysterious
Name, Duplicated Code, Long Function, Long Parameter List, Global Data, Mutable Data, Divergent
Change, Shotgun Surgery, Feature Envy, Data Clumps, Primitive Obsession, Repeated Switches, Loops,
Lazy Element, Speculative Generality, Temporary Field, Message Chains, Middle Man, Insider Trading,
Large Class, Alternative Classes with Different Interfaces, Data Class, Refused Bequest, Comments.
Candidate cross-links target the ACTUAL catalog leaf anchors authored in waves 2-3 (read the leaves).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 24 bad-smell leaves via the clean-room loop</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/smells/mysterious-name.md, plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md, plugins/lz-tdd/skills/lz-refactor/references/smells/long-function.md, plugins/lz-tdd/skills/lz-refactor/references/smells/long-parameter-list.md, plugins/lz-tdd/skills/lz-refactor/references/smells/global-data.md, plugins/lz-tdd/skills/lz-refactor/references/smells/mutable-data.md, plugins/lz-tdd/skills/lz-refactor/references/smells/divergent-change.md, plugins/lz-tdd/skills/lz-refactor/references/smells/shotgun-surgery.md, plugins/lz-tdd/skills/lz-refactor/references/smells/feature-envy.md, plugins/lz-tdd/skills/lz-refactor/references/smells/data-clumps.md, plugins/lz-tdd/skills/lz-refactor/references/smells/primitive-obsession.md, plugins/lz-tdd/skills/lz-refactor/references/smells/repeated-switches.md, plugins/lz-tdd/skills/lz-refactor/references/smells/loops.md, plugins/lz-tdd/skills/lz-refactor/references/smells/lazy-element.md, plugins/lz-tdd/skills/lz-refactor/references/smells/speculative-generality.md, plugins/lz-tdd/skills/lz-refactor/references/smells/temporary-field.md, plugins/lz-tdd/skills/lz-refactor/references/smells/message-chains.md, plugins/lz-tdd/skills/lz-refactor/references/smells/middle-man.md, plugins/lz-tdd/skills/lz-refactor/references/smells/insider-trading.md, plugins/lz-tdd/skills/lz-refactor/references/smells/large-class.md, plugins/lz-tdd/skills/lz-refactor/references/smells/alternative-classes-with-different-interfaces.md, plugins/lz-tdd/skills/lz-refactor/references/smells/data-class.md, plugins/lz-tdd/skills/lz-refactor/references/smells/refused-bequest.md, plugins/lz-tdd/skills/lz-refactor/references/smells/comments.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- smell leaf)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ORACLE-MODEL.md ("The loop", smell-leaf axes candidates/recognition/motivation/applicability/spirit, "Canonical per-axis anchors", "Driver responsibilities")
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-02-SUMMARY.md (calibrated loop mechanics)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md (read the actual leaf headings/slugs so candidate cross-links resolve)
    - .planning/research/refactoring-com-overview.md (orientation smell-name list)
  </read_first>
  <action>
    PREREQUISITE: ensure `oracle` and `oracle-reviewer` are live (run /reload-plugins if not).
    Do NOT open any file under .oracle/ from this context.

    Author the 24 smell leaves (one file per smell, kebab-case slug) via the core clean-room loop,
    per leaf or per small batch:
    (1) AUTHOR BLIND from public knowledge, in the LOCKED smell-leaf format: `# <Smell>`, a
    one-line `Recognize by:` selector, `## How to recognize` (fuller -- CONFIRM the fuzzy match,
    separate near-neighbors), `## Why it's a problem`, and `## Candidate refactorings` -- each a
    resolving link to a fowler-catalog leaf plus "pick when <smell-specific discriminator>". This
    candidate section is where the smell->refactoring map lives (FWL-02). Confirm the complete
    candidate set (Fowler's catalog is hypertext; every named candidate must be present and must
    resolve). ASCII only.
    (2) GATE: spawn `oracle-reviewer` with DRAFT_PATHS (the smell leaves), SOURCE
    ".oracle/refactoring-2e/index.md" + "Ch.3 -- bad smells in code", CONTENT_TYPE "smell-leaf",
    SCOPE (each leaf's smell), the smell-leaf canonical anchors. Use `oracle` for open-ended
    candidate-set lookups.
    (3) REVISE BLIND from the directives -- never opening the source.
    (4) ITERATE to convergence (every entry `pass`, or owner-accepted `blocked`); DRIVER caps
    rounds at ~3 and escalates a non-converging/blocked entry to Task 3. Apply the fan-out guard
    (dedupe/cap; do NOT persist a full ordered name+chapter map).
    (5) Run check-smells + check-crossrefs + check-hygiene over the smell leaves; commit on green.
  </action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && rg -q "Recognize by:" plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md && rg -q "Candidate refactorings" plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md && rg -q "fowler-catalog/[a-z-]+\.md#" plugins/lz-tdd/skills/lz-refactor/references/smells/duplicated-code.md && test $(ls plugins/lz-tdd/skills/lz-refactor/references/smells/*.md | wc -l) -eq 24</automated>
  </verify>
  <done>All 24 smell leaves exist as contract-complete files (Recognize by + How to recognize + Why + Candidate refactorings with resolving links + discriminators); each oracle-reviewer verdict is pass (or owner-accepted blocked); check-hygiene green; no .oracle/ file opened by this context.</done>
</task>

<task type="auto">
  <name>Task 2: Author the smell index + finalize the catalog index + run the full battery</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/smells.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md</files>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-ROUTING-ARCHITECTURE.md ("Content contracts" -- smell index (navigation only) + catalog index)
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (Phase-6 stub to replace with the recognize-by navigation index)
    - plugins/lz-tdd/skills/lz-refactor/references/smells/*.md (mirror each smell's Recognize by: line into the index)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (pilot scaffold: legend + Ch.6 rows + empty Ch.7-12 headings)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md (read each leaf's Use when: line to mirror into its README row)
  </read_first>
  <action>
    Replace references/smells.md with a NAVIGATION-only recognize-by index: per smell a
    `### <Smell>  -- recognize by: <line>` (mirrored from the leaf) + a resolving link to
    references/smells/<slug>.md. NO candidate refactorings here (deliberately -- forces
    confirm-via-leaf). Update its Sources section to a real Fowler Ch.3 attribution. Do NOT add
    Kerievsky rows (Phase-8 fold).

    Finalize fowler-catalog/README.md: keep the pilot's legend + Ch.6 rows; add the Ch.7-12 rows
    under their chapter headings -- each row = name + 1st-ed alias(es) + the leaf's `Use when:`
    line mirrored VERBATIM + a resolving relative link. Confirm all 62 refactorings appear exactly
    once, grouped under the chapter the oracle confirmed (do NOT reopen .oracle/; the chapter each
    leaf belongs to is recorded in the wave-2/3 SUMMARYs). Keep the README thin (no entry content
    inlined). Do NOT touch SKILL.md.

    Then run the FULL deterministic battery and make it green: check-catalog (62/62 + index-row
    Use-when mirror), check-smells (24 leaves + smells.md index + candidate links resolve),
    check-crossrefs (bidirectional smell<->refactoring + inverse-of + no self-ref + all links
    resolve), extract-samples (tsc --strict over every example), check-hygiene, check-principles.
    Also run `claude plugin validate .` as a regression check. Commit on green.
  </action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs && node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && node .claude/skills/lz-refactor-workspace/tools/check-principles.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>smells.md is a navigation-only recognize-by index (24 smells, resolving links, no candidates); fowler-catalog/README.md lists all 62 refactorings grouped by chapter with mirrored Use-when lines and resolving links; the full battery is GREEN (check-catalog 62/62, check-smells 24/24, check-crossrefs, extract-samples tsc, check-hygiene, check-principles) and `claude plugin validate .` passes; SKILL.md untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Owner escalation for non-converging / blocked smell leaves (conditional)</name>
  <decision>How to resolve any smell leaf the oracle-reviewer loop cannot converge within ~3 rounds or returns as a blocked ambiguity (e.g. an ambiguous candidate-set boundary)?</decision>
  <context>
    07-ORACLE-MODEL "Driver responsibilities": the adversarial reviewer can false-`revise` or
    oscillate. The driver caps rounds (~3) and escalates that ENTRY to the owner via
    AskUserQuestion. Fires ONLY for entries that do not reach `pass`.
  </context>
  <options>
    <option id="accept-blocked">
      <name>Owner accepts the blocked smell leaf as-authored</name>
      <pros>Unblocks a faithful draft the reviewer over-flagged; records the rationale</pros>
      <cons>Bypasses the automated gate for that entry; must be logged in the SUMMARY</cons>
    </option>
    <option id="owner-directive">
      <name>Owner supplies a targeted own-words correction (e.g. a missing candidate), then re-run the loop once</name>
      <pros>Resolves a genuine candidate-set / recognition gap the reviewer could not phrase without leaking</pros>
      <cons>One more round for that entry</cons>
    </option>
  </options>
  <action>
    ONLY for entries that did not reach pass within ~3 rounds (or returned blocked): open an
    AskUserQuestion with the entry, the reviewer's ambiguities/unable-to-verify axes (own words),
    and the two options; apply the choice; re-run the harness; record it in the SUMMARY. Do NOT
    reveal or request verbatim source prose.
  </action>
  <resume-signal>Owner selects accept-blocked or owner-directive for each escalated smell leaf</resume-signal>
  <done>Every smell leaf is oracle-reviewer pass or owner-adjudicated; escalations recorded in the SUMMARY (a no-op note if nothing escalated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.oracle/refactoring-2e/` (owner full-text book, git-ignored) -> authored public-repo smell leaves + indexes | copyrighted expression must never cross; the ONLY crossing is via the `oracle`/`oracle-reviewer` subagents (isolated context, own-words verdicts, no prose/code/paths) |
| authored files -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal (DST-04) | authored smell-leaf prose | mitigate | Clean-room firewall: main context NEVER reads `.oracle/`; leaves authored BLIND; `oracle-reviewer` near-verbatim gate; `check-hygiene` no-verbatim heuristic; Phase-10 scan |
| T-07-03 | Tampering (fabricated smell set / candidate mappings) | recognition + candidate fidelity | mitigate | `oracle-reviewer` smell-leaf gate (candidates/recognition axes) + converge loop; driver caps rounds (~3) and escalates to owner (Task 3) |
| T-07-06 | Tampering (dangling cross-links; validate does not check body links) | smell candidate links + catalog index rows | mitigate | check-crossrefs + check-smells assert every candidate/index link resolves; wave 4 runs after all leaves exist |
| T-07-05 | Tampering / firewall (fan-out aggregation) | merged oracle output reconstructing the book's smell selection | mitigate | driver dedupes/caps; does NOT persist a full ordered name+chapter map |
| T-07-02 | Information Disclosure | work email in a smell leaf / index | mitigate | `check-hygiene` work-email allowlist gate |

No UI, no network, no secrets, no new packages. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The full battery is GREEN: check-catalog 62/62 + index-row mirror; check-smells 24 leaves + smells.md index + candidate links resolve; check-crossrefs bidirectional + no self-ref + all resolve; extract-samples (tsc --strict); check-hygiene; check-principles. `claude plugin validate .` passes.
- Every smell leaf reached oracle-reviewer pass (or owner-accepted blocked).
- smells.md is navigation-only (no candidates); fowler-catalog/README.md is the finalized thin index; SKILL.md untouched; no `.oracle/` file opened by this context.
</verification>

<success_criteria>
- 24 Fowler bad-smell leaves authored with resolving candidate cross-links + discriminators (FWL-02); the navigation-only smell index authored; the catalog index finalized with all 62 mirrored rows (FWL-01).
- The phase-gate full battery is green (FWL-01/FWL-02/FWL-04); table shaped for the Phase-8 Kerievsky fold; ASCII-only; no verbatim prose.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-10-SUMMARY.md` when done, recording the confirmed 24-smell set, per-leaf round counts, any owner escalations, and the final full-battery result.
</output>
