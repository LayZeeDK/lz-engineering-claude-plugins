I want to clean up `updateQuality` in `app/gilded-rose.ts`. I'm thinking of a subclass per item
type -- but note the `Item` class can't be modified (another team owns it). Is that the right
approach, or would you do something else?
