[
  {
    "query": "red test `expect(of(2)).toBe(1)`, my `of` is `return n <= 1 ? n : 1;`. whats the smallest edit to get it passing? doing tdd",
    "should_trigger": false
  },
  {
    "query": "my failing test wants fib(3) to be 2 but the code returns 1 for anything above 1. whats the minimal transformation to green it without writing the whole recursion yet?",
    "should_trigger": false
  },
  {
    "query": "which change makes this failing test pass - is there a smaller step than writing the full algorithm?",
    "should_trigger": false
  },
  {
    "query": "refactor this to be faster - it's O(n^2) and i need it under 50ms",
    "should_trigger": false
  },
  {
    "query": "write a jest test for this `formatCurrency(cents)` helper covering negatives and zero",
    "should_trigger": false
  },
  {
    "query": "write a debounce helper in typescript",
    "should_trigger": false
  },
  {
    "query": "add pagination to the GET /users endpoint in this express app",
    "should_trigger": false
  },
  {
    "query": "explain the SOLID principles with a short typescript example for each",
    "should_trigger": false
  },
  {
    "query": "my CSV parse step throws on empty rows and one of my tests is red - help me find the bug",
    "should_trigger": false
  },
  {
    "query": "whats the time and space complexity of this recursive merge sort?",
    "should_trigger": false
  },
  {
    "query": "i've got a failing test `expect(parseTag('a:b')).toEqual({ scope: 'a', name: 'b' })` and `parseTag` currently just returns `null`. what refactoring gets it to pass?",
    "should_trigger": false
  },
  {
    "query": "green across the `api` package - now go through every route module and add request-validation and rate-limiting to each one.",
    "should_trigger": false
  },
  {
    "query": "the whole `reporting` module is green but slow. sweep the package and rewrite the hot paths to be faster - i want to halve the current latency.",
    "should_trigger": false
  },
  {
    "query": "a bunch of failing tests across the `parser` package after a spec bump - whats the smallest edit to make each one pass? doing tdd.",
    "should_trigger": false
  }
]