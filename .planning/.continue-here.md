---
context: milestone-close-prep
supersedes: 260802-j03 (quick task; its pilot is now RESOLVED)
phase: 21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i
status: phase_21_fully_closed_milestone_pending_owner
last_updated: 2026-08-02
---

# State: the D-12 question is CLOSED and phase 21 is fully audited

The pilot that the previous handoff was paused mid-way through has RUN and RESOLVED. Phase 21's
three close-out gates are done. The only owner-gated item left is the milestone itself.

## BLOCKING CONSTRAINTS -- read before anything else

- [ ] **DO NOT RE-OPEN THE D-12 CELL BY REWORDING `a1` AGAIN.** The pre-registered pilot returned
      N = 3 of 3 -> CEILING HELD. The discriminator is now correct on 21 of 21 runs, and the
      baseline arm alone is 6 of 6 on `a1` across BOTH prompt versions. The ceiling is STRUCTURAL:
      the shipped `lz-red` SKILL.md that the BASELINE arm loads already carries the qualify-by-side
      rule verbatim AND a worked example whose production-side stub IS this cell's correct answer.
      A skill cannot be shown to beat a baseline that is reading the answer out of that same skill.
      Measuring a taxonomy effect needs a NEW cell where the baseline is not already correct.
- [ ] **`FUT-TAXONOMY-SHARED` IS NOT-TESTED, NOT NULL.** Two rounds failed to test it -- round 1
      because the treatment dose was zero, the pilot because the baseline is saturated. Recording
      it as "no effect" would retire a live hypothesis on no evidence. It stays OPEN /
      NOT-YET-TESTED.
- [ ] **GATE ON EXIT STATUS, ALWAYS.** `cmd; test $? -eq 0 && echo TOKEN`. A trailing `echo $?`
      always exits 0, and `check-red-references.mjs` prints its check count even when RED. This
      recurred AGAIN on 2026-08-02 (a `test $?` placed after an `echo`) and was caught before the
      result was trusted.
- [ ] **`npm run check` IS NOT THE BATTERY.** It runs ONLY `check-red-references.mjs`;
      `extract-samples.mjs` and `check-evals.mjs` must be invoked separately.
- [ ] **NEVER RUN TWO BATTERIES OR TWO GRADERS CONCURRENTLY.** They contend over
      `D:\.lz-red-grade-tmp`. Killing one mid-crux strands a grading worktree, and the next run's
      crux-7 canary correctly fails on the leftover. Clean up with
      `git --git-dir=<repo>/.git --work-tree=<repo> worktree remove --force <path>` then `prune`.
- [ ] **EVL-01 / EVL-02 STATUS IS UNVERIFIED.** They carry the same "Pending (build complete;
      empirical run gated)" phrasing EVL-03 did. EVL-03's reconciliation is NOT evidence about
      them. Verify each against its own results write-up before trusting its row.

## Done this session (2026-08-02)

1. **Repaired the mixed results tree** -- verified the round-1 archive intact (27/27) FIRST, cleared
   three stale grades, recorded a resume boundary BEFORE re-running, re-ran all three with
   `--force`, and asserted fail-closed that every meta `started_at` AND every grade mtime postdates
   the boundary before computing anything.
2. **Finished the pilot and applied the committed rule VERBATIM** -- N = 3 of 3, zero void runs,
   $4.11. CEILING HELD -> the ~$27 k=5 round was NOT bought. Recorded in
   `260802-j03-PILOT-RESULT.md` and in a NEW `targets.json` `pilot_result` field;
   `pilot_decision_rule` and `ceiling_note` are byte-identical to their committed form (verified by
   parse-and-compare), because the rule forbids editing itself after the pilot runs.
3. **`/gsd-secure-phase 21`** via `gsd-security-auditor` -> SECURED, 9/9 closed, `threats_open: 0`.
   T-21-SC (the only `high`) closed by a RECORDED legitimacy-gate PASS per nominated target, not a
   merely documented requirement. Two threats runtime-probed (9/9 and 6/6 fail-closed paths).
4. **`/gsd-validate-phase 21`** via `gsd-nyquist-auditor` -> PARTIAL, and it found a REAL gap:
   `selfcheck-red` never parity-asserted the d12 arms, so 2 of 5 own-skill arms were unasserted.
   Closed in test code (+164/-2); its FIRST fix failed its own mutation test and was fixed with
   path-insensitive `samePath()`. `nyquist_compliant` set only AFTER correcting the map.
5. **EVL-03 drift reconciled (owner chose Path A)** -- all 36 captures re-graded under the current
   grader, ZERO verdict changes, so the published numbers are grader-regime-invariant. EVL-03
   Pending -> Complete. Historical SUMMARYs NOT rewritten; 21-04 got a forward-pointer addendum.
6. **`/gsd-extract-learnings 21`** + the manual global-store bridge (32 item sections copied; temp
   `.planning/LEARNINGS.md` built, copied and removed as one uninterrupted sequence, never staged).

## IN FLIGHT at handoff

- **`gsd-security-auditor` for phase 17.1.** Found while checking milestone readiness: 17.1 is the
  ONLY phase with no SECURITY.md, and its single PLAN does carry a `<threat_model>` (5 threats, all
  low, T-17.1-SC disposition ACCEPT). If the agent's result was not consumed, RE-RUN the audit --
  do not author `17.1-SECURITY.md` from the register alone, and do not take the workflow's inline
  short-circuit. Its verdict must come from the dedicated agent.

## Remaining

1. Write + commit `17.1-SECURITY.md` from the auditor's returned verdict (orchestrator authors, agent decides).
2. `/gsd-audit-milestone lz-tdd@0.0.3` -- and COMMIT the audit; that commit is part of the command.
3. `/gsd-complete-milestone lz-tdd@0.0.3` with **MANUAL archival** -- `gsd-sdk milestone.complete`
   rejects the `@`-scoped non-`vX.Y` version string. Follow the 0.0.2 precedent.
4. Tag + GitHub Release -- OUTWARD-FACING, confirm with the owner first.
5. Nothing has been pushed. The branch is ~380 commits ahead of `origin/main`.

## Infrastructure

- Working tree CLEAN. Borrowed kata pristine, no leftover worktrees, `plugins/lz-tdd` untouched.
- Archives: `D:/.lz-red-archive/{2026-07-27-preskill-drift, 2026-08-01-d12-round-1,
  2026-08-01-optionC-abandoned, 2026-08-01-stray-opus5-run}`.
- The `probe-d12-ambiguity/` leftover was deleted after `diff -r` proved it byte-identical to its archive.
- `worktree.baseRef: "head"` IS set -- load-bearing at this commit depth.
- Full battery takes MINUTES and can exceed 600 s; run it in the background and read the exit code
  from a file, never from a pipeline's last stage.
