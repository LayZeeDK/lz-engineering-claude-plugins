---
phase: 17-assertion-design-stance-router-ts-vitest-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
autonomous: true
requirements: [ASRT-01, ASRT-02, ASRT-03, RTR-01, RTR-03, VIT-01, VIT-02, ANTI-01, ANTI-02]
must_haves:
  truths:
    - "check-red-references.mjs exits 1 (RED) against the current Phase-15 stubs: the six new Phase-17 file entries + the flipped assertions-slice ASRT tokens report FAIL -- the instrument-first Wave-0 RED baseline"
    - "the two fully-Phase-16 core entries (three-laws-and-test-selection.md, naming.md) still report all-PASS (no regression); the existing STR tokens on test-structure-and-assertions.md still PASS"
    - "extract-samples typecheck exits 0 (GREEN-on-empty / existing fences) and check-hygiene exits 0 -- neither instrument is touched"
    - "the seams-and-legacy cross-link presence guard asserts the string refactoring-without-tests.md; the requireFence flag is per-file; Phase-18 deferral guards are added on the assertions-slice, vitest-mechanics, and principle-backing entries"
  artifacts:
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  key_links:
    - "requireFence per-file flag replaces the unconditional ts-fence assertion"
    - "seams-and-legacy.md -> refactoring-without-tests.md cross-link presence guard"
    - "co-edit boundary (D-15): three-laws Phase-18 guard kept; new Phase-18 guards on assertions-slice + vitest + principle-backing"
---

<objective>
Extend the dev-only `check-red-references.mjs` content-completeness gate to a Phase-17 RED
baseline (D-13, instrument-first Nyquist). This is Wave 0: the checker is extended BEFORE any
content lands, so the six Phase-17 slices (plus the flipped assertions slice and the co-edited
principle-backing map) turn its new guards RED now and GREEN only as Waves 2-3 author the content.
No content is written in this plan; no shipped file is touched; no build dependency enters
`plugins/lz-tdd`.

Purpose: give every Phase-17 requirement a machine signal before authoring (the tsc extractor is
GREEN-on-empty so it cannot be the content signal; THIS checker is).
Output: the extended checker + an asserted RED baseline.

Decisions implemented (decision-coverage):
- **D-13:** instrument-first -- extend the existing `check-red-references.mjs` in place (not a
  sibling), keep the tsc gate + no-verbatim scan mandatory, add no build dep to `plugins/lz-tdd`.
- **D-14:** the no-verbatim scan (check-hygiene) stays the non-negotiable owned-surface backstop;
  this plan confirms it exits 0 unchanged.
- **D-15:** co-edit, do not split -- the checker keeps the three-laws Phase-18 guard and adds
  Phase-18 deferral guards on the assertions slice (F.I.R.S.T.-as-baseline PROCEDURE / LAW-02),
  the vitest leaf (LAW-02 spine), and principle-backing (Three-Laws / seam backing rows), so no
  Wave-2 content over-fills a Phase-18 slice.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-RESEARCH.md
@.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-PATTERNS.md
@.claude/skills/lz-red-workspace/tools/check-red-references.mjs
@.claude/skills/lz-refactor-workspace/tools/check-backing.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-red-references.mjs to the Phase-17 RED baseline</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <read_first>
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs (the instrument to extend -- current FILES array lines 30-63, unconditional TS_FENCE_RE line 68/103-104, deferral guard idiom lines 109-112)
    - .claude/skills/lz-refactor-workspace/tools/check-backing.mjs (the cross-link topic idiom, lines 54-56: a `]\((?:\.\/)?fowler-catalog/...\.md/` presence pattern to model the seams cross-link guard on)
    - 17-RESEARCH.md "Instrument Extension (D-13)" (changes a-e, lines 439-473) + 17-PATTERNS.md "Instrument Extension" (lines 355-394)
  </read_first>
  <action>
    Extend the FILES array and the per-file loop in place (do NOT stand up a sibling checker; D-13).

    (a) Flip the `test-structure-and-assertions.md` entry: DELETE its current later-phase deferral
    guard (the one asserting an ASRT-slice marker remains -- Phase 17 FILLS that slice). KEEP its
    five existing STR topic tokens (arrange-act-assert, given-when-then, assert-first, evident test
    data, one concept). ADD five ASRT content tokens: four pillars (/four pillars/i), resistance to
    refactoring (/resistance to refactoring/i), F.I.R.S.T. (/F\.I\.R\.S\.T\./), observable behavior
    (/observable behavior/i), and an output/state/communication selection token (/communication-based/i).
    Set requireFence: true. ADD a NEW deferral guard asserting the Phase-18 F.I.R.S.T.-as-baseline
    PROCEDURE marker remains (/Phase 18|LAW-0/).

    (b) ADD six Phase-17 FILES entries (subdir names include the `testing-stance/` segment joined
    under REFERENCES). Content tokens per file:
      - testing-stance/README.md: detection signal (/detection signal/i), route table (/route table/i),
        and a link to each of the three leaves (/functional-core\.md/, /message-matrix\.md/,
        /seams-and-legacy\.md/). requireFence: false.
      - testing-stance/functional-core.md: functional core (/functional core/i), imperative shell
        (/imperative shell/i), value-based/output-based assert (/value-based|output-based/i), no
        doubles in the core (/no (test )?doubles?/i). requireFence: true.
      - testing-stance/message-matrix.md: incoming query (/incoming query/i), incoming command
        (/incoming command/i), outgoing command (/outgoing command/i), outgoing query (/outgoing query/i),
        expect-to-send (/expect[ -]to[ -]send|expect to send/i). requireFence: true.
      - testing-stance/seams-and-legacy.md: seam (/\bseam/i), characterization (/characterization/i),
        sequencing (/sequenc/i). requireFence: false. PLUS the cross-link presence guard in (d).
      - vitest-typescript-mechanics.md: it.todo (/it\.todo|test\.todo/), test.each/it.each
        (/test\.each|it\.each/), vi.* restraint (/restraint/i), watch (/watch/i), fails-for-the-right-reason
        mechanic (/right reason|AssertionError/), ADV forward-pointer (/ADV-0|expectTypeOf|fast-check/).
        requireFence: true. ADD a Phase-18 deferral guard (/Phase 18|LAW-0/) for the LAW-02 spine note.
      - anti-patterns.md: over-mock/test-per-class (/over-mock|test-per-class/i), private methods
        (/private method/i), multiple unrelated assertions (/unrelated assert|multiple.*assert/i),
        passes-immediately/no-red (/passes immediately|never.?red|no red/i), snapshot (/snapshot/i),
        slow/order-dependent (/order-dependent|slow test/i), listen to the tests (/listen to the tests/i),
        GOOS counterpoint (/goos|counterpoint/i), Test Desiderata (/desiderata/i), tradeoff/heuristic
        (/tradeoff|heuristic/i). requireFence: false.

    (+) ADD a seventh entry for the co-edited principle-backing.md (D-15): owned tier string
    (/oracle-verified/i), no-oracle tier string (/no-oracle/i), at least one recommendation link
    (/\]\([^)]+\.md/), a named source (/Khorikov/i). requireFence: false. ADD a Phase-18 deferral
    guard (/Phase 18|LAW-0|SEAM-0/) for the Three-Laws / seam backing rows deferred to Phase 18.

    (c) Make the fence assertion per-file: replace the unconditional TS_FENCE_RE check with a
    `spec.requireFence === true` guard so nav-only / prose-only files are not false-RED-ed. Preserve
    the existing default behavior only where requireFence is true. Keep the three-laws and naming
    entries passing (three-laws requireFence: true -- it already has a fence; naming requireFence:
    true -- it already has a fence; keep their existing behavior).

    (d) On the seams-and-legacy.md entry, add a topic asserting the cross-link string
    /refactoring-without-tests\.md/ appears (machine-proof of RTR-01 "cross-linked, not copied"),
    modeled on check-backing's cross-link topic idiom.

    (e) KEEP the three-laws-and-test-selection.md deferral guard (/Phase 18|LAW-0|SEAM-0/) untouched.

    Update the header comment and the SUMMARY strings to describe the Phase-17 scope (nine entries,
    per-file requireFence, the cross-link guard, the co-edit deferral guards). Do NOT redefine
    SCAFFOLD_RES -- keep importing it from ./lib/scaffold-phrases.mjs. Node builtins only.
  </action>
  <acceptance_criteria>
    - The FILES array has nine entries: three-laws-and-test-selection.md, test-structure-and-assertions.md,
      naming.md, testing-stance/README.md, testing-stance/functional-core.md, testing-stance/message-matrix.md,
      testing-stance/seams-and-legacy.md, vitest-typescript-mechanics.md, anti-patterns.md, principle-backing.md
      (10 total counting the three carried-over core entries -- three carried + one flipped + six new).
    - The fence assertion is gated by a per-entry requireFence flag; README.md, seams-and-legacy.md,
      anti-patterns.md, principle-backing.md have requireFence: false.
    - test-structure-and-assertions.md has no ASRT-marker-remains deferral guard; it has a Phase-18
      (F.I.R.S.T.-baseline / LAW-0) deferral guard instead, plus the five STR tokens AND five ASRT tokens.
    - seams-and-legacy.md has a topic asserting /refactoring-without-tests\.md/.
    - vitest-typescript-mechanics.md and principle-backing.md each carry a Phase-18 deferral guard.
    - SCAFFOLD_RES is imported, not redefined; the file is Node-builtins-only and parses (node runs it).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1</automated>
  </verify>
  <done>The checker runs, exits 1 (RED baseline), and its output shows the new Phase-17 rows as FAIL while three-laws and naming rows PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the Wave-0 RED baseline across all three gates</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <read_first>
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs (as extended in Task 1)
    - .claude/skills/lz-red-workspace/extract-samples.mjs (GREEN-on-empty tsc gate; recursive walk; README skip)
    - 17-VALIDATION.md "Wave 0 Requirements" + "Global gates"
  </read_first>
  <action>
    Establish and record the instrument-first RED baseline (no file edits beyond any small SUMMARY /
    comment fix Task 1 left). Run the three gates and confirm the expected signals:
    (1) content gate `check` exits 1 -- RED by design: the six new file entries + the assertions-slice
    ASRT tokens FAIL on the unfilled Phase-15 stubs; three-laws and naming still PASS.
    (2) tsc gate `typecheck` exits 0 -- GREEN-on-empty / over the two existing Phase-16 fences
    (three-laws, naming) which still compile; no Phase-17 fences exist yet.
    (3) hygiene `check-hygiene` exits 0 -- the lz-red tree is ASCII-clean, work-email-free,
    no-verbatim clean (unchanged from Phase 16).
    Confirm the regression floor: the flip did NOT break the three-laws or naming rows, and the STR
    tokens on test-structure-and-assertions.md still PASS. If any of these regress, fix the entry in
    Task 1's file (a token typo or an over-broad flip) and re-run.
  </action>
  <acceptance_criteria>
    - `npm --prefix .claude/skills/lz-red-workspace run check` exits 1; its output shows three-laws-and-test-selection.md and naming.md rows all PASS, and the six new Phase-17 rows FAIL.
    - `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0.
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
    - No shipped `plugins/lz-tdd` file was modified; no dependency was added.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && ( npm --prefix .claude/skills/lz-red-workspace run check; test $? -eq 1 )</automated>
  </verify>
  <done>typecheck exits 0, hygiene exits 0, and the content gate exits 1 (RED-on-stubs baseline) with three-laws + naming still PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling -> shipped tree | The checker lives in the dev-only workspace; it must never add a build dependency to `plugins/lz-tdd`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Tampering (supply chain) | lz-red-workspace instrument | medium | mitigate | The extension edits one dev-only `.mjs`; the pinned typescript/vitest devDeps are unchanged; no package is installed; `plugins/lz-tdd` gains no dep. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | Phase 17 installs zero packages (17-RESEARCH Package Legitimacy Audit); the workspace deps are already present and pinned. No install task. |
</threat_model>

<verification>
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` exits 1 (RED baseline by design).
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0; `check-hygiene.mjs` exits 0.
- `git diff --stat` shows only `.claude/skills/lz-red-workspace/tools/check-red-references.mjs` changed.
</verification>

<success_criteria>
- The instrument gates all nine Phase-17 requirements via the flipped assertions slice + six new file entries + the co-edited principle-backing entry.
- RED-on-stubs baseline is asserted; typecheck + hygiene are GREEN; no regression on the Phase-16 core rows.
- No shipped file touched; no build dep added.
</success_criteria>

## Artifacts this phase produces (Plan 17-01)

- MODIFIED: `.claude/skills/lz-red-workspace/tools/check-red-references.mjs` (dev-only, NOT shipped).
- New checker guards/flags: six Phase-17 FILES entries (testing-stance README + 3 leaves,
  vitest-typescript-mechanics, anti-patterns) + a seventh co-edited principle-backing entry; the
  flipped assertions-slice entry (STR tokens kept, five ASRT tokens added, ASRT-marker deferral
  deleted); a per-file `requireFence` flag; the `refactoring-without-tests.md` cross-link presence
  guard on seams-and-legacy; Phase-18 deferral guards on assertions-slice, vitest-mechanics, and
  principle-backing.
- No new shipped files. No new dependency.

<output>
Create `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-01-SUMMARY.md` when done.
</output>
