---
quick_id: 260716-lq9
slug: extend-lz-refactor-drive-to-fixpoint-to-
description: Extend lz-refactor drive-to-fixpoint from package/directory scope to single-target apply
date: 2026-07-16
mode: full (gated: subagent review + reload + approval-gated eval)
status: in-progress
must_haves:
  truths:
    - The SKILL.md drive-to-fixpoint discipline applies to a single-target apply COMMAND, not only package/dir/module scope.
    - Every existing guard is preserved verbatim (step-4 APPLY/DECLINE, step-5 blast-radius pause, verify-between-rounds, stop-at-fixpoint-not-zero-smells).
    - The change ships ONLY if a held-out validation eval shows idiom_pattern lifts with no regression on the other graded dims; else revert and report null.
  artifacts:
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (prose broadening of the fixpoint section)
  key_links:
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md
    - .claude/skills/lz-refactor-workspace/e2e-angular/RESULTS.md
---

# Quick Task 260716-lq9: Extend drive-to-fixpoint to single-target apply

## Why

k=5 apply/drive eval (e2e-angular/RESULTS.md) found `idiom_pattern` is the one marginal
soft spot: on the mild-smell an1 cell, forced-invoke DEFERRED a warranted `.map()`
pipeline (idiom 1/4) because a single-target apply lands the first refactoring
(Extract Function) then STOPS, deferring the exposed follow-on. The package-sweep path
already drives to a fixpoint and scored idiom higher. Lever = extend that existing
fixpoint discipline to single-target apply, keeping every guard.

Eval reality check (why this is a thoroughness task, not a defect fix): idiom_pattern is
NOT a measured defect (with_skill 89% > baseline 73%; invoke 77%). Expect a possibly-null
lift. Ship only on a real, regression-free lift.

## The change (single locus, prose-only, SKILL.md)

1. Drive paragraph ("Coach by default; drive when asked"): on an explicit single-target
   COMMAND, after the chosen refactoring lands green, keep driving newly-exposed,
   warranted, in-scope follow-ons on that same target to a fixpoint (pointer to the
   fixpoint section) before stopping.
2. Rename the "Whole-package / directory sweeps" section to "Driving to a fixpoint
   (single target or whole-package sweep)" and broaden its opening so a single-target
   apply command is in scope; add the concrete exposed-follow-on example (extracted
   function reveals a loop that becomes a clean `.map()`/pipeline). All guard text
   (APPLY/DECLINE, blast-radius pause, revert-on-red, no-churn, stop-at-fixpoint)
   stays unchanged and now covers single-target too.

No new guards, no new section, no loosened gate. Reuses existing anti-over-reach text so
the over_under_engineering / incrementality dims cannot regress from a loosened contract.

## Gated execution (deviates from standard auto-commit executor)

Standard quick executor commits atomically; here the commit is CONDITIONAL on an eval that
runs after a human `/reload-plugins`, so the edit is applied UNCOMMITTED and shipped only
if the eval passes.

- [ ] T1 Apply the two SKILL.md edits (uncommitted).
- [ ] T2 Subagent review (>=2 reviewers, >=1 unbiased from-scratch) per standing rule; fix or revert on NEEDS-CHANGES.
- [ ] T3 Run the check battery: `npm run check` + `npm run typecheck` + `claude plugin validate .`.
- [ ] T4 GATE: user runs `/reload-plugins` (Claude cannot); committed != live, eval must measure the LIVE skill.
- [ ] T5 GATE: user approves metered `claude -p` spend for the held-out validation eval.
- [ ] T6 Run held-out validation eval on NEW targets (not ac1/cd1/an1), invoke_skill arm, k=3.
- [ ] T7 Decide: commit iff idiom lifts with no regression; else revert SKILL.md and report null.

## Success criteria

Idiom_pattern measurably lifts on held-out targets with zero regression on
output_quality / behavior_preservation / over_under_engineering / incrementality.
Otherwise: null result, revert, ship nothing.
