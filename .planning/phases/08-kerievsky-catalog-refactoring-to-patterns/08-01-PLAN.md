---
phase: 08-kerievsky-catalog-refactoring-to-patterns
plan: 01
type: execute
wave: 1
depends_on: []
branching_strategy: none
tags: [kerievsky-catalog, harness, checkers, typescript, validation, non-shipped-workspace]
files_modified:
  - .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs
  - .claude/skills/lz-refactor-workspace/extract-samples.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-smells.mjs
  - .claude/skills/lz-refactor-workspace/package.json
autonomous: true
requirements: [KRV-01, KRV-02, KRV-03, KRV-04]
must_haves:
  truths:
    - "D-01: a new check-kerievsky.mjs asserts all 27 canonical Kerievsky names are present EXACTLY once each as a leaf H1 (identity, not cardinality -- the WR-02 gap), filename == kebab-slug of the name"
    - "D-02: check-kerievsky asserts each leaf carries the LOCKED Fowler contract fields (Use when:, ## Motivation, ## Mechanics, ## Example with >=1 ts/js fence) PLUS the three Kerievsky field labels in lockstep with the leaf template (Pitfall 4)"
    - "D-03: check-kerievsky asserts every leaf has a Direction: value in {To, Towards, Away} and that the 3 named Away cases (Inline Singleton, Move Accumulation to Visitor, Encapsulate Composite with Builder) are present AND carry an 'refactor AWAY from <pattern>' callout"
    - "D-04: check-kerievsky asserts every leaf carries a 'GoF pattern:' field (a non-empty name OR the literal 'n/a -- utility' for the 3 Utilities); check-hygiene already guards no-verbatim GoF text (DST-04)"
    - "D-05: check-smells is extended so the candidate-link regex resolves BOTH (fowler|kerievsky)-catalog targets, source tags (Fowler|Kerievsky|both) are valid on every smell, and no un-deduped duplicate exists (a Kerievsky-tagged new leaf must not collide with an existing Fowler smell)"
    - "KRV-01 compile: extract-samples.mjs walks references/kerievsky-catalog/ (incl. *-walkthrough.md) and namespaces sample filenames per catalog so a Kerievsky slug cannot silently overwrite a Fowler sample module (false green)"
    - "check-crossrefs.mjs adds kerievsky-catalog leaves to sourceFiles so composed-Fowler links (../fowler-catalog/<slug>.md#<slug>) get resolution-checked; the inverse-of mutuality guard stays Fowler-CATALOG-scoped so one-directional Kerievsky->Fowler links are correctly excluded"
    - "All harness lives under .claude/skills/lz-refactor-workspace/ (NON-shipped, gitignored samples/), so claude plugin validate . still sees only pure Markdown; RED against the empty kerievsky-catalog is the expected Wave-1 baseline"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs"
      provides: "KRV-01/02/04 checker: 27-name identity + contract fields + Direction + 3 Away + GoF field + composed-Fowler presence + README-row mirror"
      min_lines: 60
    - path: ".claude/skills/lz-refactor-workspace/extract-samples.mjs"
      provides: "extended fence extractor: walks both catalogs + namespaced sample modules"
      contains: "kerievsky-catalog"
    - path: ".claude/skills/lz-refactor-workspace/tools/check-smells.mjs"
      provides: "extended smell checker: (fowler|kerievsky) candidate resolution + source-tag validity + dedup"
      contains: "kerievsky-catalog"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/extract-samples.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/*.md"
      via: "walk + extract ts fences (namespaced output)"
      pattern: "kerievsky-catalog"
    - from: ".claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      via: "kerievsky leaves in sourceFiles; composed-primitive link resolution"
      pattern: "KERIEVSKY|kerievsky-catalog"
---

<objective>
Extend the NON-shipped checker battery to gate the 27 Kerievsky leaves, their three
Kerievsky-specific fields, the 3 de-patterning (Away) cases, the composed-Fowler
cross-links, and the Ch.4 smell fold. No Kerievsky CONTENT is authored here -- this
plan produces only the instruments the authoring waves (08-02..08-06) are graded against.

Purpose: make KRV-01..KRV-04 machine-checkable BEFORE any content lands (Nyquist
scaffold-first), reusing the shipped-green Phase-7 harness. Every new/extended check is
RED against the empty kerievsky-catalog now and turns GREEN as the authoring waves fill
the leaves; this is the expected baseline, not a failure.

Output: a new `tools/check-kerievsky.mjs` + re-pointed `extract-samples.mjs`,
`check-crossrefs.mjs`, `check-smells.mjs` + the `package.json` check chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md
@.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-VALIDATION.md
@.claude/skills/lz-refactor-workspace/tools/check-catalog.mjs
@.claude/skills/lz-refactor-workspace/tools/check-smells.mjs
@.claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs
@.claude/skills/lz-refactor-workspace/extract-samples.mjs
@.claude/skills/lz-refactor-workspace/package.json

<interfaces>
This is a re-point + additive-checker job, NOT new tooling. check-catalog.mjs is the exact
template for check-kerievsky.mjs (name-identity model, report()/SUMMARY/exit idiom, slugFor,
walkMd, collectLeaves, README-row mirror). Reuse those helpers verbatim; swap the catalog dir
and the NAMES array. Assert IDENTITY not cardinality (WR-02). Keep the Fowler-verified
check-catalog.mjs UNTOUCHED (a sibling file, per RESEARCH recommendation).

The 27 canonical Kerievsky names the checker keys on (identity array; grouped by the book's
6 chapters, EXPECTED = 27). These are CITED from Kerievsky's catalog; the authoring waves
oracle-confirm exact wording -- if the oracle corrects a name, update this array in lockstep:
  Ch.6 Creation (6): Replace Constructors with Creation Methods; Move Creation Knowledge to
    Factory; Encapsulate Classes with Factory; Introduce Polymorphic Creation with Factory
    Method; Encapsulate Composite with Builder; Inline Singleton.
  Ch.7 Simplification (6): Compose Method; Replace Conditional Logic with Strategy; Move
    Embellishment to Decorator; Replace State-Altering Conditionals with State; Replace
    Implicit Tree with Composite; Replace Conditional Dispatcher with Command.
  Ch.8 Generalization (7): Form Template Method; Extract Composite; Replace One/Many
    Distinctions with Composite; Replace Hard-Coded Notifications with Observer; Unify
    Interfaces with Adapter; Extract Adapter; Replace Implicit Language with Interpreter.
  Ch.9 Protection (3): Replace Type Code with Class; Limit Instantiation with Singleton;
    Introduce Null Object.
  Ch.10 Accumulation (2): Move Accumulation to Collecting Parameter; Move Accumulation to
    Visitor.
  Ch.11 Utilities (3): Chain Constructors; Unify Interfaces; Extract Parameter.

The 3 named Away cases (D-03) the checker must find present + Away-flagged: Inline Singleton,
Move Accumulation to Visitor, Encapsulate Composite with Builder.

The three Kerievsky leaf field LABELS (keep the checker regex and the leaf template IDENTICAL
-- Pitfall 4 token lockstep):
  Composed Fowler primitive(s):   (>=1 link matching ../fowler-catalog/<slug>.md#<slug>)
  Direction:                      (value in {To, Towards, Away})
  GoF pattern:                    (a non-empty name OR literal "n/a -- utility")

The 3 Utilities (Chain Constructors, Unify Interfaces, Extract Parameter) MAY carry
"n/a -- utility" for GoF pattern (Open Question 1 default); the checker accepts a non-empty
value OR that literal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New check-kerievsky.mjs (27-name identity + fields + Direction + Away + GoF + composed-primitive presence + README mirror) and wire the check chain</name>
  <files>.claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs, .claude/skills/lz-refactor-workspace/package.json</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs (the EXACT template: path/CATALOG constant lines 24-28; NAMES array + identity loop; contract-field regexes lines 256-274; WEB_ONLY Set membership pattern lines 101-102, 280-294; readmeRowsBySlug + mirror assertion lines 184-208, 303-315; slugFor/walkMd/collectLeaves/report/isDir helpers)
    - .claude/skills/lz-refactor-workspace/package.json (the check script chain to extend)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md (check-kerievsky.mjs section: analog line refs + the exact Kerievsky-specific assertion regexes)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md (Harness Extension item 2; Open Question 1 GoF n/a; Pitfall 4 token lockstep; Assert identity not cardinality)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Clone check-catalog.mjs wholesale into a new sibling tools/check-kerievsky.mjs; do NOT edit
    check-catalog.mjs. Change the CATALOG constant from "fowler-catalog" to "kerievsky-catalog".
    Replace the NAMES array with the 27 canonical Kerievsky names (interfaces block, grouped by
    the 6 chapters), EXPECTED = 27. Keep the identity loop (each name present EXACTLY once as a
    leaf H1; filename == kebab-slug via slugFor; flag unknown/typo headings; no dupes). Reuse the
    contract-field regexes verbatim for Use when:, ## Motivation, ## Mechanics, ## Example, and
    the >=1 ts/js fence. ADD three Kerievsky field assertions keyed on the EXACT labels
    (Pitfall 4): Direction: with a value matching (To|Towards|Away); GoF pattern: with a non-empty
    value OR the literal "n/a -- utility"; Composed Fowler primitive(s): with at least one link
    matching ../fowler-catalog/<slug>.md#<slug> (PRESENCE only -- RESOLUTION is check-crossrefs's
    job). Model a 3-name Away Set on check-catalog's WEB_ONLY membership pattern: assert the 3
    named Away cases are present AND each carries Direction: Away plus an "away from <pattern>"
    callout in the leaf body (case-insensitive). Reuse the README-row mirror (readmeRowsBySlug +
    the mirror assertion): each leaf's Use when: line must appear in a kerievsky-catalog/README.md
    row that links to the leaf. Keep the report()/SUMMARY/process.exit(failures===0?0:1) idiom;
    against the empty catalog it MUST run and report the RED baseline (0/27 present, README rows
    absent) -- that proves the checker works before content lands. Then add
    "&& node tools/check-kerievsky.mjs" to the package.json "check" script chain (leave typecheck
    unchanged -- extract-samples's extended walk is inside it). Node builtins only; ASCII-only;
    add NO runtime dependency.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` executes, prints per-name PASS/FAIL and a `SUMMARY:` line, and on the empty kerievsky-catalog exits 1 reporting 0/27 present (expected RED baseline).
    - `rg -c "Replace Constructors with Creation Methods|Inline Singleton|Move Accumulation to Visitor|Encapsulate Composite with Builder|Extract Parameter" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` confirms the canonical names + the 3 Away cases are enumerated.
    - `rg -q "Direction:" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs && rg -q "GoF pattern:" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs && rg -q "Composed Fowler primitive" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` all match (the 3 field regexes exist).
    - `rg -q "n/a -- utility" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` matches (the utility GoF allowance).
    - `rg -q "check-kerievsky.mjs" .claude/skills/lz-refactor-workspace/package.json` matches (wired into the check chain).
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs` returns no hits (ASCII-only).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs; test $? -eq 1 && rg -q "check-kerievsky.mjs" .claude/skills/lz-refactor-workspace/package.json && ! rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/tools/check-kerievsky.mjs</automated>
  </verify>
  <done>check-kerievsky.mjs exists as a sibling to check-catalog.mjs, keys 27-name identity + the 3 Kerievsky fields + the 3 Away cases + the README-row mirror, exits 1 on the empty catalog (RED baseline), is ASCII-only, and is wired into the package.json check chain; check-catalog.mjs is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend extract-samples.mjs (walk kerievsky + namespace) + check-crossrefs.mjs (sourceFiles) + check-smells.mjs (candidate regex + source tags + dedup)</name>
  <files>.claude/skills/lz-refactor-workspace/extract-samples.mjs, .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs, .claude/skills/lz-refactor-workspace/tools/check-smells.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/extract-samples.mjs (CATALOG constant line 17; walk loop line 93; output-name line 122 -- the two edits)
    - .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs (CATALOG/SMELLS_DIR constants lines 27-29; sourceFiles line 98; the Fowler-CATALOG-scoped inverse-of mutuality guard line 176 that must STAY Fowler-only)
    - .claude/skills/lz-refactor-workspace/tools/check-smells.mjs (SMELLS array lines 28-53; candidate-link regex line 173; resolve logic lines 174-194; per-smell contract loop lines 199-240)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-PATTERNS.md (extract-samples / check-crossrefs / check-smells MODIFY sections -- exact line refs and diffs)
    - .planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-RESEARCH.md (Harness Extension items 1/3/4; Pitfall 1 uncompiled examples; Pitfall 2 candidate false-fail; D-05 fold assertions)
  </read_first>
  <action>
    extract-samples.mjs (KRV-01 compile): change the single-CATALOG walk to iterate BOTH
    references/fowler-catalog/ and references/kerievsky-catalog/ (walkMd already picks up
    *-walkthrough.md via its .md filter). Namespace the extracted output filename by catalog
    (e.g. prefix "kerievsky-" for kerievsky leaves) so a Kerievsky slug cannot silently overwrite
    a Fowler sample module (false green -- Pitfall 1/2). No tsconfig change (samples/**/*.ts is
    already globbed; keep pure examples, lib es2021 only). check-crossrefs.mjs: define a KERIEVSKY
    constant alongside CATALOG/SMELLS_DIR and add ...collectLeafFiles(KERIEVSKY) to sourceFiles so
    every composed-Fowler link (../fowler-catalog/<slug>.md#<slug>) is resolution-checked. Do NOT
    broaden the inverse-of mutuality guard -- it must stay CATALOG (Fowler)-scoped so the
    one-directional Kerievsky->Fowler composed-primitive links are correctly excluded from
    mutuality. check-smells.mjs (D-05 fold): broaden the candidate-link regex from fowler-catalog/-
    only to (?:fowler|kerievsky)-catalog/ keeping the same resolve logic (the
    path.resolve(REFERENCES,"smells",rel) base still works for ../kerievsky-catalog/... relatives);
    add a source-tag validity assertion (every smell row/leaf carries a tag in {Fowler, Kerievsky,
    both}); and add a dedup assertion (no smell name appears BOTH as a new Kerievsky-tagged leaf
    AND as an existing Fowler leaf). Derive the Kerievsky smell set from the source-tagged leaves
    rather than hardcoding names (the exact unique-vs-overlap map is oracle-settled in 08-06 per
    D-05); the checker validates STRUCTURE + resolution + dedup, leaving name-correctness to the
    oracle-reviewer. Node builtins only; ASCII-only. Against the current tree (no Kerievsky content
    yet) the extended checkers MUST still run: extract-samples stays green (Fowler examples only),
    check-crossrefs stays green, check-smells stays green (24 Fowler smells, all Fowler-tagged).
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` runs, prints a `SUMMARY:` line, and exits 0 over the current (Fowler-only) tree; `rg -q "kerievsky-catalog" .claude/skills/lz-refactor-workspace/extract-samples.mjs` confirms the second catalog is walked.
    - `rg -q "KERIEVSKY|kerievsky-catalog" .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` matches; `node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs` exits 0 over the current tree.
    - `rg -q "kerievsky" .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` matches (candidate regex broadened); `node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` exits 0 over the current 24 Fowler smells.
    - `rg -n "[^\x00-\x7F]" .claude/skills/lz-refactor-workspace/extract-samples.mjs .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` returns no hits.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/extract-samples.mjs && node .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs && rg -q "kerievsky-catalog" .claude/skills/lz-refactor-workspace/extract-samples.mjs && rg -q "KERIEVSKY|kerievsky" .claude/skills/lz-refactor-workspace/tools/check-crossrefs.mjs && rg -q "kerievsky" .claude/skills/lz-refactor-workspace/tools/check-smells.mjs</automated>
  </verify>
  <done>extract-samples walks both catalogs with per-catalog namespaced sample files; check-crossrefs resolves Kerievsky composed-Fowler links with the mutuality guard still Fowler-scoped; check-smells resolves (fowler|kerievsky) candidate links + asserts source-tag validity + dedup; all three stay GREEN over the current Fowler-only tree and are ASCII-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored harness files -> public GitHub repo | anything committed is world-readable (IP + secrets surface); the harness stays in the NON-shipped .claude/skills/lz-refactor-workspace/ |
| npm registry -> workspace devDependency | no NEW dependency enters this phase; the sole dep typescript@6.0.3 is pre-existing and Phase-7-verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | a checker that silently passes (WR-02 identity-vs-cardinality gap) | mitigate | check-kerievsky asserts 27-name IDENTITY (each present once) + field lockstep; every checker ends with SUMMARY + non-zero exit so a wave merge gates on exit code |
| T-08-02 | Information Disclosure | work email in any committed harness/checker file | mitigate | check-hygiene work-email allowlist gate (pre-existing) runs over authored files; ASCII scan in acceptance_criteria |
| T-08-03 | Tampering (integrity) | non-ASCII / mojibake corrupting a committed checker | mitigate | ASCII-only acceptance check (rg no non-ASCII byte) on every new/edited .mjs |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | NO new package installs this phase (RESEARCH Package Legitimacy Audit: N/A); sole dep typescript@6.0.3 pre-existing + Phase-7-verified; no registry surface change |

No high-severity threats: this plan ships no runtime, no network, no user input, and no
copyrighted content (checkers hardcode only public refactoring names). Nothing rises to the
`block_on: high` bar; no package-legitimacy checkpoint is needed (no installs).
</threat_model>

<verification>
- check-kerievsky.mjs runs and reports the RED baseline (0/27) on the empty kerievsky-catalog (exit 1).
- extract-samples, check-crossrefs, check-smells run and stay GREEN over the current Fowler-only tree (exit 0).
- check-catalog.mjs is unchanged; the harness lives only under .claude/skills/lz-refactor-workspace/; `git status` shows no new files under plugins/.
- All new/edited .mjs + package.json are ASCII-only; check-kerievsky.mjs is wired into the check chain.
</verification>

<success_criteria>
- A new check-kerievsky.mjs asserts 27-name identity + the 3 Kerievsky fields + the 3 Away cases + the README-row mirror, gating on exit code (RED baseline now).
- extract-samples walks both catalogs with namespaced output; check-crossrefs resolves composed-Fowler links (mutuality Fowler-scoped); check-smells resolves (fowler|kerievsky) candidates + validates source tags + dedup.
- No new runtime dependency; all tooling ASCII-only and inside the non-shipped workspace.
</success_criteria>

<output>
Create `.planning/phases/08-kerievsky-catalog-refactoring-to-patterns/08-01-SUMMARY.md` when done, recording the RED baseline counts and the exact harness diffs.
</output>
