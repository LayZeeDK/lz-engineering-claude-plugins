---
phase: quick-260803-53q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
autonomous: true
requirements: [TD-02]
must_haves:
  truths:
    - "D-01: the lz-refactor seam routes to lz-red on stated INTENT (the ask is to decide or write the next failing test), never on whether a failing test currently exists."
    - "D-01: a cleanup-on-green request still routes to lz-refactor, because no branch is conditioned on the absence of a red test."
    - "D-02: one widened seam section covers both siblings; no second parallel seam section exists, and the pre-existing lz-tpp routing survives inside it."
    - "D-03: the prose is written from lz-refactor's vantage (the stay-here branch is the refactor step), reusing the corrected lz-tpp decision SHAPE rather than its text."
    - "The step 1 back-reference quotes the renamed heading exactly; no dangling pointer remains."
    - "D-04: the new guard exits 1 with the pointer stripped and exits 0 with it present, both measured."
  artifacts:
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  key_links:
    - "check-red-references.mjs post-loop guard -> plugins/lz-tdd/skills/lz-refactor/SKILL.md (mirror of the existing SEAM-02 block that reads lz-tpp/SKILL.md)"
    - "SKILL.md step 1 back-reference -> the renamed seam heading in the same file"
    - "EXPECTED_CHECKS literal -> the emitted-check count (124 -> 125)"
---

<objective>
Close TD-02: the shipped `lz-refactor` skill has zero references to `lz-red`, so the
`lz-refactor -> lz-red` return hop of the red-green-refactor seam does not exist. Widen the
existing seam section (and the step 1 that feeds off it) to route to both siblings on stated
intent, and add a regression guard so the pointer cannot silently disappear again.

Purpose: SEAM-02 was marked Complete while the pointer was absent, in the sibling direction,
because nothing checked it. Prose alone did not hold; the guard is what makes this stay closed.
Output: two edited files, a battery that goes RED then GREEN on the change, and a guard proven
to fail in both directions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260803-53q-close-td-02-widen-the-lz-refactor-seam-s/260803-53q-CONTEXT.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md
@plugins/lz-tdd/skills/lz-tpp/SKILL.md
@.claude/skills/lz-red-workspace/tools/check-red-references.mjs
@AGENTS.md
</context>

<scope_fence>
Exactly two files change. Inside `lz-refactor/SKILL.md`, only the seam section (currently the
heading at line 37 plus its three-sentence body) and coach procedure step 1 (line 45) are edited.

Explicitly NOT touched, in either file:

- The `description` frontmatter of any skill. Editing it changes trigger behavior and would
  require an eval round; TD-02 is a body-content finding.
- The intro paragraph, `## Two modes`, and coach procedure steps 2 through 6.
- `lz-tpp`, `lz-red`, `README.md`, `CHANGELOG.md`, `plugin.json`, `marketplace.json`.
- `NEW_LABELS` and `RETIRED_LABELS` in the checker. D-04 says follow the SEAM-02 precedent, and
  that precedent bumped `EXPECTED_CHECKS` only. The existing SEAM-02 guard is deliberately absent
  from `NEW_LABELS`; the new sibling guard matches it.
- The docs commit (PLAN / SUMMARY / STATE). The orchestrator owns that.
</scope_fence>

<!-- planner-discipline-allow: lz-red -->
<!-- The Task 3 mutation step uses `rg -v 'lz-red'` to STRIP the pointer, which reads like a
     negative acceptance grep but is the opposite: the acceptance criterion is that the token is
     PRESENT (`git grep -c 'lz-red' ... ` returns non-zero). The literal is the subject of the
     task and must appear throughout. -->

<house_rules>
ASCII only, in both files and in every commit message. No emojis, em dashes, en dashes, curly
quotes, or ellipsis characters; `check-hygiene.mjs` gates this and the repo is public.

The new SKILL.md prose lands inside the tree that the `[wev G17]` bare-qualifier gate walks. Do
not use the contested word (the s-t-u-b family) unqualified anywhere in the new text. The seam
prose has no reason to reach for it, so the safe move is to not use it at all.

The seam section also lives in a file walked by the `[lc9]` dead-relative-link and ragged-table
gates. If the new text carries a markdown link, its target must resolve on disk.
</house_rules>

<tasks>

<task type="auto">
  <name>Task 1: Add the TD-02 return-pointer guard instrument-first and assert it RED</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <action>
Add ONE post-loop guard asserting that the shipped `lz-refactor/SKILL.md` carries a return
pointer to `lz-red`, placed immediately after the existing SEAM-02 block that reads
`lz-tpp/SKILL.md` (around line 526). This is the mirror-image seam guard, so it belongs next to
its sibling and follows the same post-loop block idiom.

Guard requirements (D-04):

- Resolve the target with `path.join(repoRoot, "plugins", "lz-tdd", "skills", "lz-refactor", "SKILL.md")`.
- Read it inside a try/catch that yields the empty string on any failure. Do NOT wrap the guard
  in an `existsSync` branch: this file's own header condemns existsSync-gated post-loop blocks as
  a silent vacuous pass, and an emit-nothing path would make the roster gate fire on the count
  instead of on the guard that found something. Empty text has no match, so the guard fails
  closed with no extra branch.
- Test `/lz-red/` presence and call `report()` exactly ONCE, unconditionally.
- Use this exact label literal, because Task 3 greps it:
  `lz-refactor/SKILL.md: TD-02 return pointer to lz-red`
- Fail detail: name what is missing, for example that the lz-refactor skill carries no return
  pointer to the red step.

Bump `EXPECTED_CHECKS` from 124 to 125 and append the arithmetic to the existing comment block
above it as its own term, matching the file's convention of keeping each decision legible on its
own line: +1 for the TD-02 return-pointer guard, the sibling of the restored SEAM-02 pointer
guard, so 124 + 1 = 125. Leave every prior term in that comment intact.

Extend the GREEN `SUMMARY:` line at the end of the file with a short clause noting the
lz-refactor return pointer, so the instrument's own output does not overstate what it checked.

Do not touch `NEW_LABELS` (see the scope fence). Do not weaken, rename, or reorder any existing
guard; the emitted count must move by exactly one.

Instrument-first is the point: at this task's HEAD the pointer does not exist yet, so the guard
FAILS against the unmodified tree. That is the D-04 stripped-pointer direction measured against a
real state rather than a synthetic mutation, and it proves the guard can fail before any content
is written to satisfy it.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1 && echo RED-DIRECTION-OK</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '\[FAIL\] lz-refactor/SKILL\.md: TD-02 return pointer to lz-red' && echo GUARD-ATTRIBUTED-OK</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '\[PASS\] \[2ig\] roster integrity' && echo ROSTER-OK</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -c '\[FAIL\]'</automated>
  </verify>
  <done>
The checker exits 1. The only `[FAIL]` line is the new TD-02 guard (the last verify command
prints `1`). The roster gate PASSes at 125, proving the count bump is correct and the RED is
attributable to the new guard alone rather than to a dropped or duplicated check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen the seam section and step 1 to route to both siblings on intent</name>
  <files>plugins/lz-tdd/skills/lz-refactor/SKILL.md</files>
  <action>
Two coupled edits in one file. Both move together; the second is the known trap.

SUB-STEP A -- widen the seam section (D-02, D-03).

Rename the heading at line 37 so it names all three steps rather than only the lz-tpp seam. A
heading in the shape `## Refactoring vs the red step (lz-red) and the green step (lz-tpp)` works;
the exact wording is yours. Then widen the body IN PLACE. Do not append a second seam section:
two adjacent blocks about how this skill differs from its neighbors is precisely what D-02
forbids, and the existing lz-tpp routing must survive intact inside the widened one.

The widened body states the ownership of each step and then routes by INTENT, stated positively:

- lz-refactor owns the refactor step: improving the structure or readability of code without
  changing what it does.
- lz-tpp owns the green / transformation step: changing behavior so a failing test passes.
- lz-red owns the red step: deciding and writing the next failing test.
- Route on what the developer is ASKING FOR. If the ask is to make a failing test pass by
  changing behavior, hand off to lz-tpp. If the ask is to decide or write the next failing test,
  hand off to lz-red. If the ask is to improve structure or readability without changing
  behavior, stay here.

D-01 is the decisive constraint and it is a constraint on the CONDITION, not on the wording.
The lz-red branch keys on the developer's stated intent. It must NOT be conditioned on whether a
failing test currently exists, in any phrasing: no branch may fire because a red test is absent,
missing, not yet written, or not in hand. That condition is true of a developer who wants to
clean up passing code, so it strictly contains the cleanup branch and would mis-route
cleanup-on-green -- which is this skill's PRIMARY use case -- to lz-red. Do not restate that
reasoning in the shipped prose; just do not write the defective condition.

D-03: reuse the DECISION SHAPE of the corrected lz-tpp pointer (three named owners, then a
classify-before-acting rule with one positively-stated branch each), and write the sentences from
lz-refactor's own vantage. A verbatim lift reads wrong here, because this skill's stay-here
branch is the refactor step, not the green step.

Keep it proportionate to the section it replaces. This is a router, and the surrounding sections
run three to five lines.

SUB-STEP B -- move the back-reference with the heading (the trap).

Step 1 of the coach decision procedure currently closes by naming the old heading literally
inside quotes and telling the reader not to restate it. Renaming the heading in sub-step A
without updating that line leaves a pointer to a section that no longer exists under that name.
Update the quoted heading text so it matches the new heading EXACTLY, character for character.

While in step 1, widen its classify gate the same way the section was widened: it currently
classifies against the lz-tpp seam only. It must classify against both seams, with the same three
positive branches and the same D-01 prohibition. Keep step 1 short -- it defers to the section
above rather than restating it, and that division of labor is what keeps the router lean.

Verify no other back-reference to the old heading text survives anywhere in the shipped tree
(the third verify command below). Matches inside `.planning/` are archived historical records of
what the file said at the time and are correctly left alone.
  </action>
  <verify>
    <automated>git grep -c 'lz-red' -- plugins/lz-tdd/skills/lz-refactor/SKILL.md</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '\[PASS\] lz-refactor/SKILL\.md: TD-02 return pointer to lz-red' && echo GREEN-DIRECTION-OK</automated>
    <automated>MSYS_NO_PATHCONV=1 git grep -c 'Refactoring vs the green step' -- plugins; test $? -eq 1 && echo NO-DANGLING-BACKREF</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 0 && echo RED-BATTERY-GREEN</automated>
  </verify>
  <done>
The first command prints a non-zero count and exits 0 -- this is the exact measurement that made
TD-02 a finding, and its flip from no-match to matched IS the close. The TD-02 guard now PASSes,
no reference to the old heading text survives under `plugins/`, and the whole lz-red checker
exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the guard fails when mutated and run the full acceptance battery</name>
  <files>plugins/lz-tdd/skills/lz-refactor/SKILL.md, .claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <action>
No content changes. This task measures, and it must leave the tree byte-identical to how it found
it.

PART 1 -- mutation test the guard against the FINAL tree (D-04).

Task 1 proved the stripped-pointer direction against the pre-edit tree. Re-prove it against the
shipped state, so the proof covers the guard and the content that actually ships:

1. Copy `plugins/lz-tdd/skills/lz-refactor/SKILL.md` to a temp path outside the repo.
2. Strip every line containing the pointer: `rg -v 'lz-red'` the file to a temp file, then copy
   that temp file back over the original. The needle does not match `lz-refactor`, so the strip
   removes the pointer lines and nothing else.
3. Run the checker and assert the TD-02 guard reports FAIL. Grep for the guard's label rather
   than relying on the exit code alone: line removal can also trip the link or table gates, and
   attribution to the guard under test is the whole point.
4. Restore from the backup copy and assert byte identity with `cmp`. Assert this BEFORE running
   anything else; a mutation test that leaves a mutated tree behind is worse than no test.

PART 2 -- the full acceptance battery, every command runnable, every exit code asserted.

Run all four gates from the verification bar. The lz-refactor battery is not optional here even
though no file in that workspace changed: the lz-refactor tree is inside the hygiene and
cross-reference scan targets, so a bad link or a non-ASCII character in the new seam prose
reddens it.

Report each command with its observed exit code in the SUMMARY. If any gate is RED, fix the cause
in the file that owns it and re-run the whole battery -- do not paper over a gate.
  </action>
  <verify>
    <automated>cp plugins/lz-tdd/skills/lz-refactor/SKILL.md "${TMPDIR:-/tmp}/53q-SKILL.bak" && rg -v 'lz-red' plugins/lz-tdd/skills/lz-refactor/SKILL.md > "${TMPDIR:-/tmp}/53q-SKILL.stripped" && cp "${TMPDIR:-/tmp}/53q-SKILL.stripped" plugins/lz-tdd/skills/lz-refactor/SKILL.md && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '\[FAIL\] lz-refactor/SKILL\.md: TD-02 return pointer to lz-red' && echo MUTATION-EXIT-1-OK</automated>
    <automated>cp "${TMPDIR:-/tmp}/53q-SKILL.bak" plugins/lz-tdd/skills/lz-refactor/SKILL.md && cmp "${TMPDIR:-/tmp}/53q-SKILL.bak" plugins/lz-tdd/skills/lz-refactor/SKILL.md && echo RESTORED-IDENTICAL</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '\[PASS\] lz-refactor/SKILL\.md: TD-02 return pointer to lz-red' && echo MUTATION-EXIT-0-OK</automated>
    <automated>npm --prefix .claude/skills/lz-red-workspace run check; test $? -eq 0 && echo RED-WORKSPACE-GREEN</automated>
    <automated>npm --prefix .claude/skills/lz-refactor-workspace run check; test $? -eq 0 && echo REFACTOR-WORKSPACE-GREEN</automated>
    <automated>claude plugin validate .; test $? -eq 0 && echo PLUGIN-VALIDATE-OK</automated>
    <automated>git grep -c 'lz-red' -- plugins/lz-tdd/skills/lz-refactor/SKILL.md; test $? -eq 0 && echo TD-02-CLOSED</automated>
    <automated>git status --porcelain -- plugins/lz-tdd/skills/lz-refactor/SKILL.md</automated>
  </verify>
  <done>
The guard is measured in BOTH directions against the final tree: FAIL with the pointer stripped,
PASS with it restored, and the restore is proven byte-identical by `cmp`. All four verification
bar gates pass: both workspace batteries exit 0 (the lz-refactor one across its 10 checkers),
`claude plugin validate .` exits 0, and the TD-02 grep returns a non-zero count at exit 0. The
last command shows no unexpected working-tree residue from the mutation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub | Every byte of both files is published; maintainer identity and ASCII rules bind here |
| plan -> shipped skill prose | Text authored here becomes runtime routing instructions for the skill |
| instrument -> shipped tree | The checker reads the shipped tree; it must not be weakened while claiming to gate it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53q-01 | Tampering | check-red-references.mjs | medium | mitigate | The guard emits unconditionally and fails closed on an unreadable file; the roster gate holds the exact count at 125; Task 3 mutation-tests both directions, so a guard weakened to always-pass is caught |
| T-53q-02 | Information disclosure | plugins/lz-tdd/skills/lz-refactor/SKILL.md | medium | mitigate | New prose is original and ASCII-only; `check-hygiene.mjs` runs in the lz-refactor battery and gates ASCII plus email allowlist-inversion over this tree |
| T-53q-03 | Repudiation | seam routing prose | low | mitigate | Task actions cite D-01 through D-04 by ID, so the shipped wording traces to a recorded decision rather than to planner discretion |
| T-53q-04 | Denial of service | mutation test in Task 3 | low | mitigate | The mutation is copy-strip-restore with a `cmp` byte-identity assertion and a `git status` residue check before the task closes |

No package-manager install is in scope, so no `T-53q-SC` supply-chain entry applies; both
workspaces run on already-pinned dev dependencies and no manifest is edited.
</threat_model>

<verification>
The four gates from the verification bar, all in Task 3, each asserted on its exit code:

1. `npm --prefix .claude/skills/lz-red-workspace run check` exits 0.
2. `npm --prefix .claude/skills/lz-refactor-workspace run check` exits 0 (10 checkers).
3. `claude plugin validate .` exits 0.
4. `git grep -c 'lz-red' -- plugins/lz-tdd/skills/lz-refactor/SKILL.md` returns a non-zero count
   at exit 0. It exits 1 with no output today; that flip IS the close of TD-02.

Plus the D-04 both-directions guard proof, measured twice over: instrument-first against the
pre-edit tree in Task 1, and by copy-strip-restore mutation against the final tree in Task 3.
</verification>

<orchestrator_gate>
NOT an executor task. `gsd-executor` has no Agent or Task tool and cannot spawn a reviewer; any
instruction to do so would be unexecutable.

After the executor returns, the ORCHESTRATOR spawns the acceptance review of the edited
`SKILL.md`. At least TWO reviewers, and at least ONE briefed from scratch with no prior findings
and no priming about what the change was meant to fix.

This is not ceremony on this specific file. The D-01 defect shipped in `lz-tpp`, survived Phase
18's own verification, and was caught only by an unprimed from-scratch read. The unbiased brief is
the only gate that has ever caught this class of routing defect in this repo.

Give the unbiased reviewer the shipped file and the question, never the decision record: ask
whether the seam prose routes correctly for a developer who wants to clean up code whose tests
already pass. Do not tell that reviewer what D-01 says -- if the brief names the trap, the review
cannot independently find it.
</orchestrator_gate>

<success_criteria>
- TD-02 is closed by measurement, not by assertion: the grep that produced the finding now
  returns a non-zero count.
- One widened seam section, not two. The pre-existing lz-tpp routing survives inside it.
- The lz-red branch fires on stated intent. No branch anywhere in the file is conditioned on the
  absence of a failing test, so cleanup-on-green still routes to lz-refactor.
- The step 1 back-reference quotes the renamed heading exactly; no reference to the old heading
  text survives under `plugins/`.
- The guard exits 1 with the pointer stripped and 0 with it present, measured both ways, with the
  tree restored byte-identical.
- `EXPECTED_CHECKS` is 125 and the roster gate PASSes, so exactly one check was added and none
  was dropped or duplicated.
- All four verification bar gates exit 0. Both files are ASCII-only.
</success_criteria>

<output>
Create `.planning/quick/260803-53q-close-td-02-widen-the-lz-refactor-seam-s/260803-53q-SUMMARY.md`
when done. Record each verification bar command with its observed exit code, both mutation
directions with the observed guard verdict, and the before/after `lz-red` grep counts.
</output>
