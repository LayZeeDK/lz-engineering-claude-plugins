---
phase: 05-skill-effectiveness-evals
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .claude/skills/lz-tpp-workspace/trigger-results.json
autonomous: false
requirements: [EVAL-01]
user_setup: []

must_haves:
  truths:
    - "EVAL-01 runs NATIVELY on Windows via the vendored, bug-fixed harness (.claude/skills/lz-tpp-workspace/tools/skill-creator-eval, run_eval reimplemented per #3041) -- NOT under WSL and NOT the upstream run_eval (which crashes on Windows select() and mis-detects triggering on every OS). See 05-CONTEXT.md D-09 (POST-RESEARCH PIVOT). (EVAL-01, D-09)"
    - "A quick smoke measurement (trigger-smoke.json, runs-per-query 1) reads rate=1/1 on the should-trigger query and 0/1 on the near-miss BEFORE the full set runs; if the should-trigger reads 0 the run STOPS and surfaces (bad model id, or the detection/probe did not fire). (D-01 as amended by D-09)"
    - "The full 27-query trigger set (13 should-trigger / 14 near-miss) is measured 3x/query; should-trigger recall and should-not-trigger specificity are reported and assessed against the D-06 soft bar (~90% correct, near-misses quiet); missing the bar feeds at most the 05-04 tuning pass and never reopens Phases 1-4. (EVAL-01, D-05, D-06)"
  artifacts:
    - path: ".claude/skills/lz-tpp-workspace/trigger-results.json"
      provides: "run_eval output: per-query {should_trigger, trigger_rate, triggers, runs, pass} + summary {total, passed, failed} for the current lz-tpp description (EVAL-01)"
      contains: "trigger_rate"
  key_links:
    - from: ".claude/skills/lz-tpp-workspace/evals/trigger-eval.json"
      to: "tools/skill-creator-eval scripts.run_eval (native)"
      via: "claude -p --model claude-opus-4-8 ephemeral-skill trigger probe"
      pattern: "should_trigger"
---

<objective>
Run EVAL-01 (the trigger eval) NATIVELY on Windows using the vendored, bug-fixed harness.
Research (05-RESEARCH.md; issues anthropics/claude-plugins-official#3548, #3041, #2678)
showed the upstream run_eval both crashes on Windows (select() on a pipe) AND mis-detects
triggering on every OS (quits early; probes a slash-command not a skill). WSL fixes only the
crash, so WSL was ABANDONED (05-CONTEXT.md D-09). Instead we run the locally reimplemented
run_eval (reader-thread+queue; whole-turn detection; real ephemeral skill + prefix match) --
it runs natively with the already-authenticated `claude`, no WSL / interop / extra auth.

Purpose: prove EVAL-01 -- the lz-tpp `description` fires on red-green-refactor / TDD /
transformation-priority / TPP contexts and stays quiet on genuine near-misses. Output:
trigger-results.json (per-query trigger rates + summary) plus should-trigger recall and
should-not-trigger specificity recorded in the SUMMARY, assessed against the ~90% soft bar.

NOTE (autonomous:false): running the probe spends plan tokens via `claude -p` and is gated
on the user's explicit go (standing instruction: no evals/trigger optimization without
approval). A3 (lz-tpp not an enabled competing skill) must be re-verified at run time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md
@.planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md
@.planning/phases/05-skill-effectiveness-evals/05-01-SUMMARY.md

Inputs (must exist; authored in 05-01 / this session): evals/trigger-eval.json (27 queries),
evals/trigger-smoke.json (2 queries), both under .claude/skills/lz-tpp-workspace/evals/.

Constants (native Windows path forms):
- TOOL_DIR = .claude/skills/lz-tpp-workspace/tools/skill-creator-eval  (cwd for `python -m scripts.run_eval`; `scripts` is a package here)
- LZTPP = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp
- EVALS = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.claude/skills/lz-tpp-workspace/evals
- WS = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.claude/skills/lz-tpp-workspace
- Model id: claude-opus-4-8 (the session model - matches what users experience)

Harness facts (05-CONTEXT.md D-09, tool README): the vendored run_eval uses a reader
thread + queue (no select), so it runs on native Windows Python (verified: compiles/imports
on 3.13.6). It probes a REAL ephemeral `.claude/skills/<id>/SKILL.md` via the Skill tool and
matches the shared `lz-tpp-skill-` prefix. Invoke as `python -m scripts.run_eval ...` with
cwd = TOOL_DIR. Only run_eval is vendored (measurement); the auto-improve run_loop is NOT --
best_description / D-07 tuning is 05-04's gated, human call.
</context>

<tasks>

<task type="auto">
  <name>Task 1: A3 recheck + native smoke measurement (gate before the full set)</name>
  <files>
    (no committed files; produces console output only)
  </files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/tools/skill-creator-eval/README.md (exact run command + A3 note + the ephemeral-skill mechanism)
    - .claude/skills/lz-tpp-workspace/evals/trigger-smoke.json
    - .planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md D-09 (why native, not WSL) + 05-RESEARCH.md lines 205-219 (Pitfall 2 the enabled-skill false negative)
  </read_first>
  <action>
    First re-verify A3: confirm lz-tdd/lz-tpp are absent/disabled in ~/.claude/plugins/config.json and ~/.claude/settings.json (an enabled real skill would steal the trigger and cause false negatives). Then, from cwd = TOOL_DIR, run the smoke measurement on trigger-smoke.json: `python -m scripts.run_eval --eval-set <WS>/evals/trigger-smoke.json --skill-path <LZTPP> --model claude-opus-4-8 --runs-per-query 1 --num-workers 2 --verbose`. Read the per-query output: the should_trigger:true query MUST read rate=1/1 and the should_trigger:false query should read rate=0/1. If the should-trigger query reads 0 (or both read 0), STOP: do NOT run the full set; report the failure and the two likely causes -- an invalid `--model claude-opus-4-8` id, or the ephemeral-skill probe not firing on this Claude Code version -- and await user direction. This is the native replacement for the old WSL smoke gate.
  </action>
  <verify>
    <automated>echo "GATE: the should_trigger:true smoke query must read rate=1/1 (recorded in SUMMARY as smoke_rate). This marker line documents the gate; the rate itself comes from the run_eval --verbose output."; test -n "1"</automated>
  </verify>
  <acceptance_criteria>
    - A3 re-verified: lz-tdd/lz-tpp absent/disabled in config.json + settings.json (recorded in SUMMARY).
    - The smoke run prints rate=1/1 for the should_trigger:true query and ~0/1 for the near-miss.
    - If rate=0 on the should-trigger query, the plan HALTS with a surfaced report (model-id / probe cause); the full set is NOT run.
  </acceptance_criteria>
  <done>Smoke passed (rate=1/1) natively with lz-tpp not enabled, OR the plan halted and surfaced the failure.</done>
</task>

<task type="auto">
  <name>Task 2: Full native trigger measurement + capture results</name>
  <files>
    .claude/skills/lz-tpp-workspace/trigger-results.json
  </files>
  <read_first>
    - .claude/skills/lz-tpp-workspace/tools/skill-creator-eval/scripts/run_eval.py (output shape: results[] with should_trigger/trigger_rate/triggers/runs/pass + summary)
    - .claude/skills/lz-tpp-workspace/evals/trigger-eval.json
    - .planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md D-05 (3x sampling) + D-06 (~90% soft bar)
  </read_first>
  <action>
    Only after Task 1's smoke passes. From cwd = TOOL_DIR, run the full trigger measurement over the 20-query set: `python -m scripts.run_eval --eval-set <WS>/evals/trigger-eval.json --skill-path <LZTPP> --model claude-opus-4-8 --runs-per-query 3 --num-workers 3 --verbose > <WS>/trigger-results.json`. (Use run_eval for MEASUREMENT; do NOT run an auto-improve loop here -- best_description / D-07 tuning is 05-04's gated call.) From trigger-results.json capture: per-query trigger_rate + pass, the summary passed/total, should-trigger recall (fraction of should_trigger:true queries with rate>=0.5) and should-not-trigger specificity (fraction of should_trigger:false with rate<0.5). Sanity-check the --verbose stream: obvious should-trigger queries should read 3/3 and near-misses ~0/3 (a 0/3 everywhere pattern means the probe or model id is wrong -- Task 1 should have caught it). Record recall, specificity, the per-query table, and the overall accuracy vs the ~90% D-06 soft bar in the SUMMARY. Do NOT edit SKILL.md here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.claude/skills/lz-tpp-workspace/trigger-results.json'; if(!fs.existsSync(p)){console.error('FAIL trigger-results.json missing (did the smoke pass + full run complete?)');process.exit(1);} const r=JSON.parse(fs.readFileSync(p,'utf8')); const ok=Array.isArray(r.results)&&r.results.length>=18&&r.summary&&('passed'in r.summary); if(!ok){console.error('FAIL shape',Object.keys(r));process.exit(1);} console.log('OK trigger-results.json:', r.summary.passed+'/'+r.summary.total,'passed');"</automated>
  </verify>
  <acceptance_criteria>
    - trigger-results.json exists with results[] (>=18 queries) each carrying trigger_rate + pass, plus a summary with passed/total.
    - The --verbose stream shows obvious should-trigger queries at 3/3 (not 0/3, which would mean the probe/model id corrupted the run).
    - SUMMARY records should-trigger recall, should-not-trigger specificity, the per-query table, and overall accuracy vs the ~90% soft bar (no SKILL.md edit made here).
  </acceptance_criteria>
  <done>The full set ran natively, trigger-results.json captured with per-query rates + summary, and accuracy assessed against the D-06 bar. No description applied yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger-eval.json + skill description -> claude -p subprocess | The vendored run_eval synthesizes a throwaway ephemeral SKILL.md from the authored description and shells claude -p; input is author-supplied only |
| vendored harness (.claude/, non-distributed) -> repo | The tool is a reviewed, first-party reimplementation of upstream skill-creator (MIT); not shipped under plugins/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering / EoP | run_eval ephemeral .claude/skills/<id>/SKILL.md + claude -p | mitigate | The ephemeral skill is generated by the reviewed local harness from the authored description and deleted in a finally block; run only against the authored trigger sets. Re-verify (A3) lz-tpp is not an enabled competing skill before running (Pitfall 2). |
| T-05-05 | Spoofing (false negative) | trigger detection via ephemeral-skill prefix match | mitigate | The smoke gate confirms rate=1/1 on a should-trigger query before trusting the full run; a 0/3 pattern signals a bad model id or a non-firing probe and halts the run. |
| T-05-SC | Tampering (supply chain) | vendored run_eval reimplementation | mitigate | run_single_query was re-authored as reviewed first-party code (stdlib only; no network) rather than applying a third-party fork patch; upstream base is Anthropic MIT (LICENSE.upstream.txt). |
</threat_model>

<verification>
- A3 re-verified (lz-tpp not enabled) and native smoke rate=1/1 before the full set (Task 1).
- trigger-results.json captured with per-query rates + summary (Task 2).
- Obvious should-trigger queries at 3/3, near-misses quiet; overall accuracy assessed vs the ~90% soft bar.
</verification>

<success_criteria>
- EVAL-01 measured NATIVELY on Windows with a passing smoke gate: the description's per-query trigger rates, should-trigger recall, and should-not-trigger specificity are known and trustworthy (not corrupted by the upstream select() crash or the slash-command mis-detection).
- Results captured in trigger-results.json for 05-04's D-06 assessment and (gated) D-07 tuning decision.
- No SKILL.md change made in this plan (tuning is 05-04's gated call).
</success_criteria>

<output>
Create `.planning/phases/05-skill-effectiveness-evals/05-03-SUMMARY.md` when done, recording: the smoke result (rate + A3 recheck), should-trigger recall + should-not-trigger specificity, overall accuracy vs the ~90% bar, and the per-query trigger/quiet table. If the smoke halted the run, record the failure + likely cause (model id / probe) and that EVAL-01 needs attention.
</output>
