---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 08
type: execute
wave: 4
depends_on: [07-01, 07-04, 07-05, 07-06, 07-07]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/smells.md
autonomous: false
requirements: [FWL-02]
must_haves:
  truths:
    - "D-04: smells.md is a coach trigger table of Fowler's 24 bad smells ONLY -- each row = smell name (original words) + source tag Fowler + candidate named refactoring(s) cross-linked to fowler-catalog entries + a one-line when-to-pick"
    - "D-04: the table is shaped to accept the Phase-8 Kerievsky Ch.4 fold (source-tagged, deduplicated rows) WITHOUT re-touching the Phase-7 Fowler rows"
    - "D-07: an AskUserQuestion oracle-access checkpoint is opened BEFORE authoring the smell rows"
    - "FWL-02: every candidate cross-link resolves to a real fowler-catalog leaf anchor (sequenced after the leaves in wave 3)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      provides: "24 Fowler bad-smell trigger-table rows with resolving cross-links"
      contains: "| Smell | Source |"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/smells.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      via: "candidate-refactoring cross-links to leaf anchors"
      pattern: "fowler-catalog/[a-z-]+\\.md#"
---

<objective>
Fill `references/smells.md` with Fowler's 24 bad smells as a coach trigger table (FWL-02),
behind the mandatory D-07 oracle checkpoint. This runs in wave 4, AFTER all fowler-catalog
leaves exist, so the candidate-refactoring cross-links resolve to real anchors (07-RESEARCH
Open Question 3).

Purpose: the coach consumes this table in Phase 9 to route a detected smell to a candidate
named refactoring. Fowler rows only now; the table is shaped so Phase 8 appends Kerievsky
Ch.4 rows without editing the Fowler rows.

Output: a populated smells.md with 24 Fowler rows, each cross-linked into the catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@plugins/lz-tdd/skills/lz-refactor/references/smells.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md

<interfaces>
Smell trigger-table row shape (D-04, extension seam for Phase 8 -- from 07-RESEARCH.md
lines 404-412 and 07-PATTERNS.md lines 120-142):
`| Smell | Source | Candidate refactoring(s) | When to pick |` with a `Fowler` source tag
and candidate links of the form `[<Refactoring>](fowler-catalog/<leaf>.md#<anchor>)`. The
Source column is the extension seam: Phase 8 appends Kerievsky-tagged rows (and merges
`Fowler, Kerievsky` where they overlap) without editing the Fowler rows. Anchors must be
computed from the ACTUAL leaf headings authored in 07-04..07-07 (read the leaves). Keep each
row on ONE line (Pitfall 3). The 24 smell names are orientation only; the oracle confirms
the exact 2nd-ed wording + set at the checkpoint.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-07 oracle checkpoint -- Fowler 24 bad smells</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md (D-04 smell table scope; D-07 oracle mandate)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md (A1 orientation smell-name list -- oracle-confirmed here)
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (stub row contract + Phase-8 fold shape)
  </read_first>
  <what-needed>
    The authoritative Fowler Ch.3 set of 24 bad smells -- exact 2nd-ed names -- and, for each,
    the candidate named refactoring(s) Fowler associates with it, from the owner's Fowler
    Refactoring 2nd-ed e-book / web edition (ISBN 9780135425664). The set/wording MUST come
    from the oracle, not training memory (D-07). The orientation list in 07-RESEARCH A1 is a
    starting point to confirm, not the source.
  </what-needed>
  <how-to-provide>
    Open the AskUserQuestion oracle-access checkpoint. The owner confirms the 24 smell names
    and the candidate-refactoring mapping per smell so the table can be authored in ORIGINAL
    words with resolving cross-links.
  </how-to-provide>
  <action>
    Open the AskUserQuestion oracle-access checkpoint and BLOCK: author NO smell row until the
    owner confirms the 24 smells + candidate-refactoring mappings (D-07). This gate survives
    the --auto/--chain run.
  </action>
  <resume-signal>Owner confirms the 24 smell names and their candidate refactorings</resume-signal>
  <acceptance_criteria>
    - The AskUserQuestion oracle-access checkpoint is opened and the owner has confirmed the 24 smells + mappings BEFORE any row is written.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author smells.md (24 Fowler rows)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/smells.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/smells.md (stub contract + Phase-8 fold shape)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md (read the actual leaf headings to compute cross-link anchors)
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md (GFM table shape + one-row-per-line + cross-link-resolves discipline)
  </read_first>
  <action>
    Fill smells.md as a coach trigger table with the columns Smell / Source / Candidate
    refactoring(s) / When to pick. Author exactly 24 rows, one per Fowler bad smell (original
    words), each with Source `Fowler`, one or more candidate named refactorings cross-linked
    to the real fowler-catalog leaf anchor(s) authored in wave 3, and a one-line when-to-pick.
    Keep every row on ONE line (Pitfall 3). Do NOT add Kerievsky rows -- the Source column and
    row shape are the seam so Phase 8 appends Kerievsky Ch.4 rows without editing these (D-04).
    NO verbatim Fowler prose (DST-04). ASCII only. Update the Sources section from placeholder
    to a real Fowler Ch.3 attribution.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/tools/check-smells.mjs` exits 0: 24 Fowler-tagged rows AND every candidate cross-link resolves to a real fowler-catalog leaf anchor.
    - `rg -c "| Fowler |" plugins/lz-tdd/skills/lz-refactor/references/smells.md` returns 24 (Fowler rows only; no Kerievsky rows yet).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0 (ASCII-only + no work email).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner e-book -> authored smells.md | oracle smell set crosses into a public-repo file |
| authored file -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal | verbatim Fowler smell prose | mitigate | DST-04 distilled voice; check-hygiene no-verbatim heuristic + Phase-10 scan |
| T-07-03 | Tampering | fabricated smell set/mappings bypassing the oracle | mitigate | D-07 blocking checkpoint BEFORE authoring |
| T-07-06 | Tampering | dangling cross-links (validate does not check body links) | mitigate | check-smells.mjs asserts every candidate anchor resolves; wave 4 runs after the leaves exist |
| T-07-02 | Information Disclosure | work email in smells.md | mitigate | check-hygiene work-email allowlist gate |

No high-severity threats: a 24-row trigger table of public smell names behind a blocking
human oracle checkpoint. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The oracle checkpoint was opened before any row was authored.
- check-smells.mjs exits 0 (24 Fowler rows + all cross-links resolve); check-hygiene exits 0.
- No Kerievsky rows present (Phase-8 fold seam intact).
</verification>

<success_criteria>
- 24 Fowler smell rows authored with resolving candidate cross-links (FWL-02).
- Table shaped for the Phase-8 Kerievsky fold; ASCII-only; no verbatim prose.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-08-SUMMARY.md` when done.
</output>
