---
phase: 19-distribution-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/.claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [DST-01, DST-03]

must_haves:
  truths:
    - "plugins/lz-tdd/.claude-plugin/plugin.json version is 0.0.3 (was 0.0.2); its description names ALL THREE skills (lz-red at the RED step AND lz-tpp at the green step AND lz-refactor at the refactor step) framed as the completed red-green-refactor loop; its keywords keep the existing 13 TDD/refactoring terms and add RED-phase vocabulary; author.email / homepage / repository / license are byte-unchanged (author.email stays the public gmail larsbrinknielsen@gmail.com)"
    - ".claude-plugin/marketplace.json plugins[0].description names all three skills (three-skill marketplace-listing wording); NO version field is added anywhere in marketplace.json (plugin.json is the sole version source per D-06); $schema / name / owner / source / category unchanged (owner.email stays the public gmail)"
    - "README.md documents lz-red alongside lz-tpp and lz-refactor: the lead paragraph is a THREE-skill statement framed as the completed loop (lz-red red -> lz-tpp green -> lz-refactor refactor) and no longer says the plugin pairs only two skills; the 'What this is' list gains an lz-red bullet mirroring the existing /lz-tdd:<skill> invocation shape; a new '## What lz-red does' section mirrors the '## What lz-tpp does' / '## What lz-refactor does' two-mode Coach/Reference shape; a brief ORIGINAL RED primer + authoritative sources (LINK ONLY) + a pointer into plugins/lz-tdd/skills/lz-red/references/ mirror the existing per-skill primer blocks"
    - "README.md Installation block (the two /plugin commands) and the License block (public gmail) are UNCHANGED; the optional lz-red inventory line uses counts + names RE-VERIFIED against the live plugins/lz-tdd/skills/lz-red/references/ tree at write time (NOT transcribed) and does NOT imply a complete Beck / Metz / Feathers catalog (D-04)"
    - "CHANGELOG.md gains a '## [lz-tdd@0.0.3] - <date>' entry ABOVE the [lz-tdd@0.0.2] entry in the existing Keep a Changelog shape: a one-paragraph lead, an '### Added' list of bold-led bullets covering the lz-red skill (dual-mode coach + reference, /lz-tdd:lz-red) / the RED decision procedure on the Three Laws spine / the adaptive testing-stance router / the TypeScript + Vitest RED mechanics / the lz-tpp seam + reverse pointer / the anti-pattern + Test Desiderata references, and a bottom link-ref '[lz-tdd@0.0.3]: .../releases/tag/lz-tdd%400.0.3' (URL-encoded %40; the tag does not exist yet -- correct)"
    - "All four edited files are ASCII-only and carry only the approved public gmail among email-shaped tokens (allowlist-inversion clean); the new README primer, CHANGELOG entry, and manifest descriptions are ORIGINAL prose (link, don't inline) and node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 after each edit (all three axes GREEN)"
  artifacts:
    - path: "plugins/lz-tdd/.claude-plugin/plugin.json"
      provides: "version 0.0.3 + three-skill description + RED-phase keywords"
      contains: "0.0.3"
    - path: ".claude-plugin/marketplace.json"
      provides: "three-skill marketplace-listing description; no version field"
      contains: "lz-red"
    - path: "README.md"
      provides: "the lz-red section, three-skill lead + What-this-is, RED primer + link-only sources + references pointer"
      contains: "## What lz-red does"
    - path: "CHANGELOG.md"
      provides: "the lz-tdd@0.0.3 Keep-a-Changelog entry"
      contains: "[lz-tdd@0.0.3]"
  key_links:
    - from: "README.md"
      to: "plugins/lz-tdd/skills/lz-red/references/"
      via: "the RED primer section points readers into references/ for the RED content (link, don't inline)"
      pattern: "plugins/lz-tdd/skills/lz-red/references"
    - from: "CHANGELOG.md"
      to: "github.com/LayZeeDK/lz-engineering-claude-plugins/releases/tag/lz-tdd%400.0.3"
      via: "bottom link-ref mirroring the 0.0.1 / 0.0.2 %40-encoded release-tag URL"
      pattern: "releases/tag/lz-tdd%400.0.3"
    - from: "plugins/lz-tdd/.claude-plugin/plugin.json"
      to: "the /plugin update delivery path"
      via: "version 0.0.3 is what /plugin update compares; an unchanged version makes auto-update SKIP the plugin, so existing installs never see lz-red"
      pattern: "0.0.3"
---

<objective>
Author the Phase-19 three-skill 0.0.3 content edits under the already-GREEN hygiene gate: bump the
manifest metadata to a truthful three-skill 0.0.3 (DST-01/D-05/D-06), document lz-red in the README
alongside lz-tpp and lz-refactor as the RED step completing the red-green-refactor loop (DST-01/D-03/D-04),
and add the [lz-tdd@0.0.3] CHANGELOG entry (DST-01/D-11). Every new byte is authored ASCII-only, original
prose, and re-verified GREEN under check-hygiene as it lands (DST-03).

Purpose: plugin.json version is the update trigger -- an unchanged version makes /plugin update SKIP the
plugin, so existing installs never see lz-red; and the README / CHANGELOG / both manifests currently
describe a TWO-skill (lz-tpp + lz-refactor) plugin, which is a factual inaccuracy that must be corrected
before the public ship (factual inaccuracy is FIX-in-phase, D-05). Every edit mirrors an analog that
already lives in the same file (19-PATTERNS.md); no new source is authored.

## Decisions implemented (D-NN)

- **D-03:** the README gains an lz-red section MIRRORING the lz-tpp / lz-refactor shape (two-mode Coach/Reference block + brief ORIGINAL RED primer + authoritative sources link-only + references/ pointer); the lead and "What this is" listing become a THREE-skill statement framed as the completed red-green-refactor loop (lz-red red -> lz-tpp green -> lz-refactor refactor); section ordering / tagline / wording are Claude's discretion (D-13).
- **D-04:** the README MAY carry a one-line lz-red inventory (counts + names are FACTS) RE-VERIFIED against the live references/ tree at write time; it MUST NOT imply a complete Beck / Metz / Feathers catalog (those are no-oracle high-confidence core or single-surface owned, not exhaustive catalogs); do not transcribe counts.
- **D-05:** bump plugin.json version 0.0.2 -> 0.0.3 AND correct the two stale two-skill strings in the same file (description names lz-red as the third/RED skill; keywords gain RED-phase vocabulary) plus correct marketplace.json plugins[0].description to name all three skills -- all one-line factual-accuracy edits inside DST-01's blast radius.
- **D-06:** do NOT add a version field to marketplace.json (plugin.json wins silently, the marketplace value is masked, the validator warns); version lives in plugin.json only.
- **D-07:** re-run the authoritative hygiene gate (check-hygiene.mjs) GREEN after each content edit -- it ALREADY scans README.md / CHANGELOG.md / both manifests on the applicable axes (Research Finding 1), so no widening is needed; this plan re-verifies, it does not extend the gate (no new root prose file is introduced).
- **D-11:** add "## [lz-tdd@0.0.3] - <date>" in the existing Keep a Changelog shape mirroring the 0.0.2 entry (one-paragraph lead + "### Added" list + bottom %40-encoded link-ref); link, don't inline; no complete-catalog implication.

## Artifacts produced

- plugin.json: version string "0.0.3"; a three-skill description; keywords extended with RED-phase terms.
- marketplace.json: plugins[0].description naming all three skills; still no version key.
- README.md: a new "## What lz-red does" section heading, a new lz-red RED primer section, a three-skill lead paragraph, an lz-red "What this is" bullet, and an lz-red references/ pointer with a recounted inventory line.
- CHANGELOG.md: a new "## [lz-tdd@0.0.3] - <date>" entry and a new "[lz-tdd@0.0.3]:" release-tag link-ref.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-distribution-hygiene/19-CONTEXT.md
@.planning/phases/19-distribution-hygiene/19-RESEARCH.md
@.planning/phases/19-distribution-hygiene/19-PATTERNS.md

Each edit is a diff-in-words against an intra-file analog: the README lz-tpp / lz-refactor per-skill
sections and primer blocks, the CHANGELOG [lz-tdd@0.0.2] entry, and the current manifest fields -- all
excerpted with exact line refs in 19-PATTERNS.md ("Pattern Assignments"). The live files carry the analog
shapes directly (all were written under the same DST hygiene rules). Do NOT read plugins/lz-tdd/skills/lz-red
reference bodies to copy them into the README/CHANGELOG (link, don't inline) -- only recount the inventory.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump plugin.json to 0.0.3 + three-skill descriptions + RED-phase keywords; keep marketplace.json version-free (D-05, D-06)</name>
  <files>plugins/lz-tdd/.claude-plugin/plugin.json, .claude-plugin/marketplace.json</files>
  <read_first>
    - plugins/lz-tdd/.claude-plugin/plugin.json (the FILE being edited -- version at line 3 = "0.0.2"; description at line 4 names only lz-tpp + lz-refactor, NOT lz-red; keywords at lines 12-25 = 13 entries (tdd, test-driven-development, transformation-priority-premise, tpp, red-green-refactor, clean-code, typescript, refactoring, code-smells, design-patterns, gang-of-four, fowler, kerievsky) with no RED-phase vocabulary; author.email at line 7 = the public gmail, UNCHANGED)
    - .claude-plugin/marketplace.json (the FILE being edited -- plugins[0].description at line 13 names two skills; NO version key present today; $schema / name / owner / source / category UNCHANGED; owner.email = the public gmail)
    - .planning/phases/19-distribution-hygiene/19-PATTERNS.md (the plugin.json + marketplace.json "Pattern Assignments" sections: exact current field values, the three-skill description target, the D-13 keyword candidates, and the "leave unchanged" lists)
    - .planning/phases/19-distribution-hygiene/19-RESEARCH.md (Finding 6 current manifest state; Pitfall 6 the version-collision trap D-06; the D-13 keyword candidate list)
  </read_first>
  <action>
    Edit plugins/lz-tdd/.claude-plugin/plugin.json: set version to "0.0.3"; rewrite description to
    three-skill wording that names lz-red (choosing and writing the next failing test -- the RED step)
    ALONGSIDE lz-tpp (the green step, Transformation Priority Premise) and lz-refactor (the refactor step,
    Fowler + Kerievsky) as ORIGINAL prose framed as the completed red-green-refactor loop; extend keywords
    to keep the existing 13 and ADD RED-phase vocabulary from the D-13 candidate list (at minimum
    unit-testing, vitest, failing-test; optionally assertions, three-laws-of-tdd) -- exact list is D-13
    discretion. Leave author.email, homepage, repository, and license byte-for-byte unchanged. Edit
    .claude-plugin/marketplace.json: rewrite plugins[0].description to three-skill marketplace-listing
    wording consistent with the plugin.json description (names lz-red as the RED step). Do NOT add a version
    field to marketplace.json (D-06) and leave $schema, top-level name/description/owner, source, and
    category unchanged. Keep both files valid JSON and ASCII-only (straight quotes, --, ->); write only the
    approved public gmail as an email-shaped token, never any other email or domain.
  </action>
  <acceptance_criteria>
    - plugin.json line for version reads "version": "0.0.3"
    - plugin.json description contains all of "lz-red", "lz-tpp", and "lz-refactor"; keywords array contains at least unit-testing, vitest, and failing-test in addition to the original 13
    - marketplace.json plugins[0].description contains "lz-red"; marketplace.json has no version key anywhere in the file
    - plugin.json author.email and marketplace.json owner.email are unchanged (the public gmail larsbrinknielsen@gmail.com); both files parse as valid JSON
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (both manifests are in the widened (a)/(b) target set; ASCII + email allowlist-inversion PASS)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./plugins/lz-tdd/.claude-plugin/plugin.json'); if(p.version!=='0.0.3')process.exit(1); const d=p.description; if(!/lz-red/.test(d)||!/lz-tpp/.test(d)||!/lz-refactor/.test(d))process.exit(1); for(const k of ['unit-testing','vitest','failing-test']){if(!p.keywords.includes(k))process.exit(1);}" && node -e "const m=require('./.claude-plugin/marketplace.json'); if(!/lz-red/.test(m.plugins[0].description))process.exit(1); if('version' in m || m.plugins.some(x=>'version' in x))process.exit(1);" && git grep -qF 'larsbrinknielsen@gmail.com' -- plugins/lz-tdd/.claude-plugin/plugin.json && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>plugin.json is at 0.0.3 with a truthful three-skill description + RED-phase keywords; marketplace.json's listing description names all three skills and carries no version field; both parse as JSON, keep the public gmail, and pass the hygiene gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add the README lz-red section, three-skill lead + What-this-is, RED primer + sources + references pointer (D-03, D-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the FILE being edited -- the two-skill lead at :3-7; the "What this is" bullets at :9-16 (Marketplace / Plugin / Skill lz-tpp / Skill lz-refactor); the UNCHANGED Installation block at :23-30; the "## What lz-tpp does" two-mode shape at :32-42 and "## What lz-refactor does" at :66-77 to mirror; the "## Transformation Priority Premise" primer+sources+pointer shape at :44-64 and "## Refactoring" at :79-104 to mirror; the UNCHANGED License block at :106-108, public gmail)
    - .planning/phases/19-distribution-hygiene/19-PATTERNS.md (README "Pattern Assignments": the exact lead reword, the lz-red bullet, the per-skill section shape, the primer+sources+pointer shape, the D-04 inventory phrasing, and the "stays VERBATIM" install + license blocks)
    - .planning/phases/19-distribution-hygiene/19-RESEARCH.md (Finding 2 the live lz-red inventory; Finding 5 the README edit surface; the loop one-sentence pitch)
    - plugins/lz-tdd/skills/lz-red/ (recount ONLY -- confirm the /lz-tdd:lz-red invocation and the reference inventory: SKILL.md + the flat references + the testing-stance/ router leaves; do NOT copy reference bodies into the README; link, don't inline)
  </read_first>
  <action>
    Edit README.md. (1) Reword the lead paragraph (:3-7) so it frames THREE skills as the completed
    red-green-refactor loop -- lz-red at the RED step (choosing and writing the next failing test), lz-tpp
    at the green step, lz-refactor at the refactor step -- ORIGINAL prose; keep the closing "each skill is
    both an auto-triggering coach and an on-demand reference" sentence (it holds for all three). (2) Add an
    lz-red bullet to the "What this is" list (:9-16) mirroring the existing Skill bullets' /lz-tdd:<skill>
    invocation shape (invoked as /lz-tdd:lz-red); order the three by the loop or keep existing order (D-13).
    (3) Leave the Installation block (:23-30) verbatim. (4) Add a "## What lz-red does" section mirroring
    "## What lz-tpp does" / "## What lz-refactor does" (the same two-bullet Coach (auto-triggers at the RED
    step -- choosing/writing the next failing test, coaches not edits/runs) / Reference (invoked via
    /lz-tdd:lz-red) structure), ORIGINAL prose. (5) Add a RED primer section mirroring "## Transformation
    Priority Premise" / "## Refactoring": a brief ORIGINAL RED-phase primer, then an "Authoritative sources:"
    LINK-ONLY list (Beck, Robert C. Martin's Three Laws, Metz, Feathers as appropriate -- by title/author +
    URL, never inline text), then a pointer into plugins/lz-tdd/skills/lz-red/references/. Optionally add a
    one-line inventory using counts + names RE-VERIFIED against the live references/ tree at write time (run
    the recount; do NOT transcribe from the research or context files); the line MUST NOT imply a complete
    Beck / Metz / Feathers catalog (D-04). (6) Leave the License block (:106-108) unchanged. Section
    ordering, tagline wording, and whether to include a minimal ASCII usage snippet or badges are Claude's
    discretion (D-13). ASCII-only; ORIGINAL prose; only the approved public gmail as an email-shaped token.
  </action>
  <acceptance_criteria>
    - README.md contains a "## What lz-red does" heading and the invocation string lz-tdd:lz-red
    - The "What this is" list has an lz-red bullet; the lead paragraph names all three skills (lz-red, lz-tpp, lz-refactor) and no longer says the plugin pairs only two skills
    - The Installation block still contains both verbatim commands (plugin marketplace add LayZeeDK/lz-engineering-claude-plugins and plugin install lz-tdd@lz-engineering-claude-plugins)
    - The RED sources are LINK-ONLY (no inlined RED reference bodies) and a plugins/lz-tdd/skills/lz-red/references pointer is present; any inventory counts match a fresh recount of the live tree and do not claim a complete Beck/Metz/Feathers catalog
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (README is in the widened (a)/(b) set AND the axis (c) no-verbatim set -- the primer is original prose)
  </acceptance_criteria>
  <verify>
    <automated>git grep -qF '## What lz-red does' -- README.md && git grep -qF 'lz-tdd:lz-red' -- README.md && git grep -qF 'lz-tdd:lz-tpp' -- README.md && git grep -qF 'lz-tdd:lz-refactor' -- README.md && git grep -qF 'plugin marketplace add LayZeeDK/lz-engineering-claude-plugins' -- README.md && git grep -qF 'plugins/lz-tdd/skills/lz-red/references' -- README.md && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>The README documents lz-red alongside lz-tpp and lz-refactor (three-skill lead + What-this-is, a mirrored What-lz-red-does section, and an original RED primer + link-only sources + references pointer); the install + license blocks are untouched; any inventory counts are recounted live; the hygiene gate is GREEN on all three axes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the lz-tdd@0.0.3 CHANGELOG entry mirroring the 0.0.2 entry (D-11)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the FILE being edited -- the Keep a Changelog + SemVer header at :1-6; the "## [lz-tdd@0.0.2] - 2026-07-09" entry at :8-37 with its one-paragraph lead, "### Added" bullet list, and the two bottom %40-encoded link-refs at :63-64 -- the exact shape to mirror)
    - .planning/phases/19-distribution-hygiene/19-PATTERNS.md (the CHANGELOG "Pattern Assignments": the entry shape to mirror, the D-11 content list, the bottom link-ref shape, and the percent-encoded tag rule)
    - .planning/phases/19-distribution-hygiene/19-RESEARCH.md (Finding 5 the CHANGELOG edit surface; D-11 content; the tag-not-yet-cut note)
  </read_first>
  <action>
    Edit CHANGELOG.md. Insert a new "## [lz-tdd@0.0.3] - <date>" entry ABOVE the [lz-tdd@0.0.2] entry (use
    the current date, 2026-07-20), mirroring the 0.0.2 entry's structure: (1) the heading; (2) a
    one-paragraph lead summarizing the lz-red skill addition and that the manifest moves to 0.0.3; (3) an
    "### Added" list of bold-led bullets -- one per deliverable: the lz-red dual-mode coach + reference skill
    via /lz-tdd:lz-red; the RED decision procedure on the Three Laws of TDD spine; the adaptive
    testing-stance router (functional core / message matrix / seams + legacy); the TypeScript + Vitest RED
    mechanics; the lz-tpp seam + reverse pointer; and the anti-pattern + Test Desiderata references -- bullet
    granularity is D-13. (4) a bottom link-ref
    [lz-tdd@0.0.3]: https://github.com/LayZeeDK/lz-engineering-claude-plugins/releases/tag/lz-tdd%400.0.3
    ABOVE the 0.0.2 link-ref, with the URL-encoded %40 for @, mirroring the existing refs verbatim in shape.
    Same link-don't-inline rule (no inlined RED reference bodies / examples). The entry MUST NOT imply a
    complete Beck / Metz / Feathers catalog. The release tag does not exist yet -- that is correct and
    matches the 0.0.1 / 0.0.2 precedent. ASCII-only; ORIGINAL prose; only the approved public gmail as an
    email-shaped token.
  </action>
  <acceptance_criteria>
    - CHANGELOG.md contains a "## [lz-tdd@0.0.3] -" heading positioned ABOVE the "## [lz-tdd@0.0.2]" heading
    - The entry has a one-paragraph lead, an "### Added" list of bold-led bullets, and a bottom link-ref containing "releases/tag/lz-tdd%400.0.3" (the %40 encoding is present)
    - The entry links (does not inline) the RED references and does not claim a complete Beck / Metz / Feathers catalog
    - node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (CHANGELOG is in the widened (a)/(b) set AND the axis (c) no-verbatim set)
  </acceptance_criteria>
  <verify>
    <automated>git grep -qF '[lz-tdd@0.0.3]' -- CHANGELOG.md && git grep -qF 'releases/tag/lz-tdd%400.0.3' -- CHANGELOG.md && git grep -qF '[lz-tdd@0.0.2]' -- CHANGELOG.md && node -e "const s=require('fs').readFileSync('CHANGELOG.md','utf8'); if(s.indexOf('[lz-tdd@0.0.3]') > s.indexOf('## [lz-tdd@0.0.2]'))process.exit(1);" && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <done>CHANGELOG.md has an lz-tdd@0.0.3 entry above the 0.0.2 entry, mirroring its shape (lead + Added bullets + %40 link-ref), linking not inlining the RED references, claiming no complete Beck/Metz/Feathers catalog; the hygiene gate is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

New human/AI-authored prose (the README RED primer, the CHANGELOG entry, the manifest descriptions)
crosses into the committed public repo. No runtime / network / session / crypto / input surface -- the
threats are content hygiene (PII, verbatim IP, mojibake) and factual / manifest correctness.

| Boundary | Description |
|----------|-------------|
| new authored prose -> committed public README/CHANGELOG/manifests | must be ASCII-only, work-email-free (only the approved gmail), original (not inlined book/talk text) |
| README/CHANGELOG/manifest claims -> live tree + LICENSE | install strings, invocations, contact, license, version, and lz-red inventory counts must not drift from reality |

## STRIDE Threat Register

ASVS level 1; block on `high`. None is left OPEN -- each threat has an existing GREEN mitigation.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-EM | Information Disclosure (PII) | work/PII email in new README/CHANGELOG/manifest prose | high | mitigate | check-hygiene axis (b) allowlist-inversion already scans all four files; assert only the approved gmail present; re-run GREEN after each edit; leak recurred twice in Phase 4 |
| T-19-IP | Information Disclosure / IP | verbatim book prose / talk transcript inlined into the README RED primer or CHANGELOG | high | mitigate | Link-don't-inline (D-03/D-11); primer is ORIGINAL prose; check-hygiene axis (c) no-verbatim already scans README + CHANGELOG; DST-04 owned-surface re-sweep is 19-03 |
| T-19-ASCII | Integrity | non-ASCII (cp1252 mojibake) in new prose | medium | mitigate | check-hygiene axis (a) ASCII byte-scan over the wide set; author ASCII-only (--, straight quotes, ->) |
| T-19-DRIFT | Tampering / Integrity | README/manifest/CHANGELOG stating a wrong version, install string, invocation, contact, or lz-red count | medium | mitigate | Lift install strings + email/URL/license verbatim from the live files; recount the inventory (D-04); version 0.0.3 agrees with the CHANGELOG %400.0.3 link-ref; claude plugin validate confirms manifest correctness at the 19-03 gate |
| T-19-SC | Tampering (supply chain) | npm/pip/cargo package installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: Not applicable); shipped plugins/lz-tdd stays dependency-free Markdown/JSON |
</threat_model>

<verification>
- After each task, node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs exits 0 (all three axes GREEN over the edited files).
- plugin.json version == 0.0.3; both descriptions name all three skills; marketplace.json has no version key.
- README has "## What lz-red does" + the three /lz-tdd:<skill> invocations + an unchanged install block + a references/ pointer; inventory counts match a fresh recount.
- CHANGELOG has the [lz-tdd@0.0.3] entry above [lz-tdd@0.0.2] with the %400.0.3 link-ref.
- The full battery + claude plugin validate + the review agents + the DST-04 re-sweep run at the 19-03 gate, not here.
</verification>

<success_criteria>
The manifests truthfully describe a three-skill 0.0.3 plugin, the README documents lz-red alongside
lz-tpp and lz-refactor as the RED step completing the red-green-refactor loop with original prose and
link-only sources, and the CHANGELOG records the 0.0.3 release -- all four files ASCII-only, carrying only
the approved public gmail, and GREEN under check-hygiene as each edit lands.
</success_criteria>

<output>
Create `.planning/phases/19-distribution-hygiene/19-01-SUMMARY.md` when done.
</output>
