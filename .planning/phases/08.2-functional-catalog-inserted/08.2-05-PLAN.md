---
phase: 08.2-functional-catalog-inserted
plan: 05
type: execute
wave: 3
depends_on: [08.2-02, 08.2-03, 08.2-04]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-loop-with-pipeline.md
autonomous: true
requirements: [FUN-02]

must_haves:
  truths:
    - "The functional-catalog README is a finalized N:1 pattern->idiom map: it declares the N:1 contract, mirrors each leaf's Use when: verbatim, caps every note cell to one line, and cites the research artifact in ## Sources"
    - "Every source item resolves in the map -- all 23 GoF, all 27 Kerievsky refactorings, all 5 extra patterns, and the ~15 Fowler FP-analog refactorings map to an idiom leaf; the Fowler moot/data-modeling/light items appear as one-line NOTES with no leaf link"
    - "N:1 leaves are targeted by intra-leaf anchor (slug.md#pattern-anchor) so each served pattern's row reaches its ### residual sub-heading; 1:1 leaves are targeted bare (slug.md)"
    - "The existing Fowler replace-loop-with-pipeline.md leaf gains a one-line Replace-Pipeline-with-Loop reverse-direction note (clarity is the default; reverse only for a measured hot path or a named house-style reason)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md"
      provides: "Finalized N:1 map (all rows + Fowler notes + ## Sources)"
      contains: "## Sources"
      min_lines: 60
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-loop-with-pipeline.md"
      provides: "The D-14 reverse-direction note (Replace Pipeline with Loop)"
      contains: "loop"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md"
      to: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/discriminated-union-and-fold.md"
      via: "N:1 rows targeting intra-leaf residual anchors (#visitor, #state, #interpreter, #composite)"
      pattern: "discriminated-union-and-fold.md#"
    - from: "plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md"
      to: ".planning/research/functional-depatterning-ts.md"
      via: "## Sources citation"
      pattern: "functional-depatterning-ts.md"
  decisions: |
    - **D-04:** the README is an N:1 pattern->idiom map that declares its N:1 contract in the header (siblings are 1:1 selector-mirrors), still mirrors each leaf's Use when: verbatim, caps note cells to one line, and cites the research artifact in ## Sources
    - **D-07:** de-patterning coverage -- every dissolvable GoF (23), Kerievsky refactoring (27), and Fowler FP-analog refactoring resolves via the README map to an idiom leaf OR a one-line note, deduped by idiom
    - **D-10:** the Fowler true-Stays-OO / moot refactorings are one-line README notes (3 moot + 8 data-modeling + 3 light per S12.3, reconciled against the shipped fowler leaf names, A5), NOT leaves; the Fowler FP-analogs fold into existing idiom leaves via README rows
    - **D-14:** the Replace-Pipeline-with-Loop reverse-direction note lives IN the existing Fowler replace-loop-with-pipeline.md leaf, not a new Fowler refactoring
    - **D-02:** the map is deduped BY IDIOM -- many patterns share one leaf row-set (e.g. Visitor/State/Interpreter/Composite all point at discriminated-union-and-fold)
---

<objective>
Finalize the functional-catalog N:1 README map -- the single artifact that resolves every OO
source item to an idiom leaf or a one-line note -- and add the D-14 reverse-direction note to
the existing Fowler replace-loop-with-pipeline leaf.

Purpose: FUN-02 (de-patterning coverage) + the selector-mirror (C3) / note-discipline (C7) /
Sources (C8) gates. This must run AFTER all 19 leaves exist (plans 02-04) because the map
mirrors each leaf's Use when: and targets N:1 leaves by intra-leaf residual anchor.

Output: finalized README.md; the Fowler leaf's reverse-direction note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-functional-catalog-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md

Map content source: `.planning/research/functional-depatterning-ts.md` Section 12 (the Fowler
3-way split + note sets) and Section 12.7 (the Replace-Pipeline-with-Loop evidence for the note).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the N:1 pattern->idiom map README</name>
  <read_first>
    - .planning/phases/08.2-functional-catalog-inserted/08.2-RESEARCH.md ("The N:1 Coverage Map (Deliverable 2)" -- all GoF/Kerievsky/extra/Fowler-analog tables + the Fowler NOTE sets; checker sub-checks C3, C7, C8)
    - .planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md (the functional-catalog/README.md section: row format, bare-link vs #anchor targeting, ## Sources convention)
    - plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/README.md and references/gof-catalog/README.md (the grouped-table + selector-mirror + ## Sources shape to mirror)
    - All 19 functional-catalog leaves (their exact Use when: lines to mirror verbatim, and their ### residual-anchor slugs)
  </read_first>
  <action>
    Rewrite `functional-catalog/README.md` as the finalized N:1 map. Intro prose declares the N:1 contract (unlike the 1:1
    sibling READMEs, many pattern rows fan into one idiom leaf). Emit map rows for every source item resolving to an idiom leaf:
    all 23 GoF (per the RESEARCH GoF->leaf table), all 27 Kerievsky refactorings (per the Kerievsky->leaf table, incl. A4's
    3 utilities: Chain Constructors -> factory-function, Unify Interfaces -> first-class-function, Extract Parameter ->
    currying-and-partial-application), all 5 extra patterns, and the ~15 Fowler FP-analog refactorings (per the Fowler-analog
    table). For an N:1 leaf, target the served pattern's intra-leaf residual anchor `<slug>.md#<pattern-anchor>` (e.g.
    `discriminated-union-and-fold.md#visitor`); for a 1:1 leaf, target the bare `<slug>.md`. Mirror each leaf's `Use when:`
    verbatim in the row's selector cell. Then add the Fowler NOTE rows (one line each, NO functional-catalog leaf link): the
    3 moot (Change Reference to Value; Remove Setting Method; Replace Superclass with Delegate), the 8 data-modeling (Pull Up
    Method/Field; Push Down Method/Field; Pull Up Constructor Body; Extract Superclass; Collapse Hierarchy; Remove Subclass),
    and the 3 light notes (Move Field; Encapsulate Variable/Record; Encapsulate Collection). A5: reconcile these note names
    against the actual shipped fowler-catalog leaf filenames (read the fowler-catalog dir) and use the exact leaf names; record
    any name adjustments in the SUMMARY. Add a `## Sources` block citing `.planning/research/functional-depatterning-ts.md`
    with a DST-04 no-verbatim disclaimer. Every note cell is exactly one line (no embedded newline / >1 sentence-line, C3 cap).
    ASCII-only.
  </action>
  <acceptance_criteria>
    - README has an intro declaring the N:1 contract and a `## Sources` block citing the research artifact
    - Every one of the 19 leaves' `Use when:` appears verbatim in at least one README row (C3 selector-mirror)
    - All 23 GoF + 27 Kerievsky + 5 extra + the Fowler FP-analog refactorings have a row resolving to an idiom leaf
    - The Fowler NOTE items (3 moot + 8 data-modeling + 3 light) each appear as a one-line note row with NO functional-catalog leaf link (C7)
    - N:1 leaf rows target `<slug>.md#<pattern-anchor>`; 1:1 leaf rows target bare `<slug>.md`
    - node tools/check-crossrefs.mjs resolves every README link (file + intra-leaf anchor); node tools/check-hygiene.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-crossrefs.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>The README is a finalized N:1 map; every leaf selector is mirrored, every source item resolves to a leaf or a one-line note, ## Sources cites the artifact; A5 reconciliation recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add the D-14 Replace-Pipeline-with-Loop reverse note to the Fowler leaf</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-loop-with-pipeline.md (the leaf itself + its ## Example pipeline domain)
    - .planning/research/functional-depatterning-ts.md Section 12.7 (the V8 pipeline-vs-loop evidence: measured hot path; intermediate-array/early-exit mechanism; single-reduce / typed-array middle grounds; clarity/debuggability/house-style)
    - .planning/phases/08.2-functional-catalog-inserted/08.2-PATTERNS.md (the replace-loop-with-pipeline reverse-note section -- Fowler leaves get NO Functional alternative: line)
  </read_first>
  <action>
    Append a concise reverse-direction note to `fowler-catalog/replace-loop-with-pipeline.md` (the inverse "Replace Pipeline
    with Loop" direction). Frame it exactly as D-14 / S12.7: clarity is the default; reverse to a loop only for a concrete named
    reason -- a measured performance hot path (mention the intermediate-array cost and the single-reduce / short-circuiting /
    typed-array middle grounds) or a house-style / debuggability / early-exit reason. Do NOT create a new Fowler refactoring and
    do NOT add a `Functional alternative:` line to this leaf (Fowler participates via README map rows only, D-10). ASCII-only;
    paraphrased (DST-04). Keep it short -- a short paragraph or a labelled note, not a second Example.
  </action>
  <acceptance_criteria>
    - replace-loop-with-pipeline.md contains a reverse-direction note mentioning both a measured hot path and a clarity/house-style reason
    - The leaf has NO `Functional alternative:` line
    - node tools/check-catalog.mjs still exits 0 (Fowler contract intact) and node tools/check-hygiene.mjs exits 0
    - node extract-samples.mjs still exits 0 (any code in the note, if present, compiles; prefer prose-only)
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/skills/lz-refactor-workspace && node tools/check-catalog.mjs && node extract-samples.mjs && node tools/check-hygiene.mjs</automated>
  </verify>
  <done>The Fowler leaf carries the reverse-direction note; the Fowler contract and hygiene stay GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Markdown reference content only -- no runtime/network/user-input surface. Boundaries are
repo-hygiene / licensing invariants.

| Boundary | Description |
|----------|-------------|
| repo -> published git history | README + the Fowler note must be ASCII-only, work-email-free, no verbatim book prose/code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-IP | Information Disclosure | README prose + the Fowler reverse note | mitigate | DST-04 paraphrase; the note is synthesized from S12.7 evidence, not copied. check-hygiene no-verbatim heuristic. |
| T-08.2-EM | Information Disclosure | committed files | mitigate | Work-email allowlist enforced by check-hygiene. |
| T-08.2-SC | Tampering | typescript@6.0.3 | accept | No new installs. |
</threat_model>

<verification>
- `node tools/check-crossrefs.mjs` exits 0 (every README row link + intra-leaf anchor resolves).
- `node tools/check-catalog.mjs` exits 0 (Fowler contract intact after the note).
- `node tools/check-hygiene.mjs` exits 0.
- check-functional advances on C3/C7/C8 (README rows + notes + Sources present); C4 still RED until plan 06 adds the 55 links.
</verification>

<success_criteria>
The README is a finalized N:1 map (all source items -> leaf-or-note, selectors mirrored, notes
one-line, ## Sources cites the artifact); the Fowler leaf carries the D-14 reverse note.
</success_criteria>

<output>
Create `.planning/phases/08.2-functional-catalog-inserted/08.2-05-SUMMARY.md` when done
(record the A5 Fowler-note-name reconciliation).
</output>
