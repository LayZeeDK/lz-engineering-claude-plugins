# .continue-here -- lz-refactor trigger-opt + coach/apply wrinkle fix

Paused 2026-07-11 to resume in a fresh session. Quick task:
`.planning/quick/20260710-lz-refactor-trigger-opt/`. **The detailed, per-phase resume runbook is
[RESUME.md](.planning/quick/20260710-lz-refactor-trigger-opt/RESUME.md)** -- follow it; this file is the
entry point.

## Where we are

COMPLETE (2026-07-11). All eval phases done and committed. Skill EDITS applied+committed (241c1fb);
isolated trigger-eval before/after DONE (92->100%); Step 4b e2e ground-truth DONE (coach auto-trigger
1/18 -> 18/18; ee95cd4); Step 5 apply-verify DONE (command drives 6/6, question advises 7/8, incl.
reviewed imperative prompts p2cmd/gr1cmd; c7942cb); results written into E2E-FINDINGS.md Resolution +
STATE.md Quick Tasks. **Only remaining action: user runs `/reload-plugins`** to make the tuned skill live
in the interactive session (NOT an eval prereq). This file + RESUME.md are historical now.

- **DONE + committed:**
  - Research: `.planning/research/skill-trigger-optimization.md` (reusable) + quick-task `RESEARCH.md`.
  - Eval set: `evals/trigger-eval.json` 13 pos / 11 neg (54/46, 4 seam), `check-evals` green; p6 seam
    negative in both `trigger-eval.json` and `d07-chunks/negatives.json`.
  - **SKILL.md edited** (`plugins/lz-tdd/skills/lz-refactor/SKILL.md`, commit 241c1fb): new 1245-char
    intent-based/pushy description + question-vs-command coach/apply rewrite + step 5 tweak. Reviewed by 4
    subagents (domain skill-reviewer, unbiased A/B, rubric, final in-context) -- all clean.
  - Trigger-eval BEFORE/AFTER (isolated harness): baseline 92% recall / 100% spec (in
    `.claude/skills/lz-refactor-workspace/baseline/`); after **100% recall / 100% spec** (in `after/`).
    The `groupImports` readability miss is fixed; ZERO specificity regression.
  - Char-cap finding saved to memory (`skill-description-char-cap`): field accepts >1024; ~1000-1500
    load-bearing; supersedes the repo's stale "1024 hard cap" note.

- **REMAINING (both GATED `--plugin-dir` spend; all resumable):**
  - **Step 4b -- e2e ground-truth** (the REAL-gap test): recommend, `--arm with_skill`, nx p1-p4 + kata
    gr1/gr2, at fresh run indices **run-4/5/6** (run-1/2/3 = OLD desc, preserved). Compare new-desc
    auto-trigger vs the e2e baseline (nx coach 1/12, kata 0/6). Commands in RESUME.md Step 4b.
    **STARTED (partial):** `p1 run-4` and `p2 run-4` landed, **both fired=true** -- an EARLY POSITIVE
    signal, since p1 NEVER fired in the old-desc e2e baseline. Only 2 of ~18 runs done (rest throttled by
    the limit). Resume the Step 4b commands (skip guard keeps p1/p2 run-4) to complete run-4/5/6 across all
    coach prompts, then tabulate old (run-1/2/3) vs new (run-4/5/6) per prompt.
  - **Step 5 -- wrinkle apply-verify**: kata gr1 (n=3) + nx p2 (n=5) apply on THROWAWAY branches, new desc,
    fresh indices; confirm drive-when-asked (no 0-edit runs). Commands in RESUME.md Step 5.
  - **Step 6 -- wrap**: analyze both, write before/after + e2e-gap verdict into the workspace docs
    (E2E-FINDINGS.md / a trigger-opt results doc), update STATE.md Quick Tasks + the quick SUMMARY, commit;
    then escalate to the user to `/reload-plugins` so the tuned skill is live in the interactive session.

## Key decisions / findings (do not re-litigate)

- Description = match user INTENT not jargon, "pushy", with an exclude-and-reroute clause naming lz-tpp
  (the green-step seam). Sized to the ~1000-1500 load-bearing window (1245), exclusions before the 1536 cut.
- Term-misuse ("what refactoring makes this failing test pass") is handled by the DESCRIPTION exclusion +
  graded as a SUPPORTING should-not-trigger negative; the true seam check is a routing/outcome test (lz-tpp
  wins) already shown by the e2e `p6 -> lz-tpp` result. "Grade outcomes, not paths."
- **KEY reframe:** the isolated trigger-eval (92->100%) does NOT reproduce the e2e coach gap (1/18); that
  gap is a busy-session / multi-skill-competition effect. So Step 4b (e2e ground-truth) is what actually
  answers "did the trigger-opt help real usage?"
- Coach/apply rewrite = QUESTION (advise) vs explicit COMMAND (drive + run tests, then stop; NO auto-commit).

## Blockers / how to resume

- **Repeated usage-limit hits** -- every runner is resumable: the trigger chunk runners are canary-gated
  (throttled chunks marked INVALID, re-run on re-invoke); `run-e2e.mjs` skips completed runs. Resume =
  re-run the SAME command; proven to recover cleanly (recovered 3x). Before re-measuring a CHANGED
  description on the trigger-eval, delete `trigger-results-d07-*-chunk-*.json` first (baseline/after are
  preserved in their subdirs). Details + exact commands: RESUME.md.
- SKILL.md is committed with the new description -> the evals read it from disk (no reload needed for
  evals). `/reload-plugins` only for live interactive use (Step 6).
- nx + kata must be pristine before recommend runs / on a throwaway branch before apply runs -- see RESUME.md.
