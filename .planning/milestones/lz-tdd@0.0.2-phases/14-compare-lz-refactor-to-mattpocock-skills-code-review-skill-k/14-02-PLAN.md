---
phase: 14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-enforce-module-boundaries.md
  - .claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-runtime-lint-utils.md
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/cr-gilded-rose.md
  - .claude/skills/lz-refactor-workspace/e2e-nx/suite.json
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/suite.json
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "Three report-framed cr-* prompts exist, each pointing at the SAME single target file code-review reviews (nx enforce-module-boundaries.ts, nx runtime-lint-utils.ts, kata app/gilded-rose.ts), read-only, non-leading"
    - "Each suite.json gains matching prompt entries wiring cr-* ids to their targets.json target"
    - "The invoke_skill arm prepends /lz-tdd:lz-refactor to each cr-* body automatically (bodies stay framing-only)"
    - "Each cr-* prompt passed subagent review including >= 1 unbiased from-scratch reviewer before any run"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-enforce-module-boundaries.md"
      provides: "report-framed lz-refactor query for the nx Long-Function/Conditional-Complexity target (T1)"
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-runtime-lint-utils.md"
      provides: "report-framed lz-refactor query for the nx functional/FP loop target (T3/T4)"
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/cr-gilded-rose.md"
      provides: "report-framed lz-refactor query for the kata pattern-directed target (G1)"
  key_links:
    - from: "suite.json.prompts"
      to: "prompts/cr-*.md"
      via: "each entry's file + target fields wire a cr-* prompt to its targets.json ground-truth id"
      pattern: "cr-"
  decisions: |
    - **D-08:** Author new recommend/report-oriented lz-refactor queries mirroring code-review's "review these files, surface smells + named fixes" framing, pointing at the SAME files code-review reviews, explicit /lz-tdd:lz-refactor invocation via the invoke_skill arm, read-only; subagent-review each (incl. >= 1 unbiased) before any run.
---

<objective>
Author the three report-framed `/lz-tdd:lz-refactor` queries (D-08) that let the lz-refactor arm do the SAME
work code-review does -- "review this file, surface the smells and the named refactorings you would
recommend" -- pointing at the exact single files the code_review arm reviews as whole-file diffs. Wire them
into both suites' `suite.json`, then subagent-review them (including an unbiased from-scratch reviewer) so the
queries are provably non-leading before any spend.

Purpose: equal-input, equal-framing comparison. code-review reviews a diff and reports; these prompts make
lz-refactor report on the same file instead of driving an apply.
Output: three `cr-*.md` prompt bodies + two `suite.json` edits, review-cleared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESEARCH.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-PATTERNS.md

Target files (the same files code_review reviews; single file per cell for equal input):
- nx T1: `packages/eslint-plugin/src/rules/enforce-module-boundaries.ts` (Long Function, Conditional Complexity, Duplicated Code)
- nx T3/T4: `packages/eslint-plugin/src/utils/runtime-lint-utils.ts` (imperative loops -> pipeline; reduce -> Map group-by)
- kata G1: `app/gilded-rose.ts` (Conditional Complexity, Magic Numbers, Primitive Obsession, Repeated Switches)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author cr-* prompts + suite.json entries</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-enforce-module-boundaries.md, .claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-runtime-lint-utils.md, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/cr-gilded-rose.md, .claude/skills/lz-refactor-workspace/e2e-nx/suite.json, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/suite.json</files>
  <read_first>
    - e2e-nx/prompts/p8-eslint-plugin-package-directive.md and e2e-gilded-rose/prompts/gr1-update-quality.md (existing non-leading prompt shape). e2e-nx/suite.json lines 7-23 + e2e-gilded-rose/suite.json lines 7-14 (prompt-entry shape `{id,file,target,code}`). e2e-nx/targets.json (T1 line 8, T3 line 28, T4 line 38) + e2e-gilded-rose/targets.json (G1 line 8). 14-PATTERNS.md "prompts/cr-*.md" section + 14-RESEARCH.md "Recommend-Mode Query Authoring (D-08)".
  </read_first>
  <action>
    Write three plain-Markdown, deliberately NON-LEADING, read-only report queries following the RESEARCH template ("Review `<target path>`. The tests are green. Surface the code smells you see and the named refactorings you would recommend for each. Do not edit anything."). Point each at its single target FILE path (relative to the run cwd = the synthetic worktree): cr-enforce-module-boundaries.md -> `packages/eslint-plugin/src/rules/enforce-module-boundaries.ts`; cr-runtime-lint-utils.md -> `packages/eslint-plugin/src/utils/runtime-lint-utils.ts`; cr-gilded-rose.md -> `app/gilded-rose.ts`. Keep bodies framing-only (the invoke_skill arm prepends `/lz-tdd:lz-refactor` at composePrompt time; do NOT put the slash command in the body). Do NOT name any expected smell/refactoring in the body (non-leading). Add matching `suite.json.prompts` entries following the existing shape: e2e-nx -> `{ "id": "cr-emb", "file": "cr-enforce-module-boundaries.md", "target": "T1", "code": true }` and `{ "id": "cr-rlu", "file": "cr-runtime-lint-utils.md", "target": "T3", "code": true }`; e2e-gilded-rose -> `{ "id": "cr-gr", "file": "cr-gilded-rose.md", "target": "G1", "code": true }`. ASCII-only. (The `target` id lets the synthetic-base setup and the grader resolve the file + ground truth; the runtime-lint file carries both T3 and T4 smells -- grading references both.)
  </action>
  <acceptance_criteria>
    - Three cr-* prompt files exist, ASCII-only, read-only framing, no expected smell/refactoring named, no `/lz-tdd:lz-refactor` in the body.
    - Both suite.json files parse and contain the new cr-* entries with `file`/`target` resolving to a real prompt file + a real targets.json id.
    - `node e2e-nx/run-e2e.mjs --dry-run --arm invoke_skill --prompt cr-emb --prompt cr-rlu` composes prompts prefixed with `/lz-tdd:lz-refactor`; `--suite e2e-gilded-rose --arm invoke_skill --prompt cr-gr` likewise.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --dry-run --arm invoke_skill --prompt cr-emb --prompt cr-rlu</automated>
  </verify>
  <done>Three report-framed cr-* prompts and their suite.json entries exist and compose under invoke_skill.</done>
</task>

<task type="auto">
  <name>Task 2: Subagent review of the cr-* prompts (incl. unbiased)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-enforce-module-boundaries.md, .claude/skills/lz-refactor-workspace/e2e-nx/prompts/cr-runtime-lint-utils.md, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/cr-gilded-rose.md</files>
  <read_first>
    - E2E-FINDINGS.md (the p2cmd/gr1cmd prompt-review discipline). MEMORY.md [[unbiased-review-beats-primed]]. 14-RESEARCH.md D-08 ("subagent-review each ... before any run") + CLAUDE.md "Spawning researcher agents" (fetch chain not needed here; these agents only read local prompt text).
  </read_first>
  <action>
    Spawn at least two review subagents on the three cr-* prompts. Give at least ONE an UNBIASED from-scratch brief: "Read these three prompt bodies. Are they leading the model toward any specific smell, refactoring, pattern, or verdict? Are they read-only report framing (no apply)? Do they name a target file that exists?" -- with NO prior findings and NO hint of the expected answer. Apply any leading-language or read-only-leak fixes the reviewers flag; re-review changed prompts. Record the review outcome in the SUMMARY. Do not proceed to Wave 1 with an un-reviewed prompt.
  </action>
  <acceptance_criteria>
    - >= 2 review subagents ran; >= 1 had an unbiased from-scratch brief.
    - Every flagged leading-language / read-only issue is fixed and the prompt re-reviewed; the SUMMARY records the reviewers' verdicts.
    - The three prompts remain ASCII-only and read-only after fixes.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --dry-run --arm invoke_skill --prompt cr-emb --prompt cr-rlu</automated>
  </verify>
  <done>The cr-* prompts are review-cleared (incl. unbiased) and safe to run in Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt authoring -> committed workspace | New prompt files + suite.json edits land in a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | committed cr-* prompts + suite.json | mitigate | ASCII-only; no work email; allowlist-inversion hygiene at commit |
| T-14-06 | Tampering (measurement validity) | a leading prompt biasing the head-to-head | mitigate | Subagent review incl. >= 1 unbiased from-scratch reviewer before any run (D-08) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | N/A -- no packages installed; static Markdown + JSON only |
</threat_model>

<verification>
- Both suite.json parse; `--dry-run --arm invoke_skill` composes each cr-* prompt with the `/lz-tdd:lz-refactor` prefix.
- Prompts are ASCII-only, non-leading, read-only, and point at existing target files.
- Subagent review (incl. unbiased) is complete and recorded.
- No metered `claude -p` run occurs in this plan.
</verification>

<success_criteria>
Three review-cleared, non-leading, report-framed cr-* prompts point lz-refactor at the exact files code-review reviews, wired into both suites, ready for the Wave-1 gated runs.
</success_criteria>

<output>
Create `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-02-SUMMARY.md` when done.
</output>
