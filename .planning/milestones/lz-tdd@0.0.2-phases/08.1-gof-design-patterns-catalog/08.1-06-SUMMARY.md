---
phase: 08.1-gof-design-patterns-catalog
plan: 06
subsystem: docs
tags: [extra-patterns-catalog, kerievsky, design-patterns, skill-authoring, oracle-clean-room, typescript]

# Dependency graph
requires:
  - phase: 08.1-gof-design-patterns-catalog
    provides: check-extra-patterns.mjs contract + extract-samples extra prefix + extra-patterns-catalog/README scaffold (Wave 1, 08.1-01)
provides:
  - 5 Tier-1 extra-pattern leaf files (null-object, factory, creation-method, composed-method, collecting-parameter) on the D-04 5-section contract
  - the extra-catalog link targets for the Wave-3 Kerievsky linkify (6 extra tokens; Factory -> extra-patterns-catalog/factory.md, NOT factory-method)
affects: [08.1-07 Wave-3 gate/linkify (README rows + check-extra 5/5 + the extra-token linkify resolution)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Clean-room oracle loop against .oracle/refactoring-to-patterns/ (Kerievsky), per-pattern chapters"
    - "Original-wording Intents from the first draft (DST-04 lesson carried from the GoF leaves): no near-verbatim flagged"
    - "Assembled Consequences where the source's explicit liabilities list is empty (Collecting Parameter), documented not fabricated"

key-files:
  created:
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/null-object.md
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/factory.md
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/creation-method.md
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/composed-method.md
    - plugins/lz-tdd/skills/lz-refactor/references/extra-patterns-catalog/collecting-parameter.md
  modified: []

key-decisions:
  - "All 5 leaves reached oracle-reviewer pass; no owner escalation"
  - "Factory H1 kept exactly 'Factory' so the Wave-3 linkify resolves Encapsulate Classes with Factory + Move Creation Knowledge to Factory into extra-patterns-catalog/factory.md, never factory-method.md (Pitfall 1)"
  - "Collecting Parameter's downside assembled from prose + the Visitor contrast (D-07); reviewer confirmed faithful, nothing fabricated"
  - "DST-04: Intents authored in original wording from the start (lesson carried from GoF leaves); no near-verbatim flagged on any extra leaf"

requirements-completed: []  # partial: 5/5 extra leaves authored (XTR-01 content). XTR-01 + GOF-03 close at the Wave-3 GREEN gate (08.1-07).

# Metrics
completed: 2026-07-07
---

# Phase 08.1 Plan 06: Tier-1 Extra-Pattern Leaves Summary

**The 5 Tier-1 extra patterns (Null Object, Factory, Creation Method, Composed Method, Collecting Parameter) authored on the same D-04 5-section contract as the GoF leaves, via the clean-room oracle loop against Kerievsky's Refactoring to Patterns -- every leaf oracle-reviewer `pass`, every `## Example` tsc --strict-clean, Factory's H1 kept exactly "Factory" as the extra link target, and Collecting Parameter's downside assembled faithfully from the source prose + the Visitor contrast (D-07).**

## Accomplishments
- Authored 5 extra-pattern leaves BLIND with distinct example domains (notifier null-object, parser factory, Color creation-methods, checkout composed-method, validation-problems collecting-parameter).
- Deterministic layer GREEN before reviewer calls: extract-samples tsc --strict clean, check-hygiene clean, all 5 leaves pass the 5-section contract, Factory H1 exact.
- oracle-reviewer clean-room gate against .oracle/refactoring-to-patterns/ (Ch.6 Creation, Ch.7 Simplification, Ch.9 Protection, Ch.10 Accumulation); main context read NO `.oracle/` file.

## Oracle Loop -- per-leaf round counts
| Leaf | Rounds | Verdict | Round-1 directive addressed |
|------|--------|---------|------------------------------|
| Null Object | 2 | pass | added the "not warranted when null checks are few" caution |
| Factory | 1 | pass | faithful on first gate |
| Creation Method | 2 | pass | replaced neutral note with the source's creation-inconsistency liability |
| Composed Method | 1 | pass | faithful on first gate |
| Collecting Parameter | 1 | pass | assembled downside (Visitor contrast) confirmed faithful (D-07) |

## Settled Consequences (D-07)
- Collecting Parameter: the source's explicit liabilities list is empty; the downside is assembled from the surrounding prose + the Visitor contrast (accumulation tied to a shared interface, reads poorly across heterogeneous objects, where a Visitor is preferred). Reviewer confirmed faithful, nothing fabricated.
- Null Object: source liabilities carried (silences genuine failures; ongoing upkeep) + the "not warranted when few checks" caution.
- Creation Method: the source's sole stated liability (creation-style inconsistency across a codebase).

## Task Commits
1. **Task 1: author the 5 extra-pattern leaves (clean-room loop -> converge)** - feat commit (5 leaf files).
2. **Task 2: owner escalation** - NO-OP: all 5 reached oracle-reviewer pass within <=2 rounds.

## Decisions Made
- Marked NO requirements complete (content authored; XTR-01 + GOF-03 close at the Wave-3 GREEN gate).
- Kept the Factory H1 exactly "Factory" so the Wave-3 set-membership linkify resolves the two Kerievsky Factory tokens into this leaf, not factory-method.md.

## Deviations from Plan
None. Two leaves took a 2nd round (Null Object, Creation Method) for a missing source liability each; within the ~3-round cap, no owner escalation.

## Firewall
The main context opened NO file under `.oracle/`. All source contact was via the read-only oracle-reviewer subagent.

## Self-Check: PASSED
- 5 leaf files exist with single H1 + 5-section contract + >=1 ts fence; Factory H1 is exactly "Factory".
- extract-samples tsc --strict GREEN; check-hygiene GREEN.
- Every leaf oracle-reviewer `pass`.
- No README / SKILL.md / checker edited; no `.oracle/` file opened by this context.

---
*Phase: 08.1-gof-design-patterns-catalog*
*Completed: 2026-07-07*
