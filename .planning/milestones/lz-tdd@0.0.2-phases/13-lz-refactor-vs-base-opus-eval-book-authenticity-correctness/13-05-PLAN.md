---
phase: 13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness
plan: 05
type: execute
wave: 4
depends_on: ["13-03", "13-04"]
files_modified:
  - .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md
  - .claude/skills/lz-refactor-workspace/grading/summary.json
autonomous: true
requirements: [SPEC-req-5, SPEC-req-6]
user_setup: []

must_haves:
  truths:
    - "**D-08:** 13-RESULTS.md tabulates with_skill/invoke_skill vs no_skill per cell for BOTH dimensions (book-authenticity fidelity + correctness), with per-cell scores and Pass@k where run counts allow, reusing the EVAL-RESULTS.md / E2E-RESULTS.md format conventions; the verdict is framed as an EMPIRICAL finding (parity, or a delta with magnitude AND direction), NEVER a pre-assumed skill win; it confirms or refutes the prior finding (base Opus 4.8 @ high is catalog-grade on plain mechanical work; the skill's edge is pattern-directed / de-patterning / seam) and carries the comparability caveat that reused single-target skill-arm diffs were captured under earlier SKILL.md states (read single-target delta as skill-at-capture-time vs base-now; lean on the freshly-re-run sweep pair for the headline)."
    - "The phase leaves no residue: both borrowed repos are restored pristine (nx clean git status; kata back on protected main) and any throwaway worktrees/junctions are removed; the persisted diff.patch/meta.json + grading records in THIS repo remain as the tracked record; the hygiene gate passes (ASCII-only committed output; email allowlist-inversion asserting only the approved gmail; no .oracle/ path or prose in any committed artifact; committer identity = approved public gmail) (SPEC req 6)."
  artifacts:
    - path: ".planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md"
      provides: "the head-to-head record: two dimension tables (book authenticity + correctness), per-cell with/without + Pass@k, caveats, and the empirical verdict sentence"
      contains: "Verdict"
      min_lines: 40
    - path: ".claude/skills/lz-refactor-workspace/grading/summary.json"
      provides: "machine-readable rollup joining fidelity.json + name-layer.json + behavior.json per cell/arm with Pass@k"
      contains: "passAt3"
  key_links:
    - from: "grading/book-authenticity/fidelity.json + grading/correctness/{name-layer,behavior}.json"
      to: "grading/summary.json + 13-RESULTS.md"
      via: "join per cell/arm; Pass@k via run-e2e.mjs passAtK/passHatK"
      pattern: "passAt3|Verdict"
    - from: "borrowed repos (nx, kata)"
      to: "pristine restored state"
      via: "reset --hard base + clean -fd; kata back on main; worktrees removed"
      pattern: "clean"
---

<objective>
Aggregate the wave-3 grading records into 13-RESULTS.md (two tables -- book-authenticity fidelity + correctness -- with per-cell with/without scores, Pass@k, caveats, and an empirical verdict), run an unbiased from-scratch grading reviewer to catch grader bias on the verdict, then restore both borrowed repos pristine and pass the hygiene gate.

Purpose: no head-to-head record exists on book authenticity or correctness of applied output. This is the phase deliverable and the terminal measurement of the milestone. The verdict must be an honest empirical finding (the prior evidence is parity on mechanical work), not a pre-assumed skill win.
Output: 13-RESULTS.md (the only artifact under .planning/), grading/summary.json (the rollup), both borrowed repos restored pristine, hygiene scan clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESEARCH.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-PATTERNS.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-CONTEXT.md

Grading inputs (from wave 3):
- .claude/skills/lz-refactor-workspace/grading/book-authenticity/fidelity.json
- .claude/skills/lz-refactor-workspace/grading/correctness/name-layer.json
- .claude/skills/lz-refactor-workspace/grading/correctness/behavior.json

Results-format analogs (reuse the header/table/verdict/caveats conventions):
@.claude/skills/lz-refactor-workspace/e2e-nx/E2E-RESULTS.md
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/GR-RESULTS.md
@.claude/skills/lz-refactor-workspace/E2E-FINDINGS.md

13-RESULTS.md table skeleton: 13-RESEARCH.md "Code Examples -> REQ-5". Pass@k math: run-e2e.mjs passAtK/passHatK. Borrowed repos: nx D:/projects/github/nrwl/nx; kata D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate both dimensions into 13-RESULTS.md + summary.json with an empirical verdict</name>
  <files>.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md, .claude/skills/lz-refactor-workspace/grading/summary.json</files>
  <read_first>
    - 13-RESEARCH.md "Code Examples -> REQ-5: 13-RESULTS.md table skeleton" (one table per dimension) and "Common Pitfalls -> Pitfall 5" (ASCII + PII hygiene when quoting reused answer.md/oracle verdicts)
    - 13-PATTERNS.md "13-RESULTS.md (report, aggregate)" (header block, per-cell Pass@k table, with/without comparison, Verdict pattern, Caveats section) and E2E-RESULTS.md / GR-RESULTS.md / E2E-FINDINGS.md for the prior finding to confirm or refute
    - grading/book-authenticity/fidelity.json + grading/correctness/{name-layer,behavior}.json (the numbers to tabulate)
  </read_first>
  <action>
    Join fidelity.json + name-layer.json + behavior.json per {cell, arm} into grading/summary.json (a machine-readable rollup: per cell/arm the fidelity pass/partial/fail tally, name_layer pass count, tests_green count, and a Pass@k block {n, c, passAt1, passAt3, passHat3} for each scored dimension via run-e2e.mjs passAtK/passHatK -- import or copy the formulas, do not hand-roll). Then author 13-RESULTS.md in the phase dir with: (1) a header block (skill lz-refactor; model claude-opus-4-8 effort high; --setting-sources project; arms with_skill/invoke_skill vs no_skill; borrowed repos + branches; pointer to results/apply with the outputs/ gitignored note); (2) a "## Book authenticity" table (columns: cell | arm | runs | claims | pass | partial | fail | pass@1 | pass@3 | pass^3); (3) a "## Correctness" table (columns: cell | arm | name/layer k=3 | tests-green k=3 | pass@1 | pass@3); (4) a "## Caveats" section reusing the E2E-RESULTS caveats plus THIS phase's comparability caveat (reused single-target skill-arm diffs were captured under earlier SKILL.md states -> read single-target with/without as skill-at-capture-time vs base-now; the freshly-re-run sweep pair p8/gr4 carries no such caveat -> lean on it for the headline); (5) a "## Verdict" sentence framed as an EMPIRICAL finding -- parity, or a signed delta with magnitude and direction -- that confirms or refutes the prior finding (base Opus 4.8 @ high catalog-grade on mechanical; skill edge concentrated in pattern-directed / de-patterning / seam) and NEVER pre-assumes a skill win. When quoting any model claim or oracle verdict, normalize punctuation to ASCII (-> and straight quotes; no em-dashes/curly quotes/box-drawing). No .oracle/ path or prose; no work email.
  </action>
  <verify>
    <automated>test -s .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md && rg -qi 'verdict' .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md && rg -qi 'book authenticity' .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md && rg -qi 'correctness' .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md && test -s .claude/skills/lz-refactor-workspace/grading/summary.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 13-RESULTS.md exists with a header block, a Book authenticity table and a Correctness table (per-cell with_skill/invoke_skill vs no_skill), a Caveats section incl. the single-target version caveat, and a Verdict framed as an empirical finding (parity or signed delta), not a pre-assumed win
    - Pass@k/Pass^k are computed via run-e2e.mjs functions; summary.json joins all three grading records per cell/arm
    - ASCII-only; no .oracle/ path or prose; no work email
  </acceptance_criteria>
  <done>13-RESULTS.md tabulates both dimensions with Pass@k and an honest empirical verdict; grading/summary.json is the machine-readable rollup.</done>
</task>

<task type="auto">
  <name>Task 2: Unbiased from-scratch grading review of the verdict</name>
  <files>.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md</files>
  <read_first>
    - 13-CONTEXT.md "Claude's Discretion" (add an unbiased from-scratch grading reviewer -- recommended per unbiased-review-beats-primed -- to catch grader bias)
    - 13-VALIDATION.md "Manual-Only Verifications" (the verdict is framed as a finding; oracle verdicts contain no source prose)
  </read_first>
  <action>
    Spawn ONE unbiased reviewer subagent with a from-scratch brief (do NOT prime it with the authored verdict). Give it the grading records (fidelity.json, name-layer.json, behavior.json) and the two 13-RESULTS.md tables, and ask it to independently derive the verdict direction/magnitude from the per-cell numbers and flag any place the authored Verdict overstates a skill win, under-reports parity, mis-computes Pass@k, or lets a book-fidelity verdict quote source prose. If the unbiased reviewer's independent read diverges from the authored verdict, reconcile the Verdict sentence to the evidence (parity or a signed delta) and note the reconciliation. Optionally, if a per-claim list was drafted as a document, route it through oracle-reviewer as a second book-fidelity pass (oracle-reviewer gates drafted docs only, not raw transcripts). Keep the reviewer's finding summary ASCII-only; no .oracle/ prose; no work email.
  </action>
  <verify>
    <automated>rg -qi 'verdict' .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md && echo REVIEWED</automated>
  </verify>
  <acceptance_criteria>
    - an unbiased from-scratch reviewer independently checked the verdict against the per-cell numbers (Pass@k, parity vs delta, no overstated win)
    - any divergence was reconciled in the Verdict sentence; the reviewer confirmed no oracle verdict quotes source prose
    - the reviewer summary is ASCII-only, no .oracle/ prose, no work email
  </acceptance_criteria>
  <done>The empirical verdict is bias-checked by an unbiased reviewer and reconciled to the evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Restore borrowed repos pristine and pass the hygiene gate</name>
  <files>.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md</files>
  <read_first>
    - 13-RESEARCH.md "Runtime State / Repo Isolation & Restoration -> Canonical restoration check (REQ-6)" and "Common Pitfalls -> Pitfall 5 / Pitfall 7" (ASCII + PII scan; git grep for tracked, rg -uu for gitignored, never the Grep tool)
    - 13-PATTERNS.md "Shared Patterns -> Repo restoration (REQ-6)" and "ASCII + PII hygiene (REQ-6, Pitfall 5)"
    - CLAUDE.md / AGENTS.md public-repo hygiene (allowlist-inversion; approved gmail committer)
  </read_first>
  <action>
    Restore both borrowed repos to pristine using separate cd calls (never cd path && ... chaining; run git directly in each repo dir). nx (D:/projects/github/nrwl/nx): git reset --hard origin/23.0.x, git clean -fd, confirm git status --porcelain is empty. kata (D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript): git checkout main, git reset --hard main, git clean -fd (remove any locally written __snapshots__), delete the throwaway branch lz13-kata-apply, confirm git status is clean on main. Remove any throwaway git worktrees + node_modules junctions created during the phase (git worktree remove --force as needed). Then run the hygiene gate on THIS repo's newly authored/committed artifacts (13-RESULTS.md, grading/**, results/apply new diffs+meta, the 13-01 baseline files): (a) ASCII -- rg -nP '[^\x00-\x7F]' over the authored artifacts returns nothing; (b) email allowlist-inversion -- assert the ONLY email-shaped token present in the authored artifacts is the approved public gmail (larsbrinknielsen@gmail.com); flag anything else; never write the work email/domain as a search needle; (c) no .oracle/ path or source prose in any committed artifact (git grep over tracked; rg -uu only if checking a gitignored tree); (d) confirm the phase commits' author AND committer email = the approved gmail (git log --format='%ae %ce'). Fix any finding before finalizing. The persisted diffs/meta + grading records in THIS repo remain (they are the tracked record). Commit 13-RESULTS.md + summary.json (specific paths only) with committer = approved gmail.
  </action>
  <verify>
    <automated>rg -nP '[^\x00-\x7F]' .planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md && echo NON_ASCII_FOUND && exit 1 || echo ascii-clean</automated>
  </verify>
  <acceptance_criteria>
    - nx borrowed repo git status --porcelain is empty (reset to origin/23.0.x, cleaned); kata is back on main with a clean status and the throwaway branch deleted; all throwaway worktrees/junctions removed
    - hygiene gate passes: authored artifacts are ASCII-only; the only email-shaped token is the approved gmail; no .oracle/ path or source prose in any committed artifact; phase commit author AND committer email = the approved gmail
    - 13-RESULTS.md + summary.json committed (specific paths only)
  </acceptance_criteria>
  <done>Both borrowed repos restored pristine; the hygiene gate is clean; 13-RESULTS.md + summary.json committed with the approved gmail committer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grading records / model claims -> committed 13-RESULTS.md | non-ASCII must be normalized; no .oracle/ prose may be quoted |
| maintainer identity -> public repo commit | only the approved public gmail may appear in files, messages, and committer identity |
| borrowed-repo state -> post-phase | both repos must return to pristine; no residue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | .oracle/ path or source prose in 13-RESULTS.md / summary.json | mitigate | verdicts are the oracle agent's own words (paraphrase); the hygiene scan (c) asserts no .oracle path/prose in any committed artifact; main context never read .oracle/ |
| T-13-02 | Information Disclosure | maintainer work email/domain in 13-RESULTS.md or commits | mitigate | allowlist-inversion scan asserts only the approved gmail is present; author + committer email = approved gmail (git log check); never write the work email/domain as a needle |
| T-13-03 | Tampering | non-ASCII/mojibake from reused transcripts/oracle output in 13-RESULTS.md | mitigate | normalize -> and straight quotes when quoting; rg -nP '[^\x00-\x7F]' on the authored artifacts is empty |
| T-13-SC | Tampering | npm/pip installs | accept | phase installs NO packages; only in-repo tooling (node for Pass@k, repo test runners) is used |
</threat_model>

<verification>
- 13-RESULTS.md exists with a Book authenticity table + a Correctness table (per-cell with/without + Pass@k), a Caveats section, and an empirical Verdict (parity or signed delta), bias-checked by an unbiased reviewer
- grading/summary.json joins all three grading records per cell/arm with Pass@k via run-e2e.mjs
- nx and kata borrowed repos are restored pristine (nx clean; kata clean on main; throwaway branch deleted); worktrees/junctions removed
- hygiene gate clean: ASCII-only authored artifacts; only the approved gmail present; no .oracle/ path/prose committed; author + committer = approved gmail
</verification>

<success_criteria>
The head-to-head record (13-RESULTS.md) states an honest empirical verdict on both dimensions; both borrowed repos are pristine; the hygiene gate passes; the milestone-terminal measurement is complete.
</success_criteria>

<output>
Create `.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-05-SUMMARY.md` when done.
</output>
