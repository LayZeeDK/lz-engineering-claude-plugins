---
phase: quick-260725-63f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-TRIAGE.md
  - .claude/skills/lz-red-workspace/grade-red.mjs
  - .claude/skills/lz-red-workspace/selfcheck-red.mjs
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/r1-next-test.md
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/EVAL-RESULTS.md
  - .claude/skills/lz-red-workspace/fixtures/canary-rundir/
  - .claude/skills/lz-red-workspace/fixtures/canary-nocollect/
  - .claude/skills/lz-red-workspace/fixtures/canary-modernlib/
autonomous: true
requirements: [F1, F2, F3, F4, F5]

must_haves:
  truths:
    - Each of F1..F5 carries exactly one triage verdict (CONFIRMED / REJECTED / WONTFIX) backed by
      re-verified offline evidence recorded in a committed TRIAGE.md.
    - The D-06 gate produces a verdict (never an exception) for a fabricated runDir graded against
      the real kata toolchain. This holds whatever F1/F2 triage concludes -- if they are CONFIRMED
      the fix makes it true, if they are REJECTED it was already true and the canary proves it.
    - red-grade.json records the kata's own runner AND a real runner_version (not "unknown"),
      proving the target toolchain was visible to the grading worktree.
    - Every CONFIRMED code finding has an offline check that FAILS without its fix and PASSES with
      it. Doc-only findings instead carry a positive content assertion over the rewritten section.
    - The borrowed kata is left git-clean, with its real node_modules intact and no leftover
      worktrees or branches.
    - The full offline battery plus `claude plugin validate .` exit 0; zero metered spend.
  artifacts:
    - .planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-TRIAGE.md
    - .claude/skills/lz-red-workspace/fixtures/canary-rundir/ (meta.json + diff.patch) -- built
      UNCONDITIONALLY; it is the durable structural close to the "the battery grades fixtures, not
      the target" gap, independent of any finding's verdict
    - .claude/skills/lz-red-workspace/fixtures/canary-nocollect/ (only if F2 confirmed)
    - grade-red.mjs / selfcheck-red.mjs / targets.json / RUN-GATE.md / r1-next-test.md edits, each
      gated on its finding's triage verdict
  key_links:
    - selfcheck-red.mjs crux 7 -> grade-red.mjs gradeRun -> kata worktree with a visible node_modules
    - targets.json machine-readable runner selector -> grade-red.mjs runnerName
    - targets.json test_dir -> the selected runner's collection root (they must agree)
    - teardown ordering -- the node_modules link is removed BEFORE `git worktree remove --force`,
      so the kata's real node_modules is never followed into deletion
---

<objective>
Independently audit and triage the five candidate findings F1..F5 in 260725-63f-FINDINGS.md, then
fix only what survives triage, in bisect-safe atomic commits, with an offline anti-regression check
per confirmed finding.

Purpose: the Phase-21 RED apply instrument is BUILD-complete and HALTED at a metered-run gate. If
F1/F2 are real, the D-06 correctness gate throws instead of grading and the metered run burns spend
for nothing. Closing them offline is strictly cheaper than discovering them after the fan-out.

Output: a committed TRIAGE.md verdict table, plus per-finding fixes under
`.claude/skills/lz-red-workspace/` and a zero-spend canary that exercises the gate against the real
kata toolchain.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-FINDINGS.md
@CLAUDE.md
@AGENTS.md
@.claude/skills/lz-red-workspace/grade-red.mjs
@.claude/skills/lz-red-workspace/selfcheck-red.mjs
@.claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json
@.claude/skills/lz-red-workspace/e2e-red-gilded-rose/suite.json
@.claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/r1-next-test.md
@.claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md
</context>

<standing_fences>
These apply to EVERY task below. Violating one fails the task regardless of the code produced.

- ZERO SPEND. No `claude -p`, no eval fan-out, no metered command. All verification is offline.
- SCOPE. Only `.claude/skills/lz-red-workspace/**` and this quick task's own
  `.planning/quick/260725-63f-.../` artifacts may be modified. `plugins/lz-tdd/**` is untouchable.
  No dependency may be added to `plugins/lz-tdd`.
- KATA IS READ-ONLY THIRD-PARTY. `D:/projects/github/emilybache/GildedRose-Refactoring-Kata` is not
  ours. Never write into its working tree. Temporary worktrees live under `os.tmpdir()` and are
  removed in the same task that created them; finish with `git worktree prune`. End state: clean
  `git status --porcelain`, exactly one entry in `git worktree list`, `TypeScript/node_modules`
  still present.
- ASCII ONLY in every file and commit message. No emoji, no Unicode dashes/quotes/ellipses.
- EMAIL HYGIENE by allowlist-inversion. The only email-shaped token permitted in anything you write
  is `larsbrinknielsen@gmail.com`. Never write a forbidden address or domain as a search needle --
  the needle is the leak.
- STAGE BY NAME. Never `git add .` / `-A` / `-u`.
- SEARCH with `git grep` or `rg`. Never `grep`.
- NO SUBAGENTS. You have no Agent/Task tool. If a gate needs an agent, report it for the
  orchestrator instead of attempting it.
- NO NEW DEPENDENCY without asking. If a fix appears to require adding a devDependency to the
  workspace, STOP and report it as a decision point rather than installing it. The intended design
  uses the kata's own toolchain, so this should not arise.
</standing_fences>

<commit_boundaries>
Emit only the commits whose finding triage CONFIRMED. A REJECTED or WONTFIX finding produces no fix
commit -- its verdict lives in TRIAGE.md and that is the whole deliverable for it.

- CM-1 (always) `docs(quick): triage RED instrument findings F1..F5`
  Files: TRIAGE.md only.
- CM-2 (ALWAYS) `fix(lz-red-workspace): grade a fabricated runDir against the real kata toolchain`
  Files: fixtures/canary-rundir/*, selfcheck-red.mjs, plus grade-red.mjs and targets.json ONLY if
  F1 and/or F2 confirmed.
  The canary is UNCONDITIONAL -- it is the durable structural close to the gap that let F1 and F2
  through (the battery grades workspace fixtures, never the target's own toolchain), and it is
  worth having whether or not those two findings survive triage. If both are REJECTED this commit
  is canary-only and passes against the untouched gate; if either is CONFIRMED its fix ships in the
  same commit. F1 and F2 stay together on purpose: the canary is the check for both and cannot pass
  until the gate can both see the toolchain and pick a runner that collects the test. Splitting them
  would commit a red battery, which is not bisect-safe. targets.json rides along because grade-red
  reads the new selector field from it -- checker and checked change together.
- CM-3 (if F2 confirmed) `fix(lz-red-workspace): classify a recognized no-tests miss instead of throwing`
  Files: grade-red.mjs, fixtures/canary-nocollect/*, selfcheck-red.mjs.
  Separate from CM-2 because sub-fix (c) has its own fixture and its own revert-and-reassert
  demonstration; folding it into CM-2 would leave (c) unproven (W3).
- CM-4 (if F4 confirmed) `fix(lz-red-workspace): pin the differential typecheck lib`
  Files: grade-red.mjs, plus the fixture/assertion that proves the false compile_error is gone.
- CM-5 (if F3 confirmed as a prompt fix) `fix(lz-red-workspace): drop the false premise from the RED prompt`
  Files: prompts/r1-next-test.md, plus its guard in selfcheck-red.mjs.
- CM-6 (if F5 CONFIRMED **or** F3 triaged WONTFIX) `docs(lz-red-workspace): correct the RUN-GATE canary and toolchain steps`
  Files: RUN-GATE.md, and EVAL-RESULTS.md when it carries an F3 caveat.
  The OR gate is deliberate: an F3 WONTFIX verdict obliges a documented caveat, and that edit needs
  a defined commit boundary even when F5 itself is rejected. If both triggers fire, they still ship
  as one commit -- both are RUN-GATE.md prose corrections landing together.

Every commit must leave the offline battery exiting 0. Verify before each commit, not just at the end.
</commit_boundaries>

<tasks>

<task type="auto">
  <name>Task 1: Audit and triage F1..F5 independently, offline</name>
  <files>.planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-TRIAGE.md</files>
  <action>
Re-verify each candidate finding against the actual source and the actual environment. Do NOT trust
the findings doc; it is one reviewer's candidate list and F3 is explicitly flagged as a judgment
call. Reach your own verdict per finding: CONFIRMED, REJECTED (with the evidence that refutes it),
or WONTFIX (real but deliberately not fixed, with the reason).

F1 -- no node_modules in the grading worktree. Confirm by source that gradeRun creates a detached
worktree under os.tmpdir() and runs both the differential tsc and the target runner inside it with
no install step anywhere in the file. Then reproduce: create a throwaway detached worktree of the
kata under the session scratchpad or os.tmpdir(), observe whether `TypeScript/node_modules` exists
there, remove the worktree with `--force`, run `git worktree prune`, and confirm the kata is back to
one worktree and a clean status. Reason about `targetTscBin`: if neither armCwd nor the worktree has
a local typescript it returns null and `targetTscErrors` falls through to `npx tsc`, which is the
global toolchain, not the kata's pinned 4.4.4 -- contradicting the function's own comment. Record
whether that fall-through is reachable in the real gate path.

F2 -- runner hard-lock plus a throwing miss. Confirm by source that `preferJest` is a case-insensitive
substring test over a PROSE field, so any prose mentioning the runner name makes it truthy and
`runnerName` is always that runner. Cross-check against `test_dir` in targets.json and the kata's
own collection config (jest roots vs the vitest include glob) to establish whether the configured
runner can even see the configured test_dir. Reproduce empirically in the kata's `TypeScript/` dir,
READ-ONLY: run the configured runner command against a spec inside the non-collected dir and against
one inside the collected dir, capturing exit code, stdout byte count, and the stderr message. Pass
`--coverage=false` so the run does not write into the kata's coverage output. Then confirm by source
that gradeRun feeds ONLY stdout to parseRunnerJson, so an empty stdout throws rather than reaching
the no_tests class.

F3 -- the prompt's premise. Read the prompt and both placeholder specs the kata ships, and establish
from the F2 positive control whether the existing tests actually pass on current code. Then make the
judgment call and justify it. The premise is byte-identical across all three arms, so it is symmetric
noise rather than arm bias; weigh that against the risk that it invites the model to repair the
placeholder, which can trip drove_to_green and corrupt the correctness gate. If you decide to fix it,
the replacement must stay short and human, must not name the missing behavior or any smell, and must
make no claim about the current pass/fail state of the existing tests. If you decide not to fix it,
say WONTFIX and specify exactly where the caveat gets documented.

F4 -- false compile_error from a modern-method test under the kata's es5 target. Confirm the kata
tsconfig's target and the absence of an explicit lib. Demonstrate the mechanism offline without
writing into the kata: put a probe .ts file in the session scratchpad that uses a lib-gated modern
array or object method, and run the kata's own tsc binary by absolute path with the same
`--noEmit --strict` the gate uses plus the kata's target setting, and observe whether a lib-missing
error appears. Separately record that the CLI strict override re-enables a check the kata tsconfig
disables, that this inflates the BASELINE set, and that the differential therefore absorbs it -- so
it is a comment-worthy surprise, not a defect.

F5 -- the RUN-GATE canary. Confirm what Step 2 currently requires and what it costs. Establish from
gradeRun's source which inputs a fabricated runDir would actually need. Decide whether a fabricated
runDir canary is strictly cheaper and strictly broader than the metered one, and note that it still
needs the kata present, so it inherits crux 3's SKIP-if-absent discipline. Also assess the F1
knock-on on RUN-GATE Step 3a: the operator's apply checkout is created the same worktree way and is
likewise toolchain-less, which would suppress the fail-for-the-right-reason behavior in all three
arms. Fold that into F5's verdict or raise it as F6, your call, but do not drop it silently.

Write the verdicts to TRIAGE.md. Open with a one-line summary of the audit method and the
environment facts you re-verified. Then a table whose rows begin with the finding id, a single
verdict token, and a one-line rationale, in the exact shape
`| F1 | CONFIRMED | ...` so it is machine-countable. Follow the table with a short per-finding
section carrying the evidence you actually observed -- commands run and what they returned, not
restatements of the findings doc -- and, for each CONFIRMED finding, the fix you will implement and
the offline check that will prove it. State explicitly which findings you REJECTED and why the
original evidence does not hold.

Commit CM-1 with TRIAGE.md staged by name.
  </action>
  <verify>
    <automated>test "$(rg -c '^\| F[1-5] +\| (CONFIRMED|REJECTED|WONTFIX) ' .planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-TRIAGE.md || echo 0)" -eq 5 && test "$(rg -o '^\| F[1-5] +\| (CONFIRMED|REJECTED|WONTFIX) ' .planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-TRIAGE.md | rg -o 'F[1-5]' | sort -u | wc -l)" -eq 5 && test -z "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git --work-tree=D:/projects/github/emilybache/GildedRose-Refactoring-Kata status --porcelain)" && test "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git worktree list | wc -l)" -eq 1 && test -d D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript/node_modules</automated>
  </verify>
  <done>
TRIAGE.md is committed and contains exactly five verdict rows, one per finding, each with a single
verdict token and observed evidence. The kata is git-clean, has one worktree, and still has its
node_modules. No metered command ran.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the zero-spend canary and fix the confirmed toolchain and runner-selection defects</name>
  <files>.claude/skills/lz-red-workspace/grade-red.mjs, .claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json, .claude/skills/lz-red-workspace/fixtures/canary-rundir/, .claude/skills/lz-red-workspace/fixtures/canary-nocollect/, .claude/skills/lz-red-workspace/selfcheck-red.mjs</files>
  <action>
Build the canary UNCONDITIONALLY. Implement the F1/F2 code fixes only if Task 1 CONFIRMED them.
Keep every change minimal -- this is throwaway eval tooling, not production code. Do not refactor
beyond what a confirmed finding requires.

F1 fix, if confirmed. After the worktree is created in gradeRun and before the baseline typecheck,
make the target's real node_modules visible to the directory that actually runs the commands
(armCwd, the repo subdir -- not the worktree root, unless they are the same). Use a Windows-safe
directory link: `fs.symlinkSync(target, link, 'junction')`. Fail closed if the source node_modules
is absent: throw an operator-actionable error naming the directory and telling them to install the
kata's dependencies there first. Do not silently fall through to the global toolchain.

Teardown safety is load-bearing and is NOT a place to be minimal. Remove the link BEFORE
`git worktree remove --force`, inside the same finally block, so no recursive delete can follow the
junction into the kata's real node_modules. Removing a directory junction on Windows unlinks the
link itself, not its target -- but the ordering guarantee is what makes that true regardless of who
does the deleting. Make the link removal itself failure-tolerant so a link that is already gone does
not mask the real error, and make sure teardown still removes the worktree if link removal throws.

F2 fix, if confirmed. Three parts:
(a) Stop sniffing prose. Add an explicit machine-readable runner selector to the target's runner
block in targets.json and select on it in grade-red.mjs. Keep the existing prose field as
documentation. Fail closed with a clear error if the selector names a runner that has no command.
(b) Make the test location deterministic and coherent with the selected runner. The selected runner
must be one whose collection config actually includes the target's `test_dir`. Pick the pairing you
justified in triage and update `test_dir` and `test_dir_note` to match, so the runner and the test
dir cannot disagree. The Jest-compatible JSON shape is what classify() already reads and the kata's
jest 29 is the version whose shape is stable, so jest paired with the jest-collected dir is the
default recommendation -- deviate only with a written reason in the commit body. While you are in
the runner command, add the flag that disables coverage collection: the kata's config turns it on,
and coverage output on stdout is exactly the interleaving that parseRunnerJson's brace-extraction
fallback has to fight.
(c) Feed stderr into the no-tests disambiguation. When the runner's stdout will not parse, check
both streams against the existing no-tests pattern; on a RECOGNIZED no-tests signal, synthesize the
minimal runner report that drives classify() to the no_tests verdict, carrying the observed message
so the excerpt stays honest. Anything else still throws. The fail-closed contract for genuinely
garbled output is preserved -- only a recognized signal becomes a verdict.

Canary -- build this UNCONDITIONALLY, whatever F1 and F2 triaged to. It closes the structural gap
that let both findings through: every existing selfcheck grades workspace fixtures with the
workspace's own pinned toolchain, so the target's toolchain is never exercised and the gate's real
failure mode is invisible. That gap outlives any single finding, and the next target repo would
reintroduce the same class of bug. If both findings are REJECTED the canary simply passes against
the untouched gate and the commit is canary-only -- no red intermediate state either way.

Add a committed fabricated runDir fixture under
`fixtures/canary-rundir/`: a `meta.json` carrying arm, target, prompt_id, run_idx and a
changed_files array, and a hand-written `diff.patch` that adds one spec file at the pinned test dir.
Paths in both must be ROOT-relative the way a real capture is, since git apply runs at the worktree
root and gradeRun strips the repo-subdir prefix itself. The spec asserts the kata's documented
missing behavior, so it compiles and fails on an assertion against current code.

Wire it as a new crux in selfcheck-red.mjs, after the existing classifier crux. The crux copies the
fixture to a fresh os.tmpdir() runDir (never grading the committed fixture in place, since gradeRun
writes red-grade.json into the runDir), calls gradeRun against the RED suite dir, and asserts:
the call did not throw; the verdict is genuinely_red with pass true; the recorded runner is the
configured one; and runner_version is a real version rather than the unknown sentinel -- that last
assertion is what specifically pins F1. Then assert the kata's real node_modules still exists, the
kata status is clean, and no grading worktree leaked. Mirror crux 3's SKIP-if-absent discipline: if
the kata repo or its node_modules is not on disk, print a SKIP line and return rather than failing
the battery. Remove the tmp runDir when done.

Second fixture for sub-fix (c), if F2 was confirmed. The canary above is engineered to be
genuinely_red, so it exercises F1 and F2(a)/(b) but never reaches the stderr disambiguation branch
-- reverting (c) alone would leave the canary green, which violates this plan's own
anti-regression rule. Add `fixtures/canary-nocollect/`: the same fabricated runDir shape, but with
the produced spec placed OUTSIDE the selected runner's collection root, so the runner emits nothing
usable on stdout and a recognized no-tests signal on stderr. Extend the crux to grade it too and
assert the verdict is no_tests with pass false, and specifically that the call did not throw.
Reverting (c) must make exactly this assertion fail.

Anti-regression demonstration, per commit boundary: with the fix hunk temporarily reverted (stash
it -- never commit a red state), the new assertion must FAIL; with the fix applied it must PASS.
Do this per sub-fix, not once for the whole task, so each confirmed finding has its own evidence.
Record the observed pre-fix failure text in that commit's body. Then restore, re-run the battery,
and commit.

Commit CM-2, and CM-3 if F2 was confirmed, staging files by name. Each commit must leave both
`grade-red.mjs --selfcheck` and `selfcheck-red.mjs` exiting 0.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/grade-red.mjs --selfcheck && node .claude/skills/lz-red-workspace/selfcheck-red.mjs && node .claude/skills/lz-red-workspace/tabulate-mechanical-red.mjs --selfcheck && test -d D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript/node_modules && test -z "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git --work-tree=D:/projects/github/emilybache/GildedRose-Refactoring-Kata status --porcelain)" && test "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git worktree list | wc -l)" -eq 1</automated>
  </verify>
  <done>
The canary crux exists and passes, grading a fabricated runDir against the kata's own toolchain.
Every CONFIRMED F1/F2 defect is fixed and covered by an assertion that was observed to fail with
that fix reverted and pass with it applied. The battery exits 0. The kata still has its
node_modules, is git-clean, and has exactly one worktree. No dependency was added anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix the confirmed differential-typecheck lib gap</name>
  <files>.claude/skills/lz-red-workspace/grade-red.mjs, .claude/skills/lz-red-workspace/fixtures/canary-modernlib/, .claude/skills/lz-red-workspace/selfcheck-red.mjs</files>
  <action>
Skip this task entirely if Task 1 triaged F4 as REJECTED or WONTFIX -- say so and move on. It is a
separate task from the F1/F2 work because it is logically independent, already has its own commit
boundary, and keeping it out of Task 2 keeps that node's context load reasonable.

Pin an explicit modern lib on the differential typecheck args so a produced test using a lib-gated
modern array or object method does not manufacture a NEW error the differential cannot absorb. The
same args are used for both the baseline and the with-test run, so the differential stays valid.

Leave a comment saying WHY the lib pin exists and, separately, why the CLI strict override stays
despite disagreeing with the kata tsconfig -- it inflates the BASELINE set, which the differential
absorbs, so it is a surprise rather than a defect. The current strict flag looks deliberate and the
next reader will otherwise "fix" it.

Prove it with a third fabricated runDir, `fixtures/canary-modernlib/`, whose spec uses a lib-gated
modern method and otherwise mirrors the genuinely-red canary. Extend the crux to grade it and
assert the verdict is genuinely_red and specifically NOT compile_error. Demonstrate the
anti-regression property the same way: revert the lib pin, observe this assertion fail, restore,
re-run, record the pre-fix failure text in the commit body.

Commit CM-4, staging files by name.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/grade-red.mjs --selfcheck && node .claude/skills/lz-red-workspace/selfcheck-red.mjs && test -d D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript/node_modules && test -z "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git --work-tree=D:/projects/github/emilybache/GildedRose-Refactoring-Kata status --porcelain)" && test "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git worktree list | wc -l)" -eq 1</automated>
  </verify>
  <done>
If F4 was CONFIRMED: the lib pin is in place with its comment, and a fixture using a lib-gated
modern method grades genuinely_red where it previously graded compile_error, demonstrated by
revert-and-reassert. If F4 was not confirmed: no change, and the reason is recorded in TRIAGE.md.
Battery exits 0, kata pristine.
  </done>
</task>

<task type="auto">
  <name>Task 4: Close the confirmed prompt and run-gate findings, then prove the whole battery</name>
  <files>.claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/r1-next-test.md, .claude/skills/lz-red-workspace/e2e-red-gilded-rose/RUN-GATE.md, .claude/skills/lz-red-workspace/e2e-red-gilded-rose/EVAL-RESULTS.md, .claude/skills/lz-red-workspace/selfcheck-red.mjs</files>
  <action>
Implement only what Task 1 CONFIRMED for F3 and F5.

F3, if the triage verdict was a prompt fix. Edit the prompt so it makes no claim about the current
pass/fail state of the existing tests. Simply removing the claim is the smaller and safer change
than replacing it with an opposite claim, which would leak information about the target. Preserve
every property the 21-01 subagent review established: short, human, non-leading, naming the target
only by path, and naming no expected behavior gap or smell. The prompt is a single shared file, so
byte-identity across arms is structural -- confirm crux 2's parity assertion still passes rather
than assuming it. Add a guard so the premise cannot come back: extend the prompt-parity crux with an
assertion that the prompt body carries no test-state claim, implemented over a small set of
state-claim tokens defined in the checker. If the triage verdict was WONTFIX, skip the prompt edit
entirely and instead write the caveat into RUN-GATE.md and EVAL-RESULTS.md as a named known
limitation of the correctness gate, so a reader interpreting a drove_to_green run knows the prompt
may have provoked it.

F5, if confirmed. Rewrite RUN-GATE.md Step 2 so the zero-spend canary is the REQUIRED pre-fan-out
gate: name the exact offline command, state what it proves (the gate produces a real verdict against
the kata's own toolchain, with the runner and its version recorded), and demote the metered
single-run canary to an optional extra rather than the primary path. Delete the now-obsolete
offline-alternative bullet if the fix made it moot, and say plainly that it is moot. Correct the
narrow framing of the residual risk so the next reader is not told the only remaining hazard is
reporter-shape drift.

Also close the F1 knock-on in Step 3a if triage assigned it here. The operator's apply checkout is
created by the same worktree mechanism and is therefore toolchain-less, which means the model under
test cannot run the test it writes and the fail-for-the-right-reason behavior is suppressed across
all three arms. Add the explicit install-or-link step for that checkout, positioned before the drive
command, and say why it matters for the measurement rather than just listing a command.

Anti-regression for these doc-only findings. F5 (and an F3 WONTFIX caveat) are prose rewrites with
no natural revert-and-reassert mechanism, so they are exempt from the code-finding fail-then-pass
rule. They instead need a positive content assertion recorded in the commit body: an `rg` invocation
over the rewritten section showing the new framing is present -- that the canary is a REQUIRED
pre-fan-out gate naming the offline command, and that the metered run is demoted to optional. Run
that same `rg` against the pre-edit file (from git, not by stashing) and record that it returned
nothing, so the assertion is shown to discriminate rather than merely to pass. An F3 prompt fix is a
code-adjacent finding and keeps the full fail-then-pass rule via its checker guard.

Then prove the whole thing. Run the full offline battery: grade-red selfcheck, tabulate-mechanical-red
selfcheck, merge-judge selfcheck, selfcheck-red, check-evals, and `claude plugin validate .`. All
must exit 0.

Run the hygiene gates over every file this task set changed, by name: a non-ASCII character scan must
return nothing, and an email-shaped-token scan must return nothing other than the approved public
gmail. Use allowlist-inversion -- scan for the shape and inspect what comes back. Do not encode any
forbidden value as a needle.

Finish with the cleanliness proof: the kata is git-clean, has exactly one worktree and no stray
branches, and still has its node_modules; the plugin repo has no unintended modifications outside
the two permitted trees; no grading worktree survives under the temp dir.

Commit CM-5 and CM-6 as their triggers dictate, staging by name. Remember CM-6 fires on F5 CONFIRMED
**or** F3 WONTFIX -- an F3 WONTFIX caveat has a defined commit boundary even when F5 is rejected, so
it must never land as an ad hoc commit or a stray uncommitted edit.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/grade-red.mjs --selfcheck && node .claude/skills/lz-red-workspace/tabulate-mechanical-red.mjs --selfcheck && node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck && node .claude/skills/lz-red-workspace/selfcheck-red.mjs && node .claude/skills/lz-red-workspace/check-evals.mjs && claude plugin validate . && test -z "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git --work-tree=D:/projects/github/emilybache/GildedRose-Refactoring-Kata status --porcelain)" && test "$(git --git-dir=D:/projects/github/emilybache/GildedRose-Refactoring-Kata/.git worktree list | wc -l)" -eq 1 && test -d D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript/node_modules && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>
All six offline battery commands and `claude plugin validate .` exit 0. Every changed file is
ASCII-only and free of any email-shaped token other than the approved public gmail. The kata is
pristine. The plugin repo working tree is clean, with every change committed in its own bisect-safe
commit and nothing staged outside `.claude/skills/lz-red-workspace/` and this quick task's planning
directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grading worktree -> the borrowed kata working tree | A temp worktree and a directory link both point back into a third-party repo we do not own. Deletion and write paths cross here. |
| captured runDir -> gradeRun | meta.json and diff.patch are attacker-shaped inputs relative to the grader; a fabricated fixture exercises the same path a real capture does. |
| authored artifacts -> the public repo | TRIAGE.md and every edited file are committed to a public repository under the maintainer's identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63f-01 | Denial of Service | gradeRun teardown following a directory junction into the kata's real node_modules | critical | mitigate | Remove the link BEFORE `git worktree remove --force`, in the same finally block; assert in the canary crux that the kata's node_modules still exists after the run (Task 2). |
| T-63f-02 | Tampering | temporary worktrees and probe files left in the borrowed third-party repo | high | mitigate | Every temp worktree lives under os.tmpdir(), is removed in the task that created it, followed by `git worktree prune`; each task's automated verify asserts a clean kata status and exactly one worktree entry. |
| T-63f-03 | Information Disclosure | maintainer email or work domain leaking into TRIAGE.md, code comments, or commit messages | high | mitigate | Allowlist-inversion scan over every changed file in Task 3; no forbidden value is ever written as a search needle. |
| T-63f-04 | Spoofing | the D-06 gate silently grading against the wrong (global) toolchain and reporting a verdict as if it came from the target | high | mitigate | Fail closed when the source node_modules is absent instead of falling through to the global resolver; the canary asserts runner_version is a real version, not the unknown sentinel (Task 2). |
| T-63f-05 | Tampering | a recognized-signal fallback widening into a general "swallow the error" path that manufactures verdicts from garbage | medium | mitigate | Only a RECOGNIZED no-tests signal becomes a verdict; every other unparseable output still throws, and grade-red --selfcheck's existing fail-closed assertions stay green (Task 2). |
| T-63f-06 | Elevation of Privilege | unplanned metered spend crossing the still-closed eval-run-approval-gate | high | mitigate | No task runs `claude -p` or any metered command; the canary is fabricated-input only and the RUN-GATE HALT text is preserved (all tasks). |
| T-63f-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install is planned. If a fix appears to need one, the executor STOPS and reports it as a decision point rather than installing -- no legitimacy audit exists for this task and none may be self-approved. |
</threat_model>

<verification>
Whole-task definition of done, all offline and zero spend:

1. `node .claude/skills/lz-red-workspace/grade-red.mjs --selfcheck` exits 0
2. `node .claude/skills/lz-red-workspace/tabulate-mechanical-red.mjs --selfcheck` exits 0
3. `node .claude/skills/lz-red-workspace/merge-judge.mjs --selfcheck` exits 0
4. `node .claude/skills/lz-red-workspace/selfcheck-red.mjs` exits 0
5. `node .claude/skills/lz-red-workspace/check-evals.mjs` exits 0
6. `claude plugin validate .` exits 0
7. Each CONFIRMED CODE finding has a check that was OBSERVED to fail with its fix reverted and pass
   with it applied, with the pre-fix failure text recorded in that fix's commit body. Doc-only
   findings (F5, and an F3 WONTFIX caveat) are EXEMPT from revert-and-reassert -- prose has no such
   mechanism. They instead carry a positive `rg` content assertion over the rewritten section,
   recorded in the commit body together with the same `rg` returning nothing against the pre-edit
   file from git, which shows the assertion discriminates
7b. The canary crux exists and passes regardless of any verdict -- it is unconditional
8. The kata is git-clean, one worktree, no stray branches, node_modules intact
9. The plugin repo working tree is clean; every change is committed; nothing outside
   `.claude/skills/lz-red-workspace/` and `.planning/quick/260725-63f-.../` was touched
</verification>

<success_criteria>
- TRIAGE.md is committed with exactly five verdict rows, each backed by evidence the executor
  observed rather than restated.
- The fix set is exactly the CONFIRMED subset, plus the unconditional canary. No fix commit exists
  for a REJECTED finding, no confirmed finding is silently unaddressed, and an F3 WONTFIX verdict
  still produces its documented caveat under CM-6.
- Commits follow the CM-1..CM-6 boundaries; every commit leaves the offline battery exiting 0.
- The full battery plus `claude plugin validate .` exit 0.
- Kata and plugin repo both end clean; zero metered spend.
</success_criteria>

<output>
Create `.planning/quick/260725-63f-audit-and-triage-all-findings-then-addre/260725-63f-SUMMARY.md`
when done. Include the triage verdict table, the commits emitted with their SHAs, the observed
fail-without-fix evidence per confirmed finding, and any finding deferred with its reason.
</output>
