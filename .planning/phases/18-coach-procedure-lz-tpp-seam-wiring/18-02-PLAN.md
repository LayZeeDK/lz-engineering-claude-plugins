---
phase: 18-coach-procedure-lz-tpp-seam-wiring
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
autonomous: true
requirements: [LAW-01, LAW-02, SEAM-01, SEAM-02]
must_haves:
  truths:
    - "D-04: the owned distilled Three-Laws prose lands in the three-laws-and-test-selection.md leaf (not SKILL.md); SKILL.md later carries only the compact spine and points here."
    - "D-05: the Three-Laws surface is authored at the TARGET tier `Owned; oracle-verified` against Clean Code Ch. 9; the orchestrator oracle-reviewer gate at finalize DECIDES the tier per Law statement (may fall back to no-oracle)."
    - "D-12: the owned surface is authored BLIND (clean-room); main context never reads .oracle/ prose; the no-verbatim scan is the deterministic backstop."
    - "D-03: the classify-first framing (red vs green vs refactor before any code) lands in the leaf; Law 3 as the lz-tpp handoff is lz-red orchestration (no-oracle)."
    - "D-14: only the Phase-18 slice is filled; the SEL slice and every Phase-16/17 row stay byte-stable; no stale deferral marker remains."
    - "LAW-01 / LAW-02 / SEAM-01 / SEAM-02 backing rows are added to principle-backing.md in the exact three-column pipe shape without tripping the D-05 honesty gate."
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
    - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  key_links:
    - "principle-backing LAW rows link to three-laws-and-test-selection.md (owned; oracle-verified) and stay tier-consistent with the leaf prose"
    - "principle-backing SEAM rows tag lz-red orchestration (no-oracle)"
---

<objective>
Fill the ONE owned surface Phase 18 introduces -- the Three Laws of TDD spine + classify-first framing
in the `three-laws-and-test-selection.md` leaf -- and the LAW / SEAM backing rows in
`principle-backing.md`. Both are authored BLIND (clean-room DST-04); the owned surface is gated by the
orchestrator-driven oracle-reviewer at finalize (18-06), which decides the tier per Law statement.

The Three Laws mapping is lz-red-specific: Law 1 gates ENTRY (no production code before a failing
test), Law 2 sizes the TEST (only enough to fail; a not-yet-defined symbol counts), Law 3 is the
HANDOFF (making it pass is lz-tpp's job). Laws 1-2 are Robert C. Martin-owned (Clean Code Ch. 9);
Law-3-as-lz-tpp-handoff is the lz-red orchestration reframe (no-oracle). LAW-02's "only enough to
fail; not-compiling counts" is the SAME thread as the existing owned "Take one small step" row, so the
spine is gateable against the clean-room source.

Purpose: give the coach procedure (18-05) a real owned leaf to point at, and back the LAW / SEAM
recommendations in the traceability map.
Output: the filled Three-Laws section (heading de-marked), the rewritten head blockquote, and the LAW /
SEAM backing rows.

## Decisions implemented (D-NN)

- **D-04:** owned Three-Laws prose in the leaf; SKILL.md carries only the compact spine (18-05).
- **D-05:** author at the target `Owned; oracle-verified` tier against Clean Code Ch. 9; tier is gate-decided at 18-06 (17.1 gate-decides pattern; may fall back to no-oracle high-confidence core).
- **D-12:** author BLIND; never read .oracle/; the no-verbatim scan is the only deterministic backstop.
- **D-03:** classify-first framing (red / green / refactor before any code) in the leaf; Law 3 handoff is no-oracle orchestration.
- **D-14:** fill only the Phase-18 slice; keep the SEL slice and all Phase-16/17 principle-backing rows byte-stable; leave no stale deferral marker.

## Artifacts produced

- `three-laws-and-test-selection.md`: the filled "## The Three Laws spine and classify-first" section (heading loses its parenthetical marker); a rewritten head blockquote declaring the now-present owned Three-Laws surface.
- `principle-backing.md`: filled "## Three-Laws spine and lz-tpp seam backing" rows -- LAW-01 + LAW-02 (Clean Code Ch. 9, `Owned; oracle-verified`), SEAM-01 + SEAM-02 (lz-red orchestration, `Unowned; high-confidence core only (no-oracle)`); rewritten head blockquote.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-CONTEXT.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
@plugins/lz-tdd/skills/lz-red/references/principle-backing.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill the owned Three Laws spine + classify-first section (blind, clean-room)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md</files>
  <read_first>plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md</read_first>
  <action>
Fill the "## The Three Laws spine and classify-first" section (currently the insertion-point marker at
lines 91-95) with distilled own-words prose. Drop the parenthetical marker from the heading so it
reads "## The Three Laws spine and classify-first". Author BLIND -- do NOT open or read anything under
`.oracle/`; write in original words with no verbatim source prose or code (DST-04).

Content:
- The Three Laws of TDD as the RED spine. Law 1 gates entry: write no production code until a failing
  unit test demands it. Law 2 sizes the test: write only as much of a test as is enough to fail, and a
  not-yet-defined symbol (code that does not compile) counts as that failure. Law 3 is the handoff:
  write only enough production code to pass -- which is lz-tpp's job, the green step, not this skill's.
  Tag Law 1 and Law 2 statements owned in the same inline style the file already uses for the "Take one
  small step" row: "(Robert C. Martin, owned; oracle-verified)". Frame Law 3 as the lz-red-to-lz-tpp
  handoff and tag it lz-red orchestration (no-oracle) -- it is the reframe, not RCM's words.
- The classify-first framing: sort every request into red, green, or refactor BEFORE any code. A
  failing test that must be made to pass hands off to lz-tpp (green) and stops; already-passing code to
  clean up hands off to lz-refactor (refactor); otherwise choosing and writing the next failing test is
  lz-red. Keep this consistent with the existing SKILL.md "RED vs the green step" framing; do not
  restate the SEL rows above it.

Rewrite the head blockquote sentence (currently lines 19-20) that says the Three Laws spine / classify-first
seam are "deferred to Phase 18 and left as a marker below" so it instead declares the now-present owned
Three-Laws surface (owned; oracle-verified against the clean-room source; Law 3 handoff no-oracle). The
whole file must contain no remaining phase-name-plus-number deferral marker afterward. Keep the SEL
slice (lines 22-89) and the Sources section byte-stable except for any needed one-line Sources note
consistent with the existing Clean Code Ch. 9 entry.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "three-laws-and-test-selection.md"</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs reports PASS for three-laws-and-test-selection.md on: the Three Laws spine topic, the classify-first topic, every retained SEL topic, the ts-fence requirement, and the no-stale-marker (`absent`) guard.
    - The section heading no longer carries a parenthetical phase marker; the head blockquote declares the present owned surface.
    - The SEL slice content (running test list, one small step, degenerate case, triangulation, the sumOf fence) is byte-unchanged.
    - Law 1 / Law 2 carry the owned inline tier tag; Law 3 is tagged lz-red orchestration (no-oracle).
  </acceptance_criteria>
  <done>The owned Three-Laws spine + classify-first section is filled blind, the marker is gone, the SEL slice is intact, and the checker passes this file's topics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the LAW / SEAM backing rows to principle-backing.md</name>
  <files>plugins/lz-tdd/skills/lz-red/references/principle-backing.md</files>
  <read_first>plugins/lz-tdd/skills/lz-red/references/principle-backing.md</read_first>
  <action>
Fill the "## Three-Laws spine and lz-tpp seam backing" section (insertion-point marker at lines 56-60);
drop the parenthetical marker from the heading. Add backing rows in the EXACT three-column pipe shape
the D-05 parser depends on (`| [recommendation](doc.md) | Source | Access tier |`), matching the "Grow
the test only enough to fail" row at line 31 as the tier model:
- LAW-01 row: recommendation "Three Laws of TDD spine" linking three-laws-and-test-selection.md; Source
  "Robert C. Martin, Clean Code Ch. 9"; tier "Owned; oracle-verified against the clean-room source."
- LAW-02 row: recommendation "Fail for the right reason" linking vitest-typescript-mechanics.md (or
  test-structure-and-assertions.md for the F.I.R.S.T. baseline); Source "Robert C. Martin, Clean Code
  Ch. 9"; tier "Owned; oracle-verified against the clean-room source."
- SEAM-01 row: recommendation "Classify-first and the forward lz-tpp handoff" linking
  three-laws-and-test-selection.md; Source "lz-red orchestration"; tier "Unowned; high-confidence core
  only (no-oracle)."
- SEAM-02 row: recommendation "Reverse lz-tpp -> lz-red pointer" linking the lz-tpp skill; Source
  "lz-red orchestration"; tier "Unowned; high-confidence core only (no-oracle)."

Use the canonical tier strings verbatim ("Owned; oracle-verified against the clean-room source." and
"Unowned; high-confidence core only (no-oracle).") -- the checker and honesty gate key on
`/oracle-verified/i` and `/no-oracle/i`. NEVER tag a row Owned while citing Test-Driven Development by
Example (the D-05 honesty gate trips). Rewrite the head blockquote (line 13) to drop the phase-name
deferral sentence. Keep the existing rows (lines 27-54) byte-stable.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "principle-backing.md|honesty gate"</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs reports PASS for principle-backing.md on: the Three Laws backing-row topic, the lz-tpp seam backing-row topic, the retained tier topics, and the no-stale-marker (`absent`) guard.
    - The D-05 honesty gate reports PASS (no Owned row cites Test-Driven Development by Example).
    - The four new rows are in the exact `| [rec](doc.md) | Source | tier |` shape with the canonical tier strings.
    - Every existing row (lines 27-54) is byte-unchanged.
  </acceptance_criteria>
  <done>The LAW-01/LAW-02 (owned) and SEAM-01/SEAM-02 (no-oracle) rows are added in-shape, the marker is gone, the honesty gate holds, and the prior rows are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .oracle/ book prose -> distilled Markdown | Clean Code Ch. 9 text must not cross into the shipped leaf verbatim |
| authored content -> committed public repo | maintainer work-email/domain and non-ASCII must not enter committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Info disclosure (IP) | owned Three-Laws distillation in three-laws-and-test-selection.md | medium | mitigate | Clean-room blind own-words authoring + orchestrator oracle-reviewer gate (18-06) + check-hygiene no-verbatim scan over the lz-red tree. |
| T-18-05 | Tampering | prompt/instruction injection via .oracle/ source prose | medium | mitigate | Main context never reads .oracle/; the owned distillation is authored blind and oracle-reviewer-gated. |
| T-18-02 | Info disclosure | authored Markdown | medium | mitigate | check-hygiene allowlist-inversion work-email scan (approved gmail only). |
| T-18-03 | Tampering (integrity) | authored Markdown | low | mitigate | check-hygiene ASCII scan. |

No high-severity runtime threats: the leaf is Markdown that executes no code. ASVS level 1; block on
`high` (none exist).
</threat_model>

<verification>
- check-red-references.mjs three-laws-and-test-selection.md + principle-backing.md rows PASS; D-05 honesty gate PASS.
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 (the sumOf fence still compiles; no new fence added here).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- The owned Three-Laws surface is queued for the orchestrator oracle-reviewer gate at 18-06 (NOT run here).
</verification>

<success_criteria>
The owned Three-Laws spine + classify-first is authored blind in the leaf, the LAW / SEAM backing rows
are in place at the target tier, no stale deferral marker remains in either file, the SEL / Phase-16/17
content is byte-stable, and the D-05 honesty gate holds.
</success_criteria>

<output>
Create `.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-02-SUMMARY.md` when done.
</output>
