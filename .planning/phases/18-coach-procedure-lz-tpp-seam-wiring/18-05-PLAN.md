---
phase: 18-coach-procedure-lz-tpp-seam-wiring
plan: 05
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - plugins/lz-tdd/skills/lz-red/SKILL.md
autonomous: true
requirements: [LAW-01, LAW-02, RTR-02, SEAM-01, VIT-02]
must_haves:
  truths:
    - "D-01 / D-02: the coach procedure is authored INLINE in SKILL.md (not a new reference file) as a compact numbered decision tree following the ROADMAP SC1-3 step order."
    - "D-03 / SEAM-01: step 1 classifies RED / GREEN / REFACTOR before any test; step 6 is the forward lz-red -> lz-tpp handoff (Law 3); both reuse the existing 'RED vs the green step' section without restating it."
    - "D-04 / LAW-01: SKILL.md carries only the compact Three Laws spine (Law 1 gates entry, Law 2 sizes the test) and links the owned leaf; it does not restate the owned prose."
    - "D-06 / D-07 / RTR-02: step 3 detects and matches the house test idiom, routes the stance via the testing-stance route table, states the route and why, and honors a natural-language override (no CLI flag)."
    - "D-08 / LAW-02: step 5 confirms the test fails for the right reason (an AssertionError, not a compile / setup error or false green; F.I.R.S.T. baseline) BEFORE the forward handoff; the coach questions, it does not run the suite."
    - "D-11 / VIT-02: ONE tsc --strict-clean Vitest worked example walks the RED path; it is a compiling stub returning the WRONG value (AssertionError at runtime), NOT a missing symbol and NOT a `ts ignore` fence."
    - "D-14: all three stale deferral markers in SKILL.md are removed; the closing paragraph coaches, it does not drive."
  artifacts:
    - plugins/lz-tdd/skills/lz-red/SKILL.md
  key_links:
    - "step 2 links three-laws-and-test-selection.md (owned spine); step 3 links testing-stance/README.md (route table); step 4 links test-structure-and-assertions.md; step 5 links vitest-typescript-mechanics.md + test-structure-and-assertions.md; step 6 hands off to lz-tpp"
    - "the VIT-02 fence compiles as SKILL-1.ts under tsc --strict (extractor + non-ignore ts-fence guard from 18-01)"
---

<objective>
Replace the SKILL.md coach-procedure placeholder with the real inline numbered decision procedure on
the Three Laws spine, and add the ONE VIT-02 tsc-strict Vitest worked example. This is the phase's
centrepiece: it ties the Phase-16/17/18 references together into an executable RED procedure that
classifies first, spines on the Three Laws, routes the stance, fails for the right reason, and hands
off forward to lz-tpp.

The procedure is orchestration: it links file-level to the leaves and NEVER restates their content
(the owned Three-Laws prose lives in the leaf, 18-02; F.I.R.S.T. baseline + red-bar procedure live in
the leaves, 18-03). It mirrors the two shipped precedents (lz-refactor's 6-step procedure + closing
"coach by default" paragraph; lz-tpp's step-7 show-don't-drive fold). Size discipline (SKL-02): aim
under ~200 lines, near lz-tpp's ~81-line grain -- lz-red needs no analogue of lz-refactor's heavy loop
audit.

Depends on 18-02 + 18-03 so the procedure's links point at filled sections, and on 18-01 for the
SKILL.md checker coverage + the tsc extractor.

Purpose: LAW-01, LAW-02, RTR-02, SEAM-01, and the VIT-02 SKILL.md clause. Output: the filled
"## Coach decision procedure" + closing paragraph + one worked example fence.

## Decisions implemented (D-NN)

- **D-01:** author the procedure inline in SKILL.md, replacing the placeholder -- not a new reference file.
- **D-02:** compact numbered decision tree; step order = classify -> Three Laws spine -> route stance -> structure + assert -> fail-for-the-right-reason -> forward handoff; closing "coach by default; hand off, do not drive" paragraph.
- **D-03:** classify RED / GREEN / REFACTOR before any test; reuse the existing "RED vs the green step" section (do not restate).
- **D-04:** SKILL.md carries only the compact Law 1 / Law 2 / Law 3 spine and links the owned leaf.
- **D-06:** step 3 detects + matches the house idiom, then routes via the testing-stance route table (open the leaf to act).
- **D-07:** the override is a natural-language phrase, not a CLI flag; honor a stated stance preference and still state the route.
- **D-08:** step 5 = fail for the right reason (AssertionError, not a compile / setup error or false green; F.I.R.S.T. baseline); the coach questions, does not run the suite.
- **D-11:** one compact tsc --strict Vitest worked example walking the RED path end to end.
- **D-14:** remove all three SKILL.md deferral markers; the closing paragraph coaches, it does not drive.

## Artifacts produced

- `lz-red/SKILL.md`: the filled "## Coach decision procedure" (6-step numbered tree), the closing "coach by default; hand off, do not drive" paragraph, and ONE VIT-02 `ts` fence extracted as `SKILL-1.ts`; the Coach-mode bullet (line 30) rewritten to describe the now-present procedure; all three phase-name deferral markers removed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-CONTEXT.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-RESEARCH.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/SKILL.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md
@plugins/lz-tdd/skills/lz-tpp/SKILL.md
@plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace the placeholder with the inline 6-step coach decision procedure</name>
  <files>plugins/lz-tdd/skills/lz-red/SKILL.md</files>
  <read_first>plugins/lz-tdd/skills/lz-red/SKILL.md, plugins/lz-tdd/skills/lz-refactor/SKILL.md, plugins/lz-tdd/skills/lz-tpp/SKILL.md</read_first>
  <action>
Replace the placeholder section (currently "## Coach decision procedure (deferred to Phase 18)" +
its body at lines 52-58) with a real "## Coach decision procedure" -- a compact numbered decision tree
(each step 1-4 lines: an imperative move + a file-level link to the leaf that carries the content;
NEVER inline a leaf's content). Author BLIND own-words. Step order (D-02):

1. Classify the request against the lz-tpp / lz-refactor seams [SEAM-01]. Failing test to make pass ->
   hand off to lz-tpp and stop. Already-passing code to clean up -> hand off to lz-refactor. Otherwise
   the next failing test is this skill. Point back at the existing "## RED vs the green step (lz-tpp)
   and the refactor step (lz-refactor)" section (lines 36-42); do NOT restate it.
2. Three Laws spine [LAW-01]. Law 1 gates entry: no production code before a failing test. Law 2 sizes
   the test: only enough to fail -- a not-yet-defined symbol counts. (Law 3 is step 6.) Link
   references/three-laws-and-test-selection.md; do NOT restate the owned prose.
3. Detect and match the house test idiom, then route the stance by structural control / seam
   availability via the references/testing-stance/README.md route table; open the routed leaf to act.
   STATE the route chosen and why. If the developer states a stance preference in plain language
   (for example "test this as a functional core", "use the message-matrix stance", "characterize it"),
   honor it over the auto-route and still state the route [RTR-02, D-06/D-07]. No CLI flag.
4. Structure the test and assert observable behavior. Link references/test-structure-and-assertions.md;
   do NOT restate it.
5. Fail for the right reason [LAW-02, D-08]. Confirm the fresh test fails on the asserted behavior (an
   AssertionError), not a compile / import / setup error or a false green; F.I.R.S.T. is the baseline.
   Explain which failure is the right one -- do NOT run the suite unprompted. Link
   references/vitest-typescript-mechanics.md and references/test-structure-and-assertions.md.
6. Hand off forward to lz-tpp [SEAM-01]. Once the test is red for the right reason, making it pass is
   Law 3 -- the green step, lz-tpp's job, not this skill's.

Then a closing "Coach by default; hand off, do not drive." paragraph mirroring lz-refactor's model
(lines 114-121): on a QUESTION present the next test and the smallest move and let the developer write
it -- do not edit their tests or run the suite unasked; on an explicit COMMAND to write the test, write
the failing test (do not make it pass -- that is lz-tpp), then stop.

Also rewrite the Coach-mode bullet parenthetical (line 30) to describe the now-present procedure
instead of the deferred placeholder. Remove ALL phase-name-plus-number deferral markers from SKILL.md
(the file currently has three). Keep the description frontmatter, the Two modes / RED-vs-green-step /
Listen-to-the-tests sections, and the Reference material list byte-stable. Keep SKILL.md under ~200
lines.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "SKILL.md"</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs SKILL.md entry reports PASS on the classify-first, Three Laws spine, stance-routing, house-idiom, natural-language-override, fail-for-the-right-reason, and forward-lz-tpp-handoff topics, plus the no-stale-marker (`absent`) guard.
    - The procedure is a numbered tree whose steps link the leaves file-level and do NOT restate their content.
    - Step 3 states the route + why and honors a natural-language override with no CLI flag; step 5 precedes step 6 and frames the coach as questioning; the closing paragraph distinguishes QUESTION from COMMAND and never edits tests / runs the suite unasked.
    - The frontmatter description, Two modes, RED-vs-green-step, Listen-to-the-tests, and Reference material sections are byte-unchanged; SKILL.md is under ~200 lines.
  </acceptance_criteria>
  <done>The inline 6-step coach procedure + closing paragraph replace the placeholder, all deferral markers are gone, the leaves are linked without restatement, and the checker passes the SKILL.md content topics (the ts-fence guard still fails until Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the VIT-02 tsc-strict Vitest worked example</name>
  <files>plugins/lz-tdd/skills/lz-red/SKILL.md</files>
  <read_first>plugins/lz-tdd/skills/lz-red/SKILL.md, plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md</read_first>
  <behavior>
    - The fence extracts (as SKILL-1.ts) and compiles tsc --strict --noEmit clean (types line up).
    - The example depicts a VALID red: a compiling production stub that returns the WRONG value, so the test fails its assertion at RUNTIME with an AssertionError -- the LAW-02 "right reason".
    - It is NOT a missing symbol (which breaks tsc) and NOT a `ts ignore` fence (which the extractor skips and the non-ignore guard rejects).
  </behavior>
  <action>
Add ONE compact TypeScript + Vitest worked example inside the coach procedure that walks the RED path
end to end: classify -> pick the degenerate / simplest case -> structure arrange-act-assert -> assert
observable behavior -> confirm it fails for the right reason -> hand off to lz-tpp. Open it with a
plain TypeScript fence whose info string is exactly `ts` (no `ignore` token) so the extractor compiles
it as SKILL-1.ts and the 18-01 non-ignore ts-fence guard is satisfied.

Depict the RED with a COMPILING stub, following the tsc-safe shape the reference fences use (the sumOf
example in three-laws-and-test-selection.md is the analog): import describe / it / expect from
'vitest'; write the failing test that asserts the real expected value; define the production function
with a correct TYPE signature but a body that returns the WRONG value (for example, a discount
function that returns the untouched total), with a trailing comment noting it is not yet implemented so
the assertion fails with an AssertionError. Types must line up so tsc --strict is clean; the test is a
valid red because the assertion fails at runtime. Do NOT reference an undeclared symbol and do NOT use
a `ts ignore` fence. Keep the surrounding prose tying the example to steps 1-6 of the procedure.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "SKILL.md" ; npm --prefix .claude/skills/lz-red-workspace run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The SKILL.md non-ignore ts-fence guard reports PASS (>= 1 bare `ts` fence present).
    - `npm run typecheck` exits 0 with SKILL-1.ts among the compiled modules (the example is tsc --strict --noEmit clean).
    - The example depicts a compiling stub returning the wrong value (AssertionError at runtime), not a missing symbol and not a `ts ignore` fence.
    - check-red-references.mjs SKILL.md entry is fully GREEN (all topics + ts-fence + absent guard).
  </acceptance_criteria>
  <done>One tsc-strict Vitest RED worked example is in the procedure, compiles as SKILL-1.ts, and turns the SKILL.md checker entry fully GREEN.</done>
</task>

</tasks>

<orchestrator_gate>
The lz-red SKILL.md coach procedure MUST be reviewed by >= 1 subagent (including >= 1 unbiased
from-scratch) BEFORE acceptance (the standing rule that every SKILL.md change is subagent-reviewed;
17-06 precedent). This is an ORCHESTRATOR-driven gate run AFTER the executor returns its blind edits --
gsd-executor has no Agent/Task tool, so the executor does NOT spawn it. It is documented and run at
finalize (18-06). The reviewer checks: coach-don't-drive (QUESTION vs COMMAND; no auto-edit / no
running the suite), the natural-language override honored, no leaf content restated, and the 6-step
Three Laws spine present.
</orchestrator_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content -> committed public repo | maintainer work-email/domain, non-ASCII, and any verbatim source prose must not enter committed files |
| example fence -> tsc gate | the worked example must compile tsc --strict clean (it is compiled, never executed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Info disclosure (IP) | SKILL.md coach-procedure prose | medium | mitigate | Blind own-words orchestration authoring + check-hygiene no-verbatim scan (lz-red SKILL.md is in the verbatimTargets set) + the orchestrator skill-review gate (18-06). |
| T-18-02 | Info disclosure | SKILL.md | medium | mitigate | check-hygiene allowlist-inversion work-email scan. |
| T-18-03 | Tampering (integrity) | SKILL.md | low | mitigate | check-hygiene ASCII scan. |
| T-18-06 | Tampering (integrity) | VIT-02 example fence | low | mitigate | tsc --strict extractor covers SKILL-1.ts; the non-ignore ts-fence guard blocks a coverage-skipping `ts ignore` fence. |

No high-severity runtime threats: SKILL.md is Markdown that executes no code; the example fence is
compiled by tsc, never run. ASVS level 1; block on `high` (none exist).
</threat_model>

<verification>
- check-red-references.mjs SKILL.md entry fully GREEN (all coach-procedure topics + non-ignore ts-fence + no-stale-marker guard).
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 with SKILL-1.ts compiled.
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- Orchestrator gate (18-06): >= 1 unbiased skill-review of the coach procedure.
</verification>

<success_criteria>
The inline 6-step coach procedure on the Three Laws spine (classify-first, stance routing with a
natural-language override, fail-for-the-right-reason before the forward lz-tpp handoff, coach-not-drive
closing) plus one tsc-strict Vitest RED worked example replace the placeholder; no deferral marker
remains; the SKILL.md checker entry and typecheck are GREEN; the leaves are linked without restatement.
</success_criteria>

<output>
Create `.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-05-SUMMARY.md` when done.
</output>
