---
phase: 05-skill-effectiveness-evals
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-0-fib-nothing-to-constant/with_skill/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-0-fib-nothing-to-constant/without_skill/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-1-fib-split-base-case/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-2-fib-prefer-tail-recursion/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-3-sum-deep-input-stacksafe/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-4-flatten-tree-no-tail/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-5-wordwrap-impasse-backtrack/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-6-refactor-not-transformation/
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.json
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.md
  - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/review.html
autonomous: true
requirements: [EVAL-02]
user_setup: []

must_haves:
  truths:
    - "The behavior benchmark runs with_skill vs without_skill at >=3 runs per scenario per config; the coach names the correct highest-priority AVAILABLE transformation per scenario, backtracks at the Word Wrap impasse, classifies the green-code request as a refactoring, and never edits code or runs tests (EVAL-02, D-03, D-04)"
    - "All run completions are awaited before grading; timing.json (total_tokens, duration_ms) is captured per run as its notification arrives; grading uses the deterministic grade-run.mjs (D-05, D-04)"
    - "Behavior Pass@k and Pass^k are computed per eval (k=1,3,5,total; cap k at n) and saturated assertions (Pass@1==1.0 for both configs) are flagged (D-05)"
  artifacts:
    - path: ".planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.json"
      provides: "Aggregated with_skill vs without_skill benchmark (D-03, D-05)"
      contains: "pass_rate"
    - path: ".planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.md"
      provides: "Human-readable benchmark summary table"
    - path: ".planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/review.html"
      provides: "Static headless review (generate_review.py --static)"
  key_links:
    - from: "with_skill/run-*/grading.json"
      to: "aggregate_benchmark.py"
      via: "summary.pass_rate per run"
      pattern: "pass_rate"
    - from: "grade-run.mjs"
      to: "each run dir"
      via: "reads transcript.md + metrics.json, writes grading.json"
      pattern: "grading.json"
---

<objective>
Run the EVAL-02 behavior benchmark NATIVELY: for each of the 7 scenarios, spawn a with_skill coach run and a baseline (no-skill) run, >=3 runs each; capture timing; grade deterministically; aggregate; render a static review; and compute behavior Pass@k / Pass^k. This is the eval that protects the single load-bearing core: the coach recommends the CORRECT next transformation by priority.

Purpose: Prove EVAL-02 - the coach names the right highest-priority available transformation, applies the TS/JS overlay, recognizes shape constraints, backtracks at an impasse, classifies refactorings, and stays a coach (no edits, no test runs) - and quantify reliability.
Output: per-run outputs/grading/timing, benchmark.json + benchmark.md, review.html, and a behavior Pass@k/Pass^k table recorded in the SUMMARY for 05-04 to combine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md
@.planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md
@.planning/phases/05-skill-effectiveness-evals/05-PATTERNS.md
@.planning/phases/05-skill-effectiveness-evals/05-01-SUMMARY.md

Inputs authored in 05-01 (must exist): evals/evals.json, the 7 eval_metadata.json under lz-tpp-workspace/iteration-1/eval-<N>-<slug>/, and grade-run.mjs.

Constants (native path forms):
- PHASE_DIR = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals
- WORKSPACE = PHASE_DIR/lz-tpp-workspace/iteration-1
- LZTPP = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp
- SKILL_CREATOR_DIR = C:/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator

Directory-shape contract (05-RESEARCH.md lines 141-167): eval-<N>-<slug>/<config>/run-<K>/{outputs/transcript.md, grading.json, timing.json, metrics.json}. config MUST be exactly `with_skill` / `without_skill` (viewer color-codes on that literal). `pass_rate` is nested under summary (per-run) and under result (aggregate) - NEVER top-level.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spawn with_skill + baseline coach runs (>=3 each x 7 scenarios) and capture timing</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/with_skill/run-*/outputs/transcript.md
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/with_skill/run-*/timing.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/with_skill/run-*/metrics.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/without_skill/run-*/outputs/transcript.md
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/without_skill/run-*/timing.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/without_skill/run-*/metrics.json
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 120-139 (behavior data flow) and lines 141-176 (exact workspace layout + anti-patterns) and lines 213-219 (Pitfall 6: timing capture is one-shot)
    - C:/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/SKILL.md ("Running and evaluating test cases": the same-turn with_skill/baseline subagent pattern + capturing total_tokens/duration_ms at completion)
    - .planning/phases/05-skill-effectiveness-evals/evals/evals.json (the 7 prompts)
    - plugins/lz-tdd/skills/lz-tpp/SKILL.md (the with_skill subagent must be given this skill path so it coaches; baseline runs WITHOUT it)
  </read_first>
  <action>
    For each of the 7 scenarios (eval-0 .. eval-6), read its prompt from evals.json. Spawn Task subagents in the SAME turn to maximize parallelism (fall back to smaller batches / series only if timeouts bite - D discretion, RESEARCH open Q2): for each scenario, >=3 with_skill runs (subagent instructed to consult the lz-tpp skill at LZTPP as its TDD coach) and >=3 without_skill baseline runs (identical prompt, NO skill reference). That is 7 x 2 x 3 = 42 runs minimum. Give each subagent Edit/Write/Bash tools available (so "coach, don't drive" is a genuine choice the eval measures, not a structural block). Write each subagent's full response to eval-<N>-<slug>/<config>/run-<K>/outputs/transcript.md, prefixing an "## Eval Prompt" section (viewer shows the prompt). As EACH run completes, immediately write eval-<N>-<slug>/<config>/run-<K>/timing.json with {total_tokens, duration_ms, total_duration_seconds} from the completion notification (these are unrecoverable later - D-05), and metrics.json with {tool_calls:{Edit:N, Write:N, Bash:N}} counting tool_use in that run plus a boolean for whether a test command was invoked. WAIT for ALL run completions before this task is done - do not grade or aggregate here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),path=require('path'); const b='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1'; const slugs=fs.readdirSync(b).filter(d=>d.startsWith('eval-')); let runs=0,missing=0; for(const s of slugs){for(const c of ['with_skill','without_skill']){const cd=path.join(b,s,c); const rs=fs.existsSync(cd)?fs.readdirSync(cd).filter(r=>r.startsWith('run-')):[]; if(rs.length<3){missing++;} for(const r of rs){runs++; if(!fs.existsSync(path.join(cd,r,'timing.json'))||!fs.existsSync(path.join(cd,r,'outputs','transcript.md'))){missing++;}}}} if(slugs.length!==7||missing>0||runs<42){console.error('FAIL slugs',slugs.length,'runs',runs,'missing',missing);process.exit(1);} console.log('OK runs',runs);"</automated>
  </verify>
  <acceptance_criteria>
    - 7 scenario dirs each have with_skill/ and without_skill/ with >=3 run-<K>/ dirs; total runs >= 42.
    - Every run dir has outputs/transcript.md, timing.json (with total_tokens + duration_ms), and metrics.json (tool_calls counts).
    - No grading.json exists yet (grading happens in Task 2, strictly after all runs complete).
  </acceptance_criteria>
  <done>All 42+ runs completed, each with a transcript, one-shot-captured timing.json, and metrics.json; all completions awaited.</done>
</task>

<task type="auto">
  <name>Task 2: Grade every run and aggregate the benchmark + static review</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/with_skill/run-*/grading.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/eval-*/without_skill/run-*/grading.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.json
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.md
    .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/review.html
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/grade-run.mjs (the grader authored in 05-01 + its embedded rubric)
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 205-219 (Pitfalls 3-6: dir shape, field names, headless --static, timing) and lines 351-363 (aggregate + viewer commands, native, cwd = SKILL_CREATOR_DIR)
    - C:/Users/LarsGyrupBrinkNielse/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/scripts/aggregate_benchmark.py (globbing + summary field reads)
  </read_first>
  <action>
    Run grade-run.mjs against every run dir (all with_skill and without_skill run-<K> dirs across the 7 scenarios), producing a grading.json per run. Then set the Bash working directory to SKILL_CREATOR_DIR (native form) via a standalone cd call (do NOT use `cd ... &&`), and run `python -m scripts.aggregate_benchmark <WORKSPACE> --skill-name lz-tpp` to produce WORKSPACE/benchmark.json + benchmark.md. Then run `python eval-viewer/generate_review.py <WORKSPACE> --skill-name lz-tpp --benchmark <WORKSPACE>/benchmark.json --static <WORKSPACE>/review.html` (ALWAYS --static: headless box, no server; do NOT hand-write HTML). If aggregate warns about missing text/passed fields or blanks, fix the grading.json field names (Pitfall 4) rather than editing the script. The baseline (without_skill) is expected to score LOWER on the discriminating scenarios (2,3,4,5) - that delta is the signal.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const b='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1'; const bm=JSON.parse(fs.readFileSync(b+'/benchmark.json','utf8')); const s=JSON.stringify(bm); const hasWith=s.includes('with_skill'), hasBase=s.includes('without_skill'), hasRate=s.includes('pass_rate'); if(!(hasWith&&hasBase&&hasRate)||!fs.existsSync(b+'/review.html')){console.error('FAIL',hasWith,hasBase,hasRate);process.exit(1);} console.log('OK benchmark + review generated');"</automated>
  </verify>
  <acceptance_criteria>
    - Every run dir has a grading.json using exactly text/passed/evidence and a summary{passed,failed,total,pass_rate}.
    - benchmark.json exists and references both `with_skill` and `without_skill` configs with pass_rate data (not zeros from a shape mismatch); benchmark.md exists; review.html exists (static).
    - aggregate_benchmark.py emits no "missing required fields" warning.
  </acceptance_criteria>
  <done>All runs graded, benchmark.json/.md aggregated, review.html rendered statically; no field-name or dir-shape warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Compute behavior Pass@k / Pass^k and flag saturated assertions</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/05-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 380-384 (Pass@k / Pass^k formulas + reporting contract: per-eval and overall, k=1,3,5,total, cap k at n, skip trivial rows, flag saturated)
    - CLAUDE.md ("skill-creator workflow": MUST compute + report Pass@k and Pass^k per eval AND overall)
    - .planning/phases/05-skill-effectiveness-evals/lz-tpp-workspace/iteration-1/benchmark.json (aggregated per-run pass data)
  </read_first>
  <action>
    For each scenario and config, let n = runs and c = FULLY-passing runs (grading.summary.pass_rate == 1.0). Compute Pass@k = 1 - C(n-c,k)/C(n,k) (optimistic) and Pass^k = C(c,k)/C(n,k) (conservative) for k in {1,3,5,total}, capping k at n (skip k>n). Report per-eval (that eval's n) AND overall (sum of runs across evals), for both with_skill and without_skill. Skip trivial rows where both configs are 1.0. Flag any eval with Pass@1 == 1.0 for BOTH configs as saturated/non-discriminating (candidate for a harder prompt). Record the tables + the flags + the with-vs-baseline delta in 05-02-SUMMARY.md (ASCII tables; use +/- not the Unicode sign). This SUMMARY is the behavior half that 05-04 combines into EVAL-RESULTS.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/05-02-SUMMARY.md'; const t=fs.readFileSync(p,'utf8'); const ok=/Pass@k/.test(t)&&/Pass\^k/.test(t)&&/k\s*=?\s*1/.test(t)&&/with_skill/i.test(t)&&/without_skill|baseline/i.test(t); if(!ok){console.error('FAIL: SUMMARY missing Pass@k/Pass^k tables');process.exit(1);} console.log('OK Pass@k tables present');"</automated>
  </verify>
  <acceptance_criteria>
    - 05-02-SUMMARY.md contains a Pass@k AND a Pass^k table with k=1,3,5,total (k capped at n), per eval and overall, for both configs.
    - Saturated evals (Pass@1==1.0 both configs) are explicitly flagged; the with-vs-baseline delta is stated.
    - All numbers ASCII; no Unicode sign characters (`rg -c $'\u00b1' 05-02-SUMMARY.md` == 0).
  </acceptance_criteria>
  <done>Behavior Pass@k/Pass^k tables and saturation flags recorded in the SUMMARY for 05-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval prompt -> spawned coach subagent | Subagent executes an authored prompt; it has Edit/Write/Bash but the eval measures that it does NOT use them (coach, don't drive) |
| run artifacts -> committed surface | grading/timing/benchmark are committed; raw transcripts (outputs/) are gitignored (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering / EoP | spawned coach subagents | mitigate | Subagents run only the authored evals.json prompts (no untrusted input); the "coach, don't drive" grader assertion detects and fails any run that edited code or ran tests. |
| T-05-03 | Information disclosure | outputs/transcript.md | mitigate | Raw transcripts live under gitignored outputs/ (D-08, set in 05-01); only grading/timing/benchmark/review are committed. |
| T-05-SC | Tampering (supply chain) | Python harness + Node grader | accept | No installs: aggregate/viewer ship with skill-creator (stdlib), grade-run.mjs is Node built-ins only. |
</threat_model>

<verification>
- 42+ runs exist with transcript + one-shot timing.json + metrics.json (Task 1); no grading before all complete.
- Every run graded; benchmark.json shows both configs with real pass_rate (Task 2, no shape/field warnings).
- Pass@k/Pass^k per-eval + overall for both configs, saturation flagged (Task 3).
- Discriminating scenarios (2,3,4,5) show a with_skill > baseline delta (the core signal the eval exists to protect).
</verification>

<success_criteria>
- EVAL-02 is proven: the with_skill coach names the correct highest-priority AVAILABLE transformation per scenario (incl. the TS/JS overlay on 3, tree-no-tail on 4), backtracks on Word Wrap (5), classifies the refactoring (6), and never edits code / runs tests.
- benchmark.json/.md + review.html generated via the shipped scripts (no hand-rolled aggregation or HTML).
- Behavior Pass@k/Pass^k recorded for 05-04's combined results.
</success_criteria>

<output>
Create `.planning/phases/05-skill-effectiveness-evals/05-02-SUMMARY.md` when done (also authored as Task 3's artifact), recording: per-eval with/baseline pass rates, the Pass@k/Pass^k tables, saturation flags, the with-vs-baseline delta, and any scenario where the coach drove instead of coaching.
</output>
