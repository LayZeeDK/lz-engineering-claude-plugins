# Phase 8.2: Functional Catalog (INSERTED) - Context

**Gathered:** 2026-07-07
**Status:** Ready for planning

> Auto-discussed (`--analyze --auto`). Most gray areas were pre-decided by a two-round
> advisor board recorded in `.planning/research/functional-depatterning-ts.md` Sections
> 10-13; `--auto` here ratifies those high-confidence, board-locked choices and
> auto-resolves a few HOW micro-decisions. No trap-quadrant (high-impact +
> low-confidence) items -- nothing deferred as UNRESOLVED.

<domain>
## Phase Boundary

Deliver a single by-idiom `functional-catalog/` reference group under
`plugins/lz-tdd/skills/lz-refactor/references/`: per-idiom leaves documenting, in
TypeScript, how OO patterns/refactorings dissolve into functional idioms AND native FP
patterns, on the LOCKED Kerievsky-aligned shared template; an N:1 pattern->idiom map
README; one-line `Functional alternative:` cross-links added to the existing OO catalog
leaves; and a new `check-functional` harness gate. No coach behavior (that is Phase 9).

</domain>

<decisions>
## Implementation Decisions

### Template & organization (board-ratified -- research S13)
- **D-01:** Locked leaf template (Kerievsky-aligned): header `Use when:` / `Correspondence: dissolves-from | alternative-to -> [OO pattern links]` / `Keep the OO form when:`, then body `## Idiom` / `## Example` (Before -> After, tsc --strict-clean, + one-line "mechanics: <cross-link>, run tests between steps"; NO duplicated Mechanics) / `## When each fits`. Source: research S13.2.
- **D-02:** Organized BY IDIOM, not per-pattern (~17 leaves indicative); many OO patterns map to one idiom leaf (e.g. Visitor/State/Interpreter/Composite -> discriminated-union + fold). Source: research S13.1.
- **D-03:** ONE unified `functional-catalog/` covers both de-patterning idioms and native FP patterns, distinguished only by the `Correspondence` value. Source: research S13.1.
- **D-04:** README is an N:1 pattern->idiom map that declares its N:1 contract (siblings are 1:1 selector-mirrors), still mirrors each leaf's `Use when:`, caps note cells to one line, and cites the research artifact in `## Sources`. Source: research S13.3.

### Correctness anchor -- NO oracle (departure from Phases 7/8/8.1)
- **D-05:** A new `check-functional` gate enforces the selector-mirror, the `Correspondence` closed enum, bidirectional + intra-leaf-anchor `Functional alternative:` <-> `Correspondence:` link integrity spanning gof/kerievsky/extra, a hard one-line-per-served-pattern cap, and `tsc --strict` on every Example. Source: research S13.4.
- **D-06:** This phase has NO owned book oracle. The correctness anchor is the committed research artifact (cited no-oracle tier) + `check-functional` + `tsc --strict`. Do NOT open the AskUserQuestion oracle-access checkpoint that Phases 7/8/8.1 required (Phase 6 carry-forward D-09) -- there is no book to verify against. DST-04 (original prose/code, no verbatim) still applies. Source: research S12.6.

### Scope & coverage (board-ratified -- research S11-S13)
- **D-07:** De-patterning coverage -- every dissolvable/collapsible GoF pattern (23), Kerievsky pattern-directed refactoring, and Fowler FP-analog refactoring resolves via the README map to an idiom leaf OR a one-line note, deduped by idiom. Source: research S11 (map) + S12.
- **D-08:** Native FP patterns in scope -- Option/Either, functor/monad, lens/optics, currying/partial application, transducers -- as `alternative-to` leaves cross-referencing their OO alternative (Option<->Null Object, functor<->Iterator, memoization<->Flyweight, etc.). Flagged FUT-04 (may graduate to its own skill if it outgrows). Source: research S12.4.
- **D-09:** Each existing gof/kerievsky/extra leaf gains a one-line `Functional alternative:` cross-link to its idiom leaf, contract-preserving (same mechanism as the Direction:Away links). Source: research S13.
- **D-10:** The ~13 Fowler true-"Stays OO" (inheritance/identity) + 2 moot refactorings are one-line README notes (or omitted), NOT leaves; the Fowler FP-analogs fold into existing idiom leaves via README rows; selector/lens + normalized-store are the two new idiom leaves sourced from the Fowler side. Source: research S12.3.

### Claude's Discretion (auto-resolved HOW items this discussion)
- **D-11:** Native FP leaves live FLAT in `functional-catalog/` (no `native/` subdir); the `Correspondence: alternative-to` tag plus the README map already distinguish them, and a subdir would fragment the N:1 map. [auto: recommended; low-impact]
- **D-12:** `check-functional.mjs` lives in the non-shipped workspace `.claude/skills/lz-refactor-workspace/tools/` alongside check-gof / check-kerievsky / check-crossrefs / extract-samples, wired into package.json + the full `npm run check` battery + typecheck. [auto: follows the established Phase 7/8/8.1 harness pattern]
- **D-13:** The canonical idiom-leaf inventory and slugs are derived by the researcher from research S11's pattern->idiom map (dedupe patterns to idioms); the ~17 count is indicative, not fixed here. [auto: researcher's job]
- **D-14:** The Replace-Pipeline-with-Loop reverse-direction note goes IN the existing Fowler `replace-loop-with-pipeline.md` leaf, not a new Fowler refactoring (fidelity to Fowler 2e). Source: research S12.4 + S12.7. [auto: recommended]

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Functional-catalog design (LOCKED -- source of record)
- `.planning/research/functional-depatterning-ts.md` Sections 10-13 -- the pattern->FP-idiom->TS-feature disappearance map (S11), the full-corpus Stays-OO + opposite-direction analysis (S12), the Replace-Pipeline-with-Loop findings (S12.7), and the board-ratified locked template + `check-functional` spec (S13). This is THE decision source; read before research and planning.

### Roadmap & requirements
- `.planning/ROADMAP.md` -> "Phase 8.2: Functional Catalog (INSERTED)" -- goal, dependencies (7/8/8.1), success criteria.
- `.planning/REQUIREMENTS.md` -> "Functional Catalog (Phase 8.2)" -- FUN-01, FUN-02, FUN-03, FUN-04.

### Sibling catalogs (template source + cross-link targets)
- `plugins/lz-tdd/skills/lz-refactor/references/kerievsky-catalog/` (+ its README) -- the leaf shape the locked template mirrors (Use when + metadata header + Example).
- `plugins/lz-tdd/skills/lz-refactor/references/gof-catalog/`, `.../extra-patterns-catalog/`, `.../fowler-catalog/` -- the OO leaves that gain `Functional alternative:` cross-links and whose patterns/refactorings the map covers.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Non-shipped validation harness at `.claude/skills/lz-refactor-workspace/tools/` (check-gof, check-kerievsky, check-catalog, check-crossrefs, check-smells, extract-samples, check-hygiene, the one-module-per-fence `tsc --strict` extractor) -- extend with `check-functional`; reuse the extractor, the selector-mirror check, and the cross-ref/link-resolution patterns.
- The Kerievsky leaf + `kerievsky-catalog/README.md` -- the concrete shape to mirror for the locked template and the thin index.
- `plugins/lz-tdd/skills/lz-refactor/SKILL.md` router -- add a functional-catalog pointer after the existing catalog pointers (mirrors the Phase 8.1 D-14 SKILL.md wiring).

### Established Patterns
- Instrument-first / Nyquist scaffold: build `check-functional` to a RED baseline BEFORE authoring content; FUN-01..04 close only when the battery is GREEN (the Phase 7/8/8.1 pattern).
- By-idiom dedup: many OO patterns -> one idiom leaf; README rows are the N:1 bridge and carry each arriving pattern's per-pattern residual anchor.
- DST-04 clean-room discipline (original prose + original tsc-clean code) applies even without an oracle; the research artifact is the source.

### Integration Points
- Bidirectional cross-links between the three OO catalogs and functional-catalog (the checker enforces both directions + intra-leaf anchors).
- SKILL.md router pointer to the new catalog.

</code_context>

<specifics>
## Specific Ideas

- `Correspondence` enum values are exactly `dissolves-from | alternative-to`.
- The `Keep the OO form when:` guardrail line sits in the header (before the Example), listing identity / coordinated mutable state / measured hot path / new-variant churn (expression problem) / house style.
- For a multi-pattern (N:1) idiom (e.g. discriminated-union + fold serving Visitor/State/Interpreter/Composite), `## When each fits` carries exactly one capped residual line per served pattern, each with a stable anchor the README row targets -- this is where the expression-problem flip lives.
- The `## Example` is Before (OO) -> After (idiom), behavior-preserving by construction (pure, same inputs -> outputs), ideally mirroring the linked Fowler/Kerievsky refactoring's domain.

</specifics>

<deferred>
## Deferred Ideas

- Coach behavior that ROUTES to the functional-catalog -- CCH-02 de-patterning routing, the new CCH-06 (functional-alternative surfacing + Replace-Pipeline-with-Loop coaching), and CCH-04 reference-mode extension -- belongs to Phase 9, not here.
- Native FP as its OWN skill (FUT-04) if the functional layer outgrows a single catalog -- tracked, not now.
- Multi-language examples beyond TypeScript/JavaScript (FUT-02).
- None of these are scope creep into Phase 8.2 -- they are downstream/future by design.

</deferred>

---

*Phase: 8.2-Functional Catalog (INSERTED)*
*Context gathered: 2026-07-07*
