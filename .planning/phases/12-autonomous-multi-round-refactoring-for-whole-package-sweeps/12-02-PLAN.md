---
phase: 12-autonomous-multi-round-refactoring-for-whole-package-sweeps
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
autonomous: true
requirements: [SC1, SC2, SC3]
user_setup: []

must_haves:
  truths:
    - "**D-01:** the edit BUILDS ON the committed quick-task form (8acd2b8) and does not re-open it; the two gaps are split -- Gap 1 is a description change, Gap 2 is a NARROW change to the command/drive arm only; no catalog / smells.md / principles re-authoring, no weakening of the lz-tpp seam, no codemods (FUT-03 out)."
    - "**D-02:** the frontmatter description is broadened to also fire on the whole-package / codebase / multi-file SWEEP scan-and-fix intent CATEGORY (developer names a package/directory/module/codebase and asks to find smells and fix them, no slash command), framed as the SAME improve-existing-working-code intent applied at scope; third-person voice and the bounded-pushy posture are kept; the category is addressed, not the e2e phrasing (no overfit)."
    - "**D-04:** the description stays in the load-bearing ~1000-1500 char window, all exclusions sit BEFORE the 1536-char listing-truncation boundary, and it is ASCII-only; claude plugin validate . accepts it (>1024 OK)."
    - "**D-05:** the drive arm gains ONE sweep-drive sentence-cluster (not a new mode, flag, or sub-skill): when the COMMAND names a package/directory/module scope, the termination changes from do-one-then-stop to loop the decision procedure to a FIXPOINT within the named scope, driving all clearly-safe in-scope test-covered rounds autonomously without per-item asking."
    - "**D-06:** the cluster states the stop-condition = a scan pass finds no remaining warranted/actionable/in-scope refactoring PLUS an anti-runaway round/diff/file ceiling, then lands at a terminal review gate (N rounds, tests green each round, changes uncommitted, here is the summary, please review); NEVER commit unless asked; the goal is the fixpoint, NOT zero smells."
    - "**D-07:** the cluster carries all 7 pause/surface boundaries. Boundaries 1-4 map onto EXISTING decision-procedure steps and are REFERENCED by number, not restated: (1) seam/behavior-changing item -> route to lz-tpp and exclude [step 1]; (2) unrecoverable red -> revert + hard stop and (3) untested target -> stop and advise Feathers (never sweep uncovered code, never auto-author characterization tests) [step 5]; (4) speculative pattern introduction -> leave with a one-line reason, de-patterning AWAY stays autonomous [step 4]. Boundaries 5, 6, 7 have NO existing step and are STATED FRESH in the sweep-drive cluster: (5) genuinely ambiguous behavior -> pin or ask, never guess; (6) blast radius escaping the named scope -- an exported/public-API symbol change or a cross-package edit outside the target's own test scope -> pause and ask; (7) flaky or too-slow-to-run-each-step suite -> fall back to advise mode. Step 6 is Reference mode (CCH-04), NOT a pause -- do not map any boundary onto it."
    - "**D-08:** the cluster is forward-only within a sweep -- never both introduce AND remove the same abstraction in one sweep, and never re-touch code an earlier round already refactored (anti-oscillation)."
    - "**D-09:** the QUESTION vs COMMAND axis stays the PRIMARY gate; sweep is a SCOPE sub-branch INSIDE the command/drive arm only; the SINGLE-target command clause (perform in small steps, then stop) is UNCHANGED; both leaks are guarded (the sweep loop must not bleed into single-target commands; the single-target stop must not bleed into sweeps)."
    - "**D-10:** value-framing leads with pattern-directed / de-patterning / seam routing where the skill beats a strong baseline; plain mechanical Extract Function is acknowledged as already base-Opus-grade, tempering over-churn."
    - "**D-11:** the wording is informed by the research substrate (skill-trigger-optimization.md + the primary Anthropic loop-engineering / effective-harnesses / building-effective-agents posts) per source-authority precedence; the sweep-drive cluster is modeled on the loop-in-SKILL.md template (do not stop after one; run tests each round; keep going until nothing remains; explicit pause list; leave uncommitted)."
    - "**D-17:** both edits are triggering-critical AND agent-instruction changes and are subagent-reviewed BEFORE acceptance, including >= 1 UNBIASED from-scratch reviewer; review fixes are applied before commit."
    - "SC1/SC2/SC3: the description broadening serves sweep auto-trigger (SC1), the sweep-drive cluster serves multi-round autonomous drive (SC2), and both are research-informed (SC3); SKILL.md stays < 500 lines and claude plugin validate . + npm run check (no-regression on frozen catalogs + hygiene) stay green."
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      provides: "broadened description (sweep intent category) + one sweep-drive sentence-cluster in the Coach-by-default/drive-when-asked paragraph"
      contains: "Coach by default; drive when asked"
      min_lines: 125
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md description (frontmatter)"
      to: "the sweep intent category (whole-package scan-and-fix of green code)"
      via: "broadened positive framing + exclude-and-reroute tail before 1536 chars"
      pattern: "package|codebase|across"
    - from: "plugins/lz-tdd/skills/lz-refactor/SKILL.md drive arm (Coach by default; drive when asked)"
      to: "decision-procedure steps 1/4/5 (D-07 boundaries 1-4) + 3 fresh-stated boundaries (5/6/7)"
      via: "the sweep sub-branch references steps 1/4/5 for boundaries 1-4 and states boundaries 5/6/7 fresh (they have no step to reference)"
      pattern: "fixpoint|scope|scan"
---

<objective>
Close both gaps in the shipped skill with two small, research-informed text edits to plugins/lz-tdd/skills/lz-refactor/SKILL.md: (1) broaden the frontmatter description to also fire on the whole-package / codebase SWEEP scan-and-fix intent category (Gap 1 / SC1), and (2) add ONE sweep-drive sentence-cluster to the existing "Coach by default; drive when asked" paragraph that flips the drive arm termination from do-one-then-stop to loop-to-fixpoint within the named scope, with a terminal review gate and all 7 D-07 pauses (boundaries 1-4 reference decision steps 1/4/5; boundaries 5/6/7 are stated fresh in the cluster because no existing step carries them) (Gap 2 / SC2). Then route the change through the mandatory D-17 subagent review (incl. >= 1 unbiased) before committing behind the build-time gates.

Purpose: The description is the sole routing lever for sweep triggering; the sweep-drive cluster is the instruction prose that makes multi-round autonomy shippable. Both edit the shipped public surface, so they re-open the review + hygiene gates.
Output: An edited, reviewed, hygiene-clean, plugin-validate-passing SKILL.md (< 500 lines) committed to git. NOT yet live (the user reload is Plan 03's gate). No metered eval run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-CONTEXT.md
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-RESEARCH.md
@.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-PATTERNS.md

The file being edited (read its current committed form first -- EXTEND, do not rewrite):
@plugins/lz-tdd/skills/lz-refactor/SKILL.md

Key excerpts the executor needs (already in the file above):
- Description frontmatter: lines 3-17 (the surface to broaden; the exclude-and-reroute tail is the last sentence).
- Decision procedure steps 1-6: lines 45-73 (steps 1/4/5 carry D-07 boundaries 1-4 -- reference them by number; step 6 is Reference mode / CCH-04, NOT a pause; D-07 boundaries 5/6/7 have no step and must be stated fresh in the cluster).
- "Coach by default; drive when asked" paragraph: lines 75-82 (the COMMAND arm is where the sweep sub-branch attaches; the single-target "then stop" clause stays unchanged).
- RESEARCH.md Code Examples: the Anthropic loop-in-SKILL.md template shape ("do not hand back partially verified work") and the conceptual sweep-drive transfer wording.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden the description + add the sweep-drive sentence-cluster</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (the full committed 8acd2b8 form -- description lines 3-17, decision steps 45-73, drive paragraph 75-82)
    - 12-PATTERNS.md sections "description frontmatter" and "Coach by default; drive when asked" (the in-place conventions to preserve; the pause-boundary -> step mapping: boundaries 1-4 -> steps 1/4/5, boundaries 5/6/7 have no step and are stated fresh)
    - 12-RESEARCH.md Pattern 1 (sweep intent category signals + the 3 boundaries that must hold), Pattern 2 (loop-to-fixpoint as instruction prose), and Code Examples (the loop-in-SKILL.md template)
    - 12-CONTEXT.md D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-10 (the exact scope of each edit)
  </read_first>
  <action>
    Description edit (Gap 1): in the frontmatter description, add the whole-package / codebase / directory / module SWEEP scan-and-fix intent category to the positive "Use it whenever ..." framing -- signal scanning a package/module/codebase for smells and fixing/cleaning/sweeping them, framed as the SAME improve-existing-working-code intent applied at scope (D-02). Keep the third-person, action- and trigger-oriented voice and the bounded-pushy posture. Ensure the existing exclude-and-reroute tail (do NOT use it to make a red test pass -> lz-tpp; nor for writing new code / adding a feature) still reads correctly at scope; do NOT add a literal "not for performance" clause (overfit -- the perf-sweep NEGATIVE carries specificity, per Open Question 1). Address the category, never the e2e prompt keywords. Keep the block ASCII-only and inside the ~1000-1500 char load-bearing window with all exclusions before the 1536 truncation (D-04).
    Sweep-drive cluster (Gap 2): add ONE sentence-cluster to the "Coach by default; drive when asked" paragraph, keyed off SCOPE INSIDE the COMMAND branch (D-05/D-09). Reuse the existing "small behavior-preserving steps, running the tests after each" phrasing so it reads as the existing arm iterated, not a new mode. State: when the command names a package/directory/module scope, do not stop after one refactoring -- repeat the decision procedure to a fixpoint within that scope (scan for the next warranted, in-scope, test-covered refactoring; apply it in small steps; run the tests; keep going), forward-only (never re-touch an already-refactored region, never introduce-then-remove the same abstraction) (D-08); stop when a scan pass finds nothing warranted/actionable left OR the round/diff/file ceiling is hit, then land at a terminal review gate (N rounds, tests green each round, changes uncommitted, summary for review; never commit unless asked) (D-06). Pause and surface -- do not silently proceed -- on all 7 D-07 boundaries: for boundaries 1-4 (seam/behavior route-out; revert on unrecoverable red; stop + advise Feathers on untested; leave speculative pattern introductions) REFERENCE decision steps 1/4/5 by number, do NOT restate them; boundaries 5, 6, 7 have NO existing step, so STATE them FRESH in the cluster -- (5) genuinely ambiguous behavior: pin or ask, never guess; (6) blast radius escaping the named scope (an exported/public-API symbol change or a cross-package edit outside the target's own test scope): pause and ask; (7) flaky or too-slow-to-run-each-step suite: fall back to advise mode. Do NOT map any boundary onto step 6 (it is Reference mode, CCH-04, not a pause) (D-07). Leave the SINGLE-target command clause ("then stop and leave the changes for the developer to review") UNCHANGED (D-09). Keep the value-lead that pattern-directed / de-patterning / seam routing is where the skill beats a strong baseline (D-10). Keep SKILL.md < 500 lines.
  </action>
  <acceptance_criteria>
    - The description contains the whole-package/sweep scan-and-fix category signals (package/module/directory/codebase scope words) and keeps the exclude-and-reroute tail; no literal performance-exclusion clause was added; no e2e prompt wording is quoted.
    - The description block is ASCII-only, within ~1000-1500 chars, with all exclusions before char 1536.
    - The "Coach by default; drive when asked" paragraph gains exactly ONE sweep-drive cluster; the single-target COMMAND "then stop" clause is byte-unchanged; the QUESTION->advise clause is unchanged.
    - All 7 D-07 pause boundaries are present in the shipped cluster prose: boundaries 1-4 by reference to decision steps 1/4/5 (not restated); boundaries 5, 6, and 7 by explicit NEW wording (5 = ambiguous behavior -> pin/ask; 6 = blast radius / exported-signature or cross-package edit escaping scope -> pause and ask; 7 = flaky/too-slow suite -> advise). No boundary is mapped onto step 6 (Reference mode). The nx seeded trap exercises boundary 6 and the kata seeded trap exercises boundary 5, so both fresh boundaries must be verifiably present in the cluster.
    - SKILL.md is < 500 lines and claude plugin validate . passes.
  </acceptance_criteria>
  <verify>
    <automated>claude plugin validate .</automated>
    <note>Also confirm SKILL.md line count < 500 and the description char count is in the ~1000-1500 window with exclusions before 1536 (a scratchpad wc/char-count check; no metered run).</note>
  </verify>
  <done>SKILL.md carries the broadened sweep-trigger description and the single sweep-drive cluster; single-target behavior and the seam are unchanged; plugin validate passes.</done>
</task>

<task type="auto">
  <name>Task 2: D-17 subagent review (incl. >= 1 unbiased) + apply fixes + build gates + commit</name>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (the edited form from Task 1)
    - 12-CONTEXT.md D-17 (review requirement) and the MEMORY notes [[agent-skill-instruction-changes-need-review]] + [[unbiased-review-beats-primed]]
    - .claude/skills/lz-refactor-workspace/check-evals.mjs (stays green) and the repo hygiene checker tools/check-hygiene.mjs (part of npm run check) -- do not modify either
  </read_first>
  <action>
    Spawn subagent review of the SKILL.md diff BEFORE acceptance (D-17): at least one PRIMED reviewer (given the phase intent -- does the description address the sweep category without overfitting/leaking specificity, does the cluster implement loop-to-fixpoint with the correct pauses and unchanged single-target behavior) AND at least one UNBIASED from-scratch reviewer (given only the edited SKILL.md and the skill-authoring goal, no Phase-12 framing, to catch mis-routing, seam leakage, or over-reach the primed reviewer would confirm past). Apply the review fixes. Then run the build-time gates: claude plugin validate . (and --strict), npm run check from the workspace (frozen-catalog no-regression + hygiene: ASCII-only, no verbatim prose), check-evals.mjs green, and the public-repo email allowlist-inversion scan (assert the only email-shaped token present on the shipped surface is the approved public gmail; never write the maintainer work email/domain as a search needle). Commit SKILL.md with an ASCII-only, PII-clean message. Do NOT run /reload-plugins (Claude cannot; it is Plan 03's user gate) and do NOT run any metered claude -p eval.
  </action>
  <acceptance_criteria>
    - At least 2 subagent reviews ran, including >= 1 unbiased from-scratch reviewer; every review returned PASS (or PASS-with-fixes and the fixes were applied) before commit.
    - claude plugin validate . and --strict pass; npm run check passes (frozen catalogs untouched, hygiene clean); check-evals.mjs green.
    - Email allowlist-inversion scan on the shipped surface is clean (only the approved public gmail present); ASCII-only committed content; commit message is ASCII + PII-clean and carries no AI-attribution trailer.
    - SKILL.md is committed; no metered eval was executed; /reload-plugins was NOT attempted.
  </acceptance_criteria>
  <verify>
    <automated>claude plugin validate . && cd .claude/skills/lz-refactor-workspace && npm run check && node check-evals.mjs</automated>
    <note>The D-17 subagent review is a process gate (agent spawn + returned PASS), not a script; record the reviewer verdicts in the SUMMARY. The email allowlist-inversion scan is run before commit.</note>
  </verify>
  <done>The SKILL.md change is reviewed (incl. unbiased), hygiene- and validate-clean, and committed -- built but not yet live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill description -> Claude skill-selection routing | The description is untrusted-input-shaped only in that an over-broad phrasing mis-routes the green/transformation step away from lz-tpp; the risk is a correctness/seam regression, not code execution |
| sweep-drive instruction -> autonomous edits to real code | The cluster authorizes multi-round autonomous edits; without guards it could edit outside the named scope, on uncovered code, or change behavior |
| SKILL.md edit -> public repo surface | plugins/ ships publicly; committed prose must be ASCII + maintainer-PII-clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering (seam mis-route) | broadened description | mitigate | Address the sweep CATEGORY only (no generic whole-codebase imperative vocabulary that neighbors the green step / feature work, D-03); keep the exclude-and-reroute tail; the specificity guarantee is re-measured on the new surface by the Plan-01 sweep negatives + seam set (SC1); reviewed by the D-17 unbiased reviewer for seam leakage |
| T-12-02 | Tampering / Elevation (autonomous over-reach) | sweep-drive cluster | mitigate | The 7 D-07 pause boundaries (seam/behavior route-out, revert on red, stop on untested, leave speculative introductions, pin/ask on ambiguity, pause on blast-radius escape, advise on flaky suite) + D-08 forward-only + D-06 terminal review gate + never-commit-unless-asked are the shipped safety controls; the security auditor verifies the guards are present in the prose, not merely in the eval design |
| T-12-03 | Information disclosure (public-repo PII/hygiene) | committed SKILL.md + commit message | mitigate | ASCII-only + email allowlist-inversion scan before commit (only the approved public gmail may appear; never the maintainer work email/domain, plain or obfuscated); no AI-attribution trailer |
| T-12-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package install occurs in this phase (audit N/A); nothing to verify |
</threat_model>

<verification>
- claude plugin validate . and --strict pass; SKILL.md < 500 lines.
- npm run check (frozen catalogs no-regression + hygiene) green; check-evals.mjs green.
- Description in the ~1000-1500 window, exclusions before 1536, ASCII-only.
- Single-target COMMAND behavior and the QUESTION->advise clause byte-unchanged; the seam ("Refactoring vs the green step") untouched.
- >= 2 subagent reviews (incl. >= 1 unbiased) returned PASS before commit; verdicts recorded in the SUMMARY.
- Email allowlist-inversion scan clean; commit message ASCII + PII-clean, no AI-attribution.
</verification>

<success_criteria>
The shipped SKILL.md fires on the whole-package sweep intent category (SC1) and drives multi-round autonomously to a fixpoint with the seven pause guards and a terminal review gate (SC2), both research-informed (SC3), reviewed (incl. unbiased) and committed clean -- built but not yet live pending the user reload.
</success_criteria>

<output>
Create `.planning/phases/12-autonomous-multi-round-refactoring-for-whole-package-sweeps/12-02-SUMMARY.md` when done.
</output>
