---
phase: 08.2-functional-catalog-inserted
plan: 04
subsystem: reference-content
tags: [functional-catalog, native-fp, alternative-to, option-either, functor-monad, lens, currying, transducers, normalized-store, tsc-strict]

# Dependency graph
requires:
  - phase: 08.2-functional-catalog-inserted
    plan: 01
    provides: check-functional.mjs (C1-C8) + 19-idiom LEAVES contract + extract-samples/check-crossrefs wiring + README scaffold
provides:
  - the 6 native-FP alternative-to idiom leaves (option-and-either, functor-and-monad, lens-and-optics, currying-and-partial-application, transducers, normalized-store) on the locked D-01 template, every Example tsc --strict clean
  - the 19-leaf idiom inventory is now complete on disk (13 dissolves-from from plans 02/03 + these 6 alternative-to)
  - the mutual-back-link half of the FUN-04 contract for Null Object and Extract Parameter (this side authored; the OO-side Functional alternative: lines land in plans 05/06)
affects: [08.2-05, 08.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "alternative-to native leaf: Correspondence names the OO alternative it competes with, not a pattern that dissolves into it"
    - "Before(OO) -> After(idiom) Example is behavior-preserving by construction (pure, same inputs -> same outputs); one module per fence keeps Before/After symbol names from colliding"
    - "mechanics: one-line cross-link inside ## Example (no duplicated ## Mechanics, D-01)"

key-files:
  created:
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/option-and-either.md
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/currying-and-partial-application.md
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/functor-and-monad.md
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/normalized-store.md
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/lens-and-optics.md
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/transducers.md
  modified: []

key-decisions:
  - "option-and-either and currying-and-partial-application carry MUTUAL back-links (Null Object; Extract Parameter, A4); the OO-side halves are authored in plans 05/06"
  - "functor-and-monad -> Iterator, currying -> Builder, and normalized-store -> Mediator are RESOLUTION-ONLY (not mutual): those OO leaves' own Functional alternative: points at their dissolution leaf (Iterator->generator, Mediator->reducer-and-store) or Builder->function-composition"
  - "lens-and-optics, transducers, normalized-store target fowler-catalog leaves in Correspondence -- A3 resolution-only, accepted by check-functional C2 and resolved by check-crossrefs; Fowler leaves never receive a Functional alternative: line (D-10)"

requirements-completed: []  # FUN-01, FUN-03 remain OPEN by design -- close only when the full battery is GREEN (README rows + OO cross-links in plans 05/06)

# Metrics
duration: ~20min
completed: 2026-07-07
---

# Phase 08.2 Plan 04: Native-FP alternative-to idiom leaves Summary

**The six native-FP `alternative-to` idiom leaves (Option/Either, functor/monad, lens/optics, currying/partial application, transducers, normalized store) authored FLAT in functional-catalog/ on the locked D-01 template -- each with a paraphrased ## Idiom, a behavior-preserving tsc-strict-clean Before->After ## Example, and a single un-split ## When each fits -- completing the 19-leaf idiom inventory on disk.**

## Performance

- **Duration:** ~20 min active
- **Completed:** 2026-07-07
- **Tasks:** 3 (each authoring two leaves)
- **Files created:** 6 (0 modified; no shared artifacts touched)

## Accomplishments

- All six `alternative-to` native leaves exist FLAT in `functional-catalog/` (D-11), each on the locked D-01 template: `Use when:` / `Correspondence: alternative-to -> [links]` / `Keep the OO form when:` header, then `## Idiom` / `## Example` / `## When each fits`.
- Every TypeScript Example is `tsc --strict`-clean and behavior-preserving (Before OO -> After idiom, pure, same inputs -> same outputs). extract-samples went from 213 to 225 modules (12 new fences: 6 leaves x Before+After), all compile.
- The two mutual-back-link natives point at their OO counterparts: `option-and-either -> Null Object` (extra) + `Introduce Null Object` (kerievsky); `currying-and-partial-application -> Builder` (gof) + `Extract Parameter` (kerievsky, A4).
- The four resolution-only refs are in place and resolve: `functor-and-monad -> Iterator` (gof); `normalized-store -> Change Value to Reference` (fowler) + `Mediator` (gof); `lens-and-optics -> Hide Delegate` + `Remove Middle Man` (fowler); `transducers -> Replace Loop with Pipeline` (fowler).
- All Correspondence links + in-Example `mechanics:` cross-links resolve via check-crossrefs (563 links GREEN, up from 547); hygiene GREEN over 163 files; the 8 pre-existing checkers stay GREEN (no regression).

## Task Commits

1. **Task 1: option-and-either + currying-and-partial-application (mutual back-links)** - `02575fe` (feat)
2. **Task 2: functor-and-monad + normalized-store** - `fdeef08` (feat)
3. **Task 3: lens-and-optics + transducers** - `f29f99a` (feat)

**Plan metadata (this SUMMARY.md):** committed separately (docs) after Task 3.

## Files Created

- `functional-catalog/option-and-either.md` - Option/Result tagged unions replace null checks and exceptions; MUTUAL with Null Object (extra) + Introduce Null Object (kerievsky).
- `functional-catalog/currying-and-partial-application.md` - staged arguments / pre-bound configuration; Builder (gof) resolution-only + Extract Parameter (kerievsky) MUTUAL (A4).
- `functional-catalog/functor-and-monad.md` - map/flatMap container; Iterator (gof) resolution-only.
- `functional-catalog/normalized-store.md` - id + Map<Id,Entity> shared reference; Change Value to Reference (fowler, A3) + Mediator (gof), both resolution-only.
- `functional-catalog/lens-and-optics.md` - composable immutable getter/setter focus; Hide Delegate + Remove Middle Man (fowler, A3), resolution-only.
- `functional-catalog/transducers.md` - reducer-wrapping steps composed into one single-pass reducer; Replace Loop with Pipeline (fowler, A3), resolution-only.

## Correspondence classification (mutual vs resolution-only)

Per the Task 2 acceptance criteria and the RESEARCH Cross-Link Contract (Deliverable 3), the six leaves' Correspondence refs split as:

| Leaf | Correspondence target(s) | Classification | Why |
|------|--------------------------|----------------|-----|
| option-and-either | Null Object [extra], Introduce Null Object [kerievsky] | MUTUAL | Null Object's / Introduce Null Object's own `Functional alternative:` will point back here (plans 05/06) |
| currying-and-partial-application | Extract Parameter [kerievsky] | MUTUAL (A4) | Extract Parameter's `Functional alternative:` will point back here (plan 06) |
| currying-and-partial-application | Builder [gof] | resolution-only | Builder dissolves to function-composition; its `Functional alternative:` points there, not here |
| functor-and-monad | Iterator [gof] | resolution-only | Iterator dissolves to generator; its `Functional alternative:` points at generator, not here |
| normalized-store | Mediator [gof] | resolution-only | Mediator dissolves to reducer-and-store; its `Functional alternative:` points there |
| normalized-store | Change Value to Reference [fowler] | resolution-only (A3) | Fowler leaves never receive a `Functional alternative:` line (D-10); reached via README rows only |
| lens-and-optics | Hide Delegate, Remove Middle Man [fowler] | resolution-only (A3) | same -- Fowler participation is README-row + resolution-only |
| transducers | Replace Loop with Pipeline [fowler] | resolution-only (A3) | same; the D-14 reverse (Replace Pipeline with Loop) note lives IN the Fowler leaf, added in plan 05 -- NOT duplicated here |

check-functional C2 accepts the fowler-catalog Correspondence targets (A3/OQ2), and check-crossrefs resolves them (all target files + anchors verified present before authoring).

## Battery state after this plan (partial, RED overall by design)

This is a mid-phase wave; check-functional is EXPECTED to remain RED overall until the README rows (plan 05) and the 55 OO-leaf `Functional alternative:` lines (plans 05/06) land. The exact recorded state:

| Gate | Exit | Result |
|------|------|--------|
| `node extract-samples.mjs` (typecheck) | 0 | GREEN -- 225 modules tsc --strict clean (12 new fences from these 6 leaves) |
| `node tools/check-crossrefs.mjs` | 0 | GREEN -- 563 links resolve, 20 inverse pairs mutual, no self-refs |
| `node tools/check-hygiene.mjs` | 0 | GREEN -- ASCII + email clean over 163 files, 0 no-verbatim WARN |
| `node tools/check-functional.mjs` | 1 | RED (by design) -- 6/19 idiom leaves present; the SOLE per-leaf failure for these six is C3 "no README index row" (README rows are plan 05). No C1/C2 structural failures on any of the six. |
| check-catalog / check-kerievsky / check-gof / check-extra-patterns / check-smells / check-principles | 0 | GREEN -- no regression (62/62, 27/27, 23/23, 5/5, 24/24, 8/8) |

Progress on check-functional: 0/19 (plan 01 baseline) -> 6/19 present (these leaves). The remaining 13 dissolves-from leaves are authored by the sibling wave-2 executors (plans 02/03).

## Decisions Made

- **The two MUTUAL natives author only their own side now.** option-and-either and currying-and-partial-application name Null Object / Extract Parameter in Correspondence; the reciprocal `Functional alternative:` lines on those OO leaves are plans 05/06 work. check-functional C4 mutuality is forward-subset and only fires once the OO leaf carries the line, so no premature failure here.
- **fowler-catalog Correspondence targets are resolution-only (A3).** lens-and-optics, transducers, and normalized-store target Fowler leaves; those Fowler leaves get NO `Functional alternative:` line (D-10). check-crossrefs resolves the links; there is no mutuality obligation.
- **The transducer Example uses composable reducer-wrapping steps** (mapping/filtering transducers folded via a single reduce) rather than a hand-inlined fold, so the leaf teaches the distinguishing property (collection-agnostic, composable single pass), and it compiles clean under tsc --strict with an explicit `reduce<number[]>` annotation.
- **Idiom H1 names match the plan-01 LEAVES contract exactly** (Title Case: "Option and Either", "Functor and Monad", "Lens and Optics", "Currying and Partial Application", "Transducers", "Normalized Store") so C1 identity + filename==slug pass.

## Deviations from Plan

None affecting scope. The tasks are marked `tdd="true"`; for reference-content leaves the pre-existing check-functional RED baseline (0/19, authored instrument-first in plan 01) IS the failing test -- authoring these six leaves turns them from FAIL to structural PASS on C1/C2, which is the GREEN transition. No separate test file is authored (the checker battery is the test). This is the established Phase 7/8/8.1 instrument-first content pattern, not a departure.

## DST-04 / hygiene compliance

- Every `## Idiom` and prose line is written in original wording (no book/artifact prose lifted; the Pitfall-1 near-verbatim Intent trap was avoided by paraphrasing from the first draft). All Example code is original TypeScript.
- check-hygiene GREEN: ASCII-only (no Unicode, `->` for arrows, `--` for dashes), no non-allowlisted emails, 0 no-verbatim WARN over 163 files.

## Known Stubs

None. All six leaves are complete, compiling content -- no placeholders, no empty data, no deferred wiring within this plan's scope. The pending README rows and OO-side cross-links are owned by plans 05/06 (out of this plan's declared files_modified), not stubs in these leaves.

## Self-Check: PASSED

- Created files exist: all 6 leaf files FOUND (option-and-either, currying-and-partial-application, functor-and-monad, normalized-store, lens-and-optics, transducers).
- Task commits exist on the worktree branch: 02575fe (Task 1), fdeef08 (Task 2), f29f99a (Task 3) -- all FOUND.
- No file deletions across the three commits; working tree clean; no shared-artifact (STATE.md / ROADMAP.md) or sibling-owned files touched (orchestrator-owned).

---
*Phase: 08.2-functional-catalog-inserted*
*Completed: 2026-07-07*
