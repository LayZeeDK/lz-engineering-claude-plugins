---
phase: 13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/golden-master/approvals.spec.ts.snap
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/behavior-baseline.md
autonomous: true
requirements: [SPEC-req-4, SPEC-req-6]
user_setup: []

must_haves:
  truths:
    - "The nx behavior-oracle baseline is captured ONCE on pristine origin/23.0.x and recorded as a differential reference (expected ~15 failed / 169 passed) so behavior-preservation grading (SPEC req 4b) can score NO NEW failures rather than the wrong all-green gate (RESEARCH Pitfall 2)."
    - "The kata golden-master snapshot is SEEDED from PRISTINE main (approvals.spec.ts run once on unedited source) and kept aside in the workspace, so edited-source runs are checked against pristine behavior with jest --ci, never against a snapshot of their own edits (RESEARCH Pitfall 3)."
    - "Both borrowed repos are captured non-destructively and returned toward pristine after baseline capture (nx reset to origin/23.0.x; kata back to main), so the metered runs in wave 2 start from a known base (SPEC req 6 discipline)."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json"
      provides: "nx @nx/eslint-plugin pristine failing/passing counts + the failing spec names (differential reference)"
      contains: "passed"
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/golden-master/approvals.spec.ts.snap"
      provides: "kata golden-master snapshot seeded from pristine main"
      min_lines: 1
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/behavior-baseline.md"
      provides: "which kata runner was used (jest --ci) + how to reuse the seeded snapshot"
      min_lines: 3
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json"
      to: "13-04 correctness grading (behavior oracle, nx differential)"
      via: "the differential reference the edited-source runs are scored against"
      pattern: "passed|failed"
    - from: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/golden-master/approvals.spec.ts.snap"
      to: "13-04 correctness grading (behavior oracle, kata --ci)"
      via: "the pristine golden master jest --ci refuses to overwrite and fails on mismatch"
      pattern: "exports"
---

<objective>
Capture the two independent behavior-oracle baselines that every correctness grade in this phase is scored against, on PRISTINE borrowed repos, BEFORE any metered apply run disturbs them. This is the phase's Wave-0 requirement (13-VALIDATION.md): the nx differential baseline (the ~15 pre-existing failures on this arm64 box) and the kata golden-master snapshot seeded from pristine main. No metered claude -p spend; no shipped-skill change.

Purpose: Without a pristine baseline, behavior-preservation grading is wrong on both suites -- nx would fail every run at import time (15 pre-existing failures), and the kata snapshot would be written from edited source and pass trivially. Capturing baselines first also de-risks Assumption A2 (is the 15-fail set stable?) before spending 18 metered runs in wave 2.
Output: e2e-nx/behavior-baseline.json (pristine counts + failing spec names), e2e-gilded-rose/golden-master/approvals.spec.ts.snap (pristine snapshot), e2e-gilded-rose/behavior-baseline.md (runner + reuse note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESEARCH.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-VALIDATION.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-PATTERNS.md

Borrowed repos (OUTSIDE this repo; use Bash git in the borrowed repo path, not git grep):
- nx: D:/projects/github/nrwl/nx (branch lz-refactor-e2e-smoke; applyBase origin/23.0.x; node_modules present)
- kata: D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript (branch main is PROTECTED; node_modules present; NO __snapshots__ on disk)

The workspace tree is git-tracked (except outputs/ and e2e-reference/results/). New baseline artifacts here WILL be committed and must pass the ASCII + PII hygiene scan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the nx differential behavior baseline on pristine source</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json</files>
  <read_first>
    - 13-RESEARCH.md "Common Pitfalls -> Pitfall 2" (nx has ~15 pre-existing failures; all-green is the wrong gate) and "Code Examples -> REQ-4(b): nx behavior oracle" (the exact reset + nx test commands)
    - 13-VALIDATION.md "Wave 0 Requirements" (nx ~15-fail baseline on pristine origin/23.0.x)
  </read_first>
  <action>
    In the nx borrowed repo (D:/projects/github/nrwl/nx), reset to pristine base: git reset --hard origin/23.0.x then git clean -fd (the clone is on the non-protected throwaway branch lz-refactor-e2e-smoke, so reset is allowed). Run npx nx test eslint-plugin and capture the totals line. Filter the output with rg (never the grep tool), e.g. rg -N 'Tests:|failed|passed'. Record into e2e-nx/behavior-baseline.json: base ref (origin/23.0.x), the failed count, the passed count, the total, and the list of failing spec file names (the pre-existing set, expected to include the dependency-checks.spec.ts native-binding / spyOn import crash). Add a one-line note that behavior-preservation grading in 13-04 is DIFFERENTIAL: a run is behavior-preserving when it introduces NO NEW failures beyond this baseline (same-or-fewer failures; specs covering touched files still pass), and that the prior-art workaround for the import-time crash is running the affected spec directly via raw jest. ASCII-only; no .oracle/ paths; no work email. Leave the repo reset to pristine origin/23.0.x afterward.
  </action>
  <verify>
    <automated>test -s .claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json && node -e "const b=require('./.claude/skills/lz-refactor-workspace/e2e-nx/behavior-baseline.json'); if(b.passed==null||b.failed==null){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - behavior-baseline.json exists and is non-empty, records failed + passed + total counts and the failing spec names for pristine origin/23.0.x
    - the file states the differential rule (no NEW failures) in its own words, ASCII-only, no .oracle/ path or prose, no work email
    - the nx borrowed repo git status is on origin/23.0.x with a clean tree after this task
  </acceptance_criteria>
  <done>e2e-nx/behavior-baseline.json records the pristine differential reference (~15 failed / 169 passed + failing spec names); nx clone reset to pristine.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the kata golden-master snapshot from pristine main</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-gilded-rose/golden-master/approvals.spec.ts.snap, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/behavior-baseline.md</files>
  <read_first>
    - 13-RESEARCH.md "Common Pitfalls -> Pitfall 3" (snapshot must be seeded from pristine; approvals.spec.ts uses toMatchSnapshot and no .snap is on disk) and "Code Examples -> REQ-4(b): kata behavior oracle" (checkout throwaway branch off main; seed; --ci)
    - 13-RESEARCH.md "Open Questions -> OQ-2" (use jest with --ci; ignore test/jest/gilded-rose.spec.ts which asserts 'fixme')
  </read_first>
  <action>
    In the kata borrowed repo (D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript), leave the PROTECTED main branch first: git checkout -b lz13-kata-apply main (or reuse it if present), then git reset --hard main and git clean -fd so source is pristine. Run npx jest test/jest/approvals.spec.ts ONCE on unedited source so it writes test/jest/__snapshots__/approvals.spec.ts.snap (the golden master = pristine behavior). Copy that .snap into the workspace at e2e-gilded-rose/golden-master/approvals.spec.ts.snap (this is the retained, tracked golden master reused across all runs and both arms). Author e2e-gilded-rose/behavior-baseline.md recording: the runner is jest with --ci (refuses to write, fails on mismatch); the golden master was seeded from pristine main and lives at golden-master/approvals.spec.ts.snap; the reuse recipe for 13-04 (git apply the diff to a fresh checkout, restore the golden master, run npx jest --ci test/jest/approvals.spec.ts); and that test/jest/gilded-rose.spec.ts is ignored (it asserts the 'fixme' placeholder, not behavior). ASCII-only; no .oracle/ paths; no work email. After capture, restore the kata repo: git checkout main, git reset --hard main, git clean -fd (remove the locally written __snapshots__ from the borrowed repo; the tracked copy in the workspace is the record).
  </action>
  <verify>
    <automated>test -s .claude/skills/lz-refactor-workspace/e2e-gilded-rose/golden-master/approvals.spec.ts.snap && test -s .claude/skills/lz-refactor-workspace/e2e-gilded-rose/behavior-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - golden-master/approvals.spec.ts.snap exists, is non-empty, and was generated from pristine main (not edited source)
    - behavior-baseline.md names jest --ci as the runner and gives the reuse recipe for 13-04, ASCII-only, no .oracle/ path or prose, no work email
    - the kata borrowed repo is back on main with a clean git status after this task (locally written __snapshots__ removed; workspace copy retained)
  </acceptance_criteria>
  <done>Pristine golden master retained at golden-master/approvals.spec.ts.snap; behavior-baseline.md records the jest --ci reuse recipe; kata repo restored to main clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model transcript / test output -> committed baseline artifact | non-ASCII from tool output must be normalized when written into the tracked baseline files |
| maintainer identity -> public repo commit | only the approved public gmail may appear in committed artifacts and committer identity |
| borrowed-repo state -> phase runs | baselines must be captured on pristine source; repos returned toward base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | .oracle/ prose in baseline artifacts | mitigate | baselines are derived from OUR test runs only; main context never reads .oracle/; post-write scan asserts no .oracle path/prose |
| T-13-02 | Information Disclosure | maintainer work email/domain in committed baseline files | mitigate | ASCII, original-words baselines; allowlist-inversion email scan before commit (only approved gmail); committer = approved gmail |
| T-13-03 | Tampering | non-ASCII/mojibake in behavior-baseline.json/.md | mitigate | author ASCII-only; normalize any quoted test-output punctuation; rg -nP '[^\x00-\x7F]' scan is empty on the new files |
| T-13-SC | Tampering | npm/pip installs | accept | phase installs NO packages; nx/jest already present in the borrowed repos' node_modules; no registry surface |
</threat_model>

<verification>
- e2e-nx/behavior-baseline.json exists with failed/passed/total counts + failing spec names for pristine origin/23.0.x
- e2e-gilded-rose/golden-master/approvals.spec.ts.snap exists (seeded from pristine main) and behavior-baseline.md documents the jest --ci reuse
- both borrowed repos are on their base (nx origin/23.0.x clean; kata main clean) after the plan
- rg -nP '[^\x00-\x7F]' on the three new artifacts returns nothing (ASCII-only)
</verification>

<success_criteria>
Both behavior-oracle baselines are captured on pristine source and retained in the tracked workspace, ready for wave-3 correctness grading; no metered spend occurred; borrowed repos returned to base.
</success_criteria>

<output>
Create `.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-01-SUMMARY.md` when done.
</output>
