---
phase: quick-260729-lc9
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12]
files_modified:
  - .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
  - .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
  - plugins/lz-tdd/skills/lz-red/SKILL.md
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  - plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
  - plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
  - plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
  - .planning/research/test-double-taxonomy.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RESEARCH.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN-CHECK.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN-CHECK-2.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN-CHECK-3.md
  - .planning/STATE.md

must_haves:
  truths:
    - "D-01 D-02: no file named test-double-taxonomy.md exists anywhere under plugins/, and the battery FAILS by name if one reappears."
    - "D-03 D-12: lz-red ships NO test-double reference; the never-bare-stub rule survives inline in lz-red/SKILL.md and lz-tpp/SKILL.md, each carrying the phrase which side they mean."
    - "D-08: deleting a data cell together with its pipe from any table in the shipped tree or in the archived copy makes the battery FAIL naming the table's start line and the ragged row's line."
    - "Deleting or mistyping any relative markdown link target under plugins/ makes the battery FAIL by file:line -- a class that does not exist in the battery today."
    - "D-04: the archived copy states it is an inert record, not accepted, not an agent input, and names what is still open in it."
    - "D-05: the record-then-inspect double in message-matrix.md is named a Test Spy; the string Mock rule occurs nowhere under plugins/; the mockist label is attributed to Fowler at both its sites."
    - "D-07: the Beck side of the live conflict cites the surface that actually carries it, not the rows above it."
    - "check-red-references.mjs and row-guards.selftest.mjs both exit 0; claude plugin validate . passes; git status clean."
  artifacts:
    - .planning/research/test-double-taxonomy.md
    - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md
    - .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
  key_links:
    - "EXPECTED_CHECKS, NEW_LABELS and RETIRED_LABELS must move in lockstep with every guard added and retired, or the roster gate fails for the wrong reason."
    - "row-guards.selftest.mjs asserts RETIRED_LABELS length AND shape, ROW_SCOPED_GUARDS key set, COUNT_GUARDS key set, and OLD_NEEDLES key parity -- all four break when the taxonomy guards retire."
    - "G17 stays scoped to plugins/ while the shape guard reaches the one .planning/ path; the two scopes must stay textually separate (H8)."
    - "scanTables and parseRows must split cells through ONE shared helper, or the shape guard and the row parser can disagree about a row's width."
---

<objective>
Scope the test-double taxonomy to lz-red only by removing it from the shipped plugin entirely,
archive it as an inert record, harden the table instrument with two general guards, fix three
content correctness errors, and re-scope the checker battery to match.

Purpose: the artifact violated a standing owner constraint at its introduction (D-01) and survived
three acceptance gates because nothing enforced the constraint. This task removes the artifact from
the deliverable, makes the constraint machine-enforced (D-02), and closes the dead-link class that
the removal itself creates.

Output: 6 atomic commits (5 if the tree is already clean when Task 1 starts, which it is not today).
Commit 1 is Task 1's planning-artifact commit (no code, and the only optional one); commits 2 and 3
leave the battery deliberately RED with an exactly enumerated failure set; commit 4 returns it to GREEN
at 125 checks; commit 5 deletes the surface commit 4 left unreferenced, at the same 125; commit 6
finishes the archive. Task 4's verify pins each of the five MANDATORY commit subjects at exactly one
occurrence and bounds the conventional-type total at six -- which is both the commit accounting and the
anti-vacuity control for the two commit-scoped hygiene legs, with no branch-dependent literal for an
executor to edit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md
@.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RESEARCH.md
@.planning/.continue-here.md
@AGENTS.md
</context>

<decisions_coverage>
## Locked decisions and where each is implemented

- **D-01:** the taxonomy is used by lz-red only. Task 3 deletes all three shipped copies; guard N3
  (Task 1) makes the constraint machine-enforced instead of prose-enforced.
- **D-02:** no byte-identical copies ever again. Task 3 deletes the sha256 gate OUTRIGHT (not
  narrowed, not archived) and replaces the three-copies rationale at BOTH duplicated comment sites
  with the forward constraint.
- **D-03:** the how-and-why leaves the shipped tree. Task 3 moves the whole 522-line document out of
  plugins/ to .planning/research/.
- **D-04:** the archived copy is an inert record, never an agent input. Task 4 writes the header. No
  agent definition under .claude/agents/ is edited by any task in this plan.
- **D-05:** narrow reading plus the three real correctness errors. Task 2 fixes exactly those three
  and adds no vocabulary guard.
- **D-06:** authoritative pattern names stay verbatim. Task 2 changes only the label word and the one
  mislabelled example; the eight correct "one warranted double" sites and every framework API name
  are left alone.
- **D-07:** fix content blocker C-B1 now. Task 4 re-sources the Beck side of the live conflict.
- **D-08:** the general table-shape guard replaces the narrow row-count repair. Task 1, guard N1.
- **D-09:** the lz-refactor "refactor is overloaded" parallel is an observation, not a work order.
  Nothing in lz-refactor changes beyond the one deletion D-01 requires.
- **D-10:** do not investigate the shared-reference mechanism. Tasks 3 and 4 RECORD the forward
  constraint in prose and in guard N3. No spike, no prototype, no validation of the mechanism.
- **D-11:** the deleted absence rule is reinstated in the archived copy. Task 4.
- **D-12:** the end state is the no-artifact baseline arm of a future A/B. Task 3 leaves lz-red with
  the inline rule and no reference file. That is not a regression to patch. Authoring the runtime
  intent-disambiguation artifact and its A/B is OUT of scope for this task.
</decisions_coverage>

<ordering_resolution>
## The two ordering hazards this plan resolves explicitly

**H1 -- the link guard's RED proof is destroyed by the deletion.** The only two failing cases today
are `plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md:289` and
`plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md:289`, both pointing at
`principle-backing.md`, which exists only under `lz-red/references/`. Task 3 deletes both files.
**Resolution: Task 1 builds and runs the link guard and records those two hits as its RED baseline,
before any deletion.** Re-measured this session, fence-aware: 1014 inline links across 197 files,
0 inside code fences, exactly 2 unresolved.

**H2 -- step 4 says "verbatim" while steps 1, 6 and D-11 all edit that same text.** VERBATIM MEANS
"COPIED WITHOUT REWRITING", NOT "restored to its pre-edit bytes". **Resolution: the move happens
FIRST (Task 3), as a pure `git mv` rename with no content change; every content edit to that
document then lands on the moved copy in place (Task 4).** Do NOT re-apply the C-B1 fix, the D-11
reinstatement or the S1-S4 sweep to the shipped copy before moving it -- editing one shipped copy
alone breaks the byte-identity gate, and editing all three is waste on files that are deleted in the
same commit. This is the single most likely executor misreading; if you find yourself editing
`plugins/**/test-double-taxonomy.md`, stop.

**H3, H6 -- the deletion and the battery re-scope are ONE commit, Task 3's FIRST.** Deleting the
taxonomy makes 43 checks vanish SILENTLY (the FILES loop skips a missing file); only 15 fail loud. A
battery run in that window is not a signal. So the deletion, the FILES-entry removal, the retirements and
the selftest repairs all land together in commit 3a, after which the battery is a real signal at 125.

**Task 3's SECOND commit is separable and is the one split that exists.** Commit 3a also drops the
checker's `COUNT_GUARDS` import and every count-guard call site, so nothing the battery reads references
that surface afterwards. Commit 3b then deletes the export, the nine guard bodies, their helpers and the
selftest's orphaned count-guard block -- and the emitted count is 125 both before and after it, which is
the mechanical proof that only unreferenced code went. What is NOT separable is the reverse order: never
delete an export before its consumers.

**H9 -- a `topics` needle containing a newline can never match.** `check-red-references.mjs:549` splits
the file into lines and `:552` tests each line, so guard N9's region-scoped multi-line needle is
structurally unmatchable against the shipped evaluator -- RED even when the content is right. Task 1's
step B0 adds a one-line, strictly additive per-entry whole-text opt-in and sets it on N9 alone.
Measured through the real evaluator, not the regex: without B0, in-procedure placement still FAILS. This
is the `.continue-here.md` row "Inferring behavior from structure", and it cost this task an iteration.

**H4 -- the D-05 fix is over-wide by default, and the battery structurally cannot catch it.** The
phrase family is enumerated site by site in Task 2's blast-radius block; re-measure it before editing.
The reason the battery is blind is STRUCTURAL, not a matter of counting: the only topic whose LABEL
mentions the phrase is `check-red-references.mjs:209`, and its regex is
`/expect[ -]to[ -]send|expect to send/i` -- it never matches `warranted` or `double` at all. So NO
rename of that phrase at ANY site can redden it, at any site count. Task 2's per-site verify pins are
the only protection, so every non-target site carrying any form of the phrase must be pinned.

**H8 -- keep the two tree scopes textually separate.** G17 walks `plugins/` only and its
`TAXONOMY_BASENAME` filter is deleted. The shape guard walks `plugins/` plus the ONE
`.planning/research/test-double-taxonomy.md` path. Do not reuse one constant for both.

**Deliberate RED windows.** After Task 1 the battery exits 1 with EXACTLY 8 failures; after Task 2,
EXACTLY 4. Both sets are enumerated in the task's `<done>`. This is the instrument-first discipline
this workspace already uses (see `260728-wev-RED-BASELINE.md`), and the exact-count assertion is
what stops a real failure hiding inside a designed one.
</ordering_resolution>

<tasks>

<task type="auto">
  <name>Task 1: Instrument first -- add nine guards, prove each can fail, record the RED baseline</name>

  <files>
.claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
.claude/skills/lz-red-workspace/tools/check-red-references.mjs
.claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN.md
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RESEARCH.md
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN-CHECK.md
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN-CHECK-2.md
.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-PLAN-CHECK-3.md
.planning/STATE.md
  </files>

  <action>
**STEP ZERO -- COMMIT THE PLANNING ARTIFACTS FIRST. This task owns them.** Without this the plan
DEADLOCKS: Task 4's final leg requires `git status --porcelain` empty, and the four planning artifacts
plus `STATE.md` appeared in no task's `<files>` and no commit message. Task 1 therefore makes TWO
commits, and this is the first:

**DERIVE THE FILE LIST FROM THE TREE, do not copy a list from this plan.** The plan-check reports
accumulate one per revision iteration, so the set is not knowable when this plan is written -- at
authoring time `260729-lc9-PLAN-CHECK-2.md` and `260729-lc9-PLAN-CHECK-3.md` were both present and
UNTRACKED, and a later iteration adds `-4`. An add-list frozen into this plan leaves them untracked, and
Task 4's final leg then requires an empty `git status --porcelain` that can never be reached. That is a
deadlock, and it is the reason this step reads as a procedure rather than as a command:

```
git status --porcelain -- .planning/
```

Then `git add` every path that command lists, each spelled out BY NAME on the command line. Expect the
plan, its CONTEXT, its RESEARCH, EVERY `260729-lc9-PLAN-CHECK*.md` report, and `.planning/STATE.md`.

Commit: `docs(quick-260729-lc9): commit the revised plan and its planning artifacts`

Notes. Stage BY NAME -- never `git add .`/`-A`/`-u`, and never a bare directory path. If
`git status --porcelain -- .planning/` lists anything that is NOT a planning artifact of this task, STOP
and report it rather than sweeping it in. Some paths were already committed by the WIP commit and may be
unchanged; a path with nothing staged is simply absent from the commit, which is fine.
`260729-lc9-RED-BASELINE.md` is NOT in this commit -- Task 1's second commit creates it.

**Clean-tree fallback, and it now requires NO verify edit (NB-5).** If `git status --porcelain` is
already empty when Task 1 starts, skip this commit and record the skip in the SUMMARY. **Do NOT edit any
verify literal.** Task 4's chain no longer carries a total commit count: it pins the five MANDATORY
subjects individually and bounds the conventional-type total at six, and this commit is the optional
sixth. So both branches -- six commits with step zero, five without -- satisfy the same unmodified
chain, and a MISSING mandatory commit still fails its own pin. Granting an executor permission to edit a
verify literal is latitude a verify block must never have, so that permission is withdrawn.

Then the instrument work, which is this task's second commit.

Add NINE emitted checks. Nothing in `plugins/` is edited by this task. `EXPECTED_CHECKS` moves
174 -> 183 with the arithmetic written into the comment above it, in the style already there.

**A. `lib/pipe-table.mjs` -- one shared splitter and two new exports.**

1. Extract the cell split into ONE internal helper used by BOTH `parseRows` and the new
   `scanTables`. **Name it `splitCells`, verbatim** -- the verify pins that identifier at THREE or more
   occurrences in this module, which is a definition plus a call from each of the two consumers, and it
   is the only mechanical trace the sharing leaves. It must be aware of a backslash-escaped pipe, which
   today's `line.split("|")` at `:47` miscounts. Measured: `plugins/**/*.md` has ZERO escaped pipes so
   behaviour is unchanged today, but `.planning/research/STACK.md` has three rows that this exact bug
   would report ragged, which is proof the class is live in this repo. Load-bearing reason for sharing:
   if the shape guard and the row parser disagree about a row's width, the guard passes while the parser
   silently SKIPS the row -- the delimiter-evasion defect class this instrument exists to close.

   What the count leg proves: the name is defined and used at least twice. What it does NOT prove: that
   the two uses are inside `parseRows` and `scanTables` respectively. That reading belongs to
   `<orchestrator_followup>` item 4, whose scope this revision widens to cover it.
2. Export `scanTables(text) -> { tables, offenders }`. Algorithm, exactly as measured in RESEARCH
   Q1: split on `/\r?\n/`; fence-aware via `/^\s{0,3}(`{3,}|~{3,})/` where the same fence character
   closes and a fenced line also CLOSES any open table; a line is a table row iff it starts with a
   pipe (the same predicate `parseRows` uses); width via the shared splitter; the first pipe row of
   a run sets the width; any later row of a different width is an offender; a non-pipe line closes
   the table. Offender string VERBATIM:
   `table at line 233: header is 9 wide, but 1 row(s) differ -- line 237 is 8`.
   Return the table COUNT alongside the offenders -- that is what makes the checker's anti-vacuity
   leg testable.
3. Export `findLinkTargets(text) -> {line, raw, kind}[]`, pure and disk-free so classification is
   fixture-testable. Extract with `/\[[^\]]*\]\(([^)\s]+)(?:\s+"[^"]*")?\)/g` per line. Kinds:
   `anchor` for a target starting with `#`; `scheme` for `/^[a-z][a-z0-9+.-]*:/`; `absolute` for a
   leading `/`; `relative` otherwise. Use the SAME fence helper -- measured 0 fenced links today, so
   this costs nothing and closes the class permanently.
4. Update the module header, but ONLY for what is true AFTER THIS COMMIT. Add the two new exports and
   the shared splitter to the header's description of the module. **Both export NAMES must appear in the
   header COMMENT BLOCK, which is everything above `const SEPARATOR_CELL_RE`** -- the verify pins each
   one with a region-scoped needle bracketed between the header's own opening `ROW PARSER` and that
   `const` line, so a name that appears only at its `export function` site (below the bracket) does NOT
   satisfy it. Measured: both bracket keys occur exactly once, and a needle whose first occurrence is
   below the closing key provably fails the form (probed with `columnCount`, exit 1), so the bracket is
   load-bearing rather than decorative. **PRESERVE only the TOKEN `ROW PARSER` on the header's first
   line** -- that token is the bracket's opening key, so losing it would make both legs false-fail on
   correct work. **You MUST rewrite the REST of that line.** It currently reads
   `One exported pure function over a GitHub pipe table`, and THIS commit adds more exports, so keeping
   the line verbatim would ship a stale claim inside the very commit that falsifies it -- the same
   same-commit stale-claim class this task exists to close, and the earlier "keep as-is" wording
   ORDERED that defect (F-1). Keep the token; correct the export count. The splitter SENTENCE is not separately pinned --
   the `splitCells` count leg in A1 covers the splitter's existence, and prose quality is the reviewer's
   job. **Do NOT rewrite the `:16-18` measurement in this task.** That measurement cites the taxonomy as one of two targets, and the
   taxonomy IS still a target of this module until Task 3 moves it -- rewriting it here would make the
   header describe a scope that is not yet true for commits 1 and 2, which is the same stale-claim
   class this task exists to close, just pointed at the future instead of the past. The `:16-18`
   rewrite belongs to Task 3, which is where the file actually leaves `plugins/`; Task 3's step for it
   is already written ("confirm the header no longer names the taxonomy as a target of the module") --
   change that step to OWN the rewrite rather than confirm it. The `principle-backing.md` measurement
   at `:19-23` survives untouched in both tasks.

**B. `check-red-references.mjs` -- one evaluator fix (B0), five post-loop blocks, four FILES-entry
guards.**

Place N1, N2, N3 next to the G17 block and REUSE its existing `collectMarkdown(dir)` helper. Do not
write a second directory walker. Each is ONE `report()` call with a by-file-and-line detail string,
the G17 idiom. Each FAILS CLOSED on an unreadable file or tree -- push a hit, never filter.

- **N1** label `[lc9] no ragged pipe table`. Scope `plugins/` ONLY for now; Task 3 adds the one
  `.planning/` path. Anti-vacuity leg, and it is load-bearing: also FAIL when `tablesSeen === 0`.
  Without it a wrong path yields 0 files -> 0 offenders -> permanent PASS, the guard-that-cannot-fail
  class this file's own header condemns. Say in the comment that the pure function CANNOT fail on
  empty text -- "no ragged tables" is true of an empty document -- so the anti-vacuity control lives
  here on the table count and not in the selftest, unlike every guard in `row-guards.mjs`.
- **N2** label `[lc9] every relative markdown link resolves`. Scope `plugins/` only: the exit gate's
  own wording is "no dead relative links in any shipped SKILL.md or reference", and the archived
  copy's own link is handled as CONTENT in Task 4. Skip `anchor`, `scheme` and `absolute` kinds;
  split a relative target at its first `#` and resolve the file part against `path.dirname(file)`
  with `fs.existsSync`. The fragment split is REQUIRED, not optional -- the lz-refactor catalogs
  carry file-plus-fragment links. Anti-vacuity leg: FAIL when `linksSeen === 0`. Record in the
  comment that an allowlist was MEASURED unnecessary rather than forgotten -- but record only the
  ZEROES, which are the load-bearing facts and are invariant across Task 3: **0 external-scheme links,
  0 absolute links, 0 reference-style link definitions, 0 links inside code fences.** Those four zeroes
  are why no allowlist is needed.

  **DO NOT write the total link, file or anchor counts into the comment.** They are exact today and
  Task 3 falsifies every one of them: measured, the census moves from 197 files / 1014 inline / 984
  relative / 30 anchor to 194 / 990 / 981 / 9 the moment the three copies go. The prior revision wrote
  those totals into this very comment and gave Task 3 no sweep for them -- the plan reintroducing,
  inside its own instrument, the exact stale-claim class its step-6 sweep exists to close. The nonzero
  magnitude is already asserted MECHANICALLY by the `linksSeen === 0` anti-vacuity leg, every run, which
  is strictly better than a literal in a comment. Same rule for N1: its comment records `0 ragged` and
  says the table count is asserted nonzero by the anti-vacuity leg; it does NOT record 31 tables or 405
  pipe rows, both of which become 28 and 297.

  **General rule for every comment this task writes into `check-red-references.mjs`, `row-guards.mjs`
  and `pipe-table.mjs`:** a number that the same commit set will change is a defect, not documentation.
  Write the invariant (a zero, a direction, a reason) or write nothing and let a guard derive it.

  **The two anti-vacuity counters are MANDATED IDENTIFIERS: `tablesSeen` for N1 and `linksSeen` for N2,
  spelled exactly so.** The threat register (T-lc9-03) claims both guards carry an explicit anti-vacuity
  leg on their own seen-count, and until this revision nothing checked that the counters existed at all.
  The verify pins at least TWO lines in the checker mentioning either identifier -- a floor, since each
  counter needs at minimum an increment and a comparison. What it does NOT prove: that the comparison
  feeds the guard's `report()`. That reading is `<orchestrator_followup>` item 4.
- **N3** label `[lc9] no test-double taxonomy copy in the shipped tree`. FAIL if any file under
  `plugins/` has that basename, listing each. This is what makes D-01 and D-02 machine-enforced
  rather than prose-enforced; the constraint was violated once already and survived three acceptance
  reviews precisely because nothing checked it.
- **N7** label `[lc9] mockist label attributed to Fowler at both sites`. ONE report over TWO files:
  require the phrase `Fowler's label` in `anti-patterns.md` AND in `principle-backing.md`. One
  report because it is one claim restated in a dependent, the same reasoning the existing
  `owned-source count re-derived across both files` check records.
- **N8** label `[lc9] lz-tpp/SKILL.md carries the side-qualification rule inline`. Require the phrase
  `which side they mean` in the shipped `lz-tpp/SKILL.md`. Purpose: G17 is an ABSENCE gate, so
  deleting the rule outright passes it. This makes the rule's PRESENCE mandatory in the skill that
  fills the empty production symbol -- measured, `lz-tpp/SKILL.md:93,95` are the ONLY occurrences of
  the contested word or of `production-side` in that entire tree.

Four FILES-entry guards:

- **N4** on the `testing-stance/message-matrix.md` entry, a `topics` guard labelled
  `[lc9] Test Spy named for the record-then-inspect double`, needle `/test spy/i`.
- **N5** on the same entry, an `absent` guard labelled `[lc9] no Mock rule label`, needle
  `/Mock rule/i`.
- **N6** on the `testing-stance/functional-core.md` entry, an `absent` guard with the same label text
  and needle.
- **N9** on the `SKILL.md` entry (the lz-red router, `dir`-overridden), a `topics` guard labelled
  `[lc9] side-qualification rule inline in the coach procedure`, needle **REGION-SCOPED to the coach
  procedure**: `/which side they mean[\s\S]*?\n## Reference material/i`, **and carrying the per-entry
  flag `wholeText: true`, which section B0 below adds to the evaluator. Without that flag this guard
  CANNOT PASS -- see B0.**

  The needle MUST NOT be the bare `/which side they mean/i`. The label claims a PLACEMENT -- "in the
  coach procedure" -- and a file-wide needle is satisfied by an appendix placement, which is the
  "needle outlives its subject" BLOCKING anti-pattern in `.continue-here.md`. `## Reference material`
  (`lz-red/SKILL.md:141`) is the stable key that closes the `## Coach decision procedure` slice
  (`:52-140`), so requiring the phrase to occur BEFORE that heading pins the placement the label
  asserts.

  **THE NEEDLE SHAPE IS ITSELF LEGGED (F-3), and this closes a real escape.** Legs 9 and 10 pin only
  the FLAG (`topic.wholeText`, `wholeText: true`); leg 14's 8-`[FAIL]` count is identical either way;
  and leg 25 pins only the RED-BASELINE prose. So an executor who used the FORBIDDEN bare needle
  together with `wholeText: true` would have passed EVERY leg in the plan -- the exact overclaim this
  section forbids, protected by nothing but this paragraph. Block 1 leg 26 now asserts
  `## Reference material` is present in the checker SOURCE at count >= 1. MEASURED: absent today, so
  the leg is RED (exit 1), and the `-ge 1` direction is fail-closed on a mistyped path. The bare needle
  contains no such literal, so it cannot satisfy leg 26.

  **Discrimination, MEASURED THROUGH THE REAL EVALUATOR -- not against the regex in isolation.** The
  probe patched the `wholeText` line into a throwaway worktree copy of `check-red-references.mjs`,
  injected this exact topic into the `SKILL.md` FILES entry, wrote three variants of the live
  `lz-red/SKILL.md`, ran `node check-red-references.mjs` for each, and read the emitted `[PASS]` /
  `[FAIL]` line for this label. Four matcher-x-needle combinations, twelve runs:

  | Matcher x needle | absent | rule IN the coach procedure | rule ONLY in the appendix |
  |---|---|---|---|
  | per-line (evaluator AS SHIPPED) + region needle | FAIL | **FAIL -- the deadlock** | FAIL |
  | `wholeText: true` + region needle (THIS PLAN) | FAIL | **PASS** | **FAIL** |
  | `wholeText: true` + BARE needle (FORBIDDEN) | FAIL | PASS | **PASS -- the overclaim** |
  | per-line + BARE needle | FAIL | PASS | **PASS -- the overclaim** |

  Row 1 is why B0 exists: with the shipped per-line matcher this guard is RED-ALWAYS, so Task 3's
  "battery exits 0" is unreachable and Task 4 inherits the stall. Row 2 is the required behaviour and
  the ONLY row that discriminates all three ways. Rows 3 and 4 are the tempting escape and are
  FORBIDDEN: they ship exactly the placement overclaim this guard's label would then be lying about.

**B0. ONE-LINE EVALUATOR FIX, and this task OWNS it. Do it before adding N9.**

`check-red-references.mjs:549` splits the file with `text.split(/\r?\n/)` and `:552` matches
`lines.some((line) => topic.re.test(line))`. The split strips every newline, so a needle CONTAINING a
newline can never match any element of `lines` -- a `topics` guard is structurally per-line. N9 is the
first multi-line needle in this mechanism (searching the checker for the `[\s\S]` idiom returns
nothing), so the mechanism has to admit one. Replace `:552` with:

`const hit = topic.wholeText ? topic.re.test(text) : lines.some((line) => topic.re.test(line));`

Three constraints, all load-bearing:

1. **Strictly additive.** Every existing topic leaves `wholeText` undefined and keeps the per-line
   path byte-for-byte. Verified in the probe: with the flag set on N9 alone, no other emitted line
   changed its verdict.
2. **Set `wholeText: true` on N9 AND NOTHING ELSE.** A blanket flip to whole-text would loosen every
   one of the ~100 pre-existing per-line topics -- a multi-token needle would start matching across a
   line break. The verify pins the flag's occurrence count at exactly ONE for this reason.
3. **Do NOT touch `:591`**, the `absent`-guard matcher. It is per-line for the same reason and no
   `absent` guard in this plan carries a multi-line needle.
4. **Comment-text discipline.** The verify pins the literal `wholeText: true` at exactly ONE occurrence
   in `check-red-references.mjs`, because that count IS the "only N9 opts in" constraint. So do NOT
   write that literal into any comment in that file. Explain the flag by concept ("the per-entry
   whole-text opt-in, set on N9 alone") and let the code carry the literal. Same for
   `topic.wholeText`, pinned at exactly one.

Why not the alternatives, recorded so the choice is not silently re-opened. Weakening N9 to the bare
needle ships the row-3 overclaim. Flipping the shared matcher to whole-text is unauthorized surgery on
machinery serving ~100 topic and 14 `absent` guards. Reimplementing N9 as a post-loop block works but
loses the `SKILL.md: ` composed prefix that Task 1's and Task 2's `<done>` both assert.

Add all nine composed labels to `NEW_LABELS`. Compose each EXACTLY as emitted: the FILES loop emits
`<entry name>: <label>`, so the message-matrix and functional-core labels carry the
`testing-stance/` path segment, and a post-loop label is verbatim.

**C. `row-guards.selftest.mjs` -- the fixture proof for what has no baseline failure.**

Extend the existing file; do NOT create a second selftest. Its stated invariant, IN-MEMORY FIXTURES
ONLY, is exactly right for these. Add two sections:

- `lib/pipe-table.mjs -- scanTables`, six cases: (1) a well-formed 3-column table -> 0 offenders;
  (2) header 3 wide with a data row 2 wide because a pipe was DELETED -> exactly 1 offender with the
  verbatim string `table at line 1: header is 3 wide, but 1 row(s) differ -- line 3 is 2`; (3) a row
  whose cell is BLANKED with the pipe KEPT -> 0 offenders, because content emptiness is a different
  guard's job and the two mutations render identically in GFM; (4) a ragged-looking table inside a
  fence -> 0 offenders; (5) two tables of different widths separated by a blank line -> 0 offenders;
  (6) `scanTables("")` -> `{ tables: 0, offenders: [] }`, with a comment stating this is why the
  anti-vacuity control lives on the table count in the checker.
- `lib/pipe-table.mjs -- findLinkTargets`, one case per kind (`anchor`, `scheme`, `absolute`,
  `relative`) plus a file-with-fragment target and a link inside a fence -- six cases.
- One more `parseRows` case: a row containing a backslash-escaped pipe parses to the intended width.

**MANDATED LABEL SHAPES, because the verify block pins them and the whole fixture set is otherwise
unlegged.** Measured against the shipped selftest: `check()` prints `  [PASS] <label>` on success and
prints the offender string ONLY on FAILURE (the `[FAIL]` branch JSON-dumps actual/expected). So a PASS
line reveals nothing beyond its own label, and the verbatim offender string never reaches stdout on a
green run. Two consequences, both binding:

1. Every `scanTables` case's label MUST begin `scanTables: ` and every `findLinkTargets` case's label
   MUST begin `findLinkTargets: `, one `check()` call per enumerated case (six each). The verify
   asserts at least six `[PASS]` lines under each prefix, so a section written but silently empty, or
   omitted entirely, fails.
2. The new `parseRows` case's label MUST contain the words `escaped pipe`.
3. The verbatim offender string is asserted against the SELFTEST SOURCE, not its output -- exactly one
   occurrence of
   `table at line 1: header is 3 wide, but 1 row(s) differ -- line 3 is 2`
   in `row-guards.selftest.mjs`.

   **What that pair proves, stated exactly (NEW-4).** Source-presence plus `node row-guards.selftest.mjs`
   exiting 0 is a FLOOR: it kills the "wrote nothing" case, which was the actual blocker, and it is why
   the string must be pinned somewhere given that a PASS line never carries it. It does NOT prove that
   `scanTables` PRODUCES the string -- the literal could sit in a comment, or be hardcoded on both sides
   of one `check()`, and both legs still pass. The earlier wording here claimed a proof; the claim is
   withdrawn. This is the same residue the step-12 label carries ("cannot prove the assertion has
   teeth"), and the offender-string fixture is therefore named inside `<orchestrator_followup>` item 2,
   which already reads this file.

**Why any of this is needed: OBSERVED, the UNMODIFIED selftest exits 0 today.** A bare
`node row-guards.selftest.mjs` leg is therefore satisfied by an executor who writes ZERO of these
fixtures -- and this fixture set is guard N1's ONLY accepted falsifiability proof (a CONTEXT hard
constraint, and blocking item 3 in `.continue-here.md`). Do not weaken these four legs; they are the
proof, not decoration.

**D. `260729-lc9-RED-BASELINE.md`** in this task's directory. This file IS the falsifiability record for
all nine guards, so its verify leg asserts its CONTENT, not its existence -- a `touch` must not satisfy
it. Required shape: a markdown table with exactly NINE data rows whose FIRST CELL is the guard id
`N1` .. `N9`, one row per guard, each row carrying the composed label, the PROOF KIND, and the evidence.

**Evidence form depends on the guard's DIRECTION, and demanding a `file:line` from all of them is
unsatisfiable.** The prior revision did exactly that and it is the same self-contradiction shape that
cost an earlier round a cycle. Three forms, and each guard takes exactly one:

| Direction | Guards | Proof kind | Evidence form |
|---|---|---|---|
| ABSENCE-required (fails because the subject IS there) | N2, N3, N5, N6 | `RED-at-baseline` | a `file:line` per hit |
| PRESENCE-required (fails because the subject is NOT there) | N4, N7, N8, N9 | `RED-at-baseline` | "phrase absent from `<file>`, measured count 0" -- there is no line to cite |
| Invariant-GREEN | N1 | `invariant-GREEN + fixture set` | the fixture list |

**N9 carries ONE extra evidence field, and the verify pins a literal from it.** Its label asserts a
PLACEMENT, so "absent, measured count 0" proves only half of it. Its row must also record the
three-way discrimination measured THROUGH THE REAL EVALUATOR (the table under N9 above), using the
verbatim tokens `absent -> RED`, `in-procedure -> GREEN` and `appendix-only -> RED`. The last one is
the load-bearing token: it is the only evidence that the guard rejects the placement its label says is
wrong, and the verify greps for it. Do NOT record a discrimination result measured against the regex in
isolation -- that is precisely the error this iteration exists to correct, and it is the
`.continue-here.md` row "Inferring behavior from structure".

Measured this session, so the rows can be written without re-deriving: N2 has 2 located hits (both
`test-double-taxonomy.md:289`, in the lz-refactor and lz-tpp copies); N3 has 3 located hits (the three
copy paths); N5 has 4 (`message-matrix.md:38,47,57,67`); N6 has 2 (`functional-core.md:42,44`). And the
four presence-required guards each measure count 0: `/test spy/i` in `message-matrix.md`,
`Fowler's label` in BOTH `anti-patterns.md` and `principle-backing.md`, and `which side they mean` in
`lz-tpp/SKILL.md` and in `lz-red/SKILL.md`.

For N1, do NOT manufacture a baseline failure -- measured AT TASK 1 TIME, `plugins/**` has 31 tables,
405 pipe rows and 0 ragged, so the guard correctly starts GREEN and the fixture set IS its proof. Do
not demand both proof kinds of any guard. Those two totals are a DATED SNAPSHOT for the RED-BASELINE
record, correct only for this commit; Task 3 moves them to 28 and 297, which is exactly why they must
not be copied into any instrument comment (see the general rule under N2).

Also record the exact 8-failure set this commit leaves behind.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs > /tmp/lc9-st1.txt && test "$(rg -c '^  \[PASS\] scanTables: ' /tmp/lc9-st1.txt || echo 0)" -ge 6 && test "$(rg -c '^  \[PASS\] findLinkTargets: ' /tmp/lc9-st1.txt || echo 0)" -ge 6 && test "$(rg -c '\[PASS\].*escaped pipe' /tmp/lc9-st1.txt || echo 0)" = "1" && test "$(rg -c -F -e 'table at line 1: header is 3 wide, but 1 row(s) differ -- line 3 is 2' .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs || echo 0)" = "1" && test "$(rg -c -F -e 'splitCells' .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs || echo 0)" -ge 3 && rg -U -q 'ROW PARSER[\s\S]*?scanTables[\s\S]*?const SEPARATOR_CELL_RE' .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs && rg -U -q 'ROW PARSER[\s\S]*?findLinkTargets[\s\S]*?const SEPARATOR_CELL_RE' .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs && test "$(rg -c -F -e 'topic.wholeText' .claude/skills/lz-red-workspace/tools/check-red-references.mjs || echo 0)" = "1" && test "$(rg -c -F -e 'wholeText: true' .claude/skills/lz-red-workspace/tools/check-red-references.mjs || echo 0)" = "1" && test "$(rg -c -e 'tablesSeen|linksSeen' .claude/skills/lz-red-workspace/tools/check-red-references.mjs || echo 0)" -ge 2 && ! node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > /tmp/lc9-t1.txt && test "$(rg -c '^\s+\[FAIL\]' /tmp/lc9-t1.txt)" = "8" && rg -q '183 checks, equal to the PREDICTED literal' /tmp/lc9-t1.txt && test "$(rg -c 'lz-refactor.references.test-double-taxonomy.md:289' /tmp/lc9-t1.txt)" = "1" && test "$(rg -c 'lz-tpp.references.test-double-taxonomy.md:289' /tmp/lc9-t1.txt)" = "1" && rg -q 'no ragged pipe table' /tmp/lc9-t1.txt && test "$(rg -c '\[PASS\].*no ragged pipe table' /tmp/lc9-t1.txt)" = "1" && test "$(rg -c '^\| N[1-9] .*\[lc9\]' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md)" = "9" && test "$(rg -o -N --no-filename '^\| N[1-9] ' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md | sort -u | wc -l)" = "9" && rg -q -F 'invariant-GREEN' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md && rg -q -F 'RED-at-baseline' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md && rg -q -F 'measured count 0' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md && rg -q -F 'test-double-taxonomy.md:289' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md && rg -q -F 'appendix-only -> RED' .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-RED-BASELINE.md && test "$(rg -c -F -e '## Reference material' .claude/skills/lz-red-workspace/tools/check-red-references.mjs || echo 0)" -ge 1</automated>

    <verify_leg_notes>
The RED-BASELINE legs replace a bare `test -f`, which a `touch` satisfied. That file carries the proof
kind and the evidence for all nine guards -- the entire falsifiability record -- so its CONTENT is
asserted. Six sub-legs: nine guard-id rows each carrying an `[lc9]` label; nine DISTINCT ids `N1`..`N9`;
both proof-kind strings present; the presence-required evidence form (`measured count 0`) present, which
is what stops the four PRESENCE-required guards being fudged into a fabricated `file:line`; and N2's
actual located hit `test-double-taxonomy.md:289`, which stops the ABSENCE-required guards being fudged
the other way into a bare "count 0".

OBSERVED EXIT CODES, run verbatim against mock records this session:

| Subject | exit |
|---|---|
| A record of the required shape | 0 |
| `touch`-created empty file | 1 |
| Absent path | 1 |
| Eight rows instead of nine | 1 |
| Nine rows but no `measured count 0` evidence anywhere | 1 |

The first leg is deliberately `^\| N[1-9] .*\[lc9\]` rather than a file-wide `rg -c '\[lc9\]'`: a
file-wide count breaks the moment the record's own prose mentions `[lc9]`, which was verified to happen
in the mock. Row-anchoring makes it immune.

**FIVE legs added across the last two iterations. Block 1 goes 21 -> 26 legs (25 `&&`) -- the fifth is
leg 26, the F-3 N9 needle-SHAPE pin. OBSERVED exit against the
unmodified tree still 1, first failing at leg 2 (the `scanTables: ` PASS count).** Before this revision
`lib/pipe-table.mjs` was touched by NO leg in ANY of the four blocks, so sections A1 and A4 -- half this
task's file A work -- were entirely unobserved. Each new leg run verbatim in isolation:

| Leg | What it asserts | Observed | Fail direction |
|---|---|---|---|
| 6 | `splitCells` occurs >= 3 times in `lib/pipe-table.mjs` (A1's ONE shared splitter) | **1** | fail-closed: `\|\| echo 0` makes a mistyped path read 0, which fails `-ge 3` -- probed, exit 1 |
| 7 | `scanTables` named in the header comment BLOCK, bracketed `ROW PARSER` .. `const SEPARATOR_CELL_RE` (A4) | **1** | fail-closed: `rg -q` on a missing path exits 2 and the leg is not negated -- probed, exit 2 |
| 8 | `findLinkTargets` named in the same bracket (A4) | **1** | same |
| 11 | >= 2 lines in the checker mention `tablesSeen` or `linksSeen` (T-lc9-03's anti-vacuity counters) | **1** | fail-closed, same form as leg 6 |

**Legs 7 and 8 are the only region-BRACKETED legs in this block, and the bracket is load-bearing.** Both
keys occur exactly once in the module. The closing key sits at `:28`, ABOVE every `export function`, so a
name that appears only at its export site cannot satisfy the pattern -- probed with `columnCount`, whose
first occurrence is `:34`: exit **1**. Positive control for the form, needle `FAILS CLOSED` which is
already inside the header: exit **0**. That is what makes these legs assert the HEADER rather than the
file.
    </verify_leg_notes>
  </verify>

  <done>
- `row-guards.selftest.mjs` exits 0, including the pipe-DELETION fixture that makes `scanTables`
  return exactly one offender with the verbatim string.
- `check-red-references.mjs` exits 1 with EXACTLY 8 `[FAIL]` lines, and they are exactly:
  `[lc9] every relative markdown link resolves` (2 hits, both `:289`),
  `[lc9] no test-double taxonomy copy in the shipped tree` (3 hits),
  `testing-stance/message-matrix.md: [lc9] Test Spy named for the record-then-inspect double`,
  `testing-stance/message-matrix.md: [lc9] no Mock rule label`,
  `testing-stance/functional-core.md: [lc9] no Mock rule label`,
  `[lc9] mockist label attributed to Fowler at both sites`,
  `[lc9] lz-tpp/SKILL.md carries the side-qualification rule inline`,
  `SKILL.md: [lc9] side-qualification rule inline in the coach procedure`.
- The roster gate PASSES at 183, proving no guard was dropped or duplicated while nine were added.
- `[lc9] no ragged pipe table` PASSES -- invariant-GREEN, proof is the fixture set, no baseline
  failure is manufactured for it.
- `260729-lc9-RED-BASELINE.md` carries nine guard-id rows `N1`..`N9`, each with an `[lc9]` label, both
  proof-kind strings, the `measured count 0` evidence form for the four PRESENCE-required guards, and
  N2's real located hit `test-double-taxonomy.md:289`. A `touch` does not satisfy this -- verified.
- `lib/pipe-table.mjs` names both new exports in its HEADER COMMENT BLOCK, and `splitCells` occurs at
  least three times so the split is shared rather than copied. Both are legged; before this revision this
  file was observed by no leg in any block. The `:16-18` measurement is deliberately NOT rewritten here
  -- that is Task 3 step 15, and Task 3's chain legs it.
- The checker names both anti-vacuity counters `tablesSeen` and `linksSeen`, so T-lc9-03's claimed
  mitigation has a mechanical trace instead of only prose.
- Nothing under `plugins/` changed: `git status --short plugins/` is empty.
- TWO commits from this task, in this order:
  1. `docs(quick-260729-lc9): commit the revised plan and its planning artifacts`
  2. `test(quick-260729-lc9): add nine guards with proofs, RED baseline recorded`
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix the three D-05 correctness errors, narrowly</name>

  <files>
plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md
plugins/lz-tdd/skills/lz-red/references/testing-stance/functional-core.md
plugins/lz-tdd/skills/lz-red/references/anti-patterns.md
plugins/lz-tdd/skills/lz-red/references/principle-backing.md
  </files>

  <action>
Per D-05 this settles NO new vocabulary and adds NO vocabulary guard. It fixes three defects that are
wrong regardless of any vocabulary question. Per D-06, authoritative pattern, smell and refactoring
names stay verbatim: do not touch `vi.fn`, `vi.spyOn`, `vi.mock`, "over-mocking", "mockist" as
Fowler's own term, "doubles as documentation", "double duty", or "One skeleton" at
`test-structure-and-assertions.md:24,28`.

**1. The mislabelled Test Spy.** `message-matrix.md:131-138` demonstrates a `vi.fn()` that records a
call which the test inspects afterwards with `toHaveBeenCalledWith`. Per the taxonomy that is a Test
Spy; a Mock Object is reserved for a double that CARRIES THE EXPECTATION ITSELF. Name it. Edit the
bullet at `:57` and the in-fence comment at `:137` so the artifact is called a Test Spy and the
distinction from a Mock Object is stated in one clause.

**BLAST RADIUS -- RE-MEASURE BEFORE YOU EDIT, then do not exceed it.** The prior round's site list was
asserted and wrong, so the numbers below are stated as the OUTPUT OF A NAMED COMMAND and every
non-target is pinned in the verify block. Run these three commands first and reconcile any
disagreement before touching a file; your measurement wins over this prose.

```
git grep -n -F 'warranted double' -- plugins/
git grep -n -F 'the one warranted double' -- plugins/
git grep -n -i -E 'warrant[a-z]*( a| the)?( one)? double' -- plugins/
```

Measured 2026-07-29 against this tree:

- The LITERAL `warranted double` occurs on **5 lines in 4 files**: `anti-patterns.md:38,166`;
  `test-structure-and-assertions.md:130`; `message-matrix.md:79,137`.
- The exact `the one warranted double` occurs on **4** of those 5 -- `anti-patterns.md:38` reads
  "that one warranted double".
- Four further lines carry a DIFFERENT phrasing and NOT the literal, so a literal grep misses them and
  a careless sweep still hits them: `anti-patterns.md:128` ("the one warranted BOUNDARY"),
  `message-matrix.md:57` ("the ONE cell that WARRANTS A double"), `message-matrix.md:73` ("only one of
  them WARRANTS A double"), `vitest-typescript-mechanics.md:66` ("genuinely WARRANTS A double").

**IN SCOPE, exactly two lines:** `message-matrix.md:57` (its `Mock rule:` label word, per fix 2 below,
plus the Test Spy naming) and `message-matrix.md:137` (the in-fence comment). `:137` is the ONLY
literal-phrase site in scope; `:57` is edited for its label word, not for the phrase.

**OUT OF SCOPE, all seven remaining sites.** `double` is the correct umbrella term at every one of
them -- a Test Spy IS a test double -- so renaming any of them is a fresh error. `vitest-typescript-
mechanics.md` is not even in this task's `<files>`. The battery CANNOT catch such a rename: see H4 --
`check-red-references.mjs:209` matches only `expect[ -]to[ -]send`, never the phrase. The six verify
pins below are therefore the only protection, and they cover all seven out-of-scope sites.

**2. The six `Mock rule:` labels whose bodies say "double".** Replace the label word at all six --
`message-matrix.md:38,47,57,67` and `functional-core.md:42,44`. Note that only
`functional-core.md:42` is an actual H2 HEADING; the other five are BULLET LABELS. Do not
restructure any section. `Double rule:` is the natural replacement. Two live topic needles must stay
green, and the reason is a DISJOINTNESS rather than a count: `functional-core.md`'s `no doubles in
the core` topic is `/no (test )?doubles?/i`, matched by `:42` ("no doubles in the core") and `:44`
("no test doubles"), and the label swap touches neither substring. `message-matrix.md`'s
`/expect[ -]to[ -]send|expect to send/i` matches at `:54,79,81,92,137` -- FIVE lines, measured; the
sixth occurrence in the wider lz-red tree is `vitest-typescript-mechanics.md:67`, which this topic
does not read. None of the four label lines this fix edits (`:38,47,57,67`) is one of those five, so
the needle survives the label swap independently of fix 1's edit to `:137`.

**3. The mockist attribution.** RECORDED STRENGTH IS PLAUSIBLE, NOT CONFIRMED -- read strictly, the
current clause names the school's proponents rather than its namers, which is defensible. Clarify
it; do not describe it as a fabrication and do not remove the GOOS counterpoint. At
`anti-patterns.md:123` and in the mirrored row at `principle-backing.md:52`, attribute the label to
Fowler. The taxonomy row records `classicist` and `mockist` as his OWN contribution, and the
counterpoint POSITION is still correctly sourced to GOOS. Both edits must carry the exact phrase
`Fowler's label`, which is guard N7's needle. Keep `principle-backing.md:52` at three cells -- the
row parser skips a row of the wrong width, so a stray pipe there silently removes it from every
guard that reads that table.

Run the battery before proceeding, so the diff for this fix is legible and separate from the much
larger Task 3 diff.
  </action>

  <verify>
    <verify_leg_notes>
**The two Test Spy legs are REGION-BRACKETED, not file-level (NB-7).** A file-wide
`git grep -c -i 'test spy' | wc -l = 1` proves only that the phrase landed SOMEWHERE in a 140-line file;
it cannot tell the `:57` bullet from the `:137` in-fence comment, and satisfying one would satisfy the
leg for both. Each leg instead requires `Test Spy` to fall BETWEEN two stable keys that bracket its
target, so the window needs no character budget and cannot drift as the section grows:

- the `:57` bullet, bracketed by `## Outgoing command: expect the message was sent` and
  `## Outgoing query and self-messages`;
- the `:131-138` example's comment, bracketed by `gate.openGate` and `toHaveBeenCalledWith`.

Measured: all four keys occur EXACTLY ONCE in the file, which is what makes the closing key a real
delimiter rather than a hint. Both legs exit **1** today (`test spy` count 0 in that file). Positive
controls for the bracket FORM: substituting `Mock rule` into leg 1 exits 0 and substituting
`warranted double` into leg 2 exits 0, so the pattern matches when its target is inside the bracket.
Negative control that the bracket is load-bearing: the literal `only one of them warrants a double`
exists at `:73`, AFTER the closing heading, and leg 1's form with that needle exits **1** -- so a phrase
placed outside the section provably cannot satisfy it.
    </verify_leg_notes>

    <automated>node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && ! node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > /tmp/lc9-t2.txt && test "$(rg -c '^\s+\[FAIL\]' /tmp/lc9-t2.txt)" = "4" && rg -q '183 checks, equal to the PREDICTED literal' /tmp/lc9-t2.txt && test "$(rg -c '\[PASS\].*no ragged pipe table' /tmp/lc9-t2.txt)" = "1" && test "$(git grep -c 'Mock rule' -- plugins/ | wc -l)" = "0" && rg -U -q '## Outgoing command: expect the message was sent[\s\S]*?Test Spy[\s\S]*?## Outgoing query and self-messages' plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md && rg -U -q 'gate\.openGate[\s\S]*?Test Spy[\s\S]*?toHaveBeenCalledWith' plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md && test "$(git grep -l "Fowler's label" -- plugins/ | wc -l)" = "2" && test "$(rg -c -F 'warranted double' plugins/lz-tdd/skills/lz-red/references/anti-patterns.md)" = "2" && test "$(rg -c -F 'the one warranted boundary' plugins/lz-tdd/skills/lz-red/references/anti-patterns.md)" = "1" && test "$(rg -c -F 'the one warranted double' plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md)" = "1" && test "$(rg -c -F 'Expect to send (the one warranted double)' plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md)" = "1" && test "$(rg -c -F 'only one of them warrants a double' plugins/lz-tdd/skills/lz-red/references/testing-stance/message-matrix.md)" = "1" && test "$(rg -c -F 'genuinely warrants a double' plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md)" = "1" && test "$(rg -c '^\|' plugins/lz-tdd/skills/lz-red/references/principle-backing.md)" = "37"</automated>
  </verify>

  <done>
- `check-red-references.mjs` exits 1 with EXACTLY 4 `[FAIL]` lines, and they are exactly the two
  taxonomy-deletion guards (`[lc9] every relative markdown link resolves`,
  `[lc9] no test-double taxonomy copy in the shipped tree`) and the two inline-rule guards
  (`[lc9] lz-tpp/SKILL.md ...`, `SKILL.md: [lc9] ...`). N4, N5, N6 and N7 have flipped to PASS.
- `Mock rule` occurs in ZERO files under `plugins/`.
- `Test Spy` occurs in the `:50-59` outgoing-command section AND inside the `:131-138` example, each
  proven by its own region-BRACKETED leg rather than by one file-wide count that either site could
  satisfy alone (NB-7).
- `Fowler's label` occurs in exactly TWO files under `plugins/`.
- All SEVEN out-of-scope phrase sites are UNTOUCHED, each pinned individually by its own literal:
  `anti-patterns.md` still has 2 `warranted double` (`:38`, `:166`) and 1 `the one warranted boundary`
  (`:128`); `test-structure-and-assertions.md` still has 1 (`:130`); `message-matrix.md` still has the
  `:79` table cell `Expect to send (the one warranted double)` and the `:73` sentence `only one of them
  warrants a double`; `vitest-typescript-mechanics.md` still has `genuinely warrants a double`
  (`:66`). These six pins are the H4 over-wide-sweep protection and they are the ONLY protection --
  `check-red-references.mjs:209` matches only `expect[ -]to[ -]send`, so the battery structurally
  cannot catch a sweep at any site count.
- `[lc9] no ragged pipe table` still PASSES, which is what actually protects the shape of
  `principle-backing.md`'s table through the mockist row edit. Guard N1 compares every row's CELL
  COUNT against its header's, so it catches both a missing pipe and a stray in-cell pipe.
- `principle-backing.md` still has 37 pipe-leading lines. Note what this pin does and does NOT prove:
  it proves no row was added or removed, and it does NOT prove the row's WIDTH is unchanged -- a stray
  in-cell pipe widens the row while leaving the line count at 37. N1 above is the width control. Both
  matter, because the row parser SKIPS a row of the wrong width, so either mutation would silently
  remove that row from every guard reading that table.
- Commit: `fix(quick-260729-lc9): name the Test Spy, drop the Mock rule label, attribute mockist`
  </done>
</task>

<task type="auto">
  <name>Task 3: Move the taxonomy out, delete all three copies, re-scope the battery -- two commits</name>

  <files>
plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
.planning/research/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-red/SKILL.md
plugins/lz-tdd/skills/lz-tpp/SKILL.md
plugins/lz-tdd/skills/lz-refactor/SKILL.md
plugins/lz-tdd/skills/lz-red/references/principle-backing.md
.claude/skills/lz-red-workspace/tools/check-red-references.mjs
.claude/skills/lz-red-workspace/tools/lib/row-guards.mjs
.claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
.claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
  </files>

  <action>
**TWO commits, along the one seam that exists. Everything the BATTERY OBSERVES lands in commit 3a;
commit 3b then deletes the surface 3a left unreferenced.**

The atomicity argument (H3, H6) constrains only 3a: deleting the taxonomy before re-scoping makes 43
checks vanish silently, retiring the count guards before deleting makes them fail on a present file,
and removing a router link without deleting the file (or the reverse) trips N2 or N3. So **commit 3a**
carries sections A, B, C, steps 1-9, steps 11-14 and step 15 -- the whole observable change -- and the
battery is a real signal again the moment 3a lands.

**Commit 3b** carries step 10 ALONE: the dead-surface deletion inside `lib/row-guards.mjs`, plus the
selftest's now-orphaned count-guard fixture block, its `COUNT_GUARDS` roster assertion and its
`COUNT_GUARDS` import. This is safe and separable because **step 5 removes the checker's `COUNT_GUARDS`
import and every count-guard CALL SITE in 3a, so after 3a nothing the battery reads references that
surface at all.** The emitted-check count is therefore expected to be 125 both before and after 3b.

**What that claim is and is not backed by -- stated precisely, because the earlier wording called it
"the mechanical proof" and it is not one.** This task's verify block runs the battery ONCE, at the end,
after 3b, so the after-3a count is never measured by any leg -- a verify chain cannot observe an
intermediate commit's runtime output. Two things ARE mechanical: the final run is GREEN at 125 with the
roster gate holding, and a leg pins commit 3b's CHANGED-FILE SET to `lib/row-guards.mjs` and
`row-guards.selftest.mjs` only, so 3b provably re-scopes nothing (it cannot touch `EXPECTED_CHECKS`,
`NEW_LABELS` or any `report()` call, all of which live in the checker). That is strictly weaker than
observing the intermediate count and is the honest limit. The `<done>` therefore requires the executor
to RUN the battery after 3a and record the emitted count in the SUMMARY; that record is the observation,
and it is a human-read artifact, not a gate.

Order is not negotiable: 3a first. Never delete an export before its consumers. Both commit subjects
are pinned individually by Task 4's chain, so collapsing the two into one FAILS there.

**A. Move, then delete. `git mv` FIRST, no content change (H2).**

`git mv plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md .planning/research/test-double-taxonomy.md`,
then `git rm` the lz-tpp and lz-refactor copies. Moving the canonical lz-red copy gives git one clean
rename plus two deletions instead of three deletions and an addition, so `git log --follow` keeps the
D-02 history legible. Keep the basename. `.planning/research/` already exists and its five files are
tracked, so this adds a file to a tracked directory with no gitignore question. Do NOT edit the moved
file in this task.

**B. Replace the three router blocks (D-12: lz-red ends with the rule inline and NO reference).**

- `lz-red/SKILL.md`: DELETE the appendix bullet at `:154-159` entirely, and add the rule INLINE in
  step 6 of the coach procedure, adjacent to the two existing side-qualified uses at `:99-100`.
  Suggested wording, adjust freely but keep the two constraints below:

  > Always qualify the contested word by side -- a production-side stub is the symbol's own unwritten
  > implementation, a collaborator-side stub stands in for something else. Owned sources assign the
  > bare word to opposite sides, so when a developer uses it unqualified, establish which side they
  > mean before answering.

- `lz-tpp/SKILL.md`: replace the bullet at `:92-96` with ONE inline bullet. lz-tpp is the skill that
  FILLS the empty production symbol and `:93,:95` are the ONLY occurrences of the contested word in
  its whole tree, so the replacement must CARRY THE NAMING RULE, not merely drop the pointer:

  > - A production symbol with no implementation yet is a production-side stub. Always qualify it by
  >   side, because owned sources assign the bare word to opposite sides; when a developer says it
  >   unqualified, establish which side they mean before answering.

- `lz-refactor/SKILL.md`: **DELETE the H2 section at `:182-186` outright and write NO replacement.**
  MEASURED: that tree has ZERO occurrences of the contested word or of either side qualifier outside
  the deleted copy, so there is no naming rule to preserve here (D-09 -- change nothing else in
  lz-refactor).

  MEASURED FURTHER, and this is the OWNER'S DECISION on F-4: `:182-186` is a PURE POINTER -- one H2
  heading, three lines of prose and one link -- and it is the FILE'S LAST SECTION (the file is 186
  lines). So deleting through `:186` inclusive leaves no dangling prose, no orphaned heading and no
  trailing-structure repair; the no-tests pointer at `:181` simply becomes the file's final section.
  Plain deletion is sufficient, and it is what D-01 implies: the taxonomy is lz-red-only, so
  lz-refactor stops pointing at it rather than paraphrasing it.

  **NO heading is mandated here and NO replacement leg exists, deliberately.** An earlier revision
  mandated a net-new H2 section at this site and pinned its heading at exactly one line; the owner
  replaced that with plain deletion, so that pin was REMOVED and its slot in Task 3's chain reused for
  the `TAX` assertion (see the `TAX` note in step 14). Coverage is unchanged in substance: leg 15
  already proves the taxonomy POINTER is gone from `plugins/`, and with no replacement to arrive there
  is nothing further for a leg to observe. **Do NOT author a replacement section, and do NOT re-add a
  heading pin** -- a pin on a heading that must never arrive would false-fail correct work.

TWO CONSTRAINTS ON ALL THREE, both load-bearing rather than cosmetic. (i) G17's allowlist is
per-line and anchored to end-of-slice, so a line wrap BETWEEN the side qualifier and the contested
word DEFEATS it -- keep every qualifier and its word on ONE line. (ii) `lz-red/SKILL.md` and
`lz-tpp/SKILL.md` must each contain the phrase `which side they mean` (guards N9 and N8).

**C. `principle-backing.md` -- remove every reference to the departed document.**

Delete the whole `[Test-double taxonomy](test-double-taxonomy.md)` row at `:71` -- every cell in it
describes a document no longer in the tree, including a count restated from it. Delete the
cross-reference sentence at `:98-99`. Then sweep the file for any remaining mention and remove it;
N2 catches a surviving LINK but not a surviving prose mention, which is what the sweep is for.

**D. Re-scope the battery. Retire 58, keep 2, RETARGET 0.**

Retargeting is rejected for three reasons, recorded so the choice is not silently re-opened: a
frozen archive has no regression surface, so a guard on it can only fail on a deliberate archive edit
(the guard-that-cannot-fail class in a new costume); pointing 44 checks of the lz-red SKILL gate at a
planning artifact makes a planning-doc edit able to redden the skill gate, which is the wrong
direction of coupling; and NO `.planning/` subdirectory survives a milestone close, so every
retargeted guard would break at the next one.

In `check-red-references.mjs`:

1. Delete the entire `test-double-taxonomy.md` FILES entry (29 topics, 14 `absent`, 1 auto scaffold
   check = 44 emitted checks). `FILES.length` goes 12 -> 11.
2. Delete the sha256 byte-identity block OUTRIGHT -- not narrowed, not archived (D-02). Narrowing it
   to one copy makes its predicate `digests.every(d => d === digests[0])` VACUOUS on a one-element
   array, reproducing the guard-that-cannot-fail class this file's own header condemns.
3. Delete the two taxonomy row-scoped entries and all NINE count entries from `TWO_IG_GUARDS`; it
   drops to four principle-backing row-scoped guards. Delete `taxonomyRowBacked` too -- it asserts
   the very row deleted in step C, so it must retire, not retarget.
4. Delete the chronology post-loop block, `taxonomyText`, `TAXONOMY_BASENAME` and the
   `path.basename(file) !== TAXONOMY_BASENAME` filter in G17's target list. Deleting the filter is
   SELF-ENFORCING: if a copy is ever re-added to a shipped reference tree, G17 fires loudly on it,
   which is the correct outcome under D-01/D-02. Keeping a vacuous filter would preserve the retired
   mechanism's shape, which is exactly what D-02 forbids. G17 keeps its scope otherwise -- measured
   10/3/177 files before and after; it never scanned the copies.
5. Drop `COUNT_GUARDS` from the `:89` import.
6. Prune `NEW_LABELS` to the survivors -- the four remaining `TWO_IG_GUARDS` labels plus the nine
   `[lc9]` labels = 13. Set `EXPECTED_CHECKS = 125` and write the arithmetic into the comment above
   it in the existing style: `174 baseline + 9 [lc9] additions - 58 retirements = 125`.
7. Add `.planning/research/test-double-taxonomy.md` to guard N1's target list as ONE extra path
   alongside the `plugins/` walk, failing closed if it is unreadable. Keep this textually separate
   from G17's scope (H8) -- do not introduce a shared basename constant.

   **This step is legged as of this revision, and the FLOOR is deliberate.** Two legs: the archive path
   must appear as a literal in the checker (count >= 1), and N1's `no ragged pipe table` PASS line must
   still be emitted exactly once at this task's end -- an assertion no post-Task-2 block previously
   carried, which is what let this step be skipped with the chain still green. What the pair proves: the
   path is IN the checker and N1 still reports. What it does NOT prove: that the path is inside N1's own
   target loop rather than merely mentioned somewhere in the file. **A real mutation test -- deleting a
   cell plus its pipe from the archived copy and asserting the battery reddens -- is DELIBERATELY NOT
   built here: it is too heavy for an `&&` verify chain (it would have to mutate a committed file,
   re-run, and restore, leaving the tree dirty if any later leg fails).** The wiring is confirmed
   instead by `<orchestrator_followup>` item 6, a one-line read of N1's target list. A mutation test
   becomes warranted only if the archived copy later grows tables that something depends on; today it is
   a frozen record and the shape gate is a hygiene floor, not a regression surface. Do not read the
   floor as an oversight.
8. **Step 8, first half (D-02).** The three-copies rationale is DUPLICATED: `:35-37` in the file
   header and `:632-637` in the byte-identity block, and the second states it in MORE detail
   including a clause about there being no plugin-root shared dir, which the forward architecture
   contradicts. The block deletion removes the second; the header comment at `:35-37` must be
   REPLACED with the forward constraint, not merely deleted. Also correct `:20-23` (twelve FILES
   entries -> eleven), `:669-679` (G17's scope comment still cites the basename exclusion), and the
   final SUMMARY string, which names the taxonomy and asserts byte-identity across three skills.
   The forward constraint to record: if a future milestone needs this material in the plugin it goes
   in as ONE plugin-wide shared reference, never as byte-identical per-skill copies. Per D-10, record
   the constraint and do NOT investigate, spike or prove the mechanism.

   **All four corrections are now legged, and the SUMMARY one is the important one.** The final SUMMARY
   string is HAND-WRITTEN at `:980` and prints on the GREEN path only -- exactly the path this task ends
   on -- so an uncorrected one ships a stale claim in the single most-read line the tool emits. Only its
   `12/12` prefix self-corrects, because that is interpolated from `filesPresent` and `FILES.length`; the
   prose does not. Two negated legs over the captured output cover it: the byte-identity-across-three-
   skills clause and the taxonomy item inside the parenthetical surface list must BOTH be gone. Note the
   trap: do NOT reach for a blanket taxonomy-absent leg on the output, because guard N3's own label
   contains that word by design. The three COMMENT corrections (`:20-23`, `:35-37`, `:669-679`) are
   covered by ONE combined negated leg over the checker source carrying a literal from each, anchored by
   the archive-path count leg from step 7 on that same file.

   **Step 8, second sweep target -- the comments THIS TASK wrote.** The four sites above are the
   pre-existing stale claims. There is a second class: any tree-dependent number Task 1 wrote into its
   own nine guards' comments. Task 1's general rule forbids writing them at all, so this should find
   nothing -- but check rather than assume, because the prior revision's sweep omitted exactly this and
   the omission was the finding. Re-read every comment Task 1 added and confirm none states a file
   count, link count, anchor count, table count or pipe-row count. Measured, the pairs that go stale in
   THIS commit are: files 197 -> 194, inline links 1014 -> 990, relative 984 -> 981, anchor 30 -> 9,
   tables 31 -> 28, pipe rows 405 -> 297. If any such literal survives, replace it with the invariant it
   was standing in for, not with the new number -- a new number goes stale at the next deletion.

   **Step 8, third sweep target -- G17's own emitted LABEL, not just its comment.** Measured, `:690`
   reads
   `const BARE_QUALIFIER_LABEL = "[wev G17] no bare unqualified contested word outside the taxonomy";`
   and the checker emits that string verbatim today. After this commit there IS no taxonomy in the tree
   to be "outside" of, so the label is a stale claim in the INSTRUMENT'S OWN OUTPUT -- precisely the
   class step 6 exists to close, pointed at the gate rather than at the prose. Rename it to
   `[wev G17] no bare unqualified contested word in the shipped tree`.

   Verified safe before proposing it, three ways: the string is NOT in `NEW_LABELS` (checked the block
   at `:932-956`); it is NOT in `RETIRED_LABELS` (count 0 in `lib/row-guards.mjs`); and its only
   RESEARCH.md occurrence, `:341`, is inside the **KEEP -- 2** block, not the retire list, so it is not
   one of the 58. The roster gate compares full strings over count, missing-new and surviving-retired,
   and a rename moves none of the three: the emitted count stays 125 and `174 - 58 + 9 = 125` still
   holds. The new string collides with nothing -- `[lc9] no test-double taxonomy copy in the shipped
   tree` (N3) shares four words but is a different string.

   One consequence to record rather than fix: RESEARCH.md's KEEP block names the PRE-rename string. It
   is a dated research record, not a live claim, so leave it -- the same stance as Task 4 section E.

In `lib/row-guards.mjs`:

9. Grow `RETIRED_LABELS` from 6 to 64 by appending the 58 labels enumerated in RESEARCH Q2, composed
   EXACTLY as emitted. Three have an unusual shape and are the likely transcription errors: the
   scaffold label puts its bracket prefix FIRST, the byte-identity label has no colon after the
   filename, and the owned-source label has no filename prefix at all. If a retired label is
   mistyped, the roster gate cannot see that the check really went -- transcribe from a captured run,
   not from memory.
10. **COMMIT 3a half.** Delete the three retired `ROW_SCOPED_GUARDS` entries (the two taxonomy ones and
    `taxonomyRowBacked`) with their matching `OLD_NEEDLES` entries. These are OBSERVABLE surface: the
    selftest's export-roster assertion in step 13 reads the key set directly, so they cannot lag behind
    step 3.

    **COMMIT 3b, and nothing else is in it.** Delete the surface 3a leaves unreferenced, so the
    retirement does not leave a large dead export area the roster gate cannot see: `TAXONOMY_COLUMNS`,
    `TAXONOMY_HEADER`, `TAX`, `taxonomyRows`, the entire count-guard section including `NUM_WORDS`,
    `CARDINAL_ALT`, `statedCardinals`, `assertStatedTotal`, `sourceBulletCount`, `axisValueCounts`,
    `productionOwnTransitionalRows`, `enumerationRuns` and all nine guards, and the `COUNT_GUARDS`
    export itself. Then, in the selftest, its `COUNT_GUARDS` import, the count-guard fixture block and
    the `COUNT_GUARDS exports exactly the nine` roster assertion -- all three orphaned by the export
    going. Every one of these is unreachable from the battery after 3a, which is why the emitted count
    is expected to be 125 both before and after 3b.

    **Two legs cover this list beyond `COUNT_GUARDS` itself.** The pre-existing leg pins the four
    `TAXONOMY_*` / `taxonomyRows` identifiers; a second combined leg now pins the count-guard HELPERS
    absent from `lib/row-guards.mjs` as well, so a partial deletion that leaves the helper bodies behind
    fails instead of shipping a dead area the roster gate cannot see. Both sit after the positive
    `ROW_SCOPED_GUARDS` anchor on that same path. And a leg pins this commit's changed-file set to
    `lib/row-guards.mjs` plus `row-guards.selftest.mjs` and nothing else, which is what makes "3b
    re-scopes nothing" checkable rather than asserted. Three residues, all stated rather than legged:
    `taxonomyText` is not in any identifier leg (its own guard's label is in the retired roster, so its
    CHECK is gated; the leftover binding would be dead code only); and the `TAXONOMY_LINES` KEEP is
    itself guarded only indirectly, since deleting it breaks the `parseRows` fixtures the selftest
    already runs. Both fall inside `<orchestrator_followup>` item 2's scope.

    **`TAX` IS LEGGED, and an earlier revision was WRONG to say otherwise (F-2).** That revision
    claimed `TAX` "cannot be legged AT ALL" because a bare `TAX` needle would also match
    `TAXONOMY_LINES`, which step 14 requires to SURVIVE. The reasoning is FALSE for this file, and it
    was measured false: `TAXONOMY_LINES` occurs ZERO times in `lib/row-guards.mjs` -- it lives in
    `row-guards.selftest.mjs` (`:42` and `:167`), which this leg does not scan. The four surviving
    row-scoped guards reference `backingRows`, never `TAX`. And a word-boundary needle cannot
    false-match it regardless: `\bTAX\b` fails against `TAXONOMY_LINES` because the next character is a
    word character (probed directly, exit 1). So Task 3's chain now asserts `\bTAX\b` at count 0 in
    `lib/row-guards.mjs`. RED today -- `TAX` occurs at `:39` plus eight further sites, so the leg exits
    1. It sits immediately AFTER the positive `ROW_SCOPED_GUARDS` anchor on that same path, so the
    `= "0"` direction, which would otherwise pass on a mistyped path via `|| echo 0`, is anchored.
    **DELETE `TAX` COMPLETELY in 3b** -- the leg now enforces it rather than trusting prose.

In `row-guards.selftest.mjs` -- FOUR assertions break in COMMIT 3a and each is named because an
executor that runs only the main battery sees GREEN and ships a red selftest (H5):

11. `RETIRED_LABELS carries exactly six composed labels` -> 64.
12. The `RETIRED_LABELS are COMPOSED strings` assertion regex `/^[a-z-]+\.md: /` FAILS on exactly
    THREE of the 58 (the three unusual shapes above), and would also fail a `testing-stance/` path
    label. Do NOT just delete it -- its purpose is catching a bare-label transcription error, which
    is a live hazard given those shapes. REPLACE it with one that still has teeth: every entry is
    non-empty, the list is duplicate-free, and every entry contains either `.md` or a bracketed tag
    prefix, so a bare `label` value from the checker can never slip in.

    **The replacement's `check()` label is MANDATED VERBATIM, because the verify pins it and deleting
    the assertion otherwise satisfies every leg in this task** (`node row-guards.selftest.mjs` exits 0
    either way -- the same fail-open shape as Task 1 step C). Write it exactly:
    `RETIRED_LABELS are non-empty, duplicate-free, and each carries a filename or a bracketed tag`.
    Assert all three properties in ONE `check()` so that one PASS line proves all three.

    **Residue, stated so a reviewer does not read it as a miss.** The pinned label proves the assertion
    exists and passes; it cannot prove the assertion has teeth (a `check(label, true, true)` would also
    pass), and NO machine check can prove the 58 retired strings were transcribed correctly -- a mistyped
    retired label is trivially "not emitted", so the roster gate is blind to it by construction, and the
    pre-change emitted set is gone once 3a lands. That gate is `<orchestrator_followup>` item 2, which is
    why it exists and why it is not optional.
13. `ROW_SCOPED_GUARDS exports exactly the seven named row-scoped guards` -> four, and `OLD_NEEDLES`
    parity must shrink in lockstep. The `COUNT_GUARDS exports exactly the nine` assertion is NOT touched
    here -- the export still exists after 3a; it goes in 3b with the export (step 10).
14. Delete the two retired taxonomy row-scoped guard fixture blocks. The count-guard fixture block is
    3b's, not 3a's.

    **KEEP `TAXONOMY_LINES`** -- it is an in-memory 9-column fixture and it is the ONLY fixture
    exercising `parseRows`'s header-dropped-BY-VALUE and width-mismatch-SKIPS cases, plus the new
    `scanTables` cases. Add a comment saying it is a synthetic fixture, not a copy of any shipped
    document. Remove from `BACKING_LINES` the taxonomy prose line and the taxonomy table ROW; that
    drops the fixture from 5 data rows to 4, so the `backing fixture parses to 5 data rows`
    assertion literal must move to 4. `mutate()` asserts its replacement actually happened, so any
    fixture mutation still referencing a deleted line will throw -- fix those, do not weaken
    `mutate`.

In `lib/pipe-table.mjs`:

15. **OWN the `:16-18` header rewrite here** (S14), do not merely confirm it. Task 1 deliberately left
    it alone: until this commit the taxonomy IS still a target of the module, so rewriting it earlier
    would have described an untrue scope for commits 1 and 2. This is the commit where the file leaves
    `plugins/`, so this is where the measurement stops being true. State the module's new scope. The
    `principle-backing.md` measurement at `:19-23` stays.

    **Legged as of this revision, and this file was previously touched by NO leg in ANY block.** The
    header's `:16-18` census is a MEASUREMENT OF THE DEPARTING FILE -- 36 pipe lines, 11 parts, 9
    columns -- so leaving it makes the module header describe a file the module no longer targets, the
    exact class the general rule under Task 1's N2 forbids. Two legs, anchored: a positive counted
    `export function parseRows` on this path FIRST (the second leg is a fail-open direction and a
    mistyped path would otherwise pass it), then the taxonomy basename absent from this file. Do NOT
    write the new pipe-row or column counts into the replacement: state the module's target and the
    invariant, per the same general rule. The rewrite must NOT keep the departing file's numbers under a
    new heading.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs > /tmp/lc9-st3.txt && rg -q -F -e 'RETIRED_LABELS are non-empty, duplicate-free, and each carries a filename or a bracketed tag' /tmp/lc9-st3.txt && test "$(rg -c '^  \[PASS\] scanTables: ' /tmp/lc9-st3.txt || echo 0)" -ge 6 && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > /tmp/lc9-t3.txt && rg -q '125 checks, equal to the PREDICTED literal' /tmp/lc9-t3.txt && rg -q 'none of the 64 retired labels survive' /tmp/lc9-t3.txt && rg -q 'all 13 new labels present' /tmp/lc9-t3.txt && test "$(rg -c '\[PASS\].*no ragged pipe table' /tmp/lc9-t3.txt)" = "1" && test "$(rg -c -F -e '[wev G17] no bare unqualified contested word in the shipped tree' /tmp/lc9-t3.txt || echo 0)" = "1" && test "$(rg -c -F -e 'outside the taxonomy' /tmp/lc9-t3.txt | wc -l)" = "0" && test "$(rg -c -F -e 'byte-identical in all three skills' /tmp/lc9-t3.txt | wc -l)" = "0" && test "$(rg -c -F -e '+ the test-double taxonomy)' /tmp/lc9-t3.txt | wc -l)" = "0" && test "$(rg -c -F -e '.planning/research/test-double-taxonomy.md' .claude/skills/lz-red-workspace/tools/check-red-references.mjs || echo 0)" -ge 1 && test "$(rg -c -e 'TWELVE FILES entries|ships as three copies|taxonomy-basename exclusion' .claude/skills/lz-red-workspace/tools/check-red-references.mjs | wc -l)" = "0" && test "$(git grep -c 'test-double-taxonomy' -- plugins/ | wc -l)" = "0" && test "$(git grep -c -F "Fowler's label" -- plugins/lz-tdd/skills/lz-red/references/principle-backing.md | wc -l)" = "1" && test "$(git grep -c -i 'taxonom' -- plugins/lz-tdd/skills/lz-red/references/principle-backing.md | wc -l)" = "0" && test "$(git log -M --name-status --format= --grep='scope the test-double taxonomy to planning' | rg -c '^R100' || echo 0)" = "1" && test -s .planning/research/test-double-taxonomy.md && test "$(git grep -c 'which side they mean' -- plugins/lz-tdd/skills/lz-red/SKILL.md plugins/lz-tdd/skills/lz-tpp/SKILL.md | wc -l)" = "2" && test "$(git grep -c 'production-side stub' -- plugins/lz-tdd/skills/lz-tpp/SKILL.md | wc -l)" = "1" && test "$(rg -c 'ROW_SCOPED_GUARDS' .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs || echo 0)" -ge 1 && test "$(rg -c -e '\bTAX\b' .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs || echo 0)" = "0" && test "$(rg -c 'COUNT_GUARDS' .claude/skills/lz-red-workspace/tools/check-red-references.mjs | wc -l)" = "0" && test "$(rg -c 'COUNT_GUARDS' .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs | wc -l)" = "0" && test "$(rg -c -e 'NUM_WORDS|CARDINAL_ALT|assertStatedTotal|statedCardinals|sourceBulletCount|axisValueCounts|productionOwnTransitionalRows|enumerationRuns' .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs | wc -l)" = "0" && test "$(rg -c 'COUNT_GUARDS' .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs | wc -l)" = "0" && test "$(rg -c -e 'TAXONOMY_BASENAME|TAXONOMY_COLUMNS|TAXONOMY_HEADER|taxonomyRows' .claude/skills/lz-red-workspace/tools/check-red-references.mjs .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs | wc -l)" = "0" && test "$(rg -c -F -e 'export function parseRows' .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs || echo 0)" -ge 1 && test "$(rg -c -F -e 'test-double-taxonomy.md' .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs | wc -l)" = "0" && test "$(git show --name-only --format= "$(git log -1 --format=%H --grep='delete the unreferenced count-guard surface')" | wc -l)" -ge 2 && test "$(git show --name-only --format= "$(git log -1 --format=%H --grep='delete the unreferenced count-guard surface')" | rg -c -v 'lib/row-guards\.mjs|row-guards\.selftest\.mjs' || echo 0)" = "0" && claude plugin validate .</automated>

    <verify_leg_notes>
Legs added this iteration, each with its OBSERVED exit code against the unmodified tree, and each
proven to discriminate rather than merely to be red.

**The selftest is no longer a bare exit-code leg (NB-3).** Its output is captured and the step-12
REPLACEMENT assertion's mandated label is pinned. Deleting that assertion instead of replacing it leaves
`node row-guards.selftest.mjs` exiting 0, so the bare form could not tell the two apart. The
`scanTables: ` PASS-count leg is carried forward from Task 1 so a later commit cannot quietly drop
guard N1's only falsifiability proof. Both legs: exit **1** today.

**G17's renamed label is asserted BOTH ways (NB-2).** The new string must be emitted exactly once, and
the literal `outside the taxonomy` must not appear in the emitted output at all -- a rename that is
merely additive, or a comment sweep that misses the label constant, fails. Measured: the OLD string is
emitted today (`[PASS] [wev G17] no bare unqualified contested word outside the taxonomy`), so the
positive leg exits **1** and the negative leg exits **1** today. Both flip together and only together.

**`principle-backing.md` gets a COUNTED POSITIVE ANCHOR before its `taxonom`-absent leg (NB-1).** The
absent leg is `git grep -c ... | wc -l = 0`, and `git grep` on a mistyped or untracked path writes to
stderr and emits nothing, so `wc -l` reads 0 and the leg PASSES on a typo. The anchor is
`git grep -c -F "Fowler's label"` on that same path, count 1 -- present from Task 2 onward. Measured:
the anchor exits **1** today (the phrase arrives in Task 2) and exits **1** against a deliberately
mistyped path, so it genuinely closes the fail-open. Do NOT use the file's pipe-line count as the
anchor: it drops 37 -> 36 when the taxonomy row is deleted.

**H2 now has a mechanical leg (NB-4).** `git log -M --name-status` over the commit selected by its own
subject must report `R100` -- a 100%-similarity rename -- exactly once. Measured in a throwaway
worktree by actually performing the move and committing it: a pure `git mv` reports
`R100 plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md .planning/research/test-double-taxonomy.md`
and the leg exits **0**; the same move with a single line prepended before the commit reports `R099`
and the leg exits **1**. So the leg discriminates exactly the H2 misreading it exists to catch, and it
survives both of Task 3's commits because it selects by subject rather than by `HEAD`. `test -f` on the
destination is upgraded to `test -s`, since a zero-byte file cannot be an `R100` rename anyway.

**3b's dead-surface deletion is legged, with a positive anchor first.** `ROW_SCOPED_GUARDS` must still
be present in `lib/row-guards.mjs` (count >= 1) BEFORE the four absence legs run over that same file --
otherwise a mistyped path would pass all four. Then `COUNT_GUARDS` must be absent from all three
modules, and none of `TAXONOMY_BASENAME`, `TAXONOMY_COLUMNS`, `TAXONOMY_HEADER`, `taxonomyRows` may
survive in the checker or in `row-guards.mjs`. Measured today: the anchor exits **0** (the export
exists), and the `COUNT_GUARDS`-absent legs exit **1**, so the block is RED for the right reason.

`rg -c` prints NOTHING and exits 1 on a genuine no-match, so a bare `test "$(rg -c ...)" -ge N` dies
with "integer expected". Every threshold leg here uses `|| echo 0`, which also collapses `rg`'s exit-2
path-failure to 0 and therefore FAILS CLOSED on a typo. Verified both ways.

**ELEVEN legs added this iteration. Block 3 goes 22 -> 33 legs (21 -> 32 `&&`), and the block's OBSERVED
exit against the unmodified tree is still 1, first failing at leg 2 (the step-12 mandated label).** Each new
leg was run VERBATIM in isolation against the unmodified tree; observed exit codes below.

| Leg | What it asserts | Observed | Direction / anchor |
|---|---|---|---|
| 8 | `[PASS] ... no ragged pipe table` emitted exactly once (NEW-1) | **1** | fail-closed; anchored by leg 4 which writes the output file |
| 11 | the byte-identity-across-three-skills clause absent from the output (NEW-2) | **1** | fail-open form, anchored by legs 5-8 which positively grep the same file |
| 12 | the taxonomy item absent from the GREEN SUMMARY's parenthetical (NEW-2) | **1** | same anchor |
| 13 | the archive path present in the checker, count >= 1 (NEW-1) | **1** | fail-closed, AND it is the positive counted anchor on the checker SOURCE for legs 14, 24 and 28 |
| 14 | the three stale checker comment claims all gone, one combined negated leg | **1** | fail-open form, anchored by leg 13 on the same path |
| 22 | `ROW_SCOPED_GUARDS` present in `lib/row-guards.mjs`, count >= 1 | **0** -- deliberate ANCHOR | probed with a mistyped path: exit **1**; it is the positive counted anchor on that path for legs 23, 25 and 26 |
| 23 | `\bTAX\b` absent from `lib/row-guards.mjs` (F-2) | **1** | fail-open form, anchored by leg 22 on the same path. Replaced the withdrawn lz-refactor heading pin (F-4), so block 3's leg count is unchanged at 33 and legs 24+ keep their numbers |
| 26 | the eight count-guard HELPER identifiers absent from `lib/row-guards.mjs` | **1** | fail-open form, anchored by leg 22 (`ROW_SCOPED_GUARDS` >= 1) on the same path |
| 29 | `export function parseRows` present in `lib/pipe-table.mjs` (NEW-3) | **0** -- deliberate ANCHOR | probed with a mistyped path: exit **1**, so it genuinely closes leg 30 |
| 30 | the taxonomy basename absent from `lib/pipe-table.mjs` (NEW-3) | **1** | fail-open form, anchored by leg 29 |
| 31 | commit 3b's changed-file list has >= 2 entries (NEW-5) | **1** | fail-closed ANCHOR: with no such commit the subject resolves empty, `git show` errors, `wc -l` reads 0 |
| 32 | commit 3b changed NOTHING outside the two row-guards files (NEW-5) | **0** -- fail-open in isolation | closed by leg 31 immediately before it, probed: the `rg -c -v` form exits **1** on a commit whose files do not match, so it has teeth |

**Legs 31 and 32 are the only place a nested command substitution appears.** Shape verified by running
it against an existing commit: `git show --name-only --format= <sha>` prints one path per line with no
blank line, so `wc -l` is the file count and `rg -c -v <allowlist>` is the count of files OUTSIDE the
allowlist. The subject is resolved with `git log -1 --format=%H --grep=...`, so the pair selects commit
3b by its own mandated subject and does not depend on `HEAD`.

**Leg 30 is the one place a claim was DELIBERATELY narrowed rather than widened.** A blanket
"`test-double-taxonomy` absent from the checker" leg is IMPOSSIBLE after step 7, because the archive path
is required to be there -- and leg 13 pins it. So the taxonomy-absent assertion is scoped to
`lib/pipe-table.mjs`, which has no legitimate reason to name the file after this commit.
    </verify_leg_notes>
  </verify>

  <done>
- Battery exits 0 at 125 emitted checks plus the roster gate, with all 13 new labels present and none
  of the 64 retired labels surviving.
- **The battery was RUN after commit 3a as well, and its emitted count is RECORDED in the SUMMARY.** No
  leg can observe an intermediate commit's output, so this record is the only observation of the
  after-3a count and it must actually be taken -- do not reconstruct it from the after-3b run. What IS
  legged is commit 3b's changed-file set: exactly `lib/row-guards.mjs` and `row-guards.selftest.mjs`,
  nothing else, so 3b provably re-scoped nothing. Together with the final GREEN at 125 that is a strong
  but NOT complete backing for "125 both times"; the phrase "mechanical proof" is withdrawn.
- `row-guards.selftest.mjs` exits 0 with all four broken assertions repaired, not deleted. The step-12
  replacement is PINNED by its mandated label, so deleting it fails; a bare exit-code leg could not tell
  a replacement from a deletion.
- `.planning/research/test-double-taxonomy.md` arrived by a PURE rename: `git log -M` reports `R100` for
  it, so no content changed during the move (H2). A move-plus-edit reports `R099` and fails.
- G17's emitted label reads `in the shipped tree`, and the battery's output no longer carries the old
  label phrase, the byte-identity-across-three-skills clause, or the taxonomy item in the GREEN SUMMARY's
  parenthetical. All three are legged, which is what makes the claim "the instrument no longer names a
  document that has left the tree" TRUE OF THE WHOLE OUTPUT rather than of the label alone (NEW-2).
- The checker's three stale comment claims are gone (the twelve-entries count, the three-copies
  rationale, G17's basename-exclusion scope note), pinned by one combined negated leg anchored on the
  archive-path count.
- `lib/pipe-table.mjs` no longer names the departed document anywhere -- its `:16-18` census was a
  measurement OF that file. Anchored by a positive `parseRows` count on the same path (NEW-3).
- `lz-refactor/SKILL.md` has the taxonomy section DELETED with no replacement (F-4, owner-decided), so
  there is no heading to pin here: leg 15 already proves the pointer is gone from `plugins/`, and the
  file's last section is now the no-tests pointer. Deliberately unlegged because the correct outcome is
  an absence, and a heading pin would false-fail it.
- `\bTAX\b` appears NOWHERE in `lib/row-guards.mjs` (F-2), anchored by the positive
  `ROW_SCOPED_GUARDS` count on that same path.
- `COUNT_GUARDS` appears in NONE of the checker, `lib/row-guards.mjs` or the selftest, and none of
  `TAXONOMY_BASENAME`, `TAXONOMY_COLUMNS`, `TAXONOMY_HEADER`, `taxonomyRows` survives.
- ZERO files under `plugins/` mention `test-double-taxonomy`, and `principle-backing.md` has no
  remaining taxonomy mention of any kind.
- Both `lz-red/SKILL.md` and `lz-tpp/SKILL.md` carry the inline rule with `which side they mean`;
  lz-tpp still carries `production-side stub`, so the naming rule survives in the skill that fills
  the empty symbol. `SKILL.md: [lc9] side-qualification rule inline in the coach procedure` is the guard
  that proves the PLACEMENT, and it can only PASS because Task 1's B0 evaluator fix landed.
- `claude plugin validate .` passes.
- TWO commits from this task, in this order:
  1. `refactor(quick-260729-lc9): scope the test-double taxonomy to planning, re-scope the battery`
  2. `refactor(quick-260729-lc9): delete the unreferenced count-guard surface`
  </done>
</task>

<task type="auto">
  <name>Task 4: Make the archived copy an honest inert record</name>

  <files>
.planning/research/test-double-taxonomy.md
  </files>

  <action>
Every edit lands on the MOVED copy. Nothing under `plugins/` is touched. This is where step 1 (C-B1),
step 6 (the sweep) and D-11 finally apply -- see the H2 resolution above for why they run after the
move and not before.

**A. The inert-record header (D-04).** Above the existing `# ` title, add a header that states: the
document is an `INERT RECORD` -- that exact uppercase literal, because the verify block greps for it;
do not write "archived record" in its place -- not a deliverable and not an agent input; it shipped as a bundled
reference in three lz-tdd skills and was removed from all of them by this quick task; no skill, agent
or command reads it; it must NOT be wired into `oracle.md`, `oracle-reviewer.md` or any SKILL.md.
State WHY it is kept: the per-author map is the useful part -- it records which author uses which
test-double term, on which side, and which source to cite -- and its job is to help a HUMAN or the
ORCHESTRATOR understand the oracle's sources and frame a question to the oracle, never to be an
authority a coach quotes. State that it FAILED THREE CONSECUTIVE ACCEPTANCE GATES, with round three's
independent unprimed review returning ACCEPT WITH FIXES at 2 BLOCKING, 15 IMPORTANT, 10 MINOR, and
point at
`.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-ACCEPTANCE-REVIEW.md`.
Name what remains OPEN: the IMPORTANT and MINOR findings are not all closed; absence claims here rest
on a degraded scan and nobody has verified a whole-book negative, because the oracle agent has no
search tool; and statements about how this document was distributed, gated or enforced describe a
state that no longer exists. Close with the forward constraint (D-02, step 8 second half): if a
future milestone needs this material in the plugin it goes in as ONE plugin-wide shared reference,
never as byte-identical per-skill copies. Per D-10 the header RECORDS the constraint and does not
investigate or prove the mechanism.

**SIX MANDATED LITERALS in this header, because the verify pins each one (NB-6).** The prior revision
gated only `INERT RECORD` and `plugin-wide shared reference`, leaving the rest of the header's required
content unlegged -- so a header stating its status and nothing else passed. Write these six verbatim,
anywhere in the file, in ordinary prose:

| Literal | What it proves is present |
|---|---|
| `INERT RECORD` | the status claim, uppercase, not "archived record" |
| `not an agent input` | the D-04 half that the status claim alone does not carry |
| `2 BLOCKING`, then `15 IMPORTANT`, then `10 MINOR` | round three's actual verdict, as three counts in that order within one clause -- pinned as ONE ordered leg |
| `260729-2ig-ACCEPTANCE-REVIEW.md` | the pointer at the review, so the claim is checkable |
| `has no search tool` | the reason the absence claims cannot be verified -- the load-bearing half of "what remains open" |
| `plugin-wide shared reference` | the D-02 forward constraint |

These six are a FLOOR, not the header's content. The header still has to read as honest prose, and the
unprimed review in `<orchestrator_followup>` remains the real gate on it -- six greps cannot audit
honesty. Do not pad the file with the literals in a list to satisfy the legs.

Do NOT edit any file under `.claude/agents/`. Measured: `git grep -ln "test-double-taxonomy" --
.claude/agents/` returns nothing today and both oracle agents are `tools: Read, Glob`. Wiring the map
in would be a NEW data path, not a preserved one, and D-04 forbids it.

**B. Fix content blocker C-B1 (D-07, step 1).** At `:285-287` the Beck side of the Clean-Code-versus-
Beck conflict is sourced to "the report and essays cited in his rows above" -- rows `:235-238`, which
give a 1994 report plus essays from 2007-2010, February 2022, August 2008 and January 2022. None of
those carries the position. The surface that does is named correctly by the dependents at
`principle-backing.md:67` and `:85` and at `lz-refactor/references/beck-tdd-by-example.md:26` and
`:83`. The dependents are right and this map is wrong, and TWO QUALIFIERS THE DOCUMENT ITSELF MARKS
NOT OPTIONAL hang off the wrong work. Re-source the Beck side to the surface the dependents name.

**The surface, measured, and the two literals the verify gates on.** All four dependent sites name the
same thing -- the essay `TDD is Kanban for Code`. Re-measure before editing:
`git grep -n -F 'TDD is Kanban for Code' -- plugins/` returns `principle-backing.md:67`, `:85`,
`beck-tdd-by-example.md:26`, `:83`, and NOT the taxonomy. So:

- REMOVE the literal `report and essays cited in his rows above` (measured: exactly 1 occurrence today).
- ADD the literal `TDD is Kanban for Code` INSIDE this paragraph -- specifically within 900 characters
  after the line `- Clean Code versus Beck on whether a build failure is a valid red`. The verify leg is
  region-scoped with `rg -U` precisely so that naming the essay somewhere ELSE in the document (a new
  header line, another row) does NOT satisfy it. Measured: the essay title appears NOWHERE in the
  taxonomy today, so this leg is RED at baseline; the region form's positive control passes.

Keep the two NOT-OPTIONAL qualifiers attached, now to the right work: the claim is GATED on a red test
already existing, and it is ONE POSITION AMONG SEVERAL that Beck himself calls contradictory. Do not
weaken either, and do not present the kanban cycle as his settled doctrine.

**C. Reinstate the deleted absence rule (D-11), and scope the claim it exists to catch.** BOTH halves
are gated; the prior revision gated only the first.

1. The hard rule in section 1, before its deletion last round, said the document states only what
   POPULATES each cell, never about the literature at large and NEVER ABOUT WHAT SOME AUTHOR DOES OR
   DOES NOT NAME. Restore the struck clause. Gated literal: `does or does not name`.
2. Then scope the claim it would have caught, at `:106` ("He does not name that artifact"), to what was
   ACTUALLY READ. Nobody can verify a whole-book negative here. This is the SUBSTANTIVE half: the claim
   is the entire warrant for section 2's ruling that this author is not the authority for the
   production side, so scope it rather than delete it, and make the scoping VISIBLE in the text.
   Gated: the house hedge `read end to end` -- already this document's own vocabulary elsewhere in the
   file (pre-edit `:136`) and the needle of a retired guard -- must appear within 600 characters after the bullet
   `- He is NOT the authority for the PRODUCTION side`.

   **The region scope is load-bearing, not fussiness.** Measured: `rg -i -c 'read end to end'` over the
   whole file returns 1 TODAY (`:136`), so a file-wide leg for this would pass before any edit was
   made. Writing the hedge into the `:106` bullet is what the gate requires; writing it anywhere else
   does not satisfy it.

**D. The step-6 sweep -- four surviving claims the removal makes false.** All four are BLOCKING.

- `:7` claims the document SETTLES what to call things and which author to cite for each cell. That
  is a claim to operative authority, and it would sit on the same screen as a header saying the
  document is not accepted and not an agent input. Rewrite it to describe what the document RECORDS.
- `:81` justifies omitting a matrix diagram on the ground that the page is read mid-cycle and a list
  is cheaper to scan than a grid. After the removal the page is read mid-cycle by nobody. It was
  already weak: `lz-red/SKILL.md:52-100` routes steps 2-5 to four other references and never routes
  to this document at all. Drop the justification or restate it without the readership claim.
- `:211-215` claims a gate checks the never-bare-word rule across all three skills' reference trees
  and all three routers, exempting the three copies of this document. There are no three copies and
  the exemption is deleted. The prior acceptance review certified this sentence TRUE, which is
  exactly why step 6 exists. Restate it in the past tense as a record of what the gate did while the
  document shipped, or delete it.
- `:289` declares the two NOT-OPTIONAL qualifiers "stated in full" behind a relative
  `[principle-backing.md](principle-backing.md)` link. That link already dead-ended in two of three
  copies and dead-ends always from here. This is the document's ONLY non-anchor relative link
  (verified). Either state the two qualifiers inline or name the dependent by its repository path
  without a relative link.

Advisory, and a reviewer will flag it: `:171` and `:174` make present-tense claims of an operative
role ("this is the reason the document exists", "what this page adds is"). Not false, but they sit
under a header calling the document inert. Adjust the tense if it is cheap; it is not blocking.

**E. Leave the planning history alone.** `.planning/.continue-here.md` and `.planning/HANDOFF.json`
describe the merged three-copy state. They are dated planning records, not shipped claims, and
rewriting history is worse than a dated record. This omission is DELIBERATE and is stated here so a
reviewer does not read it as a miss.

**F. Keep the archive shape-clean.** Guard N1 now scans this file. Do not introduce a ragged row
while editing, and do not introduce a backslash-escaped pipe inside a table cell.

**G. Public-repo hygiene by ALLOWLIST-INVERSION, over FOUR subjects (T-lc9-01).** AGENTS.md requires
inversion, not a zero-tokens-in-one-file check, and it requires the maintainer's commit IDENTITY to be
the public contact. Assert the only email-shaped token present is the approved public contact
`larsbrinknielsen@gmail.com` and flag every other match. NEVER write the forbidden address or its bare
domain as a search needle, plain, split or obfuscated -- the needle is itself the leak, and this repo
has a recorded twice-recurring leak of exactly this class.

The token pattern is `[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}`. **In `rg`, `-E` means
`--encoding`, NOT extended-regexp.** Passing `-E <pattern>` makes rg exit 2 with an "unknown encoding"
error, print nothing, and a downstream `wc -l` then reads 0 -- a silent PASS. That fail-open was found
in this plan's own draft leg. Pass the pattern positionally, with no `-E`.

Four subjects, all four in the verify chain:

1. **The archived copy** -- inversion over `.planning/research/test-double-taxonomy.md`.
2. **This task's commit MESSAGES** -- subject plus body of every `quick-260729-lc9` commit. The threat
   names the commit messages; a file-only scan does not cover them.
3. **This task's commit IDENTITIES** -- author AND committer email of every `quick-260729-lc9` commit,
   inverted against the approved contact. This is MAINTAINER-SCOPED BY CONSTRUCTION: it selects commits
   by this task's own message tag, so it can never see, let alone block, an outside contributor's
   identity. Do not generalise it to a blanket scan over all commits.
4. **The shipped public surface** -- do NOT hand-roll a scanner for this. The repo already has one:
   `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` performs exactly this inversion
   (`APPROVED_EMAILS` + `EMAIL_RE`, enumerate-then-subtract) across all three skill trees, the root
   prose and both manifests, and it exits non-zero on a failure. Measured: it exits 0 today. Reuse it,
   and note the bonus -- it also runs the ASCII gate over the new router sentences Task 3 wrote.

   Its own summary line prints a scanned-file count. Do NOT copy that count into this plan or into the
   SUMMARY as a fixed literal: its `wideTargets` walks all three skills' `references/` directories, so
   it includes the three taxonomy copies and the count DROPS BY THREE when Task 3 deletes them. Assert
   the exit code, which is invariant; the count is the tool's own output and re-derives itself.

Anti-vacuity for all four: the chain first pins each of the FIVE MANDATORY commit subjects
individually, at count 1 each, plus an upper bound of SIX on the conventional-type
`quick-260729-lc9` total. Without those legs, subjects 2 and 3 would pass on an empty commit set.

**Why per-subject pins replaced the single `= "5"` total (NB-5).** The old form was a hand-editable
literal: step zero's clean-tree fallback told the executor to change 5 to 4, which is a verify leg the
EXECUTOR edits -- latitude a verify block must never grant, however fail-closed the branch happens to
be. Five exact subject pins carry no branch-dependent number at all, so neither fallback branch requires
an edit, and they are strictly TIGHTER than a total: a total of five cannot say WHICH five, while these
name every one. The `-le 6` bound is a plan-structural ceiling (five mandatory plus at most one
step-zero commit), not a tree measurement, and it is what still catches a stray extra commit.

The five mandatory subjects, each pinned at exactly one occurrence:

1. `test(quick-260729-lc9): add nine guards with proofs, RED baseline recorded`
2. `fix(quick-260729-lc9): name the Test Spy, drop the Mock rule label, attribute mockist`
3. `refactor(quick-260729-lc9): scope the test-double taxonomy to planning, re-scope the battery`
4. `refactor(quick-260729-lc9): delete the unreferenced count-guard surface`
5. `docs(quick-260729-lc9): archive the taxonomy as an inert record, fix C-B1, sweep the claims`

Subject 4 is what forces Task 3's split to actually happen: collapsing 3a and 3b into one commit leaves
that pin at 0 and the chain fails. The step-zero commit
(`docs(quick-260729-lc9): commit the revised plan and its planning artifacts`) is the OPTIONAL sixth and
is deliberately NOT pinned, because whether it exists depends on the tree state at Task 1.

**H. Gate D-04 mechanically, not by inference.** `git status --porcelain` being empty proves the tree is
committed; it does NOT prove `.claude/agents/` was left alone. The verify asserts that ZERO
`quick-260729-lc9` commit touches any path under `.claude/agents/`, which is the actual D-04 claim.
Measured today: that count is 0, and the same command form returns non-empty output for commits that DO
touch the directory, so it is a discriminating check rather than a constant zero.
  </action>

  <verify>
    <automated>test -s .planning/research/test-double-taxonomy.md && test "$(rg -c -F 'Clean Code versus Beck on whether a build failure is a valid red' .planning/research/test-double-taxonomy.md)" = "1" && rg -q 'INERT RECORD' .planning/research/test-double-taxonomy.md && rg -q 'plugin-wide shared reference' .planning/research/test-double-taxonomy.md && rg -q -F -e 'not an agent input' .planning/research/test-double-taxonomy.md && rg -q -F -e '260729-2ig-ACCEPTANCE-REVIEW.md' .planning/research/test-double-taxonomy.md && rg -U -q '2 BLOCKING[\s\S]{0,120}15 IMPORTANT[\s\S]{0,120}10 MINOR' .planning/research/test-double-taxonomy.md && rg -q -F -e 'has no search tool' .planning/research/test-double-taxonomy.md && rg -q 'does or does not name' .planning/research/test-double-taxonomy.md && rg -U -i -q 'He is NOT the authority for the PRODUCTION side[\s\S]{0,600}read end to end' .planning/research/test-double-taxonomy.md && rg -U -q 'Clean Code versus Beck on whether a build failure is a valid red[\s\S]{0,900}TDD is Kanban for Code' .planning/research/test-double-taxonomy.md && ! rg -q -F 'report and essays cited in his rows above' .planning/research/test-double-taxonomy.md && ! rg -q -F 'settles what to CALL things' .planning/research/test-double-taxonomy.md && ! rg -q -F 'This page is read mid-cycle' .planning/research/test-double-taxonomy.md && ! rg -q -F 'exempting the three copies of this document' .planning/research/test-double-taxonomy.md && ! rg -q '\]\(principle-backing\.md\)' .planning/research/test-double-taxonomy.md && test "$(git log --format=%s | rg -c -F -e 'test(quick-260729-lc9): add nine guards with proofs, RED baseline recorded' || echo 0)" = "1" && test "$(git log --format=%s | rg -c -F -e 'fix(quick-260729-lc9): name the Test Spy, drop the Mock rule label, attribute mockist' || echo 0)" = "1" && test "$(git log --format=%s | rg -c -F -e 'refactor(quick-260729-lc9): scope the test-double taxonomy to planning, re-scope the battery' || echo 0)" = "1" && test "$(git log --format=%s | rg -c -F -e 'refactor(quick-260729-lc9): delete the unreferenced count-guard surface' || echo 0)" = "1" && test "$(git log --format=%s | rg -c -F -e 'docs(quick-260729-lc9): archive the taxonomy as an inert record, fix C-B1, sweep the claims' || echo 0)" = "1" && test "$(git log --format=%s | rg -c '^(test|fix|refactor|docs)\(quick-260729-lc9\)' || echo 0)" -le 6 && test "$(git log --format=%H --grep='quick-260729-lc9' -- .claude/agents/ | wc -l)" = "0" && test "$(git log --format='%ae%n%ce' --grep='quick-260729-lc9' | sort -u | rg -v -F -x 'larsbrinknielsen@gmail.com' | wc -l)" = "0" && test "$(git log --format='%s%n%b' --grep='quick-260729-lc9' | rg -o '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' | rg -v -F -x 'larsbrinknielsen@gmail.com' | wc -l)" = "0" && test "$(rg -o -N --no-filename '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' .planning/research/test-double-taxonomy.md | rg -v -F -x 'larsbrinknielsen@gmail.com' | wc -l)" = "0" && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && claude plugin validate . && test "$(git status --porcelain | wc -l)" = "0"</automated>

    <verify_leg_notes>
OBSERVED EXIT CODES, run verbatim this session. Do not trust these; re-run them.

- Whole chain, unmodified tree: **exit 1**.
- Whole chain against a `touch`-created EMPTY file at the target path: **exit 1** (leg 1, `test -s`).
- Whole chain against a VERBATIM copy of the shipped taxonomy at the target path -- i.e. Task 3's
  `git mv` done and NONE of Task 4's edits: **exit 1**. Legs 3, 4, 5 each individually exit 1 on that
  copy, so a move-without-edits cannot pass.

WHY LEG 2 IS AN ANCHOR AND MUST STAY FIRST. Every `! rg -q ...` leg below it is FAIL-OPEN in
isolation: `rg` on a missing path writes its error to stderr and exits 2, and `!` turns that into 0.
Measured: `! rg -q -F 'report and essays cited in his rows above' <absent path>` exits **0**. Leg 2 is
a POSITIVE, COUNTED `rg` on the same file, so it proves the file exists, is readable, and was actually
scanned before any negative leg runs. Do not reorder, and do not replace it with `test -f`.

WHY TWO LEGS ARE REGION-SCOPED WITH `-U`. A file-wide needle for a claim about ONE paragraph is the
"needle outlives its subject" anti-pattern. Measured proof that this is not theoretical: file-wide
`rg -i -c 'read end to end'` on the taxonomy returns **1 today** (`:136`), so a file-wide leg for the
`:106` scoping would PASS VACUOUSLY before any edit. The 600-char window excludes `:136` and is RED at
baseline; widening it to 3000 makes it pass, which is how the window was calibrated. The positive
control for the region-regex form itself -- same anchor, `{0,900}`, needle `kanban cycle` -- exits 0,
proving the pattern matches when the target is present.
    </verify_leg_notes>
  </verify>

  <done>
- The archived copy opens with an inert-record header naming its status, its purpose, its three
  failed gates, what remains open, and the D-02 forward constraint.
- The C-B1 Beck attribution points at the surface the dependents name, and it is MACHINE-CHECKED both
  ways: the wrong-source literal is gone, and the essay title `TDD is Kanban for Code` is present
  INSIDE the Clean-Code-versus-Beck paragraph, region-scoped so a
  mention elsewhere in the file cannot satisfy it. This is the leg the prior revision was missing
  entirely, which let an executor skip the owner's step 1 and still exit 0.
- BOTH halves of D-11 hold: the absence hard rule carries the reinstated `does or does not name`
  clause, AND the `:106` production-side claim is visibly scoped, checked by a region-scoped needle
  rather than a file-wide one that would have passed vacuously.
- None of the four falsified claims survives: the settles-authority lead, the read-mid-cycle
  justification, the three-copies gate sentence, and the dead relative link are all gone.
- Allowlist-inversion is clean across all FOUR subjects: the archived copy, this task's commit
  messages, this task's commit author AND committer identities, and the shipped public surface via the
  repo's existing `check-hygiene.mjs`. The identity check is maintainer-scoped by construction.
- D-04 is gated mechanically, not inferred: ZERO `quick-260729-lc9` commit touches `.claude/agents/`.
- All FIVE mandatory `quick-260729-lc9` commit subjects exist, each exactly once, and the
  conventional-type total is at most six. That is both the commit accounting and the anti-vacuity control
  for the two commit-scoped hygiene legs, and it carries no branch-dependent literal (NB-5).
- The inert-record header carries all SIX mandated literals, not just its status claim (NB-6).
- Battery exits 0, selftest exits 0, `check-hygiene.mjs` exits 0, `claude plugin validate .` passes,
  `git status` is clean.
- Commit: `docs(quick-260729-lc9): archive the taxonomy as an inert record, fix C-B1, sweep the claims`
  </done>
</task>

</tasks>

<step_leg_map>
## Every step, and the leg that FAILS if that step is skipped

Four iterations of plan-checking produced the same defect shape every time: a step the plan performs
that no leg observes, or a leg that observes something adjacent to what its prose claims. This table is
the standing answer. It is exhaustive over all four tasks including step zero, and it exists so a
reviewer can spot the next hole without re-deriving the chain. Leg numbers count `&&`-separated legs from
1 within each task's own `<automated>` block. Block sizes: Task 1 = 25, Task 2 = 16, Task 3 = 33,
Task 4 = 31.

| Step | Leg that fails if it is skipped |
|---|---|
| T1 step zero (commit the planning artifacts, derived from `git status`) | T4 leg 31, `git status --porcelain` empty. Untracked `PLAN-CHECK*.md` reports keep it non-empty. |
| T1 A1 shared escape-aware splitter | T1 leg 6, `splitCells` >= 3 in `lib/pipe-table.mjs`; plus leg 4, the `escaped pipe` selftest case |
| T1 A2 `scanTables` + offender string | T1 legs 2 and 5; plus legs 17 and 18, N1's emitted PASS line |
| T1 A3 `findLinkTargets` | T1 leg 3; plus legs 15 and 16, N2's two `:289` hits |
| T1 A4 header names the two new exports | T1 legs 7 and 8, region-bracketed to the header comment block |
| T1 B0 evaluator whole-text opt-in | T1 legs 9 and 10, `topic.wholeText` and `wholeText: true` each exactly once |
| T1 N1 anti-vacuity counter | T1 leg 11, `tablesSeen` / `linksSeen` >= 2 lines |
| T1 N2 anti-vacuity counter | T1 leg 11 (same combined leg) |
| T1 N1..N9 all nine added | T1 legs 13 and 14: exactly 8 `[FAIL]` lines and the roster PASS at 183 |
| T1 N9 region-scoped needle + placement | T1 legs 9, 10 and 14; the discrimination itself is recorded, T1 leg 25 |
| T1 D RED-BASELINE record | T1 legs 19-25, seven content sub-legs; a `touch` fails leg 19 |
| T2 fix 1 Test Spy at both sites | T2 legs 7 and 8, each region-BRACKETED to its own site |
| T2 fix 2 six `Mock rule` labels | T2 leg 6, zero files under `plugins/`; plus leg 3, exactly 4 `[FAIL]` |
| T2 fix 3 mockist attribution | T2 leg 9, `Fowler's label` in exactly 2 files |
| T2 blast-radius: 7 out-of-scope sites | T2 legs 10-15, one literal pin each, plus leg 16 the pipe-line count |
| T3 A `git mv` FIRST, no content change | T3 leg 18, `git log -M` reports `R100` exactly once; a move-plus-edit gives `R099` |
| T3 A archive exists and is non-empty | T3 leg 19, `test -s` |
| T3 B lz-red inline rule, in the coach procedure | T3 leg 20; placement by guard N9, which must PASS for leg 4's exit-0 battery |
| T3 B lz-tpp inline rule | T3 legs 20 and 21 |
| T3 B lz-refactor taxonomy section DELETED, no replacement (F-4) | T3 leg 15, the pointer gone from `plugins/`. No heading pin exists BY DESIGN -- the correct outcome is an absence. |
| T3 D `TAX` deleted from `lib/row-guards.mjs` (F-2) | T3 leg 23, `\bTAX\b` count 0, anchored by leg 22 |
| T1 B0 N9 needle SHAPE, not just the flag (F-3) | T1 leg 26, `## Reference material` present in the checker source, count >= 1 -- fails if the forbidden bare needle is used |
| T3 C `principle-backing.md` swept | T3 leg 17, `taxonom` absent, anchored by leg 16 |
| T3 D1 delete the FILES entry | T3 leg 5, 125 checks |
| T3 D2 delete the sha256 block | T3 leg 6, no retired label survives |
| T3 D3 retire the row-scoped and count entries | T3 leg 1 via the selftest's key-set assertions; plus leg 6 |
| T3 D4 delete chronology, `TAXONOMY_BASENAME`, G17's filter | T3 legs 6 and 28. Residue: `taxonomyText`, followup item 2d. |
| T3 D5 drop the `COUNT_GUARDS` import | T3 leg 24 |
| T3 D6 prune `NEW_LABELS`, set 125 | T3 legs 5 and 7 |
| T3 D7 add the archive path to N1 | T3 legs 13 and 8. Wiring: followup item 6. |
| T3 D8 three stale comment claims | T3 leg 14, one combined negated leg, anchored by leg 13 |
| T3 D8 the emitted SUMMARY string | T3 legs 11 and 12 |
| T3 D8 G17's emitted LABEL | T3 legs 9 and 10, both directions |
| T3 D9 `RETIRED_LABELS` 6 -> 64 | T3 leg 6, which prints the list length |
| T3 D10 3a half, three entries deleted | T3 leg 1 via the selftest key-set assertion |
| T3 D10 3b dead-surface deletion | T3 legs 23, 25, 26 and 28, anchored by leg 22 |
| T3 D10 3b touches nothing else | T3 legs 31 and 32, the changed-file set |
| T3 D11-D14 selftest repairs | T3 leg 1 (selftest exits 0) and leg 2 (the mandated replacement label) |
| T3 D15 `pipe-table.mjs` header rewrite | T3 legs 29 and 30, anchored pair |
| T4 A inert-record header | T4 legs 3-8, the six mandated literals |
| T4 B C-B1 re-source | T4 legs 11 and 12, region-scoped plus the negated wrong source |
| T4 C1 D-11 absence rule | T4 leg 9 |
| T4 C2 scope the `:106` claim | T4 leg 10, region-scoped to 600 chars |
| T4 D four falsified claims | T4 legs 13-16, one negated literal each |
| T4 E leave the planning history alone | **NO LEG, and deliberately so.** A "leave it alone" step is satisfied by inaction. The obvious commit-scoped leg is UNSOUND: the pre-existing `wip(quick-260729-lc9)` commit already touches both paths, so a `--grep` path filter counts 1 today and false-fails. Routed to followup item 3. |
| T4 F keep the archive shape-clean | T4 leg 29, the bare battery run -- meaningful ONLY because T3 D7 put the path in N1's scope |
| T4 G hygiene over four subjects | T4 leg 26 (archive), leg 25 (commit messages), leg 24 (identities), leg 27 (`check-hygiene.mjs`) |
| T4 H the `.claude/agents/` gate | T4 leg 23 |
| Six commits, five of them mandatory | T4 legs 17-21, one exact subject pin each, plus leg 22's `-le 6` ceiling |

**Six residues have no leg and each is named above rather than hidden:** Task 4 section E (unguardable by
construction); N1's archive path being in the LOOP rather than in the FILE; `splitCells` being called
from both consumers; `tablesSeen` / `linksSeen` actually gating a `report()`; the `scanTables` offender
string being PRODUCED rather than merely present; and the honesty of every piece of prose the plan
mandates by literal. All six are in `<orchestrator_followup>`. None is a step that can be silently
skipped -- each is a finer-grained property of a step whose skipping some leg does catch.
</step_leg_map>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub | This is a public repo; committed content and commit identity are published. |
| planning artifact -> agent context | An archived document that an agent might later be wired to read. |
| guard source -> guarded content | A guard whose needle no longer has a subject reports a false GREEN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-lc9-01 | Information disclosure | archived copy + 6 commit messages + 6 commit identities + the shipped public surface | high | mitigate | Task 4 runs ALLOWLIST-INVERSION (enumerate every email-shaped token, subtract the approved public contact, assert the remainder is empty) over all four subjects: the archived copy; every `quick-260729-lc9` commit MESSAGE (subject + body); every such commit's AUTHOR and COMMITTER email, maintainer-scoped by selecting on this task's own message tag so an outside contributor is never in scope; and the shipped surface via the repo's existing `check-hygiene.mjs`. Anti-vacuity: the chain first pins the five mandatory commit subjects individually. Never encode a forbidden value as a needle. |
| T-lc9-02 | Tampering | guard retirement | high | mitigate | 58 retirements recorded BY NAME in `RETIRED_LABELS` and asserted absent by the roster gate; without this a deliberate retirement and an accidental drop are the same green run. |
| T-lc9-03 | Repudiation | guard-that-cannot-fail | high | mitigate | The byte-identity gate is deleted outright rather than narrowed to a vacuous one-element predicate; N1 and N2 each carry an explicit anti-vacuity leg on their own seen-count. |
| T-lc9-04 | Elevation of privilege | oracle agent inputs | medium | mitigate | D-04: no file under `.claude/agents/` is edited. Task 4's verify gates this MECHANICALLY -- zero `quick-260729-lc9` commits touch that path -- rather than inferring it from an empty `git status`, which proves only that the tree is committed. Wiring the map in would be a new data path. |
| T-lc9-05 | Denial of service | dead shipped links | medium | mitigate | Guard N2 is added BEFORE the deletion that creates five dead links, with its RED-at-baseline proof captured first. |
| T-lc9-06 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; no dependency is added. Node builtins only, matching the existing modules. |
</threat_model>

<verification>
Run at the end of Task 4, all four legs, `&&`-chained:

```
node .claude/skills/lz-red-workspace/tools/check-red-references.mjs \
  && node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs \
  && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs \
  && claude plugin validate . \
  && test "$(git status --porcelain | wc -l)" = "0"
```

`check-hygiene.mjs` is the repo's EXISTING allowlist-inversion + ASCII gate over the shipped public
surface. It is added here rather than hand-rolled; measured, it exits 0 today, and it covers every
`plugins/` file Tasks 2 and 3 edit. Assert its EXIT CODE only -- its scanned-file count includes the
three taxonomy copies and therefore drops when Task 3 deletes them.

Plus the two proofs that cannot be re-derived after the fact and must therefore be READ from
`260729-lc9-RED-BASELINE.md`:

- guard N2's RED baseline, two hits with `file:line`, captured in Task 1 before the deletion;
- guard N1's fixture set, including the pipe-DELETION case that returns exactly one offender;
- guard N9's three-way discrimination, measured THROUGH THE REAL EVALUATOR (`absent -> RED`,
  `in-procedure -> GREEN`, `appendix-only -> RED`). Only the middle result is re-derivable afterwards, by
  the battery going green at Task 3; the other two are not, and `appendix-only -> RED` is the one that
  proves the label is not overclaiming its placement.

**A green battery is not acceptance.** This document has gone fully green over a blocking defect
three times. The archived copy's CONTENT is gated only by the sixteen literal legs in Task 4 plus guard
N1's table shape; the honesty of the prose around those literals has no machine gate at all, so an
independent unprimed review is the only gate on it, and the orchestrator owns that step because the
executor has no Agent tool.

**The table-shape half is real only because Task 3 step 7 is now legged.** Before this revision the
archive path entered no assertion anywhere, so an executor could skip step 7 and the whole chain still
went green at 125 -- which would have left this paragraph, `must_haves.truths` entry 3 and Task 4 section
F all false at once. The step 7 legs pin the path into the checker and carry N1's PASS line forward into
Task 3's chain; `<orchestrator_followup>` item 6 reads the loop itself.
</verification>

<success_criteria>
- No file named `test-double-taxonomy.md` exists under `plugins/`, and the battery fails by name if
  one reappears.
- `check-red-references.mjs` exits 0 at 125 emitted checks plus the roster gate.
- `row-guards.selftest.mjs` exits 0, including the pipe-deletion fixture that makes the shape guard
  fail and the link-classification fixtures.
- `claude plugin validate .` passes; no dead relative link in any shipped SKILL.md or reference.
- lz-red and lz-tpp each carry the side-qualification rule inline, with the qualifier and the word on
  the same line so G17's allowlist still applies.
- The archived copy carries the inert-record header, the re-sourced C-B1 Beck attribution, both halves
  of D-11, and none of the four falsified claims.
- SIX atomic commits -- Task 1 makes two (the planning-artifact commit, then the instrument commit) and
  Task 3 makes two (the observable re-scope, then the dead-surface deletion) -- `git status` clean, no
  stray worktrees or branches. The planning artifacts and `STATE.md` are OWNED by Task 1's first commit;
  without that, Task 4's clean-tree leg is unsatisfiable. That first commit is the only OPTIONAL one, so
  a clean tree at Task 1 yields five, and the verify chain is unchanged either way.
- Allowlist-inversion clean over four subjects (archived copy, commit messages, commit identities,
  shipped surface via `check-hygiene.mjs`); zero `quick-260729-lc9` commits touch `.claude/agents/`.
- Guard N9 PASSES, which is only possible because Task 1's B0 evaluator fix landed; and it still FAILS
  on an appendix-only placement, which is what its label claims.
- Zero spend: no `claude -p`, no metered run, no eval. The runtime intent-disambiguation artifact and
  its A/B (D-12) are NOT authored.
</success_criteria>

<orchestrator_followup>
The executor has NO Agent or Task tool and cannot spawn a reviewer. After the executor returns, the
ORCHESTRATOR must:

1. Spawn at least one WHOLLY UNPRIMED reviewer (no prior findings in its brief, firewalled from
   `.oracle/`) over the archived copy and the **TWO** replacement router sentences -- lz-red's and
   lz-tpp's. **There are two, not three: F-4 replaced lz-refactor's section with plain DELETION, so no
   third replacement is authored and none must be.** If the reviewer proposes adding lz-refactor prose,
   that is out of scope; say so and move on. Three consecutive rounds of this work stream shipped a
   blocking defect behind a green battery; the unprimed review is the gate, and a primed reviewer
   confirms the assumptions instead of testing them. No leg reads the BODY of either replacement, so
   this item owns their content outright.
2. Spawn an instrument auditor over the 58 retirements: for each, confirm the label string matches
   what the pre-change run actually emitted, and confirm no surviving guard was weakened in place
   (a weakened guard leaves the roster count unchanged, so the roster gate is blind to it). **This gate
   is NOT optional and has no machine substitute**: a mistyped retired label is trivially "not emitted",
   so the roster gate cannot see a transcription error, and the pre-change emitted set no longer exists
   after Task 3's first commit. Include FOUR further items in its scope, each one a residue this plan
   states rather than legs: (a) the step-12 replacement assertion -- confirm it actually derives
   non-emptiness, duplicate-freedom and the filename/tag shape rather than asserting a constant; (b) the
   `scanTables` offender-string fixture -- confirm the pinned verbatim string is PRODUCED by `scanTables`
   and is not a comment or a value hardcoded on both sides of one `check()` (NEW-4); (c) `TAXONOMY_LINES`
   survived as a synthetic fixture, since only the existing `parseRows` cases indirectly guard it;
   (d) no orphaned `taxonomyText` binding was left in the checker.
   **`TAX` is deliberately NOT in this list: it is now LEGGED (F-2), block 3 leg 23.** An earlier
   revision called it unguardable on a reason that measured false -- `TAXONOMY_LINES` is absent from
   `lib/row-guards.mjs`, so no false-match is possible. Do not re-add it as a residue.
3. Confirm the executor did not edit any file under `.claude/agents/` (D-04). Also confirm
   `.planning/.continue-here.md` and `.planning/HANDOFF.json` were NOT rewritten (Task 4 section E).
   **That one deliberately has no leg**: it is a "leave it alone" instruction, satisfied by inaction, and
   the obvious commit-scoped leg is UNSOUND here -- the pre-existing `wip(quick-260729-lc9)` commit
   already touches both paths, so a `--grep='quick-260729-lc9'` path filter counts 1 today and would
   false-fail. Measured, not assumed. Do not add that leg in a later round.
4. Confirm guard N9's B0 evaluator fix is the ONE-LINE additive form and that `wholeText: true` is set on
   N9 alone -- a blanket flip would loosen every other per-line topic, and the roster gate cannot see a
   loosened guard. **Also confirm N9's needle is the REGION-SCOPED form verbatim,
   `/which side they mean[\s\S]*?\n## Reference material/i`, and NOT the forbidden bare
   `/which side they mean/i`.** Block 1 leg 26 now pins the `## Reference material` literal (F-3), which
   the bare form cannot satisfy, so this read is a complement rather than the sole gate -- but read it
   anyway: the leg proves the literal is present somewhere in the checker, not that it sits inside N9's
   own needle. Widened this revision, two more reads on the same pass: that `splitCells` is really
   called from BOTH `parseRows` and `scanTables` (the count leg proves only that the name appears three
   times), and that `tablesSeen` / `linksSeen` actually gate their guards' `report()` rather than merely
   being computed.
5. Do NOT commit `260729-lc9-PLAN.md`, the `260729-lc9-PLAN-CHECK*.md` reports or `.planning/STATE.md`
   before dispatching the executor. Task 1's step zero owns that commit. Committing first is harmless to
   the verify chain (both branches satisfy it unmodified), but it silently drops the plan from six
   commits to five.
6. Read guard N1's target list and confirm `.planning/research/test-double-taxonomy.md` is IN THE LOOP,
   not merely present somewhere in the checker. One line. This is the deliberate complement to Task 3
   step 7's two legs, which pin the path's presence and N1's continued reporting but cannot distinguish a
   wired path from a mentioned one. A mutation test was considered and rejected as too heavy for a verify
   chain; see step 7 for the reasoning.
7. Read `lib/pipe-table.mjs`'s rewritten header and confirm it carries no measurement of a file the
   module no longer targets and no census number that a later deletion will falsify. The legs pin the
   departed basename's absence and the two export names' presence; neither can see a NEW stale number.
   **Specifically check the header's FIRST LINE (F-1).** It must still carry the token `ROW PARSER` --
   both A4 legs bracket on it -- but the rest of that line must have been REWRITTEN: it originally read
   `One exported pure function over a GitHub pipe table`, which Task 1 falsifies by adding exports. No
   leg can see this, because the bracket needle matches the token regardless of what follows it. An
   earlier revision's "keep as-is" wording actively ORDERED this stale claim, so a competent executor
   following that text would have shipped it.
</orchestrator_followup>

<output>
Create `.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-SUMMARY.md`
when done. Record, per task: the commit SHA, the emitted-check count, and any hole you found rather
than banked -- the four holes the last two executors self-reported were all CONFIRMED, so a
self-reported weakness is worth more than a banked pass.
</output>
