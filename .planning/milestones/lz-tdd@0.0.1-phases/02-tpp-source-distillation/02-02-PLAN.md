---
phase: 02-tpp-source-distillation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - plugins/lz-tdd/skills/lz-tpp/references/transformations.md
autonomous: true
requirements: [TPP-01, TPP-02, TPP-03, TPP-04]
user_setup: []

must_haves:
  truths:
    - "references/transformations.md is the single SHIPPED canonical reference file, its content limited to phase-2 subject matter (D-01)"
    - "The file excludes TypeScript examples, the worked Fibonacci/Word-Wrap walkthroughs, the coach decision procedure, and the opinionated TS/JS ordering overlay; it captures ONLY the source language-specificity caveat as provenance (D-02)"
    - "The revised 14-item FibTPP list is adopted as canonical, with the original 12-item list, the 3 FibTPP revisions, and secondary-source drift documented and the 12-vs-14 discrepancy resolved explicitly rather than silently (D-03)"
    - "Every list entry and key quote is verbatim-faithful and inline-cited to its specific primary post (14-item -> FibTPP, 12-item + defs/mantra/premise + most hedges -> TPP, 'language specific' -> FibTPP), with all arrows normalized to ASCII -> and a one-line notation note (D-04)"
    - "Provenance and reconciliation appear as clearly delimited sections WITHIN the single file, following the D-05 section order (D-05)"
    - "The NDC 2011 transcript is reconciled against the blog list with discrepancies NOTED not silently resolved, stating explicit precedence blogs > talk > secondary (D-06)"
  artifacts:
    - path: plugins/lz-tdd/skills/lz-tpp/references/transformations.md
      provides: "Canonical, cited TPP reference: 14-item list, 12-vs-14 resolution + provenance, transformations-vs-refactorings distinction, provisional-heuristic framing, amended red-green-refactor process, and NDC reconciliation (D-01, D-03, D-04, D-05)"
      contains: "(statement -> tail-recursion)"
      min_lines: 60
  key_links:
    - from: "plugins/lz-tdd/skills/lz-tpp/references/transformations.md"
      to: "https://blog.cleancoder.com/uncle-bob/2013/05/27/FibTPP.html"
      via: "inline citation of the canonical 14-item list + language caveat"
      pattern: "FibTPP\\.html"
    - from: "plugins/lz-tdd/skills/lz-tpp/references/transformations.md"
      to: "https://blog.cleancoder.com/uncle-bob/2013/05/27/TheTransformationPriorityPremise.html"
      via: "inline citation of the 12-item list, definitions, mantra, premise, hedges, amended RGR"
      pattern: "TheTransformationPriorityPremise\\.html"
    - from: "plugins/lz-tdd/skills/lz-tpp/references/transformations.md (reconciliation section)"
      to: ".planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md"
      via: "reconciliation notes citing the retained transcript (D-06)"
      pattern: "reconcil"
---

<objective>
Author the single SHIPPED canonical reference
`plugins/lz-tdd/skills/lz-tpp/references/transformations.md` (creating the `references/`
dir): the verbatim-faithful, cited 14-item FibTPP transformation-priority list, the
12-vs-14 discrepancy resolution + provenance, the transformations-vs-refactorings
distinction, the provisional-heuristic framing (the author's own hedges), the amended
red-green-refactor process, and the NDC-2011 transcript reconciliation -- all ASCII-only.

Purpose: Lock a correct, cited source-of-truth BEFORE any skill behavior is authored on
top of it (Phase 3). The value here is fidelity and provenance, not authorship -- the
file should read as a faithful, well-cited restatement of two primary posts with every
deviation from the raw source (arrow normalization, canonical-list choice) documented.
Output: `plugins/lz-tdd/skills/lz-tpp/references/transformations.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tpp-source-distillation/02-CONTEXT.md
@.planning/phases/02-tpp-source-distillation/02-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

# Produced by plan 02-01 (soft dependency for the reconciliation section only):
@.planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md

Authoring rules (from RESEARCH.md, load-bearing):
- Transcribe list entries and key quotes from RESEARCH.md "Verified Content Anchors"
  and FEATURES.md Part 2 -- NOT from Wikipedia or memory (secondary sources have drifted).
- Citation micro-format (discretion, resolved for this plan): inline parenthetical label
  "(FibTPP post)" / "(TPP post)" next to each list and quote, plus a Sources section
  mapping each label to its full URL. The 14-item list cites FibTPP; the 12-item list
  cites the TPP post; each hedge cites the post it actually came from.
- Hedge attribution is SPLIT: "roughly ordered", "informal at best", "not likely",
  "probably not", "might be more complicated than a simple ordinal sequence" -> TPP post;
  "language specific" -> FibTPP post. See RESEARCH.md "Author's hedges" table.
- "it's" typo decision (discretion, resolved): quote the transformations-vs-refactorings
  definition with the source spelling "it's" and append "[sic]" so fidelity is deliberate.
- ASCII-only: normalize every arrow to `->`, every quote/dash/ellipsis to ASCII.
- Section order fixed by D-05 (see RESEARCH.md "Recommended section layout").
- Quote the LIST + KEY definitions/hedges/process only; summarize narrative (do not paste
  large essay swaths -- PITFALLS Pitfall 6 / over-quoting).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical core (definitions, framing, 14-item list, notation, amended RGR)</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/transformations.md</files>
  <read_first>
    - .planning/phases/02-tpp-source-distillation/02-CONTEXT.md (D-01, D-02, D-04, D-05)
    - .planning/phases/02-tpp-source-distillation/02-RESEARCH.md ("Verified Content Anchors": CANONICAL 14-item list, transformations-vs-refactorings, mantra, premise, hedges table, amended red-green-refactor process, notation anchors; "Recommended section layout")
    - .planning/research/FEATURES.md Part 2 (lines 168-258: Core Concepts, canonical 14-item list, notation notes, amended RGR)
  </read_first>
  <action>
    Create the `references/` dir and the file. Add an optional one-line "how produced /
    reconciled" provenance header note at the top (D-05 discretion -- include it). Then
    author these sections in D-05 order: (1) "Transformations vs refactorings" -- the
    verbatim definition quote attributed "(TPP post)", preserving "it's [sic]" (TPP-04);
    (2) "The premise and the mantra" -- the premise quote and the "As the tests get more
    specific, the code gets more generic" mantra, each "(TPP post)"; (3) "TPP is a
    provisional heuristic" -- the author hedges quoted verbatim, each cited to its ACTUAL
    post per the RESEARCH hedges table ("language specific" -> FibTPP, the rest -> TPP)
    (TPP-04); (4) "Canonical transformation priority list" -- all 14 entries verbatim from
    the Verified Content Anchors, in order, arrows as ASCII `->`, attributed "(FibTPP
    post)" (TPP-01); (5) "Notation notes" -- the one-line arrow-normalization note
    (mention ASCII) plus the {}/nil/constant+/scalar/container/(case) glossary (D-04);
    (6) "Amended red-green-refactor process" -- the verbatim 3-bullet block "(TPP post)"
    including the backtrack bullet (D-01). Capture ONLY subject matter -- NO TypeScript
    examples, NO Fibonacci/Word-Wrap walkthrough, NO coach decision procedure, NO
    "prefer iteration in TS" default (D-02 boundary; those are Phase 3). Leave the
    provenance/reconciliation/sources sections for Task 2 (append after this content).
  </action>
  <acceptance_criteria>
    - File exists: `test -f plugins/lz-tdd/skills/lz-tpp/references/transformations.md`.
    - All 14 canonical entries present and the two FibTPP-only markers match: `rg -F '(statement -> tail-recursion)' <file>` and `rg -F '(case)' <file>` each match; `rg -Fc '->' <file>` shows the list arrows present.
    - Transformations-vs-refactorings definition present: `rg -F 'change the behavior of code' <file>` matches.
    - Heuristic hedges present: `rg -F 'informal at best' <file>` and `rg -F 'language specific' <file>` each match.
    - Amended RGR present: `rg -F 'When passing a test' <file>` and `rg -iF 'backtrack' <file>` each match.
    - Notation note present: `rg -iF 'ASCII' <file>` matches.
    - ASCII gate clean: `rg -n '[^\x00-\x7F]' <file>` returns nothing.
    - D-02 boundary (semantic): no TypeScript/JS code block, no Fibonacci/Word-Wrap worked walkthrough, no coach decision procedure in this content.
  </acceptance_criteria>
  <verify>
    <automated>f=plugins/lz-tdd/skills/lz-tpp/references/transformations.md; rg -F '(statement -> tail-recursion)' "$f" >/dev/null && rg -F '(case)' "$f" >/dev/null && rg -F 'change the behavior of code' "$f" >/dev/null && rg -F 'informal at best' "$f" >/dev/null && rg -F 'When passing a test' "$f" >/dev/null && ! rg -n '[^\x00-\x7F]' "$f" && echo CORE_OK</automated>
  </verify>
  <done>The file exists with the definitions, premise/mantra, hedges (correctly attributed), the full verbatim 14-item list (FibTPP-cited, ASCII arrows), notation note, and the amended RGR 3-bullet block -- ASCII-clean, with no Phase-3 scope material (D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Author provenance, 12-vs-14 resolution, secondary drift, NDC reconciliation, and sources</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/transformations.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (the content authored in Task 1)
    - .planning/phases/02-tpp-source-distillation/02-CONTEXT.md (D-03, D-05, D-06)
    - .planning/phases/02-tpp-source-distillation/02-RESEARCH.md ("ORIGINAL 12-item list", "The 3 FibTPP revisions", "Language-specificity caveat", "State of the Art", Assumption A2 on secondary drift)
    - .planning/research/FEATURES.md Part 2 (lines 215-250: original 12-item list, FLAGGED ordering discrepancy, language caveat)
    - .planning/phases/02-tpp-source-distillation/ndc-2011-tpp-transcript.md (retained transcript from 02-01, for the reconciliation section)
  </read_first>
  <action>
    Append, after Task 1's content and still in D-05 order: (7) "Provenance: original
    12-item list" -- all 12 entries verbatim from the Verified Content Anchors, attributed
    "(TPP post)", including the "There are likely others." trailer (TPP-02); (8)
    "Provenance: FibTPP revisions and the 12-vs-14 resolution" -- state the 3 revisions
    (+ `(statement -> tail-recursion)` at #9 above `(if -> while)`; + `(case)` at #14;
    plain `(statement -> recursion)` moved to #11 below `(if -> while)`), and an EXPLICIT
    sentence adopting the 14-item list as canonical while retaining the 12-item for
    provenance (D-03 -- do not silently pick). Capture the FibTPP language-specificity
    CAVEAT here as provenance for the deliberate ordering evolution ("(FibTPP post)"),
    but do NOT add the opinionated TS/JS default (D-02); (9) "Secondary-source drift" --
    note that secondary sources such as Wikipedia commonly render `constant -> scalar`
    as `constant -> variable`, mislabel `statement -> recursion` as "tail recursion", and
    publish only the 12-item list; phrase as "secondary sources commonly ..." rather than
    pinning an exact unverified quote (Assumption A2), and mark them cross-check-only,
    never canonical; (10) "NDC 2011 transcript reconciliation" -- reconcile the retained
    transcript against the blog list, NOTE any discrepancies (do not silently resolve),
    and state the explicit precedence "blogs > talk > secondary"; if 02-01 produced no
    usable transcript, note that fact honestly instead of fabricating agreement (D-06);
    (11) "Sources / citations" -- map the "(FibTPP post)" and "(TPP post)" labels to their
    full URLs and cite the retained transcript path. Do not introduce Phase-3 material.
  </action>
  <acceptance_criteria>
    - Both primary posts cited: `rg -F 'FibTPP.html' <file>` AND `rg -F 'TheTransformationPriorityPremise.html' <file>` each match.
    - 12-vs-14 resolved explicitly: provenance sections present covering the original 12-item list, the 3 FibTPP revisions, an explicit "adopt the 14-item list as canonical" statement, and secondary-source drift.
    - Reconciliation present with precedence: a reconciliation section exists AND `rg -iF 'blogs' <file>` plus a stated precedence over talk/secondary are present (`rg -iF 'precedence' <file>` or an equivalent "take precedence" phrase matches).
    - Language caveat captured as provenance (not as a TS default): `rg -F 'language specific' <file>` matches and no "prefer iteration in TypeScript/JS" coaching default appears (D-02).
    - ASCII gate clean: `rg -n '[^\x00-\x7F]' <file>` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>f=plugins/lz-tdd/skills/lz-tpp/references/transformations.md; rg -F 'FibTPP.html' "$f" >/dev/null && rg -F 'TheTransformationPriorityPremise.html' "$f" >/dev/null && rg -iF 'reconcil' "$f" >/dev/null && rg -F 'language specific' "$f" >/dev/null && ! rg -n '[^\x00-\x7F]' "$f" && echo PROVENANCE_OK</automated>
  </verify>
  <done>The file documents the original 12-item list, the 3 FibTPP revisions with an explicit canonical-choice statement, secondary-source drift (cross-check only), the NDC reconciliation with discrepancies noted and precedence stated, and a Sources section mapping both post labels to URLs -- ASCII-clean, no Phase-3 material.</done>
</task>

<task type="auto">
  <name>Task 3: Full validation gate -- mechanical suite + per-entry/per-quote semantic cross-check + hygiene</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/transformations.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (the completed file)
    - .planning/phases/02-tpp-source-distillation/02-RESEARCH.md ("Verified Content Anchors" for the per-entry/per-quote cross-check; "Validation Architecture" Phase Requirements -> Check Map)
    - .planning/research/PITFALLS.md (Pitfalls 3, 2, 5's negative-check spirit, Security Mistakes ASCII + email rows)
  </read_first>
  <action>
    Run the full mechanical check suite from RESEARCH.md "Validation Architecture" against
    the file and fix any failure IN PLACE (this task may edit the file only to correct a
    failed check, never to add Phase-3 material). Then perform the semantic cross-check:
    compare all 14 canonical entries (exact tokens, descriptions, and order) and every key
    quote against the Verified Content Anchors, and confirm each quote's inline citation
    points to the CORRECT post (14-item -> FibTPP; 12-item + defs/mantra/premise + the
    non-"language specific" hedges -> TPP; "language specific" -> FibTPP). Confirm the
    single-file / no-stray-reference constraint: transformations.md is the only new
    `references/*.md`. Confirm repo hygiene: ASCII-only and work-email absent via the
    allowlist (never spell the work-email literal). Record the check results in the plan
    SUMMARY. Do not create a separate committed checklist file -- `/gsd-validate-phase`
    owns 02-VALIDATION.md generation post-execution.
  </action>
  <acceptance_criteria>
    - Canonical list markers: `rg -F '(statement -> tail-recursion)' <file>` and `rg -F '(case)' <file>` each match; a manual count confirms exactly 14 canonical entries in order matching the Verified Content Anchors.
    - Both citations present: `rg -F 'FibTPP.html' <file>` AND `rg -F 'TheTransformationPriorityPremise.html' <file>` each match.
    - Definition + hedges + amended RGR present: `rg -F 'change the behavior of code' <file>`, `rg -F 'informal at best' <file>`, `rg -F 'language specific' <file>`, `rg -F 'When passing a test' <file>`, `rg -iF 'backtrack' <file>` each match.
    - ASCII gate: `rg -n '[^\x00-\x7F]' <file>` returns ZERO matches.
    - Work-email allowlist over shipped + phase dir returns no output: `rg -uNo -e '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]+' plugins/lz-tdd/skills/lz-tpp/references/transformations.md .planning/phases/02-tpp-source-distillation/ | rg -v 'larsbrinknielsen@gmail\.com'` returns nothing.
    - Single shipped reference file: `find plugins/lz-tdd/skills/lz-tpp/references -name '*.md'` lists only transformations.md.
    - Semantic cross-check: every list entry and key quote matches the Verified Content Anchors verbatim and cites the correct post; no D-02 scope-creep content (no TS examples, no Fibonacci/Word-Wrap walkthrough, no coach procedure, no TS ordering default).
  </acceptance_criteria>
  <verify>
    <automated>f=plugins/lz-tdd/skills/lz-tpp/references/transformations.md; rg -F '(statement -> tail-recursion)' "$f" >/dev/null && rg -F '(case)' "$f" >/dev/null && rg -F 'FibTPP.html' "$f" >/dev/null && rg -F 'TheTransformationPriorityPremise.html' "$f" >/dev/null && rg -F 'change the behavior of code' "$f" >/dev/null && rg -F 'informal at best' "$f" >/dev/null && rg -F 'language specific' "$f" >/dev/null && rg -F 'When passing a test' "$f" >/dev/null && ! rg -n '[^\x00-\x7F]' "$f" && [ "$(find plugins/lz-tdd/skills/lz-tpp/references -name '*.md' | wc -l)" -eq 1 ] && echo VALIDATION_PASS</automated>
  </verify>
  <done>The full mechanical suite is green, the per-entry/per-quote semantic cross-check against the Verified Content Anchors passes with correct per-post attribution, transformations.md is the only reference file, ASCII-only, and the work-email allowlist check is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Secondary sources / memory -> canonical shipped reference | Drifted third-party renderings could contaminate the "source of truth" list |
| Primary posts -> quoted content | Verbatim quotes must be attributed to the correct post to preserve provenance |
| Authored Markdown -> Windows cp1252 public repo | Non-ASCII glyphs from copy-paste can corrupt the committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering (of source truth) | Canonical 14-item list | mitigate | Transcribe from Verified Content Anchors + per-entry semantic cross-check (Task 3); forbid Wikipedia/memory as source |
| T-02-02-02 | Repudiation (of provenance) | Quote/list citations | mitigate | Inline per-post citation of every list and quote; hedge attribution split correctly ("language specific" -> FibTPP); Sources section maps labels to URLs; `rg -F` checks both post URLs present |
| T-02-02-03 | Information Disclosure | Work-email in a public committed file | mitigate | Work-email allowlist scan over the shipped file + phase dir (Task 3); the check never spells the work-email literal |
| T-02-02-04 | Tampering (encoding) | Non-ASCII glyph -> mojibake | mitigate | Arrow/quote/dash normalization to ASCII + `rg -n '[^\x00-\x7F]'` zero-match gate on every touched task |
| T-02-02-05 | Information Disclosure (IP) | Over-quoting Clean Coder essays | mitigate | Quote only the list + key definitions/hedges/process; summarize narrative; cite URLs (PITFALLS Pitfall 6) |
| T-02-02-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH "Package Legitimacy Audit: Not applicable") |
</threat_model>

<verification>
- Mechanical suite (RESEARCH "Validation Architecture"): 14-item markers, both post
  citations, definition/hedges/amended-RGR tokens, notation note, ASCII gate zero,
  work-email allowlist clean, single reference file.
- Semantic: per-entry + per-quote cross-check against Verified Content Anchors with
  correct per-post attribution; explicit 12-vs-14 resolution; reconciliation with
  precedence statement; no D-02 scope creep.
</verification>

<success_criteria>
- TPP-01: canonical 14-item list present, verbatim-faithful, in order, cited to FibTPP.
- TPP-02: 12-vs-14 resolved explicitly (14 canonical; 12 + 3 revisions + secondary drift documented).
- TPP-03 (reconciliation half): NDC transcript reconciled against the blog list, discrepancies noted, precedence stated (transcription + retention delivered by 02-01).
- TPP-04: transformations-vs-refactorings distinction present (verbatim, cited) and TPP framed as a provisional heuristic via the author's hedges (correctly attributed).
</success_criteria>

<output>
Create `.planning/phases/02-tpp-source-distillation/02-02-SUMMARY.md` when done.
</output>
