---
phase: 08.2
slug: functional-catalog-inserted
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-07
---

# Phase 08.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node built-in checkers (`.mjs`) + `tsc --strict` extractor (pinned typescript) — non-shipped harness at `.claude/skills/lz-refactor-workspace/tools/` |
| **Config file** | `.claude/skills/lz-refactor-workspace/package.json` (npm scripts) + `tsconfig` used by the one-module-per-fence extractor |
| **Quick run command** | `npm run check --prefix .claude/skills/lz-refactor-workspace` (full deterministic battery) |
| **Full suite command** | `npm run check --prefix .claude/skills/lz-refactor-workspace && npm run typecheck --prefix .claude/skills/lz-refactor-workspace` |
| **Estimated runtime** | ~30-60 seconds (deterministic file/link checks + tsc --strict over extracted Example fences) |

---

## Sampling Rate

- **After every task commit:** Run the relevant single checker (e.g. `node tools/check-functional.mjs`) plus `check-crossrefs`.
- **After every plan wave:** Run the full `npm run check` battery.
- **Before `/gsd:verify-work`:** Full `npm run check` + `npm run typecheck` must be green.
- **Max feedback latency:** ~60 seconds.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.2-01-01 | 01 | 1 | FUN-04 | — | N/A (Markdown/harness authoring; no runtime attack surface) | harness | `node tools/check-functional.mjs` (RED baseline) | ❌ W0 | ⬜ pending |
| 08.2-02-01 | 02 | 2 | FUN-01, FUN-02, FUN-03 | — | N/A | content+harness | `node tools/check-functional.mjs && npm run typecheck` | ❌ W0 | ⬜ pending |
| 08.2-03-01 | 03 | 3 | FUN-04 | — | N/A | crossref | `node tools/check-crossrefs.mjs && node tools/check-functional.mjs` | ❌ W0 | ⬜ pending |

*Task IDs/plans/waves are indicative — the planner sets the authoritative breakdown. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tools/check-functional.mjs` — new gate, built RED-first (instrument-first / Nyquist scaffold, per Phases 7/8/8.1). RED baseline: 0 functional-catalog leaves, unresolved `Correspondence:` / `Functional alternative:` links, empty selector-mirror set.
- [ ] `package.json` — wire `check-functional` into the `check` chain + `typecheck`; extend `extract-samples` CATALOGS and `check-crossrefs` sources to include `functional-catalog/`.

*The correctness anchor for this phase is the committed research artifact + `check-functional` + `tsc --strict` — there is NO owned book oracle (D-06).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| DST-04 clean-room fidelity (original prose/code; no verbatim book expression) | FUN-01 | Semantic judgment — a checker cannot detect near-verbatim paraphrase drift | skill-reviewer PASS + human read of each idiom Intent for original wording (paraphrase trap from Phases 07/08.1) |
| Idiom `## Example` is behavior-preserving Before->After (same inputs -> same outputs) | FUN-01 | Semantic equivalence is not machine-checkable beyond `tsc --strict` compilation | Reviewer confirms the After idiom preserves the Before OO behavior for the documented inputs |

*`tsc --strict` proves the Examples compile; behavior-preservation and clean-room fidelity are the reviewer's judgment calls.*

---

## Validation Sign-Off

- [ ] All tasks have an automated verify (a `check-functional` / `check-crossrefs` / `tsc --strict` command) or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the `check-functional` gate + package.json wiring)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter (flipped post-execution by /gsd:validate-phase)

**Approval:** pending
