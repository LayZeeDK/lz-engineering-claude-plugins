---
phase: 16-source-distillation-core-red-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-red-workspace/extract-samples.mjs
  - .claude/skills/lz-red-workspace/tsconfig.json
  - .claude/skills/lz-red-workspace/package.json
  - .claude/skills/lz-red-workspace/tools/lib/scaffold-phrases.mjs
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs
autonomous: false
requirements: [SEL-01, SEL-02, STR-01, STR-02, NAME-01]

must_haves:
  truths:
    - The dev-only lz-red-workspace tsc gate exists and reports GREEN-on-empty (0 fences -- vacuous strict-clean baseline) before any content lands.
    - check-red-references.mjs is RED against the three current Phase-15 stubs (the instrument-first Nyquist baseline) and is wired to turn GREEN only when the SEL/STR/NAME slices are authored.
    - check-hygiene.mjs (repo-global) scans the lz-red reference tree on ALL THREE axes (ASCII, work-email allowlist, no-verbatim) and stays GREEN over the clean stubs.
    - claude plugin validate . exits 0 with the new dev-only workspace in place.
  artifacts:
    - .claude/skills/lz-red-workspace/extract-samples.mjs (copied + repointed at the flat lz-red references walk)
    - .claude/skills/lz-red-workspace/tsconfig.json (copied verbatim; strict/noEmit + bundler resolution)
    - .claude/skills/lz-red-workspace/package.json (pinned typescript@6.0.3 + vitest@4.1.10, private ESM)
    - .claude/skills/lz-red-workspace/tools/lib/scaffold-phrases.mjs (copied; SCAFFOLD_RES draft-marker set)
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs (new completeness checker; RED baseline)
    - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs (extended targets, additive)
  key_links:
    - check-red-references.mjs imports SCAFFOLD_RES from ./lib/scaffold-phrases.mjs; the draft-marker on each stub's Sources heading is what keeps the baseline RED until authoring clears it.
    - extract-samples.mjs walks plugins/lz-tdd/skills/lz-red/references recursively (flat refs + testing-stance subdir), one module per fence.
    - check-hygiene.mjs adds the lz-red tree to BOTH wideTargets (ASCII + email) AND verbatimTargets (no-verbatim); lz-red is clean-room own-words, so it takes the no-verbatim gate (unlike lz-tpp).
  prohibitions:
    - NO build dependency lands in the shipped plugins/lz-tdd tree (Out-of-Scope); all deps live only in the dev-only workspace.
    - NO maintainer work-email or its bare domain in any committed file or commit identity (allowlist-inversion; only larsbrinknielsen@gmail.com is permitted).
    - NO non-ASCII byte in any committed file (cp1252 mojibake risk).
    - NO weakening of the existing check-hygiene rules (APPROVED_EMAILS, EMAIL_RE, QUOTE_THRESHOLD unchanged); the edit only widens the file set.
---

<objective>
Stand up the instrument-first dev-only validation workspace for the lz-red RED references and
assert the Nyquist RED baseline before any oracle content is authored. This is the Wave-0
instrument plan: copy the proven lz-refactor-workspace recipe (extractor + tsconfig + package.json
+ scaffold-phrase lib), author a lightweight completeness checker that is RED against the current
Phase-15 stubs, extend the repo-global hygiene gate to cover the lz-red tree, and clear the
package-legitimacy checkpoint for the two dev dependencies.

Purpose: the tsc extractor is GREEN-on-empty (0 fences compile vacuously), so it cannot be the
content-completeness signal. check-red-references.mjs is the RED->GREEN instrument; it must exist
and be asserted RED first, so Wave-1 authoring has a machine-checkable target.

Output: a ready lz-red-workspace (four copied/created files + one extended checker), an asserted
RED baseline, and a cleared legitimacy checkpoint gating the Wave-1 npm install.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md
@.planning/phases/16-source-distillation-core-red-references/16-RESEARCH.md
@.planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md
@.planning/phases/16-source-distillation-core-red-references/16-VALIDATION.md
</context>

## Decisions implemented

- **D-04:** the completeness checker guards over-fill -- it asserts each co-edited stub's later-phase deferral marker (Phase 17 for structure, Phase 18 for three-laws) still remains, so Wave-1 fills only its slice.
- **D-10:** the workspace tsc --strict --noEmit gate (extract-samples.mjs) enforces that every TypeScript sample compiles clean; the gate is stood up here (GREEN-on-empty baseline).
- **D-11:** stand up a minimal dev-only lz-red-workspace (planner's call, per RESEARCH recommendation) -- copy the lz-refactor-workspace extractor recipe repointed at the lz-red refs, pinned typescript@6.0.3 + vitest@4.1.10; the shipped plugins/lz-tdd tree gets NO build deps.
- **D-12:** the deterministic no-verbatim scan is instrumented by extending check-hygiene.mjs verbatimTargets to include the lz-red tree (clean-room own-words, no cited-verbatim exemption).

<tasks>

<task type="auto">
  <name>Task 1: Copy the lz-red-workspace extractor recipe (extractor + tsconfig + package.json + scaffold-phrase lib)</name>
  <files>.claude/skills/lz-red-workspace/extract-samples.mjs, .claude/skills/lz-red-workspace/tsconfig.json, .claude/skills/lz-red-workspace/package.json, .claude/skills/lz-red-workspace/tools/lib/scaffold-phrases.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/extract-samples.mjs (the exact analog to copy; note lines 16-29 the REFERENCES const + CATALOGS walk, lines 138-142 module-scope forcing, lines 155-160 empty-catalog vacuous GREEN, lines 162-166 the tsc spawn)
    - .claude/skills/lz-refactor-workspace/tsconfig.json (copy verbatim)
    - .claude/skills/lz-refactor-workspace/package.json (copy + rename + add vitest dep)
    - .claude/skills/lz-refactor-workspace/tools/lib/scaffold-phrases.mjs (copy verbatim into the new workspace)
    - .planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md (the extract-samples / tsconfig / package.json copy-and-repoint sections with exact target shapes)
  </read_first>
  <action>
    Copy the four analog files into a new .claude/skills/lz-red-workspace/ directory, making exactly the changes below and no others.
    (1) extract-samples.mjs: copy verbatim, then swap the multi-catalog walk for a single FLAT walk. Repoint the REFERENCES constant to plugins/lz-tdd/skills/lz-red/references and DROP the CATALOGS array + its per-catalog prefix loop; iterate `for (const file of walkMd(REFERENCES))` directly, naming each sample `${leaf}-${n}.ts`. Keep walkMd, extractFences, the unterminated-fence fail-loud, the `ts ignore` skip, the module-scope forcing (append export {} only when the fence has no import/export -- so a Vitest-import fence stays a self-contained module), the fresh samples/ rebuild, the empty-set vacuous-GREEN early return, and the `spawnSync("tsc", ["--strict","--noEmit","-p","tsconfig.json"], { cwd: here, stdio: "inherit", shell: true })` spawn all UNCHANGED. Retoken the SUMMARY label from FWL-04 to a lz-red-specific id (use RED-SAMPLES). The repoRoot depth is identical (workspace -> skills -> .claude -> root), so keep the path.resolve unchanged.
    (2) tsconfig.json: copy byte-identical (strict, noEmit, target es2021, module esnext, moduleResolution bundler, lib es2021, skipLibCheck, forceConsistentCasingInFileNames, include samples). Do NOT add a types array -- bundler resolution + a local vitest install resolves the bare `import ... from 'vitest'` fences (RESEARCH Pitfall 4, Option A).
    (3) package.json: copy, set name to lz-red-workspace, set a description naming it the NON-shipped compile + completeness gate for the lz-red RED references, keep private:true + type:module, set scripts.typecheck to `node extract-samples.mjs` and scripts.check to `node tools/check-red-references.mjs`, and set devDependencies to EXACT pins typescript 6.0.3 and vitest 4.1.10 (no ^ or ~ -- the Package Legitimacy audit requires exact pins).
    (4) tools/lib/scaffold-phrases.mjs: copy the analog verbatim so the new workspace is self-contained (do not import across workspaces).
    Do NOT run npm install in this task (Task 3 gates it). Do NOT author check-red-references.mjs here (Task 2). ASCII-only in every file; no maintainer work-email token anywhere.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/extract-samples.mjs; node -e "const p=require('./.claude/skills/lz-red-workspace/package.json'); if(p.devDependencies.typescript!=='6.0.3'||p.devDependencies.vitest!=='4.1.10'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - node extract-samples.mjs exits 0 and prints the vacuous-GREEN SUMMARY (0 modules to compile) because the lz-red refs carry 0 ts fences today -- no npm install required for this baseline.
    - package.json parses and pins typescript exactly 6.0.3 and vitest exactly 4.1.10 (no range operators); name is lz-red-workspace; private:true and type:module are present.
    - tsconfig.json is byte-identical to the lz-refactor-workspace analog (no types array added).
    - tools/lib/scaffold-phrases.mjs exports SCAFFOLD_RES identical to the analog.
    - All four files are ASCII-only and contain no non-allowlisted email token.
  </acceptance_criteria>
  <done>lz-red-workspace scaffolded (extractor repointed, tsconfig, pinned package.json, scaffold-phrase lib); extract-samples reports vacuous GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Author check-red-references.mjs (RED baseline) and extend check-hygiene.mjs to the lz-red tree</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs, .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/tools/check-backing.mjs (the DIRECT template: FILES data model lines 34-71, failure-counter + report() lines 76-84, per-file scan loop lines 92-114, SCAFFOLD guard lines 24 + 112-113, SUMMARY + exit tail lines 118-124)
    - .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs (borrow only the `>= 1 ts fence` file-level regex idiom)
    - .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs (the checker to extend: LZ path consts lines 20-33, wideTargets push lines 94-112, verbatimTargets push lines 114-129, allowlist + thresholds lines 36-38, scan-floor lines 140-152)
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md, test-structure-and-assertions.md, naming.md (the three stubs the checker must be RED against; note each carries a draft-scaffold marker on its Sources heading and the two co-edited stubs carry a later-phase marker)
    - .planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md (the check-red-references.mjs section: topics table, deferral-guard nuance, and the check-hygiene extension snippets)
  </read_first>
  <action>
    Create .claude/skills/lz-red-workspace/tools/check-red-references.mjs by mirroring check-backing.mjs (node builtins only, ESM, accumulate-then-exit, run-from-anywhere). Import SCAFFOLD_RES from ./lib/scaffold-phrases.mjs. Resolve repoRoot the same way (tools -> workspace -> skills -> .claude -> root). Define a FILES data model with one entry per lz-red reference under plugins/lz-tdd/skills/lz-red/references, each entry a name + topics list of case-insensitive line patterns plus a `>= 1 ts fence` file-level assertion (regex on ```(ts|typescript)). Per RESEARCH/PATTERNS, the content-present topics per ref are: three-laws-and-test-selection.md -- test list; one (small/single) step; degenerate/starter/empty/zero/null; triangulat; and an lz-tpp reference (the SEL-02 GREEN-firewall sentence names lz-tpp). test-structure-and-assertions.md -- arrange-act-assert; given-when-then; assert-first; evident data; one concept. naming.md -- should; behavior; Osherove OR three-part; match (house/stance). For EVERY file assert no SCAFFOLD_RES draft-marker leaks (this is the RED->GREEN engine: each stub's draft-marked Sources heading trips /\bplaceholder\b/i today, and flips GREEN only when authoring rewrites it to a clean Sources heading). Add a must-REMAIN deferral guard ONLY for the two co-edited files: three-laws must retain a Phase 18 marker (LAW/SEAM), test-structure must retain a Phase 17 marker (ASRT); naming.md has no deferral guard (fully Phase 16). Retoken the SUMMARY to a lz-red id (use RED-REFS); exit 1 on any failure. Add a header comment stating the checker is RED against the current stubs BY DESIGN (instrument-first Wave-0 baseline). <!-- planner-discipline-allow: placeholder -->
    Then extend .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs additively: add LZ_RED_SKILL_DIR / LZ_RED_SKILL_MD / LZ_RED_REFERENCES consts next to the existing skill-dir consts; after the lz-tpp wideTargets push, push LZ_RED_SKILL_MD (if it exists) + walkMd(LZ_RED_REFERENCES) into wideTargets; after the lz-refactor verbatimTargets push, push LZ_RED_SKILL_MD (if it exists) + walkMd(LZ_RED_REFERENCES) into verbatimTargets (lz-red is clean-room own-words, so it DOES take the no-verbatim gate, unlike lz-tpp which stays excluded). Leave APPROVED_EMAILS, EMAIL_RE, QUOTE_THRESHOLD, and the scan-floor anchors UNCHANGED (the edit widens the file set, never weakens a rule). ASCII-only; no work-email token as a needle.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1 && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs exits 1 (RED) and its per-file rows show the content-present topics FAILing and the draft-marker present for all three stubs -- the asserted instrument-first baseline.
    - check-hygiene.mjs exits 0 (GREEN): the lz-red stubs are ASCII-clean, carry no non-allowlisted email, and contain no verbatim-looking quoted run; the scan-floor still passes.
    - The check-hygiene edit is additive only -- APPROVED_EMAILS, EMAIL_RE (single approved gmail), and QUOTE_THRESHOLD are unchanged.
    - claude plugin validate . exits 0 with the new workspace present.
  </acceptance_criteria>
  <done>check-red-references.mjs asserted RED against the three stubs; check-hygiene extended additively and GREEN; claude plugin validate . exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Package-legitimacy checkpoint for typescript@6.0.3 + vitest@4.1.10 before install</name>
  <action>Blocking-human legitimacy gate (never auto-approvable). Present the two SUS-dispositioned dev dependencies for human verification on npmjs.com BEFORE any npm install runs; the Wave-1 install (16-02) proceeds only after approval. Automate nothing further here -- this is a human gate.</action>
  <what-built>The dev-only lz-red-workspace pins two external dev dependencies -- typescript@6.0.3 and vitest@4.1.10 -- that Wave-1 will npm install to resolve the Vitest types for the example fences. The RESEARCH Package Legitimacy audit flagged both as SUS (too-new) and dispositioned both APPROVED: typescript is already the pinned + globally-installed 6.0.3, and vitest is the milestone-locked framework (73M weekly downloads, official vitest-dev repo, no postinstall). No package installs into the shipped plugins/lz-tdd tree.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/typescript and confirm it is the official Microsoft TypeScript package (repo github.com/microsoft/TypeScript) and that version 6.0.3 exists.
    2. Open https://www.npmjs.com/package/vitest and confirm it is the official vitest-dev package (repo github.com/vitest-dev/vitest) with no install-time postinstall script, and that version 4.1.10 exists.
    3. Confirm both are pinned to EXACT versions in .claude/skills/lz-red-workspace/package.json (no ^ or ~), and that neither appears in plugins/lz-tdd/.claude-plugin/plugin.json or any shipped file.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the Wave-1 npm install of these two exact-pinned dev dependencies, or describe concerns.</resume-signal>
  <done>Human has verified both packages on npmjs.com and approved the exact-pinned dev-only install; the Wave-1 npm install is authorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev workspace | External dev deps enter only the gitignored node_modules of lz-red-workspace; never the shipped plugins/ tree. Pinned exact + legitimacy-checked. |
| author -> committed files | Only ASCII, own-words, work-email-free content may cross into git; check-hygiene gates it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-SC | Tampering/Elevation | dev deps typescript@6.0.3 + vitest@4.1.10 | low | mitigate | Pin exact versions in the workspace package.json; blocking-human legitimacy checkpoint (Task 3) verifies both on npmjs.com before any install; both are official-repo, no-postinstall, ubiquitous (SUS=too-new false positive); dev-only, never in plugins/. |
| T-16-email | Information Disclosure | any committed workspace/checker file | high | mitigate | check-hygiene allowlist-inversion over the widened target set (only larsbrinknielsen@gmail.com permitted); never write the forbidden work-email/domain even as a needle. |
| T-16-ascii | Tampering (data corruption) | committed workspace/checker files | medium | mitigate | ASCII-only authoring; check-hygiene axis (a) fails on any byte > 0x7F, reported file@byteN. |
| T-16-scope | Scope integrity | the two co-edited stubs | low | mitigate | check-red-references deferral guard asserts the Phase 17 (ASRT) and Phase 18 (LAW/SEAM) markers must REMAIN, so Wave-1 cannot over-fill a later-phase slice. |
| T-16-SC-hygiene | Tampering | check-hygiene.mjs rule set | high | mitigate | The extension is additive only; APPROVED_EMAILS / EMAIL_RE / QUOTE_THRESHOLD / scan-floor anchors are unchanged, so widening the file set cannot weaken an existing control. |

Block-on-high posture (security_asvs_level 1, security_block_on high): a non-allowlisted email token or a weakened hygiene rule is a HARD failure and blocks the plan.
</threat_model>

<verification>
- node .claude/skills/lz-red-workspace/extract-samples.mjs -> exit 0, vacuous GREEN (0 modules).
- node .claude/skills/lz-red-workspace/tools/check-red-references.mjs -> exit 1, RED baseline over the three stubs (instrument-first, expected).
- node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs -> exit 0, GREEN over the widened target set.
- claude plugin validate . -> exit 0.
- Legitimacy checkpoint cleared (human-approved) before any npm install.
</verification>

<success_criteria>
The lz-red-workspace exists with a repointed tsc extractor (GREEN-on-empty), a completeness checker asserted RED against the current stubs, and a hygiene gate widened to the lz-red tree on all three axes (still GREEN). The two dev dependencies are exact-pinned and legitimacy-approved. No dependency touches the shipped plugin. This is the machine-checkable Nyquist RED baseline Wave-1 authoring will turn GREEN.
</success_criteria>

## Artifacts this plan produces

- `.claude/skills/lz-red-workspace/extract-samples.mjs` -- one-module-per-fence tsc --strict extractor, repointed at the flat lz-red references walk.
- `.claude/skills/lz-red-workspace/tsconfig.json` -- strict/noEmit compile config (copied verbatim).
- `.claude/skills/lz-red-workspace/package.json` -- pinned typescript@6.0.3 + vitest@4.1.10 (dev-only, private, ESM).
- `.claude/skills/lz-red-workspace/tools/lib/scaffold-phrases.mjs` -- the SCAFFOLD_RES draft-marker set (copied).
- `.claude/skills/lz-red-workspace/tools/check-red-references.mjs` -- new completeness checker (RED baseline; the RED->GREEN content signal).
- `.claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` -- extended to scan the lz-red tree on ASCII + email + no-verbatim.

No new symbols land in the shipped `plugins/` tree in this plan.

<output>
Create `.planning/phases/16-source-distillation-core-red-references/16-01-SUMMARY.md` when done.
</output>
