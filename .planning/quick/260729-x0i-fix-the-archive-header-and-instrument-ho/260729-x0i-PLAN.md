---
phase: quick-260729-x0i
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [OD-X0I-1, D-02, D-04, D-10, D-12]
files_modified:
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - .planning/research/test-double-taxonomy.md
  - .planning/REQUIREMENTS.md
  - .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md
  - .planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-RED-EVIDENCE.md
  - .planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-PLAN.md
  - .planning/STATE.md

must_haves:
  truths:
    - "OD-X0I-1: the ragged-table gate walks ONLY the shipped tree. With the archived planning record absent from disk the battery is GREEN at 123 checks -- measured by moving it aside and running, not asserted."
    - "OD-X0I-1 non-vacuity: with a ragged row planted in a SHIPPED markdown file the battery FAILS at exit 1 with exactly one FAIL, naming that file, its table start line and the ragged row's line."
    - "OD-X0I-1 no fail-open: the archive left the gate's SCOPE. No existsSync skip, no try/catch-and-continue over a missing scope member, and no tolerated-absence branch was added anywhere. Proven TWICE OVER: the file-wide sentinel counts are unchanged (existsSync 9, `try {` 5, `report(` 16) AND the gate's own 33-line code slice is region-scoped-clean (existsSync zero, `try {` exactly 1, `continue;` exactly 1, `report(` exactly 1), so a tolerance that MOVED into this gate cannot hide behind a stable file-wide total."
    - "The emitted-check count is unchanged at 123 and EXPECTED_CHECKS was NOT edited -- the gate is one report() call over a loop, so a loop member cannot move the count."
    - "The gate's anti-vacuity leg still holds without the archive: the shipped tree carries 28 tables across 9 files (measured), so tablesSeen stays nonzero."
    - "D-04: the archive header's dependency claim is TRUE as written -- nothing outside the planning tree reads that path, and the checker's reference to it is at measured count 0."
    - "D-02 / D-10: the header records that the ONE plugin-wide shared reference shape it names is currently BLOCKED by guard N3, and that a relocation needs an explicit N3 carve-out. Measured, not inferred: a probe file at the canonical plugin-wide path FAILS N3 by name."
    - "D-12: the header no longer claims the shared-reference mechanism is undesigned. It cites the verified research record and the reference already in production use, and both cited artifacts exist on disk."
    - "The taxonomy BODY is byte-identical: the 540-line slice from its H1 to EOF hashes the same before and after, and the leading blockquote gains no pipe row."
    - "Three by-design gaps are recorded in the project's existing deferred-work register, each naming what closing it would cost. No new section was invented and ROADMAP.md is byte-unchanged."
    - "OD-X0I-1: the stale instruction at `260729-lc9-CONTEXT.md` line 294 is ANNOTATED in place as SUPERSEDED -- naming the id, the task and the reason -- while its original text survives verbatim exactly once, so the record that explains why the defect existed is preserved rather than rewritten."
    - "Final gate at the commit boundary: battery exit 0 at 123 checks / 0 FAILs, row-guards selftest exit 0 at 49 assertions, provenance selftest 3/3, extract-samples 8 modules clean, check-hygiene exit 0, claude plugin validate . exit 0, and BOTH shipped-skill scope invariants at 0 lines."
  artifacts:
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
    - .planning/research/test-double-taxonomy.md
    - .planning/REQUIREMENTS.md
    - .planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-RED-EVIDENCE.md
  key_links:
    - "The ragged gate is ONE report() call over a loop, so removing a loop member cannot change the emitted count. EXPECTED_CHECKS stays 123 and the roster gate is the proof -- if the count moves, a DIFFERENT guard broke; diagnose, never adjust the literal."
    - "tablesSeen is the gate's anti-vacuity leg. It is the only thing standing between a narrowed scope and a permanent vacuous PASS, because scanTables cannot report an offender on empty text."
    - "The replacement SCOPE comment must CITE the three reasons already recorded at the retirement note above TWO_IG_GUARDS (mirrored in lib/row-guards.mjs), not restate them. Two copies of a rationale drift; the current defect is exactly that the code at the constant contradicted the prose at the note."
    - "Cite the retirement note by STABLE NAME, never by line number: removing the constant shifts every line below it, so a line citation is stale the moment it is written."
    - "Task 1 MUST land before Task 2. D1's correct wording depends on the checker no longer reading the path; writing it against the pre-fix reality reproduces the self-falsification class this task exists to close."
    - "The archive header is enforced by NO guard. Every claim it gains must name a file, a line or a command a reader can run, because nothing else will catch it going stale."
    - "Naming OD-X0I-1 in THIS plan's objective protects only THIS executor. The next reader opens `260729-lc9-CONTEXT.md` and sees a live locked decision, so the supersession has to live in that artifact -- an instruction outliving the reasoning that justified it is the needle-outlives-its-subject class pointed the other way."
    - "Annotate, never rewrite. The original instruction is the trail that explains why the defect existed; deleting it would erase the diagnosis along with the bug, which is why one leg asserts the annotation and a second asserts the preserved original."
    - "check-hygiene does not scan the planning tree. ASCII discipline and email allowlist-inversion on both edited planning docs are entirely on the author and must be explicit legs."
---

<objective>
Close two measured self-falsification defects and roadmap two by-design gaps.

The instrument asserts a policy in prose and violates it in its own code: the ragged-table gate
hard-depends on an archived planning path, while the same file's retirement note records three
reasons not to point shipped-skill guards at that archive. One of those three reasons is LIVE --
`.planning/milestones/lz-tdd@0.0.1-research/` exists on disk, proving a milestone close relocates
`.planning/research/`, and `/gsd-complete-milestone lz-tdd@0.0.3` is the next remaining task in this
milestone. The dependency is fail-CLOSED, so the close would turn the battery RED on UNREADABLE.

Purpose: remove the contradiction at its source rather than at its symptom, then correct the archive
header's three measured prose defects against the post-fix reality, then record the two gaps that
stay open BY DESIGN so they are tracked rather than rediscovered.

Output: 3 atomic commits, each with an asserted verify block. Task 1 carries a recorded RED-then-GREEN
pair per the prove-a-guard-can-fail rule. Zero spend -- no metered `claude -p`, no evals.

OD-X0I-1 is the owner decision authorizing Task 1: fix the gate scope now, do not roadmap it. It
SUPERSEDES `260729-lc9-CONTEXT.md` `<specifics>` line 294 ("Scope the guard to the shipped tree AND
the `.planning/` copy"), which is the instruction that produced the defect. Task 3 step 4 records that
supersession IN THAT ARTIFACT by annotation, because saying it here protects only this executor -- the
next reader opens the CONTEXT.md and sees a live locked decision.

Locked decisions this plan serves: D-02 (one plugin-wide shared reference, never per-skill copies),
D-04 (the archive is an inert record that states what is still open in it), D-12 (lz-red ships NO
test-double reference on purpose; its absence is the baseline arm of a future A/B, never a regression
to patch). D-10 (do not investigate the mechanism) was discharged by the owner-authorized lc9
research and is recorded here as satisfied, not re-opened.
</objective>

<!-- planner-discipline-allow: ARCHIVED_RECORD -->
<!-- planner-discipline-allow: .planning/research -->
<!-- planner-discipline-allow: no mechanism for a shared reference has been designed -->

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/.continue-here.md
@.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md
@.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/PLUGIN-WIDE-REFERENCE-RESEARCH.md
@.claude/skills/lz-red-workspace/tools/check-red-references.mjs
@.claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
</context>

<pre_measurements>
Every number below was measured by the planner at HEAD `1d67430` on a clean tree, by RUNNING the
command, not by reading structure. They are here so the executor can tell a real regression from an
expected value. **RE-MEASURE each one; never copy an expected string out of this plan into an
evidence record.** If any baseline disagrees with what you measure, STOP and report -- a disagreeing
baseline means the tree moved under the plan.

**Battery baseline**
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` -> exit 0.
- Emitted result lines: `124` matching `^  \[PASS\]`. The roster gate is the 124th; it snapshots the
  count BEFORE its own emission, which is why the roster line reads `123 checks`.
- `[FAIL]` lines: 0.
- Roster line, verbatim: `123 checks, equal to the PREDICTED literal; all 13 new labels present;
  none of the 64 retired labels survive`.

**The defect (RED control, MEASURED by moving the archive aside and running the PRE-FIX checker)**
- exit `1`, exactly one FAIL:
  `[FAIL] [lc9] no ragged pipe table -- 1 problem(s): ...test-double-taxonomy.md: UNREADABLE (ENOENT)`
- The archive was restored byte-identically afterwards (`git status --porcelain` on that path: 0 lines).
- Windows prints these paths with BACKSLASH separators. A verify needle containing a forward slash
  will not match the checker's output. Match on a separator-free fragment.

**The defect's site, and why the count cannot move**
- `check-red-references.mjs:698` declares the archived-record path constant.
- `:702` iterates `[...pluginsMarkdown, ARCHIVED_RECORD]`.
- `:707-:709` catch a read error and push `UNREADABLE`, so the gate FAILS CLOSED on that path.
- `:723` is the gate's SINGLE `report()` call, OUTSIDE the loop. Removing a loop member therefore
  cannot change the emitted count. 123 stays 123 BY CONSTRUCTION.
- `:692-:696` is the SCOPE comment that claims the archive is in scope. It is the prose to replace.
- The retirement note above `TWO_IG_GUARDS` records the three reasons NOT to point guards at the
  archive; `lib/row-guards.mjs` mirrors them. That note is what `:698` contradicts.

**Anti-vacuity survives the narrowing (measured through the real `scanTables`)**
- Shipped tree: `28` tables across `9` markdown files, `0` ragged rows.
- The archive contributes `1` table. So `tablesSeen` goes 29 -> 28: still nonzero, leg intact.

**Sentinel counts in `check-red-references.mjs` (pin these; a rise means a fail-open crept in)**
- `existsSync` 9, `try {` 5, `report(` 16, `RAGGED_TABLE_LABEL` 3, `tablesSeen` 4.
- `for (const file of pluginsMarkdown) {` 1 -> must become 2 after the fix.
- `for (const file of [...pluginsMarkdown, ARCHIVED_RECORD]) {` 1 -> must become 0.
- `ARCHIVED_RECORD` 2 -> must become 0. The archived path literal 1 -> must become 0.

**RED leg A target, MEASURED (plant then revert)**
- File: `plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md`. Header at line 17 is 2
  cells wide; data rows are lines 19-21.
- Mutation: on line 19 delete the FIRST data cell TOGETHER WITH the pipe that follows it, leaving
  `| [functional-core.md](functional-core.md) |`. Deleting the SECOND cell instead would also break
  the `links functional-core.md` topic and produce two FAILs, muddying the leg.
- Observed: exit `1`, exactly ONE FAIL --
  `[FAIL] [lc9] no ragged pipe table -- 1 problem(s): ...README.md: table at line 17: header is 2
  wide, but 1 row(s) differ -- line 19 is 1`
- `git checkout --` restored it clean.

**The scope-invariant fail-open, MEASURED both directions (plan-checker iteration 1 BLOCKER)**
- `git diff nonexistent-tag-xyz -- plugins/lz-tdd/skills/lz-tpp/` exits `128` with
  `fatal: bad revision` on stderr; `| wc -l` prints `0`; `[ "0" -eq 0 ]` **PASSES**. The unanchored
  leg certifies the frozen surfaces byte-identical while comparing against nothing.
- With `git rev-parse <tag> >/dev/null 2>&1 &&` in front: the bogus tag **FAILS**, the real
  `lz-tdd@0.0.2` **PASSES**. The anchor is present at all three sites (Task 1 L9, Task 2, final gate)
  and Task 1 step 8 records the three observations.

**The stale SCOPE claim, needles for its REMOVAL (Task 1 L2b and L11's third clause)**
- `archived planning copy` -- exactly `1` occurrence, on line `692`, the SCOPE paragraph. File-wide
  absence is therefore safe: there is nowhere else for it to legitimately live.
- `PLUS the ONE` -- exactly `1` occurrence, same line `692`. Two needles for one line, deliberately, so
  a partial rewrite that keeps half the sentence still fails.
- `FAILS CLOSED if it is unreadable` -- exactly `1` occurrence, line `695`, inside the same paragraph.
  Its needle is REGION-scoped (L11's slice), NOT file-wide, because `fails closed` occurs twice more for
  unrelated guards -- measured at lines `563` (G17) and `862` (the row-guard note). A file-wide needle
  would demand deleting prose belonging to guards this task must not touch.
- All three are PRESENT at HEAD, so these legs read RED right now. That RED is the proof and it is free:
  they can only go GREEN once step 4 actually replaces the paragraph.

**SCOPE-comment region slice (Task 1 L11, the cite-do-not-restate leg)**
- Bracket keys, each occurring exactly `1` time: `[lc9] N1 TABLE SHAPE GATE` (open) and
  `const RAGGED_TABLE_LABEL` (close). Slice today: `21` lines.
- `TWO_IG_GUARDS` inside that slice: `0` -- the RED evidence for the leg.
- `TWO_IG_GUARDS` FILE-WIDE: `4`. That is why the leg must be region-scoped; a file-wide needle would
  pass unconditionally and read as coverage.
- Do NOT negative-grep the word `archived` in that region: the replacement comment legitimately uses
  it to say the archive is OUT of scope. The word is not the defect; the claim was.

**Archive header slice (Task 2 subject)**
- File total 570 lines. The leading blockquote is lines 1-29; line 31 is the H1
  `# Test-double and stand-in taxonomy across authors`.
- Slice from the H1 to EOF: `540` lines. That slice is the BODY and must hash identically after.
- Header slice (line 1 through the H1): `31` lines, `0` pipe-leading lines.
- Whole file: `0` non-ASCII bytes, `0` email-shaped tokens.
- Needles measured ABSENT from the header slice, count 0 each -- this is the RED evidence for the
  four presence-required claims, per the two-proof-kinds rule (a `file:line` is unsatisfiable for an
  absent phrase): `N3`, `carve-out`, `check-red-references`, `PLUGIN-WIDE-REFERENCE-RESEARCH`,
  `beck-tdd-by-example`.
- The D3 false claim occurs exactly `1` time in the whole file. That count 1 is the positive control
  for its removal leg: 1 -> 0 is direction-anchored.

**D2's claim is measured, not inferred (probe then delete)**
- A probe file planted at `plugins/lz-tdd/references/test-double-taxonomy.md` -- the exact canonical
  plugin-wide path the header's forward constraint names -- makes the battery exit `1` with exactly
  one FAIL: `[FAIL] [lc9] no test-double taxonomy copy in the shipped tree -- 1 copy/copies: ...`
- So the forward shape the header states is BLOCKED today. N3 matches the basename STEM over EVERY
  file under `plugins/`, with no carve-out for a canonical location.
- NOTE: `PLUGIN-WIDE-REFERENCE-RESEARCH.md` B2 quotes an OLDER N3 (filename match over the markdown
  list). The live gate is STRICTER (stem match over every file). State the constraint from the LIVE
  code; cite the research only for the ownership reasoning.

**Cited artifacts exist (so the header's citations are true, not just present)**
- `plugins/lz-tdd/references/beck-tdd-by-example.md` exists (the R100 relocation, in production use).
- `.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/PLUGIN-WIDE-REFERENCE-RESEARCH.md`
  exists and carries the VERIFIED V1-V7 table read out of the shipped `claude.exe` 2.1.220 bundle
  (`getPromptForCommand`).

**Deferred-work register (Task 3 routing, MEASURED)**
- `.planning/ROADMAP.md` has NO deferred / backlog / future section. Its only shapes are milestone
  blocks, phase detail, and a per-phase `Post-execution:` note.
- `.planning/REQUIREMENTS.md` `## Future Requirements` (line 81) is the project's deferred-work
  register. Its lead reads "Deferred to a later lz-tdd release. Tracked, **not in this roadmap**."
  `### Later milestones` (line 90) carries `**FUT-*:**` bullets and runs to `## Out of Scope` (line 106).
- `FUT-04` there is labelled "carried from 0.0.2", which is the proof this register survives a
  milestone close by convention -- the property Task 3's entries need.
- Baseline: 166 lines, 4 `^## ` headings, 14 `^### ` headings, 0 non-ASCII, 0 email-shaped tokens.
- The three proposed ids collide with nothing: `FUT-ROSTER-TYPO`, `FUT-ROSTER-LITERALS`,
  `FUT-TAXONOMY-SHARED` all measure count 0 today.
- ROUTING RULED AND ACCEPTED by the coordinator: the brief names ROADMAP.md while also forbidding an
  invented section, and those two are incompatible -- ROADMAP.md has no such section and
  REQUIREMENTS.md explicitly says deferred items are "not in this roadmap". All three entries go to
  `REQUIREMENTS.md` `### Later milestones`, and ROADMAP.md byte-unchanged stays an asserted leg. The
  decisive evidence is `FUT-04`'s "carried from 0.0.2" label: it proves that register SURVIVES a
  milestone close, which is exactly the property `.planning/research/` lacks and is the whole subject
  of Task 1.

**The stale instruction (Task 3 step 4 subject, MEASURED)**
- `260729-lc9-CONTEXT.md:294`, inside `<specifics>`, reads exactly:
  `Scope the guard to the shipped tree AND the ` + backtick + `.planning/` + backtick + ` copy.`
  It occurs exactly `1` time in that file. Line 293 is blank, 295 is blank, 296 closes `<specifics>`.
- That line is the instruction that PRODUCED the defect Task 1 removes. It is still phrased as a live
  requirement, so the next reader can restore the bug in good faith.
- Baseline: 340 lines, 6 `^## ` headings, 17 `^### ` headings, 5 pipe-leading lines, 0 non-ASCII,
  0 email-shaped tokens. `## Implementation Decisions` occurs once and is the anchor.
- Needles measured ABSENT, count 0 each -- the RED evidence for the annotation's presence legs:
  `SUPERSEDED`, `OD-X0I-1`, `260729-x0i`.
- The needle for the preserved original carries BACKTICKS. Single-quoting it inside `$( ... )` is safe
  and was smoke-tested; do NOT switch it to double quotes, where bash would attempt command
  substitution before `rg` ever sees it.
- The annotation's stated reason is independently TRUE on disk:
  `.planning/milestones/lz-tdd@0.0.1-research/` exists, which is the relocation precedent, and the
  pre-fix `UNREADABLE (ENOENT)` control above is the fail-closed measurement.
</pre_measurements>

<pipe_exit_sweep>
Complete enumeration of every `$( ... )` in this plan whose pipeline ends in a counter, or whose
comparison could otherwise be satisfied by an upstream failure. Requested because the set had
previously been cleared BY ARGUMENT ("they are all behind anchors"), which is an inference about
structure standing in for a count -- and the dismissing sentence named the unsafe comparator inside
the set it dismissed. Every row below was measured, not reasoned.

**Form behaviour, measured FIRST (the three forms do NOT behave alike)**

| Form | Failure input | Exit | Capture | `[ "$cap" -eq 0 ]` |
|---|---|---|---|---|
| `$(cmd \| wc -l)` | upstream cmd fails | wc's 0 | `0` | **PASSES -- fail-open** |
| `$(rg -c P file)` | file missing | 2 | empty | bash error -- fail-CLOSED |
| `$(rg -c P file)` | no match | 1 | empty | bash error -- fail-CLOSED |
| `$(wc -l < file)` | file missing | redirect fails | empty | bash error -- fail-CLOSED |

Only the PIPE-into-`wc` form launders. `rg -c` and the `wc -l < file` redirect form both yield an EMPTY
capture, and an empty capture makes any `[ "" -op N ]` a bash error, which fails the chain. That is why
this sweep enumerates by FORM and not by comparator alone.

**Three fail-opens FOUND, all in git pathspec legs, all measured**

| # | Measured | Verdict |
|---|---|---|
| E | `git status --porcelain -- <mistyped-path> \| wc -l` prints `0`, exit 0 | fail-open: a mistyped restore path certifies "restored clean" |
| F | `git diff <REAL tag> -- <mistyped-path> \| wc -l` prints `0`, exit 0 | fail-open: the `rev-parse` fix closed the TAG arm and left the PATH arm open |
| G | `git diff --quiet -- <mistyped-path>` exits `0` | fail-open: a mistyped path certifies "byte-unchanged" |

All three are closed by a `[ -f ]` / `[ -d ]` existence anchor on the same subject, measured both
directions: the anchors FAIL on the typo'd paths and PASS on the real ones.

**FINDING F IS THE LOAD-BEARING RESULT OF THIS SWEEP, and it is the reason the two-arm rule exists.**
The `rev-parse` anchor was ORDERED by the coordinator, IMPLEMENTED here, and MEASURED in both
directions -- bogus tag FAILS, real tag PASSES -- and it was still only half a fix. It closed the TAG
arm of a two-arm fail-open and left the PATH arm wide open, on the leg that is the sole mitigation for
the one threat this plan rates `critical`. Nobody caught it: not the coordinator who ordered it, not
this planner who measured it, not the plan-checker that had already re-measured every numeric claim in
this document. The ENUMERATION caught it, and only because it was forced to enumerate rather than
argue.

So the "a two-arm fail-open needs both arms closed" convention is NOT derived from principle. It was
extracted from a live miss inside this task's own repair, one iteration after that repair passed
review as complete. That provenance is the strongest thing that can be said for it, and it is recorded
here rather than presented as a general maxim, because a rule with a measured origin survives review
differently from one that merely sounds correct.

**THE PARTIAL-REPAIR SHAPE RECURRED A THIRD TIME IN THIS TASK, in a different repair.** L11 was added
to enforce the requirement that step 4 REPLACE the SCOPE comment, and it enforced only the ADDITION of
the citation: a minimally-compliant executor could delete the constant and the loop member (passing
L2), leave the stale paragraph fully intact, append a `TWO_IG_GUARDS` clause below it (passing L11), and
ship source whose own comment says the archive is in scope and fails closed on it, three lines above
code that does neither. That is the "guard deleted, its claim left behind" anti-pattern row -- the
SEAM-02 repeat this whole task exists to close -- reaching success criterion 4, which until now had NO
automated enforcement at all. L2b and L11's third clause are the missing direction.

No ordinal is asserted for these three, for the same reason none is asserted under `<verification>`.
What matters is that the shape is now stable enough to be a CHECK rather than an observation, and it is
promoted into `<conventions>` as one: a "replace X with Y" requirement needs a leg per direction,
because an addition can always be satisfied by appending.

**Full enumeration, after the anchors were added**

| Leg | Expression form | Comparator | Direction | Anchor earlier in the SAME chain | Verdict |
|---|---|---|---|---|---|
| T1 L1-L4 | `rg -c -F ... "$C"` | `-eq 2 / 3 / 4 / 9 / 5 / 16 / 1` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T1 L5 | `rg -c '^  \[PASS\]'` | `-eq 124` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T1 L6 | `rg -c '^  \[FAIL\]'` | `-eq 1` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T1 L6 | `git status --porcelain -- "$R" \| wc -l` | `-eq 0` | UNSAFE | `[ -f "$R" ]` ADDED | ANCHORED (was E) |
| T1 L7 | `git status --porcelain -- "$A" \| wc -l` | `-eq 0` | UNSAFE | `[ -f "$A" ]` ADDED | ANCHORED (was E) |
| T1 L8 | `wc -l < "$E"` | `-ge 20` | safe | redirect form errors on missing | SAFE |
| T1 L9 | `git diff <tag> -- <path> \| wc -l` x2 | `-eq 0` | UNSAFE | `rev-parse` + `[ -d ]` + `[ -f ]` ADDED | ANCHORED (was F) |
| T1 L10 | `rg -c '\[PASS\]'` | `-eq 49` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T1 L2b | `! rg -q -F` x2 (stale-claim removal) | exit-based | zero-assertion | L1's `rg -c` counts on `$C` | SAFE |
| T1 L11 | `wc -l < "$SCOPE_SLICE"` / `rg -c` | `-ge 10` / `-ge 1` | safe | bracket keys `-eq 1` precede | SAFE |
| T1 L11 | `! rg -q -F 'FAILS CLOSED if it is unreadable'` | exit-based | zero-assertion | slice count `-ge 10` precedes | SAFE |
| T1 L12 | `wc -l < "$GATE_SLICE"` | `-ge 20` | safe | ANTI-VACUITY for the four legs behind it | SAFE |
| T1 L12 | `! rg -q -F existsSync` | exit-based | zero-assertion | slice count `-ge 20` precedes | SAFE |
| T1 L12 | `rg -c -F 'try {' / 'continue;' / 'report('` | `-eq 1` x3 | exact NONZERO | slice count precedes | SAFE |
| T2 L1 | `wc -l` / `rg \| wc -l` slices | `-ge 560` / `-eq 540` x2 / `-ge 25` | exact NONZERO or `-ge` | self-anchoring | SAFE |
| T2 L2 | `... \| sha256sum \| cut` | string `=` | UNSAFE alone -- empty input hashes to a FIXED value, so two empty slices compare EQUAL | L1's `-eq 540` on BOTH slices; the after-side count was MOVED INTO L1 so the anchor precedes the comparison | ANCHORED |
| T2 L5 | `rg -o ... \| sort -u \| rg -v ... \| wc -l` | `-eq 0` | UNSAFE | L1 `wc -l < "$F"` `-ge 560` (fail-closed form) | ANCHORED |
| T2 L6/L7/L9 | `rg -c -F ... <slice>` | `-ge 1` | safe | L1 slice counts precede | SAFE |
| T2 L10b | `wc -l < "$E"` / `rg -c -F` | `-ge 20` / `-ge 1` | safe | redirect form errors on missing | SAFE |
| T2 L8 | `rg -c '^  \[FAIL\]'` | `-eq 1` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T2 L8 | `git status --porcelain -- plugins/ \| wc -l` | `-eq 0` | UNSAFE | `[ -d plugins ]` ADDED | ANCHORED (was E) |
| T2 L11 | `rg -c -F ...` | `-eq 1` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T2 L12 | tag diffs | `-eq 0` | UNSAFE | `rev-parse` + `[ -d ]` + `[ -f ]` ADDED | ANCHORED (was F) |
| T3 L1 | `wc -l < "$R"` / `rg -c -F` | `-ge 166` / `-eq 1` / `-ge 1` | safe / exact NONZERO | redirect form errors on missing | SAFE |
| T3 L2/L3/L5 | `rg -c` | `-eq 4 / 14 / 1` / `-ge 1` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T3 L4 | `wc -l < "$SLICE"` / `rg -c` | `-ge 15` / `-eq 1` | safe / exact NONZERO | slice count precedes | SAFE |
| T3 L6 | `rg -c -F ... "$C"` | `-eq 1` | exact NONZERO | n/a -- empty capture errors | SAFE |
| T3 L7 | `git diff --quiet -- ROADMAP.md` | exit status | UNSAFE | `[ -f .planning/ROADMAP.md ]` ADDED | ANCHORED (was G) |
| T3 L8/L13 | email inversion `... \| wc -l` | `-eq 0` | UNSAFE | L1 / L9 `wc -l < file` counts | ANCHORED |
| T3 L9-L13 | `wc -l < "$K"` / `rg -c` | `-ge 340` / exact NONZERO | safe | redirect form errors on missing | SAFE |
| T3 L14 | tag diffs | `-eq 0` | UNSAFE | `rev-parse` + `[ -d ]` + `[ -f ]` ADDED | ANCHORED |
| final | battery / selftest `rg -c` | exact NONZERO | safe | n/a -- empty capture errors | SAFE |
| final | tag diffs | `-eq 0` | UNSAFE | `rev-parse` + `[ -d ]` + `[ -f ]` ADDED | ANCHORED |
| final | `git status --porcelain \| wc -l` | `-eq 0` | UNSAFE | no pathspec to mistype; the preceding `rev-parse` and tag diffs prove a valid repo | ANCHORED |

NOT an all-clear: three fail-opens were found and closed. Task 1 step 8a proves the tag-leg anchors with
a bogus-input RED covering BOTH arms -- a bogus tag AND a bogus path.

**The `.mjs` instrument is NOT swept for this class, and the reason is categorical rather than
structural:** exit-code laundering is a property of shell pipelines, and JS has no equivalent
construct, so the class cannot exist there. That is a different kind of claim from the one rejected
earlier in this task -- that one inferred safety from structure ("they are all behind anchors") while
naming the unsafe comparator inside the set it dismissed.

**But the JS-side ANALOGUE does exist -- an `existsSync` skip or a tolerating `catch`/`continue` -- and
L3 pins those FILE-WIDE, which is the weak form.** This plan proved why in its own L11: `TWO_IG_GUARDS`
measures 0 in the target slice and 4 file-wide, so a file-wide needle there would have been the very
class it closed. The identical objection applies to L3: `existsSync` holding at 9 file-wide does NOT
prove none of those nine MOVED INTO the ragged gate as a tolerance for the now-absent archive -- the
one mutation that satisfies L3, satisfies every count leg, and is still exactly the fail-open Task 1 is
forbidden from introducing. **L12 is the region-scoped leg that closes it.**

**L12's shape, and a MEASURED correction to its specification.** The brief asked for zero `existsSync`,
zero `try` and zero `continue` in the region. Measured, the region contains exactly ONE `try {` and
exactly ONE `continue;` -- and they are the per-file read guard that FAILS CLOSED and which this task
explicitly requires to stay unchanged. A literal zero-assertion would forbid the guard that must remain
and would read RED at HEAD for the wrong reason. So L12 pins the SHAPE instead: `existsSync` at zero
(the real zero-assertion), `try {` at exactly 1, `continue;` at exactly 1, `report(` at exactly 1. That
is strictly stronger than banning a construct, because an added tolerance branch moves one of the
counts whichever form it takes.

The zero-assertion is written `! rg -q -F existsSync` and NOT `[ "$(rg -c ...)" -eq 0 ]`, because
`rg -c` with no match exits 1 and prints nothing, so the `-eq 0` form is a bash error and a FALSE RED
(measured in the form table above). The zero direction needs the exit-based form; the counting form is
for nonzero pins only.

The brackets are the `RAGGED_TABLE_LABEL` declaration and the `N2 RELATIVE-LINK RESOLUTION GATE`
marker, each occurring exactly once (measured), giving a 33-line slice. This slice ABUTS L11's without
overlapping: L11 covers the SCOPE comment above the declaration, L12 covers the gate's code below it,
so between them the whole gate is covered and neither leg is doing the other's job.

MEASURED both directions against a MUTANT COPY of the checker carrying a planted `existsSync` skip:
the leg FAILS on the mutant (fires on two axes at once -- `existsSync` 0 -> 1 and `continue;` 1 -> 2)
and PASSES on the real file. The real instrument was never touched.
</pipe_exit_sweep>

<conventions>
Binding on every task. Each came from an actual failure in this work stream.

- **`&&` between every verify leg, never `;`.** The ONE permitted `;` is an exit-code capture
  (`node ... > "$OUT" 2>&1; EX=$?`) immediately followed by an `&&`-chained assertion on `$EX`. That
  is required for the RED legs, where a nonzero exit is the expected result.
- **Anchor every absence or ceiling assertion with a counted positive control on the SAME subject
  EARLIER in the SAME chain.** `rg` and `git grep` write fatals to stderr, so a piped `wc -l` on a
  missing path yields 0 and an `-eq 0` leg PASSES on an absent subject. `|| echo 0` collapses a
  genuine no-match and an exit-2 path failure to the same `0`.
- **A pipe ending in a counter LAUNDERS the exit code of everything upstream.** `$(cmd | wc -l)`
  reports `wc`'s status, never `cmd`'s. Compared with `-eq 0` or `-le N` that is fail-open; `-ge N` is
  the only safe direction unanchored. Every such leg here carries an existence anchor first --
  `git rev-parse <tag>` before a tag diff, a counted control before an `rg` absence.
- **When REPLACING a guard, state which direction the old one failed in before claiming the new one is
  better.** A polarity inversion reads exactly like a hardening. This plan shipped one (see the note
  under `<verification>`) -- another instance of the repair-breeds-its-own-class defect, recorded
  without an ordinal because the index is not resolvable from the record and nothing depends on it.
- **A two-arm fail-open needs both arms closed.** `git rev-parse <tag>` proves the TAG resolves and
  says nothing about the PATH: `git diff <real-tag> -- <mistyped-path> | wc -l` still prints `0` and
  still PASSES (measured). Every tag diff, `git status --porcelain -- <path>` and `git diff --quiet --
  <path>` leg here therefore carries a `[ -f ]` / `[ -d ]` existence anchor as well. This rule was NOT
  derived from principle -- it was extracted from finding F, a live miss inside this task's own repair
  that survived being ordered, implemented, measured in both directions, and reviewed. See
  `<pipe_exit_sweep>`.
- **A DISMISSAL CAN BE RIGHT IN ITS VERDICT AND WORTHLESS AS EVIDENCE, when it was not tracking the
  variable that determines the answer.** Measured in this task: a set of legs was cleared on an
  `-eq`-versus-`-ge` comparator argument. The verdict happened to hold, but for a reason the argument
  never identified (`rg -c` yields an EMPTY capture, and `[ "" -eq 0 ]` is a bash error, not a pass),
  while three legs the framing never considered were broken. So a correct conclusion reached on the
  wrong axis provides no coverage over the cases the axis does not see. Enumerate by MECHANISM, and
  when clearing a set, state which property makes each member safe -- if that property turns out not
  to be the one you were reasoning about, the whole clearance is void regardless of its verdict.
- **A region-scoped assertion beats a file-wide count wherever a construct can legitimately exist
  elsewhere in the same file.** A file-wide count cannot see a construct MOVING into the region you
  care about. Bracket the region between two single-occurrence keys, and ALWAYS put a counted
  anti-vacuity leg on the slice first -- an empty region trivially contains zero of everything.
- **Pin the SHAPE, do not ban the construct, when the region legitimately needs one.** A zero-assertion
  over `try` / `continue` in a region whose fail-CLOSED read guard uses both would forbid the guard
  that must stay. Pinning each at its exact count is strictly stronger: any added tolerance branch
  moves a count whichever form it takes.
- **A zero-assertion needs the EXIT-BASED form.** Use `! rg -q -F needle file`, never
  `[ "$(rg -c -F needle file)" -eq 0 ]` -- `rg -c` with no match exits 1 and prints nothing, so the
  counting form is a bash error and a FALSE RED. The counting form is for nonzero pins only.
- **"REPLACE X with Y" needs TWO legs, one per direction. A leg asserting Y exists is NOT coverage.**
  The executor can satisfy an addition by APPENDING, leaving X fully intact beside it. Assert X is GONE
  and Y is PRESENT. Measured origin: L11 was added to enforce a replace-the-SCOPE-comment requirement
  and enforced only the arrival of the citation, so a minimally-compliant executor could keep the whole
  stale paragraph and append a `TWO_IG_GUARDS` clause below it -- shipping source whose comment says the
  archive is in scope three lines above code that does not read it. That is the "guard deleted, its
  claim left behind" row of the anti-pattern table, and it is the SEAM-02 repeat this task exists to
  close. L2b is the missing direction.
- **`git grep -c` prints `path:count`, not a bare number.** Use `rg -c -F` for counts, and `[ "$N"
  -eq K ]` so an empty capture (rg exit 2) errors the leg rather than passing it.
- **Match the checker's own output on separator-free fragments.** It prints Windows backslash paths.
- **`git grep` first, `rg` for untracked or gitignored paths. NEVER plain `grep`.**
- **ASCII only** in every file and every message. No emoji, no em dash, no curly quote, no ellipsis
  character.
- **NEVER read anything under `.oracle/`, index files included.** `ls`/`find` for existence only.
- **Zero spend.** No `claude -p`, no metered run, no eval. EVL-03 and the D-12 A/B stay owner-gated.
- **MUST NOT touch `plugins/lz-tdd/skills/lz-tpp/` or `plugins/lz-tdd/skills/lz-refactor/SKILL.md`.**
  Both are byte-identical to `lz-tdd@0.0.2` and stay so. Asserted in every task's verify.
- **Do NOT edit `EXPECTED_CHECKS`.** If the emitted count leaves 123, a different guard broke.
  Diagnose; never move the literal to meet the measurement.
- Set `WORK` once per chain to the session scratchpad directory (fall back to `/tmp` if none). Every
  temp capture and the archive-aside destination go there -- OUTSIDE the repo, so no gate walks them.
- A green battery is NOT acceptance. Every guard touched needs an observed RED and an observed GREEN.
</conventions>

<tasks>

<task type="auto">
  <name>Task 1: Rescope the ragged-table gate to the shipped tree, with a recorded RED-then-GREEN pair (OD-X0I-1)</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs, .planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-RED-EVIDENCE.md</files>
  <action>
Work in this order. Steps 1 and 2 run against the UNMODIFIED checker; they are the RED control that
turns the brief's claim into a measurement.

1. **Create the evidence record** `260729-x0i-RED-EVIDENCE.md`. It records, per leg, the command run,
   the observed exit code and the observed output line VERBATIM as the run printed it. Never paste an
   expected string from this plan into it.

2. **RED control, pre-fix (the defect exists).** Move the archived planning record out of the repo to
   `"$WORK"`, run the battery, and observe exit 1 with exactly one FAIL naming that path as
   `UNREADABLE (ENOENT)`. Move it straight back in the SAME chain and assert
   `git status --porcelain` on that path is empty. Record the FAIL line verbatim. If the pre-fix run
   is GREEN, the defect is not what the plan says it is -- STOP and report.

3. **Apply the fix.** Delete the archived-record path constant declaration, and change the gate's
   loop to iterate the shipped markdown list alone (it becomes textually identical to the loop the
   relative-link gate already uses). Nothing else in the gate changes: the per-file `try/catch` that
   already fails closed on an unreadable SHIPPED file stays exactly as it is, and so do
   `tablesSeen`, the anti-vacuity leg and the single `report()` call.

   PROHIBITED, and the whole point of the task: do NOT make the gate tolerate a missing file. No
   `existsSync` skip, no try/catch-and-continue over a missing scope member, no conditional-append,
   no `|| echo 0`. The archive leaves the gate's SCOPE entirely; it is not conditionally skipped. A
   tolerated absence is fail-open and reproduces the guard-that-cannot-fail class.

4. **Replace the gate's SCOPE comment paragraph.** It currently claims the archive is in scope. The
   replacement states the archive is OUT of scope and CITES the three reasons already recorded at the
   retirement note above the `TWO_IG_GUARDS` block (mirrored in `lib/row-guards.mjs`) instead of
   restating them -- two copies of one rationale drift, and this defect IS that drift. Cite by stable
   NAME, never by line number: removing the constant shifts every line below it. The replacement must
   contain no planning-directory path literal, so the gate carries no path it does not read.

   This is ENFORCED IN BOTH DIRECTIONS, not merely requested, because REPLACE means the old prose goes
   as well as the new prose arriving:
   - ARRIVAL: L11 slices the region between the `[lc9] N1 TABLE SHAPE GATE` marker and the
     `RAGGED_TABLE_LABEL` declaration and requires the literal `TWO_IG_GUARDS` inside it. REGION-scoped
     on purpose -- `TWO_IG_GUARDS` occurs 4 times file-wide (measured), so a file-wide needle would be
     vacuous. Restating the three reasons inline instead of pointing at them fails L11.
   - REMOVAL: L2b requires the two stale phrases to be GONE file-wide (each measured at exactly 1
     occurrence, both on the one line this step rewrites), and L11 requires the paragraph's fail-closed
     clause gone from the REGION -- region-scoped because `fails closed` legitimately occurs twice more
     for unrelated guards (measured at lines 563 and 862), so a file-wide needle there would be wrong.

   Appending the citation while leaving the stale paragraph standing is the failure mode these legs
   exist to catch; it satisfies every other leg in this task.

5. **Sweep the checker for surviving claims that the archive is in a gate's scope** -- comments AND
   both `SUMMARY:` output strings. A removed guard's claim left standing in the GREEN headline is a
   named blocking anti-pattern here. Do NOT touch `RETIRED_LABELS` entries or `TAXONOMY_COPY_STEM`:
   those are label strings and a basename stem, not a path any gate reads. The GREEN summary already
   says "in the shipped tree", which this fix makes exactly true rather than under-claimed -- verify
   that by reading it, do not assume it.

6. **RED leg A, post-fix (the gate is not vacuous).** Plant the ragged row in the lz-red
   testing-stance README exactly as `<pre_measurements>` specifies (first data cell plus its
   following pipe, line 19). Run the battery: exit 1, exactly one FAIL, naming that file with the
   table start line and the ragged row's line. Record it verbatim. Restore with `git checkout --` and
   assert the file is clean. This is the leg that proves the narrowed gate still fires.

7. **RED leg B, post-fix (the defect is closed).** Move the archive aside again, run the battery,
   observe exit 0 at 123 checks, move it back in the SAME chain, and assert byte-identical restore.
   Record it. Restore is mandatory -- the file must be byte-identical afterwards.

8. **RED proofs for the NEW LEGS themselves.** L9's anchors and L12's region gate are guards added to
   close fail-opens, so they do not ship unproven either.

   8a. **The scope-invariant anchors, BOTH ARMS.** The fail-open has two arms and the proof must cover
   both -- see `<pipe_exit_sweep>` rows E/F/G, and note the `rev-parse` anchor alone was itself only a
   partial repair. Run and record five observations:
   - bogus TAG, no `rev-parse` anchor -> PASSES (the original fail-open);
   - bogus TAG, with `rev-parse` -> FAILS;
   - bogus PATH with the real tag, no existence anchor -> PASSES (the second arm);
   - bogus PATH, with `[ -d ]` / `[ -f ]` -> FAILS;
   - real tag and real paths, fully anchored -> PASSES.
   Record the `fatal: bad revision` text so L8's needle has something to match.

   8b. **L12's region gate.** Copy the checker to `"$WORK"`, insert an `existsSync` skip inside the
   ragged loop of the COPY, extract L12's slice from the COPY, and observe the leg FAIL. Do NOT plant
   in the real instrument -- a mutant copy measures the leg's matcher exactly and risks nothing.
   Confirm it PASSES against the real file afterwards. Record both.

   Nothing in the repo is edited by this step; it only demonstrates that the legs guarding the critical
   scope invariant and the no-fail-open rule can fail.

9. Commit. Subject: `fix(quick-260729-x0i): scope the ragged-table gate to the shipped tree`.
  </action>
  <verify>
    <automated>
cd D:/projects/github/LayZeeDK/lz-engineering-claude-plugins && \
WORK="${WORK:-/tmp}" && \
C=.claude/skills/lz-red-workspace/tools/check-red-references.mjs && \
A=.planning/research/test-double-taxonomy.md && \
R=plugins/lz-tdd/skills/lz-red/references/testing-stance/README.md && \
E=.planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-RED-EVIDENCE.md && \
echo "L1 POSITIVE ANCHOR on the subject file -- proves it is present and readable before any absence claim" && \
[ "$(rg -c -F 'TAXONOMY_COPY_STEM' "$C")" -eq 2 ] && \
[ "$(rg -c -F 'RAGGED_TABLE_LABEL' "$C")" -eq 3 ] && \
[ "$(rg -c -F 'tablesSeen' "$C")" -eq 4 ] && \
echo "L2 the archive left the gate SCOPE -- constant gone, path literal gone, loop rewritten" && \
[ "$(rg -c -F 'for (const file of pluginsMarkdown) {' "$C")" -eq 2 ] && \
! rg -q -F 'ARCHIVED_RECORD' "$C" && \
! rg -q -F '.planning/research' "$C" && \
echo "L2b the STALE CLAIM is GONE, not just supplemented -- L11 proves the citation arrived, this proves the old prose left" && \
! rg -q -F 'archived planning copy' "$C" && \
! rg -q -F 'PLUS the ONE' "$C" && \
echo "L3 NO fail-open crept in -- sentinel counts pinned; a rise means a tolerated absence was added" && \
[ "$(rg -c -F 'existsSync' "$C")" -eq 9 ] && \
[ "$(rg -c -F 'try {' "$C")" -eq 5 ] && \
[ "$(rg -c -F 'report(' "$C")" -eq 16 ] && \
echo "L4 EXPECTED_CHECKS untouched -- the literal is still 123 and was not moved to meet a measurement" && \
[ "$(rg -c -F 'const EXPECTED_CHECKS = 123;' "$C")" -eq 1 ] && \
echo "L5 GREEN: battery exit 0, 124 emitted lines, zero FAIL, roster at 123" && \
node "$C" > "$WORK/x0i-t1-green.txt" 2>&1 && \
[ "$(rg -c '^  \[PASS\]' "$WORK/x0i-t1-green.txt")" -eq 124 ] && \
! rg -q '^  \[FAIL\]' "$WORK/x0i-t1-green.txt" && \
rg -q -F '123 checks, equal to the PREDICTED literal' "$WORK/x0i-t1-green.txt" && \
echo "L6 RED leg A: gate still FIRES on a shipped-tree ragged row (non-vacuity)" && \
cp "$R" "$WORK/x0i-readme.bak" && \
node -e "const fs=require('fs');const p=process.argv[1];const l=fs.readFileSync(p,'utf8').split(/\r?\n/);l[18]='| [functional-core.md](functional-core.md) |';fs.writeFileSync(p,l.join('\n'));" "$R" && \
node "$C" > "$WORK/x0i-t1-plant.txt" 2>&1; EX_A=$?; \
git checkout -- "$R" && \
[ "$EX_A" -eq 1 ] && \
[ "$(rg -c '^  \[FAIL\]' "$WORK/x0i-t1-plant.txt")" -eq 1 ] && \
rg -q -F 'no ragged pipe table' "$WORK/x0i-t1-plant.txt" && \
rg -q -F 'README.md: table at line 17: header is 2 wide' "$WORK/x0i-t1-plant.txt" && \
[ -f "$R" ] && [ "$(git status --porcelain -- "$R" | wc -l)" -eq 0 ] && \
echo "L7 RED leg B: an absent archive no longer reddens the gate, and restore is byte-identical" && \
mv "$A" "$WORK/x0i-archive-aside.md" && \
node "$C" > "$WORK/x0i-t1-noarchive.txt" 2>&1; EX_B=$?; \
mv "$WORK/x0i-archive-aside.md" "$A" && \
[ "$EX_B" -eq 0 ] && \
[ "$(rg -c '^  \[PASS\]' "$WORK/x0i-t1-noarchive.txt")" -eq 124 ] && \
! rg -q '^  \[FAIL\]' "$WORK/x0i-t1-noarchive.txt" && \
rg -q -F '123 checks, equal to the PREDICTED literal' "$WORK/x0i-t1-noarchive.txt" && \
[ -f "$A" ] && [ "$(git status --porcelain -- "$A" | wc -l)" -eq 0 ] && \
echo "L8 the evidence record exists and carries both RED legs, the pre-fix control and the anchor proof" && \
[ "$(wc -l < "$E")" -ge 20 ] && \
rg -q -F 'UNREADABLE' "$E" && \
rg -q -F 'header is 2 wide' "$E" && \
rg -q -F 'rev-parse' "$E" && \
rg -q -F 'bad revision' "$E" && \
rg -q -F 'bogus PATH' "$E" && \
rg -q -F 'existsSync' "$E" && \
echo "L9 scope invariants -- rev-parse FIRST: without it a bad revision exits 128, wc -l prints 0, and the leg PASSES" && \
git rev-parse lz-tdd@0.0.2 >/dev/null 2>&1 && \
[ -d plugins/lz-tdd/skills/lz-tpp ] && [ -f plugins/lz-tdd/skills/lz-refactor/SKILL.md ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-tpp/ | wc -l)" -eq 0 ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-refactor/SKILL.md | wc -l)" -eq 0 ] && \
echo "L10 rest of the battery unchanged by this edit" && \
node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs > "$WORK/x0i-rg.txt" 2>&1 && \
[ "$(rg -c '\[PASS\]' "$WORK/x0i-rg.txt")" -eq 49 ] && \
node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs | rg -q -F '3/3 assertions pass' && \
echo "L11 CITE-DO-NOT-RESTATE: the new SCOPE comment points at the retirement note by stable name" && \
SCOPE_SLICE="$WORK/x0i-scope-slice.txt" && \
[ "$(rg -c -F '[lc9] N1 TABLE SHAPE GATE' "$C")" -eq 1 ] && \
[ "$(rg -c -F 'const RAGGED_TABLE_LABEL' "$C")" -eq 1 ] && \
rg -N -A 100000 -F '[lc9] N1 TABLE SHAPE GATE' "$C" | rg -N -B 100000 -F 'const RAGGED_TABLE_LABEL' > "$SCOPE_SLICE" && \
[ "$(wc -l < "$SCOPE_SLICE")" -ge 10 ] && \
[ "$(rg -c -F 'TWO_IG_GUARDS' "$SCOPE_SLICE")" -ge 1 ] && \
! rg -q -F 'FAILS CLOSED if it is unreadable' "$SCOPE_SLICE" && \
echo "L12 REGION-SCOPED no-fail-open over the gate's own CODE slice -- L3's file-wide counts cannot see a tolerance that MOVED into this gate" && \
GATE_SLICE="$WORK/x0i-gate-code.txt" && \
[ "$(rg -c -F 'N2 RELATIVE-LINK RESOLUTION GATE' "$C")" -eq 1 ] && \
rg -N -A 100000 -F 'const RAGGED_TABLE_LABEL' "$C" | rg -N -B 100000 -F 'N2 RELATIVE-LINK RESOLUTION GATE' > "$GATE_SLICE" && \
[ "$(wc -l < "$GATE_SLICE")" -ge 20 ] && \
! rg -q -F 'existsSync' "$GATE_SLICE" && \
[ "$(rg -c -F 'try {' "$GATE_SLICE")" -eq 1 ] && \
[ "$(rg -c -F 'continue;' "$GATE_SLICE")" -eq 1 ] && \
[ "$(rg -c -F 'report(' "$GATE_SLICE")" -eq 1 ] && \
echo TASK1_VERIFY_OK
    </automated>
  </verify>
  <done>
The archived planning record is out of the ragged gate's SCOPE, not conditionally skipped: the path
constant and the loop member are gone (measured count 0), the pinned fail-open sentinels are
unchanged, and `EXPECTED_CHECKS` is still the untouched literal 123. The battery is GREEN at 123
checks / 0 FAILs both with the archive present and with it absent (RED leg B), and it still FAILS at
exit 1 with exactly one named FAIL when a shipped markdown table is made ragged (RED leg A). Both
legs plus the pre-fix `UNREADABLE` control and the anchor's own RED proof are recorded verbatim in
`260729-x0i-RED-EVIDENCE.md`. No gate output string or comment still claims the archive is in scope.
The new SCOPE comment CITES the retirement note by stable name rather than restating it. Both
shipped-skill scope invariants report 0 lines behind a `git rev-parse` existence anchor.

WHAT EACH LEG PROVES vs WHAT THE PROSE CLAIMS:
- L2 proves the path is absent from the file. It does NOT prove the loop is correct -- L5/L7 do.
- L2b and L11 are a REPLACE pair and neither alone is coverage: L11 proves the citation ARRIVED, L2b
  proves the stale claim LEFT. L11 alone is satisfied by appending, which is how a comment asserting
  the archive is in scope could ship three lines above code that does not read it. This pair is the
  only automated enforcement success criterion 4 has.
- L3 proves no NEW absence-tolerating construct appeared. It does not prove the 9 pre-existing
  `existsSync` uses are sound; they are out of scope and unchanged.
- L6 proves the gate fires on the SHIPPED tree, which is the only claim its label makes after the fix.
- L7 proves the milestone-close failure mode is closed, and that the archive survived the proof.
- L9's `rev-parse` leg is what makes the two tag diffs mean anything; without it they PASS against a
  nonexistent revision (measured). It proves the tag RESOLVES, not that the diff is right -- the two
  `-eq 0` legs behind it do that, and only because the anchor cleared first.
- L11 proves the new comment POINTS at the rationale. It does not prove the pointer is apt prose. It
  is region-scoped because `TWO_IG_GUARDS` occurs 4 times file-wide, so the file-wide form of this
  leg would be the "label claims more than its needle enforces" class in a new costume.
- L12 proves no absence-tolerance lives IN THIS GATE. That is strictly more than L3 proves: L3 sees
  only file-wide totals, so a tolerance MOVED into this gate from elsewhere keeps every total stable
  and passes. L12 does NOT prove the other eight `existsSync` uses in the file are sound -- they are
  out of scope and unchanged. Its `try {` and `continue;` pins are exactly 1, not zero, because the
  gate's fail-CLOSED per-file read guard legitimately uses both and must survive; a zero-assertion
  there would forbid the guard it is meant to protect.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the three measured prose defects in the archive header's leading blockquote (D-02, D-04, D-10, D-12)</name>
  <files>.planning/research/test-double-taxonomy.md</files>
  <action>
Task 1 MUST be committed first. D1's correct wording depends on the checker no longer reading the
path; writing it against the pre-fix reality reproduces the class this task exists to close.

Edit ONLY the leading blockquote header (the `>` block above the H1). Prose only. Do NOT touch the
taxonomy BODY -- it stays archived and unaccepted per D-12. Introduce no pipe table and no ragged
row. ASCII only: `check-hygiene` does not scan the planning tree, so nothing else will catch a
Unicode character here.

Before editing, re-measure each needle's baseline count in the header slice and record it in
`260729-x0i-RED-EVIDENCE.md`. For a presence-required claim the RED evidence IS "phrase absent from
the header, measured count 0" -- a `file:line` is unsatisfiable for an absent phrase, so do not
manufacture one.

**D1 -- the dependency claim.** Re-read the header against the POST-Task-1 reality and correct any
residual framing that misdescribes the dependency state. Before Task 1 the battery had a hard
fail-closed dependency on this exact path, so "No skill, agent or command reads it" was literally
true but materially misleading, and "Nothing under `plugins/` points at it any more" scoped the claim
narrower than the header's own INERT framing implies. After Task 1 the stronger statement is true and
needs no hedge: nothing outside the planning tree reads this path at all, the development instrument
included. State that, and NAME the instrument (`check-red-references.mjs`) so a reader can check it
with one command instead of trusting the sentence.

**D2 -- the forward constraint is BLOCKED today.** The header says a future in-plugin use goes in as
ONE plugin-wide shared reference. Guard N3 in `check-red-references.mjs` currently forbids exactly
that: it FAILS on the basename STEM under ANY path in the shipped tree with no carve-out, and its own
comment scopes the material to lz-red only. Record that plainly -- a relocation needs an explicit N3
carve-out ("at most one, at the canonical path", not "no file with this stem"), and the ownership
question (lz-red-only versus shared) has to be answered before the carve-out is justified. State the
constraint from the LIVE code, not from the research record's older snapshot of N3. Do NOT weaken,
rescope or add a carve-out to N3 in this task; only state the constraint honestly.

**D3 -- the mechanism claim is falsified twice over.** The header's clause saying no mechanism for a
shared reference has been designed, spiked or proven is false: `PLUGIN-WIDE-REFERENCE-RESEARCH.md`
carries a VERIFIED V1-V7 table read out of the shipped `claude.exe` 2.1.220 bundle, and
`plugins/lz-tdd/references/beck-tdd-by-example.md` is already in production use via the R100
relocation. Replace the clause with the measured state and cite BOTH artifacts by path. Note that
D-10 ("do not investigate the mechanism") was discharged by owner-authorized research, so this is a
correction of a stale claim and not a re-opening of a locked decision.

The header must not acquire a NEW claim that is not measured. Every statement it gains must be
checkable against a named file, line or command. It is enforced by NO guard, which is exactly why.
Commit. Subject: `docs(quick-260729-x0i): correct three measured claims in the archive header`.
  </action>
  <verify>
    <automated>
cd D:/projects/github/LayZeeDK/lz-engineering-claude-plugins && \
WORK="${WORK:-/tmp}" && \
F=.planning/research/test-double-taxonomy.md && \
C=.claude/skills/lz-red-workspace/tools/check-red-references.mjs && \
E=.planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-RED-EVIDENCE.md && \
H1='^# Test-double and stand-in taxonomy across authors$' && \
echo "L1 POSITIVE ANCHOR: the file and BOTH slices are real and 540 lines, so L2's hash equality cannot pass on two empty slices" && \
[ "$(wc -l < "$F")" -ge 560 ] && \
[ "$(git show HEAD:"$F" | rg -N -A 100000 "$H1" | wc -l)" -eq 540 ] && \
[ "$(rg -N -A 100000 "$H1" "$F" | wc -l)" -eq 540 ] && \
[ "$(rg -N -B 100000 "$H1" "$F" | wc -l)" -ge 25 ] && \
echo "L2 BODY BYTE-IDENTICAL: the 540-line H1-to-EOF slice hashes the same before and after" && \
B_BEFORE="$(git show HEAD:"$F" | rg -N -A 100000 "$H1" | sha256sum | cut -c1-32)" && \
B_AFTER="$(rg -N -A 100000 "$H1" "$F" | sha256sum | cut -c1-32)" && \
[ -n "$B_BEFORE" ] && [ "$B_BEFORE" = "$B_AFTER" ] && \
echo "L3 the edit is CONFINED to the header: header line count grew or held, body slice still 540" && \
[ "$(rg -N -A 100000 "$H1" "$F" | wc -l)" -eq 540 ] && \
echo "L4 no pipe row entered the header, and the file stays ASCII-only (hygiene does not scan here)" && \
! rg -N -B 100000 "$H1" "$F" | rg -q '^ {0,3}\|' && \
! rg -q '[^[:ascii:]]' "$F" && \
echo "L5 email allowlist-inversion on the edited doc: the only email-shaped token allowed is the public gmail" && \
[ "$(rg -o --no-filename '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$F" | sort -u | rg -v -F -x 'larsbrinknielsen@gmail.com' | wc -l)" -eq 0 ] && \
echo "L6 D1: the header now names the instrument, AND the instrument provably no longer reads the path" && \
[ "$(rg -N -B 100000 "$H1" "$F" | rg -c -F 'check-red-references.mjs')" -ge 1 ] && \
[ "$(rg -c -F 'TAXONOMY_COPY_STEM' "$C")" -eq 2 ] && \
! rg -q -F '.planning/research' "$C" && \
echo "L7 D2: the header names guard N3 and the carve-out a relocation would need" && \
[ "$(rg -N -B 100000 "$H1" "$F" | rg -c -F 'N3')" -ge 1 ] && \
[ "$(rg -N -B 100000 "$H1" "$F" | rg -c -F 'carve-out')" -ge 1 ] && \
echo "L8 D2 TRUTH LEG: a probe at the canonical plugin-wide path really does FAIL N3 by name" && \
P=plugins/lz-tdd/references/test-double-taxonomy.md && \
[ ! -e "$P" ] && printf 'probe\n' > "$P" && \
node "$C" > "$WORK/x0i-t2-n3.txt" 2>&1; EX_N3=$?; \
rm -f "$P" && \
[ "$EX_N3" -eq 1 ] && \
[ "$(rg -c '^  \[FAIL\]' "$WORK/x0i-t2-n3.txt")" -eq 1 ] && \
rg -q -F 'no test-double taxonomy copy in the shipped tree' "$WORK/x0i-t2-n3.txt" && \
[ -d plugins ] && [ "$(git status --porcelain -- plugins/ | wc -l)" -eq 0 ] && \
echo "L9 D3: the false mechanism clause is GONE (1 -> 0, direction-anchored by L1) and both citations are present" && \
! rg -q -F 'no mechanism for a shared reference has been designed' "$F" && \
[ "$(rg -N -B 100000 "$H1" "$F" | rg -c -F 'PLUGIN-WIDE-REFERENCE-RESEARCH.md')" -ge 1 ] && \
[ "$(rg -N -B 100000 "$H1" "$F" | rg -c -F 'beck-tdd-by-example.md')" -ge 1 ] && \
echo "L10 D3 TRUTH LEG: both cited artifacts exist on disk, so the citation is true and not merely present" && \
[ -f plugins/lz-tdd/references/beck-tdd-by-example.md ] && \
[ -f .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/PLUGIN-WIDE-REFERENCE-RESEARCH.md ] && \
echo "L10b the action's record-the-baselines instruction has a leg: this task's own needle baselines are in the evidence file" && \
[ "$(wc -l < "$E")" -ge 20 ] && \
[ "$(rg -c -F 'measured count 0' "$E")" -ge 1 ] && \
rg -q -F 'PLUGIN-WIDE-REFERENCE-RESEARCH' "$E" && \
rg -q -F 'N3' "$E" && \
echo "L11 N3 was NOT weakened by this task -- stating a constraint must not become relaxing it" && \
[ "$(rg -c -F 'const TAXONOMY_COPY_STEM = "test-double-taxonomy";' "$C")" -eq 1 ] && \
[ "$(rg -c -F 'for (const file of pluginsAllFiles) {' "$C")" -eq 1 ] && \
echo "L12 battery + scope invariants still green" && \
node "$C" > "$WORK/x0i-t2-green.txt" 2>&1 && \
[ "$(rg -c '^  \[PASS\]' "$WORK/x0i-t2-green.txt")" -eq 124 ] && \
! rg -q '^  \[FAIL\]' "$WORK/x0i-t2-green.txt" && \
rg -q -F '123 checks, equal to the PREDICTED literal' "$WORK/x0i-t2-green.txt" && \
git rev-parse lz-tdd@0.0.2 >/dev/null 2>&1 && \
[ -d plugins/lz-tdd/skills/lz-tpp ] && [ -f plugins/lz-tdd/skills/lz-refactor/SKILL.md ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-tpp/ | wc -l)" -eq 0 ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-refactor/SKILL.md | wc -l)" -eq 0 ] && \
echo TASK2_VERIFY_OK
    </automated>
  </verify>
  <done>
The leading blockquote states the dependency state as it is after Task 1 and names the instrument
(D1); records that the ONE plugin-wide shared reference shape it prescribes is BLOCKED by guard N3
today and needs an explicit carve-out, with N3 itself unweakened (D2); and replaces the false
undesigned-mechanism clause with the measured state, citing the verified research record and the
reference already in production use (D3). The taxonomy body is byte-identical (540-line slice, equal
hashes), the header gained no pipe row, the file is ASCII-only and email-clean, and both frozen
shipped surfaces are still at 0 lines against `lz-tdd@0.0.2`.

WHAT EACH LEG PROVES vs WHAT THE PROSE CLAIMS:
- L6/L7/L9 prove the header CONTAINS the required citations. They do NOT prove the sentences read
  well; that is the unprimed content reviewer's job, not a grep's.
- L8 and L10 are the legs that make the header's two substantive claims TRUE rather than merely
  present: N3 really blocks the canonical path, and both cited artifacts really exist.
- L2/L3 together are the only thing standing between a header edit and a silent body edit. L1's
  540-line anchor is what stops L2 passing on two empty slices.
- L11 guards the failure mode this task could most plausibly cause: "state the constraint" quietly
  becoming "relax the constraint".
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the two by-design instrument gaps and the contingent relocation, and annotate the superseded scope instruction</name>
  <files>.planning/REQUIREMENTS.md, .planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md</files>
  <action>
Record only, in both files. Fix NOTHING here -- all three deferred items stay open by design, and the
superseded instruction is ANNOTATED, never rewritten. Both halves are record-keeping, which is why
they share one task and one commit. Sequence this AFTER Task 1 has landed: step 4's annotation
asserts what Task 1 changed.

Append three bullets to `.planning/REQUIREMENTS.md` under the existing `### Later milestones` heading
inside `## Future Requirements`, matching the existing `**FUT-*:**` bullet shape exactly (see
`FUT-OUTSIDE-IN`, `FUT-04`, `FUT-METZ-REFACTOR`). Invent no new heading and add no new section.
ROADMAP.md is NOT edited -- see the ROUTING DEVIATION note in `<pre_measurements>` for why this
register is the right home and how to redirect it in one line if the owner disagrees.

- **FUT-ROSTER-TYPO**: the `RETIRED_LABELS` roster is blind to a TYPO in a retired label. A mistyped
  entry is absent from the emitted label set for the same reason a correctly-retired label is, so the
  leg passes either way and the roster cannot tell a typo from a retirement. Closing it needs
  cross-version label CAPTURE -- comparing the emitted label set against a prior commit's -- which is
  a different instrument from a hand-maintained roster, not an edit to the roster.

- **FUT-ROSTER-LITERALS**: 8 of the 13 `NEW_LABELS` entries are constant-derived and therefore
  rename-blind. A constant on both sides of a comparison asserts nothing: the emission site pushes
  the same constant the roster lists, so a rename moves both sides together and the leg stays green.
  Only the `[wev G17]` entry is a string literal, and it is the only one that can catch a rename
  (MEASURED: with G17's label value changed the battery exits 0 before that entry and 1 after).
  Converting the other 8 needs one rename proof each.

- **FUT-TAXONOMY-SHARED**: relocate the archived test-double taxonomy into the plugin as ONE
  plugin-wide shared reference at `plugins/lz-tdd/references/`, cited as INLINE CODE with the
  plugin-root variable and never as a Markdown link (guard N2 classifies a relative target and would
  correctly fail it). CONTINGENT on the D-12 A/B showing lift; if it shows none, the archive stays
  where it is and this entry closes as declined. It is NOT approved for shipping and its absence
  today is the A/B's baseline arm, not an oversight. REQUIRES an explicit N3 carve-out: N3 fails on
  the basename stem over every file under `plugins/` with no carve-out for a canonical location
  (MEASURED: a probe file at that exact path fails N3 by name), so the carve-out must be "at most
  one, at the canonical path" and the lz-red-only-versus-shared ownership question has to be answered
  first. The mechanism itself is VERIFIED, not speculative -- V1-V7 in
  `PLUGIN-WIDE-REFERENCE-RESEARCH.md`, with the pattern already in production at
  `plugins/lz-tdd/references/beck-tdd-by-example.md`.

Each bullet names the COST of closing it, so a future reader can price it without re-deriving.

**Step 4 -- annotate the superseded scope instruction in `260729-lc9-CONTEXT.md`.**

Line 294 of that file, inside `<specifics>`, is the instruction that PRODUCED the defect Task 1
removes, and it is still phrased as a live requirement. Naming OD-X0I-1 in this plan's objective
protects only this executor; the next reader opens that artifact and sees a locked decision.

Do NOT rewrite it. Do NOT delete it. It is a committed record of what was locked at the time, and
rewriting it would destroy the trail that explains why the defect existed. ANNOTATE it in place:

- Leave the original sentence exactly where it is, byte-for-byte. One leg asserts it survives.
- Immediately below it, add a blockquote note in the file's own existing blockquote-note idiom (the
  same shape as the `AUTHORITY NOTE, READ FIRST` block at the top). Prose only, ASCII only, no pipe
  table, no new heading.
- The note must carry, all four, because each is what makes it checkable rather than assertive:
  1. the word `SUPERSEDED` and a date;
  2. the superseding id `OD-X0I-1` and the task `260729-x0i`;
  3. an explicit statement that the line above is preserved as a record and is NOT a live instruction;
  4. the REASON in one clause -- scoping the gate to the archived planning copy made a shipped-skill
     gate fail CLOSED on a planning artifact that a milestone close relocates. Ground it in the two
     measurements already in hand: the pre-fix `UNREADABLE (ENOENT)` battery failure, and
     `.planning/milestones/lz-tdd@0.0.1-research/` existing on disk as the relocation precedent.
- Point at this plan and at `260729-x0i-RED-EVIDENCE.md` so a reader can follow the trail forward.

Touch nothing else in that file: no other decision, no heading, no table. The verify legs pin its
heading counts and its pipe-line count for exactly that reason.

Commit. Subject: `docs(quick-260729-x0i): record two by-design gaps and annotate the superseded scope instruction`.
  </action>
  <verify>
    <automated>
cd D:/projects/github/LayZeeDK/lz-engineering-claude-plugins && \
WORK="${WORK:-/tmp}" && \
R=.planning/REQUIREMENTS.md && \
echo "L1 POSITIVE ANCHOR: the register and its target region are real before any absence leg" && \
[ "$(wc -l < "$R")" -ge 166 ] && \
[ "$(rg -c -F '### Later milestones' "$R")" -eq 1 ] && \
[ "$(rg -c -F '## Out of Scope' "$R")" -eq 1 ] && \
[ "$(rg -c -F 'FUT-OUTSIDE-IN' "$R")" -ge 1 ] && \
echo "L2 no new section invented -- heading counts unchanged at 4 h2 and 14 h3" && \
[ "$(rg -c '^## ' "$R")" -eq 4 ] && \
[ "$(rg -c '^### ' "$R")" -eq 14 ] && \
echo "L3 all three ids present exactly once each" && \
[ "$(rg -c -F 'FUT-ROSTER-TYPO' "$R")" -eq 1 ] && \
[ "$(rg -c -F 'FUT-ROSTER-LITERALS' "$R")" -eq 1 ] && \
[ "$(rg -c -F 'FUT-TAXONOMY-SHARED' "$R")" -eq 1 ] && \
echo "L4 REGION-SCOPED: all three sit inside the Later-milestones slice, not merely somewhere in the file" && \
SLICE="$WORK/x0i-t3-slice.txt" && \
rg -N -A 100000 -F '### Later milestones' "$R" | rg -N -B 100000 -F '## Out of Scope' > "$SLICE" && \
[ "$(wc -l < "$SLICE")" -ge 15 ] && \
[ "$(rg -c -F 'FUT-ROSTER-TYPO' "$SLICE")" -eq 1 ] && \
[ "$(rg -c -F 'FUT-ROSTER-LITERALS' "$SLICE")" -eq 1 ] && \
[ "$(rg -c -F 'FUT-TAXONOMY-SHARED' "$SLICE")" -eq 1 ] && \
echo "L5 each entry states the COST of closing it (the whole point of a deferral record)" && \
[ "$(rg -c -F 'cross-version label' "$SLICE")" -ge 1 ] && \
[ "$(rg -c -F 'rename proof' "$SLICE")" -ge 1 ] && \
[ "$(rg -c -F 'carve-out' "$SLICE")" -ge 1 ] && \
echo "L6 NOTHING WAS FIXED: the two gaps are still open in the instrument, as designed" && \
C=.claude/skills/lz-red-workspace/tools/check-red-references.mjs && \
[ "$(rg -c -F 'TWO_IG_GUARDS.map((guard) => guard.label)' "$C")" -eq 1 ] && \
[ "$(rg -c -F 'const EXPECTED_CHECKS = 123;' "$C")" -eq 1 ] && \
echo "L7 ROADMAP.md byte-unchanged -- the routing decision is recorded by an assertion, not a claim" && \
[ -f .planning/ROADMAP.md ] && git diff --quiet -- .planning/ROADMAP.md && \
echo "L8 register stays ASCII-only and email-clean (hygiene does not scan the planning tree)" && \
! rg -q '[^[:ascii:]]' "$R" && \
[ "$(rg -o --no-filename '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$R" | sort -u | rg -v -F -x 'larsbrinknielsen@gmail.com' | wc -l)" -eq 0 ] && \
K=.planning/quick/260729-lc9-scope-the-test-double-taxonomy-to-lz-red/260729-lc9-CONTEXT.md && \
echo "L9 POSITIVE ANCHOR on the annotated file -- proves it is present and readable BEFORE any content or absence claim on it" && \
[ "$(wc -l < "$K")" -ge 340 ] && \
[ "$(rg -c -F '## Implementation Decisions' "$K")" -eq 1 ] && \
echo "L10 the ORIGINAL instruction survives VERBATIM, exactly once -- the record being annotated cannot be quietly dropped" && \
[ "$(rg -c -F 'Scope the guard to the shipped tree AND the `.planning/` copy.' "$K")" -eq 1 ] && \
echo "L11 the annotation is present and carries the id, the task and the reason (all four required elements)" && \
[ "$(rg -c -F 'SUPERSEDED' "$K")" -ge 1 ] && \
[ "$(rg -c -F 'OD-X0I-1' "$K")" -ge 1 ] && \
[ "$(rg -c -F '260729-x0i' "$K")" -ge 1 ] && \
[ "$(rg -c -F 'fail CLOSED' "$K")" -ge 1 ] && \
[ "$(rg -c -F 'milestone close' "$K")" -ge 1 ] && \
[ "$(rg -c -F 'not a live instruction' "$K")" -ge 1 ] && \
echo "L12 TRUTH LEG: the annotation's stated reason holds -- the relocation precedent is on disk and Task 1 really removed the dependency" && \
[ -d .planning/milestones/lz-tdd@0.0.1-research ] && \
! rg -q -F 'ARCHIVED_RECORD' .claude/skills/lz-red-workspace/tools/check-red-references.mjs && \
echo "L13 nothing else in that artifact moved: heading and pipe-line counts unchanged, still ASCII and email-clean" && \
[ "$(rg -c '^## ' "$K")" -eq 6 ] && \
[ "$(rg -c '^### ' "$K")" -eq 17 ] && \
[ "$(rg -c '^ {0,3}\|' "$K")" -eq 5 ] && \
! rg -q '[^[:ascii:]]' "$K" && \
[ "$(rg -o --no-filename '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' "$K" | sort -u | rg -v -F -x 'larsbrinknielsen@gmail.com' | wc -l)" -eq 0 ] && \
echo "L14 scope invariants at THIS task's commit boundary too, so the conventions claim is TRUE as written" && \
git rev-parse lz-tdd@0.0.2 >/dev/null 2>&1 && \
[ -d plugins/lz-tdd/skills/lz-tpp ] && [ -f plugins/lz-tdd/skills/lz-refactor/SKILL.md ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-tpp/ | wc -l)" -eq 0 ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-refactor/SKILL.md | wc -l)" -eq 0 ] && \
echo TASK3_VERIFY_OK
    </automated>
  </verify>
  <done>
Three `FUT-*` bullets exist exactly once each inside the existing `### Later milestones` region of
`.planning/REQUIREMENTS.md`, matching the register's established bullet shape, each naming the cost
of closing it. No heading was added (4 h2 / 14 h3 unchanged), ROADMAP.md is byte-unchanged, the file
stays ASCII-only and email-clean, and NEITHER gap was fixed -- the constant-derived roster entries
and the untouched `EXPECTED_CHECKS` literal are asserted as still present.

`260729-lc9-CONTEXT.md` line 294 now carries a SUPERSEDED annotation naming OD-X0I-1, task
260729-x0i, the fail-closed reason and the milestone-close relocation, while the original instruction
survives verbatim exactly once. That artifact's heading counts (6 h2 / 17 h3) and pipe-line count (5)
are unchanged, so no other decision was touched, and it stays ASCII-only and email-clean.

WHAT EACH LEG PROVES vs WHAT THE PROSE CLAIMS:
- L3 proves the ids exist in the file. L4 is what proves they are in the RIGHT SECTION -- an id added
  to the wrong region would pass L3 and read as coverage.
- L5 proves each entry carries its cost phrase. It does not prove the cost estimate is correct.
- L6 is the anti-drift leg: this task is a RECORD, so a "helpful" partial fix is a scope violation,
  not a bonus.
- L9 is the counted positive control that makes L10-L13 meaningful. Without it a mistyped path gives
  `rg` an exit-2 fatal on stderr, and every `-ge 1` capture collapses to an empty string while every
  bare absence check passes -- the fail-open-by-direction class.
- L10 and L11 are DELIBERATELY a pair pointing opposite ways: L11 proves the annotation arrived, L10
  proves it annotated something rather than replaced it. Either leg alone permits the failure the
  other catches, and a rewrite-instead-of-annotate would pass L11 on its own.
- L12 proves the annotation's REASON is true, not merely stated. L13 proves the edit was surgical --
  it does not prove the wording reads well, which is the unprimed reviewer's job.
- L14 exists so `<conventions>`'s claim that the scope invariants are "asserted in every task's
  verify" is TRUE as written. The alternative was hedging the sentence to match the gap, which would
  permanently weaken a stated invariant to save three lines. It also fast-fails at this task's own
  commit boundary instead of deferring the discovery to the final gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev instrument -> shipped tree | The battery is the only automated gate on the shipped plugin. Weakening it silently removes protection from published content. |
| planning doc -> public repo | `.planning/` is TRACKED and public. `check-hygiene` does NOT scan it, so ASCII and email hygiene there are unenforced by the battery. |
| plan prose -> written file | An `<action>` literal echoed into a file can invalidate the negative grep that guards it. |
| archived record -> milestone close | `/gsd-complete-milestone` relocates `.planning/` subdirectories. Any code path depending on such a path breaks at the close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x0i-01 | Tampering | ragged-table gate scope | high | mitigate | Fail-open forbidden by construction, checked at TWO scopes: L3's file-wide sentinel counts (`existsSync` 9, `try {` 5, `report(` 16) AND L12's region-scoped shape pin over the gate's own 33-line code slice, which is the only leg that can see a tolerance MOVING into this gate under a stable file-wide total. RED leg A proves the narrowed gate still fires; RED leg B proves the failure mode is closed; step 8b proves L12 itself can fail, against a mutant copy. |
| T-x0i-02 | Tampering | `EXPECTED_CHECKS` roster literal | high | mitigate | Task 1 L4 asserts the literal is still exactly `123`; the gate's single `report()` call makes a count move impossible from this edit, so a move means a different guard broke. |
| T-x0i-03 | Tampering | taxonomy body under a header edit | high | mitigate | Task 2 L2/L3 hash the 540-line H1-to-EOF slice before and after, anchored by L1 so the legs cannot pass on empty slices. |
| T-x0i-04 | Tampering | frozen shipped skills (lz-tpp, lz-refactor) | critical | mitigate | All THREE tasks plus the final gate assert `git diff lz-tdd@0.0.2` at 0 lines for both surfaces, each preceded by BOTH anchors: `git rev-parse` for the tag and `[ -d ]` / `[ -f ]` for the paths. Either arm missing fails OPEN -- a bad revision exits 128 and a mistyped pathspec exits 0, and `wc -l` prints 0 for both (measured; `<pipe_exit_sweep>` rows E/F). Task 1 step 8 records a five-observation RED proof over both arms. RED leg A plants only inside lz-red and restores via `git checkout --` with an existence-anchored cleanliness assertion. |
| T-x0i-05 | Information disclosure | maintainer identity in public planning docs | high | mitigate | Allowlist-inversion email leg on both edited planning docs (Task 2 L5, Task 3 L8): assert the only email-shaped token present is the approved public gmail. The forbidden value is never written as a needle. |
| T-x0i-06 | Tampering | N3 relaxed while "stating" its constraint | high | mitigate | Task 2 L11 pins N3's stem constant and its all-files loop; the task action forbids adding a carve-out. |
| T-x0i-07 | Repudiation | a guard trusted without a falsifiability proof | high | mitigate | `260729-x0i-RED-EVIDENCE.md` records the pre-fix control, both post-fix RED legs, and both anchor proofs verbatim as run; Task 1 L8 and Task 2 L10b assert the record carries them, so no record-keeping instruction in this plan is legless. |
| T-x0i-12 | Tampering | a removed guard's claim left standing in the source it no longer describes | high | mitigate | L2b (file-wide, two needles) plus L11's region-scoped fail-closed-clause needle form a replace-pair with L11's citation leg. Measured RED at HEAD, so they can only go GREEN once the paragraph is actually replaced. This is the "guard deleted, its claim left behind" / SEAM-02 class. |
| T-x0i-08 | Denial of service | archive lost during the move-aside proofs | medium | mitigate | Move and restore in the SAME `&&` chain, destination outside the repo, restore asserted by `git status --porcelain` at 0 lines. The file is committed at HEAD, so `git checkout --` is the backstop. |
| T-x0i-09 | Elevation of privilege | unapproved metered spend | high | mitigate | Zero `claude -p` in the plan; EVL-03 and the D-12 A/B stay owner-gated and are only REFERENCED by Task 3. |
| T-x0i-10 | Repudiation | a committed locked decision rewritten instead of annotated | high | mitigate | Task 3 L10 asserts the original instruction survives verbatim exactly once, paired against L11's annotation-present legs; L13 pins the artifact's heading and pipe-line counts so no sibling decision moves. |
| T-x0i-11 | Tampering | a superseded instruction restored in good faith by a later reader | high | mitigate | The supersession lives in the ARTIFACT, not only in this plan's objective, and names the id, the task and the measured reason (Task 3 step 4, legs L10-L12). |
| T-x0i-SC | Tampering | npm/pip/cargo installs | low | accept | No install step exists in this plan and no dependency is added; `package.json` is not in `files_modified`. Nothing to audit. |
</threat_model>

<verification>
Run at the final commit boundary, every leg asserted, `&&` throughout. This is the gate the brief
pins; it is deliberately identical to the state anchor so a drift is visible immediately.

```bash
cd D:/projects/github/LayZeeDK/lz-engineering-claude-plugins && \
WORK="${WORK:-/tmp}" && \
node .claude/skills/lz-red-workspace/tools/check-red-references.mjs > "$WORK/x0i-final.txt" 2>&1 && \
[ "$(rg -c '^  \[PASS\]' "$WORK/x0i-final.txt")" -eq 124 ] && \
! rg -q '^  \[FAIL\]' "$WORK/x0i-final.txt" && \
rg -q -F '123 checks, equal to the PREDICTED literal' "$WORK/x0i-final.txt" && \
node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs > "$WORK/x0i-final-rg.txt" 2>&1 && \
[ "$(rg -c '\[PASS\]' "$WORK/x0i-final-rg.txt")" -eq 49 ] && \
node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs | rg -q -F '3/3 assertions pass' && \
node .claude/skills/lz-red-workspace/extract-samples.mjs | rg -q -F '8 module(s) tsc --strict --noEmit clean' && \
node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs | rg -q -F 'hygiene GREEN' && \
claude plugin validate . && \
git rev-parse lz-tdd@0.0.2 >/dev/null 2>&1 && \
[ -d plugins/lz-tdd/skills/lz-tpp ] && [ -f plugins/lz-tdd/skills/lz-refactor/SKILL.md ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-tpp/ | wc -l)" -eq 0 ] && \
[ "$(git diff lz-tdd@0.0.2 -- plugins/lz-tdd/skills/lz-refactor/SKILL.md | wc -l)" -eq 0 ] && \
[ "$(git status --porcelain | wc -l)" -eq 0 ] && \
echo FINAL_GATE_OK
```

**Why `git rev-parse` leads the two scope-invariant legs, and the lesson behind it.**

`wc -l` is LAST in the pipe, so `$(...)` reports ITS status and discards git's. MEASURED:
`git diff <bogus-tag> -- <path>` exits `128` with `fatal: bad revision` on stderr, `| wc -l` prints
`0`, and `[ "0" -eq 0 ]` PASSES. Unanchored, this leg would have certified the two frozen shipped
surfaces byte-identical while comparing against nothing -- and it is the ONLY mitigation for
`T-x0i-04`, which this plan's own threat model rates `critical`. `git rev-parse` fails first on a bad
revision, which closes it (MEASURED both directions: FAILS on the bogus tag, PASSES on the real one).

**This is another instance of this work stream's signature defect -- a repair introducing the class it
repaired -- and it is recorded here rather than silently patched.** An earlier revision of this block
carried a comment arguing that the `wc -l` form REPLACED a `git diff --quiet` shape because `--quiet`
returns 1 for both "differs" and "bad revision". That reasoning is backwards: returning 1 on a bad
revision is fail-CLOSED and conservative. The "fix" INVERTED the guard's polarity while reading like a
hardening, and the argument for it was written into a comment -- which is how earlier instances
survived review. `.continue-here.md` records the prior magnitude as "six consecutive rounds each
introduced a new self-falsifying claim"; NO ordinal is asserted for this one, deliberately. That count
is of completed ROUNDS, and this shape arrived from an earlier round's comment rather than from this
plan, so whether it is a fresh instance or a fresh sighting of one already inside those six is not
resolvable from the record. An unverifiable ordinal in a document whose whole subject is claims that
outrun what they name would be self-undermining, and nothing here depends on the number.

Two generalisations for anything downstream of this plan:

1. **A pipe ending in a counter launders the exit code of everything upstream of it.** Any
   `$(cmd | wc -l)` compared with `-eq 0` or `-le N` needs a separate existence or success anchor on
   the same subject first. `-ge N` is the only direction that is safe unanchored.
2. **When replacing a guard, state which direction the OLD one failed in before claiming the new one
   is better.** A polarity inversion is invisible if you only compare expressiveness.

`git status --porcelain | wc -l` at 0 needs no separate anchor: the preceding `git rev-parse` and
`git diff` legs have already proven this is a valid repo, which is the only failure mode that would
make `git status` print nothing spuriously.

NOT covered by this gate, stated so nobody mistakes it for acceptance: the header's READABILITY. Five
plan-check iterations in the prior round passed prose an unprimed reviewer immediately found BROKEN.
Mechanism review and content review find disjoint defects. The header edit needs at least one
from-scratch unprimed content review before acceptance; a green battery is not it.
</verification>

<success_criteria>
1. The ragged-table gate walks only the shipped tree. The archived-record constant and its loop
   member are gone (measured count 0), no absence-tolerating construct replaced them -- proven at BOTH
   scopes, file-wide sentinel counts unchanged AND the gate's own code slice region-scoped-clean
   (`existsSync` zero, `try {` / `continue;` / `report(` each exactly 1) -- and `EXPECTED_CHECKS` is
   the untouched literal 123.
2. `260729-x0i-RED-EVIDENCE.md` records, verbatim as run: the pre-fix `UNREADABLE (ENOENT)` control,
   post-fix RED leg A (a planted ragged row in a shipped file FAILS by name at exit 1 with exactly
   one FAIL), post-fix RED leg B (an absent archive leaves the battery GREEN at 123), and the anchors'
   own RED proof over BOTH arms of the scope-invariant fail-open -- bogus tag unanchored PASSES /
   anchored FAILS, bogus path unanchored PASSES / anchored FAILS, real tag and paths PASS.
2a. The new SCOPE comment CITES the retirement note by stable name -- `TWO_IG_GUARDS` appears in the
   region between the `[lc9] N1 TABLE SHAPE GATE` marker and the `RAGGED_TABLE_LABEL` declaration --
   rather than restating the three reasons and recreating the drift the task exists to close.
2b. Step 8b's record shows L12 failing against a mutant copy carrying a planted `existsSync` skip and
   passing against the real file, with the real instrument never touched.
3. The archive was byte-identically restored after every move-aside (`git status --porcelain` 0 lines).
4. No comment or `SUMMARY:` string in the checker still claims the archive is in a gate's scope --
   ENFORCED, not merely asserted: L2b requires the two stale phrases gone file-wide (each measured at
   exactly 1 occurrence today) and L11 requires the paragraph's fail-closed clause gone from the region.
   Together with L11's citation leg they form the replace-pair; the addition leg alone would let the
   stale prose ship untouched.
5. The archive header states the post-Task-1 dependency state and names the instrument (D1); records
   that the plugin-wide shape is BLOCKED by N3 and needs an explicit carve-out, with N3 unweakened
   (D2); and cites the verified research record plus the in-production reference instead of claiming
   the mechanism is undesigned (D3).
6. The taxonomy body is byte-identical (540-line slice, equal hashes); the header gained no pipe row;
   the file is ASCII-only and passes email allowlist-inversion.
7. Three `FUT-*` entries exist exactly once each inside the existing `### Later milestones` region,
   each naming the cost of closing it; heading counts unchanged; ROADMAP.md byte-unchanged; neither
   gap fixed.
7a. `260729-lc9-CONTEXT.md` line 294 is annotated SUPERSEDED in place -- naming OD-X0I-1, task
   260729-x0i, the fail-closed reason and the milestone-close relocation -- with the original
   instruction preserved verbatim exactly once, and that artifact's heading counts (6 h2 / 17 h3) and
   pipe-line count (5) unchanged.
8. Final gate passes every leg: battery 123/0, row-guards 49, provenance 3/3, extract-samples 8
   modules, hygiene GREEN, `claude plugin validate .` exit 0, both scope invariants 0 lines behind a
   `git rev-parse` existence anchor, tree clean. Every task's verify carries the two scope-invariant
   legs, so the `<conventions>` claim about them is true as written rather than aspirational.
9. Zero metered spend. No `claude -p` ran.
</success_criteria>

<output>
Create `.planning/quick/260729-x0i-fix-the-archive-header-and-instrument-ho/260729-x0i-SUMMARY.md`
when done. It must record the three commit SHAs, the observed exit code and output line for each RED
leg, and -- explicitly -- that the header edit still needs an unprimed content review, because a green
battery is not acceptance in this work stream.
</output>
