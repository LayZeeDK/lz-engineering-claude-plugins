---
phase: 05-skill-effectiveness-evals
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - .planning/phases/05-skill-effectiveness-evals/EVAL-RESULTS.md
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
autonomous: false
requirements: [EVAL-01, EVAL-02]
user_setup: []

must_haves:
  truths:
    - "EVAL-RESULTS.md summarizes BOTH evals with Pass@k and Pass^k (k=1,3,5,total; cap k at n) per eval and overall, states the with-vs-baseline behavior delta and the trigger held-out accuracy, and assesses both D-06 soft bars (trigger held-out >= ~90% quiet on near-misses; behavior correct-transformation Pass@1 ~= 1.0) (D-05, D-06)"
    - "At most ONE bounded tuning pass touches plugins/: run_loop's best_description is applied to SKILL.md ONLY if it strictly beats the current description on the HELD-OUT set (before/after + scores shown); a coach-procedure/description wording tweak ONLY on a demonstrated coaching defect - NO new reference files, NO re-authoring locked content, NO scope expansion; if the skill already meets the bars, NO tuning is applied (D-07)"
    - "The tuning pass never reopens Phases 1-4 (they remain complete); committed eval artifacts contain no work email (only the public larsbrinknielsen@gmail.com), and bulky raw transcripts stay out of the committed surface (D-06, D-08)"
  artifacts:
    - path: ".planning/phases/05-skill-effectiveness-evals/EVAL-RESULTS.md"
      provides: "The committed EVAL-01 + EVAL-02 results summary with Pass@k/Pass^k, the D-06 bar assessment, and the D-07 decision record (D-05, D-06, D-07, D-08)"
      contains: "Pass@k"
  key_links:
    - from: "05-02 benchmark.json + 05-03 results.json"
      to: "EVAL-RESULTS.md"
      via: "combined Pass@k/Pass^k + held-out accuracy"
      pattern: "Pass@k"
    - from: "05-03 results.json best_description"
      to: "plugins/lz-tdd/skills/lz-tpp/SKILL.md"
      via: "conditional D-07 apply (only if it beats held-out)"
      pattern: "description"
---

<objective>
Combine both eval outcomes into one committed results record and make the single, tightly-bounded D-07 tuning decision. This is the only plan that may write into plugins/ (SKILL.md), and it does so only if the evidence warrants it. Non-blocking: Phases 1-4 stay complete regardless of the outcome.

Purpose: Produce EVAL-RESULTS.md (the phase's validation record - Pass@k/Pass^k for both evals, the D-06 bar assessment) and apply AT MOST one bounded tuning pass (best_description if it beats held-out; a coach-wording tweak only on a real defect), or none if the skill already passes.
Output: EVAL-RESULTS.md (committed) and, conditionally, an edited SKILL.md description/coach-procedure wording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md
@.planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md
@.planning/phases/05-skill-effectiveness-evals/05-02-SUMMARY.md
@.planning/phases/05-skill-effectiveness-evals/05-03-SUMMARY.md

Inputs (must exist): 05-02 lz-tpp-workspace/iteration-1/benchmark.json + behavior Pass@k tables; 05-03 trigger-workspace/results.json (best_description + held-out score) - the trigger-workspace is gitignored but on disk.

Constants:
- PHASE_DIR = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals
- LZTPP = D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp
- Public contact (the ONLY email allowed in committed artifacts): larsbrinknielsen@gmail.com
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author EVAL-RESULTS.md (combined Pass@k/Pass^k + D-06 bar assessment) + email allowlist scan</name>
  <files>
    .planning/phases/05-skill-effectiveness-evals/EVAL-RESULTS.md
  </files>
  <read_first>
    - .planning/phases/05-skill-effectiveness-evals/05-02-SUMMARY.md (behavior Pass@k/Pass^k + saturation flags + with-vs-baseline delta)
    - .planning/phases/05-skill-effectiveness-evals/05-03-SUMMARY.md (trigger held-out accuracy + per-query table + best_description)
    - .planning/phases/05-skill-effectiveness-evals/05-RESEARCH.md lines 380-384 (Pass@k/Pass^k contract) and 05-PATTERNS.md lines 198-224 (results-doc structure: Metric x config x Delta table, ASCII +/- not the Unicode sign)
    - .planning/phases/05-skill-effectiveness-evals/05-CONTEXT.md D-06 (soft bars) + D-08 (commit sets + summary + benchmark; drop raw transcripts)
  </read_first>
  <action>
    Write EVAL-RESULTS.md under PHASE_DIR combining both evals. Sections: (1) EVAL-01 trigger - held-out accuracy, per-query should-trigger/near-miss table, original-vs-best_description, assessed against the ~90% held-out soft bar with near-misses quiet. (2) EVAL-02 behavior - the with_skill vs without_skill Metric/Delta table, per-scenario correct-transformation result, saturation flags, assessed against the Pass@1 ~= 1.0 soft bar. (3) Pass@k AND Pass^k tables (k=1,3,5,total; cap k at n) per eval AND overall, for both configs, skipping trivial rows. (4) D-06 verdict: which bars are met/missed and whether a D-07 tuning pass is warranted (this is the input to Task 2). ASCII only: `->` arrows, straight quotes, `+/-` not the Unicode sign, no emojis/em-dashes. Then run the public-repo email allowlist gate on the committed eval artifacts: scan PHASE_DIR (committed files) and confirm the ONLY email string present is larsbrinknielsen@gmail.com and no other address appears (MEMORY public-repo-work-email-allowlist convention). Do NOT commit the bulky raw transcripts (they are gitignored under outputs/ + trigger-workspace/ per 05-01).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/.planning/phases/05-skill-effectiveness-evals/EVAL-RESULTS.md'; const t=fs.readFileSync(p,'utf8'); const hasK=/Pass@k/.test(t)&&/Pass\^k/.test(t)&&/k\s*=?\s*1/.test(t)&&/EVAL-01/i.test(t)&&/EVAL-02/i.test(t); const emails=(t.match(/[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+/g)||[]).filter(e=>e!=='larsbrinknielsen@gmail.com'); const asciiOk=/[^\x00-\x7F]/.test(t)===false; if(!hasK||emails.length>0||!asciiOk){console.error('FAIL hasK',hasK,'strayEmails',emails,'asciiOnly',asciiOk);process.exit(1);} console.log('OK EVAL-RESULTS combined + email-clean + ASCII');"</automated>
  </verify>
  <acceptance_criteria>
    - EVAL-RESULTS.md covers BOTH EVAL-01 and EVAL-02, with Pass@k AND Pass^k tables (k=1,3,5,total) per eval and overall, and a stated D-06 verdict per bar.
    - The only email in any committed PHASE_DIR artifact is larsbrinknielsen@gmail.com; the automated check finds no stray address.
    - No non-ASCII byte in EVAL-RESULTS.md (arrows are `->`, sign is `+/-`).
  </acceptance_criteria>
  <done>EVAL-RESULTS.md exists, combines both evals with Pass@k/Pass^k and the D-06 verdict, passes the email allowlist + ASCII checks.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [DECISION] Apply at most one bounded D-07 tuning pass to SKILL.md (or none)</name>
  <files>plugins/lz-tdd/skills/lz-tpp/SKILL.md</files>
  <action>
    Consult the EVAL-RESULTS.md D-06 verdict (Task 1) and 05-03 trigger-workspace/results.json (best_description + held-out scores). Determine which of the three options applies and PAUSE for the user to confirm the choice with the before/after evidence, then apply AT MOST one bounded change to plugins/lz-tdd/skills/lz-tpp/SKILL.md (or none). If applying best_description: edit ONLY the SKILL.md description frontmatter (keep it <= 1024 chars); optionally run a one-shot run_eval.py under WSL2 on the held-out queries before/after as a clean regression datapoint. If applying a coach-wording tweak: edit ONLY the coach-procedure/description prose to fix a demonstrated defect. HARD limits (D-07): at most one pass, NO new reference files, NO re-authoring locked content (transformations.md + the worked examples stay frozen), NO scope expansion, NO self-feeding re-eval beyond --max-iterations 5. If both D-06 bars are met, apply NO tuning and record that the evals stand as a validation record. Whatever is chosen, Phases 1-4 remain complete.
  </action>
  <decision>Whether and how to apply the single bounded tuning pass to plugins/lz-tdd/skills/lz-tpp/SKILL.md, given the EVAL-RESULTS.md D-06 verdict. This is the ONLY write into plugins/ and edits shipped code, so it is a human-gated judgment call.</decision>
  <context>
    D-07 permits AT MOST ONE bounded pass. Triggering: apply run_loop's best_description to the SKILL.md `description` frontmatter ONLY if best_test_score (held-out) STRICTLY beats iteration-1's held-out score (05-03 results.json; iteration 1 evaluates the CURRENT description, so best_description may equal the original - then no change). Behavior: a bounded wording tweak to the SKILL.md coach procedure/description ONLY if a scenario in 05-02 exposed a REAL coaching defect. HARD limits: NO new reference files, NO re-authoring locked content (transformations.md + the worked examples stay frozen), NO scope expansion, NO self-feeding re-eval loops beyond the tool's --max-iterations 5. If both D-06 bars are met, apply NO tuning - the evals stand as a validation record. Whatever is chosen, Phases 1-4 remain complete (non-blocking).

    Read first: 05-RESEARCH.md lines 243-277 (D-07 application rule + results.json shape), 05-CONTEXT.md D-06/D-07, EVAL-RESULTS.md (this plan's Task 1 verdict), plugins/lz-tdd/skills/lz-tpp/SKILL.md lines 3-12 (current description) + 42-66 (coach procedure), and 05-03 trigger-workspace/results.json (best_description + scores).
  </context>
  <options>
    <option id="apply-description">
      <name>Apply best_description to SKILL.md frontmatter</name>
      <pros>Improves triggering when best_test_score strictly beats the current held-out score; overfitting-guarded (held-out selection).</pros>
      <cons>Edits shipped code; only justified if the held-out gain is real. Optionally run a one-shot run_eval.py (WSL2) on the held-out queries before/after as a clean regression datapoint (RESEARCH open Q3).</cons>
    </option>
    <option id="apply-coach-wording">
      <name>Apply a bounded coach-procedure/description wording tweak</name>
      <pros>Fixes a demonstrated coaching defect surfaced by a 05-02 scenario (e.g. the coach failed to name the correct transformation or drove instead of coaching).</pros>
      <cons>Only on a REAL defect; must stay within the existing structure - no new files, no locked-content rewrite, no scope expansion.</cons>
    </option>
    <option id="no-tuning">
      <name>Apply no tuning (evals stand as a validation record)</name>
      <pros>Correct when both D-06 bars are met; the shipped skill is validated as-is; smallest change.</pros>
      <cons>None - this is the expected outcome if the skill already passes.</cons>
    </option>
  </options>
  <verify>
    <automated>node -e "const fs=require('fs'),cp=require('child_process'); const sk='D:/projects/github/LayZeeDK/lz-engineering-claude-plugins/plugins/lz-tdd/skills/lz-tpp/SKILL.md'; const t=fs.readFileSync(sk,'utf8'); const refs=(t.match(/references\//g)||[]).length; const asciiOk=/[^\x00-\x7F]/.test(t)===false; const desc=(t.match(/description:[\s\S]*?---/)||[''])[0]; const descLen=desc.length; const changed=cp.execSync('git -C D:/projects/github/LayZeeDK/lz-engineering-claude-plugins status --porcelain plugins/lz-tdd/skills/lz-tpp/',{encoding:'utf8'}); const onlySkill=changed.split('\\n').filter(Boolean).every(l=>l.includes('SKILL.md')); if(!asciiOk||descLen>1100||!onlySkill){console.error('FAIL asciiOnly',asciiOk,'descLen',descLen,'onlySkillChanged',onlySkill,'changes',changed);process.exit(1);} console.log('OK tuning bounded: only SKILL.md touched under lz-tpp, description <=1024ish, ASCII-only, references count',refs);"</automated>
  </verify>
  <resume-signal>Select: apply-description, apply-coach-wording, or no-tuning (with the before/after evidence). If applying, confirm the SKILL.md edit stays within D-07 limits.</resume-signal>
  <acceptance_criteria>
    - At most ONE tuning action is taken; the chosen option and its evidence (held-out before/after scores, or the specific coaching defect) are recorded in EVAL-RESULTS.md / SUMMARY.
    - If SKILL.md was edited: only the description frontmatter and/or coach-procedure wording changed under plugins/lz-tdd/skills/lz-tpp/ (no new files, no change to transformations.md / worked examples); the description stays <= 1024 chars; the file is ASCII-only; git status shows only SKILL.md modified under that dir.
    - If no tuning: EVAL-RESULTS.md records that both bars are met and the evals stand as a validation record.
  </acceptance_criteria>
  <done>Exactly one of {best_description applied, coach-wording tweaked, no tuning} is recorded with evidence; any SKILL.md edit is within D-07 bounds and Phases 1-4 remain complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval outputs -> committed public results | EVAL-RESULTS.md is committed and public; must be email-clean and ASCII-only |
| tuning decision -> shipped skill (plugins/) | The only write into the shipped surface; a human gate authorizes and bounds it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information disclosure | EVAL-RESULTS.md + committed artifacts | mitigate | Task 1 runs the public-repo email allowlist gate (only larsbrinknielsen@gmail.com permitted); raw transcripts stay gitignored (D-08). Stage files by name, never `git add .`. |
| T-05-04 | Tampering (scope integrity) | plugins/lz-tdd/skills/lz-tpp/SKILL.md | mitigate | The tuning is a blocking human-gated decision bounded by D-07: at most one pass, no new files, no locked-content rewrite; the automated verify confirms only SKILL.md changed under the skill dir and the description stays within the char limit. |
| T-05-SC | Tampering (supply chain) | this plan | accept | No installs; only doc authoring + a bounded SKILL.md edit. |
</threat_model>

<verification>
- EVAL-RESULTS.md combines both evals with Pass@k/Pass^k and a D-06 verdict; email allowlist + ASCII checks pass (Task 1).
- The tuning decision applies at most one bounded change (or none); if SKILL.md changed, only the description/coach wording moved and it stays <= 1024 chars, ASCII-only, no new files (Task 2).
- Phases 1-4 remain marked complete (non-blocking); no locked reference file changed (`git status` shows transformations.md / worked examples untouched).
</verification>

<success_criteria>
- One committed EVAL-RESULTS.md records the full EVAL-01 + EVAL-02 outcome with Pass@k/Pass^k and the D-06 bar assessment.
- The single D-07 decision is made and recorded: best_description applied (only if it beat held-out), a bounded coach-wording tweak (only on a real defect), or no tuning (skill already passes).
- The phase closes non-blocking: shipped skill either validated as-is or improved by one bounded pass; Phases 1-4 untouched.
</success_criteria>

<output>
Create `.planning/phases/05-skill-effectiveness-evals/05-04-SUMMARY.md` when done, recording: the combined Pass@k/Pass^k headline numbers, the D-06 verdict per bar, the D-07 decision + evidence (before/after held-out scores or the coaching defect), whether SKILL.md was edited (and the exact diff scope), and confirmation the email allowlist passed and Phases 1-4 remain complete.
</output>
