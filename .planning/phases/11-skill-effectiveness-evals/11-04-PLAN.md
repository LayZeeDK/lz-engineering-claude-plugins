---
phase: 11-skill-effectiveness-evals
plan: 04
type: execute
wave: 2
depends_on: [11-01, 11-02, 11-03]
files_modified: []
autonomous: false
requirements: [EVL-01, EVL-02]
user_setup: []

must_haves:
  truths:
    - "**D-10:** [HARD GATE] The phase HALTS before running anything. This final task presents the exact ready-to-run commands for EVL-01 (the native trigger probe) and EVL-02 (the behavior benchmark) and waits for EXPLICIT user approval; NO eval, trigger probe, behavior subagent benchmark, or description-optimization loop is executed in-phase. Build-time readiness is verified with selfchecks / lints ONLY (node grade-run.mjs --selfcheck, node merge-judge.mjs --selfcheck, node check-evals.mjs) -- none of which spend claude -p tokens."
    - "The presented EVL-01 command uses the LOCKED run config: python -m scripts.run_eval --eval-set ../../evals/trigger-eval.json --skill-path <repo>/plugins/lz-tdd/skills/lz-refactor --model claude-opus-4-8 --runs-per-query 3 --num-workers 1, run from tools/skill-creator-eval/, with PONYTAIL_DEFAULT_MODE=off, --strict-mcp-config, and --setting-sources project (serial; Ponytail off; MCP stripped; user plugins dropped so neither real sibling skill steals the probe)."
    - "The presented EVL-02 pipeline is: spawn UNNAMED fire-and-forget subagents per scenario x config x run (>= 3 each) in isolated scratch cwds -> grade-run.mjs -> grader agent -> merge-judge.mjs --merge -> merge-judge.mjs --verify <iteration-dir> -> python -m scripts.aggregate_benchmark iteration-1 --skill-name lz-refactor -> eval-viewer/generate_review.py."
    - "**D-06:** The presented run reminds the operator to run each trigger query and each behavior scenario >= 3x, compute and report Pass@k and Pass^k (k = 1, 3, 5, and total run count) per eval and overall, flag saturated assertions (Pass@1 = 1.0 for both configs), WAIT for ALL completion notifications before grading (persist timing.json as each arrives), and include >= 1 unbiased from-scratch reviewer."
    - "**D-08:** At most ONE bounded tuning pass may follow the run and ONLY if a soft bar is missed -- a tuned description applied to SKILL.md frontmatter only if it beats the current one on a held-out trigger set (show before/after + scores), or a bounded coach-wording tweak only on a demonstrated routing defect; NO new reference files, NO re-authoring LOCKED catalog content, NO scope expansion; this is the ONLY write-back into plugins/ and it never reopens Phases 6-10 (non-blocking)."
  artifacts: []
  key_links:
    - from: ".planning/phases/11-skill-effectiveness-evals (present-and-halt)"
      to: ".claude/skills/lz-refactor-workspace (harness + trigger-eval.json + evals.json + grade-run.mjs)"
      via: "readiness selfchecks + the presented ready-to-run commands (no execution)"
      pattern: "selfcheck"
---

<objective>
Confirm all eval assets are built and READY, present the exact ready-to-run EVL-01 + EVL-02 commands with the locked run config, and HALT for explicit user approval. This is the D-10 hard-gate boundary: everything up to the run is done; the run itself is user-gated.

Purpose: Running any eval spends claude -p tokens and is a standing user-gated action. This task proves readiness without spending tokens, then stops.
Output: A presented command block + a blocking checkpoint. No eval is executed; no file under plugins/ or .planning/ is written (a D-08 tuning pass, if any, happens only AFTER approval and only if a bar is missed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skill-effectiveness-evals/11-RESEARCH.md

Built assets to confirm ready (from 11-01/02/03):
- .claude/skills/lz-refactor-workspace/tools/skill-creator-eval/ (vendored probe)
- .claude/skills/lz-refactor-workspace/merge-judge.mjs, eval-status.mjs, run-spec-chunks.mjs
- .claude/skills/lz-refactor-workspace/evals/trigger-eval.json, evals/evals.json
- .claude/skills/lz-refactor-workspace/grade-run.mjs, check-evals.mjs
- .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md (template, numbers blank)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify readiness, present ready-to-run commands, HALT for approval</name>
  <read_first>
    - 11-RESEARCH.md "Build / Run split (D-10 HARD GATE)" (the exact BUILD vs RUN enumeration + the locked EVL-01 command string and the EVL-02 pipeline chain)
    - .claude/skills/lz-refactor-workspace/EVAL-RESULTS.md (the authoritative run-config caveat + the Reproduce/resume command chain to echo)
    - 11-CONTEXT.md D-06, D-08, D-10 (sampling/metrics, tuning discipline, and the hard gate) -- do not restate, just honor
  </read_first>
  <what-built>
    Plans 11-01/02/03 built the full eval rig under .claude/skills/lz-refactor-workspace/: the vendored native-fixed trigger probe, the orchestration utils (merge-judge / eval-status / run-spec-chunks), the two eval-data files (trigger-eval.json 10+10, evals.json >= 8 leaf-sourced scenarios), the rewritten deterministic grader (grade-run.mjs with the D-04-RUBRIC name+layer matcher + RUBRICS + name->layer lookup), the check-evals.mjs lint, and the EVAL-RESULTS.md template (numbers blank).
  </what-built>
  <action>Run the three build-time readiness checks ONLY (they do not spend claude -p tokens): node grade-run.mjs --selfcheck, node merge-judge.mjs --selfcheck, node check-evals.mjs -- all must exit 0. Then PRESENT to the user, verbatim, the two ready-to-run command blocks: (EVL-01) the native trigger probe from tools/skill-creator-eval/ with --eval-set ../../evals/trigger-eval.json --skill-path <repo>/plugins/lz-tdd/skills/lz-refactor --model claude-opus-4-8 --runs-per-query 3 --num-workers 1, plus PONYTAIL_DEFAULT_MODE=off, --strict-mcp-config, --setting-sources project; and (EVL-02) the behavior pipeline -- unnamed fire-and-forget subagents per scenario x config x run (>= 3) in isolated scratch cwds, then grade-run.mjs -> grader agent -> merge-judge.mjs --merge -> merge-judge.mjs --verify <iteration-dir> -> python -m scripts.aggregate_benchmark iteration-1 --skill-name lz-refactor -> eval-viewer/generate_review.py, then compute Pass@k/Pass^k (k=1,3,5,total), fill EVAL-RESULTS.md, and run >= 1 unbiased from-scratch reviewer. State explicitly that NOTHING is being run and that execution requires the user's explicit approval (D-10). Do NOT execute run_eval, any subagent benchmark, aggregate_benchmark, run_loop/improve_description, or any claude -p invocation. Do NOT write into plugins/ (a D-08 tuning pass is post-approval and only if a soft bar is missed).</action>
  <how-to-verify>
    1. Confirm the three readiness checks exit 0: `node grade-run.mjs --selfcheck` (SELFCHECK OK), `node merge-judge.mjs --selfcheck` (SELFCHECK OK), `node check-evals.mjs` (OK), run from .claude/skills/lz-refactor-workspace/.
    2. Read the two presented command blocks and confirm the locked config is present (--num-workers 1, PONYTAIL_DEFAULT_MODE=off, --strict-mcp-config, --setting-sources project, model claude-opus-4-8, >= 3 runs).
    3. Confirm no eval / probe / benchmark / optimization loop was executed and no file under plugins/ was changed.
  </how-to-verify>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/grade-run.mjs --selfcheck && node .claude/skills/lz-refactor-workspace/merge-judge.mjs --selfcheck && node .claude/skills/lz-refactor-workspace/check-evals.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All three readiness checks exit 0 (grade-run --selfcheck, merge-judge --selfcheck, check-evals).
    - Both ready-to-run command blocks are presented verbatim with the exact locked run config for EVL-01 and the full EVL-02 pipeline.
    - The task explicitly states it is the halt point and that running requires explicit user approval (D-10).
    - NO eval / trigger probe / behavior subagent / aggregate / optimization loop is executed; NO write into plugins/ or .planning/.
  </acceptance_criteria>
  <resume-signal>User types "approved" (or "run the evals") to authorize the gated EVL-01 + EVL-02 run out-of-phase; or describes changes to the eval assets first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readiness checks -> local Node scripts | Selfchecks/lints read local files only; no network, no claude -p |
| gated eval RUN -> claude -p (out of phase) | The actual probe/benchmark spends tokens; guarded behind the blocking human checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Elevation of Privilege | running an eval spends claude -p tokens (standing user-gated action) | mitigate | D-10 hard gate: present commands + HALT (autonomous:false, gate blocking) for explicit user approval; the automated readiness check runs ONLY selfchecks/lints (no eval, no claude -p). |
| T-11-08 | Information Disclosure | a post-approval D-08 tuning write-back into plugins/lz-tdd/skills/lz-refactor/SKILL.md | accept | Tuning is out-of-phase, post-approval, bounded, and re-scanned (ASCII-only + allowlist-inversion email scan) at that point; only if a soft bar is missed. Not performed in this plan. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. Low severity, no ASVS L1 install surface. |
</threat_model>

<verification>
- The three readiness selfchecks/lints exit 0 (no claude -p spend).
- The presented command blocks carry the exact locked run config for both evals.
- Nothing is executed; the checkpoint blocks for explicit user approval (D-10).
</verification>

<success_criteria>
- Readiness proven via selfchecks/lints only; ready-to-run EVL-01 + EVL-02 commands presented verbatim with the locked config.
- The phase halts for explicit user approval before any run; no write into plugins/ or .planning/.
</success_criteria>

<output>
Create `.planning/phases/11-skill-effectiveness-evals/11-04-SUMMARY.md` when done (recording the readiness-check results and that the phase halted for approval).
</output>
