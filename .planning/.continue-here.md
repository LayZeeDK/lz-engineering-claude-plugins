---
context: default
task: milestone-close-inherited
total_tasks: 2
status: ready
last_updated: 2026-07-16
---

# Where we are

Quick task 260716-oby (Loops/pipeline few-shot TS examples) is COMPLETE and RESOLVED. Examples SHIPPED
on teaching merit (ad56c21). The held-out recognition eval was NULL (0/3, eval 30ceb65).

THEN a design discussion + web research led to a forcing-function diagnostic (user-approved, k=5), which
produced a POSITIVE, conclusion-changing result:

- **Enum loop-audit directive in the PROMPT flips the loop-to-pipeline miss: idiom 0/3 (control) -> 5/5**,
  all `tsc --strict` CLEAN + behavior-preserving + discriminating (left the already-pipeline `not` site).
- CORRECTION to the prior record: the miss is a DISCRETION/SALIENCE gap, NOT a judgment ceiling. PASSIVE
  skill content (prose/cues/few-shot examples) is null across 5 probes; an ACTIVE enumeration
  forcing-function works (prompt-level, n=1). See heldout-enum/RESULTS-enum.md.

## Corrected anti-pattern (supersedes the old "stop editing skill content" note)
- Passive skill-content edits to chase accumulation-loop recognition ARE null -- do not repeat those.
- But an ACTIVE forcing-function (a loop-audit PROCEDURE step) is a different, working lever. Do NOT
  treat the loop-to-pipeline miss as an unfixable model ceiling.

## Remaining
1. Inherited milestone close (not blocked by the below): user runs `/reload-plugins` (make ad56c21 live),
   then `/gsd-audit-milestone lz-tdd@0.0.2` then `/gsd-complete-milestone lz-tdd@0.0.2`.
2. CANDIDATE (unrun, needs design + review + user go): a SKILL-LEVEL forcing-function probe -- embed a
   loop-audit step in the apply/sweep procedure; test (a) it reproduces the lift WITHOUT the prompt
   directive (skill alone), and (b) precision on targets where not every loop should convert (guard
   against overcorrection). Gate metered runs on explicit approval ([[eval-run-approval-gate]]).
