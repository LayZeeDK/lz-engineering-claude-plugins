---
phase: 10-distribution-hygiene
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/SKILL.md
  - plugins/lz-tdd/skills/lz-tpp/SKILL.md
autonomous: true
requirements: [DST-02]

must_haves:
  truths:
    - "The first-party CLI gate passes on the FINAL tree: claude plugin validate . exits 0 AND claude plugin validate . --strict exits 0 (both are regression gates -- verified exit 0 pre-edit; re-run after the manifest + README + CHANGELOG landed)"
    - "The full workspace battery is GREEN on the final tree: npm run check exits 0 (all 10 checkers incl. the widened+hardened check-hygiene from 10-01) and npm run typecheck exits 0 (extract-samples.mjs tsc --strict over every fenced sample)"
    - "The full-tree work-email guard is clean post-commit: allowlist-inversion across the whole repo including .planning/ leaves only the approved public gmail (empty remainder); this plan and its artifacts encode no forbidden value"
    - "The plugin-dev plugin-validator agent PASSes on the lz-tdd plugin (structure / manifest / security / path-traversal), and the plugin-dev skill-reviewer agent PASSes on BOTH skills (lz-refactor AND lz-tpp) on the final tree -- 09-04's skill-reviewer PASS was pre-README/pre-bump and never covered lz-tpp"
    - "Findings are triaged per D-13: FIX-in-phase = structural/manifest errors, security/path-traversal, anything breaking install or /lz-tdd:lz-refactor or /lz-tdd:lz-tpp invocation, ASCII violations, broken reference links, malformed frontmatter, factual inaccuracies in README/CHANGELOG/LICENSE/manifests, and any DST-04 verbatim hit; DEFER-to-Phase-11 = description-triggering effectiveness, over/under-triggering, and coach-routing accuracy (recorded, not acted on -- the known >500-char-description and body-too-short flags on both skills are DEFER items; the plugin-root README/LICENSE-missing note is minor/by-design)"
    - "Any edit forced onto either SKILL.md by skill-reviewer or the DST-04 sweep gets its own subagent review before acceptance (D-14); README/CHANGELOG/manifest/checker edits do NOT need such review and none require /reload-plugins"
  artifacts:
    - path: ".planning/phases/10-distribution-hygiene/10-04-SUMMARY.md"
      provides: "the recorded gate results, agent verdicts, and D-13 triage (FIX vs DEFER) for the public-ship gate"
      contains: "plugin-validator"
  key_links:
    - from: "claude plugin validate . --strict"
      to: "plugins/lz-tdd/.claude-plugin/plugin.json + .claude-plugin/marketplace.json"
      via: "the first-party hard gate validates the edited manifests + skill structure on the final tree"
      pattern: "plugin validate"
  decisions: |
    - **D-12:** run all three required gates on the FINAL tree -- (1) claude plugin validate . and --strict (HARD, regression gates); (2) the plugin-dev plugin-validator agent (not run this milestone); (3) the plugin-dev skill-reviewer agent on BOTH skills -- plus npm run check + npm run typecheck GREEN
    - **D-13:** triage findings -- FIX ship-blockers in Phase 10 (errors, security/path-traversal, broken install/invocation, ASCII violations, broken links, malformed frontmatter, factual inaccuracies, a DST-04 verbatim hit); DEFER to Phase 11 the description-triggering / over-under-triggering / coach-routing-accuracy findings (record, do not act -- do NOT shorten descriptions or inflate SKILL.md bodies)
    - **D-14:** a skill-reviewer- or sweep-forced edit to either SKILL.md needs its own subagent review before acceptance (memory agent-skill-instruction-changes-need-review); README/CHANGELOG/manifest/checker edits do not, and none require /reload-plugins
    - **D-17:** micro-decision resolved here -- the order the three D-12 gates run in is Claude's discretion (recommended: deterministic gates first -- validate + --strict + npm run check + typecheck + the full-tree work-email guard -- then the two read-only review agents, so agents review a machine-clean tree)
---

<objective>
Run the DST-02 public-ship gate on the FINAL tree after the instrument (10-01), the content edits
(10-02), and the DST-04 sweep (10-03) have all landed: the first-party CLI validation, the full checker
battery + typecheck, the full-tree work-email guard, and the two plugin-dev review agents on both skills.
Triage every finding per D-13 (fix ship-blockers now; record and defer empirical triggering/routing
findings to Phase 11), and route any forced SKILL.md edit through the D-14 subagent review.

Purpose: this is the last gating step before the public 0.0.2 ship. Phase 11 (evals) is explicitly
non-blocking and runs after. The gates ARE the tests for this docs/config phase -- there is no unit-test
framework.

Output: a machine-clean, agent-PASS final tree and a recorded 10-04-SUMMARY.md capturing every gate
result, agent verdict, and triage decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-distribution-hygiene/10-CONTEXT.md
@.planning/phases/10-distribution-hygiene/10-RESEARCH.md
@.planning/phases/04-distribution-hygiene/04-RESEARCH.md

RESEARCH "Running the review agents" says plugin-validator + skill-reviewer are read-only plugin-dev
Task-tool subagents (not CLI commands); only claude plugin validate gives an exit code. Pitfalls 4 and 5
name the expected DEFER findings (both descriptions >500 chars; body too short; plugin-root README/LICENSE
"missing"). The work-email allowlist-inversion discipline (assert only the approved public gmail is present; never
write the forbidden value as a needle, the needle is itself a leak) is documented in 04-RESEARCH.md -- do NOT re-derive it and do NOT
write the work-email literal anywhere.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the deterministic gates on the final tree -- validate --strict, npm run check, npm run typecheck, full-tree work-email guard (D-12, D-17 order)</name>
  <files>(verification only; edits a file ONLY on a D-13 ship-blocker fix -- e.g. plugins/lz-tdd/.claude-plugin/plugin.json, README.md, or a SKILL.md via D-14)</files>
  <read_first>
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md ("Hard gate: first-party CLI validation"; "Battery + typecheck"; "Full-tree work-email post-commit guard"; A1 -- validate exits 0 today, a regression would be a D-13 FIX-now)
    - .claude/skills/lz-refactor-workspace/package.json (the check + typecheck scripts -- the 10-checker chain and extract-samples.mjs)
    - .planning/phases/04-distribution-hygiene/04-RESEARCH.md (the work-email allowlist-inversion guard -- assert only the approved gmail is present; do NOT write the work-email literal)
  </read_first>
  <action>
    On the final tree (after 10-01/10-02/10-03), run the deterministic gates in this order (D-17): first
    the full-tree work-email guard -- allowlist-inversion across the whole repo (enumerate email-shaped tokens,
    subtract the approved public gmail, expect an empty remainder); NEVER type the work-email literal or its
    domain as a needle. Then claude plugin validate . (expect exit 0) and claude plugin validate . --strict
    (expect exit 0). Then, from .claude/skills/lz-refactor-workspace, npm run check (all 10 checkers incl.
    the widened+hardened check-hygiene, expect exit 0) and npm run typecheck (tsc --strict over every
    fenced sample, expect exit 0). If any gate fails, triage per D-13: a structural/manifest error,
    security/path-traversal, broken install/invocation, ASCII violation, broken link, malformed
    frontmatter, factual inaccuracy, or a DST-04 verbatim hit is a ship-blocker -- FIX it in place (edit
    the offending file, re-run the gate) and, if the fix lands on either SKILL.md, route it through the
    D-14 subagent review before accepting. Record each gate's result for the SUMMARY.
  </action>
  <acceptance_criteria>
    - claude plugin validate . exits 0 AND claude plugin validate . --strict exits 0 on the final tree
    - From .claude/skills/lz-refactor-workspace: npm run check exits 0 and npm run typecheck exits 0
    - The full-tree escaped-needle work-email git grep returns no match (absent across the whole repo including .planning/)
    - Any gate failure was triaged per D-13 and any ship-blocker fixed in place with the gate re-run GREEN; any SKILL.md fix went through the D-14 review
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git grep -hoiE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' | sort -u | rg -iv 'larsbrinknielsen@gmail\.com' | rg -iv 'lz-tdd@')" && claude plugin validate . && claude plugin validate . --strict && cd .claude/skills/lz-refactor-workspace && npm run check && npm run typecheck
  </verify>
  <done>The final tree passes claude plugin validate . and --strict, npm run check (10 checkers) and npm run typecheck, and the full-tree work-email guard is clean; any ship-blocker was fixed and re-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Run the plugin-dev review agents on both skills, triage findings (D-12, D-13, D-14)</name>
  <files>(verification only; edits plugins/lz-tdd/skills/lz-refactor/SKILL.md or plugins/lz-tdd/skills/lz-tpp/SKILL.md ONLY on a D-13 ship-blocker fix, via the D-14 review)</files>
  <read_first>
    - .planning/phases/10-distribution-hygiene/10-RESEARCH.md ("Running the review agents"; Pitfall 4 -- the >500-char-description + body-too-short DEFER items on both skills; Pitfall 5 -- the plugin-root README/LICENSE "missing" minor/by-design note)
    - .planning/phases/10-distribution-hygiene/10-CONTEXT.md (D-13 triage rule -- what is a ship-blocker vs a Phase-11 defer; D-14 skill-edit review rule)
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md and plugins/lz-tdd/skills/lz-tpp/SKILL.md (the two skills under review -- read before acting on any finding)
  </read_first>
  <action>
    Invoke the plugin-dev plugin-validator agent (Task tool) on the lz-tdd plugin -- structure / manifest /
    security / path-traversal. Then invoke the plugin-dev skill-reviewer agent on BOTH skills: once on
    lz-refactor, once on lz-tpp. Collect every finding and triage per D-13. FIX-in-phase: structural /
    manifest errors, security / path-traversal, anything breaking install or /lz-tdd:lz-refactor or
    /lz-tdd:lz-tpp invocation, ASCII violations, broken reference links, malformed frontmatter, factual
    inaccuracies in README/CHANGELOG/LICENSE/manifests, and any DST-04 verbatim hit -- fix in place and
    re-run the Task-1 deterministic gates. DEFER-to-Phase-11 (record, do NOT act): any finding about
    description-triggering effectiveness, over/under-triggering, or coach-routing accuracy -- specifically,
    do NOT shorten either description or inflate either SKILL.md body in response to the expected
    ">500 char description" / "body too short" flags (that pulls Phase-11 empirical work forward and fights
    the intentional lean/progressive-disclosure design). Triage the plugin-root README/LICENSE-"missing"
    note as minor/by-design (they live at the repo/marketplace root, correct for a single-plugin
    marketplace). If any FIX lands on either SKILL.md, route that edit through its own subagent review
    before accepting it (D-14) -- README/CHANGELOG/manifest/checker fixes do not need this. Record each
    agent's PASS/NEEDS-IMPROVEMENT verdict and the full FIX/DEFER triage in the SUMMARY.
  </action>
  <acceptance_criteria>
    - The plugin-validator agent returned a verdict on the lz-tdd plugin and the skill-reviewer agent returned a verdict on BOTH lz-refactor and lz-tpp; all are PASS (after D-13-triaged fixes) on the final tree
    - Every finding is triaged in the SUMMARY as FIX (applied in phase) or DEFER (recorded for Phase 11); no description was shortened and no SKILL.md body inflated for a triggering/routing finding
    - Any SKILL.md fix went through a D-14 subagent review before acceptance; no SKILL.md was edited without it
    - After any fixes, claude plugin validate . --strict exits 0 and npm run check exits 0 (re-verified)
  </acceptance_criteria>
  <verify>
    <automated>claude plugin validate . --strict && cd .claude/skills/lz-refactor-workspace && npm run check</automated>
  </verify>
  <done>plugin-validator PASSes on lz-tdd and skill-reviewer PASSes on both skills on the final tree; every finding is triaged FIX-or-DEFER (triggering/routing findings recorded for Phase 11, not acted on); any SKILL.md fix passed a D-14 review; the deterministic gates re-verify GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The final public-ship tree crosses into the published marketplace. The first-party validator + agents
verify structure/manifest safety; the deterministic gates + full-tree guard verify hygiene; residual
verification of the earlier plans' threats happens here on the assembled tree.

| Boundary | Description |
|----------|-------------|
| plugin manifest source -> /plugin marketplace add install | relative ./ source only; no ../ or absolute path traversal |
| assembled tree -> published public repo | ASCII-only, work-email-free (incl. .planning/), no verbatim book prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-STRUCT | Tampering | manifest path-traversal / malformed structure breaking install | mitigate | claude plugin validate . --strict (HARD gate) + the plugin-dev plugin-validator agent (structure/manifest/security/path-traversal); relative ./plugins/lz-tdd source is already compliant |
| T-10-EM | Information Disclosure | work/PII email anywhere in the repo incl. .planning/ | mitigate | Full-tree escaped-needle git grep post-commit (D-11); the widened check-hygiene (a)/(b) already gates the shippable surface |
| T-10-IP | Information Disclosure / IP | residual verbatim book prose in the shipped tree | mitigate | The hardened check-hygiene axis (c) in npm run check + the 10-03 clean-room sweep; a DST-04 verbatim hit surfaced by any gate is a D-13 ship-blocker FIX |
| T-10-SKILLEDIT | Tampering | a skill-instruction edit slipping in unreviewed | mitigate | Any SKILL.md fix routes through a D-14 subagent review before acceptance |
</threat_model>

<verification>
- claude plugin validate . and --strict exit 0 on the final tree; npm run check + npm run typecheck exit 0.
- The full-tree escaped-needle work-email git grep returns absent (rc=1).
- plugin-validator PASSes on lz-tdd; skill-reviewer PASSes on lz-refactor AND lz-tpp.
- Findings triaged FIX-or-DEFER in the SUMMARY; no description shortened / body inflated; any SKILL.md fix D-14-reviewed.
</verification>

<success_criteria>
The final 0.0.2 tree passes the first-party CLI (plain + --strict), the full checker battery + typecheck,
the full-tree work-email guard, and both plugin-dev review agents on both skills; every finding is
triaged per D-13 with empirical triggering/routing findings deferred to Phase 11 and any SKILL.md edit
reviewed per D-14 -- the public ship is gate-clean.
</success_criteria>

<output>
Create `.planning/phases/10-distribution-hygiene/10-04-SUMMARY.md` when done.
</output>
