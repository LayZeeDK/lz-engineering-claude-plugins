---
context: quick-task
quick_task: 260802-j03
supersedes: 260801-sc9
phase: 21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i
task: 3
total_tasks: 3
status: quick_task_complete_pilot_paused_midway
last_updated: 2026-08-02T13:31:49.879Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Each was discovered through an ACTUAL FAILURE this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **THE RESULTS TREE IS MIXED RIGHT NOW.** `results/apply/invoke_skill/a1/` holds one FRESH capture with a STALE grade (run-1: meta 15:29, grade 01:28) and two ROUND-1 captures (runs 2-3). Reading any of it as pilot data yields a wrong N and a wrong GO/NO-GO. Mitigation: re-run all three with `--force`, re-grade all three, and assert every meta AND every grade carries a post-resume timestamp before computing anything. Round-1 is archived intact at `D:/.lz-red-archive/2026-08-01-d12-round-1/`.
- [ ] CONSTRAINT: **A TIE IS NOT A NULL UNTIL THE DOSE IS PROVEN.** Round 1 tied on six dimensions and I reported it as an answer. The treatment arm had never opened the taxonomy in any of its six runs. Mitigation: before reporting any arm comparison, grep the transcripts for the treatment content with a positive control, and treat a zero-dose arm as uninterpretable rather than null.
- [ ] CONSTRAINT: **A ZERO WITHOUT A POSITIVE CONTROL IS NOT EVIDENCE -- AND WHEN THE CONTROL ALSO READS ZERO, THE CHECK IS BROKEN.** My first verification grepped the results directory and got zero for both the taxonomy and the control. Transcripts live at `outputs/transcript.stream.jsonl`. Mitigation: fix the check before believing either number.
- [ ] CONSTRAINT: **READ AN ARM'S REPORTED REASONING BEFORE ITS OUTCOME.** The forcing arm was told to declare settled-or-unsettled and answered SETTLED 6/6, naming the resolving antecedents. That was a built-in manipulation check reporting its own failure, sitting in data I already had.
- [ ] CONSTRAINT: **A VERDICT'S STATED REASON CAN BE FALSE.** The forcing arm's 4/6 came from `wrong_reason` claiming a runtime error was detected. Nothing was detected -- a not-implemented throw matches neither `ASSERTION_RE` nor `RUNTIME_RE` and failed by fail-closed default, penalising a form `SKILL.md:89` endorses. Mitigation: check the code path that produced a verdict before attributing it to an arm. (Closed for this shape by D-05's `blunt_red`.)
- [ ] CONSTRAINT: **GATE ON EXIT STATUS.** `cmd; echo "EXIT=$?"` always exits 0. `check-red-references.mjs` prints "123 checks" even when RED. Mitigation: `cmd; test $? -eq 0 && echo TOKEN`, always.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Reporting a tie as a null | Six dimensions tied; the treatment had zero dose | blocking | Prove delivery via transcript grep + positive control before any arm comparison |
| Inconclusive check read as confirming | Control also returned zero; check was pointed at the wrong artifacts | blocking | A failing positive control invalidates the check, not the hypothesis |
| Fail-open verify leg | Trailing `echo $?`; gating on battery log text | blocking | `test $? -eq 0 && echo TOKEN` |
| Trusting a verdict's stated reason | `wrong_reason` claimed a detection that never happened | blocking | Trace the code path before attributing to an arm |
| Skipping a pipeline gate to save time | Research, planner and checker each corrected something I had locked | advisory | Run all gates; every one changed the work this session |
| Harness silently skips completed runs | Re-runs need `--force`, else "already done, skip" | advisory | Always `--force` a deliberate re-run; verify by timestamp |

<current_state>
Working tree CLEAN apart from one untracked leftover (`probe-d12-ambiguity/`, archived, safe to delete). HEAD `f75a989` on `gsd/lz-tdd-0.0.3-lz-red`, 372 commits ahead of `origin/main`, NOTHING PUSHED. No worktrees, no async jobs.

Quick task `260802-j03` is COMPLETE and verified 10/10. The instrument is repaired.

**Paused mid-pilot.** The pre-registered 3-run gate was launched and stopped after run-1. ~$1.24 spent of ~$3.71.
</current_state>

<completed_work>

- Built the D-12 ambiguity cell and ran round 1: 18 runs, $16.36, all dimensions tied.
- The mandatory unbiased audit overturned that reading -- treatment dose was zero, prompts were referentially unambiguous, forcing arm's 4/6 was a grading artifact.
- Landed all seven repairs (D-01..D-07), verified 10/10 by independent re-measurement.
- Measured three flagged risks rather than asserting them; poisoned the re-keyed tripwire to confirm it still fires.
- Archived round 1 (27 runs) to `D:/.lz-red-archive/2026-08-01-d12-round-1/`.
- Launched the pilot; stopped it after run-1 at the pause.
</completed_work>

<remaining_work>

1. **Repair the mixed tree** (first action, see constraint 1).
2. **Finish the pilot** -- 3 gradeable runs, `--force` required.
3. **Apply the committed rule verbatim.** N = gradeable runs with non-empty `changed_production_files`. N=3 -> ceiling held, STOP, do not buy the round. N<=2 -> ceiling moved, buy k=5 (~$27).
4. `/gsd-secure-phase 21` via **gsd-security-auditor** (zero spend, genuinely outstanding).
5. `/gsd-validate-phase 21` via **gsd-nyquist-auditor**.
6. `/gsd-extract-learnings 21` LAST + the manual `~/.gsd/knowledge/` bridge.
7. EVL-03 drift decision (owner) and the six stale records.
8. `/gsd-complete-milestone lz-tdd@0.0.3` with MANUAL archival.
</remaining_work>

<decisions_made>

- `a2` EXEMPT from the antecedent cut -- cutting it would invert the ground truth, not create ambiguity.
- Taxonomy INLINED rather than pointed at, to preserve lever separation. Changes arm 2's question to "does the content help when guaranteed present", which is what D-12 asks.
- `blunt_red` added AND `verdictPass()` widened in the same edit -- a new class alone is cosmetic.
- Lever tripwire RE-KEYED, never deleted.
- Pilot rule pre-registered numerically and committed before the pilot ran.
</decisions_made>

<blockers>
- **Mixed results tree** (technical) -- repair before reading anything.
- **Arm-length confound** (technical, unfixable) -- 167/174/282 lines. Report any treatment effect as confounded with length.
- **EVL-03 drift** (human) -- Path A re-grade ($0) worthwhile regardless.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` -- structured state; the mixed-tree repair and the pilot rule.
2. `.claude/skills/lz-red-workspace/e2e-red-srvx/targets.json` -- `AMB1.pilot_decision_rule` is the committed gate; read it VERBATIM and do not reinterpret after seeing numbers. `AMB1.ceiling_note` explains why the gate exists.
3. `.planning/quick/260802-j03-.../260802-j03-SUMMARY.md` -- what the seven fixes actually did, and the three measured risks.
4. `.planning/quick/260801-w8b-.../260801-w8b-CELL.md` -- the cell as built, with its ceiling warnings.

## Infrastructure State

- srvx throwaway at `D:/.lz-red-throwaway/srvx-d12` -- present, toolchain installed, 2 dirty files (expected; the harness resets before each run).
- Borrowed repos pristine; no stranded grading worktrees.
- Archives: round 1 at `D:/.lz-red-archive/2026-08-01-d12-round-1/`, pre-drift corpus at `2026-07-27-preskill-drift/`, abandoned Option C at `2026-08-01-optionC-abandoned/`.
- `worktree.baseRef: "head"` IS set -- load-bearing at 372 unpushed commits.
- A fresh worktree has no `node_modules`; `selfcheck-red.mjs` needs `npm ci --prefix .claude/skills/lz-red-workspace` before crux 7 will run.
- Disk: D: 24 GB free.

<context>
The instrument is in the best shape it has been. Round 1 was a genuine methodological failure -- not a wasted $16.36, because it bought the six defects that are now fixed and a much sharper idea of what this cell can and cannot measure.

The open question is narrow and cheap: did cutting `a1`'s antecedent move the baseline off a ceiling that stood at 18 of 18? If it did, the $27 round can detect something. If it did not, no round can, and the honest finding is the ceiling itself.

The pilot exists precisely so that question costs $3.71 instead of $27, and the rule is already committed so the answer cannot be rationalised after the fact.
</context>

<next_action>
Start with: repair the mixed results tree. Re-run all three pilot runs with `--force`
(`node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --suite .claude/skills/lz-red-workspace/e2e-red-srvx --mode apply --cwd D:/.lz-red-throwaway/srvx-d12 --arm invoke_skill --prompt a1 --runs 3 --force`), re-grade all three, then assert EVERY meta.json and EVERY red-grade.json carries a post-resume timestamp before computing N. Then apply `AMB1.pilot_decision_rule` verbatim. The three phase-21 audits are zero-spend and independent -- they can proceed in parallel with the pilot.
</next_action>
