---
phase: 11-skill-effectiveness-evals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/evals/trigger-eval.json
  - .claude/skills/lz-refactor-workspace/check-evals.mjs
autonomous: true
requirements: [EVL-01]
user_setup: []

must_haves:
  truths:
    - "**D-02:** evals/trigger-eval.json is a hand-authored ~20-query trigger set with 8-10 should-trigger queries (varied formal/casual phrasings of: green tests + a named smell like Long Function / Duplicated Code / Feature Envy / Conditional Complexity / Combinatorial Explosion, an explicit refactor-step ask, a catalog lookup like Extract Function or Replace Conditional with Polymorphism, and a de-patterning ask) AND 8-10 near-miss should-not-trigger queries; the near-misses MUST include >= 2 lz-tpp-seam green-step negatives (which change makes this failing test pass / minimal transformation to go green) plus write-a-test, write-a-function, refactor-for-speed (behavior change), explain-SOLID, debug, and complexity-analysis. Ground truth is the shipped description (SKILL.md:3-12) incl. its explicit lz-tpp out-of-scope clause. Queries are concrete (code snippets, casual speech, typos); NO rename-variable near-miss (Rename Variable is a real Fowler refactoring -> legit trigger)."
    - "check-evals.mjs is a build-time schema/count/seam/ASCII lint over evals/trigger-eval.json that exits non-zero on any violation; it is NOT a claude -p run (D-10 respected)."
    - "All authored trigger prose is ASCII-only (`->`, straight quotes, no emojis/em-dashes); allowlist-inversion email scan clean (only the approved gmail may appear)."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/evals/trigger-eval.json"
      provides: "EVL-01 trigger set: flat array of {query, should_trigger} (>= 8 true + >= 8 false)"
      contains: "should_trigger"
    - path: ".claude/skills/lz-refactor-workspace/check-evals.mjs"
      provides: "Build-time lint: schema, >= 8/>= 8 split, >= 2 seam negatives, ASCII-only"
  key_links:
    - from: ".claude/skills/lz-refactor-workspace/evals/trigger-eval.json"
      to: "plugins/lz-tdd/skills/lz-refactor/SKILL.md"
      via: "queries derived from the shipped description in/out-of-scope clauses (lines 3-12)"
      pattern: "should_trigger"
---

<objective>
Author the EVL-01 trigger eval set (the recall + specificity probe input) and a build-time lint that gates its shape. The trigger set stresses the shipped description, most importantly the lz-tpp seam: green-step prompts that must keep lz-refactor quiet.

Purpose: EVL-01 proves the description fires on refactor-step / smell / catalog / de-patterning contexts and stays quiet on near-misses. This plan builds the DATA only; the probe RUN is gated (D-10).
Output: evals/trigger-eval.json (10 + 10) and check-evals.mjs (green lint). Nothing is RUN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-skill-effectiveness-evals/11-RESEARCH.md
@.planning/phases/11-skill-effectiveness-evals/11-PATTERNS.md
@plugins/lz-tdd/skills/lz-refactor/SKILL.md

Analog (gitignored -- read with Read/rg/Glob, NEVER git grep):
- .claude/skills/lz-tpp-workspace/evals/trigger-eval.json (schema: flat array of {query, should_trigger})
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author evals/trigger-eval.json (10 should-trigger + 10 near-miss)</name>
  <files>.claude/skills/lz-refactor-workspace/evals/trigger-eval.json</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/SKILL.md (description lines 3-12: the in/out-of-scope ground truth, incl. the "Do not use it for the green / transformation step of TDD (... is lz-tpp)" clause)
    - 11-RESEARCH.md "EVL-01 Query Candidates" (the T1-T10 should-trigger + N1-N10 near-miss tables with the in-scope facet / why-quiet rationale)
    - .claude/skills/lz-tpp-workspace/evals/trigger-eval.json (schema shape only; note lz-tpp's green-step queries FLIP to should_trigger:false for lz-refactor)
  </read_first>
  <action>Create evals/trigger-eval.json as a flat JSON array of {query, should_trigger} objects. Author 10 should-trigger:true queries from the RESEARCH T1-T10 candidates (green tests + a named smell across both layers; an explicit refactor-step ask; catalog lookups for Extract Function and Replace Conditional with Polymorphism; a de-patterning "refactor away from this Singleton" ask; a principle lookup like the rule of three) and 10 should_trigger:false near-misses from N1-N10. The near-misses MUST include the >= 2 lz-tpp-seam green-step negatives (N1-N3: "smallest edit to get the failing test passing", "minimal transformation to green it") plus write-a-jest-test, write-a-debounce-helper, add-pagination (feature work), explain-SOLID, find-the-bug, and complexity-analysis. Finalize the exact wording (you may adjust register/typos/snippets per skill-creator guidance) but keep every query concrete. Do NOT include a rename-variable near-miss. ASCII-only.</action>
  <verify>
    <automated>python -m json.tool .claude/skills/lz-refactor-workspace/evals/trigger-eval.json > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Valid JSON array; every element has a string `query` and a boolean `should_trigger`.
    - >= 8 (target 10) entries with should_trigger:true; >= 8 (target 10) with should_trigger:false.
    - >= 2 should_trigger:false entries are lz-tpp-seam green-step negatives (failing-test-to-green / minimal-transformation phrasing).
    - No rename-variable near-miss; queries are concrete (snippets / casual speech / typos).
    - ASCII-only; allowlist-inversion email scan clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author check-evals.mjs schema/count/seam/ASCII lint; run GREEN</name>
  <files>.claude/skills/lz-refactor-workspace/check-evals.mjs</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/evals/trigger-eval.json (the file this lint validates -- authored in Task 1)
    - 11-RESEARCH.md "Validation Architecture -> Phase Requirements -> Test Map" (the EVL-01 build-time lint row: >= 8/>= 8 split with >= 2 lz-tpp-seam negatives)
  </read_first>
  <action>Create check-evals.mjs (Node ESM, no new deps) that reads evals/trigger-eval.json relative to the script and asserts, exiting non-zero with a clear message on any failure: (1) top-level is an array; every element is an object with a string `query` and a boolean `should_trigger`; (2) count of should_trigger:true >= 8 and count of should_trigger:false >= 8; (3) at least 2 should_trigger:false entries match a seam regex over the query (case-insensitive alternation of: failing test, make .* pass, minimal transformation, go green / green it, smallest (edit|step)); (4) every query is ASCII-only (no byte > 0x7F). On success print an OK line and exit 0. Keep it a single self-contained script (ponytail: no framework, no config file). This is a build-time lint, NOT a claude -p run.</action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/check-evals.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node check-evals.mjs` exits 0 and prints an OK line over the authored trigger-eval.json.
    - The lint fails non-zero if the split drops below 8/8, if fewer than 2 seam negatives are present, or on any non-ASCII byte (fail-closed).
    - Script is ASCII-only, single file, no new dependencies.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer-authored eval prose -> local git tree | New committed trigger queries (with code snippets + casual phrasing), local only |
| check-evals.mjs -> local JSON | Parses maintainer-authored JSON; no network, no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | committed trigger-eval.json prose + code snippets | mitigate | ASCII-only; allowlist-inversion email scan before commit; no secrets/PII in prompts. |
| T-11-04 | Tampering | check-evals.mjs parses local JSON | mitigate | Fail-closed on malformed / short / non-ASCII input (lint exits non-zero); no dynamic eval of file contents. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; check-evals.mjs uses only Node stdlib. Low severity, no ASVS L1 install surface. |
</threat_model>

<verification>
- python -m json.tool validates trigger-eval.json structurally.
- node check-evals.mjs GREEN over the authored set (schema + >= 8/>= 8 + >= 2 seam + ASCII).
- No trigger probe or claude -p invocation runs in this plan (D-10).
</verification>

<success_criteria>
- evals/trigger-eval.json exists with 10 should-trigger + 10 near-miss (incl. >= 2 lz-tpp-seam negatives), grounded in the shipped description.
- check-evals.mjs lint GREEN and fail-closed.
- ASCII-only, work-email-free; no writes into plugins/ or .planning/.
</success_criteria>

<output>
Create `.planning/phases/11-skill-effectiveness-evals/11-02-SUMMARY.md` when done.
</output>
