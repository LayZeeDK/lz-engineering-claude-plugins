---
phase: 13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p1
  - .claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p2
  - .claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p8
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/no_skill/gr1
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/no_skill/gr4
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/with_skill/gr4
autonomous: false
requirements: [SPEC-req-1, SPEC-req-2]
user_setup: []

must_haves:
  truths:
    - "**D-01:** the single-target cells REUSE the already-persisted skill-arm apply diffs (nx with_skill p1/p2; kata with_skill + invoke_skill gr1) and this plan BACKFILLS ONLY the missing no_skill base arm (k=3) on the same prompts/targets; no prompt or target is authored."
    - "**D-02:** the sweep cells honor the AMENDED decision -- REUSE the committed nx p8 with_skill k=3 diffs (captured 2026-07-14 on current HEAD against the unchanged shipped skill) and backfill nx p8 no_skill only (k=3); kata gr4 is genuinely absent for both arms, so re-run gr4 with_skill AND no_skill (k=3 each); net new metered runs = 18 (single-target no_skill p1/p2/gr1 = 9; sweep p8 no_skill 3 + gr4 both 6 = 9)."
    - "**D-03:** every run goes through the existing .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs (--mode apply, --arm no_skill|both, --prompt, --runs 3, --cwd; kata via --suite) with NO harness code change; the runner owns arm selection, throwaway-branch safety, and diff+meta persistence."
    - "**D-04:** run-e2e.mjs stages git add -A and writes git diff --cached <applyBase> into the TRACKED workspace tree (results/apply/<arm>/<pN>/run-<k>/) BEFORE it resets the borrowed repo, so the persisted diff.patch + meta.json SURVIVE restoring the borrowed repos pristine; apply runs execute on throwaway branches (the runner refuses protected branches 23.0.x/main/master and requires --cwd); kata leaves protected main first."
    - "**D-09:** every metered claude -p run is gated -- Task 1 runs run-e2e.mjs --dry-run FIRST (prints composed prompts + commands, spends nothing), Task 2 HALTS at a blocking eval-run-approval checkpoint presenting the full run manifest + the pre-recorded approval, and NO metered run executes before that explicit go; grading (wave 3) runs on the Claude plan, not this metered pool."
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p8/run-1/meta.json"
      provides: "nx p8 sweep no_skill run-1 metadata (changed_files, used_refactor, exit_code)"
      contains: "changed_files"
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/with_skill/gr4/run-1/meta.json"
      provides: "kata gr4 sweep with_skill run-1 metadata"
      contains: "changed_files"
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/no_skill/gr4/run-1/meta.json"
      provides: "kata gr4 sweep no_skill run-1 metadata"
      contains: "changed_files"
  key_links:
    - from: "run-e2e.mjs --mode apply --arm no_skill|both"
      to: "results/apply/<arm>/<pN>/run-<k>/{diff.patch,meta.json}"
      via: "git add -A; diff --cached applyBase; write into workspace before reset --hard"
      pattern: "diff.patch|meta.json"
    - from: "the D-09 blocking checkpoint"
      to: "the 18 metered claude -p runs"
      via: "explicit user go after dry-run verification; no self-approval"
      pattern: "approved"
---

<objective>
Generate the 18 net-new metered apply runs that this phase needs, through the existing run-e2e.mjs harness, gated by the D-09 eval-run-approval checkpoint. Backfill the no_skill single-target arm (nx p1, p2; kata gr1) and re-run the sweep cells (nx p8 no_skill only per the amended D-02; kata gr4 both arms), persisting diff.patch + meta.json into the tracked workspace tree so they survive borrowed-repo restoration.

Purpose: apply/drive mode was only ever run with_skill/invoke_skill, so there is no base-vs-skill delta on applied output; and the sweep diffs were lost by prior ad-hoc runs. These runs produce the graded corpus for waves 3-4. This is the biggest metered-spend cell (nx p8 ~13 min/run), so it is dry-run-verified and HALTS for explicit approval before any spend.
Output: results/apply/no_skill/{p1,p2,p8}/run-{1..3}/ (nx) and results/apply/no_skill/{gr1,gr4}/run-{1..3}/ + results/apply/with_skill/gr4/run-{1..3}/ (kata), each with diff.patch (or an explicit no-edit meta.json) + meta.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-CONTEXT.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESEARCH.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-PATTERNS.md

Harness + suites (tracked; read the exact flags before running):
@.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
@.claude/skills/lz-refactor-workspace/e2e-nx/suite.json
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/suite.json

Exact run-e2e.mjs invocations (dry-run first, spends nothing) are in 13-RESEARCH.md "Code Examples -> REQ-1/REQ-2".
Borrowed repos: nx D:/projects/github/nrwl/nx (branch lz-refactor-e2e-smoke, applyBase origin/23.0.x); kata D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript (must leave protected main first). Runs use claude-opus-4-8 @ high, --setting-sources project; with_skill/invoke_skill pass --plugin-dir the shipped skill, no_skill omits it (the only A/B difference).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run every planned run command (spends nothing)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p1</files>
  <read_first>
    - 13-RESEARCH.md "Code Examples -> REQ-1/REQ-2: generate the missing run arms (run-e2e.mjs)" (the exact --dry-run then apply commands, per suite)
    - 13-RESEARCH.md "Common Pitfalls -> Pitfall 1" (nx p8 with_skill already exists -- backfill no_skill only) and "Pitfall 8" (p2 with_skill run-3 is a valid explicit no-edit decline)
    - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs (confirm --dry-run, --arm, --mode apply, --prompt, --runs, --cwd, --suite flag names before composing commands)
  </read_first>
  <action>
    Run run-e2e.mjs with --dry-run for EVERY planned metered command (dry-run prints the composed prompts + commands and spends nothing). Compose and dry-run: (a) nx single-target no_skill backfill -- node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --dry-run --mode apply --arm no_skill --prompt p1 --prompt p2 --runs 3 --cwd D:/projects/github/nrwl/nx; (b) nx p8 sweep no_skill only (reusing the committed p8 with_skill per D-02) -- same runner --dry-run --mode apply --arm no_skill --prompt p8 --runs 3 --cwd D:/projects/github/nrwl/nx; (c) kata gr1 no_skill backfill + gr4 both arms via --suite pointing at the absolute e2e-gilded-rose path, --cwd the kata TypeScript path, --dry-run --mode apply --arm no_skill --prompt gr1 --runs 3 and separately --arm both --prompt gr4 --runs 3. Confirm each dry-run prints the intended arm/prompt/runs and the correct --cwd, and that the composed no_skill command omits --plugin-dir while gr4 with_skill includes it. Verify the kata --cwd is NOT protected main (the runner refuses to reset --hard protected branches; the kata clone must be on a throwaway branch such as lz13-kata-apply -- create it with git checkout -b lz13-kata-apply main before the real runs). Capture the dry-run summary (counts of runs per cell) for the checkpoint. NO metered run executes in this task.
  </action>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --dry-run --mode apply --arm no_skill --prompt p1 --prompt p2 --runs 3 --cwd D:/projects/github/nrwl/nx 2>&1 | rg -N 'p1|p2|no_skill|dry' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - a dry-run was executed for all four command groups (nx single-target no_skill, nx p8 no_skill, kata gr1 no_skill, kata gr4 both) and each printed the intended arm/prompt/runs and --cwd
    - the composed no_skill commands omit --plugin-dir; the gr4 with_skill arm includes it
    - the kata --cwd is confirmed to be a throwaway branch (not protected main); NO metered claude -p run has executed
  </acceptance_criteria>
  <done>All planned run commands are dry-run-verified (spending nothing); the run manifest (cells x runs = 18) is ready for the D-09 checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-09 eval-run-approval gate (HALT before any metered spend)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p2</files>
  <read_first>
    - 13-CONTEXT.md "D-09 [HARD GATE]" and the standing eval-run-approval gate (never run metered claude -p without explicit approval)
    - the Task 1 dry-run output (the run manifest to present)
  </read_first>
  <action>
    Present the Task 1 dry-run manifest to the user and HALT: list the 6 cells and 18 metered runs (nx single-target no_skill p1/p2, nx p8 no_skill, kata gr1 no_skill, kata gr4 both arms), the reuse choice (committed nx p8 with_skill + single-target skill-arm diffs are reused, not re-run), the est. spend (multi-hour; nx p8 ~13 min/run), and the model/effort/k (claude-opus-4-8 @ high, --setting-sources project, k=3). Record the user's explicit go (or an amended manifest) before Task 3 runs anything. Do NOT execute any metered claude -p run in this task -- this is the gate, not the runs.
  </action>
  <what-built>
    Task 1 produced a spend-free dry-run of all 18 net-new metered runs: nx single-target no_skill backfill (p1 k=3, p2 k=3), nx p8 sweep no_skill (k=3), kata gr1 no_skill (k=3), kata gr4 both arms (with_skill k=3 + no_skill k=3). The committed nx p8 with_skill and the single-target skill-arm diffs are REUSED (not re-run).
  </what-built>
  <how-to-verify>
    1. Review the dry-run manifest: 6 cells, 18 metered runs total, est. multi-hour (nx p8 ~13 min/run).
    2. Confirm the amended D-02 reuse choice (reuse committed nx p8 with_skill; backfill p8 no_skill only) is acceptable, or request strict D-02 (also re-run p8 with_skill, +3 runs).
    3. Confirm the model/effort (claude-opus-4-8 @ high, --setting-sources project) and k=3 match the prior eval config.
    The user has pre-approved metered runs for this phase; this checkpoint records that approval against the concrete manifest and is the final go/no-go before spend.
  </how-to-verify>
  <acceptance_criteria>
    - the full run manifest (6 cells, 18 runs) and the D-02 reuse-vs-re-run choice were presented to the user
    - explicit approval (or an amended manifest) is recorded before Task 3 executes any metered run
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the 18 metered runs as manifested (or state an amended manifest, e.g. "also re-run p8 with_skill"), or "abort".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the approved metered apply runs and persist diffs</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/results/apply/no_skill/p8, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/with_skill/gr4, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/no_skill/gr4, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/apply/no_skill/gr1</files>
  <read_first>
    - 13-RESEARCH.md "Code Examples -> REQ-1/REQ-2" (drop --dry-run for the real runs; the runner is idempotent -- completed exit-0 runs skip unless --force) and "Runtime State / Repo Isolation & Restoration"
    - 13-PATTERNS.md "Shared Patterns -> Run-output persistence" (meta.json schema; an explicit no-edit run leaves meta.json with empty changed_files -- a valid REQ-1 outcome)
  </read_first>
  <action>
    ONLY after the Task 2 approval, run the same commands from Task 1 WITHOUT --dry-run, one cell at a time (never two apply sessions concurrently on the same borrowed repo -- both would reset --hard the same clone). Order: nx single-target no_skill (p1, p2, k=3) on --cwd D:/projects/github/nrwl/nx; then nx p8 no_skill (k=3) on the same nx clone; then kata gr1 no_skill (k=3) and kata gr4 --arm both (k=3) via --suite on --cwd the kata TypeScript path (kata on throwaway branch lz13-kata-apply). The runner resets each --cwd to its applyBase (origin/23.0.x for nx, main for kata) before each run and captures the diff into the tracked workspace before the next reset. After each cell, confirm results/apply/<arm>/<pN>/run-{1..3}/meta.json exists and diff.patch is non-empty OR meta.json records an explicit no-edit (empty changed_files) -- both are acceptable per REQ-1. If a run is interrupted, re-invoke the same command (idempotent resume). Do NOT restore the borrowed repos in this plan beyond the runner's own per-run reset (final canonical restoration is 13-05); leave the persisted diffs in place. Commit the new tracked results/apply diff.patch + meta.json files (specific paths only, never git add -A) so wave-3 grading inherits them; committer identity = the approved public gmail.
  </action>
  <verify>
    <automated>miss=0; for a in "e2e-nx/results/apply/no_skill/p1" "e2e-nx/results/apply/no_skill/p2" "e2e-nx/results/apply/no_skill/p8" "e2e-gilded-rose/results/apply/no_skill/gr1" "e2e-gilded-rose/results/apply/no_skill/gr4" "e2e-gilded-rose/results/apply/with_skill/gr4"; do for k in 1 2 3; do test -f ".claude/skills/lz-refactor-workspace/$a/run-$k/meta.json" || { echo "MISSING $a run-$k"; miss=1; }; done; done; test $miss -eq 0 && echo ALL_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - all 6 cells x 3 runs have a meta.json, and each run has a non-empty diff.patch OR a meta.json recording an explicit no-edit (empty changed_files)
    - the nx p8 with_skill and single-target skill-arm diffs were NOT re-run (reused per D-01/D-02) unless the checkpoint amended the manifest
    - the new tracked results/apply diff.patch + meta.json are committed (specific paths only) with committer identity = approved public gmail
  </acceptance_criteria>
  <done>18 net-new metered runs completed; results/apply no_skill (p1/p2/p8/gr1/gr4) + with_skill/gr4 persisted and committed; borrowed repos left at their applyBase (final restoration deferred to 13-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metered claude -p spend -> user authorization | no metered run may execute before the D-09 blocking checkpoint |
| apply run -> borrowed-repo git state | runs must execute on throwaway branches; protected branches (23.0.x/main/master) are refused |
| model transcript -> tracked results | diff.patch/meta.json are OUR output (not book prose); raw stream transcripts stay under gitignored outputs/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | .oracle/ prose leaking via run artifacts | mitigate | apply runs never read .oracle/; persisted diff.patch/answer.md are our generated output; grading (not this plan) is the only .oracle touch and is agent-firewalled |
| T-13-02 | Information Disclosure | maintainer work email/domain in committed run artifacts | mitigate | commit specific result paths only (never git add -A); allowlist-inversion email scan before commit; committer = approved gmail |
| T-13-04 | Tampering | apply run corrupts a protected borrowed branch | mitigate | run-e2e.mjs refuses reset --hard on protected branches and requires --cwd; kata is moved to throwaway lz13-kata-apply before any run |
| T-13-SC | Tampering | npm/pip installs | accept | phase installs NO packages; the runner and both repos' node_modules are already present; no registry surface |
</threat_model>

<verification>
- Task 1 dry-run printed all four command groups with correct arm/prompt/runs/--cwd and no spend
- Task 2 checkpoint recorded explicit approval (or an amended manifest) before any metered run
- all 6 cells x 3 runs have meta.json; diff.patch non-empty or explicit no-edit meta per REQ-1
- persisted diffs are captured into the tracked workspace (survive borrowed-repo reset) and committed with the approved gmail committer
</verification>

<success_criteria>
The 18 net-new metered apply runs are complete and persisted (no_skill single-target backfill + sweep-pair per amended D-02), gated by an honored D-09 approval, ready for wave-3 grading.
</success_criteria>

<output>
Create `.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-02-SUMMARY.md` when done.
</output>
