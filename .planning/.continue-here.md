---
context: research
phase: post-12-nx-sweep-autotrigger-closeout
task: 1
total_tasks: 3
status: paused
last_updated: 2026-07-14T20:41:56.439Z
---

# Close the Phase-12 nx-sweep-auto-trigger WARNING (via /gsd-quick --validate --research)

# BLOCKING CONSTRAINTS -- Read Before Anything Else

- [ ] CONSTRAINT: **Auto-trigger fires in APPLY/directive framing, NOT recommend.** Phase-12 measured
  whole-package sweep auto-trigger at 3/3 in apply mode but 0/6 in recommend. Measure with the
  `with_skill` arm in APPLY mode on the directive prompt (p8). `invoke_skill` FORCES activation, so it
  cannot measure auto-trigger -- do NOT use it here.
- [ ] CONSTRAINT: **The nx throwaway apply branch was DELETED in cleanup.** nx is pristine on `23.0.x`.
  Apply mode reset --hard's to `applyBase` each run and REFUSES protected branches -- so you MUST
  `git checkout -b lz-refactor-e2e-smoke origin/23.0.x` in `D:/projects/github/nrwl/nx` FIRST, and pass
  `--cwd` to that branch.
- [ ] CONSTRAINT: **Metered `claude -p` runs are user-gated (eval-run-approval-gate).** Halt for explicit
  spend approval before running; surface the run count. Session-cap-fragile -- run in background; the
  runner is idempotent (exit-0 skip; use `--force` only if stale results linger).
- [ ] CONSTRAINT: **Only run `no_skill` for NEW query prompts.** p8/p7/fleet are existing prompts -- do
  NOT re-run their `no_skill` baselines. Auto-trigger has no meaning for `no_skill` anyway (no skill).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Measuring auto-trigger in recommend mode | Recommend framing does not auto-fire the skill (0/6); you'd wrongly conclude "no trigger" | blocking | Run `with_skill` APPLY on the directive prompt p8 |
| Running apply on a protected branch | The runner reset --hard's per run; on 23.0.x/master that destroys the pristine tree (runner refuses, but do not fight it) | blocking | `git checkout -b lz-refactor-e2e-smoke origin/23.0.x` first; `--cwd` that branch |
| Re-opening the value question | Output value + reference lever are already tested + closed (null / narrow) | advisory | This is a NARROW trigger close-out only -- grade auto-trigger, nothing else |

<current_state>
Phase 12 is CLOSED (verify 4/4, secure 8/8, validate nyquist_compliant, learnings pooled; commit 234d25f).
Milestone lz-tdd@0.0.2 phases 6-12 are all complete on disk. The ONE loose end is a non-blocking WARNING:
the nx whole-package sweep auto-trigger was measured PRE-revert (p8 3/3, fleet 8/8) against the broadened
12-02 description that was REVERTED (9832c74); post-revert only the kata sweep (gr4 3/3) was DIRECTLY
re-confirmed on HEAD. The nx sweep on HEAD is inferred. This close-out measures it directly. The skill is
live (user ran /reload-plugins) and the runner reads HEAD via --plugin-dir regardless.
</current_state>

<completed_work>
- Phase 12 closed end-to-end (234d25f); reference eval closed (2776e49); /reload-plugins done.
</completed_work>

<remaining_work>
1. Re-create the nx throwaway apply branch: `git checkout -b lz-refactor-e2e-smoke origin/23.0.x` in
   `D:/projects/github/nrwl/nx`.
2. Run nx p8 (primary; optionally p7 + fleet p9-p12) `with_skill` apply k=3 on HEAD; grade AUTO-TRIGGER
   (does `used_refactor`/`skills_invoked` fire). Target: fires (matches pre-revert 3/3 + kata gr4 3/3).
3. If it fires: flip the WARNING to CONFIRMED in `12-VALIDATION.md` + `12-VERIFICATION.md`, commit. If it
   does NOT fire: genuine regression -- the 9832c74 revert removed something load-bearing; investigate the
   reverted 12-02 description vs the live 241c1fb description.
</remaining_work>

<decisions_made>
- Measure via `with_skill` APPLY (not recommend, not invoke_skill) -- the only faithful auto-trigger probe.
- No `no_skill` re-run (existing prompts; irrelevant for auto-trigger).
- No reload needed for the runner (--plugin-dir = HEAD); user reload covers interactive.
</decisions_made>

<blockers>
- Metered runs user-gated (eval-run-approval-gate) -- halt for approval before spend.
- nx throwaway branch deleted -- re-create before apply.
- Destructive git on nx (reset --hard) is classifier-gated; `git checkout -b` is not.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` -- machine-readable state (this task).
2. `.planning/phases/12-*/12-VALIDATION.md` (the WARNING) + `12-VERIFICATION.md` (audit_notes: the inferred-nx nuance).
3. `.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs` (arms/modes; --cwd; --force) + `suite.json` (p8 = pkgsweep directive; applyBase origin/23.0.x; protectedBranches).
4. memory `lz-refactor-output-warrant-axis-exhausted` -- do NOT re-open output/reference value; this is trigger-only.

## Infrastructure State
- nx `D:/projects/github/nrwl/nx`: pristine on `23.0.x`; throwaway branch lz-refactor-e2e-smoke DELETED (re-create).
- Live skill = HEAD (234d25f): 241c1fb intent-based description (carries auto-trigger) + 9832c74 revert (leaner sweep cluster) + 98cf482 L1 warrant. Runner reads it via --plugin-dir.
- kata already restored to main (not needed for this nx-only close-out).
- Exact command (after branch re-create + spend approval):
  `node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --mode apply --prompt p8 --arm with_skill --cwd D:/projects/github/nrwl/nx --runs 3`

<context>
Narrow close-out of a single non-blocking WARNING, not new work. Honest expectation: p8 auto-triggers on
HEAD (the auto-trigger-carrying 241c1fb description is unchanged by the revert; kata gr4 already
re-confirmed 3/3 post-revert). A fire CLOSES the WARNING. Grade ONLY auto-trigger (did the skill fire) --
drive/quality is settled (null skill delta; base catalog-grade). After this, the milestone is fully
closable via /gsd-audit-milestone (not yet run -- user scoped it out).
</context>

<next_action>
Start with: read this file + HANDOFF.json + 12-VALIDATION.md. Then run /gsd-quick --validate --research
with "Measure the nx whole-package sweep auto-trigger on HEAD to close the Phase-12 WARNING". First infra
step in the quick task: re-create the nx throwaway branch; then (after spend approval) run nx p8 with_skill
apply k=3; grade auto-trigger; update 12-VALIDATION/12-VERIFICATION.
</next_action>
