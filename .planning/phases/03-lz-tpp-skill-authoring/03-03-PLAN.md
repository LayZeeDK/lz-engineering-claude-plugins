---
phase: 03-lz-tpp-skill-authoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md
autonomous: true
requirements: [TPP-05, TPP-07]

must_haves:
  truths:
    - "Paired functional and imperative TypeScript examples -- Kata 1 (sum 1..n, LINEAR) and Kata 2 (flatten a nested list, TREE) -- show how transformation-priority choices shift by paradigm: functional reaches (statement -> tail-recursion) #9 then pays for stack safety while imperative reaches (if -> while) #10 + (variable -> assignment) #13 for free (TPP-05; D-07)"
    - "Kata 2 shows tail-recursion is not even available for tree recursion, so both paradigms converge on an explicit stack / generator (TPP-05; D-07)"
    - "The file states the no-reliable-TCO reality (only JavaScriptCore/Safari implements ES6 proper tail calls; V8/Node/Deno and SpiderMonkey do not) and flags the kangax compat-table Node 2/2 cell as a false positive, citing Chrome 0/2 + the empirical V8 overflow (TPP-07; D-08 landmine)"
    - "Teach-vs-mention split honored: TEACH iterative default + trampoline (first-class) and generator + fold (secondary); MENTION CPS stating EXPLICITLY it is stack-safe ONLY with a trampoline (naked CPS overflows), and MENTION mutual-recursion trampolining (TPP-07; D-08)"
    - "The transformation-vs-refactoring distinction and the deep-input-test decision guide are present (recurse-vs-iterate at green time is a transformation; converting an already-green recursive solution to a stack-safe form is a refactoring UNLESS a deep-input test pins behavior) (TPP-07; D-08)"
    - "The TS/JS overlay (prefer (if -> while) and (variable -> assignment) above the recursion transformations) is framed as an opinionated, source-sanctioned heuristic (FibTPP language-specificity), not a contradiction of the canonical list and not dogma (D-09)"
    - "TS typing gotchas survive verbatim: tagged-union Bounce<T> when the payload can be a function, a custom isNested guard, explicit generator return typing, and no DOM-global element name (Node/Text/Range); all fenced TS is tsc --strict-clean (D-07/D-08 landmines); this file is the second of the two focused reference files added under references/ ALONGSIDE the LOCKED, unmodified transformations.md (D-01)"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md"
      provides: "Paired functional/imperative katas (sum + flatten) demonstrating paradigm-driven priority shift, plus the JS/TS TCO reality, the five stack-safe patterns with teach-vs-mention treatment, the transformation-vs-refactoring distinction, and the recurse-vs-iterate decision guide"
      contains: "## Contents"
      min_lines: 100
  key_links:
    - from: "plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md"
      to: "(self-contained)"
      via: "no markdown link to any other reference file (one-level-deep rule: reference files must not link to other reference files)"
      pattern: "^(?!.*\\]\\(.*(transformations|fibonacci-worked-example)\\.md\\)).*$"
---

<objective>
Author the bundled TypeScript + TCO reference file: the paired functional/imperative katas
(sum and flatten) that show how transformation-priority choices shift by paradigm, plus the
JS/TS tail-call-optimization reality and the stack-safe recursion patterns with a strict
teach-vs-mention split, the transformation-vs-refactoring distinction, and the recurse-vs-
iterate decision guide.

Purpose: TPP-05 requires paired functional + imperative TS examples demonstrating paradigm-
driven priority shift; TPP-07 requires addressing the lack of reliable TCO and the stack-safe
alternatives (trampoline, generator, CPS) plus when to prefer iteration. This is the on-demand
reference tier the SKILL.md body points at and the source that justifies the TS/JS overlay.
Output: plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (TOC-headed, ASCII-only,
tsc --strict-clean TypeScript lifted near-verbatim from the verified research, self-contained --
no links to other reference files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md
@.planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md
@.planning/research/TPP-TYPESCRIPT.md

<interfaces>
Verified TS lift targets (from TPP-TYPESCRIPT.md; already tsc --strict-clean and executed at
depth 1,000,000 on Node v24.18.0 -- lift near-verbatim, do NOT rewrite):
- Pattern 1 trampoline: `type Thunk<T>` / `type Trampoline<T>` and the tagged-union `Bounce<T>` variant.
- Pattern 2 generator-as-state-machine: `Generator<Yield, Return, Next>` explicit return typing.
- Pattern 3 CPS + trampoline: the NAKED-CPS-overflows warning example and the trampolined-CPS safe form.
- Pattern 4 iterative `(if -> while)`: `sumIterative` and `flattenIterative` (explicit stack).
- Pattern 5 fold/reduce.
- Kata 1: `sum(1..n)` tests incl. the deep-input `sum(1_000_000)` test.
- Kata 2: `flatten` with `type Nested`, `type Item`, and the custom `const isNested = (x: Item): x is Nested => Array.isArray(x)` guard; element type must NOT be named `Node` (DOM collision).

Progressive-disclosure rule (RESEARCH, VERIFIED 2026-07-02): a reference file over 100 lines
opens with a table of contents; a reference file must NOT link to another reference file.

Canonical list positions referenced: 9 (statement -> tail-recursion), 10 (if -> while),
11 (statement -> recursion), 13 (variable -> assignment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the paired functional/imperative katas section (TPP-05)</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md</files>
  <read_first>
    - .planning/research/TPP-TYPESCRIPT.md ("Paired Example Design": Kata 1 sum-of-1..n and Kata 2 flatten, incl. the exact tests, the #9-vs-#10/#13 divergence narrative, and the "TS typing lesson embedded in Kata 2"; Pattern 4 iterative forms sumIterative/flattenIterative)
    - .planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md (D-07 paired-katas decision; D-09 overlay framing)
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md (canonical positions 9/10/11/13 to reference by name and number)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md (Pitfall 5 TS typing gotchas to preserve verbatim; "TS example lift targets")
  </read_first>
  <action>
    Create references/typescript-and-tco.md. Open with an intro and a "## Contents" table of
    contents (the file will exceed 100 lines; the TOC will also cover the TCO section added in Task
    2). Author the paired-katas section first.

    Kata 1 (sum 1..n, LINEAR): show the shared tests (including the deep-input test sum(1_000_000) =
    500_000_500_000). Present the FUNCTIONAL path reaching (statement -> tail-recursion) #9 (green
    for small inputs, overflows the deep-input test), then paying for stack safety with a
    trampoline; and the IMPERATIVE path where the TS/JS overlay promotes (if -> while) #10 +
    (variable -> assignment) #13 above recursion, giving a stack-safe loop for free. State the
    divergence explicitly: same tests, same behavior, different transformation sequence, driven by
    the TCO reality.

    Kata 2 (flatten a nested list, TREE): show that the natural definition is tree recursion with NO
    tail-recursive form, so (statement -> tail-recursion) #9 is unavailable; the naive recursive
    flatten overflows on deep nesting; both paradigms converge on an explicit stack -- generator
    (lazy, yields leaves) for the functional flavour and an explicit-stack while loop for the
    imperative one. Embed the TS typing lesson verbatim: `Array.isArray` does not narrow a readonly
    union member, so use the custom `isNested` guard; and do NOT name the element type `Node` (DOM
    `--lib dom` collision) -- use `Item`/`Nested`.

    Lift all TypeScript near-verbatim from TPP-TYPESCRIPT.md (already tsc --strict-clean); do not
    rewrite it. All arrows ASCII "->"; all fenced code blocks are ```ts. Do NOT link to any sibling
    reference file.
  </action>
  <verify>
    <automated>rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md` succeeds and it opens with `## Contents` (`rg -n '^## Contents'`).
    - Both katas present: `rg -in 'kata 1'` and `rg -in 'kata 2'` match; `rg -F 'sum'` and `rg -F 'flatten'` match.
    - Deep-input test present for Kata 1: `rg -F '1_000_000'` (or the chosen literal) matches inside a ```ts block.
    - Paradigm divergence tagged: `rg -F '(statement -> tail-recursion)'`, `rg -F '(if -> while)'`, and `rg -F '(variable -> assignment)'` all match.
    - Kata 2 typing lesson preserved: `rg -F 'isNested'` matches; `rg -n '\bNode\b' <file>` does NOT match as an element type name (manual confirm the element type is `Item`/`Nested`).
    - All fenced code blocks are TypeScript (`rg -n '^```' <file>` shows only ```ts or closing fences).
    - Self-contained: `rg -n '\]\((references/)?(transformations|fibonacci-worked-example)\.md' <file>` returns nothing.
    - ASCII-only: `rg -n '[^\x00-\x7F]' <file>` returns nothing.
  </acceptance_criteria>
  <done>The katas section demonstrates the paradigm-driven priority shift (Kata 1 linear #9-vs-#10/#13; Kata 2 tree, no tail form, converge on explicit stack/generator), preserves the isNested/no-DOM-Node typing lessons, uses ASCII-only TS lifted from the verified research, and links to no sibling reference file.</done>
</task>

<task type="auto">
  <name>Task 2: Author the TCO reality + stack-safe patterns + decision guide (TPP-07)</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md</files>
  <read_first>
    - .planning/research/TPP-TYPESCRIPT.md ("TCO Engine Status", "Why It Matters for TPP", "TCO-Alternative Patterns" Patterns 1-6 with teach-vs-mention treatment, "TPP Integration and Decision Guide", and "Pitfalls" -- especially the CPS-without-trampoline and compat-table Node-cell critical pitfalls)
    - .planning/phases/03-lz-tpp-skill-authoring/03-CONTEXT.md (D-08 teach-vs-mention split and the transformation-vs-refactoring rule; D-09 source-sanctioned overlay)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md (Pitfalls 3 and 4: CPS-standalone and Node-2/2 landmines; the exact framing to preserve)
    - plugins/lz-tdd/skills/lz-tpp/references/transformations.md ("Provenance: FibTPP revisions": the language-specificity quote that sanctions the overlay)
  </read_first>
  <action>
    Append the TCO section to typescript-and-tco.md (update the "## Contents" TOC to cover it).

    State the no-reliable-TCO reality: only JavaScriptCore (Safari) implements ES6 proper tail
    calls; V8 (Chrome, Node.js, Deno, Edge, Electron) and SpiderMonkey (Firefox) do not; deep tail
    recursion on Node/V8 overflows (~10,000 frames, verified). Flag the kangax compat-table "Node
    2/2" proper-tail-calls cell as a FALSE positive and cite the Chrome row (0/2) plus the empirical
    V8 overflow -- never assert Node/Deno supports PTC.

    Present the five stack-safe patterns with the exact teach-vs-mention treatment: TEACH the
    iterative default ((if -> while) + (variable -> assignment)) and the trampoline (first-class,
    including the tagged-union Bounce<T> variant when the payload can be a function); TEACH the
    generator-as-state-machine and fold/reduce (secondary); MENTION CPS and state EXPLICITLY that it
    is stack-safe ONLY when paired with a trampoline (show the naked-CPS-overflows warning example
    labelled as NOT a fix, then the trampolined-CPS safe form); MENTION mutual-recursion
    trampolining in one sentence.

    State the transformation-vs-refactoring distinction: choosing recurse vs iterate at green time is
    a transformation (positions 9/10/11/13); converting an already-green recursive solution to a
    stack-safe form is a refactoring -- UNLESS a deep-input test pins behavior, in which case stack
    safety is behavioral and the chosen transformation must itself be stack-safe. Include the
    decision guide (deep-input test? -> stack safety is behavioral -> pick a stack-safe
    transformation, choosing by recursion shape). Frame the overlay (prefer (if -> while) and
    (variable -> assignment) above the recursion transformations) as an opinionated, source-
    sanctioned heuristic quoting FibTPP's language-specificity note -- explicitly NOT a contradiction
    of the canonical list and NOT dogma.

    Lift all TypeScript near-verbatim from TPP-TYPESCRIPT.md (tsc --strict-clean). All arrows ASCII
    "->"; all fenced code blocks ```ts. Do NOT link to any sibling reference file.
  </action>
  <verify>
    <automated>rg -n '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Engine reality present: `rg -F 'JavaScriptCore'` and `rg -in 'safari'` match; `rg -in 'spidermonkey'` matches; a statement that V8/Node do NOT implement proper tail calls is present (manual confirm).
    - Compat-table landmine handled: `rg -in 'false positive'` matches AND `rg -in 'chrome'` matches near the compat-table discussion; there is NO sentence asserting Node/Deno supports proper tail calls (`rg -in 'node.{0,20}(supports|has).{0,20}(proper )?tail call'` returns nothing; manual confirm).
    - CPS landmine handled: every occurrence of "CPS" is accompanied by "trampoline" in the file, and a naked-CPS-overflow warning is present (`rg -in 'cps'` matches; `rg -in 'trampoline'` matches; `rg -in 'only when|only with|requires a trampoline|paired with a trampoline'` matches).
    - Patterns present and treated per split: `rg -in 'trampoline'`, `rg -in 'generator'`, `rg -in 'fold|reduce'`, `rg -in 'mutual'` all match; the iterative default is presented as the TS/JS default.
    - Typing gotchas preserved: `rg -F 'Bounce<T>'` matches; explicit `Generator<` return typing present (`rg -F 'Generator<'`).
    - Transformation-vs-refactoring + decision guide present: `rg -in 'refactoring'` and `rg -in 'transformation'` match in the distinction paragraph; `rg -in 'deep input|deep-input'` matches; `rg -in 'decision guide'` (or an equivalent heading) matches.
    - Overlay is source-sanctioned, not dogma: `rg -in 'language.specific'` matches (the FibTPP quote) and `rg -in 'heuristic'` matches; no "ALWAYS" mandate on the overlay (`rg -in 'always prefer (if -> while)'` returns nothing).
    - All fenced code blocks are ```ts; self-contained (no sibling-reference links); ASCII-only (`rg -n '[^\x00-\x7F]' <file>` returns nothing).
  </acceptance_criteria>
  <done>The TCO section states the no-reliable-TCO reality, defuses the Node-2/2 and naked-CPS landmines, teaches iterative+trampoline and generator+fold while only mentioning CPS-with-trampoline and mutual-recursion trampolining, carries the transformation-vs-refactoring distinction and the deep-input decision guide, and frames the overlay as a source-sanctioned heuristic -- all ASCII-only and self-contained.</done>
</task>

<task type="auto">
  <name>Task 3: Mechanical + type-safety verification of the TypeScript/TCO file</name>
  <files>plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (the file just authored)
    - .planning/phases/03-lz-tpp-skill-authoring/03-RESEARCH.md ("Validation Architecture": mechanical checks and the optional TS extraction check; the three preserved landmines; Open Question 1 -- tsc is confirmatory, not a blocker)
    - .planning/phases/02-tpp-source-distillation/02-LEARNINGS.md (mechanical-check pattern; work-email allowlist gate that never spells the work email; line-wrap brittleness; verified-no-op recorded in SUMMARY)
  </read_first>
  <action>
    Run the full mechanical gate battery over typescript-and-tco.md: ASCII gate, TOC presence,
    both-katas presence, paradigm-divergence tokens, the CPS-needs-trampoline and Node-2/2-false-
    positive landmine asserts, engine-status tokens, typing-gotcha tokens (Bounce<T>, isNested,
    Generator<), the transformation-vs-refactoring + decision-guide checks, the overlay
    source-sanctioned/no-dogma check, the no-sibling-link guard, the work-email allowlist gate, and
    the transformations.md-unchanged git guard. Then run `claude plugin validate ./plugins/lz-tdd`.

    Confirmatory type-safety (patterns are pre-verified in TPP-TYPESCRIPT.md, so this is
    confirmatory, not a hard gate): extract the fenced ts blocks and run `tsc --strict --noEmit`.
    Because multiple blocks redeclare the same identifiers (trampoline, sumTo, Thunk, etc.), compile
    each block in isolation (or namespace them) to avoid cross-block redeclaration errors; the naked-
    CPS "still overflows" example is illustrative and need not be executed. Fix any failure in place
    (formatting/wording only; keep the lifted TS faithful). Record a verified-no-op in the SUMMARY if
    nothing needed changing.
  </action>
  <verify>
    <automated>claude plugin validate ./plugins/lz-tdd</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 and Task 2 acceptance checks re-run green (ASCII, TOC, katas, divergence tokens, engine status, CPS+trampoline, Node-2/2 false positive, typing gotchas, transformation-vs-refactoring, decision guide, overlay-not-dogma, no-sibling-link guard).
    - Work-email allowlist gate green (never spells the work email): `rg -oIN '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' <file> | rg -v '^larsbrinknielsen@gmail\.com$'` returns nothing.
    - Confirmatory tsc: each extracted ts block (compiled in isolation) is `tsc --strict --noEmit`-clean, except the deliberately-labelled naked-CPS-overflows illustration. Confirmatory only -- record the result in SUMMARY.
    - `claude plugin validate ./plugins/lz-tdd` exits 0 with the new file present.
    - transformations.md unchanged: `git diff --exit-code -- plugins/lz-tdd/skills/lz-tpp/references/transformations.md` exits 0; transformations.md is absent from this plan's files_modified.
  </acceptance_criteria>
  <done>All mechanical, landmine, hygiene, and (confirmatory) type-safety gates pass for typescript-and-tco.md; the skill tree validates; failures were fixed in place and verified-no-op recorded in SUMMARY if nothing changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored reference file -> public git repo | Committed TS/TCO guidance becomes a public artifact; misinformation or hygiene errors ship publicly. |
| TCO guidance -> reader's engineering decisions | Wrong stack-safety advice (CPS-as-fix, trusting Node 2/2) would lead readers to ship code that overflows in production. |
| fenced TS -> reader's copy-paste | Non-strict-clean or mis-narrowed TS would fail to compile when lifted; typing gotchas must survive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering (misinformation) | CPS pattern | mitigate | Every CPS mention states it is stack-safe ONLY with a trampoline; the naked-CPS example is labelled NOT a fix; rg gate asserts trampoline accompanies CPS (Pitfall 3). |
| T-03-03-02 | Tampering (misinformation) | Engine/compat-table claim | mitigate | Flag the kangax "Node 2/2" cell as a false positive; cite Chrome 0/2 + the empirical V8 overflow; rg gate forbids any "Node supports proper tail calls" assertion (Pitfall 4). |
| T-03-03-03 | Tampering (source-fidelity + type safety) | Lifted TypeScript | mitigate | Lift patterns near-verbatim from the verified TPP-TYPESCRIPT.md; preserve Bounce<T>, custom isNested guard, explicit generator return typing, no DOM-`Node` name; confirmatory per-block `tsc --strict --noEmit` (Pitfall 5). |
| T-03-03-04 | Information Disclosure | Committed reference content | mitigate | Work-email allowlist gate (Task 3) that filters out the public gmail; check never spells the work-email literal. |
| T-03-03-05 | Tampering (integrity/hygiene) | Non-ASCII bytes / arrow glyphs | mitigate | ASCII-only gate; arrows as `->`; keep asserted tokens on one line (Phase-2 line-wrap lesson). |
| T-03-03-06 | Tampering (locked-source integrity) | transformations.md | mitigate | Git `--exit-code` guard proves transformations.md is byte-unchanged; it is excluded from files_modified. |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; fenced TS uses only standard library/language features (RESEARCH Package Legitimacy Audit = not applicable). Optional confirmatory tsc uses the already-installed global toolchain. |
</threat_model>

<verification>
- File exists, exceeds 100 lines, opens with a TOC, and is ASCII-only.
- Both katas present with the #9-vs-#10/#13 divergence; Kata 2 typing lessons preserved.
- No-reliable-TCO reality + Node-2/2-false-positive + CPS-needs-trampoline landmines defused; teach-vs-mention split honored; transformation-vs-refactoring distinction + decision guide + source-sanctioned overlay present.
- No link to any sibling reference file; work-email absent; transformations.md unchanged; confirmatory tsc clean; `claude plugin validate ./plugins/lz-tdd` passes.
</verification>

<success_criteria>
- ROADMAP Phase 3 SC3 (paired functional and imperative TS examples showing paradigm-driven priority shift) and SC4 (TS/JS guidance addresses the lack of reliable TCO and covers trampoline / generator / CPS-with-trampoline and when to prefer iteration) are satisfied by this file.
- TPP-05 and TPP-07 are addressed by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-lz-tpp-skill-authoring/03-03-SUMMARY.md` when done.
</output>
