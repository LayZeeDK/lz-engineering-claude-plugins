---
phase: 14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k
plan: 04
type: execute
wave: 1
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - .claude/skills/lz-refactor-workspace/e2e-nx/results/recommend/invoke_skill
  - .claude/skills/lz-refactor-workspace/e2e-nx/results/recommend/code_review
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/recommend/invoke_skill
  - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/recommend/code_review
autonomous: false
requirements: [D-12, D-10, D-09, D-01]
must_haves:
  truths:
    - "Before any spend, all Wave-0 offline self-checks are green and the exact run commands + spend estimate are presented to the user, and execution HALTS for explicit approval (no self-approval)"
    - "After approval, the two arms (lz-refactor = invoke_skill, code_review) run in recommend mode over the three cells (nx enforce-module-boundaries, nx runtime-lint-utils, kata gilded-rose) at k=3, serial, in the synthetic worktree, opus-4-8 @ high, --setting-sources project"
    - "Each run persists answer.md + meta.json (with the new token/tool fields) under results/recommend/<arm>/<cell>/run-<k>/"
    - "After all runs complete, both borrowed repos are git-status clean with no review-* branch/worktree left behind"
  artifacts:
    - path: ".claude/skills/lz-refactor-workspace/e2e-nx/results/recommend/code_review"
      provides: "code_review arm answer.md + meta.json for the two nx cells"
    - path: ".claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/recommend/invoke_skill"
      provides: "lz-refactor arm answer.md + meta.json for the kata cell"
  key_links:
    - from: "the spend gate"
      to: "run-e2e.mjs --arm code_review / invoke_skill --synthetic-base"
      via: "approved run commands executed only after the blocking human approval"
      pattern: "synthetic-base"
  decisions: |
    - **D-01:** lz-refactor runs recommend mode (read-only, Edit/Write/Bash blocked) invoked explicitly as /lz-tdd:lz-refactor via the invoke_skill arm; code_review runs /mattpocock-skills:code-review <ROOT_SHA>; both produce findings/recommendations, NOT applied diffs.
    - **D-09:** Reuse both suites' targets -- representative subset: kata gilded-rose.ts (G1) + two nx single files, enforce-module-boundaries.ts (T1, mechanical) and runtime-lint-utils.ts (T3/T4, functional/FP); the gate may trim to 2 cells to bound spend.
    - **D-10:** Two arms per cell -- lz-refactor (invoke_skill, shipped plugins/lz-tdd) vs code_review (mattpocock plugin-dir); k=3, serial (run-e2e.mjs runs one claude -p at a time), claude-opus-4-8 @ high, --setting-sources project, PONYTAIL off, MCP + user-plugins stripped except the plugin under test; reuse the Phase-11/13 locked config; DEFER the optional base no_skill third arm (Phase 13 characterized base).
    - **D-12:** BUILD all run commands, then HALT for explicit user approval before executing ANY metered claude -p run -- no self-approval, even in an --auto pass; grading (oracle calls on the Claude plan) runs only AFTER outputs exist in Wave 2.
---

<objective>
The single metered plan. It HALTS for the standing eval-run-approval gate (D-12) before spending anything,
presenting the exact commands and a spend estimate, then -- only after explicit approval -- runs the two arms
in recommend mode over the three cells at k=3 in the synthetic worktree, and confirms both borrowed repos are
left pristine.

Purpose: isolate ALL metered `claude -p` spend behind one blocking human gate, exactly as the prior gated
eval plans (11-04, 12-03, 13-02).
Output: `answer.md` + `meta.json` per run under each suite's `results/recommend/<arm>/<cell>/run-<k>/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESEARCH.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md

Cells (single file each, both arms see the byte-identical pristine file via the synthetic worktree):
- nx cr-emb -> enforce-module-boundaries.ts (mechanical: Long Function, Conditional Complexity, Duplicated Code)
- nx cr-rlu -> runtime-lint-utils.ts (functional/FP: loop->pipeline, reduce->Map)
- kata cr-gr -> app/gilded-rose.ts (pattern-directed: conditional complexity, magic numbers, primitive obsession)

Default plan = 3 cells x 2 arms x k=3 = 18 metered runs. The gate may trim to the 2-cell minimum (kata + nx
enforce-module-boundaries = 12 runs).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Eval-run approval gate (D-12) -- HALT for spend approval</name>
  <files>(none -- pre-execution spend gate; no files modified)</files>
  <action>
    Confirm the two Wave-0 offline self-checks are green (`node e2e-nx/selfcheck-code-review.mjs` and
    `node grading/head-to-head/tabulate-mechanical.mjs --selfcheck` both exit 0), then present the exact run
    commands + run count + spend estimate below to the user and HALT. Do NOT execute any metered run until the
    user explicitly selects an approve option. No self-approval, even in an --auto pass (D-12).
  </action>
  <decision>Approve the metered claude -p runs for the head-to-head, and confirm the target subset.</decision>
  <context>
    Every arm run spends metered `claude -p` (a separate pool from the Claude plan). Per the standing
    eval-run-approval gate, no run executes without explicit approval -- not even in an --auto pass. All Wave-0
    offline self-checks (14-01 selfcheck-code-review, 14-03 tabulate-mechanical --selfcheck) must be green
    FIRST; confirm them, then present the exact commands and the run count / spend estimate below.
    Present these exact commands (serial, k=3, synthetic worktree, opus-4-8 @ high):
      node e2e-nx/run-e2e.mjs --mode recommend --synthetic-base --arm invoke_skill --prompt cr-emb --prompt cr-rlu --runs 3
      node e2e-nx/run-e2e.mjs --mode recommend --synthetic-base --arm code_review  --prompt cr-emb --prompt cr-rlu --runs 3
      node e2e-nx/run-e2e.mjs --suite ../e2e-gilded-rose --mode recommend --synthetic-base --arm invoke_skill --prompt cr-gr --runs 3
      node e2e-nx/run-e2e.mjs --suite ../e2e-gilded-rose --mode recommend --synthetic-base --arm code_review  --prompt cr-gr --runs 3
    State: 18 metered runs at the 3-cell default (12 at the 2-cell minimum); PONYTAIL off; MCP + user-plugins
    stripped except the plugin under test; both borrowed repos will be built into throwaway worktrees and torn
    down. The invocation's "Approve spend" is the owner's standing intent, honored WHEN this gate is presented
    -- the gate is still PRESENTED; approval is not silently assumed.
  </context>
  <options>
    <option id="approve-3cell">
      <name>Approve the 3-cell default (18 runs)</name>
      <pros>Full mechanical + functional + pattern-directed coverage; richest material for the idiom/pattern axis</pros>
      <cons>Largest spend (18 metered runs)</cons>
    </option>
    <option id="approve-2cell">
      <name>Trim to the 2-cell minimum (12 runs: kata cr-gr + nx cr-emb)</name>
      <pros>Bounds spend; still covers mechanical + pattern-directed</pros>
      <cons>Drops the functional/FP nx cell (cr-rlu)</cons>
    </option>
    <option id="abort">
      <name>Do not run</name>
      <pros>Zero spend</pros>
      <cons>No head-to-head data; phase cannot produce 14-RESULTS.md</cons>
    </option>
  </options>
  <resume-signal>Select: approve-3cell, approve-2cell, or abort</resume-signal>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs</automated>
    <automated>node .claude/skills/lz-refactor-workspace/grading/head-to-head/tabulate-mechanical.mjs --selfcheck</automated>
  </verify>
  <done>The Wave-0 self-checks are green, the exact commands + spend estimate were presented, and the user explicitly selected an approve/abort option before any spend.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the approved runs + confirm borrowed repos pristine</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/results/recommend, .claude/skills/lz-refactor-workspace/e2e-gilded-rose/results/recommend</files>
  <read_first>
    - run-e2e.mjs `main`/`runOne` (the run + persist path) and the resume-skip at lines 319-332. CLAUDE.md "skill-creator workflow" (wait for ALL completion notifications before grading; total_tokens/duration_ms only available at completion) + the metered-run background discipline. 14-RESEARCH.md Pitfall 6 (teardown) + "Meta Capture Detail" A1/A2 (confirm sub-agent token roll-up + spawn count on the FIRST code_review run).
  </read_first>
  <action>
    Only after the Task 1 approval, run the approved command set (drop cr-rlu if approve-2cell). Run serially (run-e2e.mjs runs one `claude -p` at a time); prefer launching each command in the background and WAITING for its completion notification before the next, per the eval discipline -- do not poll or grade early. On the FIRST code_review run, confirm the two measurement-boundary assumptions from RESEARCH: A1 (whole-session `total_cost_usd`/`model_usage` roll up the sub-agents vs main-context `usage`) and A2 (the sub-agent SPAWN count, expected ~1-2). Record which token field is the fair cross-arm headline in the SUMMARY. After all runs complete, assert BOTH borrowed repos are pristine: `git -C D:/projects/github/nrwl/nx status --porcelain` empty + no `review-*` branch/worktree, and the same for the kata repo; if the runner's finally-teardown missed anything, remove it and note it. Do NOT grade here -- grading is Wave 2 (14-05).
  </action>
  <acceptance_criteria>
    - Every approved (cell, arm, k) run has `answer.md` + `meta.json` under `results/recommend/<arm>/<cell>/run-<k>/`, with the new token/tool meta fields populated.
    - Both borrowed repos report empty `git status --porcelain` and no `review-*` branch/worktree after the runs.
    - The SUMMARY records the A1 headline-token decision + the A2 sub-agent spawn count observed on code_review.
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --report</automated>
  </verify>
  <done>All approved runs are captured with token/tool meta; borrowed repos are pristine; no grading yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner -> borrowed repos (nx, kata) | Metered runs execute in throwaway worktrees of repos we do not own |
| code_review arm -> third-party plugin | Loads /mattpocock-skills via --plugin-dir (all ~22 skill descriptions load into context) |
| metered pool | Each run spends real, separately-billed tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | runs writing to borrowed repos | mitigate | Recommend tool profile (Edit/Write blocked both arms; code_review keeps Bash but not Edit/Write); runs happen in a throwaway worktree, never the pristine tree |
| T-14-02 | Tampering | leftover branch/worktree after runs | mitigate | Runner finally-teardown; Task 2 asserts `git status --porcelain` empty + no review-* in BOTH repos, cleans up any residue |
| T-14-05 | Process (unapproved spend) | metered claude -p | mitigate | D-12 HARD blocking gate: build-then-halt; runs execute ONLY after explicit approval; no self-approval |
| T-14-08 | Information Disclosure | loading the third-party mattpocock plugin | accept | code_review arm blocks Edit/Write; the plugin is user-trusted + already cached; minor context inflation is inherent and noted as a caveat (RESEARCH A4) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | N/A -- no packages installed; runs use the already-cached plugin + already-installed CLI |
</threat_model>

<verification>
- Task 1 blocking gate is presented and explicitly approved BEFORE any run (D-12).
- `node run-e2e.mjs --report` lists the captured runs with the new token/tool meta.
- Both borrowed repos are `git status` clean with no `review-*` residue.
- Grading is NOT performed in this plan.
</verification>

<success_criteria>
The head-to-head runs execute only after an explicit spend approval, produce token/tool-instrumented recommend-mode outputs for both arms across the approved cells at k=3, and leave both borrowed repos pristine.
</success_criteria>

<output>
Create `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-04-SUMMARY.md` when done.
</output>
