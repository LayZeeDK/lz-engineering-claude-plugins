---
phase: 21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/suite.json
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json
  - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/r1-next-test.md
  - .gitignore
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - EVL-03
must_haves:
  truths:
    - "run-e2e.mjs sources the tracked skill name(s) from suite.json (SUITE.trackSkills) with the lz-refactor/lz-tpp default preserved, so the existing lz-refactor suites still compose and parse byte-identically (D-11 reuse, one surgical edit)."
    - "The new e2e-red-gilded-rose suite composes all THREE own-skill arms via the reused driver: no_skill (no --plugin-dir), with_skill (--plugin-dir plugins/lz-tdd + natural prompt), invoke_skill (natural prompt prefixed with /lz-tdd:lz-red) -- genuine plugin auto-trigger, NOT pasted SKILL.md text (D-04)."
    - "The r1 prompt body is short, human-style, non-leading (never names Conjured or the expected assertion) and, for a given target, byte-identical across arms except the plugin/slash plumbing (D-02)."
    - "The Conjured anchor target is existing-compiling-API + missing-behavior: Item/GildedRose already compile, so a Conjured-degrades-by-2 test is tsc-clean AND fails on an assertion (not a compile error, not a false green) on current code (D-06 target-shape constraint; RESEARCH Pitfall 1)."
    - "targets.json flags the Conjured anchor as HIGH contamination (correctness parity expected -- a smoke anchor, not a discriminator) and encodes the discipline traps it stresses (classify-first, don't-touch-Item, assert-observable-behavior, coach-don't-drive-to-green) (D-01)."
    - "The discriminating 2nd/3rd target is NOT hard-locked in the suite; targets.json carries the RESEARCH qualification checklist for the user to confirm at the run gate (D-01 steer-at-gate)."
    - "Per-run byproducts under the new suite (results*/, run-*/, outputs/) are git-ignored and NO dependency is added to plugins/lz-tdd (D-12)."
    - "EVL-03 is formalized in REQUIREMENTS.md at plan time with the EVL-03.1..EVL-03.7 BUILD/RUN decomposition, the Traceability row, and the coverage counts updated 27 -> 28 (D-14)."
  artifacts:
    - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
    - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/suite.json
    - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json
    - .claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/r1-next-test.md
    - .gitignore (lz-red e2e-red-* results lines)
    - .planning/REQUIREMENTS.md (EVL-03 formalized)
  key_links:
    - "run-e2e.mjs extractResult() trackSkills <-> suite.json trackSkills (default keeps lz-refactor back-compat; the RED suite tracks lz-red + lz-tpp)"
    - "e2e-red-gilded-rose/suite.json skillCommand /lz-tdd:lz-red <-> the shipped plugins/lz-tdd/skills/lz-red skill (invoke_skill arm force-activation)"
    - "prompts/r1-next-test.md target path <-> targets.json GRC.file app/gilded-rose.ts (the byte-identical-except-path invariant)"
---

<objective>
Lay the RED apply eval's foundation: make the reused lz-refactor driver skill-name-agnostic (one
surgical edit), author the Gilded Rose `Conjured` RED apply suite config, git-ignore its run tree,
and formalize EVL-03 in REQUIREMENTS.md. This plan BUILDS config + a parameterization edit only; it
runs NO metered `claude -p` and adds NO grader logic (that is plan 21-02).

Purpose: Phase 21 reuses `.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs` verbatim via
`--suite` for the 3-arm apply drive. The ONLY driver change is to stop hardcoding the tracked skill
names (lz-refactor/lz-tpp) so the RED suite can track lz-red's auto-trigger. The suite config is the
RED corpus (D-01/D-02): a short, non-leading, human-style prompt against an existing-compiling-API
target with a missing behavior, so the produced test is tsc-clean AND assertion-red (D-06).

Output: the parameterized driver, the `e2e-red-gilded-rose` suite (suite.json + targets.json +
prompts/), the gitignore lines, and the EVL-03 requirement. All build-time; the metered run is gated
to 21-04.

## Locked decisions honored
- **D-01:** Anchor the corpus on the vendored Gilded Rose `Conjured` target (verified genuinely-red; flagged HIGH-contamination -> a correctness smoke anchor, NOT a discriminator). Do NOT hard-lock the discriminating 2nd/3rd target -- encode the RESEARCH qualification checklist in targets.json so the user confirms it before the gated run.
- **D-02:** The task is drive-the-next-failing-test on CURRENT code via a short, basic, human-style prompt ("what's the next failing test I should write here? add it"), byte-identical across arms except the plugin/slash plumbing; the prompt names the target path but never the expected test/assertion (non-leading). Grade the produced test file/diff, never coaching prose.
- **D-04:** THREE own-skill arms compose off the reused driver: no_skill (no --plugin-dir), with_skill (--plugin-dir plugins/lz-tdd + a natural prompt for GENUINE description auto-trigger), invoke_skill (natural prompt prefixed with /lz-tdd:lz-red). MUST-AVOID the Phase-20 conflation where "with_skill" pasted SKILL.md text -- run-e2e's with_skill is a real plugin load.
- **D-11:** Reuse run-e2e.mjs; the ONLY edit is parameterizing the tracked skill name(s) off suite.json (default preserves lz-refactor back-compat). Do not fork or rewrite the driver.
- **D-12:** Build config + the parameterization only; run nothing metered. Per-run byproducts git-ignored; NO dependency added to plugins/lz-tdd (dev-only workspace).
- **D-14:** Formalize EVL-03 in REQUIREMENTS.md at PLAN time with the EVL-03.1..EVL-03.7 BUILD/RUN decomposition mapping the 5 ROADMAP success criteria, in the EVL-01/EVL-02 "BUILT + deterministically verified this phase; empirical run user-gated" closure shape.

## Artifacts this phase produces (phase-wide manifest)
Scripts + config (dev-only `.claude/skills/lz-red-workspace/`, no shipped-code change):
- `e2e-red-gilded-rose/{suite.json, targets.json, prompts/r1-next-test.md}` (this plan) -- the RED apply corpus.
- `grade-red.mjs` + `fixtures/{red,green,compile,collect,notest}/` + `--selfcheck` (21-02) -- the D-06 correctness classifier + its offline fixtures.
- `tabulate-mechanical-red.mjs` + `--selfcheck` (21-03) -- mechanical dims + Pass@k/Pass^k on the correctness gate.
- `selfcheck-red.mjs` (21-03) -- composition + worktree base build/teardown + transcript parse + classifier crux + prompt-parity + the trackSkills regression assertion.
- `e2e-red-gilded-rose/EVAL-RESULTS.md` scaffold (21-03) -- blank Pass@k/Pass^k tables + reserved unbiased-reviewer slot + substance-only headline.
Reused verbatim: `run-e2e.mjs` (one edit, this plan), `merge-judge.mjs`, `oracle`/`oracle-reviewer` agents.
Edited in place: `run-e2e.mjs` (trackSkills parameterization), `.gitignore`, `.planning/REQUIREMENTS.md` (EVL-03).
NOT touched: `plugins/lz-tdd/**` (the skill under test).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-RESEARCH.md
@.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-CONTEXT.md

# The driver being edited + the closest suite analog to mirror:
@.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/suite.json
@.claude/skills/lz-refactor-workspace/e2e-gilded-rose/targets.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize run-e2e.mjs tracked skill names off suite.json (+ gitignore the RED run tree)</name>
  <files>.claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs, .gitignore</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs (the driver; focus extractResult() lines ~255-357 which hardcode lz-refactor/lz-tpp, runOne()'s meta write ~602-632, skillFlag() ~455-471, report() ~727-728, and the module-level SUITE parse ~57-66)
    - .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs (imports extractResult(raw) with ONE arg -- the edit MUST keep this call working)
    - .gitignore (lines ~46-64: the existing lz-red-workspace run-capture rules)
  </read_first>
  <action>
    Make the tracked-skill set suite-driven while preserving exact back-compat for the lz-refactor suites.

    1. Add a module-level constant after the SUITE parse: `const TRACK_SKILLS = SUITE.trackSkills || ['lz-refactor', 'lz-tpp'];`
    2. Change `extractResult(raw)` to `extractResult(raw, trackSkills = TRACK_SKILLS)` (default keeps the single-arg call in selfcheck-code-review.mjs working). Replace the hardcoded refactorHits/tppHits counters with a generic per-name hit map built by scanning each `tool_use` blob (lowercased JSON.stringify) for each name in trackSkills. Emit `used_skills` = an object mapping each tracked name -> hit count. RETAIN the existing back-compat fields on the returned object derived from the map: `usedRefactor`/`refactorHits` from `used_skills['lz-refactor']`, `usedTpp`/`tppHits` from `used_skills['lz-tpp']` (0 when the name is not tracked), so skillFlag()/report() keep working unchanged for the lz-refactor suites.
    3. In runOne()'s meta object, add `used_skills` alongside the existing used_refactor/used_tpp/refactor_hits/tpp_hits fields (do not remove any existing field).
    4. Do NOT change composePrompt/buildCmd/buildSyntheticBase/arm parsing/report -- the 3-arm plumbing (no_skill/with_skill/invoke_skill, --plugin-dir, slash prefix) is already correct (D-04) and reused as-is.
    5. Append gitignore lines under the existing lz-red-workspace block so the new suite's run tree is ignored: cover `.claude/skills/lz-red-workspace/e2e-red-*/**/results*/`, `.../e2e-red-*/**/run-*/`, and `.../e2e-red-*/**/outputs/`. The existing generic `**/results*/` and `**/run-*/` lz-red lines already cover most of this; add explicit e2e-red-* lines only if a path would otherwise escape them, and add a `work/` line only if any script writes an in-workspace scratch dir (prefer os.tmpdir() worktrees so nothing lands in the tracked tree). Keep the lz-refactor-workspace rules byte-unchanged.

    Do NOT inline the edited code here; author it against the read files. The change must be minimal and back-compat-preserving.
  </action>
  <verify>
    <automated>node --check .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs && node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --dry-run --arm all --prompt p1 >/dev/null && echo OK-driver</automated>
  </verify>
  <acceptance_criteria>
    - `node --check run-e2e.mjs` exits 0.
    - `node run-e2e.mjs --dry-run --arm all --prompt p1` (the default nx suite) exits 0 and still prints 3 `argv:` lines -- proves the default lz-refactor composition is unchanged.
    - `git grep -n "trackSkills" .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs` shows the suite-sourced constant.
    - `extractResult` still callable with a single `raw` arg (selfcheck-code-review.mjs unbroken); a re-run of `node .claude/skills/lz-refactor-workspace/e2e-nx/selfcheck-code-review.mjs` reaches its transcript-parse crux without a signature error (SKIP on the gitignored transcript is acceptable).
    - `.gitignore` gains e2e-red-* coverage; `git status --porcelain` shows only the intended edits.
    - `plugins/lz-tdd/**` is untouched (`git status --porcelain plugins/lz-tdd` empty).
  </acceptance_criteria>
  <done>run-e2e.mjs tracks skills from suite.json (lz-refactor default preserved), the RED run tree is git-ignored, and no lz-refactor behavior regressed.</done>
</task>

<task type="auto">
  <name>Task 2: Author the e2e-red-gilded-rose RED apply suite (suite.json + targets.json + r1 prompt)</name>
  <files>.claude/skills/lz-red-workspace/e2e-red-gilded-rose/suite.json, .claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json, .claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/r1-next-test.md</files>
  <read_first>
    - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/{suite.json, targets.json} (the closest suite/target analog to mirror -- shape, repo path, applyBase, protectedBranches)
    - .claude/skills/lz-refactor-workspace/e2e-gilded-rose/prompts/gr1-update-quality.md (the short, non-leading prompt-body style to mirror)
    - plugins/lz-tdd/skills/lz-red/SKILL.md (lines ~95-118: the applyDiscount worked example -- the compiling-stub-with-wrong-body shape that IS the D-06 target shape)
    - .planning/phases/21-.../21-RESEARCH.md ("## D-01: OSS target selection" candidate table + the qualification checklist; "Pitfall 1/3/4" runner + strict-scope constraints)
    - The kata on disk: D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript/{app/gilded-rose.ts, vitest.config.ts, test/vitest/gilded-rose.spec.ts, package.json} (Item ctr is untyped -> Pitfall 3 differential-tsc scope; vitest 0.28 include glob test/vitest/**/*.spec.ts + @/ alias; jest 29 also configured)
  </read_first>
  <action>
    Create `.claude/skills/lz-red-workspace/e2e-red-gilded-rose/`:

    suite.json -- `{ name: "red-gilded-rose", repo: "D:/projects/github/emilybache/GildedRose-Refactoring-Kata/TypeScript", applyBase: "main", protectedBranches: ["main","master"], skillCommand: "/lz-tdd:lz-red", trackSkills: ["lz-red","lz-tpp"], prompts: [ { id: "r1", file: "r1-next-test.md", target: "GRC", code: true } ] }`. (Mirror e2e-gilded-rose/suite.json exactly except name/skillCommand/trackSkills/prompts.)

    targets.json -- one target GRC for the Conjured anchor. Include: `file: "app/gilded-rose.ts"`, `symbol: "GildedRose.updateQuality (Conjured branch)"`, a `runner` object naming the target's OWN toolchain the D-06 gate will shell into (RESEARCH Pitfall 4) -- record BOTH the vitest command (`npx vitest run <producedTestFile> --reporter=json`) and the jest command (`npx jest <producedTestFile> --json`), and a `runner_preference` note that jest 29's --json shape is the more stable one (A1) so grade-red / the gate can prefer it if the vitest 0.28 shape drifts, `test_dir: "test/vitest/"` (the include glob location so a produced vitest spec is collected), `behavior_gap`: updateQuality has no Conjured branch (only Aged Brie / Backstage / Sulfuras / normal); a test asserting "Conjured degrades by 2" compiles because Item + GildedRose already exist, and FAILS on the assertion because current code degrades by 1, `expected_red_color: "genuinely_red"`, `red_shape_rationale`: existing-compiling-API + missing-behavior (D-06 / Pitfall 1) -- tsc-clean AND assertion-red, not a compile error and not a false green, `constraint: "do NOT modify Item"` (the produced test asserts observable behavior only; drove_to_green if production files change to make it pass), `discipline_traps`: [classify-first (RED not GREEN/REFACTOR), assert-observable-behavior, coach-don't-drive-to-green, lz-tpp handoff], `contamination: "HIGH"` with a note that the Conjured spec is fully documented (Conjured Mana Cake; degrade 2, and 4 after sell-by; never below 0) so correctness PARITY across arms is EXPECTED here -- this target is a correctness SMOKE anchor, not a discriminator (D-01 / Pitfall 5), `strict_scope_note`: the Item constructor params are untyped, so the D-06 gate must DIFFERENTIAL-typecheck (NEW errors attributable to the produced test, not the pre-existing non-strict source) -- RESEARCH Pitfall 3.

    Also add a top-level `discriminating_target_checklist` array in targets.json = the 7-point RESEARCH qualification checklist verbatim-in-spirit (small + Vitest/Jest + offline-vendorable; public API exists + compiles; a behavior is wrong/missing so a correct test fails on an ASSERTION; genuine bug not a design choice; low contamination preferred or flagged; byte-identical short prompt works without leading; stresses >= 1 RED-discipline axis) with a note: the discriminating 2nd/3rd target is confirmed by the USER at the run gate (D-01), NOT hard-locked here; if a NEW real-OSS repo is nominated, run the package-legitimacy gate on it before vendoring.

    prompts/r1-next-test.md -- a SHORT (1-3 sentence) human-style body. Names the target path `app/gilded-rose.ts` and states the tests are green; asks for the next failing test to write and to add it. It MUST NOT name Conjured, "degrades by 2", or any expected assertion (non-leading, D-02). Mirror the register of gr1-update-quality.md. Byte-identical across arms is guaranteed by the driver (arms differ only by plugin/slash), so the body itself carries no arm-specific text.

    ASCII-only, no emoji/Unicode (project hygiene); no maintainer work-email or its domain anywhere.
  </action>
  <verify>
    <automated>node -e "const s=require('./.claude/skills/lz-red-workspace/e2e-red-gilded-rose/suite.json'),t=require('./.claude/skills/lz-red-workspace/e2e-red-gilded-rose/targets.json');if(s.skillCommand!=='/lz-tdd:lz-red')throw'skillCommand';if(JSON.stringify(s.trackSkills)!==JSON.stringify(['lz-red','lz-tpp']))throw'trackSkills';if(!s.prompts.length||s.prompts[0].id!=='r1')throw'prompts';if(!t.targets.find(x=>x.id==='GRC'))throw'GRC';if(!t.discriminating_target_checklist)throw'checklist';console.log('OK-suite')"</automated>
  </verify>
  <acceptance_criteria>
    - suite.json + targets.json parse as JSON; suite.json has skillCommand `/lz-tdd:lz-red`, trackSkills `["lz-red","lz-tpp"]`, one r1 prompt targeting GRC.
    - targets.json GRC carries file `app/gilded-rose.ts`, a runner object (vitest + jest commands + preference note), test_dir, behavior_gap, expected_red_color `genuinely_red`, the do-not-modify-Item constraint, discipline_traps, contamination HIGH, and the differential-typecheck strict_scope_note.
    - targets.json carries the top-level discriminating_target_checklist (7 points) with the steer-at-gate + package-legitimacy note.
    - prompts/r1-next-test.md is <= ~3 sentences, names `app/gilded-rose.ts`, and does NOT contain the tokens "Conjured", "degrade", or any expected assertion value (`git grep -niE "conjured|degrade" .claude/skills/lz-red-workspace/e2e-red-gilded-rose/prompts/` returns nothing).
    - `node run-e2e.mjs --suite .claude/skills/lz-red-workspace/e2e-red-gilded-rose --dry-run --mode recommend --arm all --prompt r1` exits 0 and prints 3 argv lines (composition sanity; recommend mode needs no --cwd). [The apply-mode run itself is gated to 21-04.]
    - All new files ASCII-only; no work-email/domain present.
  </acceptance_criteria>
  <done>The RED apply corpus exists: a genuinely-red, non-leading, contamination-flagged Conjured anchor + the steer-at-gate checklist for the discriminating target; the reused driver composes all 3 arms against it.</done>
</task>

<task type="auto">
  <name>Task 3: Formalize EVL-03 in REQUIREMENTS.md (EVL section + Traceability + coverage counts)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "### Skill-effectiveness evals (EVL)" section with EVL-01/EVL-02, the Traceability table, and the Coverage block "27 total / 27 mapped")
    - .planning/phases/21-.../21-RESEARCH.md ("## Phase Requirements" table -- the EVL-03.1..EVL-03.7 BUILD/RUN decomposition mapping the 5 ROADMAP success criteria)
    - .planning/ROADMAP.md (Phase 21 section, the 5 success criteria EVL-03 maps)
  </read_first>
  <action>
    Add EVL-03 to REQUIREMENTS.md as a deterministic doc edit (no code):

    1. In "### Skill-effectiveness evals (EVL)", add:
       `- [ ] **EVL-03**: An APPLY-based, 3-arm, multi-dimensional RED eval -- short human prompts drive the next failing test in >= 1 real OSS TypeScript repo across no_skill / with_skill / invoke_skill arms; the produced test file/diff is graded on a correctness GATE (tsc --strict differential clean AND genuinely red for the right reason) plus lift dims (wall-clock, tokens, tools, output quality, book/source authenticity via oracle-reviewer, idioms, house style, TDD RED practices), with Pass@k/Pass^k + >= 1 unbiased reviewer and a substance-only headline. (Phase 21 BUILT + deterministically verified the instrument/harness/grader/selfcheck this phase; the empirical metered run is user-gated per D-11 -- Pending until run.)`
       Then a compact sub-criteria block listing EVL-03.1..EVL-03.7 with their BUILD/RUN tag and the ROADMAP success criterion each maps (copy the RESEARCH decomposition faithfully).
    2. Add a Traceability row: `| EVL-03 | Phase 21 | Pending (build complete; empirical run gated) |`.
    3. Update the Coverage block: total 27 -> 28, mapped 27 -> 28, unmapped 0.
    4. Update the "Last updated" footer line to note EVL-03 formalized at Phase 21 plan time.

    Keep the exact EVL-01/EVL-02 phrasing shape ("BUILT + deterministically verified this phase; empirical run user-gated"). ASCII-only; no work-email/domain.
  </action>
  <verify>
    <automated>git grep -n "EVL-03" .planning/REQUIREMENTS.md | head && node -e "const fs=require('fs');const t=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');if(!/EVL-03/.test(t))throw'no EVL-03';if(!/28 total/.test(t))throw'count not 28';if(!/EVL-03 \| Phase 21/.test(t))throw'no traceability row';console.log('OK-req')"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains an EVL-03 bullet in the EVL section with the BUILT-this-phase / run-gated closure shape and the EVL-03.1..EVL-03.7 sub-criteria (each tagged BUILD or RUN, each mapping a ROADMAP success criterion).
    - The Traceability table has `| EVL-03 | Phase 21 | Pending (build complete; empirical run gated) |`.
    - Coverage reads 28 total / 28 mapped / 0 unmapped.
    - File is ASCII-only; no work-email or its domain present.
  </acceptance_criteria>
  <done>EVL-03 is a formal, traceable requirement mapping the 5 Phase-21 success criteria via the BUILD/RUN sub-criteria, in the EVL-01/EVL-02 closure shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval-config -> reused driver | suite.json/targets.json/prompts are read by run-e2e.mjs to compose `claude -p`; a malformed/leading prompt would bias the eval (integrity, not security) |
| committed artifacts -> public repo | REQUIREMENTS.md + suite config are tracked in a public repo (info-disclosure surface for maintainer PII) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Information Disclosure | committed suite config + REQUIREMENTS.md | low | mitigate | ASCII-only + allowlist-inversion (no maintainer work-email/domain as a literal or needle); per-run transcripts git-ignored (this plan adds the ignore lines) |
| T-21-EVAL | Tampering (eval integrity, not security) | prompts/r1-next-test.md | low | mitigate | Non-leading prompt (never names Conjured/the assertion); byte-identical across arms enforced by the driver + verified by 21-03 selfcheck-red prompt-parity crux |
</threat_model>

<verification>
- `node --check run-e2e.mjs` exits 0; the default nx-suite dry-run still prints 3 argv lines (no lz-refactor regression).
- The RED suite dry-run (`--suite e2e-red-gilded-rose --mode recommend --arm all --prompt r1`) composes 3 arms.
- suite.json/targets.json parse; GRC + discriminating_target_checklist present; prompt is non-leading (no Conjured/degrade/assertion tokens).
- REQUIREMENTS.md: EVL-03 + Traceability row + 28/28 coverage.
- `git status --porcelain plugins/lz-tdd` empty (skill under test untouched); no metered run occurred.
</verification>

<success_criteria>
The reused driver is skill-name-agnostic (lz-refactor back-compat intact), the Gilded Rose Conjured RED
apply suite exists (genuinely-red, non-leading, contamination-flagged, with a steer-at-gate checklist),
its run tree is git-ignored, and EVL-03 is a formal traceable requirement -- all offline, zero spend,
plugins/lz-tdd untouched.
</success_criteria>

<output>
Create `.planning/phases/21-applied-red-eval-in-real-oss-repos-multi-dimensional-3-arm-i/21-01-SUMMARY.md` when done.
</output>
