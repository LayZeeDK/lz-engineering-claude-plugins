---
context: default
phase: 17-complete--followup-queued
task: null
total_tasks: 7
status: queued-followup
last_updated: 2026-07-19T20:26:07.157Z
---

# Continue here -- Follow-up #2: Phase-16 Beck owned-source upgrades

> Phase 17 is COMPLETE and committed. This handoff queues a specific follow-up for a NEW session
> to run via `/gsd:quick --full --auto`. There are NO blocking constraints -- the previous session
> succeeded. The items below are advisory guidance for doing the follow-up well.
> (Supersedes the stale 2026-07-17 lz-tdd@0.0.2-close handoff.)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Upgrade a surface with no full-text source | Tagging a surface "owned; oracle-verified" when its only source is a book summary (`Access: book`, 1-2 sentences) | advisory | Only upgrade surfaces backed by a FULL-TEXT essay (`Access: free`) in `.oracle/written-content/`. Leave the rest no-oracle and say so explicitly. |
| Main context reads `.oracle/` prose | Breaks the DST-04 clean-room; the author must stay blind | advisory | Read only the `.oracle/*/index.md` navigation files. The orchestrator drives the `oracle-reviewer` agent; the author edits blind. |
| Executor spawns review agents | gsd-executor has no Agent/Task tool | advisory | Orchestrator drives all `oracle-reviewer` gates after the deterministic edits. |
| Removing a load-bearing instrument token | `check-red-references.mjs` asserts content tokens (e.g. `ADV-0`) | advisory | Re-run the battery after every edit; only touch attribution/tier text, not asserted tokens. |

<current_state>
Phase 17 (Assertion Design, Stance Router & TS/Vitest Mechanics) is fully closed: executed ->
verified 14/14 PASS -> secured (0 open threats) -> validated (nyquist compliant) -> learnings extracted
+ 12 pooled to the global store. Working tree is clean. This session also corrected a message-matrix
provenance bug (99 Bottles -> Metz "The Magic Tricks of Testing" talk) and upgraded functional-core
(Bernhardt Boundaries) + Test Desiderata (Beck) from no-oracle to owned.

The QUEUED work is the next logical owned-source upgrade, now enabled because the maintainer
provisioned Kent Beck's essays into `.oracle/written-content/`.
</current_state>

<completed_work>

This session (Phase 17), all committed:
- Executed 6 plans (instrument-first Wave 0; assertions slice; testing-stance router + 3 leaves;
  message matrix + Vitest mechanics; anti-patterns; finalize gate).
- Fixed the message-matrix provenance (Metz "The Magic Tricks of Testing" talk + POODR corroboration),
  re-gated PASS.
- Upgraded functional-core -> Bernhardt "Boundaries" (owned) and Test Desiderata -> Beck (owned);
  added owned Metz "Design of Tests" backing for listen-to-tests + keep-doubles-honest. All 6 owned
  surfaces oracle-reviewer PASS.
- Reconciled provenance across 17-CONTEXT / 16-CONTEXT / PROJECT / REQUIREMENTS; noted FUT-METZ-REFACTOR.
- Closeout: gsd-verifier 14/14, secure 0-open, validate nyquist-compliant, 17-LEARNINGS + 12 pooled.
- Ran a 21-talk oracle survey mapping the Metz library to the roadmap.
</completed_work>

<remaining_work>

Follow-up #2 -- upgrade the currently-no-oracle Beck-backed selection/structure surfaces to owned +
oracle-verified, now that Beck's essays are provisioned:

1. Read `.oracle/written-content/index.md` (nav-only) and map each surface to a full-text essay.
2. `references/three-laws-and-test-selection.md`: "Running test list" + "Take one small step" ->
   owned against `canon-tdd.md` (Canon TDD).
3. `references/three-laws-and-test-selection.md`: "Triangulate to the next test" -> owned against
   `tri-first-one-then-many.md` (First One, Then Many).
4. `references/test-structure-and-assertions.md` (STR slice): "Assert-first" + "Evident test data" ->
   these live mainly in TDD-by-Example (`book-tdd-by-example.md`, `Access: book`, summary-only = NOT
   gateable). Upgrade ONLY if a free essay (e.g. Canon TDD, Desirable Unit Tests) actually covers the
   point; otherwise leave them no-oracle and note why.
5. `references/principle-backing.md`: flip the tier for each upgraded row (no-oracle -> owned) and
   update the Sources paragraph (it currently says Beck's selection/structure rows "remain no-oracle
   for now, pending a follow-up upgrade against his now-provisioned essays (Canon TDD, First One Then
   Many)" -- update that once done).
6. Orchestrator drives `oracle-reviewer` on each upgraded surface vs its Beck essay (converge-to-clean).
7. Keep the deterministic battery GREEN; add a Phase-16 provenance/upgrade note (this re-opens closed
   Phase-16 content, like the correction notes already added to 16/17-CONTEXT); commit.
</remaining_work>

<decisions_made>

- Only upgrade surfaces with a FULL-TEXT owned source; book-summary-only entries stay no-oracle.
- Follow the Phase-17 clean-room re-gate loop: edit attribution/tier -> oracle-reviewer gate against the
  correct `.oracle` source -> converge-to-clean. Content is likely already correct (well-known Beck TDD);
  the gate confirms and may request a wording revise.
- This is a legitimate re-open of closed Phase-16 shipped content, enabled by new source provisioning.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.oracle/written-content/index.md` -- which Beck essays (full-text vs book-summary) back each surface (nav-only; do NOT read the essay prose in main context).
2. `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md` and `test-structure-and-assertions.md` -- the surfaces to upgrade.
3. `plugins/lz-tdd/skills/lz-red/references/principle-backing.md` -- the tier map + the "pending follow-up upgrade" note to update.
4. `.planning/phases/17-assertion-design-stance-router-ts-vitest-mechanics/17-LEARNINGS.md` -- the clean-room re-gate pattern, the provenance-conflation lesson, and the instrument-token caveat.
5. `.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md` -- Phase-16 decisions + the provenance-correction note.
6. `./CLAUDE.md` + `./AGENTS.md` -- clean-room DST-04, gsd-executor-can't-spawn, ASCII/email hygiene, `rg` not `grep`, public-gmail identity.

## Infrastructure State
- Dev-only validation harness: `.claude/skills/lz-red-workspace/` (typescript 6.0.3, vitest 4.1.10). Battery: `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` + `npm --prefix .claude/skills/lz-red-workspace run typecheck` + `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` (all currently exit 0).
- `.oracle/` reorganized: transcripts under `.oracle/videos/` (+ `.oracle/videos/test-desiderata/`), written content under `.oracle/written-content/`, books at `.oracle/clean-code/` + `.oracle/99-bottles-2e-js/`.
- Working tree clean; `workflow._auto_chain_active` is false.

<context>
The maintainer is progressively building a rich owned-source `.oracle/` library and upgrading skill
surfaces from no-oracle (high-confidence core) to owned (oracle-verified) as sources land. Phase 17
proved the loop: provision source -> re-point/upgrade attribution -> oracle-reviewer gate -> converge.
This follow-up applies the same loop to the Phase-16 Beck surfaces. It is low-risk (the content is
already correct, well-known Beck TDD) but touches closed-phase shipped content, so re-gate and record
a note.

Other open follow-ups (NOT this task): #1 broader internal-ID / DST-04 3-skill polish; #3
FUT-METZ-REFACTOR lz-refactor Metz layer; and Phase 18 (coach procedure + lz-tpp seam wiring).
</context>

<next_action>
Start with: in a NEW session, run
`/gsd:quick --full --auto "Follow-up #2: Phase-16 Beck owned-source upgrades -- upgrade the no-oracle Beck-backed selection/structure surfaces (test list, one-step, triangulation; assert-first + evident-data only if a full-text essay covers them) to owned + oracle-verified against the Beck essays in .oracle/written-content/ (Canon TDD, First One Then Many); re-gate each via oracle-reviewer, keep the battery GREEN, update principle-backing tiers, add a Phase-16 upgrade note."`
Then read `.oracle/written-content/index.md` and map surfaces to full-text essays before editing.
</next_action>
