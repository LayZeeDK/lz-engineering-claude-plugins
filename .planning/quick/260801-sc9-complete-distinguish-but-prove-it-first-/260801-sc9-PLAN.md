---
phase: quick-260801-sc9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md
  - .claude/skills/lz-red-workspace/treatment/build-treatment.mjs
  - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
  - .planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FUT-TAXONOMY-SHARED
must_haves:
  truths:
    - "The reference battery EXITS 0 and, separately, its roster line reads 123 checks -- both asserted after every task (MEASURED at plan time on this HEAD: exit 0, roster line 123 checks, 0 FAILs). The exit code is the primary gate. Grepping the roster line alone is FAIL-OPEN: that line emits the substring whenever the emitted count equals the predicted literal, independently of rosterOk and of accumulated failures (:1044-1047), so a battery exiting 1 still prints it."
    - "plugins/ is unchanged for the whole task under BOTH git diff --quiet <base> -- plugins/ AND an empty git status --porcelain plugins/. The second leg is not redundant: git diff is blind to new UNTRACKED files, which is precisely the guard-evasion shape N3 exists to catch."
    - "Guard N3 stays green: no file under plugins/ has basename stem test-double-taxonomy, at any extension."
    - "One command, node build-treatment.mjs, regenerates the whole treatment plugin tree from tracked inputs, and the generated tree is git-ignored (MEASURED: out/lz-tdd-treatment is IGNORED)."
    - "run-e2e.mjs composes a NEW arm that force-invokes the skill against the treatment tree, while --arm all still expands to exactly the three pre-existing arms."
    - "selfcheck-red.mjs exits 0 after the runner edit, GATED ON THE EXIT CODE, so crux 1/2/6 are proven unaffected rather than assumed unaffected. A trailing echo of $? launders a failing run into a passing leg (MEASURED) and does not count as proof."
    - "The halt, UNRESOLVED-1 verbatim, and the newly-measured G17 ship-time blocker are recorded in tracked artifacts."
  artifacts:
    - .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md
    - .claude/skills/lz-red-workspace/treatment/build-treatment.mjs
    - .planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md
    - .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs
    - .planning/REQUIREMENTS.md
  key_links:
    - "build-treatment.mjs anchor -> the ONE inline-code beck-tdd-by-example citation line in the copied lz-red SKILL.md. MEASURED unique (git grep -c returns 1 across plugins/). The script must fail CLOSED when the anchor count is not exactly 1; a silent zero-match insertion produces a treatment tree whose artifact is never cited, and the A/B then measures nothing."
    - "TREATMENT_DIR in run-e2e.mjs -> the hardcoded OUT path in build-treatment.mjs. If these drift, the arm loads a stale or absent tree and the run silently degrades into a second baseline."
    - "the artifact filename in treatment/ -> the generated references/ filename -> the FUT-TAXONOMY-SHARED ship destination filename. All three carry the same stem on purpose; that stem is what N3 forbids INSIDE plugins/ and nowhere else (MEASURED: a planted stem file outside plugins/ leaves the battery at exit 0)."
---

<objective>
Complete steps 1 and 2 of the D-12 ruling "distinguish, but prove it first": author the runtime
test-double artifact (the treatment arm), make the treatment plugin tree reproducible from tracked
inputs, wire the A/B arm that loads it, and record the halt.

Purpose: D-12 requires the artifact be A/B'd before it ships. The build is zero-spend and can be
finished now; the proof is metered and cannot. This plan finishes the build so that the only thing
standing between here and a verdict is an owner decision plus an approval.

Output: a distilled artifact + a build script + a new runner arm + a RUN-GATE record, with
plugins/ provably untouched.

**This plan is BUILD-THEN-HALT and it COMPLETES at the metered gate.** No task here runs `claude -p`
or any other metered command. Step 3 (the ambiguous-prompt corpus) is owner-gated by UNRESOLVED-1 and
step 4 (the run) needs a fresh spend approval; both are OUT of scope and neither is planned.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-CONTEXT.md
@.planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RESEARCH.md
@AGENTS.md
@CLAUDE.md
</context>

<measured_baseline>
Every number below was MEASURED on this HEAD while planning, not inferred. Re-measure before
trusting any of it if the tree has moved.

- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` -> exit 0, roster line
  "123 checks", 0 FAILs.
- `git grep -c 'CLAUDE_PLUGIN_ROOT}/references/beck-tdd-by-example.md' -- plugins/` -> exactly one
  file, one hit: `plugins/lz-tdd/skills/lz-red/SKILL.md`. This is the insertion anchor.
- `git check-ignore -q out/lz-tdd-treatment/x.md` -> IGNORED (root `.gitignore:7`).
- `git check-ignore -q .claude/skills/lz-red-workspace/treatment/<file>` -> TRACKABLE. No
  `.gitignore` exists anywhere under `.claude/`.
- `check-hygiene.mjs` scan roots are `plugins/` plus four root files only. It does NOT see
  `.claude/skills/lz-red-workspace/treatment/`, so ASCII and email hygiene on the new files must be
  asserted explicitly by this plan rather than inherited from the battery.
- `run-e2e.mjs:169` hard-validates arm names against a literal list. A new arm name that is not
  added there is rejected by `parseArgs` before it can compose.
- `selfcheck-red.mjs:278` and `:3086` iterate the literal three-arm list and only ever read
  `arms['no_skill'|'with_skill'|'invoke_skill']`. An EXTRA key in the arm map is not an error.
- `run-e2e.mjs:936-941`: `--arm all` expands to exactly `with_skill, no_skill, invoke_skill`.
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: Author the distilled runtime test-double artifact, tracked and outside the shipped tree</name>
  <files>.claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md</files>
  <action>
Create the treatment content by distilling `.planning/research/test-double-taxonomy.md` (628 lines).
Do NOT re-derive from scratch and do NOT copy wholesale.

Location rationale, both halves measured: the directory is TRACKABLE (this artifact is the task's
deliverable and must not be lost to a gitignore) and it sits outside `plugins/`, which is N3's only
walk root. Do not name the directory `e2e-red-*` -- that is an auto-discovery namespace and would opt
a non-suite directory into the full selfcheck battery. Do not name it `run-*` or `results*` -- both
are gitignored and the deliverable would vanish.

KEEP, per RESEARCH Q3: the three axes, compressed; the operational rule at source `:308-311` (never
use the contested word bare, qualify it by side, and ask which side the developer means before
answering); the five Meszaros collaborator-side kinds with a one-line discriminator each; the
production-side naming rule; the Cooper-versus-Fowler false friend at source `:380-383`; the
per-cell citation rule in one sentence.

DROP: the entire 88-line inert-record header; all process and provenance narration; the
`Defines or uses`, `Source` and `Citability tier` columns; the four remediation rounds' history; the
Sources section; section 2's warrant argumentation; and every GoF row. The GoF rows are dropped
rather than paraphrased because canonical GoF Intent lines are this repo's single worst near-verbatim
offender class.

KEEP THE CENSUS AS A LIST. Do not draw a matrix grid. The surviving reason is the source's hard rule
on emptiness at `:143-149`: the document never asserts a cell is empty, and a grid invites a reader
to treat an empty box as a claim. That reason travels with the content regardless of who reads it.

DST-04 own-words is binding and the `pattern-leaf-intent-near-verbatim` trap applies directly. The
archived `Defining property` column is one canonical one-liner per row, so paraphrase every
discriminator BLIND, in original wording, from your own understanding -- do NOT copy or lightly edit
the archived column, which was written for a different document under a different review. Highest
risk after the GoF drop: the five Meszaros kinds, the Responder and Saboteur rows, Beck's row and
Bernhardt's two-property value criterion.

Target 60-100 lines. ASCII only, no emoji, no Unicode punctuation.

Open the file with a short head note stating three facts: this is dev-time treatment-arm content for
the D-12 A/B, it is NOT shipped and is NOT approved for shipping, and as authored it is incompatible
with guard G17 (see Task 3) so relocating it into `plugins/` would redden the battery.

Note for the record, not an action here: the distilled form is a NEW artifact for DST-04 purposes and
must be re-gated by `oracle-reviewer` before it is ever treated as shippable. The executor has no
Agent tool, so that gate is ORCHESTRATOR-driven after this plan returns; Task 3 queues it. It does
not block this task, because nothing here ships.
  </action>
  <verify>
    <automated>test -f .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md && ! git check-ignore -q .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md && echo TRACKABLE_OK</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 0 && echo BATTERY_GREEN</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -c '123 checks'</automated>
    <automated>git diff --quiet HEAD -- plugins/ && test -z "$(git status --porcelain plugins/)" && echo PLUGINS_UNTOUCHED</automated>
    <automated>! rg -q '[^\x00-\x7F]' .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md && echo ASCII_OK</automated>
    <automated>rg -c '@' .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md; test $? -eq 1 && echo NO_EMAIL_SHAPED_TOKEN</automated>
    <automated>L=$(wc -l < .claude/skills/lz-red-workspace/treatment/test-double-taxonomy.md); test "$L" -ge 60 && test "$L" -le 100 && echo "LINE_COUNT_OK=$L"</automated>
  </verify>
  <done>
The artifact exists, is git-trackable, is ASCII-only, carries no email-shaped token, is 60-100 lines
(enforced, not asserted), and every canonical discriminator in it is paraphrased rather than lifted.
The reference battery exits 0 AND its roster line still reads 123 checks; `plugins/` is clean under
both `git diff --quiet` and `git status --porcelain`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the treatment tree reproducible and wire the new A/B arm</name>
  <files>.claude/skills/lz-red-workspace/treatment/build-treatment.mjs, .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs</files>
  <action>
Two deliverables, both additive.

(a) `build-treatment.mjs` -- regenerate the treatment plugin tree from tracked inputs. Do NOT commit
an 837K duplicate of the plugin; git carries the artifact plus this script, and the tree is built on
demand.

The script's destination is a HARDCODED constant, `path.join(REPO_ROOT, 'out', 'lz-tdd-treatment')`,
with NO environment override. This is deliberate and load-bearing: the script's first action is a
recursive delete of that path, and an env-overridable destructive target is a foot-gun. Assert the
resolved destination is inside `REPO_ROOT` and its last two segments are `out` and `lz-tdd-treatment`
before deleting anything; exit non-zero otherwise.

Steps: remove the destination if present; recursive-copy `plugins/lz-tdd` into it; write the Task 1
artifact to `references/test-double-taxonomy.md` under the destination, keeping the same filename as
the FUT-TAXONOMY-SHARED ship destination; then apply the citation delta to the copied
`skills/lz-red/SKILL.md`.

The citation delta inserts ONE new bullet immediately after the existing beck-tdd-by-example bullet,
in the identical inline-code form that bullet already uses -- the plugin-root variable followed by
`/references/` and the artifact filename, wrapped in backticks, never as a Markdown link. Markdown
link syntax around that target fails guard N2, which existsSync-checks anything it classifies as
relative and a variable-prefixed path is classified relative yet can never resolve on disk. In the
`.mjs` source, write the variable reference inside a SINGLE-QUOTED JavaScript string, never a
template literal, or Node will substitute it away at build time.

FAIL CLOSED on the anchor: count occurrences of the anchor line in the copied SKILL.md and exit
non-zero unless the count is exactly 1. A zero-match insertion yields a treatment tree whose artifact
is never cited, which would make the A/B measure nothing while looking healthy.

Post-build assertions, all fail-closed, all printed: the destination root has a
`.claude-plugin/plugin.json`; that manifest's `name` is still `lz-tdd` (any other name breaks both
the forced slash command and the plugin-root variable resolution); the new citation string occurs
exactly once in the generated SKILL.md; and the generated tree differs from `plugins/lz-tdd` in
EXACTLY TWO files -- the added artifact and the edited SKILL.md. Print those two deltas by path and
exit 0.

(b) `run-e2e.mjs` -- add one new arm, named `invoke_treatment`, in place, four small edits and
nothing else. Use that exact name everywhere; the verify legs below assert it verbatim.

Add a `TREATMENT_DIR` const beside the existing competitor-plugin const, defaulting to
`path.join(REPO_ROOT, 'out', 'lz-tdd-treatment')` with an `LZ_TREATMENT_DIR` env override. The
override on THIS const is safe because the const is only ever read as a `--plugin-dir` value; the
existing `PLUGIN_DIR` const must NOT gain an override, because `selfcheck-red.mjs` crux 1 and 2
hard-assert that `with_skill` and `invoke_skill` resolve to `plugins/lz-tdd` for every suite, prompt
and mode, and its dry-run spawns with inherited environment -- an exported override would redden the
selfcheck from the operator's shell.

Add `invoke_treatment` to the literal validation list at `:169` and to the error message at `:170`.
In `composePrompt`, treat `invoke_treatment` exactly like `invoke_skill` so it force-prepends the
suite's slash command; this makes the existing `invoke_skill` arm its baseline and isolates a single
variable, the presence of the artifact in the plugin tree. In `buildCmd`, push `--plugin-dir
TREATMENT_DIR` for `invoke_treatment`.

Do NOT add `invoke_treatment` to the `both` or `all` fan-outs. Leaving it explicit-only keeps the `--arm
all` dry-run byte-for-byte what it is today, which is what crux 6 pins. Do NOT touch the default
apply preamble, the preamble map, or any other part of `composePrompt` -- crux 6 keeps its own copy
of the preamble as a tripwire against this file.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/treatment/build-treatment.mjs && echo BUILD_OK</automated>
    <automated>git check-ignore -q out/lz-tdd-treatment && test -z "$(git status --porcelain out)" && echo GENERATED_TREE_IGNORED</automated>
    <automated>test -f out/lz-tdd-treatment/references/test-double-taxonomy.md && rg -F -c 'CLAUDE_PLUGIN_ROOT}/references/test-double-taxonomy.md' out/lz-tdd-treatment/skills/lz-red/SKILL.md</automated>
    <automated>OUT=$(node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --suite .claude/skills/lz-red-workspace/e2e-red-srvx --mode apply --cwd . --arm invoke_treatment --prompt r1 --dry-run) && rg -q -F 'lz-tdd-treatment' <<< "$OUT" && rg -q -F '/lz-tdd:lz-red ' <<< "$OUT" && echo TREATMENT_ARM_OK</automated>
    <automated>OUT=$(node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --suite .claude/skills/lz-red-workspace/e2e-red-srvx --mode apply --cwd . --arm all --prompt r1 --dry-run) && rg -q -F 'with_skill' <<< "$OUT" && ! rg -q -F 'invoke_treatment' <<< "$OUT" && echo ALL_UNCHANGED</automated>
    <automated>node .claude/skills/lz-red-workspace/selfcheck-red.mjs; test $? -eq 0 && echo SELFCHECK_GREEN</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 0 && echo BATTERY_GREEN</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -c '123 checks'</automated>
    <automated>git diff --quiet HEAD -- plugins/ && test -z "$(git status --porcelain plugins/)" && echo PLUGINS_UNTOUCHED</automated>
  </verify>
  <done>
The build script regenerates the tree, reports exactly two deltas, and fails closed on a missing or
duplicated anchor. The generated tree is gitignored and produces nothing to commit. The new arm
composes with the treatment plugin directory and the forced slash command; `--arm all` still expands
to the same three arms as before, proven with a positive control (`with_skill` present in the same
output) so a crashed fan-out cannot masquerade as an absence. `selfcheck-red.mjs` exits 0, gated on
the exit code (it takes roughly 7 minutes and spends nothing -- it drives dry runs, never the API).
The reference battery exits 0 AND its roster line still reads 123 checks; `plugins/` is clean under
both `git diff --quiet` and `git status --porcelain`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the halt, UNRESOLVED-1, and the measured G17 ship-time blocker</name>
  <files>.planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md, .planning/REQUIREMENTS.md</files>
  <action>
(a) Author `260801-sc9-RUN-GATE.md`. Five sections, no padding.

1. WHAT IS BUILT: steps 1 and 2 of D-12 are complete and zero-spend; the artifact, the build script
   and the new arm, each with the measured proof from Task 1 and Task 2.

2. UNRESOLVED-1, THE OWNER DECISION BLOCKING STEP 3. Reproduce the entire `<unresolved>` block of
   `260801-sc9-CONTEXT.md` VERBATIM, including its three competing options A, B and C and the reason
   confidence is not high. Do not summarise, do not re-rank the options, do not resolve it. Add one
   line from RESEARCH that was not visible when the three options were framed: option A carries the
   crux 1 and 2 entry cost -- a byte-identical apply preamble, a pinned test directory named by every
   prompt, declared forbidden tokens the prompt names none of, no test-state claim, and a provably
   caught poisoned variant. Option A is not "a suite.json plus a prompt file".

3. THE COMMAND, STATED HONESTLY. Give the two-command sequence: build the tree, then run the new arm
   against its `invoke_skill` baseline. State plainly that `--suite` and `--prompt` are the two holes
   UNRESOLVED-1 fills, so this is single-command-ready only once the corpus exists. Do not write it
   as if it were runnable today.

4. THE GRADING WARNING, carried forward from RESEARCH Q4 so it is not rediscovered at spend time. No
   existing grader scores a vocabulary or classification choice. The smallest honest addition is ONE
   new rubric scenario whose only scored dimension is a single judge item. Do NOT reach for the
   phrase-set matcher: for a vocabulary A/B the treatment IS the vocabulary, so a phrase-set scores
   the treatment arm high by construction and measures house vocabulary rather than substance -- the
   same defect class as the RXL design-confounded verdict.

5. THE TWO GATES THAT REMAIN, and who owns each. The `oracle-reviewer` DST-04 gate on the distilled
   artifact is ORCHESTRATOR-driven (the executor has no Agent tool) and is a precondition of the
   ship, not of this task. The metered run needs a fresh spend approval under the standing
   eval-run-approval-gate; it is a separate orchestrator-driven step and this task does not sit
   waiting on it.

(b) Correct the `FUT-TAXONOMY-SHARED` entry in `.planning/REQUIREMENTS.md`. Its COST sentence
currently names an N3 carve-out as the only ship cost. That is now known to be incomplete: guard G17
also blocks a shipped taxonomy, so the entry must state that the ship needs a G17 carve-out IN
ADDITION to the N3 carve-out, and that a taxonomy artifact cannot be made G17-clean while the
Meszaros catalog names stay verbatim.

RE-DERIVE THE NUMBERS. Do NOT copy a figure from RESEARCH and do NOT copy one from the plan checker
-- they DISAGREE on the third figure (RESEARCH says 9, the checker independently got 10), which is
itself the evidence that an uncounted number is about to be written into a tracked long-lived doc.
Run the count yourself and write what YOU measure.

Methodology, stated here so the number is reproducible by the next reader:

- Subject: `.planning/research/test-double-taxonomy.md`, at the commit you measure. Name that commit
  in the entry.
- Needle: mirror G17's own regex from `check-red-references.mjs:565` -- the four inflections of the
  contested word, word-bounded, case-insensitive.
- BARE means a hit NOT excused by either of G17's two allowlist forms at `:566-567`, evaluated
  against the text immediately preceding the match on the same line: a canonical side qualifier, or
  the meta-mention form that quotes the word as a word.
- Report three figures and label the UNIT of each explicitly, because the unit is where the two prior
  counts diverge: total occurrences, bare occurrences, and the third figure. For the third, state
  whether you are counting UNIQUE LINES or OCCURRENCES, and state the exact set of formal Meszaros
  catalog names you matched -- a set restricted to one name gives a different answer than a set
  covering the collaborator-side kinds. An unlabelled count is what produced the disagreement.

Tag the addition as MEASURED this session and keep the existing entry's VERIFIED-versus-INFERRED
discipline intact -- do not upgrade any neighbouring claim while editing. Write the contested word in
its side-qualified or meta-mention form throughout, matching the shipped-tree convention, even though
this file sits outside G17's scan root. Edit only that one entry; this is a `.planning/` docs change
and touches nothing under `plugins/`.
  </action>
  <verify>
    <automated>rg -F -c 'trap quadrant: HIGH impact, NOT-HIGH confidence' .planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md .planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-CONTEXT.md</automated>
    <automated>rg -c 'G17 carve-out' .planning/REQUIREMENTS.md</automated>
    <automated>rg -F -c 'oracle-reviewer' .planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md</automated>
    <automated>! rg -q '[^\x00-\x7F]' .planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-RUN-GATE.md && echo ASCII_OK</automated>
    <automated>rg -q 'MEASURED this session' .planning/REQUIREMENTS.md && rg -q -e 'unique line' -e 'occurrence' .planning/REQUIREMENTS.md && echo METHODOLOGY_STATED</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 0 && echo BATTERY_GREEN</automated>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -c '123 checks'</automated>
    <automated>git diff --quiet HEAD -- plugins/ && test -z "$(git status --porcelain plugins/)" && echo PLUGINS_UNTOUCHED</automated>
  </verify>
  <done>
RUN-GATE.md carries UNRESOLVED-1 verbatim (the distinctive line matches in both files), the honest
two-hole command, the phrase-set grading warning, and both remaining gates with their owners.
`FUT-TAXONOMY-SHARED` names the G17 carve-out alongside the N3 one, carrying three RE-DERIVED figures
whose units are labelled, whose catalog-name set is stated, and whose subject commit is named --
not a figure copied from RESEARCH or from the plan checker. Battery exit code 0 AND roster line 123
checks; `plugins/` clean under both `git diff` and `git status --porcelain`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script -> filesystem | The script recursive-deletes and recursive-writes a directory. A wrong destination destroys real work. |
| tracked new files -> public GitHub repo | Newly committed maintainer-authored content crosses into a public repo. |
| generated tree -> Claude Code session skill loading | A generated tree containing a `SKILL.md` could be auto-loaded and contaminate both A/B arms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-sc9-01 | Tampering | `build-treatment.mjs` destructive `rm` | high | mitigate | Destination is a hardcoded constant with NO env override; the script asserts the resolved path is inside `REPO_ROOT` and its last two segments are `out` and `lz-tdd-treatment` before deleting. The env override exists only on the runner's read-only `TREATMENT_DIR`. |
| T-sc9-02 | Information disclosure | new tracked files in a public repo | medium | mitigate | `check-hygiene.mjs` scan roots are `plugins/` plus four root files and do NOT cover the new directory (MEASURED), so Task 1 asserts ASCII-only and allowlist-inversion on email-shaped tokens explicitly rather than inheriting coverage that does not exist. |
| T-sc9-03 | Tampering | generated `SKILL.md` auto-loading into both arms | high | mitigate | Generate at `out/lz-tdd-treatment`, outside `.claude/skills/`, MEASURED gitignored. RESEARCH A3 (nested `SKILL.md` is not auto-loaded) is INFERRED, not measured, and confirming it costs a live session -- so the design avoids depending on it. If it were auto-loaded it would contaminate both arms and void the A/B. |
| T-sc9-04 | Spoofing | treatment plugin identity | medium | mitigate | The build script asserts the generated manifest's `name` is still `lz-tdd`; any other name silently breaks the forced slash command and the plugin-root variable, turning the treatment arm into a second baseline. |

No package-manager install task exists in this plan (no npm, pip or cargo install), so no package
legitimacy checkpoint applies and no `T-sc9-SC` entry is warranted.
</threat_model>

<verification>
Run after all three tasks, from the repo root:

- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` -> **exit code 0**, and
  separately the roster line reads 123 checks. Gate on BOTH, and gate on the exit code FIRST.
  The two legs catch different things and neither subsumes the other: the roster line emits the
  substring "123 checks" whenever the emitted count equals the predicted literal, built independently
  of `rosterOk` and of the accumulated failure count (`:1044-1047`), so a battery that fails G17, N1,
  N2, D-05 or a row guard still prints "123 checks" on its way to `process.exit(1)` (`:1066`).
  Grepping for the count alone is FAIL-OPEN. The count leg still earns its place -- it is the only
  thing that catches a dropped or duplicated guard, which the exit code alone would not notice.
- `git diff --quiet HEAD -- plugins/` -> passes. Nothing TRACKED under the shipped tree moved.
- `git status --porcelain plugins/` -> empty. `git diff` is blind to NEW UNTRACKED files, which is
  exactly the guard-evasion shape this task exists to prevent, so both legs are required.
- `git status --porcelain out` -> empty. The generated tree is invisible to git.
- `node .claude/skills/lz-red-workspace/selfcheck-red.mjs` -> exit 0. Gate on the exit code with
  `test $? -eq 0`, never with a trailing `echo "...$?"` -- the echo succeeds and launders a failing
  run into a passing leg (MEASURED: the echo form returns 0 for a command that exited 1).
- `git ls-files` shows the artifact and the build script as tracked additions, and the generated
  tree absent.
- Allowlist inversion over every file this task adds or edits: the only email-shaped token anywhere
  in the diff is the approved public contact, or there is none at all. Do not write any forbidden
  value as a search needle.
</verification>

<success_criteria>
This task is COMPLETE when the build is finished and the halt is recorded. It is explicitly NOT
gated on any metered result.

- [ ] The distilled artifact exists, is tracked, is outside `plugins/`, is ASCII-only, and paraphrases
      every canonical discriminator rather than lifting it.
- [ ] `node build-treatment.mjs` regenerates the full treatment plugin tree from tracked inputs,
      reports exactly two deltas, and fails closed on a missing or duplicated anchor.
- [ ] The generated tree is gitignored and contributes nothing to `git status`.
- [ ] The `invoke_treatment` arm composes `--plugin-dir <treatment>` plus the forced slash command;
      `--arm all` is unchanged (with a positive control); `selfcheck-red.mjs` exits 0, gated on the
      exit code.
- [ ] The reference battery EXITS 0 and its roster line reads 123 checks -- both legs, exit code
      first. `plugins/` is clean under `git diff --quiet` AND `git status --porcelain`.
- [ ] RUN-GATE.md records the halt, UNRESOLVED-1 verbatim, the honest two-hole command, the
      phrase-set grading warning, and both remaining gates with their owners.
- [ ] `FUT-TAXONOMY-SHARED` names the G17 carve-out alongside the N3 one, with three RE-DERIVED
      figures whose units and catalog-name set are labelled and whose subject commit is named.

REMAINING AFTER THIS PLAN, and deliberately so:

1. UNRESOLVED-1 -- the owner picks the ambiguous-prompt corpus and its target. Blocks step 3.
2. The `oracle-reviewer` DST-04 gate on the distilled artifact -- orchestrator-driven, precondition
   of the ship, not of this plan.
3. A fresh spend approval for the metered A/B -- step 4. Only after that does
   `FUT-TAXONOMY-SHARED` close, either as a ship or as DECLINED. Both outcomes are pre-authorized.
</success_criteria>

<output>
Create `.planning/quick/260801-sc9-complete-distinguish-but-prove-it-first-/260801-sc9-SUMMARY.md`
when done.
</output>
