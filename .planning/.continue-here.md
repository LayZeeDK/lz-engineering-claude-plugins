---
context: default (queued-quick-task; NOT phase work)
phase: n-a
status: paused
last_updated: 2026-07-16T13:20:06.102Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **Eval-run approval gate** — the queued task's success criterion ("lift idiom_pattern without regressions") requires a held-out validation eval = `claude -p`/subagent spend. Get EXPLICIT user approval before running it; never self-approve.
- [ ] CONSTRAINT: **Skill-instruction change protocol** — editing `plugins/lz-tdd/skills/lz-refactor/SKILL.md` requires subagent review BEFORE acceptance, `/reload-plugins` AFTER commit (Claude can't run it → escalate + wait; committed ≠ live), and public-repo hygiene on the commit (author/committer = `larsbrinknielsen@gmail.com`; allowlist-scan authored content — never write the work email/domain even as a needle).
- [ ] CONSTRAINT: **Killed background runs → RESUME, don't restart** — eval chains were killed mid-run repeatedly this session (user confirms NOT a usage limit; cause unknown). The harness is idempotent: re-launch the SAME command, exit-0 runs skip, each completed run banks. Restarting from scratch wastes spend.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|-----------|
| git status without --work-tree | `git --git-dir=<repo>/.git status` (no `--work-tree`) reported every file as "deleted" (compared against the plugins-repo CWD) — nearly led to a false "the borrowed repos have uncommitted deletions" conclusion | advisory | Always pass BOTH `--git-dir` and `--work-tree` for cross-repo git ops |
| Scratchpad short-name path | Writing to the `...\LARSGYRUP~1\...` scratchpad path fails EPERM | advisory | Write eval artifacts into the workspace (`.claude/skills/lz-refactor-workspace/e2e-angular/`), not the scratchpad |
| Trusting `used_refactor` for forced activation | The harness's `used_refactor` flag under-detects `/lz-tdd:lz-refactor` (slash-command) activation (0/3 on some invoke cells despite firing) | advisory | Treat `invoke_skill` as skill-active by construction; `with_skill` auto-trigger IS reliably detected (Skill tool 15/15) |
| --auto silently locking a skill-wording choice | `/gsd-quick --auto` auto-picks recommended options; a skill-wording change that could erode the incrementality/behavior wins is HIGH-impact | advisory | Surface high-impact/low-confidence wording choices; don't auto-lock them |
| Tuning a non-defect | idiom_pattern is NOT a measured defect at k=5 (skill ≥ baseline); "fix" may show null lift | advisory | Frame the task as thoroughness, not a metric gap; let the no-regression gate reject a null-lift change |

<current_state>
The lz-refactor **apply/drive-mode eval on 3 real Angular repos is COMPLETE** (design + 45+ runs at k=5/k=3 + full multi-dimensional grading + RESULTS). Repos are restored to original branches and clean; eval branches deleted; nrwl/nx untouched.

The **queued task has NOT started**. It is gated on a go/no-go the user was about to give: `/gsd-quick --full --auto` to extend drive-to-fixpoint to single-target apply, "must lift idiom_pattern without regressions." k=5 weakened its premise (idiom is not a defect), so confirm intent first.
</current_state>

<completed_work>
- Eval design: 3 per-repo suites under `e2e-angular/{cli,cdk,core}` (suite.json + targets.json + prompts). Targets: ac1 `registry._compile`, ac2 `writer.convert*Collection`, cd1 `sticky-styler.stickRows`, an1 `change_tracker._recordImports` — all TypeScript, pre-identified nx-style.
- Runs: 3 arms (no_skill / invoke_skill / with_skill), apply/drive mode, on throwaway `eval/lz-refactor-apply` branches. k=5 on ac1/cd1/an1, k=3 on ac2 (saturated). All exit 0.
- Grading: mechanical (`tabulate-mechanical.mjs`) + blind cell-judges (arm-hidden bundles under `graded*/`, KEY held by grader) + oracle `book_authenticity` (DST-04). Aggregated in `synth-k5.mjs` → `k5-aggregate.json`.

**k=5 three-arm result (strict pass / DROVE-runs):**
| metric | invoke (forced) | with_skill (auto) | no_skill |
|---|:--:|:--:|:--:|
| auto-trigger fired | forced | 15/15 (100%) | n/a |
| drove/ran | 13/15 (87%) | 9/15 (60%) | 15/15 (100%) |
| output_quality | 100% | 78% | 60% |
| behavior_preservation | 100% | 78% | 67% |
| over_under | 100% | 100% | 73% |
| incrementality | 100% | 100% | 60% |
| idiom_pattern | 77% | 89% | 73% |
</completed_work>

<remaining_work>
- **queued-1 (primary):** `/gsd-quick --full --auto` — extend SKILL.md drive-to-fixpoint from package/directory scope to SINGLE-TARGET apply; lift idiom_pattern with no regression on the other grades.
- **milestone-1 (inherited, still pending):** `/gsd-audit-milestone lz-tdd@0.0.2`
- **milestone-2 (inherited, still pending):** `/gsd-complete-milestone lz-tdd@0.0.2`
  (Ordering of queued-1 vs milestone close is unresolved — the skill change may belong inside 0.0.2 or after. Confirm with user.)
</remaining_work>

<decisions_made>
- Concrete change: broaden the "Whole-package / directory sweeps" fixpoint behavior (SKILL.md ~L101-116) so single-target DRIVE/apply also completes bounded, warranted, in-scope, behavior-preserving, test-covered follow-on rounds (e.g. exposed loop→pipeline) instead of deferring them — keeping step-4 APPLY/DECLINE, step-5 blast-radius pause, and verify-between-rounds intact. Consider tightening the verify gate to "typecheck and/or tests" (bazel repos can't always run the suite).
- Validate on HELD-OUT targets (fresh smells, not ac1/cd1/an1). Measure idiom_pattern (must rise) + no regression on behavior_preservation/over_under/incrementality/output_quality.
- Eval verdict (RESULTS.md): skill's applied-quality edge is real WHEN IT DRIVES (baseline recurringly drops an `await` → async regression on ac1, 2/5); but auto-trigger fires 100% yet drives only 60% (question→coach routing on "what would you do?" prompts — a prompt-phrasing confound).
</decisions_made>

<blockers>
- Held-out validation eval needs user approval (spend gate).
- Background runs killed intermittently (not a limit) — resume-don't-restart.
</blockers>

## Required Reading (in order)
1. `.claude/skills/lz-refactor-workspace/e2e-angular/RESULTS.md` — the full eval verdict incl. the k=5 section and power-mode caveat.
2. `.claude/skills/lz-refactor-workspace/e2e-angular/GRADING.md` + `k5-aggregate.json` — the rubric + numbers.
3. `plugins/lz-tdd/skills/lz-refactor/SKILL.md` — the file to edit; §"Coach decision procedure" step 5 (single-refactoring apply) + §"Whole-package / directory sweeps" (the scope-gated fixpoint behavior).
4. `.planning/phases/14-.../14-RESULTS.md` — the recommend-mode head-to-head this rubric derives from.

## Infrastructure State
- Borrowed repos (D:/projects/github/angular/{angular-cli,components,angular}) restored to original branches (22.0.x / 18.2.x / main), git-clean. To run the validation eval, recreate `git switch -C eval/lz-refactor-apply` in each; angular-cli needs deps: `COREPACK_ENABLE_DOWNLOAD_PROMPT=0 corepack pnpm@10.34.4 --dir <repo> install --frozen-lockfile --ignore-scripts` (its node_modules from this session may persist, gitignored).
- Reproduce a run: `node .claude/skills/lz-refactor-workspace/e2e-nx/run-e2e.mjs --suite <e2e-angular/cli|cdk|core> --mode apply --arm <no_skill|invoke_skill|with_skill> --cwd <repo> --runs N` (add `--prompt <id>` to restrict; `--dry-run` to preview). All 3 repos are bazel-first → runs verify via `tsc --noEmit`, not the suite.
- Power mode: Balanced now (was Performance for the k=3 batch) → wall-clock not cross-batch comparable; cost/tokens/graded dims fine.
- git identity confirmed = larsbrinknielsen@gmail.com (public-repo-safe).

## Pre-Execution Critique Required
- Design artifact: the SKILL.md edit (to be drafted).
- Critique focus: does the extension risk over-reach (baseline's failure mode)? Do the step-4/step-5/verify guards actually bound it? Does it erode the incrementality/behavior-preservation wins? Is the verify gate wording correct for bazel repos?
- Gate: subagent review of the edit BEFORE acceptance; do NOT run the held-out validation until the edit is reviewed and applied and /reload-plugins has run.

<context>
The user queued this task based on the k=3 idiom soft spot (10/12 vs 9/12). k=5 then showed idiom is ~tied/skill-favorable (with_skill 89%, invoke 77%, baseline 73%) and the only deferral was forced-invoke-specific on the mild an1 cell. So the task targets a marginal/non-defect signal; its real justification is thoroughness (single-target apply finishing its safe rounds). Expect a possibly-null measurable lift, which the no-regression-and-lift gate would (correctly) reject → no ship. The eval artifacts are UNTRACKED on disk (workspace not wholesale-gitignored, but e2e-angular/ was never added); same-machine sessions see them.
</context>

<next_action>
Start by confirming go/no-go with the user (surface: k=5 shows idiom is not a defect; proceed for thoroughness or hold?). If GO: (1) draft the SKILL.md single-target-fixpoint edit; (2) spawn subagent review(s), incl. one unbiased-from-scratch; (3) apply + escalate `/reload-plugins`; (4) get approval for + run the held-out validation eval on fresh targets; (5) commit ONLY if idiom lifts with no regression (hygiene-scan first), else report null and don't ship. Then the inherited milestone close.
</next_action>
