---
phase: 16-source-distillation-core-red-references
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/naming.md
  - .claude/skills/lz-red-workspace/package-lock.json
autonomous: true

requirements: [SEL-01, SEL-02, STR-01, STR-02, NAME-01]

must_haves:
  truths:
    - three-laws-and-test-selection.md carries the SEL slice in original words -- keep a running test list, take one small (one-step) step, open from the degenerate/starter case (empty, zero, null), and use triangulation to SELECT the next failing test.
    - The SEL-02 triangulation prose carries the load-bearing firewall sentence -- triangulation here SELECTS the next failing test (the RED facet); generalizing production code is lz-tpp's GREEN job.
    - test-structure-and-assertions.md presents Arrange-Act-Assert and Given-When-Then as ONE three-part skeleton in TWO vocabularies (match the house idiom, impose no school), plus assert-first, evident/intention-revealing test data, and one-concept-per-test.
    - naming.md documents behavior/BDD "should ..." naming as the PRIMARY convention and Osherove's three-part UnitOfWork_StateUnderTest_ExpectedBehavior as the documented ALTERNATIVE, plus "match the codebase's existing naming stance."
    - Each of the three references carries at least one tsc --strict-clean Vitest example (explicit import from vitest) illustrating the RED principle.
    - Every OWNED rationale thread (RCM Clean Code one-step + one-concept; Metz name-for-behavior) passed the oracle-reviewer converge-to-clean gate; every surface passed the deterministic no-verbatim scan.
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (SEL slice filled; Phase 18 LAW/SEAM marker preserved)
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (STR slice filled; Phase 17 ASRT marker preserved)
    - plugins/lz-tdd/skills/lz-red/references/naming.md (filled fully; NAME-01 resolved)
  key_links:
    - Each Vitest fence uses an explicit `import { describe, it, expect } from 'vitest'` so the extractor treats it as a self-contained module and the workspace tsc gate resolves the types from the installed node_modules.
    - Owned surfaces (RCM in SEL/STR, Metz in NAME) route through the oracle / oracle-reviewer clean-room agents; only own-words facts cross back into the shipped refs.
    - The SKILL.md reference-pointer section (wired in Phase 15) links these three refs; filling them makes those links resolve to real content.
  prohibitions:
    - The executor's main context MUST NEVER read any .oracle/ prose -- owned facts are obtained ONLY by spawning the oracle agent with a question + the .oracle/<book>/index.md path.
    - NO verbatim book/talk prose or code in any committed file (own-words from the FIRST draft; NAMES/acronyms are statable as facts, sentence-level phrasing must be original).
    - NO maintainer work-email or its bare domain in any committed file (allowlist-inversion; only larsbrinknielsen@gmail.com).
    - NO filling of the deferred slices -- leave the Three Laws spine + classify-first seam (Phase 18) and F.I.R.S.T./Khorikov four pillars (Phase 17) as markers, not prose.
    - NO deep Vitest surface (no it.todo / test.each / vi.*) -- Phase 16 uses only the minimal describe/it/expect surface.
---

<objective>
Fill the SEL, STR, and NAME slices of the three existing Phase-15 stubs with clean-room own-words
facts and minimal tsc --strict-clean Vitest examples, gating owned-source rationale through the
oracle-reviewer converge-to-clean loop and every surface through the no-verbatim scan. This is the
Wave-1 content plan: it turns the check-red-references RED baseline GREEN slice by slice.

Purpose: these three references are the core test-shaping guidance the lz-red coach routes into --
what to test next (selection), how to structure and assert it (structure), and how to name it. The
content risk is authoring discipline: DST-04 near-verbatim leaks of canonical one-liners, tsc-strict
failures in hand-written snippets, and the load-bearing SEL-02 firewall that keeps triangulation
on the RED side of the lz-tpp seam.

Output: three filled references (own-words, tsc-clean fences, oracle-reviewer-passed owned surfaces),
the two co-edited stubs with their later-phase markers intact.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-source-distillation-core-red-references/16-CONTEXT.md
@.planning/phases/16-source-distillation-core-red-references/16-RESEARCH.md
@.planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md
@plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md
</context>

## Decisions implemented

- **D-01:** RED-phase owned facts are distilled OWN-WORDS via the clean-room oracle / oracle-reviewer agents; the executor main context NEVER reads .oracle/ book/talk prose.
- **D-02:** source tagging -- RCM Clean Code and Metz 99 Bottles rationale threads are OWNED (oracle-verified); Beck, Wake, North, Osherove are NO-ORACLE (authored blind from high-confidence core, tagged no-oracle, no oracle-verification claimed).
- **D-03:** the two Cooper talks are FLAT transcripts and SECONDARY for Phase 16 (SEL: a new behavior triggers the next test; NAME: name the behavior not the method) -- use only lightly if at all; their heavy use is Phase 17. If used, the oracle input points DIRECTLY at the flat transcript path and the agent chunk-reads to EOF.
- **D-04:** fill only the SEL and STR slices and leave the Phase 18 (LAW/SEAM) and Phase 17 (ASRT) markers intact; naming.md is filled fully.
- **D-05:** SEL-01 -- running test list, one small (one-step) step, degenerate/starter case, framed as the opening move of a new behavior; own-words from Clean Code (owned) + Beck (no-oracle).
- **D-06:** SEL-02 -- triangulation as a RED test-SELECTION move, EXPLICITLY bounded against lz-tpp's fake-it / generalize; the firewall sentence is load-bearing.
- **D-07:** STR-01 -- Arrange-Act-Assert and Given-When-Then as ONE skeleton in TWO vocabularies, design-agnostic, match the house idiom.
- **D-08:** STR-02 -- assert-first, evident/intention-revealing test data, one-concept-per-test; own-words from Beck (no-oracle) + Clean Code one-concept (owned).
- **D-09:** NAME-01 -- behavior/"should" naming PRIMARY (North); Osherove three-part as the documented ALTERNATIVE; match the codebase's naming stance; Metz name-the-behavior-not-the-method (owned) is the rationale thread.
- **D-10:** every TypeScript sample is tsc --strict clean and carries no verbatim prose/code; minimal describe/it/expect surface only.
- **D-12:** owned surfaces take the oracle-reviewer gate PLUS the no-verbatim scan; no-oracle surfaces take the no-verbatim scan only.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill the SEL slice of three-laws-and-test-selection.md (SEL-01, SEL-02) + npm install the workspace deps</name>
  <files>plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md, .claude/skills/lz-red-workspace/package-lock.json</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (the stub to fill: the scope para, the co-edit blockquote marker, the per-entry content contract, the Sub-topics list, and the draft-marked Sources heading)
    - plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md (the no-oracle grain to mirror: the provenance blockquote lines 9-11, and the triangulation RED/GREEN firewall precedent lines 39-51 -- lz-red inverts it to the RED side)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md (the own-code tsc-strict ```ts fence shape lines 37-67; lz-red fences additionally prepend an import from vitest)
    - .planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md (the three-laws fill-vs-leave map + the firewall shared-pattern)
  </read_first>
  <action>
    FIRST run `npm install` inside .claude/skills/lz-red-workspace/ (the legitimacy checkpoint cleared in 16-01) so the Vitest types resolve for the fence tsc gate; node_modules is gitignored, package-lock.json is committed as the pinned lock.
    Then fill the SEL slice of three-laws-and-test-selection.md IN ORIGINAL WORDS. Obtain the OWNED rationale thread by spawning the oracle agent (do NOT read .oracle/ yourself): packaged input = a focused question about the "one small step / only enough to fail" rationale for choosing the next test + the .oracle/clean-code/index.md path (RCM Clean Code Ch.9 Unit Tests). Use the returned own-words facts as the RCM rationale thread only. Author the NO-ORACLE Beck content blind from high-confidence core: keep a running test list (a working queue of behaviors still to pin), take one small one-step move (one new failing test, all prior tests green, code compiling), open from the degenerate/starter case (empty, zero, null) as the first move of a new behavior, and triangulation as a RED test-SELECTION facet (add a second concrete example to force the next failing test). State the LOAD-BEARING SEL-02 firewall sentence explicitly: triangulation here SELECTS the next failing test; generalizing the production code (fake-it -> generalize) is lz-tpp's GREEN job -- invert the beck-tdd-by-example precedent to the RED side. Carry dual provenance tags: an "owned; oracle-verified" note for the RCM thread and a "no-oracle" note for the Beck material. LEAVE the Three Laws spine + classify-first seam as Phase 18 markers (keep a distinct "Populated in Phase 18 (LAW-01, LAW-02, SEAM-01)" deferral note and the Sub-topic headings as markers, not filled prose). Rewrite the draft-marked Sources heading to a clean `## Sources` section citing RCM (owned) + Beck (no-oracle). Add at least one tsc --strict-clean Vitest fence: a minimal degenerate-case failing-test skeleton with an explicit `import { describe, it, expect } from 'vitest'`, AAA-commented, minimal describe/it/expect surface only. Then spawn the oracle-reviewer to GATE the RCM owned surface: packaged input = the draft path + .oracle/clean-code/index.md + CONTENT_TYPE=other + driver-supplied per-axis anchors (e.g. correct = "one small step / only enough to fail" rationale in original words; wrong = near-verbatim RCM phrasing). Converge-to-clean, 3-round cap; on a revise verdict re-draft BLIND from the category directive (never read .oracle/) and re-gate. ASCII-only; no maintainer work-email token. <!-- planner-discipline-allow: placeholder -->
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/extract-samples.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node extract-samples.mjs exits 0: the new SEL Vitest fence compiles tsc --strict --noEmit clean (Vitest types resolve from the installed node_modules).
    - node check-red-references.mjs shows the three-laws-and-test-selection.md rows PASS (test list, one step, degenerate/starter, triangulation, lz-tpp firewall reference, >=1 ts fence, no draft marker) while STR/NAME remain RED -- overall exit stays 1 until Tasks 2-3 land (expected instrument-first behavior).
    - The three-laws stub retains its Phase 18 (LAW/SEAM) deferral marker (over-fill guard holds).
    - The oracle-reviewer returns an all-pass verdict on the RCM owned surface (converge-to-clean within 3 rounds); the no-verbatim scan (check-hygiene) is GREEN.
    - The SEL-02 firewall sentence is present and states triangulation SELECTS the next test, not generalizes production code.
  </acceptance_criteria>
  <done>SEL slice authored own-words with the load-bearing lz-tpp-GREEN firewall + a tsc-strict Vitest degenerate-case fence; RCM surface oracle-reviewer all-pass; three-laws rows GREEN, Phase 18 marker intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill the STR slice of test-structure-and-assertions.md (STR-01, STR-02)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md (the stub to fill; note the Phase 17 ASRT marker to preserve and the draft-marked Sources heading)
    - plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md (no-oracle grain + provenance blockquote)
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (the proven minimal expect().toBe() Vitest surface, lines 57-62; lz-red fences add the explicit vitest import)
    - .planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md (the test-structure fill-vs-leave map)
  </read_first>
  <action>
    Fill the STR slice of test-structure-and-assertions.md IN ORIGINAL WORDS. Obtain the OWNED one-concept-per-test rationale via the oracle agent: packaged input = a focused question on one-concept-per-test (one reason for a test to fail) + the .oracle/clean-code/index.md path (RCM Clean Code Ch.9). Author the NO-ORACLE content blind: STR-01 -- present Arrange-Act-Assert (Wake) and Given-When-Then (North) as ONE three-part skeleton in TWO vocabularies, design-agnostic, instructing to MATCH the house idiom and impose no single school. STR-02 -- assert-first (write the assertion before arrange/act), evident/intention-revealing test data (data that makes the expected relationship obvious), and one-concept-per-test. Carry the dual provenance tags (no-oracle for Wake/North/Beck; owned; oracle-verified for the RCM one-concept thread). LEAVE F.I.R.S.T. and the Khorikov four pillars as Phase 17 markers (keep a distinct "Populated in Phase 17 (ASRT-01, ASRT-02)" deferral note; do not fill them). Rewrite the draft-marked Sources heading to a clean `## Sources`. Add at least one tsc --strict-clean Vitest fence: an AAA/GWT-labeled `it('should ...')` example with an explicit `import { describe, it, expect } from 'vitest'`, minimal describe/it/expect surface only. Spawn the oracle-reviewer to gate the RCM one-concept owned surface (draft path + .oracle/clean-code/index.md + CONTENT_TYPE=other + per-axis anchors: correct = one-concept-per-test rationale in original words; wrong = near-verbatim RCM). Converge-to-clean, 3-round cap, re-draft BLIND on revise. ASCII-only; no work-email token. <!-- planner-discipline-allow: placeholder -->
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/extract-samples.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node extract-samples.mjs exits 0: the STR fence(s) compile tsc --strict --noEmit clean alongside the SEL fence.
    - node check-red-references.mjs shows the test-structure-and-assertions.md rows PASS (arrange-act-assert, given-when-then, assert-first, evident data, one concept, >=1 ts fence, no draft marker); NAME remains RED until Task 3.
    - The test-structure stub retains its Phase 17 (ASRT) deferral marker (over-fill guard holds); F.I.R.S.T. and the four pillars are NOT authored.
    - AAA and GWT are presented as ONE skeleton in TWO vocabularies (not two competing schemas), with a "match the house idiom" instruction.
    - The oracle-reviewer returns all-pass on the RCM one-concept surface; the no-verbatim scan is GREEN.
  </acceptance_criteria>
  <done>STR slice authored own-words: AAA+GWT one skeleton two vocabularies, assert-first, evident data, one-concept + a tsc-strict Vitest fence; RCM surface oracle-reviewer all-pass; structure rows GREEN, Phase 17 marker intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fill naming.md fully (NAME-01)</name>
  <files>plugins/lz-tdd/skills/lz-red/references/naming.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-red/references/naming.md (the stub to fill fully; note it has NO later-phase marker -- resolve its marker entirely)
    - plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md (no-oracle grain + provenance blockquote + Sources footer shape)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/extract-function.md (own-code tsc-strict fence shape)
    - .planning/phases/16-source-distillation-core-red-references/16-PATTERNS.md (the naming fill map + the DST-04 near-verbatim hazard note for Osherove/should one-liners)
  </read_first>
  <action>
    Fill naming.md ENTIRELY IN ORIGINAL WORDS (NAME-01 is wholly Phase 16). Obtain the OWNED rationale via the oracle agent: packaged input = a focused question on naming-for-the-behavior-not-the-method + the .oracle/99-bottles-2e-js/index.md path (Metz + Owen 99 Bottles JS Ed). Author the NO-ORACLE content blind: document behavior/BDD "should ..." naming as the PRIMARY convention (North) -- the name reads as a sentence about observable behavior; document Osherove's three-part UnitOfWork_StateUnderTest_ExpectedBehavior as the documented ALTERNATIVE when the codebase already uses it; instruct to MATCH the codebase's existing naming stance rather than impose one. Thread the Metz name-the-behavior-not-the-method rationale (owned). CRITICAL DST-04 hazard: the canonical one-liners here (Osherove's three-part convention, the BDD "should" sentence) reconstruct near-verbatim in a blind draft -- keep NAMES/acronyms as statable facts but write every sentence-level definition in ORIGINAL words from the first draft. Carry dual provenance tags (no-oracle for North/Osherove; owned; oracle-verified for the Metz thread) and resolve the stub's Phase-16 marker fully. Rewrite the draft-marked Sources heading to a clean `## Sources`. Add at least one tsc --strict-clean Vitest fence: a "should"-named test (`it('should ...')`) with an explicit `import { describe, it, expect } from 'vitest'`, minimal surface only. Spawn the oracle-reviewer to gate the Metz owned surface (draft path + .oracle/99-bottles-2e-js/index.md + CONTENT_TYPE=other + per-axis anchors: correct = name-for-behavior rationale in original words; wrong = near-verbatim Metz). Converge-to-clean, 3-round cap, re-draft BLIND on revise. ASCII-only; no work-email token. <!-- planner-discipline-allow: placeholder -->
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/extract-samples.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node extract-samples.mjs exits 0: all three refs' fences compile tsc --strict --noEmit clean.
    - node check-red-references.mjs now exits 0 (GREEN): all three references pass their content-present topics, each has >=1 ts fence, no draft marker leaks, and the two co-edited refs retain their Phase 17/18 markers.
    - naming.md documents "should"/behavior naming as PRIMARY and Osherove three-part as the documented ALTERNATIVE, plus "match the codebase's naming stance"; no near-verbatim Osherove/BDD one-liner survives.
    - The oracle-reviewer returns all-pass on the Metz owned surface; the no-verbatim scan (check-hygiene) is GREEN over the whole lz-red tree.
  </acceptance_criteria>
  <done>naming.md filled fully: should-primary + Osherove three-part alternative + match-house-stance + a tsc-strict should-named Vitest fence; Metz surface oracle-reviewer all-pass; check-red-references now GREEN across all three refs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .oracle/ prose -> executor main context | Copyrighted full text must NOT cross; only own-words facts return, via the oracle / oracle-reviewer agents. The executor never Reads .oracle/. |
| author -> shipped lz-red tree | Only own-words, ASCII, work-email-free content lands in plugins/lz-tdd/skills/lz-red/references/; check-hygiene + oracle-reviewer gate it. |
| npm registry -> dev workspace | The Wave-1 install (approved in 16-01) lands only in the gitignored node_modules; never plugins/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-verbatim | Information Disclosure | owned-source rationale in the three refs (RCM/Metz; Cooper if used) | high | mitigate | Clean-room .oracle (agents only) + oracle-reviewer converge-to-clean (3-round cap) on every owned surface + deterministic no-verbatim scan (>= 120-char quoted run) + own-words from the FIRST draft; canonical one-liners (Osherove three-part, BDD "should", AAA/GWT glosses) are the specific near-verbatim hazard. |
| T-16-email | Information Disclosure | any committed reference file / commit identity | high | mitigate | check-hygiene allowlist-inversion over the lz-red tree (only larsbrinknielsen@gmail.com); verify git author/committer = public gmail before commit; never write the forbidden work-email/domain as a needle. |
| T-16-ascii | Tampering (data corruption) | shipped reference Markdown | medium | mitigate | ASCII substitutes only (--, ->, straight quotes); check-hygiene axis (a) fails on any byte > 0x7F. |
| T-16-tsc | Tampering (integrity of guidance) | the Vitest example fences | medium | mitigate | Every fence goes through the extract-samples tsc --strict --noEmit gate (one module per fence); minimal describe/it/expect surface avoids Vitest 4.x API drift. |
| T-16-scope | Scope integrity | the two co-edited refs | low | mitigate | check-red-references deferral guard asserts the Phase 17 (ASRT) and Phase 18 (LAW/SEAM) markers REMAIN; the executor fills only its slice. |
| T-16-firewall | Coach-boundary integrity | SEL-02 triangulation prose | high | mitigate | The load-bearing firewall sentence keeps triangulation on the RED (test-selection) side of the lz-tpp seam; oracle-reviewer + skill-reviewer (16-03) confirm it; check-red-references asserts an lz-tpp reference is present. |

Block-on-high posture (ASVS L1, block on high): a verbatim leak, a non-allowlisted email, or a missing/blurred SEL-02 firewall is a HARD failure and blocks the plan.
</threat_model>

<verification>
- node .claude/skills/lz-red-workspace/extract-samples.mjs -> exit 0 (all fences tsc --strict clean) after each task.
- node .claude/skills/lz-red-workspace/tools/check-red-references.mjs -> GREEN (exit 0) after Task 3 (all three slices filled, markers intact).
- node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs -> exit 0 (ASCII + email + no-verbatim GREEN over the lz-red tree) after each task.
- oracle-reviewer all-pass on each owned surface (RCM in Tasks 1-2, Metz in Task 3); converge within the 3-round cap.
</verification>

<success_criteria>
The three core RED references are filled in original words: selection (test list, one step, degenerate-first, triangulation with the load-bearing lz-tpp-GREEN firewall), structure (AAA + GWT one skeleton two vocabularies, assert-first, evident data, one-concept), and naming ("should" primary + Osherove alternative + match-house-stance). Each carries a tsc --strict-clean Vitest example. Every owned surface passed the oracle-reviewer gate and every surface passed the no-verbatim scan. The two co-edited stubs retain their later-phase markers. check-red-references is GREEN.
</success_criteria>

## Artifacts this plan produces

- `plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md` -- SEL slice filled (SEL-01, SEL-02); Phase 18 LAW/SEAM marker preserved.
- `plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md` -- STR slice filled (STR-01, STR-02); Phase 17 ASRT marker preserved.
- `plugins/lz-tdd/skills/lz-red/references/naming.md` -- filled fully (NAME-01 resolved).
- `.claude/skills/lz-red-workspace/package-lock.json` -- the pinned dependency lock from the approved install (node_modules is gitignored).

The only new content in the shipped `plugins/` tree is own-words Markdown + tsc-strict-clean Vitest fences; no code symbols or build deps.

<output>
Create `.planning/phases/16-source-distillation-core-red-references/16-02-SUMMARY.md` when done.
</output>
