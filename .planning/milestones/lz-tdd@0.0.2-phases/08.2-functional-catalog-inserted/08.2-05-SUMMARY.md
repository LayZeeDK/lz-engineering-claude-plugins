---
phase: 08.2-functional-catalog-inserted
plan: 05
subsystem: reference-content
tags: [functional-catalog, n-to-1-map, readme, selector-mirror, fowler-reverse-note, depatterning-coverage]

# Dependency graph
requires:
  - phase: 08.2-functional-catalog-inserted
    plan: 01
    provides: check-functional.mjs (C1-C8) + 19-idiom LEAVES + README scaffold + extract-samples/check-crossrefs wiring
  - phase: 08.2-functional-catalog-inserted
    plan: 02
    provides: the six N:1 dissolves-from leaves + their per-pattern residual anchors (the anchor targets the README rows point to)
  - phase: 08.2-functional-catalog-inserted
    plan: 03
    provides: the seven 1:1 dissolves-from leaves
  - phase: 08.2-functional-catalog-inserted
    plan: 04
    provides: the six native alternative-to leaves
provides:
  - the finalized N:1 pattern->idiom map README (all 23 GoF + 27 Kerievsky + 5 extra + 15 Fowler FP-analog rows + 6 native rows), every leaf's Use when: mirrored verbatim, all 14 Fowler notes, ## Sources
  - the D-14 Replace-Pipeline-with-Loop reverse-direction note in the existing Fowler replace-loop-with-pipeline.md leaf
affects: [08.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "N:1 README map: one row per source item; col-4 mirrors the TARGET leaf's Use when: verbatim (C3); N:1 rows target the served pattern's residual anchor (slug.md#anchor), 1:1 rows target the bare leaf (slug.md)"
    - "Correspondence cell reflects the target leaf's enum (dissolves-from for the OO-dissolution leaves; alternative-to for rows resolving to a native leaf, e.g. Null Object / Extract Parameter / the Fowler lens/transducer/normalized-store analogs)"
    - "Fowler notes are plain-text one-liners with NO markdown link (C7); the C7 leaf-link regex only catches bare-slug .md links, so a note carrying no link is unambiguously safe"
    - "Fowler reverse note is prose-only (no code fence) so extract-samples/check-catalog are untouched; no Functional alternative: line (D-10)"

key-files:
  created: []
  modified:
    - plugins/lz-tdd/skills/lz-refactor/references/functional-catalog/README.md
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/replace-loop-with-pipeline.md

key-decisions:
  - "A5 reconciliation: all 14 check-functional NOTE_ITEMS map exactly to shipped fowler-catalog leaf filenames (Title Case); no name adjustment needed"
  - "Kerievsky/Fowler-analog rows to an N:1 leaf inherit their target GoF pattern's residual anchor; rows to an N:1 leaf with no matching served-pattern residual (e.g. Replace Loop with Pipeline -> function-composition) target the bare leaf"
  - "Rows whose functional resolution is a native leaf (Introduce Null Object / Null Object -> option-and-either; Extract Parameter -> currying; Hide Delegate/Remove Middle Man -> lens-and-optics; Change Value to Reference -> normalized-store) carry Correspondence: alternative-to, matching the target leaf's enum"
  - "Fowler reverse note added as a ## Reverse direction section (prose-only), not a new refactoring and not a Functional alternative: line (D-14 + D-10)"

requirements-completed: []  # FUN-02 remains OPEN by design -- closes when the wave-4 (plan 06) 55 Functional alternative: cross-links land and the full battery goes GREEN

# Metrics
duration: ~30min
completed: 2026-07-07
---

# Phase 08.2 Plan 05: Finalized N:1 map README + D-14 Fowler reverse note Summary

**The functional-catalog README is now the finalized N:1 pattern->idiom map -- every dissolvable source item (23 GoF + 27 Kerievsky + 5 extra + 15 Fowler FP-analog) plus the 6 native idioms resolves to an idiom leaf in a per-item row that mirrors the target leaf's `Use when:` verbatim (C3), N:1 rows target the served pattern's residual anchor and 1:1 rows the bare leaf, all 14 Fowler moot/data-modeling/light items appear as one-line link-free notes (C7), and `## Sources` cites the research artifact (C8); the existing Fowler `replace-loop-with-pipeline.md` leaf gains the D-14 Replace-Pipeline-with-Loop reverse-direction note. check-functional dropped from 88 -> 55 FAILs -- the README/notes/Sources half is fully GREEN and only the wave-4 `Functional alternative:` cross-links remain RED.**

## Performance

- **Duration:** ~30 min active
- **Completed:** 2026-07-07
- **Tasks:** 2 (README map; Fowler reverse note)
- **Files modified:** 2 (0 created; no shared or wave-4-owned files touched)

## Accomplishments

- **Task 1 -- finalized N:1 map README.** Rewrote `functional-catalog/README.md` from the scaffold into the complete map: five grouped tables (GoF 23, Kerievsky 27, extra 5, Fowler FP-analog 15, native 6). Every one of the 19 idiom leaves' `Use when:` selector appears verbatim in at least one row linking to it (C3 selector-mirror). N:1 leaf rows target the served pattern's residual anchor (`discriminated-union-and-fold.md#visitor`, `function-composition.md#builder`, `factory-function.md#abstract-factory`, ...); 1:1 leaf rows target the bare `slug.md`. Added the 14 Fowler notes (3 moot + 8 data-modeling + 3 light) as plain one-line entries with no leaf link (C7), and kept the `## Sources` citation with the DST-04 disclaimer (C8).
- **Task 2 -- D-14 reverse note.** Appended a `## Reverse direction: Replace Pipeline with Loop` note to the existing Fowler leaf: clarity is the default, reverse to a loop only for a concrete named reason -- a measured hot path (intermediate-array cost; single-`reduce`/`for-of`, short-circuiting `find`/`some`/`every`, and typed-array middle grounds; never `[...acc, x]`) or a clarity/debuggability/early-exit/house-style reason. Prose-only, no code fence, no `Functional alternative:` line (D-10). Paraphrased from S12.7 (DST-04).

## Task Commits

1. **Task 1: finalize the N:1 pattern->idiom map README** - `597c7ef` (docs)
2. **Task 2: add the D-14 Replace-Pipeline-with-Loop reverse note to the Fowler leaf** - `61dfda5` (docs)

**Plan metadata (this SUMMARY.md):** committed with this file (docs).

## A5 Fowler-note-name reconciliation (recorded per plan output)

All 14 `check-functional` `NOTE_ITEMS` map exactly, string-for-string, to a shipped
`fowler-catalog/` leaf filename (Title Case of the kebab slug). No name adjustment was required:

| Note group | Note item | Shipped fowler leaf |
|------------|-----------|---------------------|
| moot (3) | Change Reference to Value | change-reference-to-value.md |
| moot | Remove Setting Method | remove-setting-method.md |
| moot | Replace Superclass with Delegate | replace-superclass-with-delegate.md |
| data-modeling (8) | Pull Up Method | pull-up-method.md |
| data-modeling | Push Down Method | push-down-method.md |
| data-modeling | Pull Up Field | pull-up-field.md |
| data-modeling | Push Down Field | push-down-field.md |
| data-modeling | Pull Up Constructor Body | pull-up-constructor-body.md |
| data-modeling | Extract Superclass | extract-superclass.md |
| data-modeling | Collapse Hierarchy | collapse-hierarchy.md |
| data-modeling | Remove Subclass | remove-subclass.md |
| light (3) | Move Field | move-field.md |
| light | Encapsulate Variable | encapsulate-variable.md |
| light | Encapsulate Collection | encapsulate-collection.md |

Two research-vs-shipped notes:
- **RESEARCH S12.3 supersedes the D-10 pre-adversarial "~13 + 2" phrasing.** The reconciled note set
  is 3 moot + 8 data-modeling + 3 light = 14, exactly the checker's `NOTE_ITEMS`.
- **"Encapsulate Record" is folded into the "Encapsulate Variable" note.** S12.3's light-note pair
  was "Encapsulate Variable / Encapsulate Record"; the checker demands only "Encapsulate Variable"
  (a distinct `encapsulate-record.md` leaf also ships). The Encapsulate Variable note line mentions
  Encapsulate Record in prose (no link) so both are covered without a second note row.

## Battery state after this plan (check-functional README/notes half GREEN; C4 still RED by design)

Run from `.claude/skills/lz-refactor-workspace/` inside the worktree:

| Gate | Exit | Result |
|------|------|--------|
| `node tools/check-functional.mjs` | 1 | Advanced **88 -> 55 FAILs**. All 19 leaves now PASS C1/C2/C3/C5/C6 (every `no README index row` cleared); all 14 notes PASS C7; C8 PASS. The remaining 55 FAILs are EXACTLY the wave-4 `Functional alternative:` cross-links: 23 gof + 27 kerievsky + 5 extra. Zero leaf/note/README failures remain. |
| `node tools/check-crossrefs.mjs` | 0 | GREEN -- 628 links resolve (README's ~90 new rows + anchors all resolve), 20 inverse pairs mutual, 0 self-refs |
| `node extract-samples.mjs` (typecheck) | 0 | GREEN -- 251 modules tsc --strict --noEmit clean, 0 skipped (the prose-only Fowler note added no fence) |
| `node tools/check-catalog.mjs` | 0 | GREEN -- 62/62 (Fowler contract intact after the reverse note) |
| `node tools/check-hygiene.mjs` | 0 | GREEN -- ASCII + email clean over 176 files, 0 no-verbatim WARN |
| check-gof / check-kerievsky / check-extra-patterns / check-smells / check-principles | 0 | GREEN -- 23/23, 27/27, 5/5, 24/24, 8/8 (no regression) |

The 55-FAIL floor is the expected phase-end handoff to plan 06: adding the 55 `Functional alternative:`
lines (each pointing back at the anchors this README already targets) turns check-functional GREEN and
closes FUN-02/FUN-04.

## Deviations from Plan

None affecting scope. Two notes:

- **[Rule 3 - blocking, self-corrected] cwd-drift on the first check-functional run.** The first
  verification command used `cd <main-repo-workspace> 2>/dev/null || cd <worktree-workspace>`; the
  main-repo path exists, so it ran the checker against the MAIN repo's scaffold README (0 rows -> all
  "no README index row"). Re-ran strictly inside the worktree workspace (verified
  `git rev-parse --show-toplevel` ends in `worktrees/agent-...`) and got the true result. No file was
  written to the wrong tree; only a stale reading was discarded. This is the exact `cd ... || fallback`
  trap CLAUDE.md warns about.
- **Every map row carries the target leaf's Use when: verbatim, not just one row per leaf.** C3 only
  requires one mirroring row per slug, but the README scaffold's declared contract is that "each map
  row still mirrors its target leaf's Use when: selector verbatim" -- so all rows carry it. This is
  the declared N:1 contract, not a deviation.

## Known Stubs

None. The README resolves every dissolvable source item to a leaf-or-note and every link resolves
(check-crossrefs GREEN). The 55 remaining check-functional FAILs are the wave-4 `Functional
alternative:` lines on the OO leaves (out of this plan's declared `files_modified`) -- a documented
cross-wave dependency, not a stub in either file this plan touched.

## Threat Flags

None. Both files are Markdown reference content with no runtime/network/user-input surface. The
threat-model mitigations were honored: DST-04 paraphrase on the Fowler reverse note and the README
prose (T-08.2-IP); ASCII + work-email cleanliness via check-hygiene GREEN over 176 files (T-08.2-EM);
no new packages (T-08.2-SC).

## Self-Check: PASSED

- Modified files exist: functional-catalog/README.md and fowler-catalog/replace-loop-with-pipeline.md -- both FOUND.
- Task commits exist on the worktree branch: 597c7ef (Task 1), 61dfda5 (Task 2) -- both FOUND.
- No file deletions beyond the 5 scaffold placeholder lines the README rewrite replaced (Task 1 diff: +140/-5). No shared-artifact (STATE.md / ROADMAP.md / REQUIREMENTS.md) or wave-4-owned (the 19 leaves, the 55 OO-leaf cross-links) edits.

---
*Phase: 08.2-functional-catalog-inserted*
*Completed: 2026-07-07*
