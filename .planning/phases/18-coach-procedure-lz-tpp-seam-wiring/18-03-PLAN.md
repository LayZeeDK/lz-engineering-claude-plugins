---
phase: 18-coach-procedure-lz-tpp-seam-wiring
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
  - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
autonomous: true
requirements: [LAW-02]
must_haves:
  truths:
    - "D-08: the fail-for-the-right-reason content ties the existing red-bar mechanic and F.I.R.S.T. properties to the LAW-02 procedure step -- an AssertionError on the asserted behavior, not a compile / setup error or a false green."
    - "D-14: only the two Phase-18 procedure markers are filled; the STR / ASRT slice and the VIT mechanics slice stay byte-stable; no stale deferral marker remains."
    - "D-12: both fills are lz-red orchestration authored blind (no-oracle); the existing owned F.I.R.S.T. property block is pointed at, not restated."
  artifacts:
    - plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
    - plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
  key_links:
    - "the F.I.R.S.T.-as-red-step-baseline section points at the existing owned F.I.R.S.T. property block (does not restate it)"
    - "the fail-for-the-right-reason procedure section ties the existing Read-the-red-bar mechanic to the LAW-02 step"
---

<objective>
Fill the two co-edited Phase-18 procedure markers that LAW-02 leans on -- both no-oracle orchestration
framing that ties already-authored content to the coach's procedure step. These are the LAW-02
content homes the SKILL.md coach procedure (18-05, step 5) will point at.

- `test-structure-and-assertions.md`: turn the already-owned F.I.R.S.T. properties into the coach's
  test-quality BASELINE that a fresh red test is checked against (point at the existing F.I.R.S.T.
  block; do not restate it).
- `vitest-typescript-mechanics.md`: turn the existing "Read the red bar" MECHANIC into the LAW-02
  PROCEDURE step -- a fresh test must fail on the asserted behavior (an AssertionError), not a compile /
  import / setup error or a false green, before any production code is written.

Both are authored blind as lz-red orchestration (no-oracle); the deterministic no-verbatim scan is the
backstop. Neither introduces a new owned surface.

Purpose: complete the LAW-02 content in the leaves so the coach procedure step 5 has real sections to
link. Output: two filled procedure sections (headings de-marked), rewritten head blockquotes.

## Decisions implemented (D-NN)

- **D-08:** fail-for-the-right-reason = AssertionError on the asserted behavior, not a compile / import / setup error or a false green; F.I.R.S.T. is the test-quality baseline; the coach questions (explains which failure is the right one), it does not run the suite.
- **D-14:** fill only the two Phase-18 markers; keep the STR / ASRT and VIT slices byte-stable; leave no stale deferral marker.
- **D-12:** both fills are no-oracle orchestration authored blind; point at the existing owned F.I.R.S.T. block, do not restate it.

## Artifacts produced

- `test-structure-and-assertions.md`: filled "## F.I.R.S.T. as a red-step baseline" section (heading loses its parenthetical marker); rewritten head blockquote.
- `vitest-typescript-mechanics.md`: filled "## Fail-for-the-right-reason as a procedure" section (heading loses its parenthetical marker); rewritten head note.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-CONTEXT.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-PATTERNS.md
@plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md
@plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill the F.I.R.S.T.-as-a-red-step-baseline procedure section</name>
  <files>plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md</files>
  <read_first>plugins/lz-tdd/skills/lz-red/references/test-structure-and-assertions.md</read_first>
  <action>
Fill the "## F.I.R.S.T. as a red-step baseline" section (insertion-point marker at lines 163-167);
drop the parenthetical marker from the heading. Author BLIND, no-oracle orchestration, original words
(DST-04). The section turns the F.I.R.S.T. properties (already owned and authored in the "## F.I.R.S.T.:
the baseline the pillars sit on" block at lines 106-120) into the coach's standing test-quality
baseline: when a fresh red test is picked and structured, the coach checks it against Fast / Independent
/ Repeatable / Self-validating / Timely as the baseline the LAW-02 fail-for-the-right-reason step sits
on. POINT at the existing F.I.R.S.T. block; do NOT restate the five properties. Keep the phrase
"red-step baseline" in the section so the checker topic matches.

Rewrite the head blockquote sentence (lines 20-21) that says the F.I.R.S.T.-as-baseline procedure step
is deferred, so it declares the now-present procedure framing. The file must contain no remaining
phase-name-plus-number deferral marker afterward. Keep the STR slice and the entire ASRT slice
(including the applyCredit and netOf fences and the four-pillars / F.I.R.S.T. property blocks)
byte-stable.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "test-structure-and-assertions.md"</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs reports PASS for test-structure-and-assertions.md on: the F.I.R.S.T. red-step baseline topic, every retained STR / ASRT topic, the ts-fence requirement, and the no-stale-marker (`absent`) guard.
    - The section heading no longer carries a parenthetical phase marker; the head blockquote declares the present procedure framing.
    - The STR / ASRT content (arrange-act-assert, assert-first, four pillars, the owned F.I.R.S.T. block, both fences) is byte-unchanged.
    - The new section points at the existing F.I.R.S.T. block rather than restating the five properties.
  </acceptance_criteria>
  <done>The F.I.R.S.T.-as-red-step-baseline section is filled no-oracle, the marker is gone, the STR / ASRT slice is intact, and the checker passes this file's topics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill the fail-for-the-right-reason procedure section</name>
  <files>plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md</files>
  <read_first>plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md</read_first>
  <action>
Fill the "## Fail-for-the-right-reason as a procedure" section (insertion-point marker at lines
106-111); drop the parenthetical marker from the heading. Author BLIND, no-oracle orchestration,
original words (DST-04). The section turns the "Read the red bar" MECHANIC (already authored at lines
83-92) into the LAW-02 PROCEDURE step: after a fresh test is written and before any production code,
confirm it fails on the ASSERTED BEHAVIOR (an AssertionError -- expected X, received Y), not on a
ReferenceError / TypeError / import or setup failure (a broken harness) and not as a false green (a
test that passes immediately). The coach QUESTIONS -- it explains which failure is the right one and
why -- it does not run the suite unprompted. Keep a token matching the checker topic (one of "procedure
step", "law-02", or "law 2") in the section. POINT at the "Read the red bar" mechanic above; do not
restate it.

Rewrite the head note sentence (lines 108-110) that says the procedure step is deferred, so it declares
the now-present procedure framing. The file must contain no remaining phase-name-plus-number deferral
marker afterward. Keep the VIT mechanics slice (it.todo, test.each, vi.* restraint, watch, Read-the-red-bar,
the ADV forward-pointers, and the isEven fence) byte-stable.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs 2>&1 | rg "vitest-typescript-mechanics.md"</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs reports PASS for vitest-typescript-mechanics.md on: the fail-for-the-right-reason procedure-step topic, every retained VIT topic, the ts-fence requirement, and the no-stale-marker (`absent`) guard.
    - The section heading no longer carries a parenthetical phase marker; the head note declares the present procedure framing.
    - The VIT mechanics content (the five mechanic sections, the ADV pointers, the isEven fence) is byte-unchanged.
    - The section ties the red-bar mechanic to the LAW-02 procedure step and frames the coach as questioning, not driving.
  </acceptance_criteria>
  <done>The fail-for-the-right-reason procedure section is filled no-oracle, the marker is gone, the VIT slice is intact, and the checker passes this file's topics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content -> committed public repo | maintainer work-email/domain, non-ASCII, and any verbatim source prose must not enter committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Info disclosure (IP) | no-oracle procedure prose in the two leaves | medium | mitigate | Blind own-words authoring + check-hygiene no-verbatim scan over the lz-red tree (both files are in the verbatimTargets set). |
| T-18-02 | Info disclosure | authored Markdown | medium | mitigate | check-hygiene allowlist-inversion work-email scan (approved gmail only). |
| T-18-03 | Tampering (integrity) | authored Markdown | low | mitigate | check-hygiene ASCII scan. |

No high-severity runtime threats: both leaves are Markdown that executes no code. ASVS level 1; block
on `high` (none exist).
</threat_model>

<verification>
- check-red-references.mjs test-structure-and-assertions.md + vitest-typescript-mechanics.md rows PASS.
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0 (the applyCredit / netOf / isEven fences still compile; no new fence added here).
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
</verification>

<success_criteria>
Both LAW-02 procedure sections are filled no-oracle, no stale deferral marker remains in either file,
the STR / ASRT and VIT content is byte-stable, and the checker passes both files' topics.
</success_criteria>

<output>
Create `.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-03-SUMMARY.md` when done.
</output>
