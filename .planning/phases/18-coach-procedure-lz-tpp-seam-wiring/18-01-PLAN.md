---
phase: 18-coach-procedure-lz-tpp-seam-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - .claude/skills/lz-red-workspace/extract-samples.mjs
autonomous: true
requirements: [LAW-01, LAW-02, RTR-02, SEAM-01, SEAM-02, VIT-02]
must_haves:
  truths:
    - "D-13: check-red-references.mjs and extract-samples.mjs are extended IN PLACE (no sibling); the tsc gate and no-verbatim scan stay non-negotiable."
    - "D-14: each co-edited file gains a NET-NEW `absent` guard so no stale deferral marker survives a fill; the RED baseline is asserted before content lands."
    - "D-11: the tsc extractor and a non-ignore ts-fence guard cover the lz-red SKILL.md fence so VIT-02 gets real tsc --strict coverage."
    - "D-09: a NET-NEW checker block reads lz-tpp/SKILL.md and asserts both reverse pointers (SEAM-02)."
    - "D-05: the provenance-honesty gate and its self-test stay byte-intact (3/3)."
    - "D-12: check-hygiene (no-verbatim / ASCII / work-email) over the lz-red tree runs unchanged as the clean-room backstop."
  artifacts:
    - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
    - .claude/skills/lz-red-workspace/extract-samples.mjs
  key_links:
    - "check-red-references.mjs SKILL.md FILES entry resolves the skill ROOT via a per-entry `dir` override"
    - "extract-samples.mjs walk prepends the lz-red SKILL.md path so its fence compiles as SKILL-1.ts"
    - "SEAM-02 standalone block reads plugins/lz-tdd/skills/lz-tpp/SKILL.md (outside REFERENCES)"
---

<objective>
Extend the dev-only lz-red-workspace instrument to the Phase-18 RED baseline (D-13, instrument-first),
then assert it RED before content is authored. The checker must detect the Phase-18 content BEFORE it
exists, so filling a slice turns a guard GREEN rather than passing silently.

Five extensions to `check-red-references.mjs` plus one to `extract-samples.mjs`:
1. A NET-NEW per-entry `absent` field (inverse of `deferral`) + its loop branch: fails if a regex is
   PRESENT. Flip the four co-edited files' `deferral` guards into positive `topics` + an
   `absent: /Phase 18/i` no-stale-marker guard.
2. SKILL.md coverage via a FILES entry with a `dir` base override (skill root): coach-procedure
   content tokens, a non-ignore ts-fence guard, an `absent: /Phase 18/i` guard.
3. A SEAM-02 standalone block reading lz-tpp/SKILL.md asserting BOTH reverse pointers present.
4. The tsc extractor extended to cover the SKILL.md fence.
5. Cosmetic Phase-17 -> Phase-18 header / SUMMARY strings; the D-05 honesty gate block + its import
   kept byte-intact.

Purpose: make every Phase-18 requirement machine-checkable before content lands (Nyquist).
Output: an extended checker + extractor, asserted RED (checker exit 1 by design; typecheck / hygiene /
honesty self-test / plugin-validate all GREEN). No shipped `plugins/lz-tdd` file is touched; NO build
dependency enters `plugins/lz-tdd`.

## Decisions implemented (D-NN)

- **D-13:** extend both instrument files in place, no sibling; tsc gate + no-verbatim scan non-negotiable; SKILL.md entry uses a base-dir override (the leaner option).
- **D-14:** convert each of the four co-edited deferral guards to positive content topics PLUS a NET-NEW `absent` no-stale-marker guard (needle `/Phase 18/i`, since filled content legitimately contains LAW-01 / SEAM-01).
- **D-11:** extend the tsc extractor to the SKILL.md fence and require a NON-ignore ts fence for that entry.
- **D-09:** add a checker block reading lz-tpp/SKILL.md that asserts both the lz-red and lz-refactor reverse pointers (SEAM-02).
- **D-05:** keep provenance-honesty.mjs + provenance-honesty.selftest.mjs + the in-battery honesty gate byte-intact.
- **D-12:** leave check-hygiene.mjs running unchanged as the clean-room backstop over the lz-red tree.

## Artifacts produced

- `check-red-references.mjs`: NET-NEW `absent` field + loop branch; four flipped FILES entries; NET-NEW `SKILL.md` FILES entry with a `dir` override; the `path.join(spec.dir ?? REFERENCES, spec.name)` change; NET-NEW `requireNonIgnoreFence` handling; NET-NEW post-loop SEAM-02 block reading lz-tpp/SKILL.md; refreshed Phase-18 header/SUMMARY strings.
- `extract-samples.mjs`: NET-NEW `SKILL_MD` const prepended to the walk (fence compiles as `SKILL-1.ts`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-CONTEXT.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-RESEARCH.md
@.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-PATTERNS.md
@.claude/skills/lz-red-workspace/tools/check-red-references.mjs
@.claude/skills/lz-red-workspace/extract-samples.mjs
@.claude/skills/lz-red-workspace/tools/lib/provenance-honesty.mjs
@.claude/skills/lz-red-workspace/tools/lib/scaffold-phrases.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `absent` guard field and flip the four co-edited deferral guards</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <read_first>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</read_first>
  <action>
Add a NET-NEW per-entry `absent` field. In the per-file loop, beside the existing `deferral` branch,
add a branch that, when `spec.absent` is set, computes whether any line matches `spec.absent.re` and
reports PASS only when NONE does (the inverse of `deferral`, which asserts a marker REMAINS). Keep the
`deferral` branch for entries that still use it.

Flip the four co-edited FILES entries that carry a `deferral` guard today (three-laws-and-test-selection.md,
test-structure-and-assertions.md, vitest-typescript-mechanics.md, principle-backing.md). For each:
delete the `deferral` key, add `absent: { label: "no stale deferral marker", re: /Phase 18/i }`, and
add positive `topics` asserting the filled content. Keep every existing Phase-16/17 topic (the
regression floor). Do NOT assert absence of `LAW-0` / `SEAM-0` -- filled content legitimately contains
LAW-01 / LAW-02 / SEAM-01 as requirement references; only the literal phase-name-plus-number marker
(`/Phase 18/i`) is the clean D-14 no-stale needle.

Positive topics to add per file:
- three-laws-and-test-selection.md: "Three Laws spine present" `re: /three laws|law 1|law 2|law 3/i`; "classify-first framing" `re: /classify/i`.
- test-structure-and-assertions.md: "F.I.R.S.T. red-step baseline" `re: /red-step baseline/i`.
- vitest-typescript-mechanics.md: "fail-for-the-right-reason procedure step" `re: /procedure step|law-02|law 2/i`.
- principle-backing.md: "Three Laws backing row" `re: /three laws/i`; "lz-tpp seam backing row" `re: /seam|handoff/i`.

Do not touch naming.md, testing-stance/README.md, functional-core.md, message-matrix.md,
seams-and-legacy.md, anti-patterns.md (no Phase-18 slice).
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - check-red-references.mjs parses and runs (exits 1, the RED baseline -- not a crash / syntax error).
    - The four flipped entries no longer carry a `deferral` key; each carries an `absent` key plus the new positive topics.
    - The `absent` branch reports FAIL for each of the four files now (the phase-name deferral marker is still present in the un-filled slices).
    - The provenance-honesty gate block and its `findBookCitedAsOwned` import are unchanged.
  </acceptance_criteria>
  <done>The `absent` field + loop branch exist; the four deferral guards are converted to positive topics + an absent guard; checker exits 1 (RED) by design.</done>
</task>

<task type="auto">
  <name>Task 2: Add SKILL.md coverage, the non-ignore ts-fence guard, and the SEAM-02 lz-tpp block</name>
  <files>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <read_first>.claude/skills/lz-red-workspace/tools/check-red-references.mjs</read_first>
  <action>
The checker reads `references/` only today (every FILES entry resolves via `path.join(REFERENCES, spec.name)`),
and the lz-red SKILL.md sits at the skill ROOT (parent of `references/`), so nothing touches it. Add
SKILL.md coverage with a per-entry base-dir override:

1. Define `SKILL_ROOT` as `plugins/lz-tdd/skills/lz-red` under `repoRoot`, near the `REFERENCES` const.
2. Change the per-file path resolution in the loop to honor an optional `dir`: `path.join(spec.dir ?? REFERENCES, spec.name)`.
3. Add a FILES entry for `SKILL.md` with `dir: SKILL_ROOT`, `requireNonIgnoreFence: true`, an
   `absent: { label: "no stale deferral marker", re: /Phase 18/i }`, and these coach-procedure topics:
   "classify-first" `re: /classify/i`; "Three Laws spine" `re: /three laws|law 1|law 2|law 3/i`;
   "stance routing step" `re: /route|routing/i`; "house test idiom" `re: /house .*idiom|test idiom|idiom/i`;
   "natural-language override" `re: /override|plain language|stance preference/i`;
   "fail for the right reason" `re: /right reason|AssertionError/`; "forward lz-tpp handoff" `re: /lz-tpp/i`.

Add a `requireNonIgnoreFence` branch (do NOT reuse `requireFence` for this entry): when set, assert at
least one bare TypeScript fence-open line -- a fence-open line whose info string is exactly `ts` or
`typescript` with no trailing tokens. Anchor the regex to the start of the line (allow up to three
leading spaces per CommonMark) and to end-of-line right after the language token (`\s*$`), so a
fence-open line whose info string is `ts ignore` does NOT satisfy it. This closes the gap in the
existing `TS_FENCE_RE`, which places a word boundary after the language token and therefore also
matches a coverage-skipping `ts ignore` fence that the tsc extractor silently skips (its trailing
`ignore` token routes it to the skip branch).

Add a SEAM-02 standalone block AFTER the main loop, mirroring the D-05 honesty-gate block idiom (it
reads a path OUTSIDE `REFERENCES`): read `plugins/lz-tdd/skills/lz-tpp/SKILL.md` under `repoRoot`,
guarded by `fs.existsSync`, and report PASS only when BOTH `/lz-red/` AND `/lz-refactor/` appear in it.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The checker output lists SKILL.md checks (seven coach-procedure topics, the non-ignore ts-fence guard, the no-stale-marker guard) and a SEAM-02 lz-tpp block.
    - All SKILL.md topic checks and the SEAM-02 block report FAIL now (placeholder SKILL.md has no procedure tokens / no ts fence / still carries the marker; lz-tpp has no reverse pointer yet).
    - The `dir` override leaves the existing reference entries resolving under `references/` with their prior PASS/FAIL states unchanged.
    - checker exits 1 (RED) by design.
  </acceptance_criteria>
  <done>SKILL.md is covered by content tokens + a non-ignore ts-fence guard + an absent guard via a `dir` override; a SEAM-02 block reads lz-tpp/SKILL.md for both reverse pointers; checker exits 1 (RED).</done>
</task>

<task type="auto">
  <name>Task 3: Extend the tsc extractor to SKILL.md, refresh cosmetic strings, assert the RED baseline</name>
  <files>.claude/skills/lz-red-workspace/extract-samples.mjs, .claude/skills/lz-red-workspace/tools/check-red-references.mjs</files>
  <read_first>.claude/skills/lz-red-workspace/extract-samples.mjs</read_first>
  <action>
In extract-samples.mjs: define `SKILL_MD` as `plugins/lz-tdd/skills/lz-red/SKILL.md` under `repoRoot`
and change the walk loop header from `for (const file of walkMd(REFERENCES))` to
`for (const file of [SKILL_MD, ...walkMd(REFERENCES)])`. The SKILL.md basename is `SKILL` (not
`readme`) so it passes the README-skip guard; a ts fence in it extracts as `SKILL-1.ts`. Preserve the
GREEN-on-empty behavior -- today SKILL.md carries no ts fence, so the extractor still compiles only the
existing reference fences.

In check-red-references.mjs: update the cosmetic Phase-17 references in the header comment block and
the two SUMMARY console strings to Phase-18 (honest extend-in-place). Do NOT touch the D-05
honesty-gate block or the `findBookCitedAsOwned` import.

Then assert the fresh Phase-18 RED baseline across the battery: the content checker is RED by design
(exit 1); the extractor, hygiene scan, honesty self-test, and plugin validation are all GREEN.
  </action>
  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs; test $? -eq 1 && npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - extract-samples.mjs includes SKILL.md in its walk; `npm run typecheck` exits 0 (existing reference fences still tsc --strict clean; SKILL.md contributes 0 fences today).
    - check-red-references.mjs exits 1 (the intended Phase-18 RED baseline).
    - check-hygiene.mjs exits 0; provenance-honesty.selftest.mjs reports 3/3; `claude plugin validate .` exits 0.
    - The checker header + SUMMARY strings say Phase-18; the D-05 honesty gate block is byte-unchanged.
  </acceptance_criteria>
  <done>The extractor covers the SKILL.md fence; cosmetic strings are Phase-18; the RED baseline is asserted (only check-red-references is RED, everything else GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only workspace deps -> shipped tree | the instrument must not introduce a build dependency into `plugins/lz-tdd` |
| authored content -> committed public repo | the instrument enforces the ASCII / work-email / no-verbatim gates on authored content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-SC | Tampering (supply chain) | dev-only lz-red-workspace deps | low | accept | No new dependency added this phase; shipped `plugins/lz-tdd` stays dependency-free Markdown; the two devDeps (typescript 6.0.3, vitest 4.1.10) were legitimacy-checked in Phase 16. |
| T-18-GATE | Tampering (integrity) | check-red-references.mjs / extract-samples.mjs | low | mitigate | Extend in place, keep the D-05 honesty gate + hygiene scan intact; assert the RED baseline so a later false-GREEN is impossible without real content. |

No high-severity runtime threats: the instrument is dev-only Node scripts that read repo files and
write a throwaway `samples/` dir they own; the shipped artifacts execute no code. ASVS level 1; block
on `high` (none exist).
</threat_model>

<verification>
- `node .claude/skills/lz-red-workspace/tools/check-red-references.mjs` exits 1 (RED baseline by design).
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0.
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- `node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs` reports 3/3.
- `claude plugin validate .` exits 0.
</verification>

<success_criteria>
The instrument detects all Phase-18 content (four flipped guards + SKILL.md entry + SEAM-02 block +
SKILL.md tsc coverage) and is asserted RED against the current placeholder / stubs, with the rest of
the battery GREEN. No shipped file changed; no dependency added.
</success_criteria>

<output>
Create `.planning/phases/18-coach-procedure-lz-tpp-seam-wiring/18-01-SUMMARY.md` when done.
</output>
