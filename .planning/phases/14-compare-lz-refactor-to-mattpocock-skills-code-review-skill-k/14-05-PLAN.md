---
phase: 14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k
plan: 05
type: execute
wave: 2
depends_on: ["14-03", "14-04"]
files_modified:
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/mechanical.json
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/claims.json
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/fidelity.json
  - .claude/skills/lz-refactor-workspace/grading/head-to-head/summary.json
  - .planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESULTS.md
autonomous: true
requirements: [D-04, D-05, D-11, D-03]
must_haves:
  truths:
    - "Mechanical dimensions (token/cost, tool usage + sub-agent spawn count, lift count, Pass@k) are tabulated from the run meta.json into mechanical.json"
    - "Each arm's named smells/refactorings/patterns are normalized in main context into a claim list, then graded per-claim pass/partial/fail by the oracle/oracle-reviewer against .oracle/ (DST-04) -- main context never reads book prose; functional idioms grade against functional-depatterning-ts.md"
    - "14-RESULTS.md tabulates lz-refactor vs code_review per dimension per cell, states the D-02 whole-file-diff caveat AND the D-03 Spec-axis-skip asymmetry, and frames the verdict as an empirical finding (never a pre-assumed win)"
    - "The ~139-vs-12 idiom/pattern vocabulary-breadth asymmetry is reported as the headline on that axis, not normalized away"
    - ">= 1 unbiased from-scratch reviewer confirms the verdict, and a public-repo hygiene (ASCII + allowlist-inversion) scan passes before commit"
  artifacts:
    - path: ".planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESULTS.md"
      provides: "the head-to-head record + empirical verdict + mandatory caveats"
      contains: "Verdict"
    - path: ".claude/skills/lz-refactor-workspace/grading/head-to-head/fidelity.json"
      provides: "per-claim oracle book-authenticity + idiom/pattern verdicts per arm (agent's own words)"
  key_links:
    - from: "main context (normalized claim list)"
      to: "oracle / oracle-reviewer agents (.oracle/ books)"
      via: "DST-04 firewall -- claims handed to the agent; verdicts returned in the agent's words; main context never reads book prose"
      pattern: "oracle"
  decisions: |
    - **D-03:** code-review has Standards + Spec axes; only Standards is comparable to lz-refactor's smell scan; the kata/nx corpus has no originating issue/PRD, so the Spec sub-agent skips ("no spec available") -- EXPECTED, reported as a structural asymmetry in 14-RESULTS.md, not a defect.
    - **D-04:** Report all dimensions -- Lift, Token usage, Tool usage, Output quality, Book authenticity, Over-/under-engineering, TS/FP/OOP idioms+patterns -- MECHANICAL from meta, GRADED via oracle/judge; the ~139-vs-12 vocabulary-breadth asymmetry is the headline on the idiom/pattern axis, reported not normalized.
    - **D-05:** Book-authenticity + idiom/pattern grading routes through oracle/oracle-reviewer against git-ignored .oracle/; main context NEVER reads book prose; per-claim pass/partial/fail per arm in the agent's own words; functional idioms grade against .planning/research/functional-depatterning-ts.md (no-oracle tier) + the shipped functional-catalog.
    - **D-11:** 14-RESULTS.md tabulates per cell per dimension; the verdict is an EMPIRICAL finding, never a pre-assumed win; MUST state the D-02 whole-file-diff caveat + the D-03 Spec-axis-skip asymmetry; include >= 1 unbiased from-scratch grading reviewer; reuse the E2E-RESULTS.md / 13-RESULTS.md format conventions.
---

<objective>
Grade the head-to-head across all seven dimensions and write the verdict. Mechanical dimensions come from the
run meta.json via the Wave-0 tabulator; graded dimensions (book authenticity, idiom/pattern fidelity, output
quality, over/under-engineering) route through the oracle/oracle-reviewer agents (DST-04) and a judge; then
`14-RESULTS.md` tabulates lz-refactor vs code_review per dimension with the mandatory caveats and an empirical
verdict, confirmed by an unbiased from-scratch reviewer and cleared by a public-repo hygiene scan.

Purpose: produce the terminal comparison record for milestone lz-tdd@0.0.2 -- where the book-grounded catalog
measurably differs from a general code-review skill, stated as a finding.
Output: `grading/head-to-head/{mechanical,claims,fidelity,summary}.json` + `14-RESULTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESEARCH.md
@.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-PATTERNS.md
@.planning/phases/13-lz-refactor-vs-base-opus-eval-book-authenticity-correctness/13-RESULTS.md

DST-04 firewall (hard): the oracle / oracle-reviewer agents are the ONLY channel to `.oracle/`. Main context
normalizes each arm's `answer.md` into a claim list and hands THAT list to the agent; verdicts come back in
the agent's own words. Main context NEVER reads `.oracle/` book prose. Functional idioms grade against
`.planning/research/functional-depatterning-ts.md` + the shipped functional-catalog (no-oracle tier).
Vocabulary shipped (from RESEARCH, VERIFIED): lz-refactor ~139 named refactorings/patterns/idioms + 28 smells;
code-review = 12 Fowler smells, no refactoring/pattern/idiom vocabulary.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabulate the mechanical dimensions</name>
  <files>.claude/skills/lz-refactor-workspace/grading/head-to-head/mechanical.json</files>
  <read_first>
    - grading/head-to-head/tabulate-mechanical.mjs (built in 14-03) + its --selfcheck. The captured run meta.json under both suites' `results/recommend/`. 14-RESEARCH.md "Meta Capture Detail" (headline cost = total_cost_usd/model_usage; sub-agent spawn count) + "Grading Architecture" (Lift = distinct named refactorings/smells).
  </read_first>
  <action>
    Run `tabulate-mechanical.mjs` over the two phase-14 recommend result trees to produce `mechanical.json`: per (cell, arm) token/cost (headline `total_cost_usd` + opus `model_usage`; `input/output_tokens` as main-context-only), the `tool_calls` histogram + the sub-agent SPAWN count (expected ~2 for code_review, 0 for lz-refactor -- the D-04 headline tool-usage finding), the LIFT count of distinct named refactorings/smells, and Pass@k where run counts allow. Cross-check the token headline against the A1 finding recorded in 14-04 (which field fairly rolls up code_review's sub-agents). Do not hand-count meta.json; use the self-checked tabulator.
  </action>
  <acceptance_criteria>
    - `mechanical.json` has a per-(cell,arm) row for both arms with token/cost, tool histogram + spawn count, lift count, and Pass@k.
    - The sub-agent spawn count is 0 for lz-refactor and >= 1 for code_review (the tool-usage asymmetry).
  </acceptance_criteria>
  <verify>
    <automated>node .claude/skills/lz-refactor-workspace/grading/head-to-head/tabulate-mechanical.mjs --selfcheck</automated>
  </verify>
  <done>The mechanical dimensions are tabulated deterministically from meta.json.</done>
</task>

<task type="auto">
  <name>Task 2: Oracle-graded dimensions (book authenticity + idiom/pattern + quality + proportionality) -- DST-04</name>
  <files>.claude/skills/lz-refactor-workspace/grading/head-to-head/claims.json, .claude/skills/lz-refactor-workspace/grading/head-to-head/fidelity.json</files>
  <read_first>
    - grading/book-authenticity/claims.json (per-claim record shape) + 13-RESULTS.md line 10 (the DST-04 grading-channel posture). targets.json (both suites) `smells`/`expected_family`/`judgment` = correctness + proportionality ground truth. 14-RESEARCH.md "Grading Architecture (D-04/D-05)" + "The idiom/pattern breadth asymmetry". `.planning/research/functional-depatterning-ts.md` (FP idiom anchor). MEMORY.md [[unbiased-review-beats-primed]].
  </read_first>
  <action>
    In main context ONLY (never reading `.oracle/`), normalize each arm's `answer.md` for every graded cell/run into a claim list -- the named smells, refactorings, and patterns each arm surfaced -- into `claims.json` (reuse the Phase-13 record shape; `arm` in {lz-refactor, code_review}). Hand the normalized claim list to the `oracle` (open-ended book mechanics) / `oracle-reviewer` (gates a drafted per-claim list) agents for a per-claim pass/partial/fail BOOK-AUTHENTICITY verdict against `.oracle/` (Fowler/Kerievsky/GoF) and an IDIOM/PATTERN fidelity verdict (FP idioms vs functional-depatterning-ts.md + the shipped functional-catalog; OO patterns via the oracle) -- verdicts in the agent's own words -> `fidelity.json`. Fetch reusable per-(book, refactoring/smell) mechanics ground truth ONCE and reuse across arms, PROVIDED each per-claim verdict stays oracle-owned. Use a judge read of `answer.md` vs `targets.json.judgment` for OUTPUT QUALITY and OVER-/UNDER-ENGINEERING (proportionality: no speculative patterns, no missed simplifications -- e.g. the polymorphism-decline traps). Record which claims each arm could NOT make (code_review has no refactoring/pattern/idiom vocabulary -- the ~139-vs-12 asymmetry) as data, not a normalization.
  </action>
  <acceptance_criteria>
    - `claims.json` lists every named smell/refactoring/pattern per (cell, arm, run); main context never read `.oracle/` prose.
    - `fidelity.json` carries a per-claim pass/partial/fail book-authenticity + idiom/pattern verdict per arm, in the agent's own words.
    - Output quality + over/under-engineering are judged against targets.json.judgment; the vocabulary-breadth asymmetry is recorded as data.
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- oracle/judge-graded; verified via the DST-04 agent channel + the unbiased reviewer in Task 3 (14-VALIDATION.md Manual-Only)</automated>
  </verify>
  <done>Every graded dimension has per-claim oracle/judge verdicts per arm via the DST-04 firewall.</done>
</task>

<task type="auto">
  <name>Task 3: 14-RESULTS.md + summary.json + unbiased review + hygiene</name>
  <files>.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESULTS.md, .claude/skills/lz-refactor-workspace/grading/head-to-head/summary.json</files>
  <read_first>
    - 13-RESULTS.md (full: header block, one table per dimension, per-table finding, Caveats, Verdict framing) + grading/head-to-head/summary.template.json (the rollup shape from 14-03). 14-PATTERNS.md "14-RESULTS.md" section (mandatory caveats + ASCII/hygiene). AGENTS.md "Public-repo hygiene" (allowlist-inversion, only the approved gmail). MEMORY.md [[unbiased-review-beats-primed]].
  </read_first>
  <action>
    Fill `summary.json` from `summary.template.json` with the mechanical + graded numbers (Pass@k where run counts allow). Author `14-RESULTS.md` reusing 13-RESULTS.md's structure: header block (skill under test, model/config, arms lz-refactor vs code_review, borrowed repos, graded cells, run-artifact paths, DST-04 grading-channel note); ONE table per D-04 dimension (Lift, Token usage, Tool usage, Output quality, Book authenticity, Over/under-engineering, TS/FP/OOP idioms+patterns) cell x arm; a one-line finding per table; a Caveats section stating BOTH mandatory caveats -- the D-02 whole-file-diff off-grain caveat AND the D-03 Spec-axis-skip asymmetry (plus the A4 plugin-context-inflation note and the token measurement-boundary note from 14-04); and a Verdict framed as an EMPIRICAL finding (where the book-grounded catalog measurably differs from a general code-review skill), NEVER a pre-assumed win -- the ~139-vs-12 idiom/pattern breadth is the headline on that axis, and a parity-with-broader-vocabulary result is a publishable finding, not a failure. Then spawn >= 1 UNBIASED from-scratch reviewer (no prior findings) to confirm the tables match the grading records and the verdict is not overclaimed; apply fixes it flags. Before finishing, run a public-repo hygiene scan: ASCII-only (no byte > 0x7F) across `14-RESULTS.md` + the grading JSON, and allowlist-inversion for email-shaped tokens (assert only the approved gmail appears; never write the forbidden value as a needle). Confirm both borrowed repos are still `git status` clean.
  </action>
  <acceptance_criteria>
    - `14-RESULTS.md` has one table per the seven dimensions, states BOTH the D-02 and D-03 caveats, and frames the verdict as an empirical finding with the idiom/pattern breadth headline.
    - An unbiased from-scratch reviewer confirmed the verdict against the grading records; its outcome + any fixes are recorded.
    - `14-RESULTS.md` + grading JSON are ASCII-only and pass allowlist-inversion (only the approved gmail present); both borrowed repos are clean.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const p=".planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-RESULTS.md";if(fs.readFileSync(p).some(x=>x>0x7f))throw new Error("non-ASCII in 14-RESULTS.md");JSON.parse(fs.readFileSync(".claude/skills/lz-refactor-workspace/grading/head-to-head/summary.json","utf8"));console.log("OK")'</automated>
  </verify>
  <done>14-RESULTS.md states both mandatory caveats, frames an empirical verdict, is unbiased-reviewer-confirmed, and passes the ASCII + email-allowlist hygiene scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main context <-> oracle agents | Book ground truth flows only through the oracle; source prose must never cross back into committed output |
| grading + results -> committed public repo | 14-RESULTS.md + grading JSON carry maintainer PII + copyright hygiene risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | .oracle/ book prose leaking into committed verdicts | mitigate | DST-04 firewall: oracle/oracle-reviewer only; main context never reads book prose; verdicts in the agent's own words; ASCII-only |
| T-14-03 | Information Disclosure | committed 14-RESULTS.md + grading JSON | mitigate | ASCII-only scan + allowlist-inversion email scan before commit (only the approved gmail present; forbidden value never written as a needle) |
| T-14-09 | Tampering (measurement validity) | an overclaimed / primed verdict | mitigate | >= 1 unbiased from-scratch reviewer confirms tables vs grading records; verdict framed as empirical finding, never a pre-assumed win |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | N/A -- no packages installed; grading uses in-repo tabulator + GSD oracle agents only |
</threat_model>

<verification>
- `node grading/head-to-head/tabulate-mechanical.mjs --selfcheck` exits 0 and `mechanical.json` is populated.
- `claims.json`/`fidelity.json` carry per-claim oracle verdicts per arm; main context never read `.oracle/` prose.
- The Task 3 automated check (ASCII + summary.json parse) exits 0; an unbiased reviewer confirmed the verdict.
- Both borrowed repos are `git status` clean.
</verification>

<success_criteria>
14-RESULTS.md tabulates lz-refactor vs code_review across all seven dimensions, states the D-02 and D-03 caveats, reports the idiom/pattern vocabulary-breadth headline, frames the verdict as an empirical finding, is confirmed by an unbiased reviewer, and passes public-repo hygiene -- closing the terminal measurement phase of milestone lz-tdd@0.0.2.
</success_criteria>

<output>
Create `.planning/phases/14-compare-lz-refactor-to-mattpocock-skills-code-review-skill-k/14-05-SUMMARY.md` when done.
</output>
