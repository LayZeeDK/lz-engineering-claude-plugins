---
phase: 07-fowler-catalog-refactoring-2nd-ed
plan: 06
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/simplify-conditional-logic.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/refactoring-apis.md
  - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/errors.md
autonomous: false
requirements: [FWL-01, FWL-04]
must_haves:
  truths:
    - "D-02: each of the 20 entries in this batch fills the per-entry contract exactly -- 2nd-ed name (+ 1st-ed alias), distilled motivation, distilled mechanics, a minimal focused TypeScript before->after example, and a provenance label"
    - "D-06: the four print-absent + entries in this batch (Replace Control Flag with Break, Return Modified Value, Replace Error Code with Exception, Replace Exception with Precheck) each carry [web-only]"
    - "D-07: an AskUserQuestion oracle-access checkpoint is opened for this batch BEFORE authoring any entry"
    - "FWL-04: every before AND after ts fence in this batch compiles tsc --strict --noEmit clean"
  artifacts:
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/simplify-conditional-logic.md"
      provides: "the 7 simplify-conditional-logic-tag entries"
      contains: "Decompose Conditional"
    - path: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/refactoring-apis.md"
      provides: "the refactoring-apis-tag entries (errors fold here under coarse-7)"
      contains: "Separate Query from Modifier"
  key_links:
    - from: "plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/*.md"
      to: ".claude/skills/lz-refactor-workspace/samples/*.ts"
      via: "extract-samples.mjs pulls each ts fence into a compiled module"
      pattern: "```ts"
---

<objective>
Author the "simplify-conditional-logic", "refactoring-apis", and "errors" tag-group content
-- 20 entries -- behind the mandatory D-07 oracle checkpoint. Leaf set fixed by 07-02: under
coarse-7 the errors entries fold into refactoring-apis.md; under fine-20 they go to
errors.md (owned by this plan either way).

Batch (disjoint from other content plans):
- simplify-conditional-logic (7): Decompose Conditional, Consolidate Conditional
  Expression, Replace Nested Conditional with Guard Clauses, Replace Conditional with
  Polymorphism, Introduce Special Case (alias Introduce Null Object), Introduce Assertion,
  Replace Control Flag with Break (alias Remove Control Flag) [web-only].
- refactoring-apis (11): Separate Query from Modifier, Parameterize Function (alias
  Parameterize Method), Remove Flag Argument (alias Replace Parameter with Explicit
  Methods), Preserve Whole Object, Replace Parameter with Query (alias Replace Parameter
  with Method), Replace Query with Parameter, Remove Setting Method, Replace Constructor
  with Factory Function (alias Replace Constructor with Factory Method), Replace Function
  with Command (alias Replace Method with Method Object), Replace Command with Function,
  Return Modified Value [web-only].
- errors (2): Replace Error Code with Exception [web-only], Replace Exception with Precheck
  (alias Replace Exception with Test) [web-only].

Purpose: FWL-01 coverage for these 20 + FWL-04 compile-clean examples.
Output: populated leaf files, each entry meeting the D-02 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-RESEARCH.md
@.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-PATTERNS.md
@.planning/research/refactoring-com-overview.md
@plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md
@plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md

<interfaces>
Same per-entry leaf template as 07-04 (D-02 contract): `### <Name>  [provenance]` heading
with the tag on the heading line, optional alias line, Motivation, Mechanics, one/two tsc
--strict-clean ts fences. Four entries in this batch are [web-only]; the rest [print+web].
Leaf names + whether errors is its own leaf come from 07-02-SUMMARY. Match the leaf shape
authored in 07-04.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-07 oracle checkpoint -- simplify-conditional + apis + errors batch</name>
  <read_first>
    - .planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-CONTEXT.md (D-02 contract; D-06 provenance; D-07 oracle mandate)
    - .planning/research/refactoring-com-overview.md (the 20 names + aliases + the 4 + entries in this batch)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (legend + contract)
  </read_first>
  <what-needed>
    The authoritative motivation + mechanics for the 20 named refactorings in this batch,
    from the owner's Fowler Refactoring 2nd-ed e-book / web edition (ISBN 9780135425664).
    Four are print-absent + entries. Mechanics/motivation MUST come from the oracle (D-07).
  </what-needed>
  <how-to-provide>
    Open the AskUserQuestion oracle-access checkpoint batched by these tag-groups. The owner
    confirms/provides each refactoring's substance for distillation in ORIGINAL words.
  </how-to-provide>
  <action>
    Open the AskUserQuestion oracle-access checkpoint for this batch and BLOCK: author NO
    entry until the owner confirms the batch substance (D-07). Verify in this tag-group batch,
    not one refactoring at a time. This gate survives the --auto/--chain run.
  </action>
  <resume-signal>Owner confirms the substance for the 20 refactorings in this batch</resume-signal>
  <acceptance_criteria>
    - The AskUserQuestion oracle-access checkpoint is opened and the owner has confirmed this batch's substance BEFORE any entry is written.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author simplify-conditional-logic.md (7 entries)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/simplify-conditional-logic.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (contract + legend)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/basic.md (leaf shape from 07-04)
    - plugins/lz-tdd/skills/lz-tpp/references/typescript-and-tco.md (tsc --strict-clean fence conventions)
  </read_first>
  <action>
    Author the 7 simplify-conditional-logic entries: Decompose Conditional, Consolidate
    Conditional Expression, Replace Nested Conditional with Guard Clauses, Replace
    Conditional with Polymorphism, Introduce Special Case (alias Introduce Null Object),
    Introduce Assertion, Replace Control Flag with Break (alias Remove Control Flag). Each
    follows the D-02 contract with distilled Motivation + Mechanics (original words from the
    oracle batch) and a minimal focused TypeScript before->after example, both fences tsc
    --strict-clean. Mark Replace Control Flag with Break `[web-only]`; the rest `[print+web]`.
    NO verbatim Fowler prose/code (DST-04). ASCII only. Contents TOC if over ~100 lines.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` + `tsc --strict --noEmit` exits 0 over the batch's modules (FWL-04).
    - `node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` shows all 7 simplify-conditional-logic names PASS.
    - `rg -n "Replace Control Flag with Break.*\[web-only\]" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/simplify-conditional-logic.md` matches (D-06).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author refactoring-apis.md + errors (13 entries)</name>
  <files>plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/refactoring-apis.md, plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/errors.md</files>
  <read_first>
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/README.md (contract + legend; whether errors is its own leaf per 07-02-SUMMARY)
    - plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/simplify-conditional-logic.md (match shape from Task 2)
    - .planning/research/refactoring-com-overview.md (the 13 apis+errors names + aliases)
  </read_first>
  <action>
    Author the 11 refactoring-apis entries (Separate Query from Modifier, Parameterize
    Function, Remove Flag Argument, Preserve Whole Object, Replace Parameter with Query,
    Replace Query with Parameter, Remove Setting Method, Replace Constructor with Factory
    Function, Replace Function with Command, Replace Command with Function, Return Modified
    Value) plus the 2 errors entries (Replace Error Code with Exception, Replace Exception
    with Precheck). Under coarse-7 put all 13 in refactoring-apis.md; under fine-20 put the 2
    errors entries in errors.md. Same D-02 contract; mark Return Modified Value, Replace
    Error Code with Exception, and Replace Exception with Precheck `[web-only]`; the rest
    `[print+web]`. Minimal focused TypeScript before->after, both fences tsc --strict-clean.
    NO verbatim Fowler prose/code (DST-04). ASCII only. Contents TOC if over ~100 lines.
  </action>
  <acceptance_criteria>
    - `node .claude/skills/lz-refactor-workspace/extract-samples.mjs` + `tsc --strict --noEmit` exits 0 over the batch's modules (FWL-04).
    - `node .claude/skills/lz-refactor-workspace/tools/check-catalog.mjs` shows all 13 apis+errors names PASS.
    - `rg -n "Return Modified Value.*\[web-only\]|Replace Error Code with Exception.*\[web-only\]|Replace Exception with Precheck.*\[web-only\]" plugins/lz-tdd/skills/lz-refactor/references/fowler-catalog/` returns 3 matches (D-06).
    - `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner e-book -> authored leaf files | oracle mechanics cross into public-repo files |
| authored leaves -> public GitHub repo | world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / legal | verbatim Fowler prose or code in the 20 entries | mitigate | DST-04 distilled voice + ORIGINAL TS examples; check-hygiene no-verbatim heuristic + Phase-10 scan |
| T-07-03 | Tampering | fabricated mechanics bypassing the oracle | mitigate | D-07 blocking checkpoint BEFORE authoring; batched by tag-group |
| T-07-02 | Information Disclosure | work email in a leaf | mitigate | check-hygiene work-email allowlist gate |
| T-07-05 | Tampering | a "before" example differing from "after" only by a compile error | mitigate | both fences compile tsc --strict-clean (FWL-04 harness) |

No high-severity threats: prose + original-code authoring behind a blocking human oracle
checkpoint. Nothing rises to the `block_on: high` bar.
</threat_model>

<verification>
- The oracle checkpoint was opened before any entry was authored.
- check-catalog.mjs shows all 20 batch names PASS; the 4 + entries carry [web-only]; extract + tsc exits 0; check-hygiene exits 0.
</verification>

<success_criteria>
- 20 entries authored to the D-02 contract across simplify-conditional-logic.md + refactoring-apis.md (+ errors.md if fine-20).
- All examples tsc --strict-clean (FWL-04); the 4 + entries provenance-labeled (D-06); ASCII-only; no verbatim prose.
</success_criteria>

<output>
Create `.planning/phases/07-fowler-catalog-refactoring-2nd-ed/07-06-SUMMARY.md` when done.
</output>
