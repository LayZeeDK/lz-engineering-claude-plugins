# Continue Here -- Phase 7 (Fowler Catalog, Refactoring 2nd ed)

**Paused:** 2026-07-05 at ~99% context. **Status:** mid-execute-phase 7, interrupted during oracle-agent hardening.

## Where we are

`execute-phase 7` is IN PROGRESS. The phase was heavily re-designed mid-execution (the D-07 oracle
checkpoint surfaced that the original plans' design was wrong), so the committed CONTEXT/REQUIREMENTS/
ROADMAP and PLANs 07-02..07-08 are STALE and a re-plan is pending (see Next steps).

**Done:**
- Wave 1: `07-01` harness + FWL checkers committed (SUMMARY exists). DONE.
- `07-03` `principles.md` authored blind + oracle-converged (Round 2 clean, no substantive findings). DONE.
- Locked design docs (authoritative -- READ THESE FIRST): `07-ROUTING-ARCHITECTURE.md`,
  `07-ORACLE-MODEL.md`.
- **Clean-room agents CONVERGED at round-14** (2026-07-05): `.claude/agents/oracle.md`,
  `.claude/agents/oracle-reviewer.md`. Five blind review rounds (r5/r7/r9/r11/r13), 3 lenses each
  (firewall / authoring / logic), un-primed fresh agents each round; fixes in rounds 6/7/8/10/12/14
  (see `git log`). Final (r13) verdicts: firewall "neither needs work, firewall-sound"; logic "gate
  is tight, NO HIGH"; authoring (r11) "ship-as-is". Verdict partition is MECE + exhaustive
  (`pass|blocked|error|revise`); oracle-reviewer body 9748/10000, oracle 5354. The detailed per-axis
  rubric + driver-side notes (fan-out aggregation, loop-bound/oscillation guard) live in
  07-ORACLE-MODEL.md under "Driver responsibilities + canonical anchors".
- Residual (NOT defects -- eval targets, per the firewall reviewer): `too_close_reason` and drifted
  `note` are the highest-tension fields (judgment-reliance inherent to clean-room); target them with
  adversarial book fixtures during EVL evals, not more spec edits. 07-ORACLE-MODEL.md's early
  "Firewall"/"Verdict schema" blocks are STALE (pre-round-2: `Read, Grep, Glob` + old schema) --
  reconcile in the replan; the live agents + the round-10 driver-notes section are authoritative.

## Locked decisions (durable in committed docs -- do not re-litigate)

- **Scope: 62 refactorings** (not 66). CUT 4 web-only 1st-ed relics: Replace Magic Literal, Replace
  Control Flag with Break, Replace Error Code with Exception, Replace Exception with Precheck. KEEP
  Return Modified Value (genuinely-new web-only, tagged `[web-only]`).
- **Routing (07-ROUTING-ARCHITECTURE.md):** per-refactoring leaves + a chapter-grouped catalog index;
  smell leaves + a `smells.md` recognize-by index; `principles.md`. Coach path smell->smell-leaf
  (confirm+candidates)->refactoring leaf; reference path name->leaf; non-smell reasons via catalog
  Use-when + principles triggers. Leaf format: `Use when` + Motivation + Mechanics (every decision
  branch + safety checkpoints) + 1 compact tsc-clean before/after example (behavior-preserving) +
  Watch-for; depth scales; optional `<name>-walkthrough.md` for rich ones. Smell leaf: recognize-by +
  how-to-recognize + why + conditional code sketch (structural smells only) + candidate refactorings
  (discriminators + links). SKILL.md NOT re-touched. leaf->principles back-edge allowed.
- **Oracle model (07-ORACLE-MODEL.md):** clean-room. Books live in git-ignored `.oracle/` (Fowler
  `.oracle/refactoring-2e/`, Kerievsky `.oracle/refactoring-to-patterns/`, each with `index.md`).
  MAIN CONTEXT NEVER READS `.oracle/` (only `ls` for names). Two agents: `oracle` (open-ended Q&A/
  survey) + `oracle-reviewer` (fidelity GATE, pass/revise/blocked verdict, per-content-type rubric,
  navigates via index.md). Loop: author BLIND -> oracle-reviewer gate -> revise -> iterate to
  convergence; `oracle` for lookups; ask the owner only for `ambiguities` the source can't settle.
- **Oracle availability:** oracle agents ONLY for `.oracle`-present books (Fowler, Kerievsky).
  AskUserQuestion is the oracle path for GoF / Kent Beck / Robert C. Martin (NOT in `.oracle` --
  owner has no Markdown). AskUserQuestion is retired as the oracle path ONLY for `.oracle` books.
- **principles.md split:** on-demand reference PLUS a small set of always-loaded SKILL.md coach GATES
  (Phase 9): two-hats/lz-tpp seam, serve-delivery/when-to-stop, and the SAFETY-CRITICAL
  atomic-boundary tripwire (published API or persisted data/schema -> unit tests don't prove safe ->
  parallel change / expand-contract).
- **Pacing:** pilot the `basic` chapter first (calibrate the leaf loop + rubric + harness), THEN
  parallel-fan-out the remaining 6 chapters, smells last.

## Critical Anti-Patterns

| Anti-pattern | Severity | How it is prevented |
|---|---|---|
| Main context reads `.oracle/` copyrighted book files | blocking | Only `oracle`/`oracle-reviewer` subagents read `.oracle/` (isolated context, own-words verdicts). Main context uses `ls` for names only. |
| Invoking `oracle`/`oracle-reviewer` before `/reload-plugins` | warning | The live registry is STALE (pre-fix agents still have `Grep`). Tell the user to run `/reload-plugins` before the first real invocation. |
| Using AskUserQuestion as the oracle path for Fowler/Kerievsky | warning | Those are in `.oracle/` -> use the clean-room agents. AskUserQuestion only for GoF/Beck/RCM. |
| Fabricating catalog/smell/principle content instead of the oracle loop | blocking | Author blind, gate via `oracle-reviewer` until a review round is clean; no verbatim (DST-04). |

## Next steps (resume order)

1. **DONE -- agents converged (round-14).** No further oracle-agent review rounds owed.
2. **Tell the user to run `/reload-plugins`** (make the converged, Grep-free agents live) before any
   real `oracle`/`oracle-reviewer` invocation (i.e. before executing Wave 2+). <- user action pending.
3. **Consolidated scope-correction RE-PLAN:**
   - REQUIREMENTS.md FWL-01/FWL-04: 66->62 + the web-only cut + "verified via the oracle-reviewer
     loop, not AskUserQuestion".
   - ROADMAP.md: Phase 7 summary (line ~44) + Goal (~70) + SC1 (~76) + SC4 (~79) 66->62 + clean-room
     oracle; add a Phase-8 note (Kerievsky in `.oracle`, GoF via AskUserQuestion); add a milestone
     oracle-workflow note; AskUserQuestion retired only for `.oracle` books.
   - PROJECT.md: add the oracle-access convention (for future milestones).
   - 07-CONTEXT.md: it already has a top supersede banner (2026-07-05) pointing to the two design
     docs + 62 scope + AskUserQuestion-superseded. Reconcile the stale D-NN if needed.
   - Regenerate PLANs 07-02..07-08 (gsd-planner) encoding: 62 scope, routing tiers, leaf/smell/
     principles formats + discipline, the blind-author->oracle-reviewer-gate->converge loop (+ oracle
     for lookups; `.oracle` via index.md; firewall), harness augmentations (bidirectional cross-refs,
     self-link + scaffolding-phrase bans, set/chapter completeness), pilot-basic-then-parallel pacing.
     Preserve 07-01 DONE; mark 07-03 CONVERGED/done. Then gsd-plan-checker. Driver must bound loop
     iterations (oscillation guard).
4. **Resume execute-phase Waves 2-4** (basic pilot first) with executors using the oracle agents.

## Notes
- `.planning/config.json` shows modified (the `_auto_chain_active` chain flag) -- leave it.
- Harness checkers live in `.claude/skills/lz-refactor-workspace/` (gitignored-record pattern);
  `samples/` is gitignored.
- Memory: this milestone's phases 6-11 are Markdown skill authoring, NO UI (skip UI-SPEC; the
  plan-phase UI gate false-positives on "view"/"form" substrings).
