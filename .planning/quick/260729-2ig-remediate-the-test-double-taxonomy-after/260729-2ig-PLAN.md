---
phase: quick-260729-2ig
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - 2IG-01   # Repair the Defines-or-uses rule: split a NAMING-citation from a MEANING-citation
  - 2IG-02   # Repair the ambiguity finding: universal quantifier -> survey with a re-derived count
  - 2IG-03   # CUT the degree-of-freedom proof (pure deletion)
  - 2IG-04   # CUT the deliberate-negative INTENT claim, keep the blank-column FACT
  - 2IG-05   # Five-kinds ATTRIBUTION correction at nine sites + explicit enumeration
  - 2IG-06   # Mis-levelling: ONE cell (Temporary Test Stub states its relationship)
  - 2IG-07   # Unpopulated cells get no bullet and no commentary; never assert a cell is empty
  - 2IG-08   # Tier-scoped-out sources removed from the list that uses them as evidence
  - 2IG-09   # Skill-relative content fixed by naming lz-red explicitly
  - 2IG-10   # machine-enforced claim: BOTH fixes (widen G17 scope AND narrow the wording)
  - 2IG-11   # Gang of Four back matter: three appendices, non-exhaustive enumeration, scope-hedged absence, positive remote-variant finding, skeleton refinements
  - 2IG-12   # Six row-scoped guards, each proven against its specific evasion
  - 2IG-13   # Every stated count re-derived from its list and machine-asserted
  - 2IG-14   # Roster integrity: exact emitted-check count
  - 2IG-15   # Carry the non-optional kanban qualifier the taxonomy currently drops
files_modified:
  - .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
  - .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs
  - .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
  - .claude/skills/lz-red-workspace/tools/check-red-references.mjs
  - .claude/skills/lz-red-workspace/package.json
  - plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
  - plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
  - .planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-RED-BASELINE.md
  - .planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-SUMMARY.md

must_haves:
  truths:
    - The five-kinds count is attributed to the hierarchy figure, never to the author as his own framing, at every occurrence that survives -- EIGHT rewritten, plus the ninth deleted outright by E3; a caveat records that his prose states FOUR by folding two members into one bullet.
    - The document enumerates the five kinds explicitly, and every enumerated name resolves to a table row for that author.
    - Every stated cardinal total re-derives from an authoritative list on the page (or from the axes) and is machine-asserted at EVERY site where it is stated, including the one restated in principle-backing.md.
    - The document never asserts a cell is empty in any wording, including set-scoped forms; the cell list presents only cells that ROWS populate, and no unpopulated cell carries a bullet or commentary.
    - Both absence claims -- the contested word in the 1994 book, and the numeral in the other book -- state the scope actually swept and name the unread remainder.
    - The Temporary Test Stub row states its relationship to Test Stub in its defining-property cell, following the convention the other three non-member rows already use.
    - Six row-scoped guards key on a FULL row name, FAIL when their target row is absent, and FAIL on the specific evasion the old file-scoped needle passed -- each proven by a committed fixture pair.
    - Every new or changed guard carries exactly ONE of the two named proof kinds -- RED-at-baseline with a file:line, or invariant GREEN-at-baseline with a pristine/mutation/empty fixture set -- and the RED-BASELINE names which kind applies to which guard.
    - The battery reports an exact emitted-check count that equals a literal PREDICTED in advance from the guard inventory, not a post-hoc measurement, and its label set contains every new label and none of the six retired ones.
    - All three taxonomy copies are byte-identical and no copy carries a skill-relative reference.
  artifacts:
    - .claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs
    - .claude/skills/lz-red-workspace/tools/lib/row-guards.mjs
    - .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs
    - .planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-RED-BASELINE.md
    - plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
  key_links:
    - The roster gate reads the single report() funnel and is the LAST report, snapshotting the count BEFORE its own emission.
    - row-guards.selftest.mjs is chained into the package.json check script -- otherwise its evidence rots exactly as the existing provenance selftest already has.
    - The kinds enumeration resolves every enumerated name against a Gerard Meszaros table row, so deleting a kind row FAILS the gate.
    - The sha256 byte-identity gate is satisfied by editing ONE copy and byte-copying it to the other two, never by editing three files.
    - The widened G17 scope and the narrowed machine-enforced wording are two halves of one fix; shipping either alone leaves the claim false.
---

<objective>
Remediate `test-double-taxonomy.md` (and its two byte-identical copies) plus the checker
`check-red-references.mjs`, after a three-reviewer acceptance gate on merge `1d1474b` returned
DO NOT ACCEPT, DO NOT ACCEPT and TRUSTWORTHY WITH GAPS, and after an oracle consult falsified an
attribution the document repeats at nine sites.

Purpose: the document's map measured sound. What failed was the argumentation around it and the
instrument that passed four blocking defects at 146/146. Fix the instrument first so it can see the
content defects, then fix the content, then prove the whole battery.

Output: three new checker modules, a strengthened checker, the corrected taxonomy in all three
copies, a RED-BASELINE recording per-guard `file:line` failure evidence AND per-guard evasion
evidence, and a SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-CONTEXT.md
@.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-RESEARCH.md
@CLAUDE.md
@AGENTS.md

Read on demand, not up front:
- `.claude/skills/lz-red-workspace/tools/lib/provenance-honesty.mjs` and
  `tools/provenance-honesty.selftest.mjs` -- the pure-row-parser and in-memory-fixture-selftest
  precedent this task copies. Do NOT edit either; the prior round asserted them byte-unchanged.
- `.claude/skills/lz-red-workspace/tools/check-red-references.mjs` (661 lines).
- `plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md` (441 lines).
- `plugins/lz-tdd/skills/lz-red/references/principle-backing.md` (117 lines).
- `.planning/quick/260728-wev-revise-test-double-taxonomy-drop-coined-/260728-wev-RED-BASELINE.md`
  -- the per-guard evidence-table format to copy. Its 132 -> 146 arithmetic is confirmed accurate.
</context>

<hard_rules>
Read these before Task 1. Each one has already been violated once in this document's history.

1. **NEVER read anything under `.oracle/`.** Absolute copyright firewall, no exceptions, not even
   `index.md`. Every book fact this task needs is already settled in CONTEXT.md. If a fact you need
   is not in CONTEXT.md, it is NOT available this round -- record it as UNRESOLVED and move on.
2. **Introduce NO new chapter number.** Use the chapter numbers the document already carries, or a
   descriptive chapter phrase ("the substitute-object catalog chapter", "the value-patterns
   chapter", "the narrative chapter") with no numeral. Inventing a citation is the defect class this
   task exists to remove.
3. **NEVER assert a cell is empty, in any wording, including set-scoped forms.** The previous round
   rewrote the hard rule to legalise its own scoped wording. That is not a fix. The rule must state
   the prohibition and must NOT carry a clause blessing a scoped form.
4. **Edit ONE copy, then byte-copy.** Make every content edit in the `lz-red` copy only, then
   `cp` that file over the `lz-tpp` and `lz-refactor` copies. Never hand-edit three files -- the
   sha256 gate exists because they cannot be allowed to drift, and three hand-edits is how they do.
5. **No copy may carry a skill-relative reference.** The file is byte-identical across three skills,
   so a relative reference resolves differently in each. Naming `lz-red` EXPLICITLY is legal; only
   the relative form is banned.
6. **Guards key on FULL row names.** Never a bare word that could collide with an alias.
7. **The battery is NOT the acceptance gate.** Six green checkers passed the defective version at
   12/12, and a 146/146 battery passed four blocking defects. An independent unprimed review runs
   after you finish and IS the gate. Do not treat a green battery as acceptance, do not soften a
   claim to make a gate pass, and never widen a needle to get to green.
8. **ASCII only** in every file you write -- no em dashes, curly quotes, ellipsis or box-drawing.
   Use `--`, `'`, `...`, `|`. This applies to fixtures and comments too.
9. **Public-repo hygiene: allowlist-inversion only.** No fixture, comment or label may contain a
   forbidden email or domain, not even as a search needle. Writing the needle IS the leak.
10. **You cannot spawn subagents.** Every gate in this plan is deterministic and runnable by you.
    The one oracle consult this round needed has already run; its result is in CONTEXT.md.
</hard_rules>

<proof_kinds>
## Two proof kinds. Every new or changed guard carries exactly ONE.

"Prove every new guard FAILS at baseline" and "prove every strengthened guard catches the evasion the
old guard passed" are in tension for any guard whose subject is ALREADY CORRECT in the live text. A
universal baseline-FAIL demand would order you to delete this round's core deliverable, or to contort
a needle to manufacture a RED -- which hard rule 7 forbids. So the demand is split.

**MEASURED: 14 of the roughly 34 new or changed guards correctly PASS at baseline.** Six of the seven
row-scoped guards do -- the Bernhardt row already names its delivery, the kanban row already names
its essay, and the four row-backed guards find their rows. Six of the nine count guards pass. That
is the instrument being CORRECT, not the instrument being unproven.

| Proof kind | Applies to | Evidence required |
|-----------|------------|-------------------|
| **RED-at-baseline** | a guard whose subject is wrong or missing in the live text | the `file:line` of the offending live text, plus the observed FAIL line from the checker run |
| **Invariant, GREEN-at-baseline** | a guard asserting something already true, whose job is to STOP a regression | the selftest fixture set: pristine PASSES, mutation FAILS, empty text FAILS -- and, for a STRENGTHENED guard, the additional row showing the OLD needle PASSES the same evasion. That fixture set IS the proof; a baseline FAIL is neither available nor required |

Guards expected to be RED-at-baseline: `temporaryTestStubStatesRelationship`, `fiveKindsEnumerated`,
`ambiguitySurveyCount`, **`twelveSources` (evidence: `:65`, the site E9(a) drops -- it is pinned to
the POST value 2 against a live 3, so it FAILS at baseline by design)**, every new `absent` guard,
and every new positive topic.
Guards expected to be invariant GREEN-at-baseline: the other six row-scoped guards and the six count
guards whose derivations already agree at their pinned site count.

Two gates carry neither a fixture set nor a `file:line`, because neither is a pure function: the
WIDENED G17 and the roster gate. Both are invariant GREEN-at-baseline and their evidence is the
recorded MEASUREMENT -- for G17, zero new hits across both sibling trees, re-confirmed over the 4 and
178 files the widening adds; for the roster gate, predicted-equals-measured. No fixture set is
required or possible for either.

A guard with NEITHER proof is unproven and must be fixed. A guard is never deleted or weakened to
manufacture a proof.
</proof_kinds>

<source_coverage_audit>
Every source item is COVERED by a task below. No item is deferred silently.

## GOAL (the task brief)
| Item | Covered by |
|------|------------|
| Fix the instrument first, prove every new/changed guard FAILS at baseline with per-guard `file:line` | Task 1, via `<proof_kinds>` -- SPLIT into two named proof kinds, because 15 of the new guards correctly PASS at baseline and a universal FAIL demand would delete the round's core deliverable |
| For each STRENGTHENED guard, prove it catches the SPECIFIC evasion the old guard passed | Task 1, the invariant proof kind |
| Six guards have the row-scoped defect, not two | Task 1 |
| Deleting prose: check surviving positive topics in BOTH directions | Task 2, extended to THREE directions -- count guards are neither positive topics nor absent guards, so the site-count assertion sat in an unwatched mechanism |
| Re-derive every count from the final list and machine-assert it | Task 1 (mechanism), Task 3 (final re-derivation) |
| Never assert a cell is empty, including set-scoped forms | Task 1 (guard), Task 2 (content) |
| Guards key on FULL row names | Task 1 |
| The battery is not the acceptance gate | Task 3 (`done` explicitly forbids treating green as acceptance) |

## CONTEXT (locked decisions)
| Item | Covered by |
|------|------------|
| 2IG-01 REPAIR Defines-or-uses (naming vs meaning citation) | Task 2, E1 |
| 2IG-02 REPAIR ambiguity finding (survey with re-derived count) | Task 2, E2 |
| 2IG-03 CUT degree-of-freedom proof | Task 2, E3 |
| 2IG-04 CUT deliberate-negative INTENT claim, keep blank-column FACT | Task 2, E4 |
| 2IG-05 Five-kinds attribution correction + enumeration + prose-four caveat + scope hedge | Task 2, E5 / E6 / E7 |
| 2IG-06 Mis-levelling: one cell only, no restructure | Task 2, E8 |
| 2IG-07 Unpopulated cells get no bullet and no commentary | Task 2, E9 |
| 2IG-08 Tier-scoped-out sources removed from the evidence list | Task 2, E10 |
| 2IG-09 Skill-relative content -> name lz-red explicitly | Task 2, E11 |
| 2IG-10 machine-enforced claim gets BOTH fixes | Task 1 (widen G17) + Task 2, E11 (narrow wording) |
| 2IG-11 Gang of Four back matter + skeleton refinements + positive remote-variant finding | Task 2, E12 / E13 |
| 2IG-15 Carry the non-optional kanban qualifier | Task 2, E14 |
| DEFERRED: axis-2 classification of two terms -- no rows added | Task 2 explicitly forbids adding them |
| UNRESOLVED: numeral-absence not certifiable book-wide | Task 2, E7 records the swept scope; Task 3 records it as UNRESOLVED in the SUMMARY |
| Leave `:282-286` alone; it is sound | Task 2 forbids touching it |

## RESEARCH (recommendations)
| Item | Covered by |
|------|------------|
| Reuse the provenance-honesty pure-function + fixture-selftest shape | Task 1 |
| `lib/pipe-table.mjs` parseRows; exactly-one-row-or-FAIL | Task 1 |
| Six row-scoped guards (not two), with decoy counts | Task 1 |
| Count re-derivation via number-WORD comparison, cardinals only, whitespace-flattened | Task 1 |
| Roster integrity: exact emitted-check count, snapshot before reporting | Task 1 |
| Wire the selftests into the `check` script (the existing one is unwired and rotting) | Task 1 |
| Widen G17 to the two sibling reference trees (measured: 0 new hits) | Task 1 |
| Do NOT add a path/env override to the checker | Task 1 forbids it |
| Do not triple new content guards (sha256 propagates them) | Task 1 |
| RESEARCH's discovered inconsistency (Test / own / permanent bullet has 0 rows) | Task 2, E9 -- resolved by composing 2IG-07 with the DEFERRED no-new-rows decision |
| RESEARCH A1/A2/A3 assumptions | A1 resolved (all six), A2 resolved (E9), A3 resolved (widen) |
| Skip the fragile inline-list counts (four notes, claimants, two live conflicts) | Task 1 explicitly excludes them |

No item is MISSING. No item is deferred without a CONTEXT.md record.
</source_coverage_audit>

<tasks>

<task type="auto">
  <name>Task 1: Fix the instrument, prove it FAILS at baseline, and prove each strengthened guard catches its specific evasion</name>

  <files>
.claude/skills/lz-red-workspace/tools/lib/pipe-table.mjs (new)
.claude/skills/lz-red-workspace/tools/lib/row-guards.mjs (new)
.claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs (new)
.claude/skills/lz-red-workspace/tools/check-red-references.mjs
.claude/skills/lz-red-workspace/package.json
.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-RED-BASELINE.md (new)
  </files>

  <action>
NO content file is touched in this task. `plugins/lz-tdd/**` stays byte-unchanged. Confirm that with
`git status` before you commit.

**Step 0 -- record the starting state.** Run the checker and the existing provenance selftest.
Record the emitted-check total (expect 146, 0 FAIL, exit 0) and the provenance selftest exit code.
If the provenance selftest does not already exit 0, STOP and report -- you are about to chain it
into `check` and a pre-existing failure there is a separate finding.

**Step 1 -- `tools/lib/pipe-table.mjs`.** One exported pure function, node builtins only, no deps,
mirroring `lib/provenance-honesty.mjs`:

`parseRows(text, columnCount, headerFirstCell)` returns the data rows of a GitHub pipe table as
arrays of trimmed cells. Skip any line not starting with `|`; split on `|` and drop the two empty
ends; SKIP (never guess at) a row whose cell count does not equal `columnCount`; drop the separator
row (every cell matches three-or-more dashes); drop the header row by comparing its first cell to
`headerFirstCell`, never by position. Measured facts that make this safe TODAY on both target
tables: 36 pipe lines in the taxonomy, every one splitting to exactly 11 parts, 34 data rows, zero
escaped pipes, zero pipes inside code spans, zero empty data cells. Do not write a Markdown parser.

**Step 2 -- `tools/lib/row-guards.mjs`.** Every guard is a pure `(text) => { ok, why }` so it maps
1:1 onto the existing `report(ok, label, detail)` funnel. Two shared internals:

- `oneRow(rows, predicate, description)` -- returns the single matching row or a failure whose `why`
  names how many were found. A lookup that quietly finds ZERO rows and returns "no violations" is
  the wev R1 defect class ("a guard that cannot fail is worse than no guard") reintroduced. Exactly
  one, or FAIL. This is the anti-vacuity spine of the whole module.
- `flat(text)` -- whitespace-normalised whole text (`replace(/\s+/g, " ")`). MANDATORY for every
  multi-word and every count needle: the existing `topics` and `absent` mechanisms match PER LINE,
  so any multi-word needle is defeated by a line wrap at the ~100-column margin. The checker already
  documents being bitten by exactly this (the narrowing comment on the anti-patterns absent guard).

Taxonomy column indices for the 9-column table, keyed by name not by magic number: Author 0,
Term 1, Where 2, StandsFor 3, Lifetime 4, DefiningProperty 5, DefinesOrUses 6, Source 7, Tier 8.
`headerFirstCell` is `Author`. For `principle-backing.md` the table is 3-column with
`headerFirstCell` `Recommendation`; note both of its tables are picked up by one call, which is
wanted. `lib/provenance-honesty.mjs` stays BYTE-UNCHANGED.

**(A) Six ROW-SCOPED guards.** Each replaces a file-scoped needle that names a specific table row in
its own label yet matches anywhere in the file. Ranked by decoy count -- the worst has FIVE decoy
prose lines, so its entire target row can be deleted while the old guard still PASSES. Key each on
the FULL row name.

| New guard | Document | Target row (keyed on the full name) | Asserts | Old needle it replaces | Decoy sites the old needle passed on |
|-----------|----------|-------------------------------------|---------|------------------------|--------------------------------------|
| `bernhardtDeliveryNamed` | taxonomy | Author `Gary Bernhardt`, Term `double versus value` | the Source cell names a specific delivery | `[wev G8]` | 2 Sources bullets |
| `temporaryTestStubStatesRelationship` | taxonomy | Author `Gerard Meszaros`, Term `` `Temporary Test Stub` `` | the DefiningProperty cell states the relationship to Test Stub | net-new | n/a |
| `failureVsErrorRowBacked` | principle-backing | the failure-versus-error boundary row | the row exists and its Source cell names Fowler's Refactoring 2e Ch. 4 | `[j9m] failure-vs-error boundary row` | 5 prose lines |
| `seamRowBacked` | principle-backing | the classify-first / forward lz-tpp handoff row | the row exists AND its tier cell matches the canonical no-oracle tier string; a bare non-empty-tier check cannot fail and is not acceptable | `lz-tpp seam backing row` (NO prefix -- a Phase-18 original) | 6 prose lines |
| `taxonomyRowBacked` | principle-backing | the Test-double taxonomy row | the row exists and its link target is the taxonomy | `[j9m] test-double taxonomy backing row` | 1 prose cross-link |
| `threeLawsRowBacked` | principle-backing | the Three Laws of TDD spine row | the row exists and its Source cell names Robert C. Martin | `Three Laws backing row` (NO prefix -- a Phase-18 original) | 1 section lead-in |
| `kanbanEssayNamedInRow` | principle-backing | the clear-the-compile-error row | its Source cell names the owned ESSAY, never the book | `[wev G16]` | 1 prose bullet |

That is seven guards closing the six measured row-scoped defects plus one net-new row assertion.
REMOVE the six superseded file-scoped `topics` entries from `FILES` when you add their row-scoped
replacements -- do not keep both. Record the removal-and-replacement pairing explicitly in the
RED-BASELINE so a reviewer can see no guard was silently dropped.

**The six retired labels, quoted VERBATIM AS EMITTED.** Emitted labels are COMPOSED as
`<filename>: <label>`, so these are the strings the roster gate's retired-label assertion in (F) must
look for -- not the bare `label` values in `FILES`. Two of them carry NO bracket prefix at all; they
are Phase-18 originals. Do not add a prefix that does not exist, or the assertion is vacuously true:

```
principle-backing.md: Three Laws backing row
principle-backing.md: lz-tpp seam backing row
principle-backing.md: [j9m] test-double taxonomy backing row
principle-backing.md: [j9m] failure-vs-error boundary row
principle-backing.md: [wev G16] kanban-cycle essay named as the owned surface
test-double-taxonomy.md: [wev G8] Bernhardt row names a specific delivery
```

**(B) Count re-derivation guards.** Every stated total in this document is written as a NUMBER WORD.
So: derive N from the authoritative list, then extract every stated cardinal for that noun from the
flattened text and assert BOTH the SITE COUNT and every word against `NUM_WORDS[N]`. Asserting the
site count is load-bearing -- without it, DELETING a stated total passes silently. Do NOT test for
the presence of the expected word; that cannot catch a deletion. Cardinals only in the alternation
(an ordinal produces false sites).

**SITE COUNTS ARE POST-TASK-2 VALUES, NOT TODAY'S.** Task 2's deletions remove sites that these
guards count, so a site count pinned to today's measurement FALSE-FAILS the moment the content lands.
Measured pre/post below. Record the pre/post arithmetic per guard in the RED-BASELINE.

**Each row below is exactly ONE `report` call.** Nine rows, nine reports -- that is what the 172
prediction counts. `fourOwnedSourcesName` scans BOTH files INSIDE a single guard and emits ONE report;
emitting one per file makes the measured total 173 and fails the equality gate for the wrong reason.

| Guard | Derivation | Expected | Sites: PRE -> POST (pin the POST) |
|-------|------------|----------|-----------------------------------|
| `twelveSources` | top-level `- ` bullets between `## Sources` and the next `## ` | 12 | **3 -> 2.** Live sites are `:11`, `:65`, `:159` (RESEARCH's `:67` is STALE -- do not use it). `:65` sits inside the very clause E9(a) drops, so the post value is 2 |
| `eightCellsFromAxes` | count the axis blocks, then count the VALUE BULLETS under each axis block and assert EACH yields exactly 2; the product is 8. Do NOT hardcode the 2 and do NOT write `2 ** 3` -- a hardcoded 8 in a derivation costume is the guard-in-name-only category this round exists to delete. Derive from the AXES, never from the cell bullet list, because Task 2 removes two of those bullets | 8 | 1 -> 1 |
| `threeAxes` | the `Axis one/two/three,` blocks | 3 | **3 -> 3.** Live sites `:17`, `:25`, `:70`. `:70` is inside E9(b)'s reframe, and E9(b) MANDATES that the reframed lead-in keeps the phrase -- it has to say something about crossing the axes regardless. Same line, same edit and therefore the same treatment as `eightCellsFromAxes`. Pin 3 |
| `sixRowsInCell` | rows where (Where, StandsFor, Lifetime) equals (Production, Own implementation, Transitional) | 6 | 2 -> 2 (`:74`, `:271`; neither is inside a deletion) |
| `fourOwnedSourcesName` | DISTINCT Author values of that same row set, AND every one of those 6 tiers begins `Owned` | 4 | taxonomy 1 + principle-backing 1 -- the count is restated in the dependent's taxonomy row, so scan BOTH files |
| `fiveKindsEnumerated` | the enumeration Task 2 adds: list length, AND every enumerated name resolves to a `Gerard Meszaros` table row Term | 5 | **9 -> 8.** E3 deletes the `:56` occurrence outright, so eight survive to be rewritten. Pin 8 |
| `threeFurtherQualifiers` | the numbered items in the closing qualifier block | 3 | 1 -> 1 |
| `ambiguitySurveyCount` | candidate bullets in the section-5 survey (M) and the colliding subset (N) | **DECIDE THE SURVEY SHAPE HERE, IN TASK 1** -- read the live candidate list, settle how many candidates and how many colliding, record that decision in the RED-BASELINE, and pin to it. E2 then AUTHORS TO the recorded shape. A pin deferred to "post-E2" is unsettable, because Task 1 is the only task permitted to write this file | as decided here |
| `noEmptyDataCell` | no data cell in the 9-column table is empty | 0 empty | machine-enforces the doctrine the document already states in prose |

EXCLUDED by RESEARCH measurement, do NOT gate: the four-notes cross-reference clauses, the
claimant inline lists, and the two-live-conflicts bullets. All three are prose punctuation rather
than lists, and a block-scoped or split-on-punctuation derivation is the fragile part. "Roughly
thirty rows" is correctly hedged and is not assertable.

For `fiveKindsEnumerated`: derive the count from the ENUMERATION, never from the rows. The author
carries nine rows in this table; deriving 5 from 9 needs three ad-hoc prose sniffs on a
defining-property cell, which is exactly the wording-coupling that forced two guards to be REMOVED
last round. The enumeration is the single source of truth for the number; the rows are what each
enumerated name must RESOLVE to.

**(C) New `absent` guards on the taxonomy entry.** Add one guard per banned wording. Derive each
needle by reading the current text at the line given, then choose the SHORTEST distinguishing
fragment that (i) matches the live wording and (ii) cannot match the legitimate replacement Task 2
will write. Every one of these is RED-at-baseline -- if one already passes, its needle is wrong.

**The `absent` mechanism matches PER LINE, so every needle here must be single-line-matchable at the
CURRENT wrap.** Step 2 mandates `flat()` for multi-word needles precisely because of this, but
`absent` guards run through the existing per-line loop and cannot use it. The hazard is live and
already bit this checker once: one kanban-essay occurrence wraps across `principle-backing.md:108-109`,
and the anti-patterns absent guard carries a narrowing comment recording the same bite. Verify each
needle against the wrapped text, not against the sentence you have in your head.

| Banned wording | Currently at | Why |
|----------------|--------------|-----|
| the set-scoped emptiness form | `:64` (the legalising clause of the hard rule) and `:81` (the bullet using it) | requirement: never assert a cell is empty in ANY wording, including set-scoped |
| the DELIBERATE-NEGATIVE intent inference | `:134-135` | 2IG-04: the FACT stays, the intent inference goes |
| the every-available-name universal quantifier | `:276` | 2IG-02: falsified by a non-colliding candidate |
| the possessive five-kinds attribution | 8 lines, 9 occurrences (`:56`, `:105`, `:125`, `:134`, `:216` x2, `:327`, `:387`, `:396`) | 2IG-05: attributing to a named author a framing he does not state. Needle must catch the POSSESSIVE form and must NOT catch the figure-attributed replacement, which keeps the numeral |
| the two-appendix count | `:341` | 2IG-11: the book has three, and naming the glossary separately implies it is not one |
| the only-occurrence skeleton phrasing | `:341-342` | 2IG-11: it is the only SENSE; there are three occurrences |

Note `[wev G7]` (`/\bnever asserts\b/i`) must KEEP passing: the rewritten hard rule still states the
prohibition, it just loses the legalising clause. Verify that at the end of Task 2.

**(D) New positive `topics` on the taxonomy entry**, one per content addition Task 2 makes, so each
is RED until authored: the figure attribution, the prose-states-four caveat, the enumeration, the
naming-versus-meaning citation split, the Temporary Test Stub relationship, the swept-scope hedge on
the contested-word absence, the swept-scope hedge on the numeral absence, the positive
remote-variant finding, the do-nothing-hook near-miss redirection, and the non-optional kanban
qualifier. Prefix every new label `[2ig] `.

**(E) Widen G17 (`bareQualifierTargets`).** Add `lz-tpp/references` and `lz-refactor/references` to
the walk. MEASURED: zero new hits in both trees, and the taxonomy-basename exclusion already covers
the copies. This is one half of 2IG-10; Task 2 writes the other half.

**(F) Roster integrity.** Add an `emitted` counter inside `report`, and as the very LAST report, whose
label MUST be exactly this string:

```
[2ig] roster integrity: exact emitted-check count
```

Task 2's and Task 3's verify blocks both grep for `^  \[PASS\] \[2ig\] roster integrity` -- two leading
spaces, matching the funnel's own output format. Name it differently and BOTH later gates false-FAIL
while the executor hunts a phantom content defect.

`EXPECTED_CHECKS` is a hand-maintained literal compared against the count snapshotted BEFORE the
roster report itself, so the literal equals the guard total and the arithmetic stays legible.
Deriving it from `FILES` is WRONG on purpose -- summing topics plus flags lowers BOTH sides when a
guard is deleted, making the gate unable to fail. Comment that, and comment what the gate does NOT
catch: a guard WEAKENED IN PLACE leaves the count unchanged, so only the selftest evasion proofs
cover that. Nobody may mistake the roster gate for sufficient.

What it does catch that nothing else does: a deleted `topics` or `absent` entry; `topics: []` (the
loop emits nothing) and `absent: []` (truthy, so the presence check does not save you); and an
`existsSync`-gated post-loop block that silently emits nothing -- the D-05 honesty gate and the
SEAM-02 block both do that today if their file vanishes, a fully silent vacuous pass.

**PREDICT the literal, then require equality. Do NOT set it from a post-hoc measurement.** A literal
copied from whatever you happened to build is definitionally consistent with whatever you happened to
build, and a tolerance band absorbs a short swap silently -- six removals against five additions
lands inside any band. The prediction from the inventory above:

`146 (baseline) - 6 (superseded topics) + 7 (row-scoped) + 9 (count) + 6 (absent) + 10 (topics) = 172`

Set `EXPECTED_CHECKS = 172`. If the measured pre-roster count is not exactly 172, you dropped or
duplicated a guard -- find which before changing the literal, and if the inventory itself was wrong,
record the corrected arithmetic in the RED-BASELINE with the reason. Note the row-scoped swap is
NOT net-zero: 7 in for 6 out is **+1**; only the 6-for-6 sub-swap cancels.

Content edits in Tasks 2 and 3 change PASS/FAIL, never the emitted count, so the literal is set
ONCE, here.

**Two additions that close what a bare count cannot see.** A count is blind to a short swap that
happens to balance, so add both:
- The selftest asserts `row-guards.mjs` exports EXACTLY the seven named row-scoped guards plus the
  nine count guards -- by name, comparing the export key set, so a renamed or missing guard fails.
- The roster gate additionally asserts the emitted LABEL SET contains every new `[2ig] ` label and
  contains NONE of the six retired labels, using the six COMPOSED strings quoted verbatim in (A).
  Collect labels in `report` alongside the counter.

Do NOT add a path or env override to the checker so it can be pointed at a copy. That creates a
bypass on a gate whose value rests on its paths being fixed, and the pure functions make it
unnecessary.

**Step 3 -- `tools/row-guards.selftest.mjs`.** In-memory fixtures ONLY; never reads a shipped file.
Copy the `check()` helper and the contract comment style from `provenance-honesty.selftest.mjs`.
For EVERY row-scoped guard, FOUR assertions:

1. PRISTINE fixture -> PASS.
2. EVASION fixture -> FAIL. The evasion strips the subject from the ROW while leaving a decoy
   elsewhere in the fixture (a Sources bullet, a prose line) that satisfies the OLD needle.
3. The OLD needle, asserted to PASS on the same EVASION fixture. This row IS the "catches an
   evasion the old guard passed" evidence, committed rather than thrown away in a scratchpad.
4. Empty text (row absent entirely) -> FAIL. The anti-vacuity control. Required for every guard.

The two evasions named in the brief are MANDATORY and must appear as fixtures verbatim in intent:
(a) strip the delivery name from the Bernhardt ROW while a Sources bullet still names a delivery;
(b) revert the kanban row's Source cell to the chapter that the same file says CONTRADICTS the
criterion, while the prose bullet still names the essay. The other five row-scoped guards get the
same four-assertion treatment with their own decoys.

Also selftest every count guard: a fixture where the list has N items and the stated word says N
(PASS), one where the stated word disagrees (FAIL), and one where the stated total has been DELETED
so the site count drops (FAIL -- this is the assertion most likely to be missing).

Fixtures carry no forbidden email or domain, not even as a needle.

**Step 4 -- wire it in.** `package.json`:
`"check": "node tools/provenance-honesty.selftest.mjs && node tools/row-guards.selftest.mjs && node tools/check-red-references.mjs"`.
The existing provenance selftest is run by NO script today, so its evidence is already rotting;
chaining both is the fix.

**Step 5 -- author `260729-2ig-RED-BASELINE.md`.** Copy the prior round's evidence-table format.
It MUST contain:

- The starting measurement (emitted total, failures, exit code) and the PREDICTED EXPECTED_CHECKS
  arithmetic alongside the measured count, with the two shown to be equal.
- A row PER new or changed guard with a **PROOF KIND** column (see `<proof_kinds>`), carrying the
  evidence that kind requires:
  - RED-at-baseline -> the `file:line` of the live text that makes it FAIL, plus the observed FAIL
    line from the checker output.
  - Invariant GREEN-at-baseline -> the selftest assertion names for pristine-PASS, mutation-FAIL and
    empty-FAIL, plus (for a strengthened guard) the old-needle regression row.
  Every new or changed guard carries exactly one of the two, with its evidence. A guard carrying
  NEITHER is unproven and must be fixed -- never deleted or weakened to manufacture a proof.
- A row PER strengthened guard recording the EVASION proof: the evasion described, the new guard's
  verdict, the old needle's verdict on the same fixture, and the selftest assertion name.
- The six removal-and-replacement pairings, and the `+1` net emitted delta they produce.
- The pre/post SITE-COUNT arithmetic for every count guard, so a post-Task-2 false FAIL is impossible
  to blame on the instrument.
- The final checker run: exit 1 by design, with the FAIL count and the confirmation that every
  pre-existing PASS still PASSES (no false FAIL introduced by the instrument alone).

**Step 6 -- hygiene on the workspace files, because the repo's hygiene checker does not reach them.**
`check-hygiene.mjs` scans the shippable public surface only (the three plugin skill trees plus the
root manifests). It does NOT scan `.claude/skills/lz-red-workspace/tools/**` -- yet those files are
tracked and public, so hard rules 8 and 9 bind there with no automated backstop. Task 1's verify
therefore carries its own two probes over the changed workspace files: an allowlist-inversion
assertion (enumerate every email-shaped token, subtract the approved public contact, assert the
remainder EMPTY -- never encode a forbidden value as a needle) and a non-ASCII byte probe. Fixtures
and comments are the easy place to forget both.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/provenance-honesty.selftest.mjs && node .claude/skills/lz-red-workspace/tools/row-guards.selftest.mjs && ! node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '^SUMMARY: RED-REFS RED' && test -z "$(git status --porcelain plugins/lz-tdd)" && test -z "$(rg -o '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' .claude/skills/lz-red-workspace/tools/*.mjs .claude/skills/lz-red-workspace/tools/lib/*.mjs | rg -v 'larsbrinknielsen@gmail\.com')" && ! rg -q '[^\x00-\x7F]' .claude/skills/lz-red-workspace/tools/*.mjs .claude/skills/lz-red-workspace/tools/lib/*.mjs && echo TASK1_OK</automated>
  </verify>

  <done>
`TASK1_OK` printed, meaning every leg of the chain asserted: both selftests exit 0, the checker exits
NON-zero AND prints its designed RED summary line (the negation alone is satisfied by a CRASH, and a
new module makes a top-level throw more likely, so the summary line is what proves the RED is the
designed one), `plugins/lz-tdd` is byte-unchanged,
no email-shaped token other than the approved public contact appears in the workspace tool files, and
those files are ASCII-only.

Beyond the automated chain: every new or changed guard carries exactly one of the two proof kinds
with its evidence recorded in the RED-BASELINE, and each of the seven row-scoped guards has all four
selftest assertions including the old-needle regression row and the empty-text anti-vacuity control.
The two brief-mandated evasions are committed as fixtures. The measured pre-roster emitted count
equals the PREDICTED `EXPECTED_CHECKS = 172`; a mismatch was investigated, not absorbed. The selftest
asserts the exported guard-name set, and the roster gate asserts the emitted label set includes every
new label and none of the six retired ones. `provenance-honesty.mjs` and its selftest are
byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the taxonomy content in the lz-red copy, then byte-copy to the other two</name>

  <files>
plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md
plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md
  </files>

  <action>
Edit the `lz-red` copy ONLY. When every edit below is in, `cp` it over the other two copies and
confirm the sha256 gate. Line numbers are against the pre-edit file and shift as you go -- re-locate
by content, not by number.

**E1 (2IG-01) -- repair the Defines-or-uses rule** at `:195-197`. Split the two citation kinds
explicitly: for what an author CALLS the artifact -- a NAMING citation -- a `Uses` row suffices; for
what the term MEANS -- a MEANING citation -- a `Defines` row is required. Do NOT weaken the meaning
requirement; it is the discipline that would have caught the earlier fabricated attribution. This
split is load-bearing for section 5, whose "four owned sources NAME this artifact" claim rests
entirely on `Uses` rows (Beck uses-consistently, both Metz talk rows, the 99 Bottles empty class,
Kerievsky uses-in-passing). The repair licenses that claim instead of leaving it in tension with the
column's own rule.

**E2 (2IG-02) -- repair the ambiguity finding** at `:275-305`. Weaken the quantifier from universal
to a survey with a re-derived count: N of M candidates collide. ADD every non-colliding candidate to
the survey, including the one that falsifies the universal form -- scoping it OUT would be special
pleading against the very counterexample that forced the repair. Each candidate keeps its stated
reason. State the count as number words and make it agree with the list; Task 1's
`ambiguitySurveyCount` gate asserts both the derivation and the sites.

**E3 (2IG-03) -- CUT the degree-of-freedom proof.** Pure deletion, no carry-forward rider.
Delete: the every-combination-is-occupied test and its four-combination roll-call (`:52-57`), and
the paragraph at `:59-61`. That second paragraph is a defect in its own right independent of the
cut: it claims the proof retired an EXCEPTION section 6 "used to carry for want of anywhere to put
it", leaving section 6 "only the narrow citable rule" -- but section 6 at `:316-318` still carries
the WHOLE caveat, with chapter citations. The retirement never happened.
The KEEP/DELETE boundary falls MID-LINE at `:52` -- the mis-placement sentence ends and the
degree-of-freedom test begins on the same physical line. Cut by content, not by line number.
KEEP `:49-52`: collapsing the two axes into one column mis-places real artifacts, warranted by a
real row (an IO substitute living inside a production class while standing in for a collaborator).
That is the operational reason the axes are separate; the deleted part was the proof that the design
is sound, which belongs in CONTEXT.md, not in a shipped reference.

**E4 (2IG-04) -- CUT the deliberate-negative INTENT claim, KEEP the blank-column FACT.** Surgical.
Delete only the inference at `:134-135` that the blank is deliberate rather than an oversight.
KEEP: the blank column in every row, the source listing, the owner-verification against the print
book, the independent-vocabularies framing, and the conclusion that neither author is citing or
deviating from the other. The anti-refabrication job is ALREADY discharged as a prohibition in
section 6 at `:325-333`, with both markup traps documented, so nothing needs relocating.
The pattern that produced this defect, and must not be reintroduced anywhere: both cut passages
RESTATE, with weaker warrant, a claim section 6 already carries WITH citations -- and the uncited
copy is the one that drifted. When one claim lives in both registers, the uncited copy drifts.

**E5 (2IG-05) -- correct the five-kinds ATTRIBUTION: EIGHT rewrites plus one deletion, not nine
rewrites.** MEASURED: nine occurrences across eight lines (`:56`, `:105`, `:125`, `:134`, `:216`
carries TWO, `:327`, `:387`, `:396`), every one attributing the count to the author as his own
framing. **E3 DELETES the `:56` occurrence outright**, so the arithmetic is 9 = 8 rewritten + 1
deleted. Say it that way in the SUMMARY too. Getting this number wrong is not a rounding matter --
this document's history is precisely one of miscounted claims surviving review. The consult settled that
the book NEVER states five; the one number it states is FOUR, because its narrative rundown FOLDS
two members into a single bullet. Five is a reading of the HIERARCHY FIGURE only -- that figure shows
exactly five direct subtypes under the root, so the numeral is defensible, but "his five kinds"
attributes to him a framing he does not state and sits against an explicit four in his own prose.
Counting from the catalog's Variations run instead yields six or nine, so the figure is the only
coherent basis.

This is the same defect class as the fabricated mapping this whole remediation exists to remove --
attributing to a named author something he did not say -- repeated nine times.

At every occurrence: KEEP the numeral, attribute it to the hierarchy figure rather than to him as a
framing. Cite the SET to the catalog chapter where the figure appears, using the chapter reference
the document already carries for that chapter or a descriptive phrase; introduce NO new number.

**E6 (2IG-05) -- ADD the prose-states-four caveat and the ENUMERATION.**
The caveat is exactly the "what you may not cite" content this document should carry: a coach citing
"his five kinds" to him is currently wrong, because his prose states four by folding two members
into one bullet. State that plainly.
The enumeration: Test Stub, Test Spy, Mock Object, Fake Object, Dummy Object. Write it as a
machine-parseable list -- one item per line, each leading with the backticked term exactly as that
term appears in the table's Term cell -- immediately under the sentence that states the number, so
Task 1's `fiveKindsEnumerated` gate can derive the count from the list and resolve every name to a
row. Per-member definition chapters are already cited correctly by the existing rows: four are
defined in the substitute-object catalog chapter and Dummy Object in the value-patterns chapter, and
the Dummy row already says it is filed under value patterns. Carry the nuance that Dummy Object is
drawn as a subtype yet demoted in prose in both chapters as not really one of these and in a
different league -- the existing cell wording already captures that; do not restate it twice.
The enumeration adds NO new claim: the document already names every one of these terms as an
individual row. It makes an implicit claim explicit, and it fixes a real reader problem -- the
number appears nine times and a reader cannot currently tell WHICH five.

**E7 (2IG-05, UNRESOLVED item) -- hedge the numeral-absence claim to the scope actually swept.**
The consult's "never states five" is a correctly-scoped negative over the parts it read end to end:
the opening overview, the dependency-isolation narrative chapter, the substitute-object catalog
chapter, the value-patterns catalog chapter, and the terminology appendix. It did NOT sweep the
remaining chapters and appendixes for the numeral and rightly declined to -- a whole-book token
sweep is a deterministic check belonging to a harness. State the claim at that scope and name the
remainder as unswept. This does NOT weaken the fix: the figure is the basis for five regardless of
what an unread chapter says, and the prose demonstrably states four.
Also: two conversion defects in the store touch this question -- the appendix's role-summary table
came through garbled at character level, and one bullet of the narrative rundown is an
un-transcribed scanned image. Any claim resting on that appendix table must carry the degraded-scan
treatment the document already has a mechanism for at `:338-339`.

**E8 (2IG-06) -- fix ONE cell.** MEASURED: the document's convention is to state the relationship in
the defining-property cell, and it already does for three of the four non-member rows -- the
umbrella row says it is the umbrella, both Variation rows say so, and the Dummy row says it is filed
under value patterns. Only the `Temporary Test Stub` row states NO relationship: its cell describes
the artifact and its near-miss status but never says it is a Variation of Test Stub discriminated on
a LIFECYCLE axis rather than an input-kind axis. Add that, following the convention already in the
file, and keep the rest of the cell.
Do NOT add a level column, do NOT split the table, do NOT restructure anything. A restructure would
ripple through every cell bullet, every derived count and all of Task 1's guard work.
Also measured and NOT present, so there is nothing to fix: the other families the consult warned
might be flattened have ZERO mentions and ZERO rows here. And the naming collision it flagged is not
live -- the Fake Object row carries no competing alias and no guard keys on a bare word.

**E9 (2IG-07) -- the cell list becomes a census of what ROWS populate.** Two changes:
(a) Rewrite the hard rule at `:63-68` so it states the prohibition and DROPS the clause that
legalises a set-scoped wording. The previous round rewrote this rule to bless its own scoped
phrasing; that is not a fix. Keep the sentence stating that the doctrine is stated rather than
merely obeyed, and keep the wording `never asserts` so the existing `[wev G7]` gate still binds.
(b) Reframe the list lead-in at `:70-71` as the cells that rows POPULATE, then DELETE the two
bullets no row populates -- the one that currently asserts nothing populates it (`:81-82`), and the
one populated only by two artifacts that carry no row (`:83`). No bullet, no commentary, no
explanation for either. Absence of a bullet then carries no claim, which is the whole point.
**The reframed lead-in MUST keep the phrase `three axes` and MUST keep the eight-cell arithmetic.**
That line hosts one site of each, and both are pinned in Task 1 -- the reframe changes what the LIST
is a census of, not the sentence about crossing the axes. Say something about crossing the three axes
regardless of how the list is framed.
The second deletion follows by composition, not by planner discretion: the DEFERRED decision
forbids adding rows for those two artifacts this round, and a census of what rows populate cannot
list a cell that no row populates. RESEARCH independently measured that same bullet as the ONE cell
whose claim mismatches the table.
KEEP the eight-cell arithmetic as a plain fact about the axes -- Task 1 derives it as the PRODUCT of
the per-axis value counts (2 x 2 x 2), never from the bullet count, precisely so this deletion cannot
break it.
The two artifacts remain section-6 caveats where they are already cited. Do NOT add rows for them.

**E10 (2IG-08) -- remove the tier-scoped-out sources from the side-assignment evidence list** at
`:167-168`. Two of the names in that list are scoped out by their own tiers: one row reads NOT
citable as a taxonomy, and the other is verified for one specific point ONLY. A list using them as
evidence for WHICH SIDE a term names is a taxonomy claim. Remove them from that list -- do not
annotate them in place -- and restate any count of the list after removal. Both sources KEEP their
table rows, their Sources entries and their own caveats; only their use as side-assignment evidence
goes.

**E11 (2IG-09 + 2IG-10) -- narrow the machine-enforced claim and name lz-red explicitly.** At
`:187-189`. The gate lives in the lz-red development workspace, OUTSIDE the plugin, and is not
shipped -- so no installed copy carries it regardless of how wide its scope is. Narrow the wording
to say that, and name `lz-red` explicitly rather than using any skill-relative form. Explicit
naming is the legal form; only the relative form is banned, because this file is byte-identical
across three skills and a relative reference resolves differently in each. Task 1 supplied the other
half of this fix by widening the gate's scope to the two sibling reference trees.

**E12 (2IG-11) -- fix the Gang of Four back-matter claim** at `:340-344`. Four defects in two
sentences:
(a) The book has THREE appendices, not two -- the glossary is one of them, carrying a letter
designation, as do the notation guide and the foundation-classes section. So the count is wrong AND
naming the glossary separately in the same breath implies it is not an appendix, which is also
wrong.
(b) The enumeration is not exhaustive. It misses the entire front matter -- including a front-matter
quick reference giving a one-line intent for each pattern, which is a real hit site (the consult
found an occurrence of the near-miss word there, a third site the document does not know about). It
also misses an unnumbered interstitial section introducing the catalog between two chapters, which
"all chapters" plus an appendix count does not reach, and the inside-back-cover notation summary.
(c) The absence claim is UNVERIFIED as written: it asserts the contested word is absent from the
book entirely, every sense. State the scope ACTUALLY swept and name the unread remainder.
CERTIFIED COMPLETE: the Proxy pattern in full including its remote-variant discussion, the Template
Method pattern in full, all three appendices, the bibliography, the collected notes, the index, the
front-matter pattern summaries, the catalog interstitial, and the inside-back-cover summary. NOT
SWEPT: the introduction, case-study and conclusion chapters; the creational chapter; the structural
chapter apart from Proxy; and the behavioral chapter apart from Template Method.
(d) The near-miss phrasing is loose: it is the only SENSE, not the only occurrence -- there are
three occurrences of one sense.

**E13 (2IG-11) -- ADD the POSITIVE finding, which is stronger than the absence.** The remote-variant
material describes the stand-in-across-an-address-space role three separate times -- a local
representative for an object elsewhere, the marshaling responsibility, and the encode-and-forward
round trip -- in functional vocabulary, and never reaches for the contested word. A source that
articulates the CONCEPT and still does not use the WORD is better evidence than a bare absence, and
it positively covers the one site a reader would challenge. Put that in the document.
Also redirect the near-miss warning: the real trap is the do-nothing HOOK, not the word. A hook
whose default body is empty and which exists to be overridden looks placeholder-ish, and that is
where a reader will actually go wrong. Note the book's own distinction between a hook a subclass MAY
override and an operation it MUST. This is consistent with the existing Template Method hook row,
which already types that artifact permanent on the minimise-what-must-be-overridden goal -- the
consult reached the same reading unprompted, so that row is sound and needs no change.
**TRIPWIRE: say HOOK, never METHOD.** `[wev G11]` is a live absent guard on the do-nothing-METHOD
phrasing -- a term measured to occur nowhere in the corpus. Writing this new passage with the word
method instead of hook trips that guard and reintroduces the exact non-occurring phrasing it exists
to keep out.

**DO NOT TOUCH `:282-286`.** It was suspected of self-contradicting the whole-book absence claim and
it does NOT: that entry is the candidate entry for a DIFFERENT word, which the Proxy intent sentence
does carry, while the contested word is a separate entry. The two claims are fully compatible, and
the entry is additionally precise in stating that only one term is formally registered as the
also-known-as so the other is contested usage rather than a second registration -- which is what the
consult found independently. It is sound. Leave it alone.

**E14 (2IG-15) -- carry the non-optional qualifier the taxonomy currently drops.** The dependent
`principle-backing.md:67` states that TWO qualifiers travel with the kanban-cycle claim and are NOT
optional: it is gated on a red test already existing, and it is ONE POSITION AMONG SEVERAL that the
author himself calls contradictory. The taxonomy's live-conflicts passage at `:251-254` cites that
same author's essays as one side of the conflict without carrying the second qualifier. Add it in
one sentence, pointing at the dependent rather than restating its detail. Never present the kanban
cycle as his settled doctrine.

**THE THREE-DIRECTIONS AUDIT -- do this BEFORE you delete, and record it.** Every deletion above
(E3, E4, E9, E10) must be checked in THREE directions against every gate in the battery:
- Direction 1, FALSE PASS: is a surviving positive topic's SOLE satisfying occurrence inside the text
  you are deleting? If yes, the gate keeps passing on prose that no longer exists.
- Direction 2, FALSE FAIL: is a positive topic's only SUBJECT being removed? If yes the gate flips
  PASS to FAIL and blocks the task. This is what deadlocked the previous round and forced a third
  removal. The precedent for handling it is on the record: a subject deleted from under a needle
  means the guard is REMOVED, not narrowed -- but only after you have confirmed the subject really
  is gone rather than moved.
- Direction 3, COUNT-GUARD SITE LOSS: **count guards are neither positive topics nor absent guards,
  so directions 1 and 2 do not inspect them** -- and the SITE-COUNT assertion is the load-bearing half
  of the whole count mechanism, so a deletion that removes a site it counts is exactly the false FAIL
  that direction 2 was written to prevent, hiding in an unwatched mechanism. Produce a row for every
  (count guard x deletion) pair: does E3, E4, E9 or E10 remove a site this guard counts? Three are
  already measured -- `fiveKindsEnumerated` loses `:56` to E3, `twelveSources` loses `:65` to E9(a),
  and `threeAxes` is at risk at `:70` via E9(b). Confirm those three and check the rest.
Two known live positive-topic cases to check first: the self-shunt topic (E9 removes its section-1
mentions; verify its section-6 occurrences survive) and the independent-vocabularies topic (E4 keeps
its paragraph, so verify the two-word phrase survives the surgical cut). Enumerate every checked
topic and every count-guard pair in the SUMMARY with its surviving `file:line`, not just these.

**Propagate.** `cp` the edited lz-red copy over the lz-tpp and lz-refactor copies. Then run the
checker and confirm the byte-identity gate reports one sha256 in all three. If it reports a
divergence you hand-edited a copy -- redo the copy, do not patch the difference.

ASCII only. No new chapter number. No `.oracle/` read. No new rows in the table.
  </action>

  <verify>
    <automated>node .claude/skills/lz-red-workspace/tools/check-red-references.mjs && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '^  \[PASS\] \[2ig\] roster integrity' && test "$(sha256sum plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md plugins/lz-tdd/skills/lz-tpp/references/test-double-taxonomy.md plugins/lz-tdd/skills/lz-refactor/references/test-double-taxonomy.md | awk '{print $1}' | sort -u | wc -l)" = "1" && ! rg -q '[^\x00-\x7F]' plugins/lz-tdd/skills/lz-red/references/test-double-taxonomy.md && echo TASK2_OK</automated>
  </verify>

  <done>
`TASK2_OK` printed, meaning every leg asserted: the checker exits 0 (its exit status is asserted
directly, never piped away -- this is the task that turns it green), the three copies reduce to
exactly ONE unique sha256 (an equality assertion, not three printed digests -- this is the only place
the byte-copy is checked, so a skipped `cp` must fail here), and the taxonomy is ASCII-only.

The roster-gate leg is what stops this gate passing VACUOUSLY: on an untouched tree the checker is
already green and the three copies are already identical, so without an assertion that the instrument
is present and ARMED, a green checker proves nothing about the content. Requiring the roster gate's
own PASS line means a green checker that emits the old 146 checks -- i.e. Task 1 never landed -- fails
here.

Beyond the automated chain: every `[2ig] ` positive topic and absent guard PASSES, every count guard
PASSES with its derivation agreeing at its PINNED POST-TASK-2 site count, and `[wev G7]` still PASSES.
No pre-existing PASS flipped to FAIL without a recorded three-directions justification. The taxonomy
carries no skill-relative reference, introduces no new chapter number, and adds no table row. The
three-directions audit is recorded per positive topic and per (count guard x deletion) pair with
surviving `file:line`. For the record, informationally and not as a gate: the five-kinds occurrence
count should now read 8 via `rg -o "five kinds" <file> | wc -l` -- an occurrence count, not the line
count `rg -c` returns.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dependents, final count re-derivation, full battery, and SUMMARY</name>

  <files>
plugins/lz-tdd/skills/lz-red/references/principle-backing.md (verify; edit only if a claim is now unsupported)
plugins/lz-tdd/skills/lz-red/SKILL.md (verify only)
plugins/lz-tdd/skills/lz-refactor/SKILL.md (verify only)
plugins/lz-tdd/skills/lz-tpp/SKILL.md (verify only)
plugins/lz-tdd/skills/lz-red/references/anti-patterns.md (verify only)
plugins/lz-tdd/skills/lz-red/references/three-laws-and-test-selection.md (verify only)
plugins/lz-tdd/skills/lz-red/references/vitest-typescript-mechanics.md (verify only)
plugins/lz-tdd/skills/lz-refactor/references/beck-tdd-by-example.md (verify only)
.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-SUMMARY.md (new)
  </files>

  <action>
**Step 1 -- dependent sweep, measured not assumed.** Baseline measurement taken at plan time: NO
dependent restates the five-kinds attribution anywhere in `plugins/lz-tdd`, and the four files that
link to the taxonomy carry only thin pointer sentences about the three axes. Re-measure after
Task 2 and record the result. Specifically check each dependent for:
- any restatement of the five-kinds count or its attribution;
- any claim that points at a passage E3, E4, E9 or E10 deleted;
- any restatement of a count the taxonomy states (the four-owned-sources count IS restated in
  `principle-backing.md`'s taxonomy row -- Task 1's gate already scans both files, confirm it binds);
- any bare unqualified use of the contested word introduced by the now-WIDENED G17 scope.
Edit a dependent ONLY where it now asserts something the taxonomy no longer supports. Editing a
dependent that is already correct is churn; record "no change needed" with the measurement instead.

**Step 2 -- re-derive every count from the FINAL text, by hand, and reconcile with the machine.**
For each gated total: state the derivation, the derived number, the number word(s) found, and the
site count, all read off the final file. Then confirm the checker agrees. A coincidentally unchanged
total looks untouched, so a count that did not need to change still gets its derivation recorded.
If a hand derivation and the machine disagree, the machine is not automatically right -- find out
which is wrong before touching either.

**Step 3 -- take the full battery green.** Run each and record the exit code:
- `npm --prefix .claude/skills/lz-red-workspace run check` (the chained selftests plus the checker)
- `npm --prefix .claude/skills/lz-red-workspace run typecheck`
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` -- the repo hygiene checker,
  named by path because it lives in the SIBLING workspace and is easy to miss. Note its scope: the
  three plugin skill trees plus the root manifests. It does NOT reach
  `.claude/skills/lz-red-workspace/tools/**`, which is why Task 1 carries its own hygiene probes.
- `claude plugin validate .`
Every one must exit 0. If a gate is red, fix the CONTENT or the fixture -- never widen a needle,
never soften a claim, and never delete a guard to get to green. If a guard is genuinely wrong,
record why in the SUMMARY as a finding rather than quietly removing it.

**Step 4 -- author the SUMMARY.** It must carry:
- what changed, per E-item, with the `file:line` of the result -- and for E5 the arithmetic stated as
  EIGHT occurrences rewritten plus ONE deleted by E3, never as "nine corrected";
- the three-directions audit table from Task 2: one row per checked positive topic, plus one row per
  (count guard x deletion) pair;
- the emitted-check arithmetic showing the PREDICTED 172 and the measured count as equal;
- the seven row-scoped guards with their proof kind and their evasion-proof assertion names;
- the count re-derivation table from Step 2, with pre/post site counts;
- the dependent sweep result;
- UNRESOLVED, carried forward verbatim from CONTEXT.md and NOT closed: the numeral-absence claim is
  not certifiable book-wide this round (needs a harness sweep no agent in this loop can run), and the
  axis-2 classification of the two deliberately-unplaced terms stays undecided with the conservative
  default taken (no rows added);
- an explicit statement that the green battery is NOT acceptance, and that an independent unprimed
  review is the gate and has not yet run.

Do NOT declare the task accepted. Do NOT self-certify the review gate. A green battery here means
the deterministic instruments agree with the content -- nothing more. Six green checkers passed the
defective version at 12/12 and a 146/146 battery passed four blocking defects.
  </action>

  <verify>
    <automated>npm --prefix .claude/skills/lz-red-workspace run check && node .claude/skills/lz-red-workspace/tools/check-red-references.mjs | rg -q '^  \[PASS\] \[2ig\] roster integrity' && npm --prefix .claude/skills/lz-red-workspace run typecheck && node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs && claude plugin validate . && echo BATTERY_GREEN</automated>
  </verify>

  <done>
The whole battery exits 0. Every count is re-derived by hand from the final text, reconciled with
the machine assertion, and tabulated in the SUMMARY. The dependent sweep is recorded with its
measurement, and any dependent left unedited is recorded as measured-correct rather than assumed.
The SUMMARY carries both UNRESOLVED items unclosed and states explicitly that the independent
unprimed review is the acceptance gate and has not run. No guard was widened, softened or deleted to
reach green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files -> dev-only Node checker | The checker reads tracked repo files. No network, no untrusted input, no credentials. |
| authored content -> public repo | Every file written here is published. Maintainer identity and PII rules bind. |
| `.oracle/` -> everything else | Absolute copyright firewall. Neither the executor nor the orchestrator may read it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2ig-01 | Information disclosure | new fixtures, comments and labels in `row-guards.selftest.mjs`, `row-guards.mjs` and `pipe-table.mjs` | high | mitigate | Allowlist-inversion only. No forbidden email or domain in any fixture, comment or label -- writing the needle IS the leak. **The Task 3 hygiene checker does NOT reach these files**: `check-hygiene.mjs` scans the three plugin skill trees plus the root manifests, not `.claude/skills/lz-red-workspace/tools/**`, yet those files are tracked and public. Mitigated instead by the two probes wired into Task 1's own verify chain -- an allowlist-inversion assertion (enumerate email-shaped tokens, subtract the approved public contact, assert the remainder empty) and a non-ASCII byte probe over the same file set. Fixtures are the easy place to forget both. |
| T-2ig-02 | Information disclosure | copyrighted source text reaching a public file | high | mitigate | Hard rule 1: never read `.oracle/`. Every book fact comes from CONTEXT.md, already in own words. No verbatim source prose or code. |
| T-2ig-03 | Tampering | a guard weakened in place to reach green | high | mitigate | The roster gate cannot see a broadened needle, and the plan says so in the gate's own comment. The selftest evasion proofs are the only cover, and Task 3's `done` forbids widening a needle. |
| T-2ig-04 | Tampering | silent divergence between the three taxonomy copies | medium | mitigate | sha256 byte-identity gate plus the edit-one-then-copy rule. A reported divergence means redo the copy, never patch the difference. |
| T-2ig-05 | Denial of service | `parseRows` on a malformed row | low | accept | A width mismatch SKIPS the row, and the exactly-one-row rule turns that into a loud FAIL. Fails closed by construction; measured zero malformed rows today. |
| T-2ig-06 | Tampering | package-manager install pulling a hostile package | n/a | accept | No install this round. Zero new dependencies; node builtins only. No legitimacy checkpoint needed. |
| T-2ig-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan, so the legitimacy gate does not apply. If one becomes necessary, STOP and escalate -- it needs a blocking human checkpoint. |
</threat_model>

<verification>
- Task 1 leaves `plugins/lz-tdd/**` byte-unchanged and the checker RED by design, and the RED-BASELINE
  records for every new or changed guard exactly ONE of the two proof kinds with its evidence --
  RED-at-baseline with a `file:line`, or invariant GREEN-at-baseline with its fixture-set assertions.
  A universal baseline FAIL is NOT required and must not be manufactured.
- Every strengthened guard has a committed fixture pair plus a row asserting the OLD needle passed
  the same evasion, plus an empty-text anti-vacuity control.
- All six measured row-scoped defects are closed, not two.
- Every stated count re-derives from a list on the page (or from the axes, with the per-axis value
  count itself derived rather than hardcoded) and is machine-asserted at its PINNED POST-TASK-2 site
  count, including the count restated in the dependent.
- The emitted-check total equals the literal PREDICTED from the inventory (172), and the emitted label
  set contains every new label and none of the six retired ones.
- No wording anywhere asserts a cell is empty, in any form.
- All three copies share one sha256; no copy carries a skill-relative reference.
- The full battery exits 0 and no guard was widened, softened or deleted to get there.
</verification>

<success_criteria>
- `npm --prefix .claude/skills/lz-red-workspace run check` exits 0 with the chained selftests.
- `npm --prefix .claude/skills/lz-red-workspace run typecheck` exits 0.
- `node .claude/skills/lz-refactor-workspace/tools/check-hygiene.mjs` exits 0.
- `claude plugin validate .` exits 0.
- Task 1's verify printed `TASK1_OK` and Task 2's printed `TASK2_OK`.
- `260729-2ig-RED-BASELINE.md` assigns every new or changed guard exactly one of the two proof kinds
  and carries that kind's evidence, and every strengthened guard catches its specific evasion.
- `260729-2ig-SUMMARY.md` carries the three-directions audit (including the count-guard-times-deletion
  rows), the count re-derivation table with pre/post site counts, the predicted-versus-measured
  emitted-check arithmetic, the dependent sweep, the five-kinds arithmetic stated as 8 rewritten plus
  1 deleted, both UNRESOLVED items unclosed, and an explicit statement that the independent unprimed
  review is the acceptance gate and has not run.
</success_criteria>

<output>
Create `.planning/quick/260729-2ig-remediate-the-test-double-taxonomy-after/260729-2ig-SUMMARY.md`
when done. Also create `260729-2ig-RED-BASELINE.md` at the end of Task 1, before any content edit.
</output>
