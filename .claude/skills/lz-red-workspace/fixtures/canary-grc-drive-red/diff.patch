diff --git a/TypeScript/app/gilded-rose.ts b/TypeScript/app/gilded-rose.ts
index db58d67..e731a06 100644
--- a/TypeScript/app/gilded-rose.ts
+++ b/TypeScript/app/gilded-rose.ts
@@ -51,6 +51,9 @@ export class GildedRose {
             if (this.items[i].quality > 0) {
               if (this.items[i].name != 'Sulfuras, Hand of Ragnaros') {
                 this.items[i].quality = this.items[i].quality - 1
+                if (this.items[i].name == 'Conjured Mana Cake' && this.items[i].quality > 0) {
+                  this.items[i].quality = this.items[i].quality - 1
+                }
               }
             }
           } else {
diff --git a/TypeScript/test/vitest/canary-drive-conjured.spec.ts b/TypeScript/test/vitest/canary-drive-conjured.spec.ts
new file mode 100644
index 0000000..5c9b1d2
--- /dev/null
+++ b/TypeScript/test/vitest/canary-drive-conjured.spec.ts
@@ -0,0 +1,9 @@
+import { Item, GildedRose } from '@/gilded-rose';
+
+describe('Conjured items', () => {
+  it('loses two quality per day before the sell-by date', () => {
+    const app = new GildedRose([new Item('Conjured Mana Cake', 3, 6)]);
+
+    expect(app.updateQuality()[0].quality).toBe(4);
+  });
+});
